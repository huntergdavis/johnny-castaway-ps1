# PS1 Performance Experiment Log

> 🌐 **Rendered version:** **[/docs/performance/](https://hunterdavis.com/johnny-castaway-ps1/docs/performance/)** — this doc rendered on the project website with cross-links and prose context. The GitHub copy here is the source.


Date started: 2026-04-25

Purpose: tracked, searchable ledger for PS1 scene-playback performance
experiments. Scratch JSONL logs remain useful locally, but this file records the
decision history that should survive cleanup, branch rebases, and later retry
passes.

Original comparison baseline for this experiment series:

| Field | Value |
|---|---|
| Branch | `ps1-perf-prefetch-experiments` |
| Commit | `4ddc89f4 ps1: clean up headless perf containers` |
| Run ID | `20260425-143847` |
| Scene | `fishing1` |
| Boot | `fgpilot fishing1 perf-log noloop seed 1` |
| Policy | `stage1_window` |
| Window | `32 KB default` |
| `loop_vb` | `1426` |
| `target_vb` | `1077` |
| `overrun_vb` | `349` |
| `blocking_vb` | `148` |
| `loop_reads` | `29` |
| `prefetch_hits` | `150` |
| `prefetch_due_misses` | `5` |
| `prefetch_overrun_vb` | `104` |
| Correctness | `trip=0 fallback=0 frame_mismatch=0 sound_late=0 cd_fail=0 full_fallbacks=0` |

Current accepted baseline for the next experiment:

| Field | Value |
|---|---|
| Commit | `ps1: honor final foreground frame hold` |
| Run ID | `20260501-074038-1796631` plus focused VISITOR3 high pass `20260501-075742-1897496` |
| Scene | `11-case final-frame-hold canary set` |
| Boot | `fgpilot fishing1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1` |
| Policy | `stage1_window` |
| Window | `Current FGP3/FG2 policy set plus captured final-frame hold semantics; no artificial 150-VBlank scene-pack tail` |
| Added in latest baseline | `Scene-pack playback now honors the captured final frame hold before ending the scene. FISHING1 high is under target; the 11 refreshed rows save 1455 active-loop VBlanks total.` |
| `loop_vb` | `1067` |
| `target_vb` | `1074` |
| `overrun_vb` | `0` |
| `blocking_vb` | `2` |
| `loop_reads` | `20` |
| `prefetch_hits` | `155` |
| `prefetch_due_misses` | `0` |
| `prefetch_overrun_vb` | `2` |
| `restore_bytes` | `251144` |
| `upload_bytes` | `8643840` |
| `dirty_rows` | `13506` |
| `upload_rects` | `439` |
| PS1 EXE size | `169984` |
| PS1 ELF size | `796312` |
| Correctness | `trip=0 fallback=0 frame_mismatch=0 sound_late=0 cd_fail=0` |

## Experiments

| Date | ID | Commit | Hypothesis | Command | Result | Decision |
|---|---|---|---|---|---|---|
| 2026-04-25 | `fg2-window-40kb` | `4ddc89f4` | A `40 KB` FG2 stream window might reduce due misses relative to the accepted `32 KB` default without reintroducing refill overrun. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 40960" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-143847/summary.json --require-improvement` | Failed before `JCPERF2`; headless regtest exited `137`, gate failure `missing_jcperf2`. No usable scene metrics. Artifacts: `scratch/ps1-perf-iterate/20260425-144406/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-144406/fishing1/perf-summary.json`. | Do not promote. Keep as a retry candidate after headless stability work or a lower-memory/dual-window design. |
| 2026-04-25 | `fg2-window-56kb` | `1fe1ceda` | A `56 KB` FG2 stream window might reduce reads and due misses enough to beat the `32 KB` default while staying below the failed `64 KB` pressure point. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 57344" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-143847/summary.json --require-improvement` | Completed with correct pixels/sound counters but failed the improvement gate. Baseline to current: `loop_vb 1426 -> 1442`, `blocking_vb 148 -> 170`, `prefetch_overrun_vb 104 -> 140`, `loop_reads 29 -> 16`, `due_misses 5 -> 2`. Artifacts: `scratch/ps1-perf-iterate/20260425-144631/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-144631/fishing1/perf-summary.json`. | Do not promote. Larger windows reduce transaction count but overrun held slack more often; prefer smarter refill timing/layout over simply increasing the buffer. |
| 2026-04-25 | `fg2-no-stage1-isolation` | `816b7c28` | Disabling the one-entry stage buffer might show whether stage-copy overhead or stage/window interaction is hurting the current default. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 no-stage1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-143847/summary.json --require-improvement` | Failed before `JCPERF2`; headless regtest exited `137`, gate failure `missing_jcperf2`. No usable scene metrics. Artifacts: `scratch/ps1-perf-iterate/20260425-144914/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-144914/fishing1/perf-summary.json`. | Do not promote and do not infer that staging is slow. Keep staging enabled until a clean isolation run says otherwise. |
| 2026-04-25 | `fg2-partial-tail-stage` | `1413e813` | When a staged frame straddles the current stream-window end, copy the resident prefix from the window and read only the missing tail into the stage buffer instead of refilling another overlapping full window. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-143847/summary.json --require-improvement` | Failed. The scene emitted valid metrics but the outer headless process exited `137`; metrics also regressed. Baseline to current: `loop_vb 1426 -> 1446`, `blocking_vb 148 -> 178`, `loop_reads 29 -> 37`, `due_misses 5 -> 9`, while `partial_hits=10` and `prefetch_overrun_vb 104 -> 98`. Artifacts: `scratch/ps1-perf-iterate/20260425-145256/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-145256/fishing1/perf-summary.json`. | Do not promote. Smaller tail reads increase CD transaction count and due misses; avoid partial-frame reads unless grouped with a broader sequential-read design. |
| 2026-04-25 | `fg2-stage-copy-fallthrough` | `93a91a6a` | If `fgRuntimeTryStageNextFrame()` only copies the next frame from an already-resident window, use the same held iteration to prefetch farther ahead instead of waiting immediately. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-143847/summary.json --require-improvement` | Failed before `JCPERF2`; headless regtest exited `137`, gate failure `missing_jcperf2`. The log reached `JCPERF scene-start` and repeated FG2 directory lookups but emitted no scene-end metrics. Artifacts: `scratch/ps1-perf-iterate/20260425-145649/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-145649/fishing1/perf-summary.json`. | Do not promote. The idea may be valid, but the naive same-iteration fallthrough appears to destabilize CD/window sequencing; revisit only with an explicit prefetch state machine. |
| 2026-04-25 | `x-aware-clean-restore` | `3cbbcae1` | Track dirty X extents per tile and use them only for RAM clean-background restore, leaving the existing full-row upload path unchanged. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-143847/summary.json --allow-regression 10 --require-improvement` | Promoted. Repeat run passed with correctness clean. Baseline to current: `loop_vb 1426 -> 1366`, `overrun_vb 349 -> 289`, `blocking_vb 148 -> 125`, `restore_bytes 16035840 -> 9580428`, `due_misses 5 -> 1`; `prefetch_overrun_vb` regressed `104 -> 113` but total playback improved materially. Artifacts: `scratch/ps1-perf-iterate/20260425-150236/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-150236/fishing1/perf-summary.json`. | Promote. This is a real render-side speedup; next pass should tackle X-aware upload so `upload_bytes` stops dominating. |
| 2026-04-25 | `fg2-pal4-opaque-span-compositor` | `8d8c7095` | FG2 PAL4 spans contain only visible pixels, so the runtime can remove the per-pixel transparent-index branch and split each span by destination tile once instead of selecting tiles per pixel. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-151313/summary.json --allow-regression 2 --require-improvement` | Promoted. Two clean passes showed identical speedup; one intervening headless run hit the known pre-`JCPERF2` wrapper/container `137` failure and was rejected by the harness. Baseline to current: `loop_vb 1366 -> 1335`, `overrun_vb 289 -> 258`, `blocking_vb 125 -> 106`, `prefetch_overrun_vb 113 -> 94`. Final VRAM hash matched baseline: `330784fc30046503d5f273b28c80abe8e584d40fc7dca3ff1771e59aa57cfbb0`. Artifacts: `scratch/ps1-perf-iterate/20260425-151745/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-152021/fishing1/perf-summary.json`. | Promote. This is a compositor hot-path win with no pack format change; next candidates remain exact-X upload and smarter CD grouping. |
| 2026-04-25 | `prefetch-duplicate-probe-skip` | `7858b725` | Once the next entry is already staged, the held loop should only call window prefetch if it would issue a read; otherwise the duplicate stage/window probes are pure CPU noise. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-152021/summary.json --allow-regression 2` | Promoted as a micro-cleanup. Timing stayed flat: `loop_vb 1335 -> 1335`, `blocking_vb 106 -> 106`, `prefetch_overrun_vb 94 -> 94`; duplicate no-op probes dropped `887 -> 0`. VRAM and SPU hashes matched baseline. Artifact: `scratch/ps1-perf-iterate/20260425-152635/fishing1/perf-summary.json`. | Promote. Not a visible-speed win, but it removes redundant held-loop work and makes future prefetch metrics cleaner. |
| 2026-04-25 | `prefetch-min-slack-6vb` | `17da88d1` | Reject stream-window refills unless at least `6` held VBlanks remain, so short-slack CD reads cannot overrun into visible playback. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-152635/summary.json --allow-regression 2 --require-improvement` | Failed. It did reduce prefetch overrun (`94 -> 24`), but made due misses and blocking worse: `loop_vb 1335 -> 1341`, `overrun_vb 258 -> 264`, `blocking_vb 106 -> 139`, `due_misses 1 -> 14`, `skipped_no_slack 0 -> 79`. Artifact: `scratch/ps1-perf-iterate/20260425-153142/fishing1/perf-summary.json`. | Do not promote. The threshold is too strict; it proves that avoiding prefetch overrun is not enough if the due-frame path has to block more often. |
| 2026-04-25 | `prefetch-min-slack-3vb` | `17da88d1` | A smaller stream-window refill threshold should avoid the worst short-slack overrun without starving near-term frames. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-152635/summary.json --allow-regression 2 --require-improvement` | Promoted. Two clean passes matched exactly. Baseline to current: `loop_vb 1335 -> 1325`, `overrun_vb 258 -> 248`, `blocking_vb 106 -> 106`, `prefetch_overrun_vb 94 -> 67`, `skipped_no_slack 0 -> 11`; due misses rose `1 -> 4` but did not increase blocking time. Artifacts: `scratch/ps1-perf-iterate/20260425-153424/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-153546/fishing1/perf-summary.json`. | Promote. This is a small but repeatable CD timing win; next CD work should reduce read cost or grouping rather than raising the threshold. |
| 2026-04-25 | `prefetch-short-slack-direct-stage` | `effbe295` | If a window refill is rejected with only `1-2` held VBlanks left, directly stage the next small entry instead of skipping all prefetch work. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-153546/summary.json --allow-regression 2 --require-improvement` | Failed. One run died before `JCPERF2`; the next emitted full metrics but regressed: `loop_vb 1325 -> 1332`, `overrun_vb 248 -> 255`, `blocking_vb 106 -> 112`, `prefetch_overrun_vb 67 -> 82`, while due misses improved only `4 -> 3`. Reads rose `37 -> 42`. Artifact: `scratch/ps1-perf-iterate/20260425-154130/fishing1/perf-summary.json`. | Do not promote. More small direct reads are not free; avoid increasing CD transaction count unless pack layout or grouping also changes. |
| 2026-04-25 | `holiday-overlap-restamp` | `effbe295` | Seed holiday decoration into the clean backdrop and restamp it only when the current FG2 frame overlaps, reducing repeated holiday dirty/upload work. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-153546/summary.json --allow-regression 2 --require-improvement` | Failed as a no-op. Correctness stayed clean, but all loop and gfx counters were unchanged: `loop_vb 1325 -> 1325`, `blocking_vb 106 -> 106`, `prefetch_overrun_vb 67 -> 67`, `restore_bytes 9580428 -> 9580428`, `upload_bytes 17277440 -> 17277440`. Artifact: `scratch/ps1-perf-iterate/20260425-154928/fishing1/perf-summary.json`. | Do not promote. Fishing1's active frames overlap the Christmas decoration enough that this does not reduce dirty work; revisit only for scenes/holidays where overlap is sparse. |
| 2026-04-25 | `fg2-skip-clear-otag` | `e18dfd48` | Base-diff FG2 playback uses RAM tile compositing, so skip `grBeginFrame()`/`ClearOTagR()` on rendered entries and avoid clearing an unused primitive buffer. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-153546/summary.json --allow-regression 2 --require-improvement` | Failed as unmeasured. Correctness stayed clean, but VBlank-level metrics were unchanged: `loop_vb 1325 -> 1325`, `overrun_vb 248 -> 248`, `blocking_vb 106 -> 106`, `prefetch_overrun_vb 67 -> 67`. Artifact: `scratch/ps1-perf-iterate/20260425-155223/fishing1/perf-summary.json`. | Do not promote on this pass. It may save sub-VBlank CPU, but current metrics cannot prove it; retry only with a CPU-counter or instruction-budget metric. |
| 2026-04-25 | `fg2-window-24kb-post-slack` | `38d2fede` | After the `3` VBlank refill guard, a smaller `24 KB` stream window may reduce visible refill overrun enough to beat the `32 KB` default. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 24576" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-153546/summary.json --allow-regression 2 --require-improvement` | Promoted. One pre-metrics `137` run was inconclusive; the clean parameterized run and then the default-code run matched exactly. Baseline to current: `loop_vb 1325 -> 1322`, `overrun_vb 248 -> 245`, `blocking_vb 106 -> 108`, `prefetch_overrun_vb 67 -> 58`, `due_misses 4 -> 7`, `loop_reads 29 -> 41`. Artifacts: `scratch/ps1-perf-iterate/20260425-155533/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-155947/fishing1/perf-summary.json`. | Promote as the new default despite the small blocking increase because the headless gate passed and total playback improved repeatably; keep watching `blocking_vb` in broader scene validation. |
| 2026-04-25 | `fg2-window-28kb-post-slack` | `38d2fede` | A `28 KB` stream window might keep some `24 KB` overrun savings while avoiding its extra due misses. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 28672" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-153546/summary.json --allow-regression 2 --require-improvement` | Failed. It regressed every key metric: `loop_vb 1325 -> 1345`, `overrun_vb 248 -> 268`, `blocking_vb 106 -> 128`, `prefetch_overrun_vb 67 -> 88`. Artifact: `scratch/ps1-perf-iterate/20260425-155656/fishing1/perf-summary.json`. | Do not promote. The current post-guard knee is below `28 KB`, at least for fishing1 high tide. |
| 2026-04-25 | `fg2-window-20kb-post-slack` | `38d2fede` | A `20 KB` stream window may cut refill overrun further than `24 KB`. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 20480" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-153546/summary.json --allow-regression 2 --require-improvement` | Failed. It matched the `24 KB` loop gain but violated the blocking gate: `loop_vb 1325 -> 1322`, `blocking_vb 106 -> 119`, `prefetch_overrun_vb 67 -> 51`, `due_misses 4 -> 10`. Artifact: `scratch/ps1-perf-iterate/20260425-155819/fishing1/perf-summary.json`. | Do not promote. The smaller read size cuts overrun but starves too many entries. |
| 2026-04-25 | `fg2-window-22kb-post-slack` | `84088b6b` | A `22 KB` window might beat the `24 KB` default by cutting refill overrun while staying inside the blocking guard. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 22528" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-155947/summary.json --allow-regression 2 --require-improvement` | Failed. It improved total loop but exceeded the blocking guard: `loop_vb 1322 -> 1320`, `overrun_vb 245 -> 243`, `blocking_vb 108 -> 114`, `prefetch_overrun_vb 58 -> 42`, `due_misses 7 -> 11`. Artifact: `scratch/ps1-perf-iterate/20260425-160318/fishing1/perf-summary.json`. | Do not promote. This confirms smaller windows trade refill overrun for too many due-frame misses. |
| 2026-04-25 | `fg2-window-26kb-post-slack` | `84088b6b` | A `26 KB` window might reduce the `24 KB` due misses without regaining too much refill overrun. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 26624" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-155947/summary.json --allow-regression 2 --require-improvement` | Failed. It regressed every key timing metric: `loop_vb 1322 -> 1337`, `overrun_vb 245 -> 260`, `blocking_vb 108 -> 129`, `prefetch_overrun_vb 58 -> 61`, `due_misses 7 -> 9`. Artifact: `scratch/ps1-perf-iterate/20260425-160441/fishing1/perf-summary.json`. | Do not promote. The `24 KB` point remains better than nearby larger windows. |
| 2026-04-25 | `fg2-window-23kb-post-slack` | `84088b6b` | A `23 KB` window might retain the `24 KB` blocking behavior while capturing some `22 KB` overrun savings. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 23552" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-155947/summary.json --allow-regression 2 --require-improvement` | Failed as a no-op. It matched the `24 KB` default exactly: `loop_vb 1322`, `blocking_vb 108`, `prefetch_overrun_vb 58`, `due_misses 7`. Artifact: `scratch/ps1-perf-iterate/20260425-160603/fishing1/perf-summary.json`. | Do not promote. No measurable advantage over the accepted `24 KB` default. |
| 2026-04-25 | `fg2-window-forward-extend` | `f7eedd2a` | When the next FG2 entry straddles the current stream-window end, preserve the resident suffix and append-read only the missing aligned tail instead of refilling an overlapping full window. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-155947/summary.json --allow-regression 2 --require-improvement` | Promoted. Two clean passes matched exactly after one infrastructure-only `137` retry. Baseline to current: `loop_vb 1322 -> 1305`, `overrun_vb 245 -> 228`, `blocking_vb 108 -> 84`, `prefetch_overrun_vb 58 -> 37`, `loop_read_vb 274 -> 236`, `due_misses 7 -> 7`. Artifacts: `scratch/ps1-perf-iterate/20260425-161258/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-161536/fishing1/perf-summary.json`. | Promote. This keeps the 24 KB memory budget but removes redundant overlap-read cost; future CD work should now target the remaining due misses and pack layout. |
| 2026-04-25 | `fg2-window-minimal-forward-extend` | `f4c2cc5d` | Shift the stream window only far enough to contain a straddling entry, reducing append bytes versus the promoted forward-extension strategy. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-161536/summary.json --allow-regression 2 --require-improvement` | Failed. It reduced refill overrun but starved future coverage: `loop_vb 1305 -> 1374`, `overrun_vb 228 -> 297`, `blocking_vb 84 -> 218`, `prefetch_overrun_vb 37 -> 8`, `due_misses 7 -> 69`, `hits 148 -> 86`. Artifact: `scratch/ps1-perf-iterate/20260425-161956/fishing1/perf-summary.json`. | Do not promote. A window must preserve enough future coverage; minimizing each append read locally creates many due-frame misses. |
| 2026-04-25 | `fg2-prefetch-min-slack-2vb-post-extend` | `294b1102` | With forward extension shortening many refill reads, a `2` VBlank slack gate might prefetch more near-term frames without the old overrun cost. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-161536/summary.json --allow-regression 2 --require-improvement` | Failed. One infrastructure-only `137` run emitted no `JCPERF2`; the full run traded due misses for overrun without a loop win: `loop_vb 1305 -> 1305`, `blocking_vb 84 -> 72`, `prefetch_overrun_vb 37 -> 63`, `due_misses 7 -> 1`, `hits 148 -> 154`. Artifact: `scratch/ps1-perf-iterate/20260425-162328/fishing1/perf-summary.json`. | Do not promote now. Retry after another change lowers refill overrun, because the reduced due misses may become valuable once short-slack reads are cheaper. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-unguarded` | `19aa4c40` | After a zero-VBlank stage copy from the stream window, immediately prefetch the following window in the same held iteration to reduce due misses. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-161536/summary.json --allow-regression 2 --require-improvement` | Failed gate but informed the promoted guard. It improved total loop and blocking, but regressed refill overrun: `loop_vb 1305 -> 1303`, `blocking_vb 84 -> 64`, `prefetch_overrun_vb 37 -> 51`, `due_misses 7 -> 2`. Artifact: `scratch/ps1-perf-iterate/20260425-162601/fishing1/perf-summary.json`. | Do not promote unguarded. The result proves the idea has value, but the second read needs more slack than the normal window gate. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-4vb` | `19aa4c40` | Keep the zero-VBlank stage-copy fallthrough, but only run the second window prefetch when at least `4` held VBlanks remain. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-161536/summary.json --allow-regression 2 --require-improvement` | Promoted. Two clean passes matched exactly after one infrastructure-only `137` retry. Baseline to current: `loop_vb 1305 -> 1299`, `overrun_vb 228 -> 222`, `blocking_vb 84 -> 76`, `prefetch_overrun_vb 37 -> 37`, `due_misses 7 -> 6`, `hidden_vb 152 -> 181`. Artifacts: `scratch/ps1-perf-iterate/20260425-162803/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-162926/fishing1/perf-summary.json`. | Promote. The extra guarded read converts idle held time into hidden CD work without increasing measured refill overrun. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-5vb` | `be567f27` | Raise the stage-copy fallthrough guard from `4` to `5` VBlanks to see whether it preserves the loop win with less refill overrun. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-162926/summary.json --allow-regression 2 --require-improvement` | Promoted. Two clean passes matched exactly. Baseline to current: `loop_vb 1299 -> 1297`, `overrun_vb 222 -> 220`, `blocking_vb 76 -> 76`, `prefetch_overrun_vb 37 -> 31`, `due_misses 6 -> 7`, `hits 149 -> 148`. Artifacts: `scratch/ps1-perf-iterate/20260425-163201/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-163323/fishing1/perf-summary.json`. | Promote over the `4` VBlank guard. The stricter guard slightly improves total loop and lowers refill overrun without increasing blocking. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-6vb` | `b80960f8` | Raise the stage-copy fallthrough guard from `5` to `6` VBlanks to further reduce overrun risk. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed. It gave back the `5` VBlank gain and regressed every key metric slightly: `loop_vb 1297 -> 1299`, `overrun_vb 220 -> 222`, `blocking_vb 76 -> 78`, `prefetch_overrun_vb 31 -> 33`. Artifact: `scratch/ps1-perf-iterate/20260425-163525/fishing1/perf-summary.json`. | Do not promote. `5` VBlanks is the better local point for fishing1 under the current pack layout. |
| 2026-04-25 | `fg2-window-28kb-post-fallthrough` | `c0aad256` | Re-test a larger `28 KB` stream window after forward extension and staged fallthrough changed the CD cost model. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 28672" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed. It reduced due misses but regressed overrun and loop time: `loop_vb 1297 -> 1304`, `blocking_vb 76 -> 79`, `prefetch_overrun_vb 31 -> 52`, `due_misses 7 -> 4`. Artifact: `scratch/ps1-perf-iterate/20260425-163818/fishing1/perf-summary.json`. | Do not promote. Larger windows still overrun held slack too much even after extension. |
| 2026-04-25 | `fg2-window-20kb-post-fallthrough` | `c0aad256` | Re-test a smaller `20 KB` stream window after staged fallthrough; it may lower total loop by shortening refill reads. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 20480" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed gate. It improved total loop but regressed blocking: `loop_vb 1297 -> 1292`, `overrun_vb 220 -> 215`, `blocking_vb 76 -> 82`, `prefetch_overrun_vb 31 -> 31`, `due_misses 7 -> 10`. Artifact: `scratch/ps1-perf-iterate/20260425-163939/fishing1/perf-summary.json`. | Do not promote under current gates. Keep as a retry candidate if later due-miss hiding reduces the blocking penalty. |
| 2026-04-25 | `fg2-window-21kb-22kb-post-fallthrough` | `c0aad256` | Check nearby smaller windows to see whether they keep the `20 KB` loop gain while staying inside the blocking guard. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 21504" ...` and `... prefetch-window 22528` | Failed. Both behaved the same: `loop_vb 1297 -> 1296`, `blocking_vb 76 -> 93`, `prefetch_overrun_vb 31 -> 26`, `due_misses 7 -> 11`. Artifacts: `scratch/ps1-perf-iterate/20260425-164341/fishing1/perf-summary.json`, `scratch/ps1-perf-iterate/20260425-164100/fishing1/perf-summary.json`. | Do not promote. The due-miss/blocking cost is worse than `20 KB` despite slightly lower refill overrun. |
| 2026-04-25 | `fg2-window-23kb-post-fallthrough` | `c0aad256` | Check whether `23 KB` becomes a better local point than the accepted `24 KB` after staged fallthrough. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 23552" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed as a no-op: `loop_vb 1297`, `blocking_vb 76`, `prefetch_overrun_vb 31`, `due_misses 7`. Artifact: `scratch/ps1-perf-iterate/20260425-164220/fishing1/perf-summary.json`. | Do not promote. No measurable advantage over `24 KB`. |
| 2026-04-25 | `fg2-window-20kb-min-slack-2vb` | `c0aad256` | Pair the `20 KB` window with a `2` VBlank main prefetch gate to recover due misses while keeping short refill reads. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 20480" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed gate. It reduced blocking but regressed prefetch overrun for only a tiny loop win: `loop_vb 1297 -> 1296`, `blocking_vb 76 -> 62`, `prefetch_overrun_vb 31 -> 53`, `due_misses 7 -> 2`. Artifact: `scratch/ps1-perf-iterate/20260425-164517/fishing1/perf-summary.json`. | Do not promote now. The combination may become useful only if short-slack overrun can be reduced independently. |
| 2026-04-25 | `fg2-compose-before-vsync` | `407a0ab4` | Move FG2 RAM composition before the VBlank wait so CPU compose work can happen during previous-frame scanout, then upload after VBlank. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed. It reduced prefetch overrun but stole useful held-prefetch time: `loop_vb 1297 -> 1309`, `overrun_vb 220 -> 232`, `blocking_vb 76 -> 100`, `prefetch_overrun_vb 31 -> 14`, `due_misses 7 -> 14`. Artifact: `scratch/ps1-perf-iterate/20260425-164826/fishing1/perf-summary.json`. | Do not promote. In this runtime, the current post-VBlank compose preserves more prefetch opportunity than the attempted render overlap. |
| 2026-04-25 | `fg2-tight-slack-20kb-window` | `9def720f` | Keep the normal `24 KB` window for most refills but use a `20 KB` window for the tightest allowed prefetches, reducing overrun without globally sacrificing future coverage. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed. One infrastructure-only run exited `255` before boot metrics; the real run regressed badly: `loop_vb 1297 -> 1363`, `overrun_vb 220 -> 286`, `blocking_vb 76 -> 100`, `prefetch_overrun_vb 31 -> 31`, `due_misses 7 -> 11`. Artifacts: `scratch/ps1-perf-iterate/20260425-165118/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260425-165200/fishing1/perf-summary.json`. | Do not promote. Mixing window sizes destabilizes future coverage more than it helps tight-slack refill cost. |
| 2026-04-25 | `x-aware-upload-rect-scratch` | dirty `a70fed61` | Use the existing tile-level dirty X extents to upload only the dirty rectangle width per tile, packing partial-width rows through one bounded static scratch buffer. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/20260425-163323/summary.json --allow-regression 2 --require-improvement` | Failed structurally. The build booted and reached `JCPERF scene-start`, but the scene did not reach `JCPERF2` scene-end before the `4200`-frame headless window ended. DuckStation exited successfully; the log also emitted `psxgpu: transfer data length ... is not a multiple of 16, rounding` during the partial uploads. Artifact: `scratch/ps1-perf-iterate/20260425-170443/fishing1/perf-summary.json`. | Do not promote. Naive partial-width upload with per-partial scratch/`DrawSync` is too slow or unsafe to measure as-is. Retry only with 16-pixel-aligned batching, fewer syncs, or a per-row/strip plan that preserves one final `DrawSync`. |
| 2026-04-25 | `pad-spi-diagnostics-gated-default-off` | `1337691d` | Pause support introduced always-on JCPAD/JCSPI diagnostic sampling and prints; normal screensaver playback should poll Start but leave deep controller diagnostics opt-in. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 20 --require-improvement` | Promoted after a strict-gate red-team pass documented the tradeoff. Baseline to current: `loop_vb 1369 -> 1317`, `overrun_vb 292 -> 240`, `blocking_vb 88 -> 93`, `prefetch_overrun_vb 35 -> 41`, `due_misses 8 -> 8`; correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-171354/summary.json`, `scratch/ps1-perf-iterate/20260425-171533/summary.json`. | Promote. A `52` VBlank loop win is material, and default runtime should not carry diagnostic-only controller/SPI probes; keep `pad-diag` / `pad-debug` for controller debugging. |
| 2026-04-25 | `fg2-window-20kb-post-diagnostics` | `197f18c6` | After removing default pad/SPI diagnostic overhead, re-test the smaller `20 KB` stream window; shorter refill reads may now beat the `24 KB` coverage tradeoff. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 20480" ...` then default `./scripts/ps1-perf-iterate.sh --scene fishing1 ...` | Promoted. Parameter and default-code runs matched. Baseline to current: `loop_vb 1317 -> 1312`, `overrun_vb 240 -> 235`, `blocking_vb 93 -> 91`, `prefetch_overrun_vb 41 -> 37`; due misses rose `8 -> 11`, but total playback and CD stall both improved. Artifacts: `scratch/ps1-perf-iterate/20260425-172026/summary.json`, `scratch/ps1-perf-iterate/20260425-172201/summary.json`. | Promote as the new default. The smaller window is now a net win under the post-diagnostics baseline; continue watching due misses in longer scenes. |
| 2026-04-25 | `fg2-prefetch-min-slack-2vb-post-20kb` | `8ef9aad0` | With a `20 KB` window, a `2` VBlank refill guard may recover due misses and blocking without the earlier short-slack overrun cost. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Promoted after a strict `20%` pass failed only on `prefetch_overrun_vb` by `0.6` VBlank. Baseline to current: `loop_vb 1312 -> 1300`, `overrun_vb 235 -> 223`, `blocking_vb 91 -> 66`, `due_misses 11 -> 4`, `hits 144 -> 151`, `prefetch_overrun_vb 37 -> 45`; correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-172603/summary.json`, `scratch/ps1-perf-iterate/20260425-172725/summary.json`. | Promote. The overrun tradeoff is bounded and buys a material `12` VBlank loop win plus a `25` VBlank blocking reduction. |
| 2026-04-25 | `fg2-prefetch-min-slack-1vb-post-20kb` | dirty experiment after `8ef9aad0` | Test whether a `1` VBlank refill guard recovers the remaining due misses now that the default stream window is only `20 KB`. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. Baseline to current: `loop_vb 1300 -> 1312`, `overrun_vb 223 -> 235`, `blocking_vb 66 -> 70`, `prefetch_overrun_vb 45 -> 62`, `due_misses 4 -> 2`, `hits 151 -> 153`. Artifact: `scratch/ps1-perf-iterate/20260425-173020/summary.json`. | Do not promote. It buys only two fewer due misses while making the refill-overrun and total-loop cost visible; keep the `2` VBlank guard as the accepted default. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-4vb-post-20kb` | dirty experiment after `91c0a143` | Re-test the staged-copy fallthrough guard at `4` VBlanks under the post-merge `20 KB` window and `2` VBlank refill guard. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed as a no-op. Baseline to current: `loop_vb 1300 -> 1300`, `overrun_vb 223 -> 223`, `blocking_vb 66 -> 66`, `prefetch_overrun_vb 45 -> 45`, `due_misses 4 -> 4`, `hits 151 -> 151`. Artifact: `scratch/ps1-perf-iterate/20260425-173331/summary.json`. | Do not promote. The new baseline is insensitive to this guard at `4` vs `5`; leave the stricter accepted value in place. |
| 2026-04-25 | `fg2-window-18kb-post-2vb` | `272d2e0d` | Re-sweep smaller sector-rounded window buckets after the `20 KB` window and `2` VBlank refill guard made refill overrun the remaining CD cost. | Parameter probes for `18432`, `19456`, `17408`, and `16384`, then default `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Promoted. The `17 KB` and `18 KB` requests both round to an `18 KB` window and match; `19 KB` rounds to the old `20 KB` no-op; `16 KB` does not improve loop time. Accepted baseline to current: `loop_vb 1300 -> 1296`, `overrun_vb 223 -> 219`, `prefetch_overrun_vb 45 -> 33`, `blocking_vb 66 -> 75`, `due_misses 4 -> 8`, `hits 151 -> 147`; correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-173620/summary.json`, `scratch/ps1-perf-iterate/20260425-173746/summary.json`, `scratch/ps1-perf-iterate/20260425-174033/summary.json`, `scratch/ps1-perf-iterate/20260425-174206/summary.json`. | Promote as the new default. This is a small but real total-loop win and cuts refill overrun; the extra due misses/blocking are documented as the next CD target. |
| 2026-04-25 | `spi-poll-rate-125hz-65hz` | dirty experiment after `cd3db22f` | Reducing the continuous SPI pad poll rate might lower IRQ overhead while preserving frame-level pause responsiveness. | Build with `SPI_SetPollRate(125)`, then build with `SPI_SetPollRate(65)`, each run via `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed for total playback. `125 Hz`: `loop_vb 1296 -> 1297`, `blocking_vb 75 -> 73`, `prefetch_overrun_vb 33 -> 32`. `65 Hz`: `loop_vb 1296 -> 1297`, `blocking_vb 75 -> 72`, `prefetch_overrun_vb 33 -> 31`. Correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-174653/summary.json`, `scratch/ps1-perf-iterate/20260425-174830/summary.json`. | Do not promote. It improves CD submetrics but loses one VBlank overall and would need manual input-latency validation; keep the known-good `250 Hz` poll rate. |
| 2026-04-25 | `row-level-x-dirty-restore` | `26890fbb` | The earlier x-aware restore still used one X min/max rectangle across every dirty row; tracking X extents per dirty tile row should restore fewer RAM pixels while leaving conservative full-row VRAM upload unchanged. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Promoted. Baseline to current: `loop_vb 1296 -> 1266`, `overrun_vb 219 -> 189`, `blocking_vb 75 -> 36`, `prefetch_overrun_vb 33 -> 26`, `due_misses 8 -> 1`, `hits 147 -> 154`, `restore_bytes 9520664 -> 2510092`, `upload_bytes` unchanged at `17172480`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-175722/summary.json`. | Promote. This is a large render-side cleanup that also creates more held-time headroom for CD prefetch; next dirty work should target upload batching rather than restore byte volume. |
| 2026-04-25 | `fg2-window-resweep-post-row-restore` | dirty experiment after `d973a2b6` | Row-level restore changed held-frame headroom enough that the previous FG2 stream-window knee might have moved. | Parameter sweep: `prefetch-window 16384`, `20480`, `24576`, and `32768` via `./scripts/ps1-perf-iterate.sh --case ... --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25` | Failed to beat the accepted `18 KB` default. Baseline `loop_vb=1266`; results: `16 KB -> 1270`, `20 KB -> 1276`, `24 KB -> 1281`, `32 KB -> 1285`. Blocking rose except for `16 KB` (`36 -> 30`), but the total loop still regressed. Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-180226/summary.json`. | Do not promote. Keep `18 KB` as the default under the row-level restore baseline; revisit only after grouped/async refill changes the CD cost model. |
| 2026-04-25 | `dirty-row-targeted-clear` | dirty experiment after `75b1747f` | Clearing only the dirty row-state range from the prior rendered frame might remove per-frame CPU work introduced by row-level dirty tracking. | Build with targeted `currDirtyRow*` clearing in `grClearCurrDirtyState()`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed as a no-op at VBlank resolution. Baseline to current: `loop_vb 1266 -> 1266`, `overrun_vb 189 -> 189`, `blocking_vb 36 -> 36`, `prefetch_overrun_vb 26 -> 26`, `restore_bytes 2510092 -> 2510092`, `upload_bytes 17172480 -> 17172480`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-180908/summary.json`. | Do not promote. It may save sub-VBlank CPU, but current acceptance metrics cannot prove a speedup; retry only with finer CPU counters or as part of broader dirty-state simplification. |
| 2026-04-25 | `fg2-pal4-pair-lut-runtime` | dirty experiment after `8f502aff` | A runtime 256-entry PAL4 pair LUT plus aligned 32-bit stores might reduce compositor CPU versus two palette lookups and two halfword stores per packed byte. | Build with per-palette pair LUT caching in `grCompositePacked4SpansToBackground()`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. Baseline to current: `loop_vb 1266 -> 1279`, `overrun_vb 189 -> 202`, `blocking_vb 36 -> 41`, `prefetch_overrun_vb 26 -> 35`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-181237/summary.json`. | Do not promote. The cache check, extra table load, or store pattern is worse than the simple halfword palette path on this build; retry only with pack-time direct16 commands or finer CPU counters. |
| 2026-04-25 | `x-aware-upload-16px-strip-sync` | dirty experiment after `6dc31725` | Round dirty row X extents to 16-pixel buckets, merge nearby rows into bounded strips, and upload partial-width strips through scratch while preserving full-width rows on the direct tile path. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed structurally. Build passed and the scene reached `JCPERF scene-start`, but no `JCPERF2` scene-end was emitted before the fixed `4200`-frame headless window ended. DuckStation exited successfully, which indicates the new upload path slowed scene completion beyond the measurement window rather than producing an accepted metric sample. Artifact: `scratch/ps1-perf-iterate/20260425-183011/summary.json`. | Do not promote. Per-partial-strip `DrawSync(0)` destroys the expected byte savings; the next upload attempt must use a bounded multi-strip scratch arena or other one-final-sync design before re-testing X-aware upload. |
| 2026-04-25 | `x-aware-upload-32px-arena` | dirty experiment after `54925cf6` | Use 32-pixel X buckets plus a fixed 64 KB scratch arena so partial-width upload strips can share batch syncs instead of syncing each strip. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed with clean correctness but worse timing. Upload bytes improved `17172480 -> 4933312` and exact dirty bytes fell to `3079962`, but upload rects exploded `351 -> 11575`, `upload_vb 5 -> 204`, `loop_vb 1266 -> 1321`, `blocking_vb 36 -> 83`, and `due_misses 1 -> 9`. Artifact: `scratch/ps1-perf-iterate/20260425-183845/summary.json`. | Do not promote. Byte-minimal row strips are too command-heavy; future upload work must cap rect count first, then spend bytes by widening rows/bands deterministically until upload VBlanks stay near baseline. |
| 2026-04-25 | `x-aware-upload-32px-tile-band` | dirty experiment after `3a203ba3` | Cap command count by using one widened 32-pixel X band per dirty tile row range, splitting only when the 64 KB scratch arena requires it. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed with clean correctness. Rect count stayed near baseline (`351 -> 389`) and upload bytes improved (`17172480 -> 11074240`), but scratch packing/copying made `upload_vb 5 -> 305`, `loop_vb 1266 -> 1351`, `blocking_vb 36 -> 113`, and `due_misses 1 -> 16`. Artifact: `scratch/ps1-perf-iterate/20260425-184357/summary.json`. | Do not promote. Runtime partial-width upload requires CPU repacking from strided tiles into contiguous scratch, and that cost is worse than sending full-width rows directly from stable tile memory. Future upload-byte work should be pack-time/direct-layout, not runtime scratch packing. |
| 2026-04-25 | `fg2-prefetch-min-slack-3vb-post-row-restore` | dirty experiment after `2b3c104a` | After row-level restore reduced due misses to one, raising the stream-window refill guard from `2` to `3` VBlanks might cut the remaining short-slack overrun without materially starving the queue. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed the blocking gate with clean correctness. Baseline to current: `loop_vb 1266 -> 1257`, `overrun_vb 189 -> 180`, and `prefetch_overrun_vb 26 -> 10`, but `blocking_vb 36 -> 56`, `due_misses 1 -> 9`, and `hits 154 -> 146`. Artifact: `scratch/ps1-perf-iterate/20260425-184811/summary.json`. | Do not promote. The loop win is real but unsafe under the current acceptance rule: stricter refill gating improves short-slack cost by starving near-term entries. Retry only after grouped/pipelined coverage can preserve due-frame residency. |
| 2026-04-25 | `fg2-window-16kb-3vb-post-row-restore` | `e4dc5dd0` | Combine the lower-blocking `16 KB` post-restore window with the lower-overrun `3` VBlank refill guard; each signal failed alone, but together they may keep due-frame residency inside the gate. | Parameter probe `./scripts/ps1-perf-iterate.sh --case "fishing1-w16-g3::fgpilot fishing1 prefetch-window 16384" ...`, then default `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Promoted. Parameter and default-code runs matched. Baseline to current: `loop_vb 1266 -> 1254`, `overrun_vb 189 -> 177`, `blocking_vb 36 -> 35`, `prefetch_overrun_vb 26 -> 6`, `hidden_vb 225 -> 240`, `loop_reads 56 -> 69`, `due_misses 1 -> 6`, `hits 154 -> 149`; correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-185236/summary.json`, `scratch/ps1-perf-iterate/20260425-185413/summary.json`. | Promote. The combination preserves blocking while cutting refill overrun by `20` VBlanks and total loop by `12`; the extra reads/due misses are an accepted tradeoff for this baseline and remain the next CD target. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-4vb-post-16kb-3vb` | dirty experiment after `bbfe4f2b` | Re-test the staged-copy fallthrough guard at `4` VBlanks under the new `16 KB` window and `3` VBlank refill guard; the earlier no-op might now recover due misses. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Rejected despite harness pass. Correctness stayed clean and blocking improved, but total playback regressed: `loop_vb 1254 -> 1264`, `overrun_vb 177 -> 187`, `blocking_vb 35 -> 31`, `prefetch_overrun_vb 6 -> 7`, `due_misses 6 -> 5`, `hits 149 -> 150`. Artifact: `scratch/ps1-perf-iterate/20260425-185826/summary.json`. | Do not promote. Main speed beats submetric wins; lowering the fallthrough guard buys one fewer due miss but steals enough held-loop time to lose `10` VBlanks overall. Keep the `5` VBlank guard. |
| 2026-04-25 | `fg2-stage-window-2vb-lookahead-3vb` | dirty experiment after `5f8d4248` | Split the refill guard: allow `2` VBlank window reads only for staging the immediate next frame, while keeping lookahead refills at `3` VBlanks. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. It recovered due misses and blocking but lost overall speed: `loop_vb 1254 -> 1268`, `overrun_vb 177 -> 191`, `blocking_vb 35 -> 27`, `prefetch_overrun_vb 6 -> 22`, `due_misses 6 -> 1`, `hits 149 -> 154`. Artifact: `scratch/ps1-perf-iterate/20260425-190108/summary.json`. | Do not promote. Immediate short-slack reads are still too expensive; reducing due misses requires grouped/cheaper refill, not just a lower immediate-frame guard. |
| 2026-04-25 | `fg2-pal4-row-dirty-screenwide` | dirty experiment after `68e29132` | Move PAL4 dirty marking out of every span compositor call and mark one dirty rectangle per encoded row. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Rejected after red-team review. It improved `loop_vb 1254 -> 1244` and `blocking_vb 35 -> 19`, but merged dirty extents across the 320px tile boundary and widened RAM restore: `restore_bytes 2510092 -> 3678656`; `prefetch_overrun_vb` also rose `6 -> 13`. Artifact: `scratch/ps1-perf-iterate/20260425-190649/summary.json`. | Do not promote. Row-level dirty marking must stay split per PS1 tile or it destroys the row/X restore precision won earlier. |
| 2026-04-25 | `fg2-pal4-row-dirty-per-tile` | `aeab2067` | Mark PAL4 FG2 dirty extents once per encoded row and per 320px tile, preserving exact restore/upload metrics while removing redundant per-span dirty-marker calls. | Strict probe `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement`, then acceptance run with `--allow-regression 150` because only `prefetch_overrun_vb` exceeded the strict secondary guard. | Promoted. Baseline to current: `loop_vb 1254 -> 1248`, `overrun_vb 177 -> 171`, `blocking_vb 35 -> 21`, `due_misses 6 -> 1`, `hits 149 -> 154`, `prefetch_overrun_vb 6 -> 15`; `restore_bytes=2510092`, `upload_bytes=17172480`, and correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-191039/summary.json`, `scratch/ps1-perf-iterate/20260425-191252/summary.json`. | Promote. The restore/upload invariants prove pixel-work scope is unchanged; the extra refill overrun is bounded and buys both a `6` VBlank loop win and a `14` VBlank blocking reduction. |
| 2026-04-25 | `fg2-prefetch-min-slack-4vb-post-row-dirty` | dirty experiment after `aeab2067` | After per-tile row dirty marking reduced due misses to one, raising the stream-window refill guard from `3` to `4` VBlanks might cut the new bounded refill overrun without starving due frames. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed the blocking gate. It improved `loop_vb 1248 -> 1244` and reduced `prefetch_overrun_vb 15 -> 12`, but visible CD blocking regressed `21 -> 36` and `due_misses 1 -> 5`; correctness and dirty byte metrics stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-191708/summary.json`. | Do not promote. The extra total-loop gain is not worth making due-frame reads visible again; keep the `3` VBlank refill guard. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-6vb-post-row-dirty` | `37085999` | Raise only the staged-copy fallthrough guard from `5` to `6` VBlanks, reducing second-read overrun risk without starving the immediate next frame. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Promoted. Baseline to current: `loop_vb 1248 -> 1243`, `overrun_vb 171 -> 166`, `blocking_vb 21 -> 21`, `due_misses 1 -> 1`, `hits 154 -> 154`, `prefetch_overrun_vb 15 -> 12`; `restore_bytes=2510092`, `upload_bytes=17172480`, and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-191943/summary.json`. | Promote. This is the exact shape wanted after the row-dirty change: less refill overrun and lower total loop with no visible blocking or dirty-scope regression. |
| 2026-04-25 | `fg2-window-15kb-14kb-post-fallthrough` | parameter probes after `37085999` | Re-sweep below the accepted `16 KB` stream window after the `6` VBlank fallthrough guard; shorter reads might cut the remaining refill overrun. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 15360" ...`, then `... prefetch-window 14336` with the current baseline and `--require-improvement`. | Failed. `15 KB` rounded to a no-op: `loop_vb=1243`, `blocking_vb=21`, `prefetch_overrun_vb=12`. `14 KB` reduced `prefetch_overrun_vb 12 -> 9`, but regressed `loop_vb 1243 -> 1244`, `blocking_vb 21 -> 36`, and `due_misses 1 -> 7`. Artifacts: `scratch/ps1-perf-iterate/20260425-192251/summary.json`, `scratch/ps1-perf-iterate/20260425-192416/summary.json`. | Do not promote. The local window knee remains the sector-rounded `16 KB` default; going lower starves future coverage. |
| 2026-04-25 | `fg2-row-dirty-visible-hoist` | dirty experiment after `37085999` | Hoist the `rowScreenY` visible-row test out of the per-span dirty aggregation branch to remove one repeated branch across about `91,948` fishing1 spans. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed as a VBlank-level no-op. Baseline and current both reported `loop_vb=1243`, `blocking_vb=21`, `prefetch_overrun_vb=12`, `hits=154`, and `due_misses=1`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-192658/summary.json`. | Do not promote on this pass. The idea may save sub-VBlank CPU, but the current gate cannot prove it; retry only with finer CPU counters or as part of broader compositor cleanup. |
| 2026-04-25 | `fg2-row-dirty-direct-tile-mark` | dirty experiment after `37085999` | Because PAL4 dirty aggregation already clips and splits rows per tile, call `markTileDirtyRect()` directly instead of routing each row back through generic `grMarkRectDirty()`. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. All key timing metrics regressed: `loop_vb 1243 -> 1248`, `blocking_vb 21 -> 26`, and `prefetch_overrun_vb 12 -> 17`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-192956/summary.json`. | Do not promote. The generic wrapper is cheaper in this build than the duplicated direct tile-mark path, likely because code growth/branch shape beat the theoretical call-path saving. |
| 2026-04-25 | `fg2-compose-before-vsync` | dirty experiment after `37085999` | Move FG2 RAM-only composition before `grUpdateDisplay()`'s mandatory `VSync(0)` so the VBlank is focused on framebuffer `LoadImage` upload rather than CPU composition first. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. Correctness stayed clean, but timing and prefetch coverage regressed: `loop_vb 1243 -> 1248`, `blocking_vb 21 -> 39`, `due_misses 1 -> 4`, and `prefetch_overrun_vb 12 -> 15`. Artifact: `scratch/ps1-perf-iterate/20260425-195003/summary.json`. | Do not promote. Moving composition earlier steals timing from the current prefetch/advance cadence; present sequencing needs Detail-tier attribution or a broader render-loop redesign before retry. |
| 2026-04-25 | `fg2-stage-copy-fallthrough-7vb-post-row-dirty` | dirty experiment after `37085999` | Raise the staged-copy fallthrough guard from `6` to `7` VBlanks to see whether the prior `5 -> 6` win continues smoothing refill overrun without starving due frames. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. Correctness stayed clean and `blocking_vb` held at `21`, but no key metric improved: `loop_vb 1243 -> 1246`, `overrun_vb 166 -> 169`, and `prefetch_overrun_vb 12 -> 13`. Artifact: `scratch/ps1-perf-iterate/20260425-195333/summary.json`. | Do not promote. The local fallthrough guard optimum is `6` VBlanks under this baseline; retry only after another refill-cost or held-slack change moves the knee. |
| 2026-04-25 | `fg2-window-due-direct-pointer` | dirty experiment after `37085999` | When the due frame is already resident in the stream window, point `currentFrameData` into the window instead of copying to `frameBuffer`, leaving staged prefetch copies unchanged. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 25 --require-improvement` | Failed. Correctness stayed clean, but timing regressed: `loop_vb 1243 -> 1250`, `blocking_vb 21 -> 26`, and `prefetch_overrun_vb 12 -> 19`; `due_misses` stayed `1`. Artifact: `scratch/ps1-perf-iterate/20260425-195833/summary.json`. | Do not promote. The pointer path changes hot-path code shape more than it saves; keep the explicit copy unless pack/window ownership is redesigned more broadly. |
| 2026-04-25 | `fg2-circular-stream-window` | dirty experiment after `37085999` | Replace per-refill stream-window `memmove()` with a logical circular head, compacting only when the append would wrap so CD transaction count stays unchanged. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement` | Failed as a VBlank-level no-op. Correctness stayed clean, but the gate found no key improvement: `loop_vb=1243`, `overrun_vb=166`, `blocking_vb=21`, `prefetch_overrun_vb=12`, `hits=154`, and `due_misses=1`, all matching baseline. Artifact: `scratch/ps1-perf-iterate/20260425-201517/summary.json`. | Do not promote. Window compaction is not the current critical path at JCPERF2 resolution; retry only with finer CPU counters or if a future pack/window design makes RAM movement dominant. |
| 2026-04-25 | `cd-aligned-sequential-setloc-skip` | dirty experiment after `37085999` | Because baseline playback has `seq=67`, skip `CdControl(CdlSetloc)` when an aligned read starts exactly at the previous aligned read end, and adjust the `setloc` metric to count only issued `Setloc` calls. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement` | Rejected after red-team review. The old gate passed and key timings improved (`loop_vb 1243 -> 1233`, `blocking_vb 21 -> 16`, `prefetch_overrun_vb 12 -> 7`, `setloc 76 -> 9`), but visual-work counters collapsed: `compose_calls 155 -> 4`, `upload_calls 156 -> 14`, `restore_bytes 2510092 -> 262550`, and `upload_bytes 17172480 -> 1691520`. Artifact: `scratch/ps1-perf-iterate/20260425-202530/summary.json`. | Do not promote. A CD-position continuation without explicit `Setloc` is unsafe for this runtime; future gates must treat major visual-work reductions as identity failures, not speed wins. |
| 2026-04-25 | `fg2-pal4-inline-span-compositor` | dirty experiment after `37085999` | Inline the PAL4 span compositor inside the decoded row loop so row visibility, tile base pointers, and clipping are reused instead of recomputed by a per-span helper call. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement` | Failed. Visual-work identity stayed stable (`render=156`, `restore_calls=156`, `compose_calls=155`, `upload_calls=156`), but timing regressed: `loop_vb 1243 -> 1249`, `overrun_vb 166 -> 172`, `blocking_vb 21 -> 23`, and `prefetch_overrun_vb 12 -> 14`. Artifact: `scratch/ps1-perf-iterate/20260425-203736/summary.json`. | Do not promote. The theoretical call/branch reduction lost to code-shape/cache effects in this build; retry only if the whole PAL4 compositor is regenerated or moved to a tighter scene-specialized implementation. |
| 2026-04-25 | `fg2-slack-adaptive-window-8k-12k` | dirty experiment after `37085999` | Use smaller stream-window reads for tight held-frame slack (`8 KB` at 3 VBlanks, `12 KB` at 4 VBlanks) while keeping the accepted `16 KB` read for safer slack. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement` | Failed the blocking gate. It improved `loop_vb 1243 -> 1242` and `prefetch_overrun_vb 12 -> 6`, but starved coverage: `blocking_vb 21 -> 36`, `due_misses 1 -> 5`, and `hits 154 -> 150`. Artifact: `scratch/ps1-perf-iterate/20260425-204231/summary.json`. | Do not promote. The remaining refill overrun can be reduced by reading less, but the cost immediately reappears as visible due-frame blocking; solve coverage with grouped/cheaper reads rather than smaller opportunistic windows. |
| 2026-04-25 | `events-zero-delay-fast-path` | dirty experiment after `37085999` | `eventsWaitTick(0)` is called throughout FG2 held-frame playback; return immediately after Start polling and one `VSync(-1)` anchor update instead of running zero-delay pacing math. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement` | Failed as a VBlank-level no-op. Timing and work identity matched baseline exactly: `loop_vb=1243`, `blocking_vb=21`, `prefetch_overrun_vb=12`, `render=156`, `compose_calls=155`. Artifact: `scratch/ps1-perf-iterate/20260425-204635/summary.json`. | Do not promote. It may remove sub-VBlank CPU work, but the current gate cannot prove it; retry only with finer CPU counters or as part of a broader event/pause cleanup. |
| 2026-04-25 | `fg2-held-vblank-present-wait-skip` | dirty experiment after `37085999` | Skip `grUpdateDisplay()`'s pre-upload `VSync(0)` when the next rendered FG2 frame follows a held-loop VBlank and was loaded from staged/window data. | `./scripts/build-ps1.sh`, then strict and Detail runs with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Rejected after red-team review despite headless PASS. Summary improved `loop_vb 1243 -> 1239`, `blocking_vb 21 -> 20`, and `prefetch_overrun_vb 12 -> 11` with stable work identity; Detail showed `present_wait_vb` collapsing to `35`, but compose/upload crossed VBlank boundaries more often. Artifacts: `scratch/ps1-perf-iterate/20260425-205427/summary.json`, `scratch/ps1-perf-iterate/20260425-205558/summary.json`. | Do not promote. The saved wait is not proven scanline-safe: the held-loop VBlank can be consumed before frame load/compose completes, so framebuffer `LoadImage` may occur outside the known VBlank window. Retry only with an explicit VBlank-safe present scheduler or visual validation harness. |
| 2026-04-25 | `fg2-base-diff-skip-ot-clear` | `63737abc` | Base-diff FG2 scene playback uses RAM composition plus framebuffer `LoadImage` uploads; it should not need the per-render `grBeginFrame()` ordering-table and primitive-buffer reset unless backdrop waves or legacy GPU primitives are active. | `./scripts/build-ps1.sh`, then two strict runs with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Promoted. Reproduced twice with stable visual-work identity: `loop_vb 1243 -> 1242`, `overrun_vb 166 -> 165`, `blocking_vb 21 -> 20`, `prefetch_overrun_vb=12`, `render=156`, `restore_calls=156`, `compose_calls=155`, `upload_calls=156`, `restore_bytes=2510092`, `upload_bytes=17172480`; correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-210007/summary.json`, `scratch/ps1-perf-iterate/20260425-210131/summary.json`. | Promote. This is a safe one-VBlank cleanup because it only skips unused OT state reset for base-diff FG2 frames; non-base-diff/runtime wave paths still reset the primitive state. |
| 2026-04-25 | `fg2-window-18kb-post-ot-clear` | parameter probe after `63737abc` | After the OT-clear cleanup shifted timing by one VBlank, re-test a slightly larger `18 KB` stream window to see whether fewer reads now beat the accepted `16 KB` window. | First ran `fishing1-w18` and found a harness-label issue, then fixed the harness and re-ran as `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 18432 perf-log noloop seed 1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Work identity stayed stable, but timing regressed: `loop_vb 1242 -> 1251`, `overrun_vb 165 -> 174`, `blocking_vb 20 -> 42`, `prefetch_overrun_vb 12 -> 21`, `hits 154 -> 151`, and `due_misses 1 -> 4`. Artifacts: `scratch/ps1-perf-iterate/20260425-211156/summary.json`, `scratch/ps1-perf-iterate/20260425-211321/summary.json`. | Do not promote. The post-row-dirty/post-OT-clear window knee remains `16 KB`; larger reads reduce transaction count but starve due-frame coverage and make visible blocking worse. Harness lesson: baseline label mismatches are now hard failures, not warnings. |
| 2026-04-25 | `fg2-unconditional-compose-call` | dirty experiment after `63737abc` | `foregroundPilotRuntimeCompose()` already returns immediately when inactive, so `grUpdateDisplay()` might call it directly and avoid the extra active check on active FG2 frames. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Metrics matched baseline exactly: `loop_vb=1242`, `overrun_vb=165`, `blocking_vb=20`, `prefetch_overrun_vb=12`, and all visual-work counters stayed identical. Artifact: `scratch/ps1-perf-iterate/20260425-211605/summary.json`. | Do not promote. This may be a later code-cleanup candidate, but it does not produce a measured performance win and was reverted for the perf branch. |
| 2026-04-25 | `fg2-holiday-stamp-callsite-guard` | dirty experiment after `63737abc` | Fishing1 has no holiday overlay, so checking `islandState.holiday != 0` before calling `fgBackdropStampHoliday()` might remove one no-op helper call per rendered entry. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Metrics matched baseline exactly: `loop_vb=1242`, `overrun_vb=165`, `blocking_vb=20`, `prefetch_overrun_vb=12`, and visual-work counters were unchanged. Artifact: `scratch/ps1-perf-iterate/20260425-211848/summary.json`. | Do not promote. The call is too small to move the current gate; revisit only as part of broader holiday/backdrop cleanup or binary-size cleanup. |
| 2026-04-25 | `fg2-restore-uint32-copy-helper` | dirty experiment after `63737abc` | Replace restore-row `memcpy()` calls with a PS1-local helper that handles odd 16-bit edges and copies aligned pixel pairs as unrolled 32-bit words. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Visual work stayed identical and `loop_vb` was flat, but CD/prefetch timing regressed: `blocking_vb 20 -> 22` and `prefetch_overrun_vb 12 -> 14`. Artifact: `scratch/ps1-perf-iterate/20260425-215027/summary.json`. | Do not promote. The handwritten copy path likely worsens code shape or cache pressure relative to the SDK/libc `memcpy`; future restore wins should come from less restore work or pack-time row planning, not replacing `memcpy` locally. |
| 2026-04-25 | `fg2-cache-base-diff-flag` | dirty experiment after `63737abc` | Cache the FG2 base-diff header flag in `TFgPilotRuntime` at scene start, then have per-loop helpers read a byte instead of masking `header.reserved0` in hot paths. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Work identity and timing matched baseline exactly: `loop_vb=1242`, `overrun_vb=165`, `blocking_vb=20`, `prefetch_overrun_vb=12`, `render=156`, `restore_calls=156`, `compose_calls=155`, and `upload_calls=156`. Artifact: `scratch/ps1-perf-iterate/20260425-215444/summary.json`. | Do not promote. This is safe cleanup shape but not a measured performance win; only retry as part of broader runtime-state cleanup or with finer CPU-cycle counters. |
| 2026-04-25 | `fg2-single-row-exact-upload` | dirty experiment after `63737abc` | When a dirty tile upload covers exactly one row, use the row-level dirty X union for a direct exact-width `LoadImage` instead of uploading the full 320-pixel tile row. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a no-op for fishing1. Timing matched baseline and upload work also matched exactly: `loop_vb=1242`, `blocking_vb=20`, `prefetch_overrun_vb=12`, `upload_bytes=17172480`, `upload_rects=351`, and `dirty_rows=26832`. Artifact: `scratch/ps1-perf-iterate/20260425-220038/summary.json`. | Do not promote. Fishing1's current dirty aggregation does not produce single-row upload bands; future upload wins need multi-row contiguous-band packing or pack/runtime upload-ready bands, not this special case. |
| 2026-04-25 | `fg2-fallthrough-5vb-post-ot-clear` | dirty experiment after `63737abc` | Re-test the staged-copy fallthrough guard at `5` VBlanks after the base-diff OT-clear skip, in case the one-VBlank timing shift moved the local optimum. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Timing regressed with identical work identity: `loop_vb 1242 -> 1245`, `overrun_vb 165 -> 168`, `blocking_vb=20`, `prefetch_overrun_vb 12 -> 13`, `hits=154`, and `due_misses=1`. Artifact: `scratch/ps1-perf-iterate/20260425-220316/summary.json`. | Do not promote. The `6` VBlank fallthrough guard remains the current fishing1 knee; loosening it still starts at least one refill too aggressively. |
| 2026-04-25 | `fg2-async-window-refill-prototype` | dirty experiment after `63737abc` | Start stream-window refill reads asynchronously during held-frame prefetch and poll them on later held iterations, while preserving synchronous due-frame reads. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed hard. Work identity stayed stable, but timing regressed: `loop_vb 1242 -> 1267`, `overrun_vb 165 -> 190`, `blocking_vb 20 -> 71`, and `prefetch_overrun_vb 12 -> 64`. Artifact: `scratch/ps1-perf-iterate/20260425-220858/summary.json`. | Do not promote. Naive async refill converts hidden slack into worse controller/CD blocking in this runtime shape; retry only with dedicated async metrics, explicit CD-state ownership, and a scheduler that proves completion before due-frame pressure. |
| 2026-04-25 | `fg2-clean-rect-intersection-skip` | dirty experiment after `63737abc` | Before scanning clean-rect restore backups, skip any clean rect whose bounds do not intersect the previous dirty tile bounds. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Total loop stayed flat, but CD/prefetch timing regressed: `loop_vb=1242`, `overrun_vb=165`, `blocking_vb 20 -> 23`, and `prefetch_overrun_vb 12 -> 14`; visual work changed only by `restore_calls 156 -> 155`. Artifact: `scratch/ps1-perf-iterate/20260425-221258/summary.json`. | Do not promote. The added intersection check costs more than the rare skipped restore scan saves on fishing1; future restore wins should remove restore work earlier in the pack/dirty plan rather than adding per-frame screening. |
| 2026-04-25 | `cd-stream-file-lba-cache` | dirty experiment after `63737abc` | Cache each stream-read helper's file-start LBA locally instead of recomputing `CdPosToInt(cdfile->pos)` before every `CdIntToPos()` call. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Work identity and key timings matched baseline exactly: `loop_vb=1242`, `overrun_vb=165`, `blocking_vb=20`, `prefetch_overrun_vb=12`, `render=156`, `compose_calls=155`, and `upload_calls=156`. Artifact: `scratch/ps1-perf-iterate/20260425-221719/summary.json`. | Do not promote. The conversion cost is below the current JCPERF2 resolution and not on the observed bottleneck path; retry only with finer CPU counters or if CD helper cleanup is being done for code size/readability. |
| 2026-04-25 | `fg2-pal4-tile-local-fast-path` | `853b8310` | Most fishing1 PAL4 spans are visible and wholly inside one 320px tile; cache row tile pointers and call the opaque-run compositor directly for that common case while leaving cross-tile/clipped spans on the generic path. | `./scripts/build-ps1.sh`, then two strict runs with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Promoted. Reproduced exactly: `loop_vb 1242 -> 1240`, `overrun_vb 165 -> 163`, `blocking_vb=20`, `prefetch_overrun_vb 12 -> 11`, and visual-work identity stayed stable (`render=156`, `restore_calls=156`, `compose_calls=155`, `upload_calls=156`). Artifacts: `scratch/ps1-perf-iterate/20260425-222158/summary.json`, `scratch/ps1-perf-iterate/20260425-222321/summary.json`. | Promote. This is a bounded compositor hot-path win: it avoids repeated tile/clipping setup for tile-local spans without changing dirty scope, CD reads, upload bytes, or fallback behavior. |
| 2026-04-25 | `fg2-pal4-cross-tile-direct-split` | dirty experiment after `853b8310` | The remaining cross-320px PAL4 spans might benefit from direct two-run composition using cached row tile pointers instead of falling back to the generic span helper. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Visual-work identity stayed stable, but timing regressed: `loop_vb 1240 -> 1246`, `overrun_vb 163 -> 169`, `blocking_vb 20 -> 27`, and `prefetch_overrun_vb 11 -> 18`. Artifact: `scratch/ps1-perf-iterate/20260425-222639/summary.json`. | Do not promote. The extra branch and second opaque-run call for cross-tile spans worsens code shape/cache behavior; keep the generic helper for rare boundary spans. |
| 2026-04-25 | `fg2-pal4-run0-helper` | dirty experiment after `853b8310` | The accepted tile-local path always calls the PAL4 opaque-run compositor with `srcPixel=0`; a dedicated zero-offset helper might remove the odd-start branch and source-index arithmetic. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Work identity and timing matched baseline exactly: `loop_vb=1240`, `overrun_vb=163`, `blocking_vb=20`, `prefetch_overrun_vb=11`, `render=156`, `compose_calls=155`, and `upload_calls=156`. Artifact: `scratch/ps1-perf-iterate/20260425-222916/summary.json`. | Do not promote. The compiler likely already optimizes the constant-zero call path, or the win is below JCPERF2 resolution; retry only with finer CPU counters or generated scene-specialized compositor code. |
| 2026-04-25 | `fg2-fallthrough-5vb-post-tile-fast-path` | dirty experiment after `853b8310` | Re-test the staged-copy fallthrough guard at `5` VBlanks after the tile-local PAL4 fast path, in case the compositor timing shift moved the CD slack knee. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Work identity stayed stable, but timing regressed: `loop_vb 1240 -> 1244`, `overrun_vb 163 -> 167`, `blocking_vb 20 -> 22`, and `prefetch_overrun_vb 11 -> 15`. Artifact: `scratch/ps1-perf-iterate/20260425-223253/summary.json`. | Do not promote. The `6` VBlank fallthrough guard remains the local optimum after the tile-local compositor change; the looser guard still starts refill too aggressively. |
| 2026-04-25 | `fg2-restore-full-row-coalesce` | dirty experiment after `853b8310` | Coalesce consecutive full-width dirty restore rows into one contiguous `memcpy()` inside `grRestoreBgTiles()` to reduce per-row restore-call overhead without changing restored pixels. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Work identity and timing matched baseline exactly: `loop_vb=1240`, `overrun_vb=163`, `blocking_vb=20`, `prefetch_overrun_vb=11`, `restore_calls=156`, `restore_bytes=2510092`, and `upload_calls=156`. Artifact: `scratch/ps1-perf-iterate/20260425-223612/summary.json`. | Do not promote. Fishing1 either lacks enough consecutive full-width restore rows or the saved `memcpy()` overhead is below current resolution; retry only with finer CPU counters or pack-emitted restore bands. |
| 2026-04-25 | `fg2-pal4-run4-unroll` | dirty experiment after `853b8310` | Unroll the PAL4 opaque compositor loop from two pixels per iteration to four pixels per iteration, keeping the same halfword palette stores and avoiding the previously slower pair-LUT word-store path. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Work identity and timing matched baseline exactly: `loop_vb=1240`, `overrun_vb=163`, `blocking_vb=20`, `prefetch_overrun_vb=11`, `render=156`, `compose_calls=155`, and `upload_calls=156`. Artifact: `scratch/ps1-perf-iterate/20260425-223934/summary.json`. | Do not promote. Simple PAL4 loop unrolling no longer moves the current bottleneck; future compositor wins likely need pack-generated commands or upload-ready/direct16 data. |
| 2026-04-25 | `fg2-window-2vb-post-tile-fast-path` | dirty experiment after `853b8310` | Lower the stream-window refill guard from `3` to `2` VBlanks after the tile-local PAL4 fast path, testing whether the lower compositor cost can tolerate earlier refill reads. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Work identity stayed stable, but timing regressed: `loop_vb 1240 -> 1245`, `overrun_vb 163 -> 168`, `blocking_vb 20 -> 26`, and `prefetch_overrun_vb 11 -> 18`. Artifact: `scratch/ps1-perf-iterate/20260425-224201/summary.json`. | Do not promote. The `3` VBlank refill guard remains the local lower bound; `2` VBlanks starts reads that spill into visible playback. |
| 2026-04-25 | `fg2-prefetch-single-window-check` | dirty experiment after `853b8310` | Remove the held-loop `fgRuntimeWindowPrefetchWouldRead()` pre-check and let `fgRuntimeTryPrefetchWindow()` perform the single target/window/slack decision, avoiding duplicate table/window checks before real reads. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a VBlank-level no-op. Work identity and timing matched baseline exactly: `loop_vb=1240`, `overrun_vb=163`, `blocking_vb=20`, `prefetch_overrun_vb=11`, `hits=154`, and `due_misses=1`. Artifact: `scratch/ps1-perf-iterate/20260425-224454/summary.json`. | Do not promote. The duplicate target/window checks are not measurable at current resolution; leave the existing helper split because it keeps duplicate metrics quiet and explicit. |
| 2026-04-25 | `fg2-upload-vertical-dirty-bands` | `ae95004c` | Split each dirty tile upload into contiguous vertical dirty-row bands so clean Y gaps are not uploaded, while keeping full tile width and avoiding runtime scratch packing. | Strict run with `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`; verification run without `--require-improvement`. | Promoted as a safe work-volume reduction. Key timing stayed flat (`loop_vb=1240`, `overrun_vb=163`, `blocking_vb=20`, `prefetch_overrun_vb=11`), while `upload_bytes 17172480 -> 16387840`, `dirty_rows 26832 -> 25606`, `upload_rects 351 -> 518`, and `max_upload_rects 4 -> 6`; `cap_hits=0`, `full_fallbacks=0`, and work identity stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-224810/summary.json`, `scratch/ps1-perf-iterate/20260425-224947/summary.json`. | Promote. The VBlank timer did not move, but the runtime now uploads 784,640 fewer bytes per fishing1 loop with no correctness or timing regression. Next upload work should reduce the added rectangle pressure or move band decisions to pack time. |
| 2026-04-25 | `fg2-upload-band-gap-merge-1row` | dirty experiment after `ae95004c` | Merge 1-row clean gaps inside vertical upload bands to reduce `LoadImage` rectangle pressure while preserving nearly all of the accepted byte savings. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as too small to promote. Timing was flat, but secondary movement was marginal: `upload_rects 518 -> 505`, `upload_bytes 16387840 -> 16396160`, `dirty_rows 25606 -> 25619`, `cap_hits=0`, and `full_fallbacks=0`. Artifact: `scratch/ps1-perf-iterate/20260425-225809/summary.json`. | Do not promote. The 1-row merge gives back bytes for only 13 fewer rects; the 2-row threshold is the better balance. |
| 2026-04-25 | `fg2-upload-band-gap-merge-2rows` | `a5840980` | Merge up to 2 clean rows inside vertical upload bands to recover much of the old full-band rectangle count without returning to full minY/maxY uploads. | Strict run with `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`; verification run without `--require-improvement`. | Promoted as a rect-pressure reduction with bounded byte give-back. Timing stayed flat (`loop_vb=1240`, `overrun_vb=163`, `blocking_vb=20`, `prefetch_overrun_vb=11`), while `upload_rects 518 -> 427`, `max_upload_rects 6 -> 5`, `upload_bytes 16387840 -> 16496000`, and `dirty_rows 25606 -> 25775`; `cap_hits=0`, `full_fallbacks=0`, and work identity stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-225618/summary.json`, `scratch/ps1-perf-iterate/20260425-230004/summary.json`. | Promote. It keeps 676,480 bytes of the original vertical-band upload reduction while recovering 91 upload rects, so the runtime has less command pressure and still avoids scratch packing. |
| 2026-04-25 | `fg2-upload-band-gap-merge-3rows` | dirty experiment after `a5840980` | Increase the upload band clean-gap merge threshold from `2` to `3` rows to see whether more `LoadImage` rectangle pressure can be removed without returning much upload volume. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as too small to promote. Key timing stayed flat (`loop_vb=1237`, `overrun_vb=160`, `blocking_vb=13`, `prefetch_overrun_vb=13`), while `upload_rects 427 -> 425`, `upload_bytes 16496000 -> 16499840`, `dirty_rows 25775 -> 25781`, `max_upload_rects=5`, `cap_hits=0`, and `full_fallbacks=0`. Artifact: `scratch/ps1-perf-iterate/20260425-232127/summary.json`. | Do not promote. Two fewer rectangles for 3,840 extra bytes is not a meaningful enough work-volume improvement over the accepted 2-row threshold. |
| 2026-04-25 | `fg2-upload-band-gap-merge-4rows` | dirty experiment after `a5840980` | Push the upload band clean-gap merge threshold to `4` rows to check whether the rect/byte knee is just above the accepted `2`-row setting. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as too small to promote. Key timing stayed flat (`loop_vb=1237`, `overrun_vb=160`, `blocking_vb=13`, `prefetch_overrun_vb=13`), while `upload_rects 427 -> 424`, `upload_bytes 16496000 -> 16502400`, `dirty_rows 25775 -> 25785`, `max_upload_rects=5`, `cap_hits=0`, and `full_fallbacks=0`. Artifact: `scratch/ps1-perf-iterate/20260425-232305/summary.json`. | Do not promote. Upper merge thresholds produce only tiny rectangle reductions on fishing1; future upload wins should be pack-emitted bands or upload-ready layouts rather than more runtime gap tuning. |
| 2026-04-25 | `fg2-setup-prime-first-payload` | `172cff5f` | Fishing1 frame 0 is empty; prime the first real payload during setup into the existing stage/window buffers so the first active-loop advance does not pay a due-frame CD miss. | Strict run with `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`; acceptance rerun with `--allow-regression 25` for the bounded refill-overrun tradeoff. | Promoted. Reproduced active-loop win: `loop_vb 1240 -> 1237`, `overrun_vb 163 -> 160`, `blocking_vb 20 -> 13`, `due_misses 1 -> 0`, `prefetch_hits 154 -> 155`, and `loop_reads 69 -> 68`. The tradeoff is `setup_reads 7 -> 8`, `setup_vb 182 -> 191`, `first_frame_vb 0 -> 9`, and `prefetch_overrun_vb 11 -> 13`; work identity and correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-230607/summary.json`, `scratch/ps1-perf-iterate/20260425-230737/summary.json`. | Promote. The screensaver target is active scene playback speed; moving the first real payload read from loop to setup removes the remaining due miss and lowers `loop_vb`, while setup cost is outside active playback and deterministic. |
| 2026-04-25 | `fg2-window-4vb-post-setup-prime` | dirty experiment after `172cff5f` | After setup priming eliminated due misses, raising the stream-window refill guard from `3` to `4` VBlanks might cut the remaining refill spill without starving active playback. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed the blocking guard. It produced a real total-loop win, but by reintroducing visible CD blocking: `loop_vb 1237 -> 1229`, `overrun_vb 160 -> 152`, `blocking_vb 13 -> 20`, `prefetch_overrun_vb 13 -> 8`, `due_misses 0 -> 3`, `hits 155 -> 152`, and `skipped_no_slack 8 -> 25`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-231314/summary.json`. | Do not promote. Guard-only tightening can lower total VBlanks, but it does so by starving near-term frames; that is the wrong tradeoff while the zero-due-miss baseline exists. |
| 2026-04-25 | `fg2-window-4vb-fallthrough-5vb-post-setup-prime` | dirty experiment after `172cff5f` | Pair the stricter `4` VBlank window guard with a `5` VBlank staged-copy fallthrough guard, hoping fallthrough prefetch recovers due-frame coverage while keeping refill overrun low. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed the blocking guard. It preserved the `4` VBlank guard's total-loop win and saved one blocking VBlank, but still regressed visible blocking: `loop_vb 1237 -> 1229`, `overrun_vb 160 -> 152`, `blocking_vb 13 -> 19`, `prefetch_overrun_vb 13 -> 8`, `due_misses 0 -> 3`, `hits 155 -> 152`, and `skipped_no_slack 8 -> 25`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-231458/summary.json`. | Do not promote. The paired fallthrough tweak is not enough; the missing coverage needs grouped or better scheduled reads, not just a different slack constant. |
| 2026-04-25 | `fg2-window-4vb-fallthrough-4vb-post-setup-prime` | dirty experiment after `172cff5f` | Lower staged-copy fallthrough to `4` VBlanks with the `4` VBlank window guard, testing whether more same-iteration lookahead recovers due misses without worsening refill spill. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed the blocking guard and mostly gave back the refill-overrun savings: `loop_vb 1237 -> 1235`, `overrun_vb 160 -> 158`, `blocking_vb 13 -> 19`, `prefetch_overrun_vb 13 -> 13`, `due_misses 0 -> 2`, `hits 155 -> 153`, and `skipped_no_slack 8 -> 16`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260425-231630/summary.json`. | Do not promote. This confirms the local slack-constant family is exhausted for now; it can reduce either refill spill or due misses, but not both versus the accepted 3/6 baseline. |
| 2026-04-25 | `fg2-window-18kb-post-setup-prime` | parameter probe after `172cff5f` | Setup priming changed early CD pressure, so re-test an `18 KB` stream window to see whether fewer/larger reads now beat the accepted sector-rounded `16 KB` default. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 18432 perf-log noloop seed 1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Work identity stayed stable and due misses stayed zero, but every key timing metric regressed: `loop_vb 1237 -> 1246`, `overrun_vb 160 -> 169`, `blocking_vb 13 -> 23`, and `prefetch_overrun_vb 13 -> 24`. Artifact: `scratch/ps1-perf-iterate/20260425-232648/summary.json`. | Do not promote. The current post-prime stream-window knee remains `16 KB`; larger reads still overrun held slack too much even when due-frame coverage is preserved. |
| 2026-04-25 | `fg2-tight-slack-direct-stage-8kb` | `3fe830be` | When the next frame is not resident and only the minimum accepted `3` VBlanks of slack remain, read just the immediate payload into the stage buffer if it is `<=8 KB` instead of starting a full `16 KB` stream-window refill. | `./scripts/build-ps1.sh`, then two strict runs with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Promoted. Both runs matched exactly: `loop_vb 1237 -> 1235`, `overrun_vb 160 -> 158`, `blocking_vb 13 -> 11`, `prefetch_overrun_vb 13 -> 11`, `due_misses=0`, `stage_hits 148 -> 149`, `window_hits 155 -> 154`, and `loop_reads=68`; work identity and correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260425-234929/summary.json`, `scratch/ps1-perf-iterate/20260425-235055/summary.json`. | Promote. This keeps the current zero-due-miss behavior while replacing one tight-slack window refill with a smaller immediate-payload stage read. It is a narrow version of the old failed short-slack direct-stage idea, but only at the proven `3` VBlank lower bound. |
| 2026-04-26 | `fg2-direct-stage-4vb-slack` | dirty experiment after `a7847e05` | Extend the accepted direct-stage path from exactly `3` VBlanks of slack to `<=4` VBlanks, hoping small immediate payload reads beat full `16 KB` stream-window refills at the next slack tier. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. The smaller reads reduced stream-window coverage and caused due-frame starvation: `loop_vb 1235 -> 1239`, `overrun_vb 158 -> 162`, `blocking_vb 11 -> 39`, `prefetch_overrun_vb 11 -> 12`, `due_misses 0 -> 5`, `hits 155 -> 150`, `stage_hits 149 -> 137`, `window_hits 154 -> 145`, `loop_reads 68 -> 73`, and `seek_back 8 -> 21`. Work identity stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-000031/summary.json`. | Do not promote. Direct staging at `4` VBlanks destroys too much lookahead under the current raw-window layout; retry only after pack-emitted groups or a two-entry stage queue can preserve near-term coverage. |
| 2026-04-26 | `fg2-window-15kb-post-direct-stage` | parameter probe after `a7847e05` | Re-test a slightly smaller `15 KB` stream window after tight-slack direct staging, checking whether less refill payload lowers overrun without losing the zero-due-miss baseline. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 15360 perf-log noloop seed 1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a no-op. Work identity, CD shape, and all key timing matched the `16 KB` default exactly: `loop_vb=1235`, `overrun_vb=158`, `blocking_vb=11`, `prefetch_overrun_vb=11`, `due_misses=0`, `hits=155`, and `loop_reads=68`. Artifact: `scratch/ps1-perf-iterate/20260426-000331/summary.json`. | Do not promote. The current post-direct-stage window-size knee is not improved by `15 KB`; further CD gains need grouped reads, layout changes, or better scheduling rather than one-step window shrinkage. |
| 2026-04-26 | `fg2-direct-stage-seed-window` | `d0e00ab8` | After a tight-slack direct-stage read, copy the already-read aligned sectors from `streamScratch` into the FG2 stream window so the read can become the start of future window coverage instead of being thrown away and re-read later. | `./scripts/build-ps1.sh`, then two strict runs with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Promoted. Both runs matched exactly: `loop_vb=1235`, `overrun_vb=158`, `blocking_vb 11 -> 10`, `prefetch_overrun_vb 11 -> 10`, `due_misses=0`, `hits=155`, `loop_reads=68`, `read_vb 409 -> 405`, `loop_read_vb 289 -> 285`, `bytes 1096030 -> 1093982`, `sectors 542 -> 541`, `seq 65 -> 66`, and `seek_back 8 -> 7`; work identity and correctness stayed clean. Artifacts: `scratch/ps1-perf-iterate/20260426-000736/summary.json`, `scratch/ps1-perf-iterate/20260426-000857/summary.json`. | Promote. This converts a small direct-stage read into reusable stream-window state, reducing one backward seek and one visible CD-blocking VBlank without increasing memory or changing pixel work. |
| 2026-04-26 | `fg2-staged-next-vblank-present-scheduler` | dirty experiment after `ed7dee89` | Compose the staged next frame during the final held-frame slack, wait for the transition VBlank, then upload immediately, attempting to recover the measured `present_wait_vb` while remaining scanline-safe. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Correctness and visual-work identity stayed clean (`trip=0`, `fallback=0`, `frame_mismatch=0`, `sound_late=0`, `cd_fail=0`, `render=156`, `restore_calls=156`, `compose_calls=155`, `upload_calls=156`), but timing regressed: `loop_vb 1235 -> 1306`, `overrun_vb 158 -> 229`, `blocking_vb 10 -> 13`, and `prefetch_overrun_vb 10 -> 13`. Artifact: `scratch/ps1-perf-iterate/20260426-001856/summary.json`. | Do not promote. Pulling restore/compose/present work into the held loop disrupted the current prefetch cadence more than it saved; retry only with a scheduler that reserves CD slack separately from render preparation or with a true double-buffered RAM-present plan. |
| 2026-04-26 | `fg2-held-slack-prepared-present` | `6c7b8fc0` | Pre-render the staged next frame into RAM only when held slack is at least `4` VBlanks, then upload the prepared RAM background on the transition VBlank. | `./scripts/build-ps1.sh`, threshold sweep at `2/3/4/5` VBlanks, then two strict acceptance runs with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Promoted. The accepted `4` VBlank threshold reproduced exactly twice: `loop_vb 1235 -> 1234`, `overrun_vb 158 -> 157`, `blocking_vb 10 -> 8`, `prefetch_overrun_vb 10 -> 8`, `due_misses=0`, `hits=155`, and `loop_reads=68`. Correctness stayed clean. The tradeoff is extra RAM prep work: `restore_calls 156 -> 192`, `compose_calls 155 -> 191`, and `restore_bytes 2510092 -> 3050904`. Artifacts: `scratch/ps1-perf-iterate/20260426-003350/summary.json`, `scratch/ps1-perf-iterate/20260426-003718/summary.json`. | Promote as a small deterministic speedup. This does not yet reduce measured `present_wait_vb`; it reshapes held slack and CD/refill pressure. Next present work should reduce duplicate restore/compose preparation or make the prepared state upload-ready without extra work. |
| 2026-04-26 | `fg2-held-slack-prepared-present-exact-4vb` | dirty experiment after `ad979ea8` | Restrict prepared-frame work to exactly `4` VBlanks of held slack instead of `>=4`, hoping to keep the timing win while reducing speculative restore/compose work. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It reduced speculative prep work (`restore_calls 192 -> 168`, `compose_calls 191 -> 167`) but gave back the accepted timing win: `loop_vb 1234 -> 1235`, `overrun_vb 157 -> 158`, `blocking_vb 8 -> 9`, and `prefetch_overrun_vb 8 -> 9`. Correctness and work identity otherwise stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-004337/summary.json`. | Do not promote. Some earlier-than-exact-4 preparation is contributing to the accepted slack/CD shape. Reducing prep cost needs a smarter exact-use predictor, not a simple equality guard. |
| 2026-04-26 | `fg2-held-slack-prepared-present-4-or-5vb` | dirty experiment after `ef85272b` | Allow prepared-frame work only when held slack is `4` or `5` VBlanks, testing whether the accepted win needs the exact-4 and near-5 cases but not higher-slack speculation. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It regressed the same key timing as exact-4: `loop_vb 1234 -> 1235`, `overrun_vb 157 -> 158`, `blocking_vb 8 -> 9`, and `prefetch_overrun_vb 8 -> 9`. It also increased speculative prep calls versus baseline (`restore_calls 192 -> 206`, `compose_calls 191 -> 205`), showing the simple max-slack cap perturbs the scheduler rather than just pruning high-slack work. Artifact: `scratch/ps1-perf-iterate/20260426-004643/summary.json`. | Do not promote. The prepared-present scheduler is sensitive to slack cadence; further cleanup should use explicit prepared-used counters or a frame-level prediction table before changing the guard. |
| 2026-04-26 | `fg2-fallthrough-5vb-post-prepared-present` | dirty experiment after `47dc3532` | Retest the staged-copy fallthrough guard at `5` VBlanks under the new prepared-present baseline, checking whether the CD slack knee moved after the latest accepted scheduler change. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as not worth promoting. Visible timing stayed flat (`loop_vb=1234`, `overrun_vb=157`, `blocking_vb=8`, `prefetch_overrun_vb=8`) and correctness stayed clean, but there was no key speed improvement. It saved only two speculative prep calls (`restore_calls 192 -> 190`, `compose_calls 191 -> 189`) while increasing total CD read time (`read_vb 407 -> 416`, hidden under slack). Artifact: `scratch/ps1-perf-iterate/20260426-005000/summary.json`. | Do not promote. Keep the `6` VBlank fallthrough guard; a two-call speculative-work reduction with worse hidden CD time is not a clean enough default-path win. |
| 2026-04-26 | `fg2-direct-stage-cap-6kb-post-prepared-present` | dirty experiment after `66a8fc0a` | Lower the tight-slack direct-stage payload cap from `8 KB` to `6 KB`, testing whether less immediate direct-staging improves stream-window coverage under the prepared-present baseline. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as a no-op. Timing and work identity matched baseline exactly: `loop_vb=1234`, `overrun_vb=157`, `blocking_vb=8`, `prefetch_overrun_vb=8`, `restore_calls=192`, `compose_calls=191`, and `upload_calls=156`. Artifact: `scratch/ps1-perf-iterate/20260426-010939/summary.json`. | Do not promote. The `6 KB` cap does not alter the active fishing1 direct-stage decisions; keep the accepted `8 KB` cap. |
| 2026-04-26 | `fg2-direct-stage-cap-4kb-post-prepared-present` | dirty experiment after `66a8fc0a` | Lower the tight-slack direct-stage payload cap from `8 KB` to `4 KB`, forcing more frames back through stream-window refill to see whether future coverage beats immediate staging. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It regressed the accepted baseline: `loop_vb 1234 -> 1237`, `overrun_vb 157 -> 160`, `blocking_vb 8 -> 13`, and `prefetch_overrun_vb 8 -> 13`; work identity stayed otherwise clean. Artifact: `scratch/ps1-perf-iterate/20260426-011116/summary.json`. | Do not promote. The current `8 KB` cap is doing real work for fishing1; lower caps lose immediate tight-slack coverage and reintroduce visible CD pressure. |
| 2026-04-26 | `fg2-prepared-wait-window-prefetch` | dirty experiment after `735e6e77` | When a prepared frame is waiting for its transition VBlank, use that held iteration for stream-window prefetch instead of an explicit wait, hoping to recover lookahead while retaining the prepared-present win. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It reduced duplicate RAM prep work (`restore_calls 192 -> 156`, `compose_calls 191 -> 155`) but gave back one timing VBlank: `loop_vb 1234 -> 1235`, `overrun_vb 157 -> 158`, `blocking_vb 8 -> 9`, and `prefetch_overrun_vb 8 -> 9`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-011411/summary.json`. | Do not promote. Prepared-frame wait time is part of the accepted timing shape; spending it on a raw stream-window read lowers CPU work but perturbs CD slack by one VBlank. Retry only with explicit slack/cost prediction or grouped reads. |
| 2026-04-26 | `fg2-window-12kb-post-prepared-present` | parameter probe after `70e2b702` | Shrink the stream-window read size to `12 KB` under the current prepared-present baseline, testing whether much shorter reads can remove the remaining refill overrun without reintroducing due misses. | `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 prefetch-window 12288 perf-log noloop seed 1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed hard. Refill overrun improved (`prefetch_overrun_vb 8 -> 1`) but coverage collapsed: `loop_vb 1234 -> 1245`, `overrun_vb 157 -> 168`, `blocking_vb 8 -> 89`, `due_misses 0 -> 26`, and `hits 155 -> 129`. Artifact: `scratch/ps1-perf-iterate/20260426-011845/summary.json`. | Do not promote. This is the clearest signal yet that raw smaller windows only move cost from refill spill to visible due-frame blocking; next CD work needs pack-grouped coverage or a second stage slot, not smaller reads. |
| 2026-04-26 | `fg2-two-entry-direct-stage-3vb` | dirty experiment after `c1a147d8` | Add a bounded second staged-frame buffer and, only when the immediate next frame is already staged, exactly `3` VBlanks remain, and the next-next payload is `<=8 KB`, direct-read that next-next payload instead of starting a raw stream-window refill. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Work identity stayed clean, but coverage still degraded: `loop_vb 1234 -> 1238`, `overrun_vb 157 -> 161`, `blocking_vb 8 -> 28`, `prefetch_overrun_vb 8 -> 11`, `due_misses 0 -> 3`, and `hits 155 -> 152`. Artifact: `scratch/ps1-perf-iterate/20260426-012415/summary.json`. | Do not promote. A second exact-payload stage slot is not enough by itself; replacing a raw window read loses too much forward coverage. Retry only with pack-emitted groups that preserve a covered frame range per read. |
| 2026-04-26 | `fg2-prepared-current-reuse` | dirty experiment after `6d969cba` | If held-slack preparation already composed the frame that the normal advance path just consumed, preserve that prepared RAM background through the normal VSync/upload path instead of clearing it and restoring/composing again. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It removed all duplicate prepared work (`restore_calls 192 -> 156`, `compose_calls 191 -> 155`) but regressed timing by the same one-VBlank shape as the other prepare-pruning attempts: `loop_vb 1234 -> 1235`, `overrun_vb 157 -> 158`, `blocking_vb 8 -> 9`, and `prefetch_overrun_vb 8 -> 9`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-015100/summary.json`. | Do not promote. The extra prepared restore/compose work is currently acting as pacing that protects the accepted CD slack shape. Remove it only with a scheduler that replaces that timing deliberately, not by skipping work in isolation. |
| 2026-04-26 | `fg2-held-slack-prepared-present-3vb` | dirty experiment after `f4ef7c8d` | Lower held-slack prepared-present from `4` to `3` VBlanks, trying to prepare more transition frames and reduce the one-VBlank-per-render present gap. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Total timing stayed flat but visible CD pressure regressed: `loop_vb=1234`, `overrun_vb=157`, `blocking_vb 8 -> 9`, `prefetch_overrun_vb 8 -> 9`, `due_misses=0`, `hits=155`, `restore_calls 192 -> 198`, and `compose_calls 191 -> 197`. Artifact: `scratch/ps1-perf-iterate/20260426-020020/summary.json`. | Do not promote. More aggressive preparation adds duplicate RAM work and perturbs the accepted CD slack shape without reducing `loop_vb`; the next present attempt needs an explicit render-prep budget, not another threshold-only change. |
| 2026-04-26 | `fg2-post-prefetch-leftover-prepare` | dirty experiment after `3fca9422` | After a stream-window prefetch, use the same held iteration to prepare the staged next frame only if measured prefetch elapsed time leaves the normal `4` VBlank prepare budget, preserving CD work first and using leftover slack for render prep. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It increased prep work but damaged future coverage: `loop_vb 1234 -> 1241`, `overrun_vb 157 -> 164`, `blocking_vb 8 -> 36`, `prefetch_overrun_vb 8 -> 18`, `due_misses 0 -> 5`, `hits 155 -> 150`, `loop_reads 68 -> 71`, `seek_back 7 -> 16`, `restore_calls 192 -> 200`, and `compose_calls 191 -> 199`. Artifact: `scratch/ps1-perf-iterate/20260426-020349/summary.json`. | Do not promote. Leftover slack after a CD read is still coupled to future prefetch opportunities; render prep must be scheduled from a frame-level plan or group-aware read budget, not opportunistically after every successful prefetch. |
| 2026-04-26 | `fg2-host-deadline-latency-offset` | dirty experiment after `27158e85` | Subtract one VBlank from host-deadline target calculations to account for the normal PS1 present wait, testing whether the current overrun is purely a deadline-vs-present-latency accounting bug. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as the wrong layer. `loop_vb` stayed flat at `1234`, `blocking_vb=8`, `prefetch_overrun_vb=8`, and all visual-work counters matched; only `target_vb` changed `1077 -> 1076`, making `overrun_vb 157 -> 158`. Artifact: `scratch/ps1-perf-iterate/20260426-020705/summary.json`. | Do not promote. The `presentedVBlanks` accumulator absorbs a naive cumulative deadline offset, so present-latency work must change scheduling/presentation state rather than the host-deadline conversion alone. |
| 2026-04-26 | `gfx-targeted-curr-dirty-clear` | dirty experiment after `c80ccb42` | Clear only the currently dirty row ranges in `grClearCurrDirtyState()` instead of all `4 x 240` tile rows every restore/prep, aiming to reduce CPU work without changing restore/upload identity. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed as unproven. Correctness stayed clean and key timing was exactly flat: `loop_vb=1234`, `overrun_vb=157`, `blocking_vb=8`, `prefetch_overrun_vb=8`, `due_misses=0`, and `hits=155`. Work identity shifted slightly (`restore_calls 192 -> 190`, `compose_calls 191 -> 189`) without a speed win. Artifact: `scratch/ps1-perf-iterate/20260426-020956/summary.json`. | Do not promote. This may be a sub-VBlank cleanup, but it is not measurable in the current acceptance loop and the small prepared-work identity shift is not worth carrying without a timing win. |
| 2026-04-26 | `fg2-window-14-18-20kb-post-prepared-present` | parameter probe after `3aaae04c` | Sweep nearby stream-window sizes after the prepared-present scheduler changes, testing whether the current `16 KB` knee moved. | `./scripts/ps1-perf-iterate.sh --case "fishing1-w14::fgpilot fishing1 prefetch-window 14336 perf-log noloop seed 1" --case "fishing1-w18::fgpilot fishing1 prefetch-window 18432 perf-log noloop seed 1" --case "fishing1-w20::fgpilot fishing1 prefetch-window 20480 perf-log noloop seed 1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. All candidates were slower than the accepted `16 KB` baseline: `14 KB` hit `loop_vb=1236`, `blocking_vb=36`, `due_misses=8`; `18 KB` hit `loop_vb=1247`, `blocking_vb=33`, `due_misses=2`; `20 KB` hit `loop_vb=1249`, `blocking_vb=36`, `due_misses=2`. Artifact: `scratch/ps1-perf-iterate/20260426-021539/summary.json`. | Do not promote. Smaller windows starve near-term coverage and larger windows overrun slack; keep the `16 KB` default until grouped/pack-aware reads change the coverage-per-read shape. |
| 2026-04-26 | `fg2-leading-empty-setup-consume` | dirty experiment after `174d48c6` | Treat fishing1's leading empty capture artifact as non-visual setup work: after setup primes frame 1, advance `presentedVBlanks`, consume the staged first real payload before active-loop start, and re-prime the next payload to preserve staging depth. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed strict gate but is promising. It saved `6` VBlanks (`loop_vb 1234 -> 1228`, `overrun_vb 157 -> 151`) with `due_misses=0`, `hits=155`, and clean correctness, but regressed `blocking_vb 8 -> 9` and `prefetch_overrun_vb 8 -> 9`; render work also dropped by one non-payload frame (`render 156 -> 155`). Artifacts: `scratch/ps1-perf-iterate/20260426-022436/summary.json`, `scratch/ps1-perf-iterate/20260426-022618/summary.json`. | Do not promote without a visual signoff and CD-smoothing companion. The saved work is a real nonvisual startup-frame win, but the current acceptance loop treats the extra blocking VBlank as a regression and the render-count change needs explicit policy approval. |
| 2026-04-26 | `fg2-leading-empty-setup-consume-settle-vblank` | accepted after `616b6ab2` | Consume the leading empty capture artifact during setup, immediately re-prime the next payload, then spend one setup-only settle VBlank so the active-loop CD cadence does not inherit the earlier start. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Promoted. Strict gates passed: `loop_vb 1234 -> 1227`, `overrun_vb 157 -> 150`, `blocking_vb 8 -> 7`, `prefetch_overrun_vb 8 -> 7`, `due_misses=0`, `hits=155`, and correctness stayed clean. It intentionally removes one non-payload render (`render 156 -> 155`) and lowers upload/dirty work (`upload_bytes 16496000 -> 16381440`, `upload_rects 427 -> 424`). Artifact: `scratch/ps1-perf-iterate/20260426-023007/summary.json`. | Promote. The one-VBlank settle cost is paid in setup, outside the active playback loop, and the empty entry remains counted/presented logically while avoiding a visible no-payload render pass. This is the new baseline. |
| 2026-04-26 | `fg2-leading-empty-two-setup-settle-vblanks` | dirty experiment after `6fe14027` | Spend two setup-only settle VBlanks after consuming the leading empty artifact, testing whether additional pre-loop cadence spacing lowers the remaining active-loop CD blocking. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. The extra setup wait over-smoothed the cadence and made active playback worse: `loop_vb 1227 -> 1235`, `overrun_vb 150 -> 158`, `blocking_vb 7 -> 13`, and `prefetch_overrun_vb 7 -> 13`; work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-023353/summary.json`. | Do not promote. One setup settle is the local knee; more pre-loop waiting shifts the stream schedule into a worse phase instead of hiding the remaining reads. |
| 2026-04-26 | `fg2-window-15-17kb-post-leading-empty` | parameter probe after `6fe14027` | Re-test the closest raw stream-window neighbors after the leading-empty setup cadence change, checking whether the old `16 KB` knee moved. | `./scripts/ps1-perf-iterate.sh --case "fishing1-w15::fgpilot fishing1 prefetch-window 15360 perf-log noloop seed 1" --case "fishing1-w17::fgpilot fishing1 prefetch-window 17408 perf-log noloop seed 1" --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed/no promotion. `15 KB` matched the accepted timing exactly (`loop_vb=1227`, `blocking_vb=7`, `due_misses=0`) and offers no measurable win; `17 KB` regressed (`loop_vb=1238`, `blocking_vb=29`, `due_misses=2`, `hits=153`). Artifact: `scratch/ps1-perf-iterate/20260426-023933/summary.json`. | Keep the `16 KB` default. Raw window-size tuning remains exhausted; further CD wins need grouped/layout-aware reads or scheduler changes, not another nearby byte count. |
| 2026-04-26 | `fg2-prepared-current-reuse-post-leading-empty` | dirty experiment after `ecb25581` | Re-test prepared-current RAM reuse on the new leading-empty baseline, then refine it by trying to spend the saved post-upload slot on immediate next-payload staging. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Both pure reuse and reuse-plus-immediate-prefetch produced the same regression: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 7 -> 12`, and `prefetch_overrun_vb 7 -> 12`. The work reduction was real (`restore_calls 187 -> 155`, `compose_calls 187 -> 155`), but correctness was the only clean part. Artifacts: `scratch/ps1-perf-iterate/20260426-024326/summary.json`, `scratch/ps1-perf-iterate/20260426-024541/summary.json`. | Do not promote. Prepared-current reuse still needs an explicit frame scheduler that preserves CD phase; local reuse removes wasteful CPU but worsens the active-loop cadence. |
| 2026-04-26 | `fg2-direct-stage-4vb-post-leading-empty` | dirty experiment after `563e1ef5` | Expand the exact small-payload direct-stage path from exactly `3` VBlanks of slack to `3-4` VBlanks, trying to replace some remaining spill-prone `16 KB` window reads with cheaper payload reads. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Timing regressed slightly and coverage degraded: `loop_vb 1227 -> 1228`, `overrun_vb 150 -> 151`, `blocking_vb 7 -> 16`, `prefetch_overrun_vb 7 -> 8`, `due_misses 0 -> 2`, and `hits 155 -> 153`; it only saved two speculative restore/compose calls. Artifact: `scratch/ps1-perf-iterate/20260426-024907/summary.json`. | Do not promote. Direct-stage exact reads must stay at the minimum `3` VBlank knee; widening the slack band loses forward window coverage faster than it saves read size. |
| 2026-04-26 | `gfx-upload-band-merge-gap-8` | dirty experiment after `16c07860` | Sweep wider vertical dirty-upload band merge gaps (`3`, `4`, `6`, `8`) to reduce `LoadImage` command count while keeping the full-width tile-row upload path direct and scanline-safe. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as a command-pressure micro-optimization. Key timing stayed identical (`loop_vb=1227`, `blocking_vb=7`, `prefetch_overrun_vb=7`, `due_misses=0`) while `upload_rects 424 -> 412`; the cost is a small upload widening (`upload_bytes 16381440 -> 16424960`, `dirty_rows 25596 -> 25664`). Artifacts: `scratch/ps1-perf-iterate/20260426-025141/summary.json`, `scratch/ps1-perf-iterate/20260426-025319/summary.json`, `scratch/ps1-perf-iterate/20260426-025456/summary.json`, `scratch/ps1-perf-iterate/20260426-025631/summary.json`. | Promote. This does not move VBlank-level timing on DuckStation, but it lowers GPU command pressure with negligible byte growth and no fallback/correctness change. This is the new baseline. |
| 2026-04-26 | `fg2-direct-stage-cap-sweep-post-gap8` | dirty experiment after `329540ec` | Re-sweep the tight-slack direct-stage payload cap on the 8-row upload-merge baseline, checking whether `8 KB` is still the local knee after the latest cadence and upload changes. | `./scripts/build-ps1.sh`, then three runs of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement` with caps `6 KB`, `10 KB`, and `4 KB`. | Failed/no promotion. `6 KB` and `10 KB` matched baseline exactly (`loop_vb=1227`, `blocking_vb=7`, `prefetch_overrun_vb=7`, `due_misses=0`, `restore_calls=187`, `compose_calls=187`); `4 KB` regressed (`loop_vb 1227 -> 1230`, `blocking_vb 7 -> 11`, `prefetch_overrun_vb 7 -> 11`) while only reducing speculative prep calls by two. Artifacts: `scratch/ps1-perf-iterate/20260426-030207/summary.json`, `scratch/ps1-perf-iterate/20260426-030343/summary.json`, `scratch/ps1-perf-iterate/20260426-030509/summary.json`. | Keep `8 KB`. Fishing1's active direct-stage candidates are already covered by `8 KB`; lower caps lose the small tight-slack coverage that prevents visible CD pressure, and higher caps do not change this scene. |
| 2026-04-26 | `fg2-lookahead-window-4vb-guard` | dirty experiment after `85a252a7` | Split immediate-next and pure-lookahead refill eligibility: keep the accepted `3` VBlank guard for staging the immediate next payload, but require `4` VBlanks before a standalone lookahead window refill. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It preserved correctness and zero due misses, but regressed timing: `loop_vb 1227 -> 1230`, `overrun_vb 150 -> 153`, `blocking_vb 7 -> 10`, and `prefetch_overrun_vb 7 -> 10`; speculative prep calls dropped by one (`restore_calls 187 -> 186`, `compose_calls 187 -> 186`) but not enough to offset the CD phase loss. Artifact: `scratch/ps1-perf-iterate/20260426-030832/summary.json`. | Do not promote. The remaining lookahead reads are still pacing-sensitive; a stricter fixed guard delays useful coverage and moves CD pressure into visible time. The next CD win needs cost prediction/group metadata, not another scalar slack threshold. |
| 2026-04-26 | `fg2-lookahead-read-size-predictor` | dirty experiment after `5f19091e` | Add a runtime read-size predictor for pure-lookahead refills: at exactly `3` VBlanks of slack, allow only predicted small append/window reads and defer larger reads. Sweep tight-read caps at `8 KB` and `12 KB`. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. The `8 KB` cap had a tempting total-loop gain (`loop_vb 1227 -> 1225`, `overrun_vb 150 -> 148`) but regressed `blocking_vb 7 -> 8`, `prefetch_overrun_vb 7 -> 8`, increased read time (`loop_read_vb 288 -> 298`), and changed scheduler shape (`advances 784 -> 774`, `held 666 -> 653`). The `12 KB` cap was plainly worse (`loop_vb=1233`, `blocking_vb=14`, `prefetch_overrun_vb=14`). Artifacts: `scratch/ps1-perf-iterate/20260426-031203/summary.json`, `scratch/ps1-perf-iterate/20260426-031342/summary.json`. | Do not promote. A local byte-threshold predictor can reduce nominal loop time by perturbing cadence, but it does not actually reduce CD cost or visible pressure. Retry only with per-read histograms, group metadata, or an explicit frame-deadline scheduler. |
| 2026-04-26 | `gfx-upload-band-merge-gap-12` | dirty experiment after `d79f3751` | Test whether widening the accepted dirty-upload clean-row merge gap from `8` to `12` rows reduces enough `LoadImage` command pressure to justify the extra uploaded rows. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed/no promotion. Timing and correctness matched the accepted baseline (`loop_vb=1227`, `blocking_vb=7`, `prefetch_overrun_vb=7`, `due_misses=0`, no fallback/correctness trips). It reduced upload rectangles (`412 -> 399`) but increased upload volume (`16424960 -> 16514560`) and dirty rows (`25664 -> 25804`). Artifact: `scratch/ps1-perf-iterate/20260426-031647/summary.json`. | Do not promote. Fewer upload calls are useful, but this point buys them with extra DMA volume and no measured timing win; the later `10`-row midpoint superseded the initial `8`-row stopping point while confirming `12` is still too wide. |
| 2026-04-26 | `hot-tu-o3-compile-options` | dirty experiment after `b56a7d2b` | Compile the hot playback translation units (`foreground_pilot.c`, `graphics_ps1.c`, `cdrom_ps1.c`) with `-O3` to test whether GCC can reduce runtime CPU work without a harmful code-size/I-cache tradeoff. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. It reduced speculative prepared work (`restore_calls 187 -> 182`, `compose_calls 187 -> 182`) but worsened active playback: `loop_vb 1227 -> 1229`, `overrun_vb 150 -> 152`, `blocking_vb 7 -> 10`, and `prefetch_overrun_vb 7 -> 10`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-032201/summary.json`. | Do not promote. The compiler shape perturbed scheduler cadence and visible CD pressure despite less RAM prep work; keep the SDK `-O2` default for hot units until a narrower function-level or assembly pass proves a win. |
| 2026-04-26 | `fg2-direct-stage-read-into-window` | dirty experiment after `dea714ba` | For tight-slack direct-stage reads, read the aligned sectors straight into the stream-window buffer and copy the staged payload from there, avoiding the extra scratch-to-window seed copy. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. The RAM-copy saving did not survive scheduler effects: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 7 -> 9`, `prefetch_overrun_vb 7 -> 9`, `loop_reads 68 -> 69`, and `seek_back 7 -> 9`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-032636/summary.json`. | Do not promote. The existing scratch-read plus window-seed path has better stream cadence; direct window replacement loses coverage shape even though it removes one local copy. |
| 2026-04-26 | `fg2-held-no-slack-skip` | dirty experiment after `9bb9407f` | If a held frame has no VBlank slack remaining and no prepared frame is waiting, skip the normal held wait and let `foregroundPilotRuntimeAdvance()` transition immediately, avoiding an apparent one-VBlank overshoot. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. The scheduler change regressed active timing: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 7 -> 10`, `prefetch_overrun_vb 7 -> 10`, `restore_calls 187 -> 190`, and `compose_calls 187 -> 190`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-033021/summary.json`. | Do not promote. The apparent idle wait is part of the accepted CD/render cadence; removing it causes extra prep work and moves CD pressure into visible time. |
| 2026-04-26 | `fg2-direct-stage-merge-seed` | dirty experiment after `e389838f` | When a tight-slack direct-stage read has adjacent or overlapping stream-window coverage, merge the scratch sectors into the existing window instead of always replacing the window with the exact staged range. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Correctness stayed clean, but timing regressed with the now-familiar prep/CD phase shape: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 7 -> 10`, `prefetch_overrun_vb 7 -> 10`, `restore_calls 187 -> 190`, and `compose_calls 187 -> 190`. Artifact: `scratch/ps1-perf-iterate/20260426-033446/summary.json`. | Do not promote. Even a coverage-preserving seed merge perturbs the tight-slack path enough to lose cadence; defer direct-stage window-shape work until grouped metadata or per-read histograms can prove which reads benefit. |
| 2026-04-26 | `fg2-proactive-window-extend` | dirty experiment after `945c02bf` | When the next not-yet-staged payload is already resident in the stream window, use eligible held slack to slide/append the window from that earliest needed payload instead of waiting until a later miss is imminent. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed. Zero due misses stayed intact, but lookahead reads became too visible: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 7 -> 13`, `prefetch_overrun_vb 7 -> 13`, with one extra restore/compose call. Artifact: `scratch/ps1-perf-iterate/20260426-034116/summary.json`. | Do not promote. Eager raw-window append work spends scarce held slack before the scheduler proves the read is cheap enough; retry only with group metadata or sector/read-cost prediction. |
| 2026-04-26 | `gfx-upload-band-merge-gap-10` | dirty experiment after `4b10d82f` | Test the midpoint between the accepted `8`-row upload-band merge and the rejected `12`-row merge, looking for a better rectangle/byte tradeoff with flat timing. | `./scripts/build-ps1.sh`, then two runs of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as a tiny command-pressure improvement. Both runs matched timing and correctness exactly (`loop_vb=1227`, `blocking_vb=7`, `prefetch_overrun_vb=7`, `due_misses=0`) while `upload_rects 412 -> 409`; the cost is bounded (`upload_bytes 16424960 -> 16442880`, `dirty_rows 25664 -> 25692`). Artifacts: `scratch/ps1-perf-iterate/20260426-034502/summary.json`, `scratch/ps1-perf-iterate/20260426-034633/summary.json`. | Promote. This is smaller than a VBlank-level speed win, but it removed three more `LoadImage` rectangles for only `17,920` extra bytes across the loop and no fallback/correctness change; it was superseded by the accepted 11-row probe. |
| 2026-04-26 | `gfx-upload-band-merge-gap-11` | dirty experiment after `4c8ac906` | Probe the one-row point between the accepted `10`-row upload-band merge and rejected `12`-row merge, checking whether command pressure can drop again without timing, correctness, or fallback regressions. | `./scripts/build-ps1.sh`, then two runs of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as another command-pressure micro-optimization. Both runs matched timing and correctness exactly (`loop_vb=1227`, `blocking_vb=7`, `prefetch_overrun_vb=7`, `due_misses=0`) while `upload_rects 409 -> 401`; the cost is bounded (`upload_bytes 16442880 -> 16499200`, `dirty_rows 25692 -> 25780`). Artifacts: `scratch/ps1-perf-iterate/20260426-035251/summary.json`, `scratch/ps1-perf-iterate/20260426-035421/summary.json`. | Promote. This removed eight more `LoadImage` rectangles for `56,320` extra bytes across the loop, with clean correctness and no VBlank-level regression; it was superseded by the FG2 metadata-prefix read. |
| 2026-04-26 | `cd-read-histogram-inline-metrics` | dirty experiment after `d6948f2` | Add a `JCPERF2 cdhist` section with read-elapsed, prefetch-slack, and overrun buckets so future CD experiments can distinguish read classes without visual debugging. | First run: summary-level counters/output. Second run: counters/output gated to `perf-detail` only. Both used `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed and source reverted. Both variants regressed the same way despite clean correctness: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 7 -> 10`, `prefetch_overrun_vb 7 -> 10`, with `restore_calls/compose_calls 187 -> 190`. Artifacts: `scratch/ps1-perf-iterate/20260426-040122/summary.json`, `scratch/ps1-perf-iterate/20260426-040339/summary.json`. | Do not promote. Even detail-gated inline histogram code changed binary/code shape enough to perturb the deterministic scheduler. Future read-class evidence must be compile-time isolated, extracted from existing logs, or gathered in a separate diagnostic build that is never used as the speed baseline. |
| 2026-04-26 | `fg2-metadata-prefix-read` | dirty experiment after `19604b5b` | Coalesce FG2 startup metadata loading by reading the header/palette/entry-table prefix in one bounded stream read, replacing the old three small setup reads before sound-event loading. | `./scripts/build-ps1.sh`, then three runs of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted. All runs matched active-loop timing and correctness while reducing CD/setup pressure: `setup_reads 8 -> 6`, `pack_start_vb 55 -> 42`, `setup_read_vb 120 -> 108`, `scene_vb 1419 -> 1406`, `blocking_vb 7 -> 6`, and `prefetch_overrun_vb 7 -> 6`. Tradeoff: metadata bytes rise by `5000` and speculative prep shifts to `restore_calls/compose_calls 187 -> 190`; upload work identity stays unchanged. Artifacts: `scratch/ps1-perf-iterate/20260426-041241/summary.json`, `scratch/ps1-perf-iterate/20260426-041428/summary.json`, `scratch/ps1-perf-iterate/20260426-041901/summary.json`. | Promote. This is a setup/read-amortization win that also improves the remaining active-loop CD-pressure counters by one VBlank; keep watching speculative prep count on the next scheduler experiment. |
| 2026-04-26 | `fg2-sound-events-in-metadata-prefix` | dirty experiment after `a8031676` | Move the FG2 sound-event table from end-of-pack to immediately after the entry table, parse it from the metadata prefix, and regenerate fishing1 high/low packs so startup can avoid the separate sound-event read. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source/packs reverted. Setup improved (`setup_reads 6 -> 5`, `pack_start_vb 42 -> 26`, `setup_read_vb 108 -> 93`, `scene_vb 1406 -> 1401`), but shifting every payload offset by `36` bytes regressed active playback: `loop_vb 1227 -> 1238`, `overrun_vb 150 -> 161`, `blocking_vb 6 -> 18`, and `prefetch_overrun_vb 6 -> 18`. Correctness and sound tripwires stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-043423/summary.json`. | Do not promote. Payload physical offset stability matters more than removing one startup read; retry sound/setup coalescing only if the pack format can keep payload offsets and sector-crossing behavior unchanged, or after group metadata absorbs the changed alignment. |
| 2026-04-26 | `fg2-resolve-pack-once-for-startup` | dirty experiment after `e4051b1b` | Resolve the FG2 CD file once before startup metadata/sound reads, then use the resolved `CdlFILE` for metadata and sound-event reads to avoid repeated path lookup and one malloc/copy layer. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed and source reverted. Setup improved (`pack_start_vb 42 -> 30`, `setup_read_vb 108 -> 98`, `scene_vb 1406 -> 1397`), but active playback regressed: `loop_vb 1227 -> 1230`, `blocking_vb 6 -> 8`, and `prefetch_overrun_vb 6 -> 8`. It also changed speculative prep cadence (`restore_calls/compose_calls 190 -> 186`). Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-044053/summary.json`. | Do not promote. Setup-only code-shape wins are not free; this one reduced setup work but altered the prepared-frame/CD cadence enough to lose active-loop time. Retry only with an explicit setup/loop phase barrier or after the scheduler no longer depends on startup cadence. |
| 2026-04-26 | `fg2-trim-window-to-complete-payloads` | dirty experiment after `243313d8` | Keep the same complete-entry coverage as the default `16 KB` stream window, but shorten each read to the sector after the last complete payload entry instead of carrying trailing partial overread bytes. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It preserved `hits=155` and `due_misses=0` while reducing total bytes/sectors (`1098982 -> 1094886`, `541 -> 539`), but active timing worsened: `loop_vb 1227 -> 1240`, `blocking_vb 6 -> 13`, `prefetch_overrun_vb 6 -> 13`, and `loop_read_vb 286 -> 298`. Speculative prep dropped (`restore_calls/compose_calls 190 -> 183`). Artifact: `scratch/ps1-perf-iterate/20260426-044542/summary.json`. | Do not promote. The unused trailing sectors are still part of the accepted cadence; reading slightly less does not lower elapsed CD time and reduces slack/prep pacing. Group metadata needs a cost model, not just byte trimming. |
| 2026-04-26 | `fg2-prepare-before-lookahead` | dirty experiment after `327c27e6` | When a next frame is already staged during a held entry, prepare it for next-VBlank presentation before spending that held slice on pure lookahead prefetch. | `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It reduced speculative prep work (`restore_calls/compose_calls 190 -> 186`, `restore_bytes 3034562 -> 2981102`) but regressed the active loop: `loop_vb 1227 -> 1240`, `overrun_vb 150 -> 163`, `blocking_vb 6 -> 18`, `prefetch_overrun_vb 6 -> 18`, `loop_read_vb 286 -> 305`, `loop_reads 68 -> 69`, and `seek_back 5 -> 11`. Correctness stayed clean with `hits=155` and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-051156/summary.json`. | Do not promote. Preparing a staged frame before lookahead preserves due-frame coverage but steals CD phase from future reads; present work needs explicit separate budgets, not a simple priority inversion. |
| 2026-04-26 | `ps1-section-gc` | dirty experiment after `029a5bc2` | Compile with function/data sections and link with `--gc-sections` so unused linked legacy paths stop contributing to executable pressure. | `./scripts/build-ps1.sh clean`, then two runs of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as a binary-size hygiene improvement. Both runs matched timing and work identity exactly (`loop_vb=1227`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `render=155`, `restore_calls/compose_calls=190`, `upload_calls=155`) with correctness clean. ELF size dropped `709828 -> 708656` bytes; `jcreborn.exe` remains in the same `137216` byte sector bucket. Artifacts: `scratch/ps1-perf-iterate/20260426-051927/summary.json`, `scratch/ps1-perf-iterate/20260426-052049/summary.json`. | Promote. This is not a VBlank-level speed win, but it makes subsequent release/code-size cleanup measurable and safe under the headless gate. |
| 2026-04-26 | `fg-telemetry-gate` | dirty experiment after `3c87e17b` | Gate foreground ADS-style telemetry writes behind the existing `grPs1TelemetryEnabled` flag so normal playback does not update legacy visual-debug globals on every advance. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. Timing stayed flat (`loop_vb=1227`, `blocking_vb=6`, `prefetch_overrun_vb=6`) but no key metric improved, ELF size grew `708656 -> 708724`, and speculative work identity shifted (`restore_calls/compose_calls 190 -> 188`). Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-052413/summary.json`. | Do not promote. The branch is larger than the unconditional stores under section-GC and still perturbs deterministic scheduler shape; remove telemetry more structurally during the public-code cleanup instead of gating it in the hot path. |
| 2026-04-26 | `fg-telemetry-remove` | dirty experiment after `1f945620` | Remove foreground's legacy ADS-style visual telemetry body from the active PS1 hot path so section-GC can drop the now-unreferenced debug globals. | `./scripts/build-ps1.sh`, then two runs of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as code-size/hot-path cleanup. Timing stayed flat (`loop_vb=1227`, `blocking_vb=6`, `prefetch_overrun_vb=6`) with correctness clean; speculative prep work dropped deterministically (`restore_calls/compose_calls 190 -> 188`, `restore_bytes 3034562 -> 2999408`) and loop CD read time moved `286 -> 285`. Current ELF size is `707916` bytes and `jcreborn.exe` remains `137216` bytes. Artifacts: `scratch/ps1-perf-iterate/20260426-052703/summary.json`, `scratch/ps1-perf-iterate/20260426-052834/summary.json`. | Promote. This deletes unused visual-debug writes rather than adding a branch, keeps pixels/sound clean, and makes printf/logging the intended diagnostic path. |
| 2026-04-26 | `prepared-frame-prefetch-window` | dirty experiment after `5a104dc0` | While a staged frame is already prepared but not yet presentable, spend remaining held VBlanks on future stream-window prefetch instead of waiting idle. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It removed duplicate speculative prep work (`restore_calls/compose_calls 188 -> 155`) but regressed active timing: `loop_vb 1227 -> 1228`, `overrun_vb 150 -> 151`, `blocking_vb 6 -> 7`, and `prefetch_overrun_vb 6 -> 7`; correctness stayed clean with `hits=155` and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-053408/summary.json`. | Do not promote. Prepared-frame slack is not free in the current cadence; future present/CD overlap needs an explicit cost model or group metadata, not opportunistic reads after preparation. |
| 2026-04-26 | `prepared-present-min-slack-5` | dirty experiment after `dd8432c2` | Raise held-slack prepared-present from `4` to `5` VBlanks to reduce duplicate speculative restore/compose work without adding new CD reads. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed and source reverted. It saved only three speculative prep calls (`restore_calls/compose_calls 188 -> 185`) while regressing active timing: `loop_vb 1227 -> 1228`, `overrun_vb 150 -> 151`, `blocking_vb 6 -> 8`, and `prefetch_overrun_vb 6 -> 8`; correctness stayed clean with `hits=155` and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-053903/summary.json`. | Do not promote. The `4` VBlank prepared-present bridge is still the local cadence knee; reducing prep frequency hurts CD/refill coverage more than it saves CPU work. |
| 2026-04-26 | `pad-diagnostics-compile-gate` | dirty experiment after `7c5e3cba` | Compile the heavy JCPAD/JCSPI diagnostic print block out of the default release path with a `PS1_ENABLE_PAD_DIAGNOSTICS` macro, trying to shrink `eventsWaitTick()` and executable size while leaving a re-enable hook. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed and source reverted. It was a real size win (`jcreborn.exe 137216 -> 133120`, `jcreborn.elf 707916 -> 700304`) but regressed active timing: `loop_vb 1227 -> 1232`, `overrun_vb 150 -> 155`, `blocking_vb 6 -> 12`, and `prefetch_overrun_vb 6 -> 12`; correctness stayed clean and `restore_calls/compose_calls` dropped `188 -> 183`. Artifact: `scratch/ps1-perf-iterate/20260426-054255/summary.json`. | Do not promote. Even apparently cold code-shape changes can perturb the deterministic scheduler; retry diagnostics slimming only with a layout/section strategy that proves timing flat. |
| 2026-04-26 | `dirty-row-pointer-swap` | dirty experiment after `4a2b8e5d` | Replace the end-of-frame dirty-row table `memcpy()` with pointer-swapped current/previous row buffers, removing fixed CPU/memory-copy work from every rendered FG2 frame. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. The total loop improved by one VBlank (`loop_vb 1227 -> 1226`, `overrun_vb 150 -> 149`) and binary size shrank (`jcreborn.exe 137216 -> 135168`, `jcreborn.elf 707916 -> 707224`), but visible CD/refill pressure regressed (`blocking_vb 6 -> 9`, `prefetch_overrun_vb 6 -> 9`) with fewer speculative prep calls (`restore_calls/compose_calls 188 -> 177`). Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-055031/summary.json`. | Do not promote under the current gate. The one-VBlank loop win is tempting, but it is coming from a scheduler phase shift that makes more CD time visible; retry after grouped/predictive CD scheduling can absorb the phase change. |
| 2026-04-26 | `clean-rect-dirty-y-narrowing` | dirty experiment after `8d63d829` | Narrow `grCleanRectCopyIn()` to the previous dirty Y ranges for the clean rect's touched tiles, avoiding a full saved-rect-height row scan when only a subset of rows was dirty. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed/no promotion and source reverted. Timing, CD, prefetch, gfx work identity, and correctness all matched the accepted baseline exactly (`loop_vb=1227`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `restore_calls=188`, `upload_calls=155`), while ELF size grew (`707916 -> 709856`). Artifact: `scratch/ps1-perf-iterate/20260426-055525/summary.json`. | Do not promote. The row-scan narrowing adds code without moving VBlank-level or tracked work metrics; revisit only with finer CPU counters or pack-emitted restore bands. |
| 2026-04-26 | `adaptive-high-slack-24kb-window` | dirty experiment after `6f7304af` | Keep the default stream window buffer at `24 KB`, but read only the accepted `16 KB` base window unless a lookahead prefetch has at least `8` held VBlanks of slack. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It lowered total active reads (`68 -> 56`) and loop CD read time (`285 -> 268`), but visible playback regressed: `loop_vb 1227 -> 1230`, `overrun_vb 150 -> 153`, `blocking_vb 6 -> 9`, and `prefetch_overrun_vb 6 -> 9`; correctness stayed clean with `due_misses=0`, `hits=155`, and no fallback/tripwire counters. Artifact: `scratch/ps1-perf-iterate/20260426-060022/summary.json`. | Do not promote. Coarser high-slack reads can improve hidden CD efficiency, but the deterministic cadence still exposes more refill time; retry only with pack/group metadata or a read-cost predictor that keeps visible blocking flat. |
| 2026-04-26 | `adaptive-high-slack-20kb-window` | dirty experiment after `3a8a42f4` | Try a smaller adaptive extension than the failed `24 KB` probe: keep normal refills at `16 KB`, but let high-slack lookahead prefetches read up to `20 KB`. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed and source reverted. It reduced active reads (`68 -> 59`), loop CD read time (`285 -> 282`), and speculative restore/compose calls (`188 -> 180`), but still regressed visible playback: `loop_vb 1227 -> 1228`, `blocking_vb 6 -> 8`, and `prefetch_overrun_vb 6 -> 8`. Correctness stayed clean with `due_misses=0`, `hits=155`, and no fallback/tripwire counters. Artifact: `scratch/ps1-perf-iterate/20260426-060653/summary.json`. | Do not promote. This confirms the problem is not just the `24 KB` size; adaptive larger windows steal visible cadence even when they improve hidden CD work. Avoid more raw extended-window probes until group/read-cost metadata exists. |
| 2026-04-26 | `prepared-buffer-release` | dirty experiment after `3a4c4d06` | After pre-composing a staged frame into RAM background tiles, release the staged payload buffer for future work and advance prepared frames from saved metadata instead of consuming the staged buffer at present time. | `./scripts/build-ps1.sh`, then two variants of `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. Variant A also staged the following frame from an already-resident window during prepared wait: it removed duplicate restore/compose work (`188 -> 155`) but regressed `loop_vb 1227 -> 1228`, `blocking_vb 6 -> 7`, `prefetch_overrun_vb 6 -> 7`, and ended with a suspect `last_frame=129`. Variant B only released the buffer and waited: it caused starvation (`due_misses 0 -> 14`, `blocking_vb 6 -> 72`, `loop_reads 68 -> 85`). Artifacts: `scratch/ps1-perf-iterate/20260426-061204/summary.json`, `scratch/ps1-perf-iterate/20260426-061347/summary.json`. | Do not promote. The duplicate prepared restore/compose work is currently acting as scheduler ballast; buffer release needs a first-class prepared-frame scheduler, not a local ownership swap. |
| 2026-04-26 | `legacy-fg-diagnostic-scene-gate` | dirty experiment after `2dc3c66d` | Compile legacy foreground diagnostic scene modes (`testcard`, raw title/isle/ocean probes, solid red) out of the default PS1 build so section-GC can drop unused public-cleanup code without changing FG2 playback. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as binary-size hygiene. Timing, CD, prefetch, graphics work identity, and correctness all matched baseline exactly: `loop_vb=1227`, `overrun_vb=150`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `restore_calls=188`, `compose_calls=188`, `upload_calls=155`, `due_misses=0`. Size improved materially: `jcreborn.exe 137216 -> 131072`, `jcreborn.elf 707916 -> 692612`. Artifact: `scratch/ps1-perf-iterate/20260426-062435/summary.json`. | Promote. This crosses the executable below the next 2 KB sector bucket with no cadence or correctness regression; keep legacy diagnostics available only by explicitly re-enabling the compile-time gate if needed. |
| 2026-04-26 | `pal4-direct-dirty-row-marking` | dirty experiment after `f9188508` | In the FG2 PAL4 span compositor, replace generic `grMarkRectDirty()` calls with direct tile-row dirty marking because the hot path already knows row, tile side, and clipped X bounds. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It reduced speculative restore/compose calls (`188 -> 185`) but regressed visible timing: `loop_vb 1227 -> 1229`, `overrun_vb 150 -> 152`, `blocking_vb 6 -> 10`, and `prefetch_overrun_vb 6 -> 10`; no key metric improved and ELF grew `692612 -> 694760`. Correctness stayed clean with `hits=155` and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-063021/summary.json`. | Do not promote. The generic dirty mapper is part of the accepted cadence shape; local CPU-looking cleanup can expose CD/refill time unless the scheduler has a stronger cost model. |
| 2026-04-26 | `visual-debug-screen-compile-gate` | dirty experiment after `dfe68be1` | Compile the old visual debug screen/buffer/wait implementation out by default, keep only boot font-stream initialization for pause-menu compatibility, and route fatal debug errors through printf. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Failed and source reverted. It was a strong size win (`jcreborn.exe 131072 -> 126976`, `jcreborn.elf 692612 -> 686308`), but it regressed active cadence: `loop_vb 1227 -> 1232`, `overrun_vb 150 -> 155`, `blocking_vb 6 -> 12`, and `prefetch_overrun_vb 6 -> 12`. Correctness stayed clean with `hits=155` and `due_misses=0`; speculative restore/compose dropped `188 -> 183`. Artifact: `scratch/ps1-perf-iterate/20260426-063440/summary.json`. | Do not promote. The early debug/font/GPU setup code shape still influences later deterministic CD phase; retry only with an explicit startup phase barrier or after the scheduler no longer depends on boot cadence. |
| 2026-04-26 | `prepared-present-min-slack-3` | dirty experiment after `58f35db3` | Lower held-slack prepared-present from `4` to `3` VBlanks to see whether extra prepared-frame ballast hides the remaining visible CD/refill VBlanks. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It increased speculative prep (`restore_calls/compose_calls 188 -> 193`) and regressed active cadence: `loop_vb 1227 -> 1231`, `overrun_vb 150 -> 154`, `blocking_vb 6 -> 10`, and `prefetch_overrun_vb 6 -> 10`; no key metric improved. Correctness stayed clean with `hits=155` and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-063951/summary.json`. | Do not promote. The accepted `4` VBlank prepared-present bridge remains the local knee from both directions; threshold-only prepared-present tuning is exhausted without a stronger scheduler signal. |
| 2026-04-26 | `stream-window-grow-capacity-24kb` | dirty experiment after `7423cff4` | Keep the accepted `16 KB` stream-window read size, but allocate a `24 KB` window and append sector tails before sliding so small reads can reduce refill churn without the old larger single-read stalls. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It shaved one total loop VBlank (`loop_vb 1227 -> 1226`, `overrun_vb 150 -> 149`) and reduced speculative prep (`restore_calls/compose_calls 188 -> 185`), but due misses returned (`0 -> 3`) and visible CD blocking regressed badly (`blocking_vb 6 -> 16`). It also increased read churn (`loop_reads 68 -> 80`, `seek_back 5 -> 8`) and prefetch memory (`23568 -> 31760`). Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-064648/summary.json`. | Do not promote. Larger retained coverage without a group scheduler creates more small transactions and starvation; retry only as pack-emitted groups or a costed read scheduler, not raw append growth. |
| 2026-04-26 | `fg2-payload-16kb-group-align` | generated-pack experiment after `2f500282` | Repack `FISHING1.FG2` so no payload crosses a `16 KB` payload group boundary, increasing pack size by `92413` bytes but letting the runtime's accepted `16 KB` window read complete groups instead of straddling arbitrary payload boundaries. | Generated a temporary grouped `generated/ps1/foreground/FISHING1.FG2`, then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`; restored the original pack afterward. | Failed and pack restored. Reads dropped (`loop_reads 68 -> 56`, `seek_back 5 -> 3`) and due misses stayed zero, but extra sectors and poorer cadence regressed active playback: `loop_vb 1227 -> 1241`, `blocking_vb 6 -> 22`, `prefetch_overrun_vb 6 -> 22`, `loop_read_vb 285 -> 299`, and `pack_bytes 829851 -> 922264`. Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-065124/summary.json`. | Do not promote. Raw padding reduces transaction count but increases bytes/sectors enough to lose; future pack grouping must be selective and cost-aware, not fixed-boundary padding. |
| 2026-04-26 | `fg2-payload-start-sector-align` | generated-pack experiment after `6efe2105` | Add only `904` bytes of padding between the FG2 entry table and payloads so `dataOffset` starts on the next CD sector, testing whether a cheap sector-aligned payload base improves the accepted stream-window cadence. | Generated a temporary sector-aligned `generated/ps1/foreground/FISHING1.FG2`, then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`; restored the original pack afterward. | Failed and pack restored. The tiny offset shift kept sectors flat but regressed cadence: `loop_vb 1227 -> 1230`, `blocking_vb 6 -> 12`, `prefetch_overrun_vb 6 -> 12`, and `loop_read_vb 285 -> 305`; speculative prep dropped (`restore_calls/compose_calls 188 -> 183`) but no key metric improved. Correctness stayed clean with `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-065400/summary.json`. | Do not promote. The current payload byte offsets are part of the deterministic sweet spot; even small global shifts need a stronger scheduler model before becoming safe. |
| 2026-04-26 | `fg2-pause-poll-only-events` | dirty experiment after `464e396c` | Split PS1 pause polling out of `eventsWaitTick(0)` and use the lighter pause-only path for FG2 held/prepared waits, avoiding generic delay-anchor updates that FG2 does not use for timing. | `./scripts/build-ps1.sh`, then `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. | Failed and source reverted. It reduced speculative prep (`restore_calls/compose_calls 188 -> 183`) and kept correctness clean, but regressed the accepted cadence by one VBlank: `loop_vb 1227 -> 1228`, `blocking_vb 6 -> 7`, and `prefetch_overrun_vb 6 -> 7`; no key metric improved. Artifact: `scratch/ps1-perf-iterate/20260426-071224/summary.json`. | Do not promote. The generic zero-delay event tail is still acting as part of the FG2 pacing shape; remove it only after a replacement scheduler deliberately preserves CD/refill phase. |
| 2026-04-26 | `visual-debug-screen-gate-layout-pad` | dirty experiment after `bcb92e32` | Re-test compile-gating the old visual debug screen, but pad the PS-EXE container back to `131072` bytes so the FG2 pack LBA stays fixed and the earlier miss can be separated from CD file-layout movement. | Temporarily compiled the visual debug screen/buffer/wait code out by default and padded `build-ps1/jcreborn.exe` after conversion; ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed with pack layout preserved. `FG\\FISHING1.FG2` stayed at LBA `390`, EXE container stayed `131072` bytes, and PS-EXE load size dropped to `126976`, but cadence matched the earlier miss: `loop_vb 1227 -> 1232`, `blocking_vb 6 -> 12`, `prefetch_overrun_vb 6 -> 12`, `restore_calls/compose_calls 188 -> 183`, correctness clean, and no key metric improved. Artifact: `scratch/ps1-perf-iterate/20260426-071938/summary.json`. | Do not promote. The visual-debug compile gate changes code/runtime phase enough to hurt FG2 cadence even when the foreground pack LBA is preserved; retry only with stronger scheduler barriers or a cold-section/linker placement strategy. |
| 2026-04-26 | `dirty-row-clear-memset` | dirty experiment after `56221e2a` | Replace the nested `grClearDirtyRows()` loops with two fixed-size `memset(..., 0xff, ...)` calls, targeting repeated dirty-state clearing without changing restore/upload policy. | Ran the standard fishing1 perf gate once directly, then reran with temporary PS-EXE layout padding so `FG\\FISHING1.FG2` stayed at LBA `390`; source and build-script padding were reverted after the runs. | Failed both ways. The unpadded run moved the pack to LBA `389` and regressed to `loop_vb=1233`, `blocking_vb=10`, `prefetch_overrun_vb=10`. The padded rerun preserved LBA `390` and produced the same miss: `loop_vb 1227 -> 1233`, `blocking_vb 6 -> 10`, `prefetch_overrun_vb 6 -> 10`, `restore_calls/compose_calls 188 -> 189`, correctness clean. Artifacts: `scratch/ps1-perf-iterate/20260426-072602/summary.json` and `scratch/ps1-perf-iterate/20260426-072804/summary.json`. | Do not promote. The hand-written loop is better for the current code shape; libc `memset` changes dirty/prepare cadence enough to lose even when CD layout is fixed. |
| 2026-04-26 | `setup-settle-prerender-first-frame` | dirty experiment after `07f9505f` | Use the accepted one-VBlank setup settle after consuming fishing1's leading empty entry to render the first real frame before `loop_start`, so active playback begins on a held frame instead of spending the first loop iteration restoring/composing/presenting it. | Temporarily deferred the setup settle until after clean-rect setup, rendered the first real frame there with `grRestoreBgFromRects()` + `grUpdateDisplay()`, then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed the strict gate. Active loop improved (`loop_vb 1227 -> 1220`, `overrun_vb 150 -> 143`, `render 155 -> 154`), but full scene time stayed flat (`scene_vb=1406`) and CD pressure regressed (`blocking_vb 6 -> 7`, `prefetch_overrun_vb 6 -> 7`). Correctness stayed clean with `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-075545/summary.json`. | Do not promote. Rendering inside setup can move work out of `loop_vb`, but it does not reduce total scene time and it perturbs the accepted CD/refill phase; retry only with a scheduler that preserves `blocking_vb=6` or improves `scene_vb`, not just active-loop accounting. |
| 2026-04-26 | `foreground-compose-ever-telemetry-removal` | dirty experiment after `3db0f380` | Remove the unused `foregroundPilotRuntimeComposedEver()` accessor, backing flag, and hot compose-path store to reduce per-render CPU work and code size. | Removed `gFgComposedEver`, the `gFgComposedEver = 1` store in `foregroundPilotRuntimeCompose()`, and the accessor declaration/definitions; then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed the strict gate as a neutral no-op: `loop_vb=1227`, `overrun_vb=150`, `blocking_vb=6`, `prefetch_overrun_vb=6`, and all visual-work/correctness counters matched baseline. Size red-team was not favorable enough: `jcreborn.exe` stayed `131072`, ELF file size changed `692612 -> 692436`, but loadable text grew `124732 -> 124760`. Artifact: `scratch/ps1-perf-iterate/20260426-080225/summary.json`. | Do not promote. This is dead-code cleanup in source terms, but it did not improve measured runtime and the text-size signal worsened; retry only during a broader telemetry/API removal pass where link layout can be controlled. |
| 2026-04-26 | `long-hold-deadline-catchup` | `ps1: catch up long host-deadline holds` | Host-deadline FG2 packs use absolute PC capture deadlines, so late PS1 work should be allowed to shorten later long holds slightly instead of always preserving the ideal per-frame hold and accumulating render/CD overhead. | Tested three variants. Full actual-elapsed catch-up failed at `scratch/ps1-perf-iterate/20260426-081227/summary.json` (`loop_vb 1227 -> 1195`, but `target_vb 1077 -> 924`, `blocking_vb 6 -> 103`, `due_misses=18`). One-VBlank catch-up on any late frame failed at `scratch/ps1-perf-iterate/20260426-081427/summary.json` (`loop_vb 1227 -> 1225`, but `blocking_vb 6 -> 57`, `due_misses=8`). The promoted variant catches up by at most one VBlank only when the intended hold is at least `5` VBlanks, then repeated the strict gate twice. | Promoted. Repeat runs matched exactly: `loop_vb 1227 -> 1222`, `scene_vb 1406 -> 1401`, `overrun_vb 150 -> 149`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `due_misses=0`, `hits=155`, and correctness stayed clean. It trades more speculative prep (`restore_calls/compose_calls 188 -> 195`, `restore_bytes 2999408 -> 3130442`) for fewer held-loop iterations (`668 -> 655`). Artifacts: `scratch/ps1-perf-iterate/20260426-081609/summary.json`, `scratch/ps1-perf-iterate/20260426-081743/summary.json`. | Promote. This is an absolute-deadline timing fix, not frame skipping: entries, rendered frames, upload calls, sound events, and tripwires stayed identical. Keep the long-hold guard; broader catch-up starves prefetch. |
| 2026-04-26 | `long-hold-catchup-threshold-4` | dirty experiment after `d05d961e` | Lower the accepted long-hold catch-up threshold from `5` to `4` VBlanks to see whether more absolute-deadline catch-up can be reclaimed after the first accepted timing fix. | Changed the guard to `frameHoldVBlanks >= 4`, then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed. It produced no loop win (`loop_vb=1222`) while shrinking `target_vb 1073 -> 1069` and increasing visible CD pressure (`blocking_vb 6 -> 9`, `prefetch_overrun_vb 6 -> 9`). Correctness stayed clean and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-082221/summary.json`. | Do not promote. The accepted threshold `5` is the current knee; lowering it spends prefetch slack without reducing active-loop time. |
| 2026-04-26 | `prepared-present-threshold-5-post-catchup` | dirty experiment after `65d4f29a` | The accepted catch-up rule increased speculative restore/compose work, so re-test raising prepared-present minimum slack from `4` to `5` VBlanks to reduce duplicate prep after the new timing baseline. | Changed `FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` to `5`, then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed. It reduced prep work (`restore_calls/compose_calls 195 -> 182`) and nominal loop time by one VBlank (`1222 -> 1221`), but shrank target to `1069`, regressed `overrun_vb 149 -> 152`, and exposed CD pressure (`blocking_vb 6 -> 10`, `prefetch_overrun_vb 6 -> 10`). Correctness stayed clean with `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-082533/summary.json`. | Do not promote. Duplicate prepared-frame work is still scheduler ballast; threshold-only pruning remains unsafe until CD prefetch has a stronger cost model. |
| 2026-04-26 | `long-hold-extra-catchup-9vb` | dirty experiment after `dea8f412` | Test whether very long holds can absorb a second catch-up VBlank without harming prefetch, extending the accepted one-VBlank catch-up only for `frameHoldVBlanks >= 9`. | Tested two variants: require lateness beyond `hold+1`, then allow the second catch-up for any late `>=9` hold. Both ran the strict fishing1 gate and source was reverted afterward. | Failed as an exact no-op. Both variants matched the accepted baseline exactly: `loop_vb=1222`, `target_vb=1073`, `overrun_vb=149`, `blocking_vb=6`, `prefetch_overrun_vb=6`, and work identity/correctness counters were unchanged. Artifacts: `scratch/ps1-perf-iterate/20260426-083151/summary.json`, `scratch/ps1-perf-iterate/20260426-083338/summary.json`. | Do not promote. Fishing1 does not expose useful extra catch-up at the `>=9` hold bucket under the current scheduler; move to a different optimization area. |
| 2026-04-26 | `fg2-metadata-prefix-4kb` | dirty experiment after `a390c4a0` | Lower the initial coalesced FG2 metadata read from `8192` to `4096` bytes, relying on the existing exact tail-read path only for packs whose entry tables exceed 4 KB. Fishing1 metadata is `3192` bytes, so this should reduce setup overread without moving payload offsets. | Changed `FG_PACK_METADATA_PREFIX_BYTES` to `4096`, then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed. Setup bytes/sectors dropped (`setup_bytes 282104 -> 278008`, total sectors `541 -> 539`) and setup timing barely improved (`pack_start_vb 42 -> 41`, `setup_read_vb 108 -> 107`), but active playback regressed: `loop_vb 1222 -> 1224`, `target_vb 1073 -> 1071`, `overrun_vb 149 -> 153`, `blocking_vb 6 -> 11`, and `prefetch_overrun_vb 6 -> 11`. Correctness stayed clean with `due_misses=0`; speculative prep shifted (`restore_calls/compose_calls 195 -> 184`). Artifact: `scratch/ps1-perf-iterate/20260426-084030/summary.json`. | Do not promote. The 8 KB prefix overread is currently acting as deterministic startup/CD-phase ballast; reducing bytes alone changes the prepared-frame cadence enough to hurt playback. Retry only with an explicit setup-to-loop phase barrier or layout/group metadata that makes the later cadence independent of startup read shape. |
| 2026-04-26 | `lookahead-covered-short-catchup` | dirty experiment after `02e1d0c4` | Extend the accepted one-VBlank host-deadline catch-up to shorter holds only when the immediate next frame is staged and the following payload is already covered by the stream window, attempting to reclaim deadline drift without spending unknown CD slack. | Added a staged/lookahead coverage helper, then tested `frameHoldVBlanks >= 3` and a tighter `>=4` variant with the strict command `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after both runs. | Failed. Both variants produced the same result: `loop_vb=1222`, `target_vb 1073 -> 1071`, `overrun_vb 149 -> 151`, `blocking_vb 6 -> 8`, `prefetch_overrun_vb 6 -> 8`, `due_misses=0`, and visual work identity unchanged (`restore_calls/compose_calls=195`, `upload_calls=155`). Artifacts: `scratch/ps1-perf-iterate/20260426-084902/summary.json`, `scratch/ps1-perf-iterate/20260426-085036/summary.json`. | Do not promote. Stream-window coverage is not a sufficient proof of spare cadence; even the covered short-hold catches perturb hidden CD/read phase. Keep the accepted long-hold-only guard until a scheduler can budget render-prep and CD lookahead separately. |
| 2026-04-26 | `fg2-no-holiday-compose-fast-path` | dirty experiment after `0be6d460` | Cache whether the current scene has a holiday overlay so non-holiday fishing1 can skip the per-compose `fgBackdropStampHoliday()` gate chain while preserving holiday variants. | Added `stampHolidayOverlay` to the FG runtime, set it at scene start, and called `fgBackdropStampHoliday()` only when set; then ran `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2 --require-improvement`. Source was reverted after the run. | Failed as an exact timing no-op: `loop_vb=1222`, `target_vb=1073`, `overrun_vb=149`, `blocking_vb=6`, `prefetch_overrun_vb=6`, and all visual/correctness counters matched. Size was unfavorable: `jcreborn.elf 692704 -> 692924`. Artifact: `scratch/ps1-perf-iterate/20260426-085425/summary.json`. | Do not promote. The existing holiday gate is below VBlank resolution and the cached flag worsens code size/layout; revisit only as part of a generated per-variant compose path or a broader holiday-overlay cleanup. |
| 2026-04-26 | `foreground-ever-diagnostics-removal` | `ps1: remove unused foreground ever diagnostics` | Remove the old foreground "ever" diagnostic accessors, backing flags, and hot-path stores now that printf/JCPERF2 logs are the authoritative diagnostic surface. This should reduce code size without touching playback behavior. | Removed the unused API from `foreground_pilot.h` and both PS1/non-PS1 implementations, then ran the strict fishing1 gate twice with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted. Both runs matched baseline timing and work identity exactly: `loop_vb=1222`, `target_vb=1073`, `overrun_vb=149`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `restore_calls/compose_calls=195`, `upload_calls=155`, and correctness clean. `jcreborn.elf` shrank `692704 -> 691584` bytes while `jcreborn.exe` stayed in the `131072` byte bucket. Artifacts: `scratch/ps1-perf-iterate/20260426-085917/summary.json`, `scratch/ps1-perf-iterate/20260426-090044/summary.json`. | Promote. This is not a VBlank win, but it is a safe public-build cleanup and binary-size reduction with flat deterministic playback. Keep removing unused diagnostic surfaces only when the headless cadence gate remains flat. |
| 2026-04-26 | `ads-foreground-start-hook-removal` | `ps1: remove unused ADS foreground start hook` | Remove the always-false ADS foreground auto-start hook and the now-unused requested-start wrappers, preserving the explicit PS1 `FGPILOT` debug ADS path. This should reduce code size without changing active FG2 scene playback. | Removed the hook declarations/definitions and the dead branch in `adsPlay()`, then ran the strict fishing1 gate twice with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted. Both runs matched timing and work identity exactly: `loop_vb=1222`, `target_vb=1073`, `overrun_vb=149`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `restore_calls/compose_calls=195`, `upload_calls=155`, and correctness clean. `jcreborn.elf` shrank `691584 -> 690932` bytes while `jcreborn.exe` stayed in the `131072` byte bucket. Artifacts: `scratch/ps1-perf-iterate/20260426-090423/summary.json`, `scratch/ps1-perf-iterate/20260426-090548/summary.json`. | Promote. This is another flat-cadence public-build cleanup; keep the explicit `FGPILOT` ADS debug path until the legacy diagnostic-scene cleanup removes or replaces it deliberately. |
| 2026-04-26 | `fgpilot-ads-debug-dispatch-removal` | `ps1: remove obsolete FGPILOT ADS dispatch` | Remove the obsolete PS1-only `FGPILOT` ADS debug dispatch and make `foregroundPilotRuntimeStart()` file-local; direct `fgpilot` boot now owns scene launch and this ADS shim is no longer part of the runtime methodology. | Removed the `FGPILOT` branch in `adsPlay()`, removed the public runtime-start declaration, made the implementation `static`, then ran the strict fishing1 gate twice with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted. Both runs matched timing and work identity exactly: `loop_vb=1222`, `target_vb=1073`, `overrun_vb=149`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `restore_calls/compose_calls=195`, `upload_calls=155`, and correctness clean. `jcreborn.exe` shrank `131072 -> 129024`; `jcreborn.elf` moved `690932 -> 691940` from link-layout noise. Artifacts: `scratch/ps1-perf-iterate/20260426-091242/summary.json`, `scratch/ps1-perf-iterate/20260426-091407/summary.json`. | Promote. The shipped PS-EXE crosses down one `2 KB` bucket with flat deterministic playback; future public cleanup can remove more legacy diagnostic paths only under the same flat-cadence gate. |
| 2026-04-26 | `fgpilot-status-accessor-removal` | `ps1: remove unused foreground status accessors` | Remove public foreground-pilot status accessors that no source caller uses anymore, leaving only the runtime/pause/debug-info APIs that are still active. | Removed the unused `foregroundPilotRequested*`, scene-name, runtime-mode, source-frame, display-VBlank, and frame-data accessors from the PS1 and non-PS1 foreground-pilot implementations, then ran the strict fishing1 gate twice with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted. Both runs matched exactly: `loop_vb 1222 -> 1221`, `target_vb 1073 -> 1072`, `overrun_vb=149`, `blocking_vb 6 -> 5`, `prefetch_overrun_vb 6 -> 5`, `restore_calls/compose_calls 195 -> 193`, `upload_calls=155`, and correctness clean. Normal PS1 build size is `jcreborn.exe=129024`, `jcreborn.elf=690936`. Artifacts: `scratch/ps1-perf-iterate/20260426-091827/summary.json`, `scratch/ps1-perf-iterate/20260426-092210/summary.json`. | Promote. This is a small deterministic cleanup win: active overrun is flat, but one active-loop VBlank and one visible CD-pressure VBlank are gone, and the public foreground-pilot API is narrower. |
| 2026-04-26 | `fgpilot-requested-mode-state-removal` | `ps1: remove dead foreground requested-mode state` | After removing the requested/status accessors, `gForegroundPilotRequestedMode` became write-only and `fgSceneModeForName()` existed only to populate it. Remove that dead state and helper. | Removed the PS1 and non-PS1 requested-mode globals, stopped setting them in `foregroundPilotSetScene()`, removed `fgSceneModeForName()`, then ran the strict fishing1 gate twice with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as flat-cadence size/API cleanup. Both runs matched baseline exactly: `loop_vb=1221`, `target_vb=1072`, `overrun_vb=149`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls=193`, `upload_calls=155`, and correctness clean. Normal PS1 build ELF shrank `690936 -> 690724`; PS-EXE stayed `129024`. Artifacts: `scratch/ps1-perf-iterate/20260426-092828/summary.json`, `scratch/ps1-perf-iterate/20260426-092951/summary.json`. | Promote. No new VBlank win, but the public-methodology cleanup continues to reduce dead code with deterministic playback unchanged. |
| 2026-04-26 | `base-diff-pack-requirement` | `ps1: require base-diff foreground packs` | All generated FG2 packs now use the proven base-diff methodology, so reject non-base-diff packs at scene start and remove non-base-diff fallback branches from the active runtime loop. | Verified the generated foreground directory contains `126` FG2 packs and all carry the base-diff flag, then removed the runtime `fgRuntimeUsesBaseDiffBackdrop()` checks, non-base-diff per-frame OT/wave branches, and now-unused wave tick helper. Ran the strict fishing1 gate twice with `./scripts/ps1-perf-iterate.sh --scene fishing1 --frames 4200 --timeout 180 --baseline scratch/ps1-perf-iterate/current-main-baseline.json --allow-regression 2`. | Promoted as flat-cadence fallback removal. Both final-source runs matched baseline exactly: `loop_vb=1221`, `target_vb=1072`, `overrun_vb=149`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls=193`, `upload_calls=155`, and correctness clean. Normal PS1 build ELF shrank `690724 -> 689748`; PS-EXE stayed `129024`. Artifacts: `scratch/ps1-perf-iterate/20260426-093613/summary.json`, `scratch/ps1-perf-iterate/20260426-093739/summary.json`. | Promote. This makes the current FG2 full-diff/base-diff method an enforced contract instead of a fallback path, with deterministic playback unchanged and less runtime code. |
| 2026-04-26 | `pad-diagnostics-gate-restore-post-pause-merge` | `ps1: keep pad diagnostics opt-in after pause merge` | The pause/menu merge reintroduced the always-on JCPAD/JCSPI diagnostic path in `eventsWaitTick()` and the SPI callback. Normal playback needs Start polling, not per-frame diagnostic sampling, COP0/SPI snapshots, and periodic log dumps. | Re-added `eventsSetPadDiagnostics()`, kept `pad-diag`/`pad-debug` as the opt-in boot/CLI switch, gated the SPI button tracker, startup probes, per-frame diagnostic block, and Start-path diagnostic prints, then ran the exact no-holiday fishing1 gate against the fresh post-merge baseline: `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --baseline scratch/ps1-perf-iterate/current-branch-baseline.json --allow-regression 2 --require-improvement --timeout 180`. | Promoted. The exact post-merge baseline was `loop_vb=1306`, `target_vb=1050`, `overrun_vb=256`, `blocking_vb=19`, `prefetch_overrun_vb=14`, and `due_misses=1`. The restored diagnostics gate recovered the accepted cadence: `loop_vb 1306 -> 1221`, `target_vb 1050 -> 1072`, `overrun_vb 256 -> 149`, `blocking_vb 19 -> 5`, `prefetch_overrun_vb 14 -> 5`, `due_misses 1 -> 0`, with clean correctness. A repeat gate matched exactly at `loop_vb=1221`, `blocking_vb=5`, and `prefetch_overrun_vb=5`. Artifacts: `scratch/ps1-perf-iterate/20260426-104412/summary.json`, `scratch/ps1-perf-iterate/20260426-104702/summary.json`. | Promote. This fixes the integration regression without removing pause/menu functionality: Start polling remains active by default, and the heavy controller diagnostics remain available only when explicitly requested. |
| 2026-04-26 | `fg2-window-18kb-post-pause` | dirty experiment after `4e60bfb6` | Re-test a larger `18 KB` raw stream window after the pause/menu merge and diagnostics gate recovery to see whether the CD cadence knee moved. | `PS1_PERF_FRAMES=4200 ./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 prefetch-window 18432 perf-log noloop seed 1" --baseline scratch/ps1-perf-iterate/current-branch-baseline.json --allow-regression 2 --require-improvement --timeout 180`. | Failed. The larger window regressed active playback and visible CD pressure: `loop_vb 1221 -> 1238`, `blocking_vb 5 -> 25`, `prefetch_overrun_vb 5 -> 25`, and `target_vb 1072 -> 1065`; `due_misses` stayed `0`. Artifact: `scratch/ps1-perf-iterate/20260426-105223/summary.json`. | Do not promote. The post-pause cadence still wants the `16 KB` default; larger raw windows steal held-frame slack even when due-frame coverage is clean. |
| 2026-04-26 | `fg2-window-14kb-post-pause` | dirty experiment after `4e60bfb6` | Re-test a smaller `14 KB` raw stream window after the pause/menu merge and diagnostics gate recovery to see whether refill overrun can drop without starving due frames. | `PS1_PERF_FRAMES=4200 ./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 prefetch-window 14336 perf-log noloop seed 1" --baseline scratch/ps1-perf-iterate/current-branch-baseline.json --allow-regression 2 --require-improvement --timeout 180`. | Failed. The smaller window regressed `loop_vb 1221 -> 1224`, raised `blocking_vb 5 -> 43`, raised `prefetch_overrun_vb 5 -> 9`, and introduced `due_misses 0 -> 12` with fewer hits (`155 -> 143`). Artifact: `scratch/ps1-perf-iterate/20260426-105350/summary.json`. | Do not promote. Smaller raw windows are still coverage-starved; revisit only with pack groups or a read-cost scheduler that preserves near-term entries. |
| 2026-04-26 | `upload-band-gap-0-post-pause` | dirty experiment after `4e60bfb6` | Remove clean-gap merging between dirty upload bands so each upload transfers fewer bytes, testing whether byte volume dominates over per-rectangle pressure. | Changed `GR_UPLOAD_BAND_MERGE_GAP` to `0`, then ran the exact no-holiday fishing1 gate against `scratch/ps1-perf-iterate/current-branch-baseline.json`. Source was reverted to a bounded merge after the run. | Failed. Upload bytes fell (`16499200 -> 16273280`) but rect pressure and visible timing worsened: `upload_rects 401 -> 515`, `loop_vb 1221 -> 1224`, `blocking_vb 5 -> 10`, and `prefetch_overrun_vb 5 -> 10`; speculative work dropped to `restore_calls/compose_calls=188` but did not produce a safe speed win. Artifact: `scratch/ps1-perf-iterate/20260426-105733/summary.json`. | Do not promote. Exact band splitting trades too many upload rectangles for too little byte reduction; use a bounded clean-gap merge unless a future metric proves rect setup is cheap. |
| 2026-04-26 | `cd-sequential-setloc-skip-post-pause` | dirty experiment after `4e60bfb6` | Skip explicit `CdlSetloc` for sequential CD reads and let the drive continue from the previous sector, reducing command traffic on stream-window reads. | Added a CD stream-continuation state plus detailed setloc accounting, built, then ran the exact no-holiday fishing1 strict gate. Source was fully reverted after red-team review. | Rejected as invalid. It appeared to improve `loop_vb 1221 -> 1217` and dropped `setloc 74 -> 8`, but visual workload identity collapsed (`compose_calls 193 -> 6`, `upload_bytes 16499200 -> 1919360`, `upload_rects 401 -> 55`) and CD pressure worsened (`blocking_vb 5 -> 8`, `prefetch_overrun_vb 5 -> 8`). Artifact: `scratch/ps1-perf-iterate/20260426-110257/summary.json`. | Do not promote. The coarse correctness counters did not catch the workload corruption; this path needs lower-level CD proof plus full frame/work-identity gates before any retry. |
| 2026-04-26 | `prepared-exact4-upload-gap4` | `ps1: trim prepared render work` | Combine exact-`4` VBlank prepared-frame gating with a `4`-row upload-band merge gap to remove duplicate RAM prep and trim upload bytes while keeping key timing flat. | First ran strict `--require-improvement` checks for the exact-`4` prepare gate and the combined change, then ran a no-require acceptance gate: `PS1_PERF_FRAMES=4200 ./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --baseline scratch/ps1-perf-iterate/current-branch-baseline.json --allow-regression 2 --timeout 180`. | Promoted as a bounded work-reduction checkpoint. Key pressure stayed flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`), while duplicate render prep and upload volume dropped: `restore_calls/compose_calls 193 -> 166`, `restore_bytes 3085148 -> 2701496`, `upload_bytes 16499200 -> 16387840`, and `dirty_rows 25780 -> 25606`. Tradeoff: `target_vb 1072 -> 1071`, `overrun_vb 149 -> 150`, and `upload_rects 401 -> 421`. Artifacts: `scratch/ps1-perf-iterate/20260426-110702/summary.json`, `scratch/ps1-perf-iterate/20260426-110856/summary.json`, `scratch/ps1-perf-iterate/20260426-111027/summary.json`. | Promote, but do not claim a VBlank speed win. This reduces deterministic RAM/render/upload work with clean correctness and flat visible CD pressure, giving the next scheduler or pack-layout pass less work to carry. |
| 2026-04-26 | `upload-band-gap-2-post-pause` | `ps1: tighten upload band merge gap` | Narrow the post-pause upload-band merge gap from `4` rows to `2` rows, testing whether some of the zero-gap byte savings can be recovered without the zero-gap rectangle/timing regression. | Changed `GR_UPLOAD_BAND_MERGE_GAP` to `2`, then ran the exact no-holiday fishing1 gate twice against `scratch/ps1-perf-iterate/current-branch-baseline.json` with `--allow-regression 2`. | Promoted as a repeatable work-volume micro-optimization. Both runs kept key timing flat (`loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`) while reducing upload volume: `upload_bytes 16387840 -> 16381440` and `dirty_rows 25606 -> 25596`. Tradeoff: `upload_rects 421 -> 424`, still below the zero-gap `515` rect failure. Artifacts: `scratch/ps1-perf-iterate/20260426-111913/summary.json`, `scratch/ps1-perf-iterate/20260426-112045/summary.json`. | Promote. This is not a VBlank speed win, but it recovers `6400` more upload bytes at unchanged visible timing and bounded rectangle pressure. |
| 2026-04-26 | `upload-band-gap-1-post-pause` | `ps1: narrow upload band merge gap` | Narrow the post-pause upload-band merge gap from `2` rows to `1` row, probing the byte-saving side of the boundary just above the failed zero-gap split. | Changed `GR_UPLOAD_BAND_MERGE_GAP` to `1`, then ran the exact no-holiday fishing1 gate twice against `scratch/ps1-perf-iterate/current-branch-baseline.json` with `--allow-regression 2`. | Promoted as a repeatable work-volume micro-optimization. Both runs kept key timing flat (`loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`) while reducing upload volume materially: `upload_bytes 16381440 -> 16281600` and `dirty_rows 25596 -> 25440`. Tradeoff: `upload_rects 424 -> 502` and `max_upload_rects 5 -> 6`. Artifacts: `scratch/ps1-perf-iterate/20260426-112406/summary.json`, `scratch/ps1-perf-iterate/20260426-112536/summary.json`. | Promote, but mark the boundary: `0` rows is still rejected because it reached `515` rects and regressed visible timing. The current safe point for this variant is `1` row. |
| 2026-04-26 | `single-upload-rect-reuse` | dirty experiment after `173ea53a` | Replace the local `RECT[16]` upload scratch array with one reusable `RECT`, since each `LoadImage()` consumes the rectangle immediately. Expected benefit was lower stack pressure or smaller code without changing upload decisions. | Changed `rects[GR_MAX_UPLOAD_RECTS]` to a single `RECT`, ran the exact no-holiday fishing1 gate against `scratch/ps1-perf-iterate/current-branch-baseline.json`, then reverted the source. | Failed/no promotion. Runtime metrics matched the accepted baseline exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `restore_calls/compose_calls=166`), but binary size worsened slightly: `jcreborn.elf 713320 -> 713384` while PS-EXE stayed `137216`. Artifact: `scratch/ps1-perf-iterate/20260426-112912/summary.json`. | Do not promote. The theoretical stack cleanup is not visible in the current metrics and costs code size; retry only during a broader upload-stack/layout cleanup with map review. |
| 2026-04-26 | `prepared-wait-window-prefetch-post-gap1` | `ps1: prefetch during prepared-frame waits` | Retry prepared-wait stream-window prefetch after the exact-4 prepared gating and 1-row upload-gap reductions. The earlier version regressed by one VBlank, but the current prepared/upload workload is smaller. | Replaced the unconditional wait while `preparedFrameValid` with a future window prefetch attempt, then ran the exact no-holiday fishing1 gate once with `--require-improvement` and once without it against `scratch/ps1-perf-iterate/current-branch-baseline.json`. | Promoted as a work-reduction checkpoint. Key timing stayed flat (`loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`) while duplicate render prep dropped to the actual rendered-frame count: `restore_calls/compose_calls 166 -> 155`, `restore_bytes 2701496 -> 2510092`. CD submetrics also improved slightly without changing visible blocking: `loop_read_vb 289 -> 284`, `prefetch.used_vb 292 -> 285`, and `read_vb 398 -> 393`. Artifacts: `scratch/ps1-perf-iterate/20260426-113827/summary.json`, `scratch/ps1-perf-iterate/20260426-114009/summary.json`. | Promote. This is not a VBlank speed win, but it removes the remaining duplicate prepared restore/compose work for this scene while preserving the accepted visible cadence. |
| 2026-04-26 | `prepared-threshold-ge4-after-prepared-wait-prefetch` | dirty experiment after `bd360b0c` | After prepared-wait prefetch removed duplicate prep work, retest the older `>=4` prepared-frame threshold instead of the accepted exact-`4` threshold. | Changed `fgRuntimeCanPrepareStagedFrame()` from `== FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` to `>=`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted source. | Failed/no-op. Key timing and work identity matched the accepted baseline (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls=155`, `upload_bytes=16281600`), but bookkeeping shifted (`advances 951 -> 957`, `held 868 -> 874`, `late 105 -> 100`) with no promoted metric improvement. Artifact: `scratch/ps1-perf-iterate/20260426-114304/summary.json`. | Do not promote. Threshold widening is now exhausted in this scheduler shape; keep exact-`4` until a new prepared/CD budget changes measurable work or timing. |
| 2026-04-26 | `stage-copy-fallthrough-5vb-post-prepared-wait` | dirty experiment after `bd360b0c` | Lower the staged-copy fallthrough guard from `6` to `5` VBlanks after prepared-wait prefetch, checking whether more same-iteration lookahead can fit now that duplicate prep is gone. | Changed `FG_PREFETCH_FALLTHROUGH_MIN_SLACK_VBLANKS` to `5`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted source. | Failed. Total loop stayed flat but target/pressure regressed: `target_vb 1071 -> 1070`, `overrun_vb 150 -> 151`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 300`, and `prefetch.used_vb 285 -> 305`; correctness stayed clean with `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-114534/summary.json`. | Do not promote. The `6` VBlank fallthrough guard remains the lower safe bound under the current CD/read-cost model. |
| 2026-04-26 | `long-hold-catchup-threshold-4-post-prepared-wait` | dirty experiment after `bd360b0c` | Lower the long-hold host-deadline catch-up threshold from `5` to `4` VBlanks after prepared-wait prefetch, testing whether less duplicate render work gives the timing catch-up enough slack. | Changed the catch-up guard to `frameHoldVBlanks >= 4`, ran the exact no-holiday fishing1 gate, then reverted source. The wrapper exited `137` after complete `JCPERF2` scene-end metrics, so the artifact is the per-case `perf-summary.json`. | Failed. Metrics were complete and clearly worse: `target_vb 1071 -> 1065`, `overrun_vb 150 -> 156`, `blocking_vb 5 -> 9`, `prefetch_overrun_vb 5 -> 9`, `blocking_reads 4 -> 8`, and `late 105 -> 111`; correctness stayed clean with `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/20260426-114850/fishing1/perf-summary.json`. | Do not promote. Lowering catch-up still starves CD cadence even after duplicate prep removal; keep threshold `5` until the scheduler has grouped reads or an explicit slack budget. |
| 2026-04-26 | `prefetch-wouldread-probe-removal` | dirty experiment after `544f95a0` | Remove the separate `fgRuntimeWindowPrefetchWouldRead()` probe and let `fgRuntimeTryPrefetchWindow()` answer whether a read is needed once, reducing redundant held-frame entry/window checks. | Deleted the probe helper, made prepared/staged/fallthrough paths call `fgRuntimeTryPrefetchWindow()` directly, ran the exact no-holiday fishing1 gate, then reverted source. | Failed. The redundant probe is cadence ballast: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, `prefetch.used_vb 285 -> 293`, and `late 105 -> 106`; correctness and visual work identity stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-115403/summary.json`. | Do not promote. Avoid removing tiny scheduler probes without a replacement pacing mechanism; less CPU work can still shift CD phase into visible pressure. |
| 2026-04-26 | `host-timing-preconvert-on-load` | dirty experiment after `e96f5db1` | Convert host-tick frame deadlines to VBlank units once during FG2 metadata load instead of multiplying/dividing inside `fgEntryHoldVBlanks()` for every frame. | Tested a helper-based preconvert and then a tighter parse-loop version; both source variants were reverted. | Failed/no promotion. The helper version passed with all runtime metrics exactly flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`) but grew `jcreborn.elf 713176 -> 714008`. The tighter parse-loop variant still grew the ELF (`713780`) and exited `137` before `JCPERF2`, producing only `missing_jcperf2`. Artifacts: `scratch/ps1-perf-iterate/20260426-115931/summary.json`, `scratch/ps1-perf-iterate/20260426-120239/fishing1/perf-summary.json`. | Do not promote. Per-frame timing conversion is not a current measured bottleneck, and moving it to setup either grows code or destabilizes the headless run. |
| 2026-04-26 | `pal4-aligned-pair-store` | dirty experiment after `a4240dbc` | Try a 32-bit store for aligned two-pixel PAL4 opaque runs, reducing two halfword writes to one word write in the compositor hot loop. | Added an aligned-destination branch inside `grCompositePacked4OpaqueRun()`, ran the exact no-holiday fishing1 gate, then reverted source. | Failed hard. Visual work identity stayed clean, but cadence regressed: `loop_vb 1221 -> 1225`, `target_vb 1071 -> 1068`, `overrun_vb 150 -> 157`, `blocking_vb 5 -> 12`, `prefetch_overrun_vb 5 -> 12`, and `blocking_reads 4 -> 10`. Artifact: `scratch/ps1-perf-iterate/20260426-120947/summary.json`. | Do not promote. The branch/code-shape cost is worse than the theoretical store reduction; compositor fast paths need pack-time specialization or measured assembly, not dynamic alignment branching. |
| 2026-04-26 | `main-pause-credits-captions-merge-baseline` | `d36e8319` | Pull `origin/main` pause-menu, credits, and closed-caption work into the perf branch so optimization work does not diverge from main. | Merged `origin/main`, then ran the exact no-holiday fishing1 gate against the prior accepted branch baseline. | Accepted integration baseline. Correctness stayed clean, but the larger executable moved `FISHING1.FG2` to LBA `399` and cost one cadence VBlank: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `scene_vb 1400 -> 1401`; `overrun_vb` stayed `150`, `due_misses=0`, and visual/sound counters stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-121349/summary.json`. | Promote as the new comparison baseline. This is an integration cost, not an optimization miss; future perf wins should compare against this merged branch state. |
| 2026-04-26 | `caption-render-default-off-gate` | dirty experiment after `e1222d4a` | Captions are default-off, so call `captionsGetEnabled()` before `captionsRender()` in `grUpdateDisplay()` to keep the heavier renderer out of normal playback. | Added the default-off gate, ran the exact no-holiday fishing1 gate against the post-main baseline, then reverted source. | Failed/no promotion. Runtime cadence and correctness were exactly flat (`loop_vb=1222`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `due_misses=0`), but binary size worsened (`jcreborn.exe 149504 -> 151552`, `jcreborn.elf 743944 -> 744080`). Artifact: `scratch/ps1-perf-iterate/20260426-121958/summary.json`. | Do not promote. The extra external getter/code shape costs more than the default-off renderer call saves; revisit only with an inline caption-active flag or compile-time caption profile. |
| 2026-04-26 | `pause-menu-diagnostics-compile-gate` | `ps1: compile-gate pause diagnostics` | Pause-menu JCPAUSE printf diagnostics are useful for development but should not live in the default release/perf build. Compile-gate them behind `PAUSE_MENU_DIAG_LOGS` while preserving menu behavior. | Added a default-off compile-time diagnostics gate around the pause font upload, init, show, and per-frame diagnostic prints, then ran the exact no-holiday fishing1 gate against the post-main baseline. | Promoted as a code-size cleanup. Runtime stayed exactly flat (`loop_vb=1222`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `due_misses=0`) with clean correctness and unchanged `FISHING1.FG2` LBA `399`; `jcreborn.exe` stayed `149504`, while `jcreborn.elf` shrank `743944 -> 743136`. Artifact: `scratch/ps1-perf-iterate/20260426-122423/summary.json`. | Promote. This does not recover a VBlank, but it removes default-build diagnostic text/code from a public-facing path without changing scene cadence. |
| 2026-04-26 | `graphics-diagnostics-compile-gate` | `ps1: compile-gate graphics diagnostics` | `debugMode` GPU startup, primitive-buffer, draw-tile, and odd-width screen logs are useful when explicitly enabled but should not live in the default release/perf graphics path. | Added a default-off `GRAPHICS_PS1_DIAG_LOGS` macro and routed `graphics_ps1.c` debug-only printf calls through it, then ran the exact no-holiday fishing1 gate against the pause-diagnostics baseline. | Promoted as a code-size cleanup. Runtime stayed exactly flat (`loop_vb=1222`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `due_misses=0`) with clean correctness and unchanged `FISHING1.FG2` LBA `399`; `jcreborn.exe` stayed `149504`, while `jcreborn.elf` shrank `743136 -> 740984`. Artifact: `scratch/ps1-perf-iterate/20260426-123047/summary.json`. | Promote. This keeps the default graphics path cleaner without changing deterministic playback; re-enable with `GRAPHICS_PS1_DIAG_LOGS=1` only for targeted GPU log mining. |
| 2026-04-26 | `sound-diagnostics-compile-gate` | `ps1: compile-gate sound diagnostics` | SPU startup status logs are useful for targeted audio setup debugging, but the default release/perf build should not carry routine status printf text once sound is known working. | Added a default-off `SOUND_PS1_DIAG_LOGS` macro around `soundDisabled`, loaded-count, and no-VAG status prints, leaving the real SPU RAM overflow error print intact, then ran the exact no-holiday fishing1 gate against the graphics-diagnostics baseline. | Promoted as a code-size cleanup. Runtime stayed exactly flat (`loop_vb=1222`, `blocking_vb=6`, `prefetch_overrun_vb=6`, `due_misses=0`) with clean correctness and unchanged `FISHING1.FG2` LBA `399`; `jcreborn.exe` stayed `149504`, while `jcreborn.elf` shrank `740984 -> 740664`. Artifact: `scratch/ps1-perf-iterate/20260426-123523/summary.json`. | Promote. This preserves audio behavior and keeps routine SPU setup chatter out of the default build; re-enable with `SOUND_PS1_DIAG_LOGS=1` for audio log mining. |
| 2026-04-26 | `boot-diagnostics-compile-gate` | `ps1: compile-gate boot diagnostics` | The unconditional `JCBOOT applyBootOverride` buffer dump was a useful bring-up probe, but normal perf/release runs should not print or carry the large boot-argument diagnostic unless explicitly requested. | Added a default-off `JC_BOOT_DIAG_LOGS` macro around the boot override buffer dump, then ran the exact no-holiday fishing1 gate against the sound-diagnostics baseline. | Promoted. Runtime and correctness stayed clean with a small cadence/CD improvement: `loop_vb 1222 -> 1221`, `target_vb 1072 -> 1071`, `overrun_vb=150`, `blocking_vb 6 -> 5`, `prefetch_overrun_vb 6 -> 5`, `loop_read_vb 292 -> 284`, and `read_vb 400 -> 393`; `FISHING1.FG2` stayed at LBA `399`, `jcreborn.exe` stayed `149504`, and `jcreborn.elf` shrank `740664 -> 740496`. Artifact: `scratch/ps1-perf-iterate/20260426-124004/summary.json`. | Promote. This is a small deterministic win plus public-build cleanup; re-enable with `JC_BOOT_DIAG_LOGS=1` only when the boot string itself is under investigation. |
| 2026-04-26 | `resource-count-diagnostics-compile-gate` | dirty experiment after `42cfc6ad` | The PS1 resource parser prints resource counts once during setup; compile-gating that routine status line might reduce boot/setup text and code size without affecting scene playback. | Added a default-off `PS1_RESOURCE_DIAG_LOGS` macro around the PS1 resource-count print, ran the exact no-holiday fishing1 gate against the boot-diagnostics baseline, then reverted the source change. | Failed. Correctness stayed clean and the ELF shrank (`740496 -> 740392`), but deterministic cadence regressed back to the pre-boot-gate shape: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `read_vb 393 -> 400`. Artifact: `scratch/ps1-perf-iterate/20260426-131933/summary.json`. | Do not promote. The resource-count print is accidental scheduler/code-layout ballast for the current build; retry only with a layout-preserving cold-section strategy or after CD phase is explicitly controlled. |
| 2026-04-26 | `pause-request-diagnostics-compile-gate` | `ps1: compile-gate pause request diagnostics` | The PS1 main loop prints pause-menu request consumption events only when the pause UI asks for next-scene or reset-loop. These are useful diagnostics, but normal playback should keep them opt-in. | Added a default-off `JC_PAUSE_REQUEST_DIAG_LOGS` macro around the next-scene/reset-loop request prints, then ran the exact no-holiday fishing1 gate against the boot-diagnostics baseline. | Promoted as code-size cleanup. Runtime stayed exactly flat (`loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`) with clean correctness, unchanged `FISHING1.FG2` LBA `399`, and unchanged `jcreborn.exe=149504`; `jcreborn.elf` shrank `740496 -> 740336`. Artifact: `scratch/ps1-perf-iterate/20260426-132318/summary.json`. | Promote. This removes cold diagnostic text/code without changing deterministic playback; re-enable with `JC_PAUSE_REQUEST_DIAG_LOGS=1` only when debugging pause-loop requests. |
| 2026-04-26 | `padtest-boot-mode-compile-gate` | dirty experiment after `2380e8fb` | The old PS1 `padtest` boot mode is a useful controller bring-up harness, but it is not normal FG2 playback; compile-gating it might remove cold diagnostic code from the default build. | Added a default-off `JC_PADTEST_MODE` macro around the `padtest` boot parser branch and infinite-loop test path, ran the exact no-holiday fishing1 gate, then reverted the source change. | Failed/no promotion. Runtime stayed exactly flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`) with clean correctness, but there was no measured file-size win (`jcreborn.exe=149504`, `jcreborn.elf=740336`). Artifact: `scratch/ps1-perf-iterate/20260426-140008/summary.json`. | Do not promote in the perf loop. Keep this as a later public-cleanup option if padtest is replaced by the pause/input diagnostics, but it does not currently buy timing or shipped file size. |
| 2026-04-26 | `printf-probe-compile-gate` | dirty experiment after `84c6905a` | The `printf-test` / `logtest` `JCLOG` breadcrumbs are optional bring-up probes; compile-gating them might remove enough boot diagnostic text/code to shrink the shipped executable without changing normal playback. | Added a default-off `JC_PRINTF_PROBE_LOGS` macro around the `ps1PrintfProbe()` body, ran the exact no-holiday fishing1 gate, then reverted the source change. | Failed despite a real size win. `jcreborn.exe` shrank `149504 -> 147456` and `jcreborn.elf` shrank `740336 -> 738556`, but deterministic cadence regressed: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `read_vb 393 -> 400`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-140503/summary.json`. | Do not promote. The size reduction crosses an EXE sector bucket but moves CD phase the wrong way; retry only with layout padding or explicit CD phase control so `FISHING1.FG2` cadence stays stable. |
| 2026-04-26 | `memcard-diagnostics-compile-gate` | dirty experiment after `0d123538` | The memory-card persistence path still prints routine `JCMC` setup/save/read diagnostics; compile-gating those logs might shrink the normal build while preserving status strings and card behavior. | Added a default-off `MEMCARD_DIAG_LOGS` macro around memcard printf calls, ran the exact no-holiday fishing1 gate against `current-branch-baseline`, then reverted the source change. | Failed despite a real size win. `jcreborn.exe` shrank `149504 -> 147456`, `jcreborn.elf` shrank `740336 -> 738652`, and `FG\\FISHING1.FG2` moved `LBA 399 -> 398`; cadence regressed in the same shape as other one-sector shrink misses: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `read_vb 393 -> 400`. Correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-141219/summary.json`. | Do not promote. Memcard diagnostic text is also layout/CD-phase ballast under the current ISO; retry only with explicit foreground-pack LBA preservation and code-layout proof, or after the scheduler no longer depends on this phase. |
| 2026-04-26 | `remove-isletemp-from-cd-layout` | dirty experiment after `b6f2afe2` | `ISLETEMP.SCR` is only referenced by legacy diagnostic scene helpers that are compiled out of the default PS1 FG2 build, so removing it from the active CD layout might shrink the image without touching `FISHING1.FG2`. | Removed `ISLETEMP.SCR` from `config/ps1/cd_layout.xml`, ran the exact no-holiday fishing1 gate, saved a small log excerpt around the post-metrics invalid-read spam, then restored the layout. | Failed. `FG\\FISHING1.FG2` stayed at LBA `399`, so the miss was not a foreground-pack LBA shift, but cadence still regressed: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, and `read_vb 393 -> 401`. The run also produced repeated DuckStation invalid-word-read errors after `JCPERF2`; the full generated log was trimmed to avoid a 915 MB scratch artifact. Artifacts: `scratch/ps1-perf-iterate/20260426-141745/summary.json`, `scratch/ps1-perf-iterate/20260426-141745/fishing1/headless-regtest-excerpt.log`. | Do not promote. Even dead-looking CD layout removals can perturb setup/runtime phase; retry active-layout pruning only with a broader startup phase barrier and post-scene exit sanity gate. |
| 2026-04-26 | `headless-log-size-guard` | dirty harness change after `20a48ea8` | The `remove-isletemp-from-cd-layout` miss generated a 915 MB DuckStation log after post-metrics invalid-read spam; the perf harness needs to fail runaway logs early so future experiments do not fill disk. | Added `--max-log-bytes` / `PS1_PERF_MAX_LOG_BYTES` to `scripts/ps1-perf-iterate.sh` with a default `536870912` byte cap, then ran the exact no-holiday fishing1 gate against `current-branch-baseline`. | Promoted as tooling safety. The healthy baseline run stayed far below the cap and matched exactly: `loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`, and clean correctness. Artifact: `scratch/ps1-perf-iterate/20260426-142334/summary.json`. | Promote. This does not count as a runtime speedup, but it hardens the autonomous loop and turns emulator-error spam into an early, explicit failed experiment. |
| 2026-04-26 | `fg-heap-probe-compile-gate` | dirty experiment after `6f794250` | Automatic foreground heap probes are off by default, but their printf/probe callsites still live in foreground runtime; compile-gating those callsites might shrink the normal build while preserving `fgProbeLargestAlloc()` for the pause-menu heap display. | Added a default-off `FG_HEAP_PROBE_LOGS` gate around `fgHeapProbe()` and `foregroundPilotSetHeapProbe()`, kept `fgProbeLargestAlloc()` linked for pause menu, ran the exact no-holiday fishing1 gate against `current-branch-baseline`, then reverted source. | Failed. `FG\\FISHING1.FG2` moved `LBA 399 -> 398` and cadence regressed in the familiar one-sector-shrink shape: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `read_vb 393 -> 400`. Correctness and visual work identity stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-144845/summary.json`. | Do not promote. The automatic FGHEAP code is layout/CD-phase ballast in the current build; retry only with explicit foreground LBA preservation, a cold-section/layout strategy, or after CD phase no longer depends on current code shape. |
| 2026-04-26 | `caption-inline-enabled-flag` | `ps1: skip disabled caption rendering` | Retry the default-off caption-render skip without the previous external getter call that grew the executable: expose the existing caption-enabled state as an inline-readable global and skip `captionsRender()` in normal playback when captions are off. | Added `ps1CaptionsEnabled`, routed caption API checks through it, guarded the `grUpdateDisplay()` caption render call with the flag, then ran the exact no-holiday fishing1 gate against `current-branch-baseline`. | Promoted as a safe hot-path micro-optimization. Runtime, CD, prefetch, graphics work identity, pack LBA, and correctness all matched exactly: `loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`. `jcreborn.exe` stayed `149504`; `jcreborn.elf` grew modestly `740336 -> 740492`. Artifact: `scratch/ps1-perf-iterate/20260426-145631/summary.json`. | Promote, but do not claim a VBlank speed win. The normal no-caption frame no longer enters the caption renderer, unlike the failed getter-gate probe, and the shipped executable sector/CD cadence is unchanged. |
| 2026-04-26 | `caption-startup-lookup-inline-gate` | `ps1: skip disabled caption scene lookup` | After the inline enabled flag proved safe, skip the foreground-pilot ADS caption scene lookup entirely when captions are disabled so default playback avoids startup scene-name checks and the no-op caption API call. | Guarded the fishing1/fishing2/fishing3 caption lookup block in `foregroundPilotRuntimeStart()` with `ps1CaptionsEnabled`, then ran the exact no-holiday fishing1 gate against `current-branch-baseline`. | Promoted as another disabled-caption micro-cleanup. Runtime, CD, prefetch, graphics work identity, pack LBA, and correctness all matched exactly: `loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`. `jcreborn.exe` stayed `149504`; `jcreborn.elf` grew slightly `740492 -> 740568`. Artifact: `scratch/ps1-perf-iterate/20260426-152014/summary.json`. | Promote, but do not claim a VBlank speed win. This keeps caption-on behavior intact while removing another no-caption default path from normal scene startup without changing the deterministic playback cadence. |
| 2026-04-26 | `prefetch-candidate-reuse` | dirty experiment after `df128c65` | The held-loop prepared/staged paths call `fgRuntimeWindowPrefetchWouldRead()` and then `fgRuntimeTryPrefetchWindow()`, which repeats next-entry search and window containment checks. Reuse the candidate entry for those guarded paths to remove duplicate CPU work without changing read decisions. | Tested a first direct candidate helper and a tightened version that shared the normal prefetch body, running the exact no-holiday fishing1 gate against `current-branch-baseline`; source was reverted after red-team review. | Failed/no promotion. Both variants kept timing, CD, prefetch, gfx work identity, pack LBA, and correctness exactly flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`, `FG\\FISHING1.FG2 LBA=399`), but no tracked work metric improved. The first variant grew `jcreborn.elf 740568 -> 742828`; the tightened variant still grew it to `741216` while `jcreborn.exe` stayed `149504`. Artifact: `scratch/ps1-perf-iterate/20260426-153012/summary.json`. | Do not promote. Candidate reuse is conceptually cleaner, but the current compiler/code layout turns it into size growth with no measurable runtime signal. Retry only as part of a broader prefetch state-machine rewrite or after lower-overhead CPU counters can prove the duplicate lookup cost matters. |
| 2026-04-26 | `direct-stage-cap-7kb-post-caption-gates` | dirty experiment after `509b096d` | Probe the untested `7 KB` boundary for tight-slack direct staging. Fishing1's max payload is just above `7 KB`, so this could avoid one large exact direct read while preserving most of the accepted `8 KB` behavior. | Changed `FG_PREFETCH_DIRECT_STAGE_MAX_BYTES` from `8 KB` to `7 KB`, ran the exact no-holiday fishing1 gate against `current-branch-baseline`, then reverted the source constant. | Failed/no promotion. It was an exact no-op: timing, CD, prefetch, graphics, pack LBA, correctness, and binary size all matched the accepted baseline (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=68`, `FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`, `jcreborn.elf=740568`). Artifact: `scratch/ps1-perf-iterate/20260426-153730/summary.json`. | Do not promote. The current active direct-stage decisions are unchanged by `7 KB`; the cap family remains exhausted until grouped reads or a different stage model changes coverage cost. |
| 2026-04-26 | `upload-rect-cap-8` | `ps1: lower upload rect cap` | Current fishing1 max upload rectangles per frame is `6`, so lower the local upload-band array cap from `16` to `8` to reduce stack pressure and code/data footprint without changing upload decisions. | Changed `GR_MAX_UPLOAD_RECTS` from `16` to `8`, then ran the exact no-holiday fishing1 gate against `current-branch-baseline`. | Promoted as a micro-cleanup. Timing, CD, prefetch, graphics work identity, pack LBA, and correctness all matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `max_upload_rects=6`, `cap_hits=0`, `FG\\FISHING1.FG2 LBA=399`). `jcreborn.exe` stayed `149504`; `jcreborn.elf` shrank slightly `740568 -> 740556`. Artifact: `scratch/ps1-perf-iterate/20260426-154117/summary.json`. | Promote. This is not a VBlank speed win, but it safely lowers stack headroom and binary footprint while preserving the accepted upload behavior. |
| 2026-04-26 | `upload-rect-cap-6` | dirty experiment after `b45ba219` | Test the exact measured fishing1 upload-rectangle maximum as the local upload cap. If safe, it might reduce stack pressure further than the accepted cap `8`. | Changed `GR_MAX_UPLOAD_RECTS` from `8` to `6`, ran the exact no-holiday fishing1 gate against `current-branch-baseline`, then reverted the cap to `8`. | Failed/no promotion. Runtime and work identity matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `max_upload_rects=6`, `cap_hits=0`, `FG\\FISHING1.FG2 LBA=399`), but binary size did not improve beyond the accepted cap `8` (`jcreborn.exe=149504`, `jcreborn.elf=740556`). Artifact: `scratch/ps1-perf-iterate/20260426-154454/summary.json`. | Do not promote. Cap `6` sits exactly on fishing1's max with no extra binary win and no cross-scene headroom; keep cap `8` until a broader scene matrix proves tighter caps are safe. |
| 2026-04-26 | `upload-bookkeeping-narrow-locals` | dirty experiment after `b45ba219` | After accepting the cap `8` stack reduction, narrow the remaining local upload bookkeeping arrays in `grDrawBackground()` to `sint16` row bounds and `uint8` band indices to reduce stack/binary footprint without changing upload decisions. | Changed the local upload bookkeeping arrays, ran the exact no-holiday fishing1 gate against `current-branch-baseline`, then reverted the source. | Failed. The ELF shrank (`740556 -> 740516`) and gfx work identity stayed flat (`upload_rects=502`, `upload_bytes=16281600`, `restore_calls/compose_calls=155`), but cadence regressed: `loop_vb 1221 -> 1225`, `target_vb 1071 -> 1068`, `overrun_vb 150 -> 157`, `blocking_vb 5 -> 12`, `prefetch_overrun_vb 5 -> 12`, `loop_read_vb 284 -> 294`, and `blocking_reads 4 -> 10`. Artifact: `scratch/ps1-perf-iterate/20260426-160022/summary.json`. | Do not promote. Narrower locals reduce binary/stack pressure but perturb compiler/code/register shape enough to expose CD pressure; retry only with map/register review or upload-path translation-unit isolation. |
| 2026-04-26 | `prepare-staged-slack3` | dirty experiment after `3eae7e96` | Use one more held-frame opportunity by allowing staged-frame RAM preparation at `3` VBlanks of slack as well as the accepted exact-`4` point, but only through the existing path where no future prefetch read was performed. | Changed `fgRuntimeCanPrepareStagedFrame()` to accept slack `3` or `4`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted source. | Failed strict gate. Total loop stayed flat and nominal timing improved (`loop_vb=1221`, `target_vb 1071 -> 1073`, `overrun_vb 150 -> 148`, `late 105 -> 100`), but visible CD pressure regressed (`blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`). It also shifted the read shape (`loop_read_vb 284 -> 281`, `read_vb 393 -> 390`, `sectors 541 -> 538`, `seek_back 5 -> 4`, `stage_hits 148 -> 150`, `window_hits 154 -> 155`) without producing actual loop speed. Artifact: `scratch/ps1-perf-iterate/20260426-160832/summary.json`. | Do not promote. The scheduler is close enough that a nominal deadline improvement is not sufficient; present-prep changes must reduce actual `loop_vb` or keep visible CD pressure flat. Retry only with a separate render-prep/CD budget. |
| 2026-04-26 | `direct-stage-read-into-window` | dirty experiment after `30bffb91` | For tight-slack direct-stage reads, read aligned sectors straight into the stream-window buffer and copy the staged payload from there, avoiding the current scratch-to-window seed copy. | Tested an `8 KB` aligned-window cap, then a tighter `6 KB` cap, each against the exact no-holiday fishing1 gate with `--require-improvement`; source was reverted. | Failed. The `8 KB` cap produced a real loop win (`loop_vb 1221 -> 1219`, `overrun_vb 150 -> 148`) but worsened visible CD pressure (`blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `blocking_reads 4 -> 5`, `loop_read_vb 284 -> 293`, `read_vb 393 -> 402`). The `6 KB` cap avoided the pressure regression but became an exact no-op (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`). Artifacts: `scratch/ps1-perf-iterate/20260426-161350/summary.json`, `scratch/ps1-perf-iterate/20260426-161643/summary.json`, `scratch/ps1-perf-iterate/20260426-161914/summary.json`. | Do not promote. Removing a RAM copy is not enough if it shifts a fifth read into visible time; retry only with grouped/predicted direct-stage windows that preserve the `4` blocking-read shape. |
| 2026-04-26 | `work-identity-baseline-gate` | `ps1: gate headless perf work identity` | The docs and red-team rules require invalid speedups to be rejected when render work collapses; the script still only compared timing counters, so a future sequential-CD-style corruption could pass again. | Added `--work-identity-min` / `PS1_PERF_WORK_IDENTITY_MIN_PERCENT` with a default `75%` floor for `timing.render`, `gfx.restore_calls`, `gfx.compose_calls`, and `gfx.upload_calls`, then ran the exact no-holiday fishing1 baseline without `--require-improvement`. | Promoted as tooling safety. The known-good baseline stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`) and the new identity comparison matched all four counters at `155 -> 155`. Artifact: `scratch/ps1-perf-iterate/20260426-164500/summary.json`. | Promote. This does not count as a runtime speed win, but it makes the autonomous loop reject workload-collapse false positives by default; disable only for deliberate architecture changes with explicit review. |
| 2026-04-26 | `scene-relative-offset-preapply` | `ps1: preapply FG2 scene offsets` | FG2 packs are scene-relative so the runtime can randomly place the island, but once a scene variant starts the island offset is fixed. Pre-applying the offset to the entry table should remove the per-compose header-flag branch/add while preserving random placement. | Adjusted entry `x/y` once after metadata load, kept the original pack flags for `JCPERF2`, ran the exact no-holiday fishing1 gate first with `--require-improvement` and then without it while writing the new local baseline. | Promoted as code-size cleanup. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`). `jcreborn.exe` stayed `149504`; `jcreborn.elf` shrank `740556 -> 740132`. Artifacts: `scratch/ps1-perf-iterate/20260426-164956/summary.json`, `scratch/ps1-perf-iterate/20260426-165240/summary.json`. | Promote, but do not claim a VBlank speed win. This keeps scene-relative capture semantics and random island placement while shrinking the active runtime shape. |
| 2026-04-26 | `inline-entry-draw-offsets` | `ps1: inline FG2 entry draw offsets` | After scene-relative offsets are applied once at startup, the draw-position helper functions only return `entry->x/y`; inlining those reads should shrink the hot runtime shape without changing capture semantics. | Removed the helper functions, used `entry->x/y` directly in compose and draw-bounds paths, ran the exact no-holiday fishing1 gate first with `--require-improvement` and then without it while writing the new local baseline. | Promoted as code-size cleanup. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`). `jcreborn.exe` stayed `149504`; `jcreborn.elf` shrank `740132 -> 739684`. Artifacts: `scratch/ps1-perf-iterate/20260426-165633/summary.json`, `scratch/ps1-perf-iterate/20260426-165902/summary.json`. | Promote, but do not claim a VBlank speed win. This removes dead abstraction left behind by startup pre-apply while keeping the deterministic scene-relative workflow intact. |
| 2026-04-26 | `held-prefetch-branch-collapse` | `ps1: collapse held prefetch branch` | The held loop used two adjacent branches with identical window-prefetch behavior when either a prepared frame or a staged frame was already resident. Collapsing them should reduce code shape without changing scheduling decisions. | Combined the prepared-frame and staged-frame prefetch branches, ran the exact no-holiday fishing1 gate first with `--require-improvement` and then without it while writing the new local baseline. | Promoted as code-size cleanup. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`). `jcreborn.exe` stayed `149504`; `jcreborn.elf` shrank `739684 -> 739552`. Artifacts: `scratch/ps1-perf-iterate/20260426-170442/summary.json`, `scratch/ps1-perf-iterate/20260426-170728/summary.json`. | Promote, but do not claim a VBlank speed win. This trims duplicated held-loop branch shape while preserving the current scheduler cadence exactly. |
| 2026-04-26 | `prefetch-next-index-null` | dirty experiment after `b0f7927a` | Two window-prefetch helpers request `fgRuntimeNextPayloadEntry()`'s out-frame index and immediately discard it. Passing `NULL` might remove dead local bookkeeping without changing scheduling. | Replaced the discarded out-parameter locals with `fgRuntimeNextPayloadEntry(NULL)`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`), but there was no speed win and `jcreborn.elf` grew `739552 -> 739568`; `jcreborn.exe` stayed `149504`. Artifact: `scratch/ps1-perf-iterate/20260426-171114/summary.json`. | Do not promote. The current compiler shape prefers the explicit discarded local; retry only after broader prefetch helper cleanup or compiler/link layout changes. |
| 2026-04-26 | `prefetch-no-slack-perf-branch-combine` | dirty experiment after `fc346bd9` | The no-slack prefetch paths contain adjacent `ps1PerfEnabled` checks. Combining them might remove one branch in the active perf-log build without changing metrics. | Combined the adjacent perf-log guards in both no-slack paths, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`), but there was no speed win and `jcreborn.elf` grew `739552 -> 739664`; `jcreborn.exe` stayed `149504`. Artifact: `scratch/ps1-perf-iterate/20260426-171513/summary.json`. | Do not promote. On this compiler, explicit repeated guards are smaller than a braced shared block; avoid cosmetic perf-log guard combining in hot code. |
| 2026-04-26 | `compose-active-guard-removal` | `ps1: remove duplicate compose active guard` | `grUpdateDisplay()` already calls `foregroundPilotRuntimeCompose()` only when the runtime is active, and the compose function still checks the runtime mode before drawing. Removing the duplicate active guard should trim code without changing inactive behavior. | Removed the top-level active guard from `foregroundPilotRuntimeCompose()`, ran the exact no-holiday fishing1 gate first with `--require-improvement` and then without it while writing the new local baseline. | Promoted as code-size cleanup. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`). `jcreborn.exe` stayed `149504`; `jcreborn.elf` shrank `739552 -> 739544`. Artifacts: `scratch/ps1-perf-iterate/20260426-171847/summary.json`, `scratch/ps1-perf-iterate/20260426-172120/summary.json`. | Promote, but do not claim a VBlank speed win. This removes duplicate active-state checking while keeping mode-based no-op behavior intact. |
| 2026-04-26 | `hold-displayed-active-guard-removal` | dirty experiment after `d207580c` | `fgRuntimeCanHoldDisplayedFrame()` is currently called only inside the active scene loop, so its internal active check looks redundant and might trim hot held-loop code. | Removed the active-state term from the held-frame helper, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. It was a real size win (`jcreborn.exe 149504 -> 147456`, `jcreborn.elf 739544 -> 738764`) but moved `FG\\FISHING1.FG2` from LBA `399` to `398` and regressed cadence: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `blocking_reads 4 -> 5`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-172453/summary.json`. | Do not promote. This is another one-sector shrink/CD-phase miss; retry only with explicit foreground-pack LBA preservation or after playback cadence no longer depends on the current code-layout phase. |
| 2026-04-26 | `mark-rendered-active-guard-removal` | dirty experiment after `2ce5dc9f` | `fgRuntimeMarkFrameRendered()` is called only after successful active render/present paths, so its internal active check looks redundant and might trim post-render code. | Removed the active-state term from the mark-rendered helper, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. It was a real size win (`jcreborn.exe 149504 -> 147456`, `jcreborn.elf 739544 -> 739344`) but moved `FG\\FISHING1.FG2` from LBA `399` to `398` and regressed the same cadence: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `blocking_reads 4 -> 5`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-172828/summary.json`. | Do not promote. The source simplification is valid semantically, but the current ISO/CD phase depends on the `149504` byte executable bucket; retry with deliberate padding or after CD phase is decoupled. |
| 2026-04-26 | `mark-rendered-active-guard-removal-cdpad` | dirty experiment after `ec3084da` | Retry the mark-rendered active-guard removal with a one-sector inert CD padding file after `JCREBORN.EXE`, preserving `FISHING1.FG2` at LBA `399` while keeping the smaller executable. | Added temporary `CDPHASE.PAD` to `config/ps1/cd_layout.xml`, removed the active-state term from the mark-rendered helper again, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted both source and layout changes. | Failed/no promotion. The padding did preserve `FG\\FISHING1.FG2 LBA=399`, but cadence still regressed exactly like the unpadded run: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `blocking_reads 4 -> 5`; `jcreborn.exe` remained `147456` and `jcreborn.elf=739344`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-173230/summary.json`. | Do not promote. This proves the miss is not only FG2 physical LBA; the smaller executable/code-layout phase itself perturbs the deterministic scheduler. Future retries need code-layout control, not just CD padding. |
| 2026-04-26 | `stream-window-append-disable` | dirty experiment after `cf532e59` | The stream-window append path preserves resident tail bytes with a RAM `memmove()` before reading only the forward extension. Disable it to test whether the memmove cost is helpful work or scheduler ballast. | Forced `fgRuntimeTryExtendWindow()` to return no-op so `fgRuntimeFillWindowForEntry()` always performs a full window read, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed hard. Work identity and correctness stayed clean, but CD efficiency collapsed: `loop_vb 1221 -> 1230`, `target_vb 1071 -> 1060`, `overrun_vb 150 -> 170`, `blocking_vb 5 -> 26`, `prefetch_overrun_vb 5 -> 26`, `loop_read_vb 284 -> 351`, `bytes 1098982 -> 1375462`, `sectors 541 -> 676`, `seq 66 -> 0`, `seek_back 5 -> 71`, and `blocking_reads 4 -> 18`. Artifact: `scratch/ps1-perf-iterate/20260426-173612/summary.json`. | Do not promote. The append path is real value, not ballast; future CD optimizations should make appends/grouped forward reads cheaper and more predictable, not remove tail preservation. |
| 2026-04-26 | `runtime-active-accessor-simplify` | `ps1: simplify runtime active accessor` | `foregroundPilotRuntimeActive()` normalizes the runtime byte through a ternary even though callers only need truthiness. Returning the byte directly may trim the hot loop call shape without changing public behavior. | Changed the accessor to return `gFgRuntime.active` directly, ran the exact no-holiday fishing1 gate first with `--require-improvement` and then without it while writing the new local baseline. | Promoted as code-size cleanup. Timing, CD, prefetch, work identity, pack LBA, pack flags, and correctness matched exactly (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls/compose_calls/upload_calls=155`, `FG\\FISHING1.FG2 LBA=399`, `flags=28`). `jcreborn.exe` stayed `149504`; `jcreborn.elf` shrank `739544 -> 739540`. Artifacts: `scratch/ps1-perf-iterate/20260426-174004/summary.json`, `scratch/ps1-perf-iterate/20260426-174235/summary.json`. | Promote, but do not claim a VBlank speed win. This trims a hot accessor while preserving the accepted CD/scheduler phase exactly. |
| 2026-04-26 | `window-slack-eligible-ternary-removal` | dirty experiment after `7c1b9d38` | `fgRuntimeWindowSlackEligible()` returns a comparison through an explicit `? 1 : 0`; returning the comparison directly might trim branch code in the hot prefetch path. | Replaced the ternary with the comparison result, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion as an exact no-op. Timing, CD, prefetch, work identity, pack LBA, pack flags, correctness, and binary size all matched the accepted baseline (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`, `jcreborn.elf=739540`). Artifact: `scratch/ps1-perf-iterate/20260426-174614/summary.json`. | Do not promote. The compiler already emits equivalent code; this helper shape is exhausted unless combined with a broader prefetch inlining pass. |
| 2026-04-26 | `entry-has-payload-ternary-removal` | dirty experiment after `538ee36f` | `fgEntryHasPayload()` is called throughout the stage/window path and normalizes its conjunction with `? 1 : 0`; returning the comparison directly might trim hot helper code. | Replaced the ternary with the raw conjunction, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion as an exact no-op. Timing, CD, prefetch, work identity, pack LBA, pack flags, correctness, and binary size all matched the accepted baseline (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`, `jcreborn.elf=739540`). Artifact: `scratch/ps1-perf-iterate/20260426-180136/summary.json`. | Do not promote. The compiler already emits equivalent helper code; avoid more isolated boolean-ternary cleanups unless combined with a larger hot-helper rewrite. |
| 2026-04-26 | `prefetch-window-perf-flag-cache` | dirty experiment after `ecff140c` | `fgRuntimeTryPrefetchWindow()` checks volatile `ps1PerfEnabled` repeatedly on the perf-log path; caching it once locally might reduce hot helper load/branch overhead. | Added a local `perfTrack = ps1PerfEnabled` and used it for the helper's perf marks, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. The change crossed the PS-EXE sector bucket (`jcreborn.exe 149504 -> 147456`), moved `FG\\FISHING1.FG2` from LBA `399` to `398`, and regressed cadence: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 284 -> 292`, and `blocking_reads 4 -> 5`. Work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-180600/summary.json`. | Do not promote. Perf-flag cache refactors are semantically plausible, but this code shape perturbs the executable/CD phase; retry only with code-layout control or after removing perf logging from the accepted speed baseline. |
| 2026-04-26 | `layout-identity-gates` | `ps1: gate perf layout identity` | Recent valid-looking source cleanups failed because they crossed the PS-EXE sector bucket or shifted `FG\\FISHING1.FG2` LBA; the harness needed to treat those as first-class baseline identity fields. | Added per-run build artifact recording plus baseline comparisons for `scene.pack_lba` and `build.ps_exe.sector_bucket_bytes`, with `--allow-layout-change` as the explicit opt-out for deliberate layout experiments. Ran the exact no-holiday fishing1 gate without `--require-improvement` and rewrote the local baseline to seed the new build fields. | Promoted as tooling safety. The known-good cadence stayed exact (`loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`), `FG\\FISHING1.FG2` stayed LBA `399`, `jcreborn.exe=149504` (`73` sectors), and `jcreborn.elf=739540`. Artifact: `scratch/ps1-perf-iterate/20260426-181416/summary.json`. | Promote. This is not a runtime speed win; it prevents future false promotions and makes sector/layout phase changes searchable in `experiments.jsonl`. |
| 2026-04-26 | `prepared-present-guard-collapse` | dirty experiment after `6a616f88` | `fgRuntimePresentPreparedFrame()` repeats `fgRuntimeCanPresentPreparedOnNextVBlank()` even though its only caller already checks the same condition. Removing the internal guard might trim the held-frame prepared-present path without changing behavior. | Removed the internal guard, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. Layout stayed fixed (`FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`) and ELF shrank (`739540 -> 739492`), but visible CD pressure regressed: `blocking_vb 5 -> 7`, `prefetch_overrun_vb 5 -> 7`, `blocking_reads 4 -> 5`, `loop_read_vb 284 -> 288`, and `target_vb 1071 -> 1072`; work identity and correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-182058/summary.json`. | Do not promote. The duplicate guard is acting as code-shape/scheduler ballast; retry only with map-address comparison or a broader prepared-present scheduler rewrite. |
| 2026-04-26 | `hot-symbol-map-snapshot` | `ps1: record perf map symbols` | The guard-collapse miss kept pack LBA and EXE bucket fixed but still regressed cadence, so we need address/size evidence for hot functions before assuming a source cleanup is harmless. | Extended `ps1-perf-iterate` to parse selected hot symbols from `build-ps1/jcreborn.map`, store normalized 32-bit PS1 addresses and sizes in `summary.json`, append map size to JSONL metrics, and print non-failing symbol deltas when they change. Seeded the local baseline, then reran the exact no-holiday fishing1 gate. | Promoted as tooling safety. The steady-state gate stayed exact (`loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`, `FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`), and the map snapshot captured `18` hot symbols. Artifacts: `scratch/ps1-perf-iterate/20260426-182802/summary.json`, `scratch/ps1-perf-iterate/20260426-183023/summary.json`. | Promote. This is not a runtime speed win; it makes code-layout phase measurable for future accepted/rejected experiments. |
| 2026-04-26 | `duckstation-cdlog-extractor` | `ps1: add perf CD log summarizer` | The next CD optimization needs the actual read-start sequence, but inline read histograms previously perturbed the speed build. DuckStation already logs `Setloc`/`ReadN`; parse that outside the PS1 binary. | Added `scripts/ps1-perf-cdlog-summary.py` and ran it on the accepted fishing1 baseline log with the matching summary file. | Promoted as tooling safety. The extractor identified `69` post-locate scene-pack reads from LBA `400` through `801`, `68` forward steps, no backward steps inside that active sequence, and largest host-visible read gaps around LBAs `748`, `755`, `762`, `769`, and `801`; `JCPERF2` for the same run remains `loop_reads=68`, `blocking_reads=4`, `blocking_vb=5`. Artifact: `scratch/ps1-perf-iterate/20260426-183023/fishing1/cd-read-sequence.json`. | Promote. This is not a runtime speed win; it gives the next grouped-read or fifth-blocking-read experiment a host-side target without changing the measured PS1 binary. |
| 2026-04-26 | `would-read-contained-inline` | dirty experiment after `6e766a4f` | `fgRuntimeWindowPrefetchWouldRead()` validates that the next payload fits the stream window, then calls `fgRuntimeWindowContainsEntry()`, which repeats active/window/payload checks. Inlining the contained-window math might reduce held-loop branch/call overhead while preserving pacing. | Replaced the helper call with direct `streamWindowValid`, `entryEnd`, and `windowEnd` checks, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed/no promotion. Timing, CD, prefetch, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`), but no key speed metric improved and the ELF grew (`739540 -> 739740`). The new map comparison shows `fgRuntimeWindowPrefetchWouldRead` moved earlier and grew `176 -> 236` bytes, shifting downstream hot symbols by `+60` bytes. Artifact: `scratch/ps1-perf-iterate/20260426-184543/summary.json`. | Do not promote. The current shared helper call is smaller on this compiler; retry only as part of a larger prefetch state-machine rewrite that reduces both code size and CD timing. |
| 2026-04-26 | `host-fg2-group-planner` | `ps1: plan FG2 read groups from CD logs` | The next grouped-read runtime experiment needs group boundaries that do not move payload bytes or increase total sector coverage. | Extended `scripts/ps1-perf-cdlog-summary.py` with `--pack-file` support, FG2 header/table parsing, inferred read segments, and candidate group plans. Re-ran it on the accepted fishing1 baseline log and pack. | Promoted as tooling safety. The accepted active sequence has `69` inferred post-locate read segments; candidate grouping preserves sector coverage (`sector_delta=0`) while reducing planned reads to `46` at `12` sectors, `29` at `16` sectors, or `20` at `24` sectors. The pack parser captured `155` payload entries and the max payload frame (`entry 23`, `7184` bytes). Artifact: `scratch/ps1-perf-iterate/20260426-183023/fishing1/cd-read-sequence.json`. | Promote. This is not a runtime speed win; it converts the CD-log evidence into concrete read-group boundaries for P4-201/P4-202 without touching the measured PS1 binary. |
| 2026-04-26 | `high-slack-24kb-window-capacity` | dirty experiment after `1d737913` | The host-side group planner showed zero-extra-sector candidates, but the runtime still needs a safe way to read larger forward spans. Test the most conservative raw form: keep normal refills at `16 KB`, allocate enough capacity for a `24 KB` high-slack read, and use the larger read only when held slack is at least `10` VBlanks. | Added separate normal read size versus stream-window capacity and let `fgRuntimeFillWindowForEntry()` choose a `24 KB` read only for high-slack prefetches; ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source change. | Failed. The larger read did not improve actual loop time (`loop_vb=1221`) and regressed visible pressure despite fixed layout: `target_vb 1071 -> 1069`, `overrun_vb 150 -> 152`, `blocking_vb 5 -> 8`, `prefetch_overrun_vb 5 -> 8`, and `FG\\FISHING1.FG2 LBA=399`. Artifact: `scratch/ps1-perf-iterate/20260426-192812/summary.json`. | Do not promote. Long held slack alone is not a sufficient cost model; grouped-read runtime work needs exact group boundaries, append/preserve cost, and a guard that specifically avoids creating the fifth visible read. |
| 2026-04-26 | `modern-cd-counters-only` | `ps1: skip legacy CD accumulators` | The current harness reads `JCPERF2`; the old `JCPERF cd` counters duplicate the same read accounting on every foreground buffered/aligned read. Skip legacy accumulator writes for modern foreground reads while keeping the compatibility print meaningful by sourcing it from the modern counters. | Changed foreground buffered/aligned CD read perf marks to pass `includeLegacy=0`, updated the legacy print to use the modern CD totals, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reran without `--require-improvement` and wrote the accepted baseline. | Promoted as flat-timing cleanup. Timing, CD, prefetch, work identity, layout, and correctness stayed exact (`loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`, `FG\\FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`), while `jcreborn.elf` shrank `739540 -> 739524` and both hot CD helper symbols shrank by `8` bytes. Artifacts: `scratch/ps1-perf-iterate/20260426-193642/summary.json`, `scratch/ps1-perf-iterate/20260426-193910/summary.json`. | Promote, but do not claim a VBlank speed win. This keeps old log output useful while removing duplicate hot perf-log accumulator work from the accepted foreground path. |
| 2026-04-26 | `fg2-read-groups-12sector-broad` | dirty experiment after `7ae05044` | Try the planner's 12-sector zero-extra-sector groups as runtime append extensions while keeping payload offsets and frame identity unchanged. | Added runtime group lookup for the planner table, then ran the exact no-holiday fishing1 gate with `--require-improvement`: `./scripts/ps1-perf-iterate.sh --case "fishing1::fgpilot fishing1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --baseline scratch/ps1-perf-iterate/current-branch-baseline.json --allow-regression 2 --require-improvement --timeout 180`. | Failed. It reduced transaction count (`loop_reads 68 -> 66`) but created more visible CD pressure: `loop_vb 1221 -> 1223`, `target_vb 1071 -> 1070`, `overrun_vb 150 -> 153`, `blocking_vb 5 -> 10`, `prefetch_overrun_vb 5 -> 10`, and `loop_read_vb 284 -> 293`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-195711/summary.json`. | Do not promote. Broad grouping proves fewer reads alone is not enough; each group needs per-boundary cost/slack proof before it can enter the default path. |
| 2026-04-26 | `fg2-tail-read-group-396-406` | `ps1: add fishing1 tail read group` | Keep only the broad-table group that actually reduces work without changing cadence: the fishing1 high-tide tail append at file sectors `396..406`. | Narrowed the runtime group table to `{396, 406}`, kept normal reads at `16 KB`, allowed a larger retained window only for this fishing1 high-tide tail append, ran a strict gate, then accepted with a no-require baseline update. | Promoted as a flat-timing work-reduction checkpoint. Key timing stayed flat (`loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `due_misses=0`) while CD work dropped: `loop_reads 68 -> 67`, `setloc 74 -> 73`, `loop_read_vb 284 -> 283`, `read_vb 393 -> 392`, `sectors 541 -> 540`, and `seek_back 5 -> 4`. Tradeoff: retained prefetch buffer grew to `31760` bytes and `jcreborn.elf` grew `739524 -> 741076`; PS-EXE stayed `149504`. Artifacts: `scratch/ps1-perf-iterate/20260426-200026/summary.json`, `scratch/ps1-perf-iterate/20260426-200325/summary.json`. | Promote, but do not claim a VBlank speed win. This is the first runtime grouped-read promotion and confirms the next CD wins require selective groups plus a cost predictor, not blanket planner import. |
| 2026-04-26 | `fg2-tail-group-direct-constant` | dirty experiment after `11b0dd4d` | Replace the one-entry read-group table scan with a direct sector constant check, expecting smaller code or less hot-path branch work. | Replaced `kFishing1HighReadGroups12[]` with direct start/end constants in `fgRuntimeGroupedAppendTargetEnd()`, ran the exact no-holiday fishing1 gate against `current-branch-baseline`, then reverted the source change. | Failed/no promotion. Runtime cadence and CD work stayed flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `loop_read_vb=283`), but binary/code shape worsened: `jcreborn.elf 741076 -> 741404` and `fgRuntimeFillWindowForEntry` grew by `100` bytes. Artifact: `scratch/ps1-perf-iterate/20260426-204416/summary.json`. | Do not promote. This compiler emits the tiny table loop better than the direct constant shape; keep the table form until generated pack metadata replaces the hard-coded group. |
| 2026-04-26 | `fg2-tail-group-11sector-capacity` | `ps1: tighten tail read group capacity` | The accepted tail group used a 12-sector retained stream window. Test whether 11 sectors preserves the saved read while reducing heap pressure. | Changed `FG_PREFETCH_GROUP_WINDOW_BYTES` from `12` sectors to `11` sectors, then ran the exact no-holiday fishing1 gate against `current-branch-baseline` and wrote the local baseline forward. | Promoted as memory work reduction. Key timing and CD work stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `loop_read_vb=283`, `setloc=73`, `seek_back=4`) while the retained prefetch buffer fell `31760 -> 29712` bytes. `jcreborn.elf=741076` and PS-EXE `149504` stayed flat. Artifact: `scratch/ps1-perf-iterate/20260426-204806/summary.json`. | Promote. This keeps the prior saved read and reduces deterministic scene memory by `2048` bytes, giving future grouped-read tests a tighter capacity bound. |
| 2026-04-26 | `fg2-tail-group-10sector-capacity` | dirty experiment after `7254d4b6` | Test whether the retained group window can be reduced to the literal 10-sector span of the accepted `396..406` group. | Changed `FG_PREFETCH_GROUP_WINDOW_BYTES` from `11` sectors to `10` sectors and ran the exact no-holiday fishing1 gate against the 11-sector baseline. | Failed/no promotion. Timing stayed flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`), but the saved read disappeared: `loop_reads 67 -> 68` and `group_hits=0`. The buffer would have dropped `29712 -> 27664` bytes, but that violates the rule to keep every accepted read saving. Artifact: `scratch/ps1-perf-iterate/20260426-205336/summary.json`. | Do not promote. The group append needs one sector more than the raw span, likely because the retained range must cover alignment/preserved-tail state, so 11 sectors is the current safe lower bound. |
| 2026-04-26 | `caption-tu-os` | `ps1: compile captions for size` | Test a narrow compiler-flag win by compiling only the default-off caption translation unit with `-Os`, leaving hot foreground/graphics/CD object order unchanged. | Added a source-file compile option for `src/ps1_captions.c`, ran the exact no-holiday fishing1 gate against the 11-sector baseline, and wrote the accepted summary forward. | Promoted as cold-code size reduction. Timing, CD, prefetch, heap, layout, hot-symbol addresses, and work identity stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `FG\\FISHING1.FG2 LBA=399`, PS-EXE bucket `149504`) while `jcreborn.elf` shrank `741076 -> 740816`. Artifact: `scratch/ps1-perf-iterate/20260426-205826/summary.json`. | Promote, but do not count as a VBlank speed win. This validates narrow cold-TU flag work under the layout gates; continue with one file at a time. |
| 2026-04-26 | `memcard-tu-os` | `ps1: compile memcard for size` | Continue the narrow cold-TU compiler matrix by compiling pause-menu persistence code with `-Os`. | Added `src/memcard.c` to the accepted source-file `-Os` list and ran the exact no-holiday fishing1 gate against the captions-size baseline. | Promoted as cold-code size reduction. Timing, CD, prefetch, heap, layout, hot-symbol addresses, and work identity stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `FG\\FISHING1.FG2 LBA=399`, PS-EXE bucket `149504`) while `jcreborn.elf` shrank `740816 -> 740196`. Artifact: `scratch/ps1-perf-iterate/20260426-210254/summary.json`. | Promote, but do not count as a VBlank speed win. This confirms cold-TU `-Os` can keep deterministic playback flat when hot object order and layout identity are preserved. |
| 2026-04-26 | `holidays-tu-os` | dirty experiment after `8ef9db4a` | Continue the cold-TU `-Os` matrix with `src/holidays.c`, expecting no-holiday playback to stay flat. | Added `src/holidays.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate against the memcard-size baseline, then reverted the CMake change. | Failed/no promotion. The ELF shrank materially (`740196 -> 737352`) and loop reads stayed `67`, but the executable crossed a sector bucket (`149504 -> 147456`), `FG\\FISHING1.FG2` moved `399 -> 398`, and active CD cadence regressed (`loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 283 -> 289`, `blocking_reads 4 -> 5`). Artifact: `scratch/ps1-perf-iterate/20260426-210640/summary.json`. | Do not promote. This is another one-sector shrink/CD-phase miss; retry holidays size work only with deliberate layout padding/code-phase control or after CD scheduling no longer depends on current phase. |
| 2026-04-26 | `holidays-table-tu-os` | dirty experiment after `32b13338` | Test whether the generated holiday table translation unit has any compiler-flag size sensitivity. | Added `src/holidays_table.c` to the accepted source-file `-Os` list, ran the exact no-holiday fishing1 gate, then reverted the CMake change. | Failed/no-op. Timing, CD, prefetch, layout, hot-symbol addresses, correctness, and binary size all matched the accepted baseline (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `jcreborn.elf=740196`, PS-EXE bucket `149504`). Artifact: `scratch/ps1-perf-iterate/20260426-211007/summary.json`. | Do not promote. The generated table is data-dominated under the current flags; retry only if the table representation changes. |
| 2026-04-26 | `prepared-stage-decoupling-v2` | dirty experiment after `29f41f7c` | Decouple prepared visual state from the one-entry staged payload so the runtime can precompose frame+1 while staging frame+2, keeping a ready visual frame ahead of presentation. | Changed prepared state ownership in `src/foreground_pilot.c`, ran the exact no-holiday fishing1 gate across tripwire, aggressive, and copy-only variants, then reverted the source change. | Failed/no promotion. Early versions tripped correctness (`trip=1`) at frame 30 and then frame 54 because staged ownership could duplicate or stale a prepared frame. Fixing the target frame and forbidding CD reads during prepared waits made the run correctness-clean but flat: `loop_vb 1221 -> 1221`, `blocking_vb 5 -> 5`, `prefetch_overrun_vb 5 -> 5`, `due_misses=0`; the only nominal gain was non-loop `overrun_vb 150 -> 149` under a looser slack probe. Artifacts: `scratch/ps1-perf-iterate/20260426-212243/summary.json`, `scratch/ps1-perf-iterate/20260426-212651/summary.json`, `scratch/ps1-perf-iterate/20260426-212937/summary.json`, `scratch/ps1-perf-iterate/20260426-213347/summary.json`, `scratch/ps1-perf-iterate/20260426-213707/summary.json`, `scratch/ps1-perf-iterate/20260426-213937/summary.json`. | Do not promote. The experiment proves prepared/payload ownership can be made safe only if CD reads are blocked near the prepared-present deadline; under fishing1 that removes the intended speed win. Retry only as a first-class scheduler with explicit `prepared_used`, `prepared_missed`, `future_payload_resident`, and CD-budget metrics, or with a second RAM background/VRAM-present buffer that can truly prepare more than one visual state. |
| 2026-04-26 | `fg2-tail-group-384-396` | dirty experiment after `5e7b2b54` | Add the adjacent late-scene `384..396` sector group before the accepted `396..406` tail group to see whether another tail read can be removed safely. | Tested the table-only form and then a stricter adjacent-group append variant that allows `windowStart == currentEnd` only when explicit group metadata extends the target; both used 12-sector retained capacity and were reverted. | Failed/no promotion. Both variants kept correctness and timing flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `setloc=73`), but the new group did not affect runtime read shape and only raised the retained prefetch buffer `29712 -> 31760`. Artifacts: `scratch/ps1-perf-iterate/20260426-214510/summary.json`, `scratch/ps1-perf-iterate/20260426-215117/summary.json`. | Do not promote. Keep the 11-sector retained capacity; adjacent tail grouping needs a runtime append-start trace or generated pack metadata before adding memory back. |
| 2026-04-26 | `ps1-debug-tu-os` | `ps1: compile ps1 debug for size` | Continue the cold/default-off translation-unit compiler matrix with `src/ps1_debug.c`, expecting active foreground code layout to stay unchanged. | Added `src/ps1_debug.c` to the source-file `-Os` list and ran the exact no-holiday fishing1 gate against the memcard-size baseline. | Promoted as cold-code size reduction. Timing, CD, prefetch, work identity, FG2 LBA, PS-EXE bucket, and tracked hot symbol addresses/sizes stayed exact while `jcreborn.elf` shrank `740196 -> 739948`. Artifact: `scratch/ps1-perf-iterate/20260426-215442/summary.json`. | Promote, but do not count as a VBlank speed win. This keeps shrinking cold code safely inside the fragile layout bucket. |
| 2026-04-26 | `pause-menu-tu-os` | dirty experiment after `69fdae12` | Continue the cold/default-off translation-unit compiler matrix with `src/pause_menu.c`, expecting inactive pause-menu code to shrink without changing scene playback. | Added `src/pause_menu.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate against the ps1-debug-size baseline, then reverted the CMake change. | Failed/no promotion. The ELF shrank materially (`739948 -> 735944`), but the PS-EXE crossed a sector bucket (`149504 -> 147456`), `FG\\FISHING1.FG2` moved `399 -> 398`, and active CD cadence regressed (`loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, `loop_read_vb 283 -> 289`, `blocking_reads 4 -> 5`). Correctness, work identity, and tracked hot symbol sizes stayed clean. Artifact: `scratch/ps1-perf-iterate/20260426-220011/summary.json`. | Do not promote. This is a large size win but another one-sector shrink/CD-phase miss; retry only with deliberate layout padding/cold-section control or after the CD scheduler is phase-independent. |
| 2026-04-26 | `pause-menu-tu-os-cd-pad` | dirty experiment after `7d693804` | If the `pause_menu.c -Os` regression was only caused by moving foreground packs one sector earlier, preserve the proven `FISHING1.FG2` LBA with one dummy CD sector after `JCREBORN.EXE`. | Added `src/pause_menu.c` to the source-file `-Os` list and inserted `<dummy sectors="1"/>` after `JCREBORN.EXE`, then ran the exact no-holiday fishing1 gate with intentional layout-change allowance and reverted both changes. | Failed/no promotion. The dummy sector preserved `FG\\FISHING1.FG2 LBA=399`, but cadence still regressed: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb 5 -> 6`, and `prefetch_overrun_vb 5 -> 6`; PS-EXE bucket still shrank `149504 -> 147456`. Artifact: `scratch/ps1-perf-iterate/20260426-220613/summary.json`. | Do not promote. The miss is not merely foreground-pack LBA movement; executable/load/startup phase remains part of the deterministic scheduler. Large cold-code shrink needs a setup-to-loop cadence barrier or scheduler phase independence, not just CD padding. |
| 2026-04-26 | `setup-loop-clock-reset` | dirty experiment after `14c233aa` | Reset the FG2 scene clock immediately before `loop_start` so active playback does not inherit variable setup VBlanks. | Set `gFgRuntime.sceneClockTick = fgReadTickCounter()` just before `ps1PerfMarkLoopStart()`, ran the exact no-holiday fishing1 gate, then combined it with the rejected `pause_menu.c -Os` shrink before reverting both source changes. | Failed/no promotion. The clock reset alone was correctness-clean and timing-flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`) but moved tracked hot symbols by `+4` bytes with no measurable gain. Combined with `pause_menu.c -Os`, the prior regression remained (`loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb/prefetch_overrun_vb 5 -> 6`, `FISHING1.FG2 LBA 399 -> 398`). Artifacts: `scratch/ps1-perf-iterate/20260426-221020/summary.json`, `scratch/ps1-perf-iterate/20260426-221257/summary.json`. | Do not promote. A simple clock reset is not the needed phase barrier; future setup-to-loop work must control CD/controller phase or scheduler budgets, not just reset foreground elapsed-time accounting. |
| 2026-04-26 | `fg2-tail-group-384-396-direct-fill` | dirty experiment after `5f6a626d` | The earlier adjacent `384..396` group did not fire because the append path rejects adjacent windows. Try it as a direct grouped window fill with 12-sector capacity. | Added `384..396` to the fishing1 high-tide group table, raised retained group capacity from `11` to `12` sectors, applied grouped target extension to full window fills, ran the exact no-holiday fishing1 gate, then reverted the source change. | Failed/no promotion as a no-op with higher memory. Timing and read shape stayed flat (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `loop_read_vb=283`), but prefetch buffer size rose `29712 -> 31760`. Artifact: `scratch/ps1-perf-iterate/20260426-221805/summary.json`; CD sequence stayed unchanged in `scratch/ps1-perf-iterate/20260426-221805/fishing1/cd-read-sequence.json`. | Do not promote. The inferred `384..396` planner group still does not translate into a runtime read saving; retry only after adding append/window-start trace or generated group metadata that proves the group can fire. |
| 2026-04-26 | `release-sdk-libraries` | dirty experiment after `0d27a9a4` | The build cache uses an empty `CMAKE_BUILD_TYPE`, which links PSn00bSDK debug libraries. Try Release SDK libraries as a high-impact toolchain speed/size probe. | Added `-DCMAKE_BUILD_TYPE=Release` to the PS1 build, then isolated app codegen by clearing `CMAKE_C_FLAGS_RELEASE`, tested with and without five-sector CD padding, tested a stricter `4` VBlank refill guard, and tested a hybrid link that forced `psxcd` back to the debug archive. Reverted all build/source/layout changes and clean-reconfigured back to the default empty build type. | Failed/no promotion. Release SDK libraries shrank the executable bucket (`149504 -> 139264`) and improved total loop one VBlank (`loop_vb 1221 -> 1220`, `overrun_vb 150 -> 148`), but regressed visible CD pressure (`blocking_vb/prefetch_overrun_vb 5 -> 8`) even when `FISHING1.FG2 LBA=399` was preserved by padding. The `4` VBlank guard starved coverage (`due_misses=14`, `blocking_vb=76`), and debug-`psxcd` hybrid was worse (`loop_vb=1226`, `blocking_vb=14`). Artifacts: `scratch/ps1-perf-iterate/20260426-222220/summary.json`, `scratch/ps1-perf-iterate/20260426-222501/summary.json`, `scratch/ps1-perf-iterate/20260426-222733/summary.json`, `scratch/ps1-perf-iterate/20260426-223029/summary.json`, `scratch/ps1-perf-iterate/20260426-223330/summary.json`. | Do not promote under the current gate. Release libraries are promising for code size and raw loop cadence, but not until the CD scheduler can absorb the extra visible read pressure; retry with a phase-independent read scheduler or after moving CD waits off the active deadline. |
| 2026-04-26 | `spi-tu-os` | dirty experiment after `4957044b` | Compile the SPI/controller translation unit with `-Os` to shrink controller support without shifting earlier hot foreground/graphics/CD objects. | Added `src/spi.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate, then reverted the CMake change. | Failed/no promotion. Playback timing, CD pressure, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=399`, PS-EXE bucket `149504`), but the ELF grew `739948 -> 740092`. Artifact: `scratch/ps1-perf-iterate/20260426-223725/summary.json`. | Do not promote. This TU gets larger under `-Os`; leave it at the default `-O2` unless a future input-specific benchmark shows runtime benefit. |
| 2026-04-26 | `ps1-stubs-tu-os` | dirty experiment after `8087c884` | Compile the tail `ps1_stubs.c` translation unit with `-Os` to shrink libc/file/printf stubs without moving earlier hot objects. | Added `src/ps1_stubs.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate, then reverted the CMake change. | Failed/no promotion. The ELF shrank slightly (`739948 -> 739900`), but the PS-EXE crossed `149504 -> 147456`, moved `FISHING1.FG2 LBA 399 -> 398`, and regressed active CD cadence (`loop_vb 1221 -> 1222`, `blocking_vb/prefetch_overrun_vb 5 -> 6`, `loop_read_vb 283 -> 289`, `blocking_reads 4 -> 5`). Artifact: `scratch/ps1-perf-iterate/20260426-224104/summary.json`. | Do not promote. Another one-sector shrink/phase miss; tail stub size work needs explicit layout padding plus a scheduler that does not convert phase shifts into visible CD pressure. |
| 2026-04-26 | `holidays-tu-os-cd-pad` | dirty experiment after `d8b9d80d` | Re-test `holidays.c -Os` with one dummy CD sector after the executable to separate pack-LBA movement from executable/startup phase. | Added `src/holidays.c` to the source-file `-Os` list and inserted `<dummy sectors="1"/>` after `JCREBORN.EXE`, ran the exact no-holiday fishing1 gate with intentional layout-change allowance, then reverted both changes. | Failed/no promotion. The dummy sector preserved `FG\\FISHING1.FG2 LBA=399`, but cadence still regressed (`loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb/prefetch_overrun_vb 5 -> 6`); PS-EXE bucket shrank `149504 -> 147456`. Artifact: `scratch/ps1-perf-iterate/20260426-224446/summary.json`. | Do not promote. Like the pause-menu padding probe, this shows one-sector executable shrink changes active playback phase beyond foreground-pack LBA; retry only with a phase-independent scheduler or explicit executable/code-layout padding. |
| 2026-04-26 | `render-held-loop-perf-guard-removal` | dirty experiment after `6f6e0d9d` | Remove duplicate `ps1PerfEnabled` checks inside `ps1PerfMarkRenderedLoop()` and `ps1PerfMarkHeldLoop()` because all current call sites already guard those calls. | Removed the internal branches, ran the exact no-holiday fishing1 gate, then re-ran with one dummy CD sector after `JCREBORN.EXE` to preserve `FISHING1.FG2 LBA=399`; reverted both source/layout changes. | Failed/no promotion. The code shrink moved hot tracked symbols by `-40` bytes and crossed the PS-EXE bucket; with pack LBA restored by padding, cadence still regressed (`loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb/prefetch_overrun_vb 5 -> 6`). Artifacts: `scratch/ps1-perf-iterate/20260426-224837/summary.json`, `scratch/ps1-perf-iterate/20260426-225112/summary.json`. | Do not promote. Even semantically redundant hot-code branch removal changes code phase enough to expose one extra CD/refill VBlank; keep the guarded shape until scheduler phase is controlled. |
| 2026-04-26 | `foreground-pilot-tu-o3` | dirty experiment after `56c1d1d9` | Compile `foreground_pilot.c` with `-O3` because it owns the FG2 scheduler/read/compose loop and could produce a real VBlank-level win. | Added a source-file `-O3` override for `src/foreground_pilot.c`, ran the exact no-holiday fishing1 gate, then reverted the CMake change. | Failed/no promotion. The executable grew (`149504 -> 151552`) and moved `FG\\FISHING1.FG2 LBA 399 -> 400`; total loop stayed flat (`loop_vb=1221`) while CD pressure regressed (`blocking_vb/prefetch_overrun_vb 5 -> 7`). Hot foreground code also grew materially (`foregroundPilotPlay +2000`, `fgRuntimeLoadSceneFrame +612`, `fgRuntimeFillWindowForEntry +108`). Artifact: `scratch/ps1-perf-iterate/20260426-225455/summary.json`. | Do not promote. `-O3` bloats the scheduler without proving speed; future foreground codegen work should be function-scoped and address-padded, not a whole-TU flag. |
| 2026-04-26 | `cdrom-ps1-tu-o3` | dirty experiment after `10cce532` | Compile only `src/cdrom_ps1.c` with `-O3` because the CD helpers are hot and might reduce read-side CPU work without changing scheduler logic. | Added a source-file `-O3` override for `src/cdrom_ps1.c`, ran the exact no-holiday fishing1 gate, then reverted the CMake change. | Failed/no promotion. The executable grew three sectors (`149504 -> 155648`), moved `FG\\FISHING1.FG2 LBA 399 -> 402`, and regressed active playback: `loop_vb 1221 -> 1224`, `overrun_vb 150 -> 156`, `blocking_vb 5 -> 13`, and `prefetch_overrun_vb 5 -> 13`. The buffered CD helper shrank slightly (`ps1_streamReadFromCdFileIntoBuffered -8` bytes), but the layout/phase cost dominated. Artifact: `scratch/ps1-perf-iterate/20260426-225957/summary.json`. | Do not promote. Whole-TU `-O3` is unsafe for CD helpers under the current phase-sensitive baseline; retry only after hot/cold layout padding or function-scoped assembly/codegen can preserve the accepted executable/CD phase. |
| 2026-04-26 | `pure-cd-phase-sweep-1-4-8` | dirty experiment after `43b97d82` | Move active files later on disc without changing source or executable size, to isolate whether `FISHING1.FG2` physical LBA is itself a speed lever. | Inserted dummy sectors after `JCREBORN.EXE` and ran the exact no-holiday fishing1 gate with intentional layout-change allowance at `+1`, `+2`, `+3`, `+4`, and `+8` sectors, then reverted the layout. | Failed/no promotion as timing-flat. Every tested phase kept `loop_vb=1221`, `overrun_vb=150`, `blocking_vb=5`, and `prefetch_overrun_vb=5`; only `FG\\FISHING1.FG2` LBA changed (`399 -> 400`, `401`, `402`, `403`, `407`). Artifacts: `scratch/ps1-perf-iterate/20260426-230401/summary.json`, `scratch/ps1-perf-iterate/20260426-230650/summary.json`, `scratch/ps1-perf-iterate/20260426-230919/summary.json`, `scratch/ps1-perf-iterate/20260426-231145/summary.json`, `scratch/ps1-perf-iterate/20260426-231414/summary.json`. | Do not promote because there is no speed gain. This proves the `cdrom_ps1.c -O3` LBA `402` failure was not caused by FG2 LBA alone; executable/code shape or startup phase remains the unsafe variable. |
| 2026-04-26 | `held-loop-prefetch-wouldread-removal` | dirty experiment after `9221d822` | Avoid the redundant `fgRuntimeWindowPrefetchWouldRead()` scan before calling `fgRuntimeTryPrefetchWindow()` when a frame is already staged/prepared; the callee already checks whether a read is needed. | Removed the pre-check in the held loop, ran the exact no-holiday fishing1 gate, then added one dummy CD sector after `JCREBORN.EXE` to preserve `FISHING1.FG2 LBA=399`; reverted both source and layout changes. | Failed/no promotion. Without padding the executable shrank one sector (`149504 -> 147456`) and `FISHING1.FG2` moved `399 -> 398`; with padding the pack LBA returned to `399`, but cadence still regressed: `loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, and `prefetch_overrun_vb 5 -> 6`. The cleanup shrank `fgRuntimeTryPrefetchWindow` by `56` bytes and moved several hot symbols (`fgRuntimeLoadSceneFrame -68`, prepared helpers `-124`), which is the likely unsafe variable. Artifacts: `scratch/ps1-perf-iterate/20260426-231850/summary.json`, `scratch/ps1-perf-iterate/20260426-232127/summary.json`. | Do not promote. A semantically reasonable hot-loop cleanup still exposes the fifth visible read when executable/code phase shifts; retry only with hot-symbol padding or after the CD-first scheduler is phase-independent. |
| 2026-04-26 | `pure-executable-pad-2kb` | dirty experiment after `52f3dc33` | Add inert initialized PS1 data padding with one startup volatile read to change the loaded executable footprint without changing scene-loop logic. | Added a `2048` byte initialized phase pad in `jc_reborn.c`, ran the exact no-holiday fishing1 gate with layout-change allowance, then reverted the source change. | Failed/no promotion as timing-flat. The executable bucket grew `149504 -> 151552`, `FISHING1.FG2` moved `399 -> 400`, tracked hot symbols moved `+8`, and all key metrics matched baseline exactly (`loop_vb=1221`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`). Artifact: `scratch/ps1-perf-iterate/20260426-232627/summary.json`. | Do not promote because it spends memory and disc space without speed. The result is still useful: pure executable footprint growth is safe but not beneficial; future phase work should sweep hot-symbol addresses or scheduler ordering, not inert payload size alone. |
| 2026-04-26 | `fill-window-function-align-16-32` | dirty experiment after `1175c7de` | Align `fgRuntimeFillWindowForEntry()` to controlled boundaries to test whether small hot-symbol address buckets affect the remaining CD/refill pressure. | Added `__attribute__((aligned(16)))`, ran the exact no-holiday fishing1 gate, then changed the attribute to `aligned(32)` and repeated; reverted the source after both runs. | Failed/no promotion as timing-flat. `aligned(16)` moved tracked hot symbols by `+4`; `aligned(32)` moved `fgRuntimeFillWindowForEntry +20` and downstream hot symbols by `+36`. Both runs kept `loop_vb=1221`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=399`, and PS-EXE bucket `149504`. Artifacts: `scratch/ps1-perf-iterate/20260426-233011/summary.json`, `scratch/ps1-perf-iterate/20260426-233250/summary.json`. | Do not promote because there is no speed gain. Controlled positive hot-symbol shifts are safe in this range, but they do not recover any of the remaining `150` overrun VBlanks; prioritize scheduler/read-cost changes over more single-function alignment probes. |
| 2026-04-26 | `upload-rect-cap-6-retest` | dirty experiment after `924c7502` | Re-test lowering `GR_MAX_UPLOAD_RECTS` from `8` to `6` against the current baseline because fishing1's observed max remains `6`. | Changed the cap to `6`, ran the exact no-holiday fishing1 gate without `--require-improvement`, then reverted the source. | Failed/no promotion despite passing the gate. Timing, layout, and graphics work stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `max_upload_rects=6`, `cap_hits=0`, `full_fallbacks=0`, `FISHING1.FG2 LBA=399`), but there was no binary-size win (`jcreborn.exe=149504`, `jcreborn.elf=739948`) and no headroom above fishing1's exact max. Artifact: `scratch/ps1-perf-iterate/20260426-233704/summary.json`. | Do not promote. Cap `8` remains the better public/default value until a broader scene matrix proves a tighter cap is safe; a pass with no measured win is not enough. |
| 2026-04-26 | `current-perf-detail-attribution` | diagnostic sample after `fc6dcfde` | Refresh Detail-tier attribution for the current accepted baseline before further present/scheduler work. | Ran the exact no-holiday fishing1 case with `perf-detail` and no source changes. | Diagnostic pass. Timing matched Summary baseline (`loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`). Current attribution: `render_vb=179`, `present_wait_vb=157`, `restore_vb=26`, `compose_vb=32`, `advance_vb=1`, and `loop_read_vb=283`. Artifact: `scratch/ps1-perf-iterate/20260426-234118/summary.json`. | Use this to reprioritize: the remaining gap is dominated by present wait, not visible CD. Future large wins need prepared/double-buffer/present scheduling, while CD and upload micro-tuning are now secondary. |
| 2026-04-26 | `prepared-present-event-wait-removal` | dirty experiment after `fc6dcfde` | Remove the extra `eventsWaitTick(0)` after prepared-frame upload to test whether the prepared-present path still needs a post-upload input poll. | Removed the call from `fgRuntimePresentPreparedFrame()`, ran the exact no-holiday fishing1 gate, then reverted the source. | Failed/no promotion. The source shrink crossed the executable bucket (`149504 -> 147456`), moved `FISHING1.FG2 LBA 399 -> 398`, and regressed visible CD/refill pressure (`loop_vb 1221 -> 1222`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`). It also weakens pause/input polling semantics for no measured speed win. Artifact: `scratch/ps1-perf-iterate/20260426-234454/summary.json`. | Do not promote. Present-path polling should remain explicit until a broader input/present scheduler proves a safe replacement; small hot-code shrink is again phase-sensitive. |
| 2026-04-27 | `gated-short-hold-catchup` | dirty experiment after `66f8285a` | The Detail-tier run shows `present_wait_vb=157`, so test whether 4-VBlank host-deadline catch-up can be made safe only when the next frame is already prepared and the following payload is resident in the stream window. | Added a gated short-hold catch-up helper in `foreground_pilot.c`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion as a no-op. Correctness, layout, work identity, and all key metrics stayed exact: `loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `due_misses=0`, `restore_calls/compose_calls/upload_calls=155`, `FISHING1.FG2 LBA=399`, and PS-EXE bucket `149504`. Artifact: `scratch/ps1-perf-iterate/20260427-000618/summary.json`. | Do not promote. A safety-gated threshold-4 catch-up is too restrictive to move fishing1; present-wait work needs a structural prepared/dual-buffer scheduler or explicit hold rebalance, not another local threshold guard. |
| 2026-04-27 | `hot-cd-link-order` | dirty experiment after `1adc1c1a` | Move `cdrom_ps1.c` next to `foreground_pilot.c` in link order to test whether tighter scheduler/CD code locality improves the remaining refill/present cadence without source behavior changes. | Reordered the `SOURCES` list in `CMakeLists.txt`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the CMake change. | Failed/no promotion as a no-op. The probe moved tracked graphics/perf/CD symbols by about `+9496` bytes while keeping `loop_vb=1221`, `target_vb=1071`, `overrun_vb=150`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `due_misses=0`, `FISHING1.FG2 LBA=399`, PS-EXE bucket `149504`, and `jcreborn.elf=739948`. Artifact: `scratch/ps1-perf-iterate/20260427-001052/summary.json`. | Do not promote. This link-order bucket is safe but not useful; future layout work should sweep more targeted hot-section padding or use layout control to unlock known size wins, not simply move the whole CD TU earlier. |
| 2026-04-27 | `upload-coordinate-static-tables` | dirty experiment after `1b0ec483` | `grDrawBackground()` initializes the same background tile screen-coordinate arrays every call. Move them to static tables to reduce per-frame upload setup work. | Added static coordinate tables in `graphics_ps1.c`, used them for all upload `RECT` setup paths, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion. Timing, CD pressure, upload work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `FISHING1.FG2 LBA=399`), but `grDrawBackground` grew by `24` bytes and `jcreborn.elf` grew `739948 -> 740092`. Artifact: `scratch/ps1-perf-iterate/20260427-001446/summary.json`. | Do not promote. The compiler's stack-local constants are cheaper than the static-table shape here; upload-path wins should come from pack-emitted bands or fewer commands, not hoisting these two tiny arrays. |
| 2026-04-27 | `sound-ps1-tu-os` | dirty experiment after `0165e4de` | Compile `sound_ps1.c` with `-Os` to test whether the now-working audio subsystem can shrink without changing active foreground cadence. | Added `src/sound_ps1.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate, then repeated with one dummy CD sector after `JCREBORN.EXE` to restore `FISHING1.FG2 LBA=399`; reverted both changes. | Failed/no promotion. The flag shrank `jcreborn.elf 739948 -> 739776` and PS-EXE bucket `149504 -> 147456`, but regressed cadence both with and without CD padding: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb/prefetch_overrun_vb 5 -> 6`, `loop_read_vb 283 -> 289`, and `blocking_reads 4 -> 5`. Artifacts: `scratch/ps1-perf-iterate/20260427-001834/summary.json`, `scratch/ps1-perf-iterate/20260427-002123/summary.json`. | Do not promote. The audio TU shrink is a real size win but another one-sector executable/load-phase miss; retry only with code-phase control or a CD scheduler that does not convert the smaller executable into an extra visible read. |
| 2026-04-27 | `resource-tu-os` | dirty experiment after `d55bf76b` | Compile `resource.c` with `-Os` to test whether setup/resource-loading code can shrink without hurting the active FG2 loop. | Added `src/resource.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the CMake change. | Failed/no promotion. The ELF shrank materially (`739948 -> 738376`), but the PS-EXE crossed `149504 -> 147456`, `FISHING1.FG2` moved `399 -> 398`, and active cadence regressed: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb/prefetch_overrun_vb 5 -> 6`, `loop_read_vb 283 -> 289`, and `blocking_reads 4 -> 5`. Tracked foreground hot symbols moved `-240` bytes. Artifact: `scratch/ps1-perf-iterate/20260427-004330/summary.json`. | Do not promote. Resource-code shrink is another phase-sensitive one-sector miss; retry only with hot-symbol/code-phase control or after the CD scheduler no longer turns small executable shifts into visible read pressure. |
| 2026-04-27 | `ps1-perf-tu-o3` | dirty experiment after `46f83b8a` | Compile `ps1_perf.c` with `-O3` because perf counters are on the accepted `perf-log` hot path and might lower counter overhead without touching foreground/CD logic. | Added a source-file `-O3` override for `src/ps1_perf.c`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the CMake change. | Failed/no promotion. The executable crossed `149504 -> 147456`, `FISHING1.FG2` moved `399 -> 398`, `jcreborn.elf` grew `739948 -> 742300`, `ps1PerfMarkCdReadDetailed` grew by `912` bytes, and active cadence regressed: `loop_vb 1221 -> 1222`, `target_vb 1071 -> 1072`, `blocking_vb/prefetch_overrun_vb 5 -> 6`, and `loop_read_vb 283 -> 289`. Artifact: `scratch/ps1-perf-iterate/20260427-004755/summary.json`. | Do not promote. Whole-TU `-O3` is unsafe even for the perf module; future perf-counter work should remove or compile-time isolate counters for release, not expand the summary-counter implementation. |
| 2026-04-27 | `ps1-perf-tu-os` | dirty experiment after `0741dd8f` | Compile `ps1_perf.c` with `-Os` as a separate codegen shape from the failed `-O3` probe, checking whether the perf-log implementation can shrink while staying cadence-neutral. | Added `src/ps1_perf.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the CMake change. | Failed/no promotion. Timing, CD pressure, FG2 LBA, and PS-EXE bucket stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`), and `jcreborn.elf` shrank `739948 -> 737472`; however the executable/runtime size did not move and hot perf functions grew (`ps1PerfMarkCdReadDetailed +988`, `ps1PerfEndScene +372`), so the gate had no promotable speed or runtime-size win. Artifact: `scratch/ps1-perf-iterate/20260427-005153/summary.json`. | Do not promote. `-Os` on the perf TU is layout-safe but not runtime-useful; prefer release/perf split or counter removal/isolation over whole-TU perf codegen flags. |
| 2026-04-27 | `perf-advance-unused-target` | `ps1: drop unused perf advance target` | `ps1PerfMarkAdvance()` accepted a `targetVBlanks` argument that the implementation ignored, so every foreground advance was loading/passing a dead hot-path value. | Removed the unused argument from the PS1 and host-stub declarations, implementation, and `foregroundPilotRuntimeAdvance()` call site. Ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update. | Promoted as flat-timing hot-path cleanup. Timing, CD pressure, layout, and work identity stayed exact (`loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `FISHING1.FG2 LBA=399`, `jcreborn.exe=149504`), while `foregroundPilotRuntimeAdvance.part.0` shrank by `4` bytes and `jcreborn.elf` shrank `739948 -> 739900`. Artifacts: `scratch/ps1-perf-iterate/20260427-005627/summary.json`, `scratch/ps1-perf-iterate/20260427-005923/summary.json`. | Promote, but do not claim a VBlank speed win. This removes real hot-path waste and becomes the new local branch baseline for future experiments. |
| 2026-04-27 | `unused-cd-perf-wrapper-removal` | dirty experiment after `3545aacf` | Remove the now-unused `ps1PerfMarkCdRead()` wrapper because modern CD paths call `ps1PerfMarkCdReadDetailed()` directly. | Removed the declaration, host stub, and implementation wrapper, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion. The source deletion crossed the PS-EXE bucket (`149504 -> 147456`), moved `FISHING1.FG2 LBA 399 -> 398`, unexpectedly expanded/rephased `ps1PerfMarkCdReadDetailed` by `912` bytes, and regressed cadence (`loop_vb 1221 -> 1222`, `blocking_vb/prefetch_overrun_vb 5 -> 6`, `loop_read_vb 283 -> 289`, `blocking_reads 4 -> 5`). Artifact: `scratch/ps1-perf-iterate/20260427-010345/summary.json`. | Do not promote. The wrapper is dead semantically but acts as layout ballast for the perf/CD code shape; remove it only with explicit code-phase padding or release/perf split work. |
| 2026-04-27 | `host-fifth-visible-read-locator` | `ps1: compare perf CD log summaries` | The latest failed cleanups repeatedly differ from baseline by `blocking_reads 4 -> 5`; identify the read boundary externally instead of adding PS1-side metrics that perturb cadence. | Added `--compare` mode to `scripts/ps1-perf-cdlog-summary.py`. It compares a candidate CD summary to an accepted baseline, normalizes pack reads by FG2 file sector, reports `JCPERF2` deltas, and ranks the strongest extra-visible-read timing candidates. Tested against the accepted `20260427-005923` baseline and the failed wrapper-removal run. | Promoted as tooling safety. The comparison correctly reports `blocking_reads +1`, `blocking_vb +1`, and `prefetch_overrun_vb +1`; it maps the strongest positive timing candidate to file sector `106..110` / covered entry `38`, with other one-VBlank suspects at sectors `1`, `34`, `110`, and `117`. Artifacts used: `/tmp/baseline-cd.json`, `/tmp/failed-cd.json`; source summaries remain `scratch/ps1-perf-iterate/20260427-005923/summary.json` and `scratch/ps1-perf-iterate/20260427-010345/summary.json`. | Promote, but do not claim a runtime speed win. This gives future phase/CD experiments a concrete read-boundary locator without changing the measured PS1 executable. |
| 2026-04-27 | `host-delivered-sector-cd-summary` | dirty tooling after `79b68b6f` | The first CD-summary grouping pass inferred read lengths from the next `ReadN`, which is wrong when the runtime issues overlapping reads. Parse DuckStation `DataSector` lines host-side so group planning uses actual delivered sector counts without PS1-side metrics. | Added delivered-sector parsing to `scripts/ps1-perf-cdlog-summary.py`, normalized DuckStation physical DataSector LBAs back to logical LBAs, and re-ran baseline plus wrapper-removal comparison summaries. | Promoted as tooling safety. The baseline fishing1 pack now reports `408` delivered active-loop sectors and a `12`-sector plan of `68 -> 47` reads with `sector_delta=-3`, rather than the earlier approximate `68 -> 46`/`sector_delta=0`; the wrapper-removal comparison still flags `blocking_reads +1` and keeps sector `106..110` as the top timing suspect. Artifacts used: `/tmp/current-cd-delivered.json`, `/tmp/wrapper-cd-delivered-compare.json`. | Promote, but do not claim a runtime speed win. Future grouped-read experiments should use delivered-sector output, not next-read inference, before adding runtime groups. |
| 2026-04-27 | `fg2-group-106-117` | dirty experiment after `56a21d5a` | Use the new fifth-read locator's strongest sector neighborhood and try one narrow 11-sector group, `106..117`, without increasing retained prefetch capacity. | Added `{106, 117}` beside the accepted tail group in `kFishing1HighReadGroups12`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion as a no-op with larger code. Timing, CD shape, and memory stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `loop_read_vb=283`, `group_hits=0`, prefetch buffer `29712`), but the helper grew and `jcreborn.elf 739900 -> 740444`. Artifact: `scratch/ps1-perf-iterate/20260427-012934/summary.json`. | Do not promote. The sector is a timing suspect, but this hard-coded group does not fire in the current append path; retry only with append-start trace data or generated pack metadata that proves the runtime can enter the group. |
| 2026-04-27 | `fg2-group-106-117-cap15` | dirty experiment after `69cf8a33` | Check whether the same `106..117` group was blocked only by retained-window capacity, by raising group capacity from `11` to `15` sectors for the probe. | Added `{106, 117}` and raised `FG_PREFETCH_GROUP_WINDOW_BYTES` to `15 * 2048`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion. Timing and CD shape were still exact no-op (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `loop_read_vb=283`, `setloc=73`, `group_hits=0`), while prefetch heap grew `29712 -> 37904` and ELF grew `739900 -> 740444`. Artifact: `scratch/ps1-perf-iterate/20260427-013405/summary.json`. | Do not promote. Capacity was not the blocker; the runtime never reaches this hard-coded group path. The next grouped-read step must be append-start tracing or generated metadata, not more manual early-sector entries. |
| 2026-04-27 | `fg2-group-384-396-cap13` | dirty experiment after `fd988524` | Use delivered-sector planning to retry the late `384..396` two-read span immediately before the accepted `396..406` tail group, with the smallest retained capacity that can hold the 12-sector group plus current preservation. | Added `{384, 396}` before the accepted tail group and raised `FG_PREFETCH_GROUP_WINDOW_BYTES` from `11` to `13` sectors, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion. Timing and CD shape stayed exact no-op (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `loop_read_vb=283`, `setloc=73`, `blocking_reads=4`), while the prefetch heap grew `29712 -> 33808` and `fgRuntimeFillWindowForEntry` grew by `72` bytes. Artifact: `scratch/ps1-perf-iterate/20260427-022048/summary.json`. | Do not promote. Delivered-sector planning identifies the span, but this hard-coded runtime group does not turn into a saved read under the current append path; require append-start/slack ownership tracing or generated metadata before more manual group entries. |
| 2026-04-27 | `graphics-ps1-tu-o3` | dirty experiment after `dcfd46bf` | Compile only `src/graphics_ps1.c` with `-O3` to test whether the restore/compose/upload hot path benefits from aggressive optimization. | Added a source-file `-O3` override for `graphics_ps1.c`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the CMake change. | Failed/no promotion. The executable grew two sectors (`149504 -> 153600`), moved `FISHING1.FG2 LBA 399 -> 401`, and active playback regressed: `loop_vb 1221 -> 1225`, `overrun_vb 150 -> 156`, `blocking_vb/prefetch_overrun_vb 5 -> 11`, while `target_vb` shifted `1071 -> 1069`. Hot graphics code grew materially (`grDrawBackground +808`, `grRestoreBgFromRects +600`) despite `grCompositePacked4SpansToBackground -28`. Artifact: `scratch/ps1-perf-iterate/20260427-020413/summary.json`. | Do not promote. Whole-TU `-O3` is unsafe for graphics too; future graphics codegen should be helper-scoped, assembly, or pack-generated rather than broad compiler expansion. |
| 2026-04-27 | `pal4-compositor-function-o3` | dirty experiment after `9c7380fa` | Since whole-TU graphics `-O3` shrank only `grCompositePacked4SpansToBackground`, test `__attribute__((optimize("O3")))` on just that PAL4 compositor and then control for pack LBA with a temporary one-sector CD pad. | Added the function attribute, ran the exact no-holiday fishing1 gate with `--require-improvement`, then repeated with a temporary dummy sector after `JCREBORN.EXE` and `--allow-layout-change`; reverted both source and layout. | Failed/no promotion. Without the pad, `FISHING1.FG2` moved `399 -> 398` and cadence regressed (`loop_vb 1221 -> 1226`, `blocking_vb/prefetch_overrun_vb 5 -> 11`). With the pad, `FISHING1.FG2` returned to LBA `399`, but the regression remained (`loop_vb 1221 -> 1226`, `overrun_vb 150 -> 158`, `blocking_vb/prefetch_overrun_vb 5 -> 11`) while the compositor shrank only `28` bytes. Artifacts: `scratch/ps1-perf-iterate/20260427-020800/summary.json`, `scratch/ps1-perf-iterate/20260427-021205/summary.json`. | Do not promote. The PAL4 compositor's `-O3` code shape is slower even when pack LBA is restored; future compositor wins need generated/assembly code with measured instruction shape, not GCC `O3` on the current helper. |
| 2026-04-27 | `foreground-pilot-tu-os` | `ps1: compile foreground pilot for size` | Test whether the foreground scheduler can use `-Os` as a size/I-cache cleanup without the broad `-O3` code growth that previously raised visible CD pressure. | Added `src/foreground_pilot.c` to the source-file `-Os` list, ran the exact no-holiday fishing1 gate, then accepted with `--allow-layout-change` because the timing/work/correctness identity stayed exact while the executable shrank. | Promoted as flat-timing size/code-shape cleanup. Timing, CD, prefetch, graphics work, heap, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `restore/compose/upload_calls=155`, `due_misses=0`), while PS-EXE shrank `149504 -> 145408`, ELF shrank `739900 -> 727716`, and `FISHING1.FG2 LBA` moved `399 -> 397` with no measured cadence penalty. Artifacts: `scratch/ps1-perf-iterate/20260427-022457/summary.json`, `scratch/ps1-perf-iterate/20260427-022748/summary.json`. | Promote, but do not claim a VBlank speed win. This is a material runtime binary-size win that keeps the current fishing1 speed baseline unchanged and becomes the new branch baseline. |
| 2026-04-27 | `holidays-tu-os-post-foreground-size` | `ps1: compile holidays for size` | Retry the earlier failed `holidays.c -Os` cold-code shrink after the foreground-size baseline changed code/CD phase; the old failure was a one-sector cadence miss that may no longer apply. | Added `src/holidays.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as cumulative size cleanup. The old CD regression disappeared: timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `FISHING1.FG2 LBA=397`, PS-EXE `145408`), while the ELF shrank `727716 -> 724868`. Artifacts: `scratch/ps1-perf-iterate/20260427-024324/summary.json`, `scratch/ps1-perf-iterate/20260427-024608/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. This proves at least one old no-promotion size cleanup became safe after the foreground-size baseline shift. |
| 2026-04-27 | `resource-tu-os-post-foreground-size` | `ps1: compile resource loader for size` | Retry the earlier failed `resource.c -Os` setup/resource-loading shrink after the foreground-size and holidays-size phase shifts. | Added `src/resource.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as cumulative size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `FISHING1.FG2 LBA=397`, PS-EXE `145408`), while the ELF shrank `724868 -> 723284`. Artifacts: `scratch/ps1-perf-iterate/20260427-025010/summary.json`, `scratch/ps1-perf-iterate/20260427-025251/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. Resource-size work is now phase-safe in this branch baseline and keeps setup/playback identity intact. |
| 2026-04-27 | `sound-ps1-tu-os-post-foreground-size` | `ps1: compile sound for size` | Retry the earlier failed `sound_ps1.c -Os` audio shrink after the foreground/resource size phase shifts; fishing1 exercises sound events, so this gate can catch cursor/timing regressions. | Added `src/sound_ps1.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/sound correctness stayed exact and the ELF shrank. | Promoted as cumulative size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `sound_events=9`, `sound_late=0`, `sound_cursor_end=9`, `FISHING1.FG2 LBA=397`, PS-EXE `145408`), while the ELF shrank `723284 -> 723104`. Artifacts: `scratch/ps1-perf-iterate/20260427-025629/summary.json`, `scratch/ps1-perf-iterate/20260427-025901/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. The audio TU is now safe under this phase and preserves the fully working sound path. |
| 2026-04-27 | `pause-menu-tu-os-post-foreground-size` | `ps1: compile pause menu for size` | Retry the earlier failed `pause_menu.c -Os` cold/UI shrink after the foreground/resource/sound size phase shifts; the old failure was a one-sector cadence miss. | Added `src/pause_menu.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with `--allow-layout-change` because timing/work/correctness stayed exact while the loaded executable crossed a smaller sector bucket. | Promoted as a loaded-executable size cleanup. Timing, CD, prefetch, graphics work, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `due_misses=0`), while PS-EXE shrank `145408 -> 143360`, ELF shrank `723104 -> 719096`, and `FISHING1.FG2 LBA` moved `397 -> 396` with no measured cadence penalty. Artifacts: `scratch/ps1-perf-iterate/20260427-032330/summary.json`, `scratch/ps1-perf-iterate/20260427-032607/summary.json`. | Promote, but do not claim a VBlank speed win. The fishing1 perf gate does not exercise pause-menu UI navigation, so keep normal pause visual/input validation in the merge checklist. |
| 2026-04-27 | `ps1-stubs-tu-os-post-foreground-size` | `ps1: compile ps1 stubs for size` | Retry the earlier failed `ps1_stubs.c -Os` tail-stub shrink after the loaded executable has moved to the smaller pause-menu baseline. | Added `src/ps1_stubs.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank slightly. | Promoted as a tiny cumulative size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while the ELF shrank `719096 -> 719048`. Artifacts: `scratch/ps1-perf-iterate/20260427-032953/summary.json`, `scratch/ps1-perf-iterate/20260427-033239/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. This is only a 48-byte ELF cleanup; its value is cumulative pressure toward the next sector-boundary win. |
| 2026-04-27 | `events-ps1-tu-os-post-foreground-size` | `ps1: compile events for size` | Test `events_ps1.c -Os` after the pause/stub size baseline; event/pause code runs on every rendered frame, so the perf gate should catch timing regressions even though it does not exercise menu navigation. | Added `src/events_ps1.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as cumulative size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while the ELF shrank `719048 -> 717796`. Artifacts: `scratch/ps1-perf-iterate/20260427-033624/summary.json`, `scratch/ps1-perf-iterate/20260427-033855/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. Pause/input behavior still needs normal visual/input validation before merging this size wave. |
| 2026-04-27 | `utils-tu-os-post-foreground-size` | `ps1: compile utils for size` | Test `utils.c -Os` after the events-size baseline; utility code is mostly support path, so the gate should catch any unexpected code-phase or hot helper regression. | Added `src/utils.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as cumulative size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while the ELF shrank `717796 -> 716584`. Artifacts: `scratch/ps1-perf-iterate/20260427-040329/summary.json`, `scratch/ps1-perf-iterate/20260427-040602/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. This is cumulative ELF pressure only; continue looking for CD/render changes that move the remaining `150` VBlank gap. |
| 2026-04-27 | `uncompress-tu-os-post-utils-size` | dirty experiment after `2f6b664c` | Test `uncompress.c -Os` after the utility-size baseline, checking whether legacy decompression/setup support has any remaining size sensitivity. | Added `src/uncompress.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because no tracked metric moved. | Failed/no promotion as an exact no-op. Timing, CD, prefetch, graphics work, layout, correctness, PS-EXE, and ELF all matched the accepted baseline (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`, ELF `716584`). Artifact: `scratch/ps1-perf-iterate/20260427-041125/summary.json`. | Do not promote. `uncompress.c` is exhausted as an isolated compiler-flag probe unless the decompression path itself changes. |
| 2026-04-27 | `island-tu-os-post-utils-size` | `ps1: compile island for size` | Test `island.c -Os` after the utility-size baseline; island placement is fixed in the perf boot, so exact metrics should catch any placement or scheduler perturbation. | Added `src/island.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as small cumulative size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while the ELF shrank `716584 -> 716340`; tracked hot-symbol addresses moved by `-48` bytes with no measured cadence effect. Artifacts: `scratch/ps1-perf-iterate/20260427-041449/summary.json`, `scratch/ps1-perf-iterate/20260427-041722/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. Keep broader random-island visual validation in the merge checklist because fishing1 uses one explicit island position. |
| 2026-04-27 | `jc-reborn-tu-os-post-island-size` | `ps1: compile main for size` | Test `jc_reborn.c -Os` after the island-size baseline; main boot/menu/default-path code may shrink materially, but the exact gate must reject any startup phase shift that becomes visible CD pressure. | Added `src/jc_reborn.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as cumulative main-TU size cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while the ELF shrank `716340 -> 714520`; tracked hot-symbol addresses moved by `-404` bytes with no measured cadence effect. Artifacts: `scratch/ps1-perf-iterate/20260427-042105/summary.json`, `scratch/ps1-perf-iterate/20260427-042342/summary.json`. | Promote, but do not claim a VBlank or loaded-executable-size win. Keep menu/pause visual validation in the merge checklist because the fishing1 perf gate boots through but does not interactively exercise the UI. |
| 2026-04-27 | `cdrom-ps1-tu-os-post-main-size` | dirty experiment after `84b65b9d` | Test `cdrom_ps1.c -Os` because the remaining gap is CD/prefetch-facing and smaller CD helpers might reduce hot-path pressure. | Added `src/cdrom_ps1.c` to the accepted `-Os` source-file list, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source after cadence and layout regressed. | Failed/no promotion. The flag shrank PS-EXE `143360 -> 141312`, moved `FISHING1.FG2 LBA 396 -> 395`, and shrank CD helper symbols, but active playback regressed (`loop_vb 1221 -> 1224`, `overrun_vb 150 -> 153`, `blocking_vb/prefetch_overrun_vb 5 -> 10`). Artifact: `scratch/ps1-perf-iterate/20260427-042724/summary.json`. | Do not promote. Whole-TU `-Os` on CD code is not safe under the current scheduler/layout phase; retry only with function-scoped codegen or explicit code/CD phase control. |
| 2026-04-27 | `buffered-read-file-lba-cache` | dirty experiment after `57f1c912` | Cache the CD file LBA once inside `ps1_streamReadFromCdFileIntoBuffered()` so each read avoids recomputing it for `CdIntToPos` and perf metadata. | Added a local `fileLba`, used it for the setloc calculation and perf field, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because it did not improve and grew code. | Failed/no promotion. Timing, CD pressure, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), but the buffered CD helper grew by `8` bytes and ELF grew `714520 -> 714808`. Artifact: `scratch/ps1-perf-iterate/20260427-044424/summary.json`. | Do not promote. The compiler's current repeated-call shape is smaller; cache file LBA only as part of broader CD helper restructuring that proves a real timing win. |
| 2026-04-27 | `upload-perf-guard-combine` | `ps1: combine upload perf guards` | Combine the adjacent `ps1PerfEnabled && dirtyCount > 0` upload and dirty-rect perf guards in `grDrawBackground()` so the hot upload path branches once before recording both counters. | Wrapped `ps1PerfMarkUpload()` and `ps1PerfMarkDirtyRect()` in a single guard, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and code shrank. | Promoted as hot upload-path code cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while `grDrawBackground` shrank by `64` bytes and ELF shrank `714520 -> 714440`. Artifacts: `scratch/ps1-perf-iterate/20260427-044854/summary.json`, `scratch/ps1-perf-iterate/20260427-045125/summary.json`. | Promote, but do not claim a VBlank speed win. Keep combining only adjacent hot guards when the compiler proves code shrink; earlier guard-combine shapes grew code. |
| 2026-04-27 | `upload-perf-guard-local-cache` | dirty experiment after `f48f45ac` | Cache `ps1PerfEnabled && dirtyCount > 0` in a local so the upload path computes the guard once for both the start tick and final perf marks. | Added `trackUploadPerf`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because it did not improve timing and grew the overall ELF. | Failed/no promotion. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`); `grDrawBackground` shrank by another `56` bytes, but total ELF grew `714440 -> 714456` and no measured speed metric moved. Artifact: `scratch/ps1-perf-iterate/20260427-045508/summary.json`. | Do not promote alone. Function-local shrink is not enough when the accepted artifact grows and cadence is flat; retry only if paired with a broader upload-path cleanup. |
| 2026-04-27 | `perf-detail-fast-inline` | dirty experiment after `aa76ea28` | Inline the summary/detail perf-level check for the two rendered-frame users so summary perf runs avoid calling `ps1PerfDetailEnabled()` in `foregroundPilotPlay()` and `grUpdateDisplay()`. | Added `ps1PerfDetailFast()` in the header, switched the two call sites, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because it did not improve timing and grew the overall ELF. | Failed/no promotion. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`); `foregroundPilotPlay` shrank by `24` bytes and `grUpdateDisplay` shrank by `40` bytes, but total ELF grew `714440 -> 714564` and no measured speed metric moved. Artifact: `scratch/ps1-perf-iterate/20260427-045947/summary.json`. | Do not promote. Inline detail checks only make sense if paired with a release/perf split that removes the external helper or proves a VBlank-level win. |
| 2026-04-27 | `upload-perf-marker-combine` | `ps1: combine upload perf markers` | Replace the paired upload and dirty-rect perf marker calls with one combined marker so the hot upload path records the same summary counters through one function call. | Added `ps1PerfMarkUploadDirty()`, removed the separate upload/dirty marker implementations, updated `grDrawBackground()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and code shrank. | Promoted as perf-log hot-path cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `dirty_exact_bytes=16281600`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while ELF shrank `714440 -> 714260`. Artifacts: `scratch/ps1-perf-iterate/20260427-052422/summary.json`, `scratch/ps1-perf-iterate/20260427-052651/summary.json`. | Promote, but do not claim a VBlank speed win. This keeps perf-log observability intact while removing one rendered-frame marker call. |
| 2026-04-27 | `grdrawbackground-function-os` | `ps1: optimize background upload for size` | Compile only `grDrawBackground()` with `-Os` to shrink the hot dirty-upload function without exposing the whole graphics TU to broad codegen changes. | Added a function-scoped `optimize("Os")` attribute to `grDrawBackground()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as hot upload-function code-shape cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while `grDrawBackground` shrank by `32` bytes and ELF shrank `714260 -> 713672`. Artifacts: `scratch/ps1-perf-iterate/20260427-053119/summary.json`, `scratch/ps1-perf-iterate/20260427-053358/summary.json`. | Promote, but do not claim a VBlank speed win. This scoped `-Os` is safe for the upload function under fishing1; whole graphics `O3` remains rejected. |
| 2026-04-27 | `grupdatedisplay-function-os` | `ps1: optimize display update for size` | Compile only `grUpdateDisplay()` with `-Os` to shrink the rendered-frame wrapper without exposing the whole graphics TU to broad codegen changes. | Added a function-scoped `optimize("Os")` attribute to `grUpdateDisplay()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as hot display-wrapper code-shape cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while `grUpdateDisplay` shrank by `40` bytes and ELF shrank `713672 -> 713496`. Artifacts: `scratch/ps1-perf-iterate/20260427-060420/summary.json`, `scratch/ps1-perf-iterate/20260427-060651/summary.json`. | Promote, but do not claim a VBlank speed win. This scoped `-Os` is safe for the wrapper under fishing1; continue testing function-scoped codegen one function at a time. |
| 2026-04-27 | `grrestorebgfromrects-function-os` | dirty experiment after `df87b382` | Compile only `grRestoreBgFromRects()` with `-Os` to shrink the clean-rect restore wrapper without exposing the whole graphics TU to broad codegen changes. | Added a function-scoped `optimize("Os")` attribute, ran the exact no-holiday fishing1 gate with `--require-improvement`, then ran a no-require acceptance check to inspect size/work before reverting the source. | Failed/no promotion. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_calls=155`, `restore_bytes=2510092`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), and `grRestoreBgFromRects` shrank `980 -> 108` bytes, but total ELF grew `713496 -> 714132` with no speed metric movement. Artifacts: `scratch/ps1-perf-iterate/20260427-061043/summary.json`, `scratch/ps1-perf-iterate/20260427-061318/summary.json`. | Do not promote. A local hot-symbol shrink is not enough when the accepted artifact grows and cadence is flat; retry only if paired with broader graphics layout/codegen work that also shrinks the final binary or moves VBlank metrics. |
| 2026-04-27 | `pal4-compositor-function-os` | dirty experiment after `9b21f3f8` | Retry scoped compiler-codegen on the PAL4 compositor with `-Os` instead of the already-rejected `-O3`, hoping for a loaded-executable shrink without the broad graphics-TU expansion. | Added a function-scoped `optimize("Os")` attribute to `grCompositePacked4SpansToBackground()`, ran the exact no-holiday fishing1 gate, then repeated with a temporary one-sector `CDPHASE.PAD` control that preserved `FISHING1.FG2` at LBA `396`; reverted both source and layout. | Failed/no promotion. The compositor shrank `2692 -> 1620` bytes and PS-EXE shrank `143360 -> 141312`, but cadence regressed in both runs: `loop_vb 1221 -> 1225`, `overrun_vb 150 -> 155`, `blocking_vb 5 -> 26`, `prefetch_overrun_vb 5 -> 12`, `loop_reads 67 -> 68`, `due_misses 0 -> 3`. The padded run kept `FISHING1.FG2 LBA=396`, proving this is not just a foreground-pack LBA shift. Artifacts: `scratch/ps1-perf-iterate/20260427-061744/summary.json`, `scratch/ps1-perf-iterate/20260427-062041/summary.json`. | Do not promote. The smaller compositor code shape is slower/phase-worse under the current scheduler even with CD placement restored; future compositor work should be generated or hand-shaped, not GCC `-Os` on the current helper. |
| 2026-04-27 | `single-band-narrow-upload-scratch` | dirty experiment after `075a20d2` | When exactly one upload band is pending and no primitive buffer is queued, pack the row X extent into the existing primitive buffer and `LoadImage` only that narrow rectangle, avoiding new heap allocations and avoiding scratch reuse across async uploads. | Added helper code that reuses `primitiveBuffer[db]` only for single-band or single-tile upload cases, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion as a pure no-op with code growth. Timing, CD, prefetch, layout, and correctness stayed exact (`loop_vb=1221`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), but upload work did not move (`upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `max_upload_rects=6`) and `grDrawBackground` grew by `200` bytes while ELF grew `713496 -> 716736`. Artifact: `scratch/ps1-perf-iterate/20260427-062713/summary.json`. | Do not promote. Fishing1's active upload bands are not helped by a single-band scratch path; narrow uploads need generated multi-rect packing or a measured multi-buffer design, not this special case. |
| 2026-04-27 | `touched-dirty-row-clear` | `ps1: clear touched dirty rows only` | `grClearCurrDirtyState()` was clearing all `4 * 240` current dirty-row entries every frame even though `currDirtyMinY/MaxY` already bounds the rows that were touched. Clear only those touched rows to remove hot per-frame stores without changing dirty semantics. | Changed current dirty-row clearing to iterate each tile's existing `currDirtyMinY..currDirtyMaxY` range before resetting the min/max state. Ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and the ELF shrank. | Promoted as hot dirty-state cleanup. Timing, CD, prefetch, graphics work, layout, and correctness stayed exact (`loop_vb=1221`, `target_vb=1071`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_bytes=2510092`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while ELF shrank `713496 -> 712692`. Artifacts: `scratch/ps1-perf-iterate/20260427-063158/summary.json`, `scratch/ps1-perf-iterate/20260427-063429/summary.json`. | Promote, but do not claim a VBlank speed win. The invariant is that every current dirty-row write updates the per-tile current dirty Y range; keep full clears for initialization and forced full-redraw paths. |
| 2026-04-27 | `touched-dirty-row-promote` | `ps1: promote touched dirty rows only` | After the touched-clear cleanup, `grDrawBackground()` still copied both full current dirty-row tables into previous dirty-row tables every frame. Promote only the touched row ranges and clear only the previously touched previous-row ranges. | Added shared dirty-row range clearing and replaced the full-table `memcpy` promotion with bounded row-range promotion. Ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a baseline update after a repeat pass. | Promoted as a real hot dirty-state speed win. `loop_vb 1221 -> 1219`, `target_vb 1071 -> 1072`, `overrun_vb 150 -> 147`, `scene_vb 1400 -> 1398`, `loop_read_vb 283 -> 282`; `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_bytes=2510092`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `FISHING1.FG2 LBA=396`, and PS-EXE `143360` stayed stable. `grDrawBackground` shrank by `144` bytes; ELF moved `712692 -> 712828`. Artifacts: `scratch/ps1-perf-iterate/20260427-064523/summary.json`, `scratch/ps1-perf-iterate/20260427-064747/summary.json`. | Promote and count as a VBlank speed win. The key invariant is unchanged: every current dirty-row write must update the current per-tile Y range before row promotion. |
| 2026-04-27 | `pal4-direct-row-dirty-mark` | dirty experiment after `9aba67f7` | Replace the PAL4 compositor's per-row calls to the general `grMarkRectDirty()` helper with direct `markTileDirtyRect()` calls, avoiding redundant rectangle clipping/splitting for known single-tile row spans. | Changed the row-end dirty marking in `grCompositePacked4SpansToBackground()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source after red-team review. | Failed/no promotion despite a formal gate pass. Runtime work and CD pressure stayed stable (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_bytes=2510092`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`), and `overrun_vb` moved `147 -> 146`, but only because `target_vb` shifted `1072 -> 1073`; actual loop speed did not improve and the compositor grew by `616` bytes while ELF grew `712828 -> 715112`. Artifact: `scratch/ps1-perf-iterate/20260427-065308/summary.json`. | Do not promote. Treat target-only overrun movement with flat `loop_vb` and large code growth as a false positive; retry only if generated pack metadata or a smaller direct-mark helper avoids the compositor bloat. |
| 2026-04-27 | `dirty-row-promote-overlap-clear-skip` | dirty experiment after `5b089f55` | In touched-row promotion, avoid clearing previous-row entries that overlap the current dirty range because those rows are overwritten by the current-row copy anyway. | Changed `grPromoteCurrDirtyToPrev()` to clear only the old previous rows outside the new current dirty Y range, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source. | Failed/no promotion. Runtime metrics, graphics work, layout, and correctness stayed exact (`loop_vb=1219`, `target_vb=1072`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `restore_bytes=2510092`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), but no key metric improved and ELF grew `712828 -> 713544`. Artifact: `scratch/ps1-perf-iterate/20260427-072447/summary.json`. | Do not promote. The overlap idea is semantically valid but not worth the extra branch/code shape in this build; revisit only if row promotion becomes generated or table-driven. |
| 2026-04-27 | `post-dirty-window-retune-20kb-18kb-17kb` | parameter-only experiments after `1429cb79` | Retest larger stream-window sizes on the post-dirty-row baseline to see whether the current cleaner render path can tolerate more prefetch coverage and reduce loop time. | Ran parameter probes with `prefetch-window 20480`, `18432`, and `17408` using the exact no-holiday fishing1 boot. No source changed. | Failed/no promotion. `20 KB` produced metrics and regressed badly: `loop_vb 1219 -> 1226`, `overrun_vb 147 -> 163`, `blocking_vb 5 -> 29`, `prefetch_overrun_vb 5 -> 19`, `hits 155 -> 153`, `due_misses 0 -> 2`. `18 KB` and `17 KB` both rounded into the same `18 KB` read shape and failed structurally: headless logs exceeded `512 MB` with repeated `Invalid byte read at address 0xFFFFB018, pc 0x00000CEC`, then regtest exited `137` before usable metrics. Artifacts: `scratch/ps1-perf-iterate/20260427-073155/summary.json`, `scratch/ps1-perf-iterate/20260427-073420/fishing1/headless-regtest.log`, `scratch/ps1-perf-iterate/20260427-073608/fishing1/headless-regtest.log`. | Do not promote. The accepted `16 KB` default remains the local knee; non-16 KB windows need generated group metadata/cost prediction, not blind raw-window retuning. |
| 2026-04-27 | `aligned-cd-file-lba-cache` | `ps1: cache aligned CD file LBA` | Cache the active FG2 file LBA inside the aligned CD read helper so perf-enabled reads do not compute `CdPosToInt()` once for metadata and again for `CdlSetloc`. | Added a local `fileLba` in `ps1_streamReadAlignedFromCdFileInto()`, used it for both perf metadata and `CdIntToPos()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require repeat because timing/work/correctness stayed exact and code shrank. | Promoted as hot CD helper cleanup, not a VBlank speed win. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`) while `ps1_streamReadAlignedIntoFile` shrank `540 -> 532` bytes and ELF shrank `712828 -> 712744`. Artifacts: `scratch/ps1-perf-iterate/20260427-074304/summary.json`, `scratch/ps1-perf-iterate/20260427-074539/summary.json`. | Promote as cumulative code-shape cleanup. The read helper is smaller and deterministic; total speed baseline remains unchanged at `loop_vb=1219`. |
| 2026-04-27 | `aligned-cd-single-chunk-fast-path` | dirty experiment after `8de67617` | Add a fast path for aligned CD reads with `numSectors <= 256`, since fishing1 active FG2 window reads are single-chunk and should not need loop/chunk bookkeeping. | Added a single-chunk branch in `ps1_streamReadAlignedFromCdFileInto()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because it did not move timing and grew the helper. | Failed/no promotion. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), but `ps1_streamReadAlignedIntoFile` grew `532 -> 636` bytes with no key metric movement. Artifact: `scratch/ps1-perf-iterate/20260427-075000/summary.json`. | Do not promote. Duplicating the read/error path is more expensive than the loop bookkeeping; retry only as a broader generated/assembly CD helper or if helper splitting can keep the common path smaller. |
| 2026-04-27 | `aligned-cd-helper-function-os` | `ps1: optimize aligned CD helper for size` | Compile only `ps1_streamReadAlignedFromCdFileInto()` with `-Os` to test function-scoped CD helper codegen without repeating the whole-TU `cdrom_ps1.c -Os` regression. | Added a function-scoped `optimize("Os")` attribute, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require repeat because timing/work/correctness stayed exact and the ELF shrank. | Promoted as hot CD helper code-shape cleanup, not a VBlank speed win. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_read_vb=282`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`) while `ps1_streamReadAlignedIntoFile` became a tiny wrapper (`532 -> 8` bytes) and ELF shrank `712744 -> 712556`. Artifacts: `scratch/ps1-perf-iterate/20260427-080449/summary.json`, `scratch/ps1-perf-iterate/20260427-080722/summary.json`. | Promote as cumulative code-shape cleanup. Function-scoped CD codegen is safe here; total speed baseline remains unchanged at `loop_vb=1219`. |
| 2026-04-27 | `buffered-cd-helper-function-os` | dirty experiment after `625e5daf` | Compile only `ps1_streamReadFromCdFileIntoBuffered()` with `-Os`, mirroring the accepted aligned helper probe but isolating the buffered direct-read path. | Added a function-scoped `optimize("Os")` attribute, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because it did not shrink code or improve timing. | Failed/no promotion. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), but ELF grew `712556 -> 712636` while the buffered helper stayed `604` bytes. Artifact: `scratch/ps1-perf-iterate/20260427-084512/summary.json`. | Do not promote. Function-scoped `-Os` helps the aligned helper wrapper but not the buffered helper; retry buffered reads only as part of a broader CD helper rewrite or if direct-stage frequency changes. |
| 2026-04-27 | `fallthrough-slack-5vb-post-cd-helper` | dirty experiment after `7fd51053` | Retest the staged-copy fallthrough guard at `5` held VBlanks after the CD helper cleanup, checking whether the newer code shape can hide more read work than the accepted `6` VBlank guard. | Changed `FG_PREFETCH_FALLTHROUGH_MIN_SLACK_VBLANKS` from `6` to `5`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source after visible CD pressure regressed. | Failed/no promotion. The looser fallthrough guard made active playback worse: `loop_vb 1219 -> 1221`, `target_vb 1072 -> 1070`, `overrun_vb 147 -> 151`, `blocking_vb 5 -> 10`, and `prefetch_overrun_vb 5 -> 10`; layout and correctness stayed stable. Artifact: `scratch/ps1-perf-iterate/20260427-084850/summary.json`. | Do not promote. The current `6` VBlank fallthrough guard remains the local knee; lower slack steals too much near-present time even after the CD helper shrink. |
| 2026-04-27 | `fallthrough-slack-7vb-post-cd-helper` | dirty experiment after `32aecce2` | Test the stricter side of the staged-copy fallthrough guard, raising it from `6` to `7` held VBlanks to see whether avoiding borderline follow-up reads reduces visible CD pressure. | Changed `FG_PREFETCH_FALLTHROUGH_MIN_SLACK_VBLANKS` from `6` to `7`, ran the exact no-holiday fishing1 gate, then reverted the source after the run failed structurally before metrics. | Failed/no promotion. The headless log exceeded `512 MB` and the regtest exited `137` before `JCPERF2`, with no usable timing metrics. Artifact: `scratch/ps1-perf-iterate/20260427-085205/fishing1/headless-regtest.log`. | Do not promote. The stricter guard is not a safe local change; keep the accepted `6` VBlank fallthrough guard and revisit only inside a first-class scheduler/state-machine rewrite. |
| 2026-04-27 | `unbuffered-cd-helper-function-os` | `ps1: optimize unbuffered CD helper for size` | Compile only `ps1_streamReadFromCdFile()` with `-Os`, checking whether the remaining setup/unbuffered stream helper can shrink without perturbing active playback. | Added a function-scoped `optimize("Os")` attribute, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require repeat because timing/work/correctness stayed exact and the ELF shrank. | Promoted as CD helper code-shape cleanup, not a VBlank speed win. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`) while `ps1_streamReadFromCdFile` shrank `680 -> 624` bytes and ELF shrank `712556 -> 712524`. Artifacts: `scratch/ps1-perf-iterate/20260427-085457/summary.json`, `scratch/ps1-perf-iterate/20260427-085726/summary.json`. | Promote as cumulative code-shape cleanup. This helper is setup-facing rather than the active aligned-window path, so total speed baseline remains unchanged at `loop_vb=1219`. |
| 2026-04-27 | `unbuffered-cd-file-lba-cache` | `ps1: cache unbuffered CD file LBA` | Cache the file LBA once inside `ps1_streamReadFromCdFile()` so perf-enabled setup/unbuffered reads do not compute `CdPosToInt()` separately for metadata and each `CdlSetloc`. | Added a local `fileLba`, used it for perf metadata and `CdIntToPos()`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require repeat because timing/work/correctness stayed exact and the ELF shrank. | Promoted as CD helper code-shape cleanup, not a VBlank speed win. Timing, CD pressure, layout, graphics work, and correctness stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`) while `ps1_streamReadFromCdFile` shrank `624 -> 592` bytes and ELF shrank `712524 -> 712332`. Artifacts: `scratch/ps1-perf-iterate/20260427-091724/summary.json`, `scratch/ps1-perf-iterate/20260427-091954/summary.json`. | Promote as cumulative code-shape cleanup. This setup-facing helper shrink keeps deterministic playback identity and becomes the new baseline. |
| 2026-04-27 | `fill-window-helper-function-os` | dirty experiment after `c685dfa1` | Compile only `fgRuntimeFillWindowForEntry()` with `-Os`, testing a function-scoped foreground prefetch-helper codegen pass without repeating the failed whole-foreground TU experiments. | Added a function-scoped `optimize("Os")` attribute, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because it was an exact no-op. | Failed/no promotion. Timing, CD pressure, layout, graphics work, correctness, PS-EXE, ELF, and tracked hot symbol sizes all stayed exact (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `fgRuntimeFillWindowForEntry=852`, `jcreborn.elf=712332`). Artifact: `scratch/ps1-perf-iterate/20260427-092341/summary.json`. | Do not promote. The compiler already emits the same code for this helper under the accepted foreground TU `-Os` baseline; retry only with source/assembly changes that alter the helper shape materially. |
| 2026-04-27 | `prepared-visual-decouple-metadata-v1` | dirty experiment after `0978c329` | Decouple prepared visual metadata from staged payload ownership so a prepared frame can theoretically present without keeping the staged frame buffer owned. | Added `preparedEntry`, a prepared-frame consume path, and lookahead based on `preparedFrameIndex`, then ran the exact no-holiday fishing1 gate. Source was kept dirty for the next variant, then fully reverted after v3 failed. | Failed/no promotion. Correctness and key timing stayed clean and flat (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`), but it did not stage farther ahead; `prefetch.duplicate 0 -> 130`, `jcreborn.elf 712332 -> 713072`, `fgRuntimeLoadSceneFrame +136`, and no speed/work gate improved. Artifact: `scratch/ps1-perf-iterate/20260427-094811/summary.json`. | Do not promote. Metadata decoupling alone adds code and duplicate probes unless the held loop also changes ownership for the `prepared && !staged` state. |
| 2026-04-27 | `prepared-visual-decouple-stage-next-v2` | dirty experiment after v1 | After a frame is visually prepared and the staged payload slot is released, try staging the following payload during held time instead of only probing the stream window. | Added a `preparedFrameValid && !stagedFrameValid` held-loop branch that calls `fgRuntimeTryStageNextFrame()`, ran the exact no-holiday fishing1 gate, then continued to v3 before reverting. | Failed/no promotion. Correctness stayed clean and key timing stayed flat (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`). Secondary metrics moved but did not pay off: `late 98 -> 85`, `loop_read_vb 280 -> 277`, `prefetch.used_vb 282 -> 279`, but `jcreborn.elf 712332 -> 713028`, `prefetch.attempts 212 -> 246`, and `skipped_no_slack 12 -> 46`. Artifact: `scratch/ps1-perf-iterate/20260427-095105/summary.json`. | Do not promote. The direction is promising enough to revisit, but this local branch mostly creates extra no-slack stage attempts and code growth without reducing actual loop time. |
| 2026-04-27 | `prepared-visual-decouple-prepare-ge4-v3` | dirty experiment after v2 | Give the decoupled prepared visual pipeline more time to stage the following payload by preparing at `>=4` held VBlanks instead of exactly `4`. | Changed the prepared-frame slack guard from exact `4` to `>=4`, ran the exact no-holiday fishing1 gate, then reverted all prepared-decoupling source changes after structural failure. | Failed/no promotion. The headless log exceeded `512 MB` and regtest exited `137` before usable metrics. Artifact: `scratch/ps1-perf-iterate/20260427-095359/fishing1/headless-regtest.log`. | Do not promote. Preparing earlier without a stricter CD/render budget is structurally unsafe; the next high-impact retry needs a first-class scheduler budget or a separate prepared visual buffer, not local metadata/threshold changes. |
| 2026-04-27 | `prepared-visual-decouple-stage-next-positive-slack-v4` | dirty experiment after v3 revert | Retest the stage-next branch with an explicit positive-slack guard, attempting to avoid the extra no-slack stage attempts seen in v2 while keeping the prepared-payload decoupling model. | Re-applied metadata decoupling and stage-next ownership, guarded the stage-next branch with `fgRuntimeHeldSlackBeforeWait() > 0`, ran the exact no-holiday fishing1 gate, then reverted all source changes. | Failed/no promotion. The guard did not change the outcome: key timing stayed flat (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`), secondary metrics matched the v2 shape (`late 98 -> 85`, `loop_read_vb 280 -> 277`, `prefetch.used_vb 282 -> 279`), and the no-slack attempts/code growth remained (`skipped_no_slack 12 -> 46`, `ELF 712332 -> 713060`). Artifact: `scratch/ps1-perf-iterate/20260427-095743/summary.json`. | Do not promote. Local prepared-payload decoupling is exhausted for this baseline; next retry should be a real separate prepared visual buffer or explicit read/render budget, not another branch guard. |
| 2026-04-27 | `fg2-group-102-110` | dirty experiment after `75489e05` | Test the current delivered-sector planner's overlapping read candidate at file sectors `102..110`, which should be safer than the rejected `106..117` guess because it matches the apparent append boundary. | Added `{102, 110}` beside the accepted tail group, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source after visible CD pressure regressed. | Failed/no promotion. It reduced active transactions (`loop_reads 67 -> 66`, `setloc 73 -> 72`, `seek_back 4 -> 3`) and delivered fewer sectors (`540 -> 537`), but the longer grouped read stole held-frame slack: `loop_vb 1219 -> 1220`, `target_vb 1072 -> 1071`, `overrun_vb 147 -> 149`, `blocking_vb 5 -> 8`, `prefetch_overrun_vb 5 -> 8`, `loop_read_vb 280 -> 293`, and `prefetch.used_vb 282 -> 295`. Layout and correctness stayed stable. Artifact: `scratch/ps1-perf-iterate/20260427-100748/summary.json`. | Do not promote. This confirms the cost model must consider read duration/slack ownership, not just saved transactions or sectors; `102..110` can be retried only after group-cost prediction or an explicit CD-first scheduler prevents it from creating the fifth visible read. |
| 2026-04-27 | `upload-single-rect-stack-reuse` | `ps1: reuse upload rect stack storage` | `grDrawBackground()` issued each `LoadImage` immediately, so its `RECT rects[8]` stack array was unnecessary. Reuse a single stack `RECT` without changing upload order, dimensions, or sync behavior. | Replaced the upload `RECT` array with one `RECT`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then accepted with a no-require baseline update because timing/work/correctness stayed exact and code shrank. | Promoted as hot upload-path code cleanup, not a VBlank speed win. Timing, CD pressure, layout, upload work, and correctness stayed exact (`loop_vb=1219`, `target_vb=1072`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `dirty_rows=25440`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`), while `grDrawBackground` shrank `1580 -> 1572` bytes and ELF shrank `712332 -> 712272`. Artifacts: `scratch/ps1-perf-iterate/20260427-101209/summary.json`, `scratch/ps1-perf-iterate/20260427-101441/summary.json`. | Promote as cumulative upload-path cleanup. The safety proof is simple: the SDK copies the `RECT` fields when `LoadImage()` is called, and all calls still occur before the same `DrawSync(0)`. |
| 2026-04-27 | `upload-rect-cap-6-post-stack-reuse` | dirty experiment after `eab12c9a` | Fishing1 reports `max_upload_rects=6`, so test whether lowering `GR_MAX_UPLOAD_RECTS` from `8` to `6` reduces upload-path code or stack pressure without triggering fallback. | Changed the cap to `6`, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source because no tracked speed, work, or size metric moved. | Failed/no promotion as an exact no-op. Timing, CD pressure, upload work, layout, correctness, PS-EXE, ELF, and `grDrawBackground` size all matched the accepted baseline (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `max_upload_rects=6`, `cap_hits=0`, `full_fallbacks=0`, `jcreborn.elf=712272`, `grDrawBackground=1572`). Artifact: `scratch/ps1-perf-iterate/20260427-101854/summary.json`. | Do not promote. A fishing1-only cap reduction gives no measured artifact win and narrows cross-scene headroom; retry only after a cross-scene upload-rect histogram proves `6` is globally safe or the cap feeds generated pack metadata. |
| 2026-04-27 | `upload-band-byte-arrays` | dirty experiment after `89efef99` | Convert upload band scratch arrays from `int` to `uint8` because tile indices and Y rows are bounded (`0..3`, `0..239`), hoping to reduce stack/code pressure in `grDrawBackground()`. | Changed `bandTile`, `bandMinY`, and `bandMaxY` to `uint8` arrays with explicit casts, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the source after artifact review. | Failed/no promotion. Timing, CD pressure, upload work, layout, and correctness stayed exact (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_rects=502`, `upload_bytes=16281600`, `cap_hits=0`), and `grDrawBackground` shrank `1572 -> 1544`, but total ELF grew `712272 -> 712372`. Artifact: `scratch/ps1-perf-iterate/20260427-102243/summary.json`. | Do not promote. Local hot-symbol shrink is not enough when the final artifact grows and timing is flat; retry only if combined with a broader upload stack/layout rewrite that also shrinks the ELF or moves VBlank metrics. |
| 2026-04-27 | `scheduler-ownership-counters` | `ps1: add scheduler ownership perf counters` | Add first-class held-slice ownership counters so the next scheduler attempt can distinguish present, stage-read, window-read, visual-prepare, idle-wait, CD-reserved, CD-blocked-prepare, prepared-ready, prepared-used, and prepared-wasted slots. | Added the `JCPERF2 sched` line, kept the exact no-holiday fishing1 gate, then accepted with a no-require run because timing, layout, graphics work, CD pressure, and correctness stayed exact. | Promoted as instrumentation/tooling, not a VBlank speed win. Timing and layout stayed exact (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `FISHING1.FG2 LBA=396`, PS-EXE `143360`) while the new counters reported `present=72`, `cd_stage=108`, `cd_window=54`, `visual_prepare=72`, `wait=574`, `cd_reserved=28`, `prep_blocked_cd=13`, `prepared_ready=72`, `prepared_used=72`, `prepared_wasted=0`, `slack_vb=4767`, `slack_min=1`, and `slack_max=10`. ELF grew `712272 -> 715432` from the metrics code. Artifact: `scratch/ps1-perf-iterate/20260427-105900/summary.json`. | Promote only as the scheduler measurement surface. The important proof is `72 ready / 72 used / 0 wasted`; the remaining big win is not duplicate prepared-frame waste in fishing1, but deciding which of the `574` wait slots and `28` CD-reserved slots can safely become useful work. |
| 2026-04-27 | `owned-idle-4vb-catchup` | dirty experiment after scheduler counters | Use the new ownership state to permit one extra 4-VBlank host-deadline catch-up only when the held frame had idle ownership, no CD window/reservation, the due frame was staged, and lookahead coverage was safe. | Added a per-frame owned-idle catch-up prototype, ran the exact no-holiday fishing1 gate with `--require-improvement`, then reverted the behavior after it failed. | Failed/no promotion. Timing stayed flat (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`) but the rule never fired usefully (`catchup_idle=0`, `catchup_busy=2`, `catchup_long=5`), while PS-EXE grew `143360 -> 145408` and `FISHING1.FG2 LBA` moved `396 -> 397`. Artifact: `scratch/ps1-perf-iterate/20260427-105501/summary.json`. | Do not promote. The naive first-class scheduler catch-up shape is not the big win: it adds code/layout risk and has no eligible fishing1 slots. Retry only with offline per-frame ownership analysis or a code-size-neutral scheduler proof. |
| 2026-04-27 | `prepared-defer-window-min-next-hold-5` | dirty experiment after scheduler counters | If a staged frame has a long next hold, defer a same-slack stream-window read and prepare the visual frame first. | Added a guarded prepare-before-window branch for staged frames whose next hold was at least `5`, ran the exact no-holiday fishing1 gate, then reverted. | Failed. It added one prepared frame but wasted it (`prepared_ready=73`, `prepared_used=72`, `prepared_wasted=1`) and regressed visible pressure (`loop_vb 1219 -> 1221`, `blocking_vb 5 -> 10`, `prefetch_overrun_vb 5 -> 10`). Artifact: `scratch/ps1-perf-iterate/20260427-111213/summary.json`. | Do not promote. Deferring CD to make room for speculative visual prep steals the slack that hides reads. |
| 2026-04-27 | `prepared-slack-3-or-4-existing-cd-priority` | dirty experiment after scheduler counters | Let staged-frame preparation run at `3` or `4` held VBlanks while preserving existing CD-window priority. | Changed the prepare guard from exact-`4` to `3..4`, ran the exact no-holiday fishing1 gate, then reverted. | Failed/no promotion. Prepared coverage rose (`prepared_ready/used 72 -> 74`) with no waste, but key timing stayed exact (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`). Artifact: `scratch/ps1-perf-iterate/20260427-111517/summary.json`. | Do not promote. More prepared coverage alone does not reduce loop time; the bottleneck is not missing prepared frames. |
| 2026-04-27 | `fg2-normal-render-event-wait-skip` | dirty experiment after scheduler counters | Skip `eventsWaitTick(0)` on the normal FG2 render path to test whether the zero-delay poll is measurable overhead. | Guarded the call in `grUpdateDisplay()` so active FG runtime with zero delay skipped it, ran the exact no-holiday fishing1 gate, then reverted. | Failed/no promotion. Timing and work identity stayed exact (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `upload_bytes=16281600`), while `grUpdateDisplay` grew by `60` bytes and input semantics weakened. Artifact: `scratch/ps1-perf-iterate/20260427-112000/summary.json`. | Do not promote. Present/input polling should stay explicit until a broader scheduler owns it. |
| 2026-04-27 | `fg2-preprocess-plan-analyzer` | `ps1: add fg2 preprocessing analysis tool` | Before changing FG2 runtime format, prove whether host-emitted upload/restore plans have enough theoretical savings to justify a format experiment. | Added `scripts/analyze-fg2-preprocess-plans.py`, decoded `generated/ps1/foreground/FISHING1.FG2`, applied the accepted exact baseline island offset (`-154,54`), and compared the script's runtime model to `20260427-105900`. | Promoted as host-side analysis tooling. The model matches accepted runtime counters exactly: `restore_bytes=2510092`, `restore_intervals=24300`, `upload_bytes=16281600`, `upload_rects=502`, `max_upload_bytes=614400`. It finds large theoretical savings: exact restore-under-current spans `2510092 -> 1194450` (`52.41%` byte cut) but `73417` intervals; coalesced `min8px_max4pieces` restore still saves `26.35%` with `36450` intervals; x-band upload-ready align4 saves `49.61%` but costs about `8.2 MB` of frame-band payload; exact interval upload saves `86.83%` but needs `95259` rects. Artifact: `scratch/fg2-preprocess-fishing1.json`. | Promote the tool, not a runtime behavior. Priority 1 direct upload-ready payloads are real but pack/CD-heavy; priority 2 restore-skip is promising only if coalesced enough to avoid thousands of tiny copies becoming the new bottleneck. |
| 2026-04-27 | `runtime-restore-skip-current-spans` | dirty experiment after `aaaeaf13` | Test the analyzer's restore-under-current idea without changing the pack format by parsing the current PAL4 span payload before restore and skipping clean-background copies underneath pixels that will be redrawn. | Tried five variants, then reverted all source/build changes: `min8/max4` on normal+prepared paths, `min16/max3`, normal-render-only `min8/max4`, helper-level `-Os`, and whole-graphics-TU `-Os`. | Failed/no promotion. The best byte-saving shape cut restore work but regressed deterministic timing and layout: normal-only `restore_bytes 2510092 -> 2222854`, but `loop_vb 1219 -> 1221`, `blocking_vb 5 -> 6`, `prefetch_overrun_vb 5 -> 6`, PS-EXE `143360 -> 145408`, and `FISHING1.FG2 LBA 396 -> 397`. Full graphics `-Os` shrank PS-EXE to `141312` but worsened `loop_vb=1224`, `blocking_vb=11`, and moved the pack to LBA `395`. Artifacts: `scratch/ps1-perf-iterate/20260427-114041/summary.json`, `20260427-114357/summary.json`, `20260427-114641/summary.json`, `20260427-115054/summary.json`, `20260427-115329/summary.json`. | Do not promote. Runtime parsing proves the savings are real but not useful in this form: code shape/layout and extra hot-path work erase the benefit. Retry only as pack-emitted side metadata or a true FGP3 payload layout that avoids parsing spans before restore and keeps CD phase controlled. |
| 2026-04-27 | `fg2-group-384-396` | dirty experiment after restore-skip revert | Try a second hard-coded 12-sector read group immediately before the accepted fishing1 high-tide tail group, targeting source reads that the host CD summary suggested could merge with no extra sectors. | Added `{384,396}` to `kFishing1HighReadGroups12`, built, ran the exact no-holiday fishing1 gate, then reverted. | Failed/no promotion as an exact no-op. Key timing and CD counters matched baseline (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `sectors=540`), and `group_hits=0`. Artifact: `scratch/ps1-perf-iterate/20260427-115931/summary.json`. | Do not promote. The group did not fit the retained 11-sector capacity and never fired. Future groups need append-start/capacity proof before runtime insertion. |
| 2026-04-27 | `fg2-group-307-317` | dirty experiment after `fg2-group-384-396` revert | Try a capacity-fitting late 10-sector read group around file sectors `307..317`, replacing source reads `101/102` without growing the stream buffer. | Added `{307,317}` beside the accepted tail group, built, ran the exact no-holiday fishing1 gate, then reverted. | Failed/no promotion. Timing, CD pressure, and read count stayed exact (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, `loop_reads=67`, `sectors=540`), so the strict gate rejected it for no key metric improvement while foreground code grew (`foregroundPilotPlay +432`). Artifact: `scratch/ps1-perf-iterate/20260427-120217/summary.json`. | Do not promote. A group that does not move measured reads is pure code-size/cadence risk. Continue CD work through generated/costed group metadata, not one-off tables. |
| 2026-04-27 | `direct-stage-window-cache` | dirty experiment after hard-coded group misses | In the tight-slack direct-stage path, read the sector-aligned payload window directly into the stream cache and copy the payload to the stage buffer from there, avoiding the old scratch-to-window seed copy. | Replaced the slack-3 direct-stage buffered read plus `fgRuntimeSeedWindowFromScratch()` with an aligned stream-window read, ran the exact no-holiday fishing1 gate, then reverted. | Failed/no promotion. The CPU-copy hypothesis did not pay off: `loop_vb` stayed `1219`, but `target_vb 1072 -> 1071`, `overrun_vb 147 -> 148`, `blocking_vb 5 -> 6`, and `prefetch_overrun_vb 5 -> 6`. Reads/sectors stayed `67/540`; layout stayed stable; `foregroundPilotPlay` grew by `372` bytes. Artifact: `scratch/ps1-perf-iterate/20260427-120813/summary.json`. | Do not promote. The saved copy is not the current limiter; altering the helper/read shape exposes an extra visible VBlank. Retry direct-stage only with a real group/tail predictor that keeps `blocking_vb<=5`. |
| 2026-04-27 | `foreground-pilot-o3-single-tu` | dirty experiment after direct-stage revert | Compile only `src/foreground_pilot.c` with `-O3` to isolate scheduler/runtime codegen from the earlier failed whole-hot-TU `-O3` test. | Added a temporary source-file `-O3` override in CMake, built, ran the exact no-holiday fishing1 gate, then reverted. | Failed/no promotion. Key timing stayed flat (`loop_vb=1219`, `overrun_vb=147`, `blocking_vb=5`, `prefetch_overrun_vb=5`) but there was no speed improvement and layout regressed: PS-EXE `143360 -> 149504`, `FISHING1.FG2 LBA 396 -> 399`, and `foregroundPilotPlay +5092` bytes. Artifact: `scratch/ps1-perf-iterate/20260427-121123/summary.json`. | Do not promote. Foreground `-O3` is a size/layout loss without a VBlank win. Future foreground codegen work should be function-scoped, assembly/generated, or phase-padded before promotion. |
| 2026-04-27 | `deep-trace-pipeline-owner-build` | `ps1: setup-prime fishing1 FG2 window` | Add deeper pipeline and slack-bucket ownership counters without perturbing the accepted speed binary. | First added the counters directly, observed the binary/layout regression, then moved them behind `PS1_PERF_DEEP_TRACE=ON` and verified the default build stayed exact. | Promoted as compile-time-gated tooling. The ungated attempt regressed by growing the executable and moving `FISHING1.FG2`; the gated default run stayed exact (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`, PS-EXE `143360`, LBA `396`). The trace build showed `due_frames=83`, `prepare_frames=72`, `prepared_present_frames=72`, `prepared_wasted=0`, and large wait-slack buckets. Artifacts: `scratch/ps1-perf-iterate/20260427-123552/summary.json`, `20260427-124046/summary.json`, `20260427-124311/summary.json`. | Keep default `PS1_PERF_DEEP_TRACE=OFF`. Use the trace build only for diagnosis; it is not an acceptance-speed binary. |
| 2026-04-27 | `prepared-bridge-slack-gte4` | dirty experiment after deep trace | Test whether allowing staged-frame visual prep at `>=4` held VBlanks, instead of exact `4`, converts idle buckets into prepared-present frames. | Changed the prepare guard, ran the exact gate, then ran a trace detail pass before reverting. | Failed/no promotion. The exact run was timing-flat and failed `--require-improvement` (`loop_vb=1219`, `blocking_vb=5`, `prefetch_overrun_vb=5`). Detail showed the work moved into higher-slack buckets but did not increase useful prepared output or reduce active loop time. Artifacts: `scratch/ps1-perf-iterate/20260427-124625/summary.json`, `20260427-124850/summary.json`. | Do not retry as another local threshold. The scheduler needs an ownership change, not more broad prepared eligibility. |
| 2026-04-27 | `global-catchup-threshold4-retest` | dirty experiment after deep trace | Retest the old long-hold catch-up threshold `4` now that the branch is smaller and ownership counters prove no prepared waste. | Lowered the catch-up threshold globally from `5` to `4`, ran the exact gate, then reverted. | Failed/no promotion. It regressed active timing and visible CD pressure: `loop_vb 1219 -> 1222`, `target_vb 1072 -> 1061`, `overrun_vb 147 -> 161`, and `blocking_vb/prefetch_overrun_vb 5 -> 16`. Artifact: `scratch/ps1-perf-iterate/20260427-125213/summary.json`. | Keep global catch-up at `5`. A threshold `4` path is only acceptable when another mechanism proves the needed FG2 bytes are already resident. |
| 2026-04-27 | `setup-prime-window-256kb` | dirty experiment after heap probe | Prime a large first-payload FG2 window during setup so active playback starts with many entries already resident and fewer loop reads. | Used the heap probe to confirm a 320 KB class allocation could fit, then tested a `256 KB` setup-prime window without changing catch-up. | Failed/no promotion alone. Active CD work improved (`loop_reads 67 -> 48`, `loop_read_vb 282 -> 196`) but active loop did not (`loop_vb 1219 -> 1220`) and visible pressure worsened slightly (`blocking_vb 5 -> 6`). Artifacts: `scratch/ps1-perf-iterate/20260427-125658/summary.json`, `20260427-130052/summary.json`. | Do not promote setup-prime by itself. Preloading bytes is useful only if the scheduler spends the new coverage to catch up. |
| 2026-04-27 | `setup-prime-window-192kb-256kb-catchup4` | dirty experiment after setup-prime alone | Pair setup-prime coverage with a gated `4` VBlank catch-up threshold to see whether the extra residency makes shorter catch-up safe. | Tested `256 KB` and `192 KB` setup-prime windows with catch-up threshold `4`, then reverted both sizes. | Failed/no promotion. `256 KB` was a near miss (`loop_vb 1219 -> 1215`, `overrun_vb 147 -> 145`) but still raised `blocking_vb/prefetch_overrun_vb 5 -> 6`; `192 KB` was worse (`loop_vb=1218`, `overrun_vb=150`, `blocking_vb=9`). Artifacts: `scratch/ps1-perf-iterate/20260427-130327/summary.json`, `20260427-130601/summary.json`. | Do not retry smaller setup-prime sizes blindly. The coverage boundary matters; catch-up threshold `4` needs enough primed payload to keep visible CD pressure flat or better. |
| 2026-04-27 | `setup-prime-window-320kb-catchup4` | `ps1: setup-prime fishing1 FG2 window` | Use the largest proven-safe setup-prime window to cover the early high-tide FG2 payload range, then enable threshold-`4` catch-up only when that setup prime actually succeeds. | Allocated a `320 KB` stream-window capacity for fishing1 high tide while keeping normal active reads at `16 KB`, filled that window during setup, gated threshold `4` on `setupWindowPrimed`, and reran the exact gate. | Promoted as an active-playback scheduler/CD win with a documented setup trade. Exact high-tide run improved `loop_vb 1219 -> 1215`, `target_vb 1072 -> 1075`, `overrun_vb 147 -> 140`, `blocking_vb 5 -> 1`, `prefetch_overrun_vb 5 -> 1`, `loop_reads 67 -> 43`, and `loop_read_vb 281 -> 193`; layout and correctness stayed stable. Setup rose (`setup_vb 185 -> 246`, `scene_vb 1404 -> 1461`) because the read moved before active loop. Low-tide sanity stayed clean with the old threshold path. Artifacts: `scratch/ps1-perf-iterate/20260427-130832/summary.json`, `20260427-131058/summary.json`, `20260427-131344/summary.json`, `20260427-132716/summary.json`, low-tide `20260427-133013/summary.json`. | Promote for active playback and keep the setup-cost caveat visible. The next version should hide this prime during inter-scene/loading time or generate per-scene/tide prime windows instead of treating setup time as free. |
| 2026-04-27 | `fg2-first-upload-clean-rect-y-band` | `ps1: scope fg2 first upload to clean rect band` | The static backdrop has already been presented when FG2 clean rects are saved, so the first forced dirty upload should only cover the saved dynamic clean-rect Y band instead of all four screen tiles. | Replaced the full first-frame dirty mark in `grSaveCleanBgRects()` with full-width dirty rows limited to the saved clean-rect Y union, then ran the exact no-holiday fishing1 gate with `--require-improvement` and a low-tide smoke. | Promoted as a small active-loop and upload-work win. High tide improved `loop_vb 1215 -> 1213`, `overrun_vb 140 -> 138`, and `scene_vb 1461 -> 1459`; `blocking_vb=1`, `prefetch_overrun_vb=1`, `loop_reads=43`, `restore_bytes=2510092`, `upload_rects=502`, PS-EXE `143360`, and `FISHING1.FG2 LBA=396` stayed stable. Upload work dropped `upload_bytes 16281600 -> 15888640`, `max_upload_bytes 614400 -> 221440`, and `dirty_rows 25440 -> 24826`. Low-tide smoke passed with `loop_vb=1214`, `due_misses=0`. Artifacts: `scratch/ps1-perf-iterate/20260427-140110/summary.json`, low-tide `20260427-140529/summary.json`. | Promote and count as a two-VBlank speed win. This is not broader active-region restore/mirror scoping; it only removes the stale full-screen first upload while keeping the runtime clean-rect model unchanged. |
| 2026-04-27 | `fg2-motion-comp-analyzer` | `ps1: add fg2 motion-comp analysis tool` | Before adding a motion-comp pack opcode, prove whether consecutive FG2 frames actually contain translated reusable pixels and whether fishing1 can validate that path. | Added `scripts/analyze-fg2-motion-comp.py`, decoded PAL4/indexed8 FG2 spans into sparse pixel maps, searched pairwise shifts around `0,0` and bbox deltas, and measured residual payload size plus a conservative per-move metadata estimate. | Promoted as host-side analysis tooling. Fishing1 high tide shows a large temporal-residual signal but no true translation signal: `151/154` candidate pairs, `585868` estimated payload bytes saved (`71.16%`), `zero_shift_candidate_pairs=151`, and `nonzero_shift_candidate_pairs=0`. A selected scene sweep confirms nonzero motion exists elsewhere: `WALKSTUF1` has `85` nonzero candidates saving `893115` bytes, and `WALK1LOW` has `53` nonzero candidates saving `542404` bytes. Artifacts: `scratch/fg2-motion/fishing1-motion-comp.json`, `walkstuf1-motion-comp.json`, `walk1low-motion-comp.json`, `selected-pack-summary.json`. | Promote the analyzer, not a runtime behavior. I-B should split into two FGP3 candidates: zero-shift temporal residuals for fishing1 validation, and nonzero move/residual opcodes for walking scenes. Do not build GPU `MoveImage` runtime until RAM mirror, cleanup, dirty tracking, and CD metadata costs are designed. |
| 2026-04-27 | `fg2-zero-shift-runtime-model` | `ps1: model fg2 temporal residual runtime work` | If fishing1's reuse is zero-shift, a temporal-residual renderer could leave unchanged foreground pixels in the RAM mirror, restore only vanished pixels, and draw only changed pixels. | Extended `scripts/analyze-fg2-motion-comp.py` with a zero-shift runtime model that estimates residual compose payload, exact cleanup restore bytes/intervals, and full-width dirty upload bytes for changed rows. | Promoted as host-side analysis tooling. Fishing1 model predicts `baseline_compose_payload_bytes 823277 -> residual_compose_payload_bytes 228087` (`72.30%` saved), `baseline_upload_bytes 15667200 -> residual_upload_bytes 6576000` (`58.03%` saved), and cleanup restore of only `136552` bytes across `17422` exact intervals. Artifact: `scratch/fg2-motion/fishing1-motion-comp-v2.json`. | Promote the model, not runtime behavior. This makes FGP3 zero-shift residuals the next canary-safe implementation target, but the runtime must carry full-current dirty metadata so unchanged foreground pixels remain restorable on later frames. |
| 2026-04-27 | `fg3-zero-shift-temporal-residual-pack` | `ps1: add fgp3 temporal residual fishing1 pack` | Encode fishing1 high tide as a first experimental FGP3 temporal-residual pack: restore cleanup spans for pixels that changed/disappeared, draw only residual changed pixels, and keep unchanged foreground pixels resident in the RAM mirror. | Built `generated/ps1/foreground/FISHING1.FG2` from the accepted PAL4 FG2 using `scripts/build-fg3-temporal-residual-pack.py`, added the `FGP3` runtime parser/compositor, then ran the exact high-tide gate with `--allow-layout-change --require-improvement` plus a low-tide FG2 smoke at `--frames 3000`. | Promoted as the first runtime FGP3 canary. High tide improved `loop_vb 1213 -> 1207`, `overrun_vb 138 -> 131`, `blocking_vb 1 -> 0`, `prefetch_overrun_vb 1 -> 0`, and `loop_reads 43 -> 6`; correctness stayed clean (`trip=0`, `frame_mismatch=0`, `sound_late=0`, `cd_fail=0`). Work volume dropped sharply: `restore_bytes 2510092 -> 251144`, `upload_bytes 15888640 -> 6690560`, `upload_rects 502 -> 290`, and pack bytes `829851 -> 398433`. Expected layout trade: PS-EXE `143360 -> 145408`, `FISHING1.FG2 LBA 396 -> 397`. Artifacts: high-tide `scratch/ps1-perf-iterate/20260427-161748/summary.json`, low-tide smoke `20260427-161559/summary.json`. | Promote despite intentional layout movement. The speed/work win is real and correctness-clean; the next FGP3 work should reduce the +2 KB executable cost, generate residual packs through the normal batch builder, and validate more scenes before replacing FG2 broadly. |
| 2026-04-27 | `fg3-zero-shift-temporal-residual-fishing1-low` | `ps1: add fgp3 temporal residual fishing1 low tide pack` | Extend the accepted FGP3 zero-shift residual format from fishing1 high tide to the low-tide variant, where FG2 still had visible refill pressure. | Captured a fresh low-tide FG2 baseline, converted `generated/ps1/foreground/FISH1LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide against that baseline with `--require-improvement`, then validated high tide stayed exact-flat against the accepted baseline. | Promoted. Low tide improved `loop_vb 1215 -> 1209`, `overrun_vb 142 -> 135`, `blocking_vb 5 -> 4`, `prefetch_overrun_vb 5 -> 4`, `loop_reads 31 -> 22`, and `loop_read_vb 126 -> 90`; correctness stayed clean. Work volume dropped sharply: `restore_bytes 1234716 -> 182892`, `upload_bytes 11457920 -> 5278080`, `upload_rects 453 -> 178`, and pack bytes `426082 -> 303083`. Layout stayed stable for the low pack (`FISH1LOW.FG2 LBA=592`) and high tide stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`). Artifacts: low baseline `scratch/ps1-perf-iterate/20260427-172914/summary.json`, low FGP3 `20260427-173038/summary.json`, high validation `20260427-173152/summary.json`. | Promote. This confirms FGP3 is not just a high-tide one-off; it should become the generated fishing-scene path after more scene/tide validation. The remaining low-tide pressure is now CD/refill (`blocking_vb=4`), so pack-group or setup-prime policy can target it next. |
| 2026-04-27 | `fg3-fishing1-low-setup-prime` | `ps1: setup-prime fishing1 low tide fgp3 window` | Now that the low-tide FGP3 pack is only `303083` bytes, let the existing `320 KB` setup-prime policy cover fishing1 low tide too and test whether it removes the remaining active-loop refill pressure. | Removed the high-tide-only condition from `fgRuntimeShouldPrimeSetupWindow()`, gated low tide against the accepted low FGP3 baseline with `--require-improvement`, then validated high tide stayed exact-flat against the accepted high baseline. | Promoted as an active-loop win with setup-cost trade. Low tide improved `loop_vb 1209 -> 1207`, `target_vb 1074 -> 1076`, `overrun_vb 135 -> 131`, `blocking_vb 4 -> 0`, `prefetch_overrun_vb 4 -> 0`, `loop_reads 22 -> 0`, and `loop_read_vb 90 -> 0`; correctness stayed clean and high tide stayed exact-flat. Setup cost rose as expected: `setup_vb 182 -> 238`, `setup_read_vb 110 -> 166`, and `scene_vb 1391 -> 1445`. Artifacts: low prime `scratch/ps1-perf-iterate/20260427-173631/summary.json`, high validation `20260427-173739/summary.json`. | Promote, but do not count this as an end-to-end scene-time win. It proves fishing1 high and low can both run active-loop CD-clean under FGP3; the next step is hiding setup-prime cost via inter-scene preloading or generated per-scene prime budgets. |
| 2026-04-27 | `fg3-zero-shift-temporal-residual-fishing2-high` | `ps1: add fgp3 temporal residual fishing2 pack` | Extend FGP3 zero-shift residuals to fishing2 high tide, which still had heavy FG2 CD pressure and due misses. | Captured a fresh fishing2 high FG2 baseline, converted `generated/ps1/foreground/FISHING2.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated fishing2 high against that baseline with `--require-improvement`, and ran a fishing2 low smoke afterward. | Promoted. Fishing2 high improved `loop_vb 1928 -> 1903`, `target_vb 1738 -> 1764`, `overrun_vb 190 -> 139`, `blocking_vb 50 -> 8`, `prefetch_overrun_vb 44 -> 8`, `due_misses 2 -> 0`, `loop_reads 134 -> 40`, and `loop_read_vb 609 -> 170`. Work volume dropped sharply: pack bytes `1595559 -> 542743`, `restore_bytes 5353808 -> 333876`, `upload_bytes 28215040 -> 9055360`, and `upload_rects 862 -> 508`. Layout stayed stable for the high pack (`FISHING2.FG2 LBA=740`). Artifacts: high baseline `scratch/ps1-perf-iterate/20260427-175823/summary.json`, high FGP3 `20260427-175951/summary.json`, low smoke `20260427-180122/summary.json`. | Promote. FGP3 is now validated beyond fishing1; fishing2 high still has `blocking_vb=8`, so the next scene-specific target is setup-prime or generated read grouping for fishing2. |
| 2026-04-27 | `fg3-fishing2-high-setup-prime-budget` | `ps1: setup-prime fishing2 high fgp3 window` | Give fishing2 high tide its own setup-prime budget now that FGP3 reduced the pack to `542743` bytes, while making setup priming explicitly scene/tide-budgeted instead of inferred from buffer capacity. | Added `setupPrimeWindowBytes` to runtime state, tested fishing2 high setup-prime budgets `544 KB`, `384 KB`, `256 KB`, `320 KB`, `352 KB`, and `368 KB`, then validated fishing1 high and fishing2 low. | Promoted the `352 KB` budget. Fishing2 high improved `loop_vb 1903 -> 1898`, `target_vb 1764 -> 1765`, `overrun_vb 139 -> 133`, `blocking_vb 8 -> 2`, `prefetch_overrun_vb 8 -> 2`, `loop_reads 40 -> 14`, and `loop_read_vb 170 -> 63`; correctness stayed clean. Setup cost rose as expected: `setup_vb 184 -> 251`, `setup_read_vb 113 -> 180`, `setup_bytes 282104 -> 626168`, and `scene_vb 2087 -> 2149`. Rejected probes: `544 KB` and `384 KB` failed before loop start with render/upload identity at zero, `256 KB` was safe but weaker (`loop_vb=1901`, `blocking_vb=8`), `320 KB` was safe but slightly weaker (`loop_vb=1899`, `blocking_vb=2`), and `368 KB` hit the log cap/regtest `137`. Artifacts: accepted `scratch/ps1-perf-iterate/20260427-181351/summary.json`, fishing1 validation `20260427-181708/summary.json`, fishing2 low smoke `20260427-181929/summary.json`, rejected `544 KB` `20260427-180640`, `384 KB` `20260427-180926`, `256 KB` `20260427-181056`, `320 KB` `20260427-181223`, `368 KB` `20260427-181520`. | Promote as an active-loop CD win with a setup-cost trade. The explicit budget field is also a correctness cleanup: only intended scene/tide variants prime. The remaining fishing2 high pressure is now two blocking/refill VBlanks, likely needing generated read groups or inter-scene preload rather than a larger contiguous setup prime. |
| 2026-04-27 | `fg3-fishing2-high-read-group-178-191` | log-only after `ps1: setup-prime fishing2 high fgp3 window` | Merge the first two post-prime fishing2 high stream-window appends (`178..191` relative sectors) to see whether one fewer loop read removes the remaining two blocking/refill VBlanks. | Temporarily generalized the hard-coded read-group path to a fishing2 mode and added `{178,191}`, ran the fishing2 high gate against the accepted setup-prime baseline, then reverted the source. | Failed/no promotion. The larger grouped read regressed the active loop and moved layout: `loop_vb 1898 -> 1899`, `overrun_vb 133 -> 134`, `blocking_vb/prefetch_overrun_vb 2 -> 3`, PS-EXE `145408 -> 147456`, and `FISHING2.FG2 LBA 740 -> 741`; correctness remained clean. Artifact: `scratch/ps1-perf-iterate/20260427-182532/summary.json`. | Do not promote manual fishing2 groups. The remaining two blocking VBlanks need generated read metadata, layout padding/cold-section control, or a segmented/inter-scene prime plan; a local source-table group is too phase-sensitive and too large for the benefit. |
| 2026-04-27 | `fg3-zero-shift-temporal-residual-fishing2-low` | `ps1: add fgp3 temporal residual fishing2 low tide pack` | Convert fishing2 low tide from PAL4 FG2 to FGP3 zero-shift temporal residuals, since the low-tide smoke still used `fgp2_pal4` and had `blocking_vb=20`. | Converted `generated/ps1/foreground/FISH2LOW.FG2` in place with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide against `scratch/ps1-perf-iterate/20260427-181929/summary.json`, then validated fishing2 high stayed exact-flat. | Promoted. Low tide improved `loop_vb 1912 -> 1900`, `target_vb 1755 -> 1764`, `overrun_vb 157 -> 136`, `blocking_vb/prefetch_overrun_vb 20 -> 5`, `loop_reads 58 -> 27`, and `loop_read_vb 250 -> 115`. Pack bytes fell `784126 -> 385436`, payload bytes `779078 -> 380388`, restore bytes `5353808 -> 220390`, upload bytes `28215040 -> 6727040`, and upload rects `862 -> 322`; correctness stayed clean. The low pack LBA stayed `1006`, PS-EXE stayed `145408`, and fishing2 high stayed exact-flat (`loop_vb=1898`, `blocking_vb=2`). Artifacts: low FGP3 `scratch/ps1-perf-iterate/20260427-183017/summary.json`, high validation `20260427-183155/summary.json`. | Promote. This confirms both fishing2 tide variants now benefit from FGP3 residuals. Remaining low-tide pressure is `blocking_vb=5`, so the next safe target is low-tide setup-prime sizing or generated/segmented prime coverage. |
| 2026-04-27 | `fg3-fishing2-low-setup-prime-budget` | `ps1: setup-prime fishing2 low fgp3 window` | Add a low-tide-specific fishing2 setup-prime budget now that FGP3 reduced `FISH2LOW.FG2` to `385436` bytes, aiming to remove the remaining `5` blocking/refill VBlanks. | Tested a `320 KB` low-tide prime first, rejected it after headless hit the log cap/regtest `137`, then promoted a `256 KB` budget. Shortened two cold foreground diagnostic strings to recover the PS-EXE sector bucket and validated fishing2 high stayed exact-flat. | Promoted. Low tide improved `loop_vb 1900 -> 1898`, `target_vb 1764 -> 1767`, `overrun_vb 136 -> 131`, `blocking_vb/prefetch_overrun_vb 5 -> 0`, `loop_reads 27 -> 10`, and `loop_read_vb 115 -> 43`; correctness stayed clean. Setup cost rose as expected: `setup_vb 185 -> 233`, `setup_read_vb 113 -> 161`, `setup_bytes 282104 -> 527864`, and `scene_vb 2085 -> 2131`. Layout stayed stable after code-size recovery: `FISH2LOW.FG2 LBA=1006`, PS-EXE `145408`, and fishing2 high exact-flat (`loop_vb=1898`, `blocking_vb=2`). Artifacts: rejected `320 KB` `scratch/ps1-perf-iterate/20260427-183511`, accepted low `20260427-184547/summary.json`, high validation `20260427-184710/summary.json`. | Promote as an active-loop CD-clean win with explicit setup-cost trade. Fishing2 low now reaches the same `131` VBlank overrun as fishing1 high/low; next work should hide setup-prime cost or move to the remaining high-tide `2` blocking VBlanks through generated/segmented preload. |
| 2026-04-27 | `fg3-zero-shift-temporal-residual-fishing3-high` | `ps1: add fgp3 temporal residual fishing3 pack` | Convert fishing3 high tide to FGP3 zero-shift temporal residuals, since the fresh FG2 baseline had heavy CD pressure (`blocking_vb=87`, `due_misses=11`) and a `1.83 MB` pack. | Captured a fresh fishing3 high FG2 baseline, converted `generated/ps1/foreground/FISHING3.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated high tide with `--work-identity-min 0` because the pack-format change intentionally reduces restore work, then ran a fishing3 low smoke. | Promoted. High tide improved `loop_vb 2123 -> 2099`, `target_vb 1934 -> 1950`, `overrun_vb 189 -> 149`, `blocking_vb 87 -> 24`, `prefetch_overrun_vb 39 -> 21`, `due_misses 11 -> 1`, and pack bytes `1831749 -> 724829`; correctness stayed clean with all `21` sound events. Work volume dropped: payload `1825013 -> 718093`, restore bytes to `487854`, upload bytes to `11079040`, and upload rects to `515`. Layout stayed stable for the high pack (`FISHING3.FG2 LBA=1195`) and PS-EXE stayed `145408`. Fishing3 low smoke passed after its LBA moved to `1549`. Artifacts: high baseline `scratch/ps1-perf-iterate/20260427-185039/summary.json`, high FGP3 `20260427-185412/summary.json`, low smoke `20260427-185552/summary.json`. | Promote. FGP3 now scales to the next scene with a much larger pack and sound set. Remaining high-tide pressure is `blocking_vb=24`, so follow-up targets are low-tide FGP3, setup-prime sizing, or generated segmented preload. |
| 2026-04-27 | `fg3-zero-shift-temporal-residual-fishing3-low` | `ps1: add fgp3 temporal residual fishing3 low tide pack` | Convert fishing3 low tide to FGP3 after the post-high-conversion smoke showed the low pack was still `fgp2_pal4` with `blocking_vb=21`. | Converted `generated/ps1/foreground/FISH3LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide with `--work-identity-min 0`, then validated fishing3 high stayed exact-flat. | Promoted. Low tide improved `loop_vb 2110 -> 2098`, `target_vb 1954 -> 1960`, `overrun_vb 156 -> 138`, `blocking_vb 21 -> 8`, `prefetch_overrun_vb 21 -> 9`, `loop_reads 65 -> 42`, and `loop_read_vb 280 -> 160`; correctness stayed clean with all `21` sound events. Pack bytes fell `906053 -> 549622`, payload `900517 -> 544086`, restore bytes to `359988`, upload bytes to `8512000`, and upload rects to `310`. Layout stayed stable for the low pack (`FISH3LOW.FG2 LBA=1549`), PS-EXE stayed `145408`, and fishing3 high stayed exact-flat. Artifacts: low FGP3 `scratch/ps1-perf-iterate/20260427-185856/summary.json`, high validation `20260427-190033/summary.json`. | Promote. Fishing3 now has both tide variants on FGP3. Remaining pressure is CD/refill (`blocking_vb=24` high, `8` low), so the next target is generated setup-prime sizing rather than more format conversion for this scene. |
| 2026-04-27 | `fg3-prepared-present-3vb-retry` | log-only after `ps1: add fgp3 temporal residual fishing1 pack` | Now that FGP3 residuals removed most restore/upload/CD pressure, retry the old prepared-present `3` VBlank slack point to see whether the cheaper residual render path makes the threshold viable. | Temporarily changed `FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` from `4` to `3`, ran the exact FGP3 high-tide gate against `20260427-161748`, then reverted the source. | Failed/no promotion as exact-flat. Correctness, layout, and key timing stayed unchanged: `loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `due_misses=0`, PS-EXE `145408`, and `FISHING1.FG2 LBA=397`. Artifact: `scratch/ps1-perf-iterate/20260427-163122/summary.json`. | Do not promote. FGP3 changes the work volume, but this local prepared-present threshold still does not move the canary. Future present work needs a structural scheduler or release/perf-log baseline split, not another isolated equality-guard retry. |
| 2026-04-27 | `fg3-setup-prime-catchup3-retry` | log-only after `ps1: add fgp3 temporal residual fishing1 pack` | With FGP3 high-tide visible CD pressure at zero, lower the setup-primed host-deadline catch-up threshold from `4` to `3` to see whether shorter holds can absorb one late VBlank without destabilizing prefetch. | Temporarily changed the setup-primed `catchupThreshold` from `4` to `3`, ran the exact FGP3 high-tide gate with `--allow-layout-change --require-improvement`, then reverted the source. | Failed/no promotion as exact-flat. Key timing and layout did not move: `loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `due_misses=0`, PS-EXE `145408`, and `FISHING1.FG2 LBA=397`. Artifact: `scratch/ps1-perf-iterate/20260427-163457/summary.json`. | Do not promote. FGP3 removed CD pressure, but threshold-only catch-up still does not move fishing1. Future catch-up work needs a structural scheduler or release/perf-log split, not another isolated threshold retry. |
| 2026-04-27 | `fg3-prev-dirty-bounds-discard` | log-only after `ps1: add fgp3 temporal residual fishing1 pack` | For FGP3 residual frames, cleanup spans and residual spans rebuild the exact current dirty set, so discarding previous dirty bounds might avoid the per-frame previous-row clear without widening uploads. | Temporarily replaced `grBeginResidualCleanBgFrame()`'s previous-dirty clear with bounds-only discard, then ran the exact FGP3 high-tide gate with `--require-improvement`. | Failed/no promotion as exact-flat. The source shifted several hot symbols by `-116` bytes, but timing and layout did not improve: `loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`, PS-EXE `145408`, and `FISHING1.FG2 LBA=397`. Artifact: `scratch/ps1-perf-iterate/20260427-164010/summary.json`. | Do not promote. This is plausible but unmeasured correctness risk for no timing, size, or reported work win. Retry only if a future dirty-state counter can prove the row clear is hot. |
| 2026-04-27 | `fg3-current-perf-detail-attribution` | diagnostic sample after `ps1: add fgp3 temporal residual fishing1 pack` | Refresh Detail-tier attribution after the FGP3 residual canary, because the previous detail run was FG2-heavy and no longer reflected the active bottleneck. | Ran the exact FGP3 high-tide case with `perf-detail`. The harness hit post-scene DuckStation invalid-read spam and no aggregate `summary.json`, but the per-case `perf-summary.json` completed before the log cap. | Diagnostic pass. Summary timing still matched the accepted FGP3 baseline (`loop_vb=1207`, `target_vb=1076`, `overrun_vb=131`, `blocking_vb=0`). Detail attribution shows the remaining measured render VBlank crossings are almost entirely present wait: `render_vb=155`, `present_wait_vb=155`, `compose_vb=2`, `restore_vb=0`, `upload_vb=0`, and `advance_vb=0`. Artifact: `scratch/ps1-perf-iterate/20260427-164328/fishing1/perf-summary.json`. | Use this to reprioritize. FGP3 removed the CD and restore/upload pressure from the high-tide canary; the remaining gap is dominated by one VSync per rendered frame. Large wins now need a present/scheduler architecture change or a release/perf-log split, not more local restore/upload micro-tuning. |
| 2026-04-27 | `fg3-due-frame-precompose-before-present` | log-only after `fg3-current-perf-detail-attribution` | Try to hide due-frame FGP3 composition before the present wait: compose the foreground into the RAM mirror first, then let `grUpdateDisplay()` wait for VSync and upload without recomposing. | Temporarily added a one-shot `grSetForegroundPrecomposed()` path and moved due-frame compose before `grUpdateDisplay()`, then ran the exact FGP3 high-tide gate with `--allow-layout-change --require-improvement`. | Failed/no promotion. Timing stayed exact-flat (`loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`), while the executable crossed a sector bucket (`145408 -> 147456`) and `FISHING1.FG2` moved `397 -> 398`. Artifact: `scratch/ps1-perf-iterate/20260427-164809/summary.json`. | Do not promote. Moving compose before VSync does not reduce the present-wait wall in summary timing and adds layout cost. Future present work needs a first-class scheduler/present model, not a one-shot skip flag around `grUpdateDisplay()`. |
| 2026-04-27 | `fg3-prepared-present-2vb-retry` | log-only after `fg3-current-perf-detail-attribution` | After FGP3 made residual compose cheap, lower the prepared-present slack point to `2` to see whether more short-hold frames can become prepared presents without visible CD pressure. | Temporarily changed `FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` from `4` to `2`, ran the exact FGP3 high-tide gate, then reverted the source. | Failed/no promotion. Scheduler shape changed as intended (`prepared_ready/prepared_used 105 -> 136`, `wait 773 -> 742`), but speed did not: `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. The target tightened by one (`1076 -> 1075`), so `overrun_vb` worsened `131 -> 132`. Artifact: `scratch/ps1-perf-iterate/20260427-165127/summary.json`. | Do not promote. More prepared presents alone do not reduce active time; present VSync ownership is the limiting factor. Keep the `4`-VBlank threshold until a first-class scheduler can spend prepared frames differently. |
| 2026-04-27 | `fg3-runtime-helper-os-scope` | log-only after `ps1: add fgp3 temporal residual fishing1 pack` | Try to recover some of the +2 KB FGP3 executable cost by compiling only the FGP3 residual compositor and cleanup-span restore helper for size. | Temporarily added `__attribute__((optimize(\"Os\")))` to `grCompositePacked4TemporalResidualToBackground()` and `grRestoreCleanBgSpanFromRects()`, then ran the exact FGP3 high-tide gate. | Failed/no promotion. Timing and layout stayed exact-flat (`loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`, PS-EXE `145408`, `FISHING1.FG2 LBA=397`), and the ELF shrank `726432 -> 725444`, but the runtime sector bucket did not move. Artifact: `scratch/ps1-perf-iterate/20260427-165450/summary.json`. | Do not promote. The scoped codegen change is layout-safe but not runtime-useful; retry executable-size recovery through source simplification or cold-section isolation, not helper-level `-Os` alone. |
| 2026-04-27 | `fg3-noholiday-stamp-callsite-guard` | log-only after `fg3-current-perf-detail-attribution` | Avoid calling the holiday-stamp helper on every foreground compose when the scene is explicitly in the no-holiday baseline. | Temporarily moved the `islandState.holiday > 0` guard to the compose call site and ran the exact FGP3 high-tide gate. | Failed/no promotion as exact-flat. Timing, layout, CD pressure, and work identity did not move (`loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`, PS-EXE `145408`, `FISHING1.FG2 LBA=397`); ELF changed only `726432 -> 726428`. Artifact: `scratch/ps1-perf-iterate/20260427-165828/summary.json`. | Do not promote. The helper call is not a measurable bottleneck on the FGP3 canary; keep the existing centralized holiday guard until broader holiday-path work is needed. |
| 2026-04-27 | `fg3-prepared-present-4or2-retry` | log-only after `fg3-prepared-present-2vb-retry` | Combine the accepted exact-`4` prepared-present point with the FGP3-only exact-`2` point to cover short-hold frames without losing the accepted long-hold behavior. | Temporarily allowed `fgRuntimeCanPrepareStagedFrame()` to return true at slack `4` or, for FGP3 only, slack `2`; ran the exact FGP3 high-tide gate, then reverted the source. | Failed/no promotion. The scheduler shape matched the exact-`2` retry (`prepared_ready/prepared_used 136`, `wait 742`) but key metrics stayed flat: `loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`; PS-EXE and pack LBA stayed fixed. Artifact: `scratch/ps1-perf-iterate/20260427-171837/summary.json`. | Do not promote. Covering more prepared-present slots still does not reduce active time; local slack-threshold variants are exhausted until the present/VSync ownership model changes. |
| 2026-04-27 | `perf-summary-scheduler-detail-only` | log-only after `fg3-current-perf-detail-attribution` | Move scheduler ownership counters out of Summary and into Detail-only to reduce hot perf-log overhead on held slices while keeping timing/CD/gfx/correctness counters active. | Temporarily made `ps1PerfMarkScheduler()` return early below `PS1_PERF_LEVEL_DETAIL`, then ran the exact FGP3 high-tide gate. | Failed/no promotion. Timing stayed exact-flat (`loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`), PS-EXE and pack LBA stayed fixed, and ELF grew slightly (`726432 -> 726456`) while Summary sched fields became zero. Artifact: `scratch/ps1-perf-iterate/20260427-172236/summary.json`. | Do not promote. Scheduler counters are not the measured perf-log bottleneck, and removing them from Summary loses useful evidence for no speed or size win. |
| 2026-04-27 | `fg3-prepared-present-event-tail-removal-retry` | log-only after FGP3 zero-CD-pressure baseline | Retry the old prepared-present event-tail removal now that FGP3 high tide has `blocking_vb=0`, testing whether the previous CD-phase failure was specific to FG2 pressure. | Temporarily removed `eventsWaitTick(0)` from the prepared-present path after `grDrawBackground()`, ran the exact FGP3 high-tide gate, then reverted the source. | Failed/no promotion. Timing and CD pressure stayed exact-flat (`loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`), PS-EXE and pack LBA stayed fixed, and ELF shrank only `726432 -> 726256`. Artifact: `scratch/ps1-perf-iterate/20260427-172554/summary.json`. | Do not promote. The old CD regression is gone under FGP3, but there is still no speed or sector-size win, and removing the poll weakens input responsiveness. Keep the explicit event tail until a first-class present/input scheduler replaces it. |
| 2026-04-27 | `fg3-fishing3-high-setup-prime-budget-retry` | log-only after `ps1: add fgp3 temporal residual fishing3 low tide pack` | Test whether fishing3 high can use the same contiguous setup-prime strategy as fishing1/fishing2 to cut the remaining `blocking_vb=24`. | Temporarily added a fishing3 high setup-prime branch. Tested `320 KB` first, then `256 KB`, and reverted the source after both probes. | Failed/no promotion. `320 KB` failed before playback with render/upload identity at zero and moved layout (`FISHING3.FG2 LBA 1195 -> 1196`, PS-EXE `145408 -> 147456`). `256 KB` completed but was not worth the trade: `loop_vb 2099 -> 2096`, `overrun_vb 149 -> 145`, `blocking_vb` stayed `24`, `due_misses 1 -> 2`, `prefetch_overrun_vb 21 -> 15`, and layout still moved to PS-EXE `147456` / LBA `1196`. Artifacts: `320 KB` `scratch/ps1-perf-iterate/20260427-190340/summary.json`, `256 KB` `20260427-190531/summary.json`. | Do not promote a contiguous fishing3 high setup prime. The heap/layout cost is too high and the useful blocking pressure does not move at `256 KB`; retry only with segmented/generated prime coverage or inter-scene preload. |
| 2026-04-27 | `fg3-fishing3-low-setup-prime-budget` | `ps1: setup-prime fishing3 low fgp3 window` | Test whether the lower-pressure fishing3 low-tide FGP3 path can use a smaller contiguous setup-prime budget to cut the remaining `blocking_vb=8` without disturbing fishing3 high. | Added a fishing3 low-tide `256 KB` setup-prime branch, gated low tide against `scratch/ps1-perf-iterate/20260427-185856/summary.json`, shortened cold heap/raw foreground diagnostic strings to recover the PS-EXE sector bucket, then validated fishing3 high stayed exact-flat. | Promoted as an active-loop win with setup-cost trade. Low tide improved `loop_vb 2098 -> 2091`, `target_vb 1960 -> 1957`, `overrun_vb 138 -> 134`, `blocking_vb 8 -> 7`, `prefetch_overrun_vb 9 -> 7`, `loop_reads 42 -> 24`, and `loop_read_vb 160 -> 100`; correctness stayed clean with all `21` sound events. Setup cost rose from `setup_vb 186 -> 234`, `setup_read_vb 113 -> 161`, and `setup_bytes 282152 -> 527912`; `scene_vb` rose `2284 -> 2325`. The first same-budget probe improved speed but moved layout (`FISH3LOW.FG2 LBA 1549 -> 1550`, PS-EXE `145408 -> 147456`); after cold string shrink, low stayed at LBA `1549` / PS-EXE `145408`, and fishing3 high stayed exact-flat. Artifacts: layout-moving probe `scratch/ps1-perf-iterate/20260427-191047/summary.json`, accepted low `20260427-191308/summary.json`, high validation `20260427-191446/summary.json`. | Promote, but treat this as the end of blind contiguous fishing3 setup-prime probing. The remaining low pressure is `blocking_vb=7`, while high still has `24`; next work should be generated segmented prime coverage or inter-scene preload with layout control. |
| 2026-04-27 | `fg3-fishing3-high-read-group-223-234` | log-only after `ps1: setup-prime fishing3 low fgp3 window` | Use the host CD-log gap before fishing3 high relative sector `223` to test one small grouped append, merging the measured `223..230` and `230..234` reads into one `11`-sector transaction. | Temporarily enabled read groups for fishing3 high and added `{223,234}`, then gated high tide against `scratch/ps1-perf-iterate/current-fishing3-baseline.json` with improvement required. | Failed/no promotion. The group made CD phase worse and moved layout: `loop_vb 2099 -> 2103`, `overrun_vb 149 -> 154`, `blocking_vb 24 -> 28`, `prefetch_overrun_vb 21 -> 25`, PS-EXE `145408 -> 147456`, and `FISHING3.FG2 LBA 1195 -> 1196`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260427-192153/summary.json`. | Do not promote local fishing3 read groups. Even the best-looking host-log gap regressed once encoded in the hot source path. Retry only with generated metadata plus layout control, or with an inter-scene/segmented preloader that does not perturb the active-loop executable layout. |
| 2026-04-27 | `fg3-fishing3-low-setup-prime-288kb` | `ps1: retune fishing3 low setup prime` | The FGP3-aware CD-log planner showed the `256 KB` low-tide prime stopped exactly before active relative sector `130`; raising the low-only budget to `288 KB` should cover the next small read run without approaching the rejected `320 KB` point. | Changed only the fishing3 low setup-prime budget from `256 KB` to `288 KB`, gated low tide against `scratch/ps1-perf-iterate/current-fishing3-low-baseline.json`, then validated fishing3 high stayed exact-flat. | Promoted as an active-loop CD-pressure refinement. Low tide kept `loop_vb=2091`, improved `target_vb 1957 -> 1960`, `overrun_vb 134 -> 131`, `blocking_vb 7 -> 4`, `prefetch_overrun_vb 7 -> 4`, `loop_reads 24 -> 21`, and `loop_read_vb 100 -> 90`; correctness stayed clean. Setup cost rose from `setup_vb 234 -> 241`, `setup_read_vb 161 -> 167`, `setup_bytes 527912 -> 560680`, and `scene_vb 2325 -> 2332`. Layout stayed stable (`FISH3LOW.FG2 LBA=1549`, PS-EXE `145408`), and fishing3 high stayed exact-flat. Artifacts: low `scratch/ps1-perf-iterate/20260427-192545/summary.json`, high validation `20260427-192727/summary.json`. | Promote. This is the largest safe contiguous fishing3 low budget seen so far; remaining low pressure is now `4` VBlanks, and further work should use segmented/generated prime coverage rather than retrying the rejected `320 KB` contiguous read. |
| 2026-04-27 | `fg3-fishing3-low-setup-prime-304kb` | log-only after `ps1: retune fishing3 low setup prime` | Test whether `304 KB` is a safe last contiguous step between the promoted `288 KB` low-tide budget and the previously rejected `320 KB` probe. | Temporarily raised the fishing3 low setup-prime budget from `288 KB` to `304 KB` and gated low tide against the accepted `288 KB` baseline. | Failed/no promotion. Layout and correctness stayed clean, but timing regressed: `loop_vb=2091` stayed flat, `target_vb 1960 -> 1959`, `overrun_vb 131 -> 132`, `blocking_vb 4 -> 5`, and `prefetch_overrun_vb 4 -> 5`. Artifact: `scratch/ps1-perf-iterate/20260427-193049/summary.json`. | Keep `288 KB` as the contiguous fishing3 low knee. Larger contiguous reads steal more phase than they save; remaining low pressure needs segmented prime coverage or a scheduler change, not another simple budget bump. |
| 2026-04-27 | `fg3-fishing3-high-setup-prime-128kb` | `ps1: setup-prime fishing3 high fgp3 window` | Retry fishing3 high with a smaller contiguous setup-prime budget. The earlier `256 KB` and `320 KB` high probes were too disruptive, but `128 KB` should cover the first active read run without the same heap/layout risk. | Added a high-tide `128 KB` branch while keeping low tide at the accepted `288 KB`, gated high tide against `scratch/ps1-perf-iterate/current-fishing3-baseline.json`, then validated low tide stayed exact-flat. | Promoted as an active-loop CD win. High tide improved `loop_vb 2099 -> 2094`, `target_vb 1950 -> 1955`, `overrun_vb 149 -> 139`, `blocking_vb 24 -> 16`, `prefetch_overrun_vb 21 -> 11`, `loop_reads 52 -> 44`, and `loop_read_vb 229 -> 183`; correctness stayed clean. Setup cost rose from `setup_vb 191 -> 213`, `setup_read_vb 119 -> 141`, `setup_bytes 282152 -> 396840`, and `scene_vb 2290 -> 2307`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`), and fishing3 low stayed exact-flat. Artifacts: high `scratch/ps1-perf-iterate/20260427-193340/summary.json`, low validation `20260427-193525/summary.json`. | Promote. This reopens small generated setup-prime knees for high tide, but do not retry the failed large contiguous budgets blindly; the remaining `blocking_vb=16` needs measured smaller knees, segmented prime coverage, or inter-scene preload. |
| 2026-04-27 | `fg3-fishing3-high-setup-prime-160kb` | log-only after `ps1: setup-prime fishing3 high fgp3 window` | Test whether the next small high-tide contiguous budget after the accepted `128 KB` point can remove more of the remaining `blocking_vb=16`. | Temporarily raised the fishing3 high setup-prime budget from `128 KB` to `160 KB` and gated high tide against the accepted `128 KB` baseline. | Failed/no promotion. Layout stayed stable, but the larger read crossed the bad phase boundary: `loop_vb 2094 -> 2100`, `target_vb 1955 -> 1951`, `overrun_vb 139 -> 149`, `blocking_vb 16 -> 29`, `prefetch_overrun_vb 11 -> 16`, and `due_misses 1 -> 3`. Artifact: `scratch/ps1-perf-iterate/20260427-193902/summary.json`. | Keep `128 KB` as the current high-tide contiguous knee. The next high-tide improvement needs a smaller sweep point, segmented preload, or scheduler ownership changes; larger contiguous reads are actively harmful. |
| 2026-04-27 | `fg3-fishing3-high-setup-prime-144kb` | log-only after `ps1: setup-prime fishing3 high fgp3 window` | Test the midpoint between the accepted `128 KB` high-tide prime and rejected `160 KB` high-tide prime. | Temporarily raised high tide to `144 KB`. First run kept the original cold strings and moved layout; second run shortened cold diagnostics to recover PS-EXE/LBA and reran the same candidate. | Failed/no promotion. The layout-moving run showed a tempting speed signal (`loop_vb 2094 -> 2093`, `overrun_vb 139 -> 137`, `blocking_vb 16 -> 13`) but crossed PS-EXE `145408 -> 147456` and `FISHING3.FG2 LBA 1195 -> 1196`. After cold string shrink recovered layout, the same budget regressed (`loop_vb 2094 -> 2096`, `overrun_vb 139 -> 144`, `blocking_vb 16 -> 21`, `prefetch_overrun_vb 11 -> 16`). Artifacts: layout-moving `scratch/ps1-perf-iterate/20260427-195850/summary.json`, layout-stable `20260427-200119/summary.json`. | Do not promote. The apparent `144 KB` win was phase/layout-dependent, not robust. Keep high tide at `128 KB` until segmented prime coverage or a scheduler change can preserve CD phase explicitly. |
| 2026-04-27 | `fg3-fishing3-high-setup-prime-140kb` | log-only after `ps1: setup-prime fishing3 high fgp3 window` | Use the FGP3-aware CD-log planner to target the next active read after the `128 KB` high-tide prime: relative sectors `67..73`. A `140 KB` setup prime should cover that read without reaching the failed `144/160 KB` ranges. | Temporarily raised high tide to `140 KB` and shortened cold diagnostics to keep PS-EXE/LBA stable, then gated against the accepted `128 KB` high baseline. | Failed/no promotion. Layout stayed stable, but CD phase still worsened: `loop_vb 2094 -> 2095`, `overrun_vb 139 -> 142`, `blocking_vb 16 -> 19`, and `prefetch_overrun_vb 11 -> 14`; correctness stayed clean. Artifact: `scratch/ps1-perf-iterate/20260427-200604/summary.json`. | Do not promote. Even the targeted single-read coverage point regresses once layout is stable; contiguous high-tide budget probing is exhausted at `128 KB`. |
| 2026-04-27 | `fg3-fishing3-high-window-min-slack-2` | log-only after `ps1: setup-prime fishing3 high fgp3 window` | Let fishing3 high use a `2` VBlank window-refill guard so shorter held slices can issue CD reads and potentially reduce the remaining `blocking_vb=16`. | Temporarily made `fgRuntimeWindowSlackEligible()` scene-aware for fishing3 high and lowered the direct-stage equality point to the same scene-specific minimum, then gated high tide against the accepted `128 KB` setup-prime baseline. | Failed/no promotion. The shorter guard made phase and layout worse: `loop_vb 2094 -> 2097`, `target_vb 1955 -> 1951`, `overrun_vb 139 -> 146`, `blocking_vb 16 -> 23`, `prefetch_overrun_vb 11 -> 18`, PS-EXE `145408 -> 147456`, and `FISHING3.FG2 LBA 1195 -> 1196`. Artifact: `scratch/ps1-perf-iterate/20260427-200907/summary.json`. | Do not promote. Short-slack CD reads still steal visible phase on fishing3 high; the next scheduler work needs ownership budgeting or segmented preload, not a local guard drop. |
| 2026-04-27 | `fg3-fishing3-high-catchup-threshold-5` | log-only after `ps1: setup-prime fishing3 high fgp3 window` | Disable setup-primed `4` VBlank catch-up for fishing3 high to test whether preserving more CD slack beats the accepted catch-up behavior. | Temporarily forced fishing3 high to use catch-up threshold `5` while keeping other setup-primed scenes at threshold `4`, then gated high tide against the accepted `128 KB` setup-prime baseline. | Failed/no promotion. Blocking stayed flat while timing/refill/layout worsened: `loop_vb 2094 -> 2095`, `target_vb 1955 -> 1954`, `overrun_vb 139 -> 141`, `blocking_vb=16`, `prefetch_overrun_vb 11 -> 13`, PS-EXE `145408 -> 147456`, and `FISHING3.FG2 LBA 1195 -> 1196`. Artifact: `scratch/ps1-perf-iterate/20260427-201204/summary.json`. | Do not promote. The accepted threshold `4` is still the best measured fishing3 high behavior; preserving that one VBlank does not help CD pressure. |
| 2026-04-27 | `fg3-fishing3-high-setup-segment-67-73` | `ps1: setup-prime fishing3 high segment` | Test segmented setup-prime coverage for the first active read after the accepted `128 KB` high-tide contiguous prime. Relative sectors `67..73` are too expensive when reached through a larger contiguous prime, but may be cheap if read into existing scratch during setup. | Added a FISHING3-high-only setup segment backed by `streamScratch`, then compiled default-off heap-probe logging out of the speed build to recover the accepted PS-EXE/LBA layout. Gated high tide against `scratch/ps1-perf-iterate/current-fishing3-baseline.json`, then validated low tide exact-flat. | Promoted as a small segmented-prime proof. High tide improved `loop_vb 2094 -> 2093` with stable layout (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`) and clean work identity; `target_vb 1955 -> 1954`, so `overrun_vb`, `blocking_vb`, and `prefetch_overrun_vb` stayed flat at `139`, `16`, and `11`. Setup cost rose as expected from the extra read (`setup_vb 213 -> 219`, `setup_reads 6 -> 7`, `setup_bytes 396840 -> 409128`). Low tide stayed exact-flat (`loop_vb=2091`, `blocking_vb=4`, LBA `1549`). Artifacts: layout-moving probes `scratch/ps1-perf-iterate/20260427-204152/summary.json`, `20260427-204455/summary.json`, `20260427-204835/summary.json`, `20260427-205100/summary.json`, accepted high `20260427-205604/summary.json`, final high rerun `20260427-210614/summary.json`, low validation `20260427-205753/summary.json`. | Promote, but treat this as a proof point rather than the final architecture. One hard-coded scratch segment produces only a one-VBlank high-tide win; the next version should be generated segment metadata or inter-scene preload that targets remaining `blocking_vb` without adding hot source tables. |
| 2026-04-27 | `fg3-first-frame-visual-regression-repair` | `ps1: fix fgp3 first-frame upload` | Repair the FISHING3 visual regression found after the FGP3 conversion. Hypothesis: clean-rect Y-band scoping removed the static first-frame upload, and FGP3's residual-frame dirty reset then discarded the remaining full-upload state before the first residual frame could present. | Binary-regressed visually with `RUN_TIMEOUT_SECONDS=0 PS1_CAPTURE_COUNT=0 ./scripts/rebuild-and-let-run.sh noclean fgpilot fishing3 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 seed 1`. First bad commit was `bd55780a`; with the full first-frame upload overlaid, second bad was `14ae86be`. | Promoted as a correctness repair, not a speed win. `grSaveCleanBgRects()` again marks the full screen dirty for first upload, and FGP3 due-frame rendering now uses a first-frame residual begin path that clears current dirty state without clearing the pending previous/full-upload state. Visual validation was good at the bisect point and at branch tip. | Promote. FGP3 residual packs require a full first presentation before residual-only cleanup/draw frames can safely rely on temporal continuity. Do not reintroduce clean-rect first-upload scoping unless the renderer has an explicit static-frame-present guarantee. |
| 2026-04-27 | `fg3-setup-segment-window-append-copy` | log-only after `ps1: fix fgp3 first-frame upload` | Reuse the FISHING3-high setup segment for the stream-window append when the active loop later asks for the same relative sector range, avoiding a duplicate CD transaction. | Temporarily copied append bytes from the primed setup segment when `fgRuntimeTryExtendWindow()` requested a range contained in `67..73`, then gated high tide against `scratch/ps1-perf-iterate/current-fishing3-baseline.json` with improvement required. | Failed/no promotion. It removed one loop read but worsened active pressure: `loop_reads 44 -> 43`, `loop_vb 2093 -> 2096`, `overrun_vb 139 -> 142`, `blocking_vb 16 -> 19`, `prefetch_overrun_vb 11 -> 15`, and `loop_read_vb 188 -> 194`; PS-EXE stayed `145408` and `FISHING3.FG2 LBA` stayed `1195`. Artifact: `scratch/ps1-perf-iterate/20260427-220444/summary.json`. | Do not promote. The duplicate read is real, but eliding it inside the current append path changes phase and increases visible pressure. Retry only as part of a first-class generated segmented preloader with explicit CD/prepare ownership, not as an opportunistic stream-window copy. |
| 2026-04-27 | `fg3-fishing3-high-setup-segment-73-80` | log-only after `fg3-setup-segment-window-append-copy` | Move the one hard-coded FISHING3-high setup segment from `67..73` to the next measured active range `73..80`, testing whether a different segment avoids the duplicate-window interaction while covering a later read. | Temporarily changed `FG_FISHING3_HIGH_SETUP_SEGMENT_START/BYTES` to `73` and `7` sectors, then gated high tide against `scratch/ps1-perf-iterate/current-fishing3-baseline.json` with improvement required. | Failed/no promotion. Layout stayed stable, but timing and visible pressure regressed: `loop_vb 2093 -> 2095`, `overrun_vb 139 -> 144`, `blocking_vb 16 -> 19`, `prefetch_overrun_vb 11 -> 16`, and `due_misses=1`; PS-EXE stayed `145408` and `FISHING3.FG2 LBA` stayed `1195`. Artifact: `scratch/ps1-perf-iterate/20260427-221002/summary.json`. | Do not promote. The segment location matters less than the schedule ownership: moving the setup scratch read still changes the active CD phase badly. Future segmented preload needs generated coverage and explicit ownership rather than manually sliding one segment. |
| 2026-04-27 | `fg3-fishing3-low-setup-segment-146-152` | log-only after `fg3-fishing3-high-setup-segment-73-80` | Add a low-tide FISHING3 setup segment for the first active read after the accepted `288 KB` contiguous prime: relative sectors `146..152`. | First refreshed the current low-tide post-visual-repair baseline (`scratch/ps1-perf-iterate/20260427-221839/summary.json`), then temporarily generalized setup segments by tide and gated low tide against that refreshed baseline. | Failed/no promotion as a structural failure. The per-case summary showed a tempting speed signal (`loop_vb 2091 -> 2090`, `overrun_vb 131 -> 130`, `blocking_vb/prefetch_overrun_vb 4 -> 3`, `loop_read_vb 91 -> 85`) with stable PS-EXE `145408`, but the run then spammed invalid reads until the harness killed it at the log cap (`regtest_exit=137`). Artifacts: first failed probe `scratch/ps1-perf-iterate/20260427-221510/fishing3-low/perf-summary.json`, confirmed failure `20260427-222203/fishing3-low/perf-summary.json`. | Do not promote. The low segment may be a real one-VBlank opportunity, but the current scratch-segment ownership is unsafe for low tide. Retry only after setup-segment lifetime/cleanup is first-class and the headless process exits cleanly after scene completion. |
| 2026-04-27 | `fg3-fishing3-high-setup-segment-67-72` | log-only after `fg3-fishing3-low-setup-segment-146-152` | Shrink the accepted FISHING3-high setup segment from `67..73` to `67..72` to see whether the first contained entry provides the win while setup reads one fewer sector. | Temporarily changed `FG_FISHING3_HIGH_SETUP_SEGMENT_BYTES` from `6` sectors to `5` sectors, then gated high tide against `scratch/ps1-perf-iterate/current-fishing3-baseline.json` with improvement required. | Failed/no promotion. The second contained entry is required: `loop_vb 2093 -> 2097`, `overrun_vb 139 -> 145`, `blocking_vb 16 -> 22`, and `prefetch_overrun_vb 11 -> 18`; layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260427-222524/summary.json`. | Keep the `67..73` segment at `6` sectors. Setup-byte trimming that loses contained-entry coverage is worse than the small setup saving. |
| 2026-04-27 | `fg3-fishing3-low-owned-setup-segment-146-152` | `ps1: setup-prime fishing3 low segment` | Promote the low-tide segment opportunity by fixing ownership instead of reusing `streamScratch`. High tide keeps the accepted scratch-backed `67..73` segment; low tide gets a separately owned `146..152` segment so staging/window scratch reads cannot corrupt it before use. | Added setup-segment start/size state, a small dedicated low-tide segment buffer released with the other scene-stream buffers, and kept high tide on the original scratch path. Gated high tide for exact-flat validation, then gated low tide with improvement required against the refreshed post-visual-repair baseline. | Promoted. Low tide improved `loop_vb 2091 -> 2090`, `overrun_vb 131 -> 130`, `blocking_vb/prefetch_overrun_vb 4 -> 3`, and `loop_read_vb 91 -> 85`; correctness stayed clean (`trip=0`, `frame_mismatch=0`, `sound_late=0`, `cd_fail=0`) and the run exited cleanly. Setup cost rose as expected (`setup_vb 241 -> 246`, `setup_reads 6 -> 7`, `setup_bytes 560680 -> 572968`), PS-EXE stayed `145408`, and `FISH3LOW.FG2 LBA` stayed `1549`. High tide stayed exact-flat (`loop_vb=2093`, `blocking_vb=16`, `prefetch_overrun_vb=11`). Artifacts: high validation `scratch/ps1-perf-iterate/20260427-224626/summary.json`, low accepted `20260427-224849/summary.json`. | Promote as a one-VBlank active-loop CD win and as the first safe low-tide segmented setup-prime. The ownership lesson matters: low segments need storage independent of hot streaming scratch, while the accepted high segment remains safest on the old scratch path. |
| 2026-04-27 | `fg3-fishing3-low-persistent-setup-segment` | log-only after `ps1: setup-prime fishing3 low segment` | Keep the owned low-tide setup segment valid after the first copied entry, testing whether multiple contained entries in `146..152` can avoid more active-loop read pressure. | Temporarily added a low-tide persistent segment flag so scratch-backed high tide stayed single-use while owned low tide could serve repeated hits, then gated low tide against the new accepted low-segment baseline with improvement required. | Failed/no promotion as exact-flat. Timing, layout, and visible pressure did not move: `loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, PS-EXE `145408`, and `FISH3LOW.FG2 LBA=1549`. Artifact: `scratch/ps1-perf-iterate/20260427-225307/summary.json`. | Do not promote. The first low segment win comes from phase/ownership of the first contained entry, not from keeping the whole small segment resident for later entries. Retry multi-entry segment lifetime only with generated metadata that proves repeated segment hits replace real reads. |
| 2026-04-27 | `fg3-fishing3-low-owned-segment-146-159` | log-only after `fg3-fishing3-low-persistent-setup-segment` | Combine the safe owned low-tide segment with a larger range, expanding `146..152` to `146..159` and keeping it persistent so it can cover the next measured active read group. | Temporarily changed the low setup segment from `6` to `13` sectors and kept low-tide segment hits persistent, then gated low tide against the accepted `146..152` baseline with improvement required. | Failed/no promotion as exact-flat. The larger owned segment exited cleanly but did not improve timing or visible pressure: `loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, PS-EXE `145408`, and `FISH3LOW.FG2 LBA=1549`. Artifact: `scratch/ps1-perf-iterate/20260427-232129/summary.json`. | Do not promote. Covering more early low-tide sectors in setup does not translate into additional active-loop savings; the remaining low-tide pressure is not removed by wider manual setup segments. |
| 2026-04-27 | `fg3-fishing3-high-second-setup-segment-73-80` | log-only after `fg3-fishing3-low-owned-segment-146-159` | Add a second high-tide setup segment for `73..80` while keeping the accepted scratch-backed `67..73` segment, testing whether additive segmented coverage avoids the earlier replacement failure. | Temporarily added secondary setup-segment state and an owned `73..80` high-tide read, then gated high tide against the accepted high baseline with improvement required. | Failed/no promotion. The added hot code crossed the executable sector bucket and worsened phase: `loop_vb 2093 -> 2096`, `overrun_vb 139 -> 145`, `blocking_vb 16 -> 22`, `prefetch_overrun_vb 11 -> 18`, PS-EXE `145408 -> 147456`, and `FISHING3.FG2 LBA 1195 -> 1196`. Artifact: `scratch/ps1-perf-iterate/20260427-232609/summary.json`. | Do not promote manual additive high segments. The remaining high-tide pressure needs generated/cold metadata or a scheduler/layout change; hard-coded second-segment state is too expensive and phase-sensitive. |
| 2026-04-27 | `fg3-fishing3-low-window-min-slack-2` | log-only after `fg3-fishing3-high-second-setup-segment-73-80` | With fishing3 low tide down to only `3` blocking/refill VBlanks, test whether it can safely issue window refills at `2` VBlanks of held slack, unlike high tide. | Temporarily made `fgRuntimeWindowSlackEligible()` return true at `2` VBlanks only for fishing3 low tide, then gated low tide against the accepted owned-segment baseline with improvement required. | Failed/no promotion. Short-slack reads still steal visible phase: `loop_vb 2090 -> 2094`, `overrun_vb 130 -> 134`, `blocking_vb/prefetch_overrun_vb 3 -> 7`; layout stayed stable (`FISH3LOW.FG2 LBA=1549`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260427-233000/summary.json`. | Keep the global `3` VBlank window guard. Even after low-tide segmented setup-prime, two-VBlank refill slack is too aggressive and worsens the remaining CD pressure. |
| 2026-04-27 | `fg3-setup-segment-selector-inline` | `ps1: inline fishing3 segment selection` | Recover code size from the owned low-segment promotion by removing the one-call setup-segment selector helper and folding the constants into `fgRuntimePrimeSetupSegment()`. | Removed `fgRuntimeSetupSegmentForScene()`, validated high tide against the accepted high baseline and low tide against the accepted owned-segment low baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `blocking_vb=16`, `prefetch_overrun_vb=11`), low stayed exact-flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`), PS-EXE stayed `145408`, `FISHING3.FG2 LBA=1195`, and `FISH3LOW.FG2 LBA=1549`. Relative to the accepted low-segment build, ELF shrank `726268 -> 726068` and `foregroundPilotPlay` shrank `8312 -> 8308`. Artifacts: high `scratch/ps1-perf-iterate/20260427-233325/summary.json`, low `20260427-233551/summary.json`. | Promote as work reduction, not a VBlank win. Future baselines should use these runs so symbol/layout comparisons reflect the current accepted source shape. |
| 2026-04-28 | `fg3-low-segment-size-state-prune` | `ps1: prune fixed low setup-segment state` | The fishing3 low setup segment is fixed at `146..152` and released at scene boundaries, so keeping a separate allocated-size word is unnecessary. Removing that state should shrink the runtime without changing segment ownership, CD reads, or timing. | Removed `gFgSetupSegmentBufferSize`, allocated the fixed low-segment byte count only when the owned pointer is null, then validated high tide and low tide against their current accepted baselines. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `foregroundPilotPlay` shrank `8308 -> 8280`. Artifacts: high `scratch/ps1-perf-iterate/20260428-000141/summary.json`, low `20260428-000403/summary.json`. | Promote as work reduction, not a VBlank win. This keeps the segmented-prime path simpler while preserving deterministic timing; remaining fishing3 gains still need generated segment metadata, inter-scene preload, or a scheduler change. |
| 2026-04-28 | `fg3-owned-high-setup-segment` | log-only after `ps1: prune fixed low setup-segment state` | Try using the owned fixed-size setup-segment buffer for both fishing3 tides, replacing the high-tide scratch-backed segment. This would simplify ownership and make future persistent/generated segment work less scratch-sensitive. | Temporarily allocated the owned setup-segment buffer for high tide too, read either high `67..73` or low `146..152` into that buffer, and gated high tide against the accepted baseline. | Failed/no promotion as a structural failure. The scene metrics themselves were flat and the prototype shrank `foregroundPilotPlay 8280 -> 8240`, but after scene completion DuckStation emitted invalid-read spam until the headless log exceeded `512 MB`; the harness killed the run with `regtest_exit=137`. Artifact: `scratch/ps1-perf-iterate/20260428-001039/fishing3/perf-summary.json`. | Do not replace the high scratch segment with owned storage as a local cleanup. The high path's scratch-backed lifetime appears to be part of the current safe teardown shape; retry only as a first-class generated segment model with explicit post-scene ownership/cleanup validation. |
| 2026-04-28 | `fg3-setup-segment-primed-check-prune` | `ps1: prune redundant setup-segment checks` | Keep the accepted high/low segment ownership unchanged, but remove redundant `setupSegmentBuffer != NULL` checks from the segment-window and copy paths. `setupSegmentPrimed` is already cleared by reset/read-failure/direct-read paths and only set after a valid segment buffer is installed. | Removed the redundant pointer checks, then validated high tide and low tide against their current accepted baselines. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725872 -> 725828`; tracked hot symbol sizes stayed flat. Artifacts: high `scratch/ps1-perf-iterate/20260428-001409/summary.json`, low `20260428-001637/summary.json`. | Promote as work reduction, not a VBlank win. The cleanup removes branch work from the segment hit path without changing the hard safety boundary learned from the rejected owned-high experiment. |
| 2026-04-28 | `fg3-window-contains-cache-gate-prune` | `ps1: prune duplicate window containment gate` | Remove the duplicate `fgRuntimeCanWindowCache()` check inside `fgRuntimeWindowContainsEntry()`. Callers either already proved entry/window capability before calling, or the contained stream-window/setup-segment state will safely return false. | Removed the containment-level cache gate, then validated high tide and low tide against their current accepted baselines. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725828 -> 725820`; tracked hot symbol sizes stayed flat. Artifacts: high `scratch/ps1-perf-iterate/20260428-004248/summary.json`, low `20260428-004514/summary.json`. | Promote as work reduction, not a VBlank win. This is another small simplification of the segment/window hit path; it does not change the remaining CD-pressure target. |
| 2026-04-28 | `fg3-window-copy-dst-null-prune` | `ps1: prune duplicate window copy guard` | Remove the `dst == NULL` check from `fgRuntimeCopyEntryFromWindow()`. The function is internal, and all callers pass either `frameBuffer` after a non-null capacity check or `prefetchFrameBuffer` after stage capability/allocation checks. | Removed the destination guard, then validated high tide and low tide against their current accepted baselines. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725820 -> 725720`; tracked hot symbol sizes stayed flat. Artifacts: high `scratch/ps1-perf-iterate/20260428-004930/summary.json`, low `20260428-005201/summary.json`. | Promote as work reduction, not a VBlank win. This removes another branch from the hot window-copy path while keeping the same deterministic frame identity and CD cadence. |
| 2026-04-28 | `fg3-window-entry-fit-cache-gate-prune` | log-only after `ps1: prune duplicate window copy guard` | Test whether `fgRuntimeEntryFitsWindow()` can be reduced to geometry only by removing its `fgRuntimeCanWindowCache()` state gate. Invalid/no-window states should naturally return false because the runtime read size is zero. | Temporarily removed the cache-state gate from `fgRuntimeEntryFitsWindow()`, then validated high tide and low tide against the accepted window-copy cleanup baseline. | Failed/no promotion as code-size regression. High and low stayed exact-flat (`loop_vb=2093/2090`, `blocking_vb=16/3`, `prefetch_overrun_vb=11/3`) with stable PS-EXE and pack LBAs, but `jcreborn.elf` grew `725720 -> 725784`. Artifacts: high `scratch/ps1-perf-iterate/20260428-005551/summary.json`, low `20260428-005815/summary.json`. | Do not promote. The extra state gate is apparently cheaper for codegen/layout than the geometry-only version; retry only if `fgRuntimeEntryFitsWindow()` is inlined or refactored with a broader window scheduler cleanup. |
| 2026-04-28 | `fg3-window-fill-fit-guard-prune` | log-only after `ps1: prune duplicate window copy guard` | Remove the internal `fgRuntimeEntryFitsWindow()` guard from `fgRuntimeFillWindowForEntry()`. All current call sites already check entry-fit eligibility before filling the stream window, so the callee guard looked redundant. | Temporarily removed the callee guard, then validated high tide and low tide against the accepted window-copy cleanup baseline. | Failed/no promotion as code-size regression. High and low stayed exact-flat (`loop_vb=2093/2090`, `blocking_vb=16/3`, `prefetch_overrun_vb=11/3`) with stable PS-EXE and pack LBAs, but `jcreborn.elf` grew `725720 -> 725748`. Artifacts: high `scratch/ps1-perf-iterate/20260428-012037/summary.json`, low `20260428-012300/summary.json`. | Do not promote. The local duplicate guard is cheaper in the accepted code shape than relying on callers alone; retry only as part of a broader window-fill inlining/refactor where codegen can be measured again. |
| 2026-04-28 | `fg3-sector-align-helper-macro` | `ps1: macro sector align helpers` | Replace the tiny sector-align helper functions with macros so the stream-window path does not carry callable align-down/up bodies. The logic remains the same mask/add expression. | Converted `fgSectorAlignDown()` and `fgSectorAlignUp()` to macros, then validated high tide and low tide against the accepted window-copy cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725720 -> 725348`. Artifacts: high `scratch/ps1-perf-iterate/20260428-012834/summary.json`, low `20260428-013054/summary.json`. | Promote as work reduction, not a VBlank win. The helper bodies were dead weight in the accepted code shape; the remaining gap is still CD pressure, not sector-mask arithmetic. |
| 2026-04-28 | `fg3-window-read-size-helper-macro` | `ps1: macro window read-size helper` | Replace the tiny `fgRuntimeWindowReadSize()` helper with a macro so the stream-window fit/fill path does not carry a callable read-size selector. | Converted the helper to a macro, then validated high tide and low tide against the accepted sector-align macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725348 -> 725164`. Artifacts: high `scratch/ps1-perf-iterate/20260428-013453/summary.json`, low `20260428-013715/summary.json`. | Promote as work reduction, not a VBlank win. This confirms the current compiler is not reliably inlining even tiny hot-path accessors, so more one-call/two-call helper audits are worthwhile if they preserve layout. |
| 2026-04-28 | `fg3-window-slack-helper-macro` | `ps1: macro window slack helper` | Replace the single-compare `fgRuntimeWindowSlackEligible()` helper with a macro in the prefetch scheduler. | Converted the helper to a macro, then validated high tide and low tide against the accepted window-read-size macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725164 -> 725008`. Artifacts: high `scratch/ps1-perf-iterate/20260428-014053/summary.json`, low `20260428-014313/summary.json`. | Promote as work reduction, not a VBlank win. Continue helper audits only when the helper body is trivial and call sites are stable enough for exact A/B gating. |
| 2026-04-28 | `fg3-hold-predicate-helper-macro` | `ps1: macro hold predicate helper` | Replace the single-call `fgRuntimeCanHoldDisplayedFrame()` helper with a macro in the held-frame loop. | Converted the helper to a macro, then validated high tide and low tide against the accepted window-slack macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `725008 -> 724876`. Artifacts: high `scratch/ps1-perf-iterate/20260428-020048/summary.json`, low `20260428-020309/summary.json`. | Promote as work reduction, not a VBlank win. Single-call hot predicates are good cleanup candidates when they do not duplicate large condition chains. |
| 2026-04-28 | `fg3-present-prechecked-guard-prune` | `ps1: prune duplicate prepared-present guard` | Remove the internal readiness guard from `fgRuntimePresentPreparedFrame()`. The function has one caller, and that caller checks `fgRuntimeCanPresentPreparedOnNextVBlank()` immediately before calling. | Removed the callee guard, then validated high tide and low tide against the accepted hold-predicate macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724876 -> 724760`. Artifacts: high `scratch/ps1-perf-iterate/20260428-020641/summary.json`, low `20260428-020911/summary.json`. | Promote as work reduction, not a VBlank win. The precondition is now explicit at the sole call site; do not add new callers without restoring a guard or proving the same precheck. |
| 2026-04-28 | `fg3-prepare-prechecked-guard-prune` | `ps1: prune duplicate prepared-frame guard` | Remove the internal readiness guard from `fgRuntimePrepareStagedFrameForPresent()`. The function has one caller, and that caller checks `fgRuntimeCanPrepareStagedFrame()` immediately before calling. | Removed the callee guard, then validated high tide and low tide against the accepted prepared-present guard baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `prepared_ready=148`, `prepared_used=148`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `prepared_ready=152`, `prepared_used=152`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724760 -> 724756`. Artifacts: high `scratch/ps1-perf-iterate/20260428-021246/summary.json`, low `20260428-021517/summary.json`. | Promote as a tiny work reduction, not a VBlank win. The precondition is now explicit at the sole call site; keep the prepared-ready/used identity check in future tests touching this path. |
| 2026-04-28 | `fg3-held-slack-helper-macro` | `ps1: macro held-slack helper` | Replace callable `fgRuntimeHeldSlackBeforeWait()` with a macro. It is used throughout the held-frame scheduler, so this tests whether removing call overhead and the standalone helper body beats duplicated predicate code. | Converted the helper to a macro, then validated high tide and low tide against the accepted prepared-frame guard baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724756 -> 724544`. Artifacts: high `scratch/ps1-perf-iterate/20260428-022045/summary.json`, low `20260428-022330/summary.json`. | Promote as work reduction, not a VBlank win. The result shows hot helper macro-expansion can still shrink this codebase when the helper's callable body and call overhead dominate duplicated field checks. |
| 2026-04-28 | `fg3-payload-predicate-helper-macro` | `ps1: macro payload predicate helper` | Replace callable `fgEntryHasPayload()` with a macro. It is used across frame selection, stream-window containment, and frame-load decisions, so this tests whether inlining the common payload predicate is still size-safe. | Converted the helper to a macro, then validated high tide and low tide against the accepted held-slack macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724544 -> 724132`. Artifacts: high `scratch/ps1-perf-iterate/20260428-022719/summary.json`, low `20260428-022950/summary.json`. | Promote as work reduction, not a VBlank win. This is the largest helper-audit shrink in this pass and reinforces that small static helpers should be audited under the actual PS1 compiler rather than assumed inlined. |
| 2026-04-28 | `fg3-stage-predicate-helper-macro` | log-only after `ps1: macro payload predicate helper` | Replace callable `fgRuntimeCanStageNextFrame()` with a macro. It has two call sites and looked like another small state predicate that might benefit from the helper-audit pattern. | Temporarily converted the helper to a macro and ran the high-tide FISHING3 gate against the accepted payload-predicate macro baseline, then reverted. | Failed/no promotion as code-size regression. High tide stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`) with stable PS-EXE and `FISHING3.FG2 LBA=1195`, but `jcreborn.elf` grew `724132 -> 724256`. Artifact: high `scratch/ps1-perf-iterate/20260428-024104/summary.json`. | Do not promote. This predicate is large enough that duplication across two call sites costs more than the callable helper body; retry only if call sites are consolidated or stage capability becomes a compile-time policy. |
| 2026-04-28 | `fg3-prefetch-window-capability-prune` | `ps1: prune duplicate prefetch-window check` | Remove the outer `fgRuntimeCanWindowCache()` check from `fgRuntimeTryPrefetchWindow()`. The function immediately checks the next payload with `fgRuntimeEntryFitsWindow()`, which already includes window-cache capability. | Removed the duplicate outer check, then validated high tide and low tide against the accepted payload-predicate macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724132 -> 724096`. Artifacts: high `scratch/ps1-perf-iterate/20260428-024441/summary.json`, low `20260428-024713/summary.json`. | Promote as work reduction, not a VBlank win. This keeps the hot prefetch path simpler while retaining the capability guard inside the entry-fit predicate. |
| 2026-04-28 | `fg3-prefetch-would-read-capability-prune` | `ps1: prune duplicate prefetch-would-read check` | Remove the outer `fgRuntimeCanWindowCache()` check from `fgRuntimeWindowPrefetchWouldRead()`. Like the active prefetch path, this helper immediately delegates to `fgRuntimeEntryFitsWindow()`, which already includes the same capability check. | Removed the duplicate outer check, then validated high tide and low tide against the accepted prefetch-window check cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724096 -> 724044`. Artifacts: high `scratch/ps1-perf-iterate/20260428-025101/summary.json`, low `20260428-025339/summary.json`. | Promote as work reduction, not a VBlank win. Both prefetch window-read decision paths now rely on the entry-fit predicate as the single window-capability gate. |
| 2026-04-28 | `fg3-prefetch-null-entry-prune` | `ps1: prune duplicate prefetch null checks` | Remove duplicate `entry == NULL` checks from both prefetch window helpers. `fgRuntimeEntryFitsWindow()` delegates to the payload predicate, which already returns false for null entries. | Removed the duplicate null checks, then validated high tide and low tide against the accepted prefetch-would-read cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724044 -> 724012`. Artifacts: high `scratch/ps1-perf-iterate/20260428-025719/summary.json`, low `20260428-030022/summary.json`. | Promote as work reduction, not a VBlank win. The prefetch helpers now use entry-fit as the single null/capability/payload gate. |
| 2026-04-28 | `fg3-seed-window-prechecked-guard-prune` | `ps1: prune seed-window prechecked guards` | Remove the initial entry/window/scratch guard from `fgRuntimeSeedWindowFromScratch()`. The helper has one call site, reached only after `fgRuntimeCanStageNextFrame()`, `fgRuntimeEntryFitsWindow(entry)`, and a successful direct-stage read. | Removed the duplicated guard while keeping the sector/size bounds, then validated high tide and low tide against the accepted prefetch null-check cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `724012 -> 723740`. Artifacts: high `scratch/ps1-perf-iterate/20260428-030429/summary.json`, low `20260428-030704/summary.json`. | Promote as work reduction, not a VBlank win. The helper now depends on the single existing call-site precondition; do not add a second call site without restoring guards or proving the same preconditions. |
| 2026-04-28 | `fg3-window-capability-single-call-inline` | `ps1: inline single-call window capability helper` | Inline `fgRuntimeCanWindowCache()` into `fgRuntimeEntryFitsWindow()`. After the prefetch and seed-window cleanup, the capability helper had only this one call site. | Removed the helper and folded its predicate into entry-fit, then validated high tide and low tide against the accepted seed-window guard cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723740 -> 723724`. Artifacts: high `scratch/ps1-perf-iterate/20260428-032128/summary.json`, low `20260428-032356/summary.json`. | Promote as work reduction, not a VBlank win. Single-call helpers should be inlined only after confirming they do not bloat the caller or disturb timing. |
| 2026-04-28 | `fg3-seed-window-nonempty-guard-prune` | `ps1: prune seed-window nonempty guard` | Remove the `windowEnd <= windowStart` guard from `fgRuntimeSeedWindowFromScratch()`. The helper's only caller has already proven `fgRuntimeEntryFitsWindow(entry)`, so payload size is nonzero and aligned end must exceed aligned start. | Removed the unreachable guard, then validated high tide and low tide against the accepted window-capability inline baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723724 -> 723680`. Artifacts: high `scratch/ps1-perf-iterate/20260428-032759/summary.json`, low `20260428-033028/summary.json`. | Promote as work reduction, not a VBlank win. Keep the sector/size bounds; only the impossible nonempty check was removed. |
| 2026-04-28 | `fg3-seed-window-capacity-guard-prune` | `ps1: prune seed-window capacity guards` | Remove the scratch/window capacity guard from `fgRuntimeSeedWindowFromScratch()`. The direct-stage `ps1_streamReadIntoFileBuffered()` call only succeeds when scratch can hold the same sector-aligned span, and the preceding `fgRuntimeEntryFitsWindow(entry)` check proves the span fits the stream window. | Removed the duplicated capacity guard, then validated high tide and low tide against the accepted seed-window nonempty cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723680 -> 723620`. Artifacts: high `scratch/ps1-perf-iterate/20260428-033434/summary.json`, low `20260428-033704/summary.json`. | Promote as work reduction, not a VBlank win. This relies on the direct-stage read and entry-fit preconditions; restore a guard if seed-window ever gains another call site. |
| 2026-04-28 | `fg3-seed-window-single-call-inline` | `ps1: inline single-call seed-window helper` | Inline `fgRuntimeSeedWindowFromScratch()` into its sole direct-stage call site. After the previous guard pruning, the helper only copied the proven sector-aligned scratch span into the stream-window cache. | Removed the helper and inserted its work at the direct-stage success point, then validated high tide and low tide against the accepted seed-window capacity cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723620 -> 723508`. Artifacts: high `scratch/ps1-perf-iterate/20260428-040139/summary.json`, low `20260428-040407/summary.json`. | Promote as work reduction, not a VBlank win. The direct-stage seed code is now local to its only valid precondition path. |
| 2026-04-28 | `fg3-next-payload-unused-index-prune` | `ps1: skip unused next-payload index` | Pass `NULL` to `fgRuntimeNextPayloadEntry()` from both prefetch-window helpers. Both callers previously requested a frame index, then immediately discarded it. | Removed the unused local/output write in the active prefetch and would-read helpers, then validated high tide and low tide against the accepted seed-window inline baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723508 -> 723368`. Artifacts: high `scratch/ps1-perf-iterate/20260428-040806/summary.json`, low `20260428-041036/summary.json`. | Promote as work reduction, not a VBlank win. The next-payload helper still supports index output for future users, but hot prefetch callers no longer pay for an unused write. |
| 2026-04-28 | `fg3-next-payload-output-param-prune` | `ps1: remove next-payload output parameter` | Remove the now-unused `outFrameIndex` parameter from `fgRuntimeNextPayloadEntry()`. After the prior cleanup, all callers passed `NULL`, so the output path was dead code. | Removed the parameter and dead conditional write, then validated high tide and low tide against the accepted unused-index cleanup baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723368 -> 723308`. Artifacts: high `scratch/ps1-perf-iterate/20260428-041419/summary.json`, low `20260428-041652/summary.json`. | Promote as work reduction, not a VBlank win. Reintroduce frame-index output only if a new caller proves it needs the index. |
| 2026-04-28 | `fg3-staged-frame-setter-macro` | `ps1: macro staged-frame setter` | Replace callable `fgRuntimeSetStagedFrame()` with a macro. The helper was emitted as a 72-byte symbol and had five hot-path call sites. | Converted the helper to a macro, then validated high tide and low tide against the accepted next-payload output-parameter baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723308 -> 723136`. Artifacts: high `scratch/ps1-perf-iterate/20260428-044157/summary.json`, low `20260428-044434/summary.json`. | Promote as work reduction, not a VBlank win. The macro keeps staged-frame state updates local while avoiding five hot-path helper calls. |
| 2026-04-28 | `fg3-prepare-staged-predicate-macro` | `ps1: macro prepare-staged predicate` | Replace callable `fgRuntimeCanPrepareStagedFrame()` with a macro. The helper had two held-loop call sites and was still emitted as a 172-byte symbol. | Converted the predicate to a macro, then validated high tide and low tide against the accepted staged-frame setter baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723136 -> 723012`. Artifacts: high `scratch/ps1-perf-iterate/20260428-045052/summary.json`, low `20260428-045315/summary.json`. | Promote as work reduction, not a VBlank win. The macro removes the helper while preserving the same held-loop readiness predicate. |
| 2026-04-28 | `fg3-window-prefetch-inverted-return` | uncommitted/reverted | Rewrite the final `fgRuntimeWindowPrefetchWouldRead()` ternary as `return !fgRuntimeWindowContainsEntry(entry);` to see whether the compiler emits a shorter predicate inversion. | Ran the high-tide FISHING3 gate against the accepted prepare-staged predicate macro baseline. | Rejected as an exact no-op. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`) and PS-EXE stayed `145408`, but `jcreborn.elf` stayed `723012` and `fgRuntimeWindowPrefetchWouldRead` stayed at the same address and `64` bytes. Artifact: high `scratch/ps1-perf-iterate/20260428-045724/summary.json`. | Do not retry this source-shape-only inversion without a compiler/codegen change; it produced identical output for the measured binary. |
| 2026-04-28 | `fg3-next-payload-active-guard-prune` | `ps1: prune next-payload active guard` | Remove the local `!gFgRuntime.active` guard from `fgRuntimeNextPayloadEntry()`. Its callers are active-loop prefetch probes and the returned entry is immediately validated by `fgRuntimeEntryFitsWindow()`, which already rejects inactive/non-pack/invalid-window state. | Removed the duplicate guard, then validated high tide and low tide against the accepted prepare-staged predicate macro baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `723012 -> 722988`. Artifacts: high `scratch/ps1-perf-iterate/20260428-050110/summary.json`, low `20260428-050332/summary.json`. | Promote as work reduction, not a VBlank win. Keep the broader entry-fit guard as the authoritative runtime-validity check. |
| 2026-04-28 | `fg3-next-payload-direct-table-scan` | uncommitted/reverted | Use `gFgRuntime.entryTable.entries/count` directly inside `fgRuntimeNextPayloadEntry()` to avoid repeated `fgGetEntryFromTable()` calls and duplicate range/null checks during the next-payload scan. | Ran the high-tide FISHING3 gate against the accepted next-payload active-guard baseline. | Rejected due code-size growth without timing benefit. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`) and PS-EXE stayed `145408`, but `jcreborn.elf` grew `722988 -> 723136`; `fgRuntimeLoadSceneFrame` grew `696 -> 740` and `foregroundPilotPlay` grew `8880 -> 8904`. Artifact: high `scratch/ps1-perf-iterate/20260428-050714/summary.json`. | Do not retry until there is evidence that next-payload scan CPU, not emitted code shape, is a measured bottleneck. |
| 2026-04-28 | `fg3-prefetch-perf-gate-coalesce` | `ps1: coalesce prefetch perf gates` | Coalesce duplicate adjacent `ps1PerfEnabled` checks in `fgRuntimeTryPrefetchWindow()` no-slack and slack-ineligible paths. The same two perf counters are still emitted, but each path pays one branch instead of two. | Combined each pair into a single gated block, then validated high tide and low tide against the accepted next-payload active-guard baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `fgRuntimeTryPrefetchWindow` shrank by `20` bytes and `jcreborn.elf` shrank `722988 -> 722952`. Artifacts: high `scratch/ps1-perf-iterate/20260428-052149/summary.json`, low `20260428-052413/summary.json`. | Promote as work reduction, not a VBlank win. This pattern is safe to try in other perf-only adjacent branches, but each occurrence still needs gates because code layout can dominate. |
| 2026-04-28 | `fg3-stage-next-no-slack-perf-gate-coalesce` | uncommitted/reverted | Apply the successful adjacent `ps1PerfEnabled` coalescing pattern to `fgRuntimeTryStageNextFrame()`'s no-slack path. | Ran the high-tide FISHING3 gate against the accepted prefetch perf-gate baseline. | Rejected due code-size growth without timing benefit. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`) and PS-EXE stayed `145408`, but `jcreborn.elf` grew `722952 -> 722972` and `foregroundPilotPlay` grew `8880 -> 8888`. Artifact: high `scratch/ps1-perf-iterate/20260428-052817/summary.json`. | Do not assume repeated source patterns repeat codegen wins; the same cleanup regressed when inlined into the larger scheduler body. |
| 2026-04-28 | `fg3-can-stage-next-macro` | uncommitted/reverted | Macro-expand the single-call `fgRuntimeCanStageNextFrame()` capability predicate, testing whether removing the helper call shrinks the hot stage-next path. | Ran the high-tide FISHING3 gate against the accepted prefetch perf-gate baseline. | Rejected due code-size growth without timing benefit. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`) and PS-EXE stayed `145408`, but `jcreborn.elf` grew `722952 -> 723016` and `foregroundPilotPlay` grew `8880 -> 8976`. Artifact: high `scratch/ps1-perf-iterate/20260428-053242/summary.json`. | Keep the helper. Unlike the staged-frame setter and prepare-staged predicate, this single-call capability check is cheaper out of line in the current size-optimized build. |
| 2026-04-28 | `fg3-fill-window-zero-byte-guard-prune` | `ps1: prune refill zero-byte guard` | Remove the `readBytes == 0` guard from `fgRuntimeFillWindowForEntry()`. Entry-fit already proves a nonzero payload fits a nonzero window, and the preceding `windowStart < packCdFile.size` check makes the file-end clamp nonzero for valid generated packs. | Removed the unreachable guard, then validated high tide and low tide against the accepted prefetch perf-gate baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `fgRuntimeFillWindowForEntry` shrank by `40` bytes and `jcreborn.elf` shrank `722952 -> 722828`. Artifacts: high `scratch/ps1-perf-iterate/20260428-053638/summary.json`, low `20260428-053901/summary.json`. | Promote as work reduction, not a VBlank win. The generated pack remains the correctness boundary; malformed/truncated packs should fail validation, not keep hot runtime fallback branches. |
| 2026-04-28 | `fg3-extend-window-zero-size-guard-prune` | uncommitted/reverted | Remove the `preserveBytes == 0` and `appendBytes == 0` checks from `fgRuntimeTryExtendWindow()`. Earlier range checks already prove both are nonzero before the capacity guard. | Ran the high-tide FISHING3 gate against the accepted refill zero-byte guard baseline. | Rejected as an exact binary no-op. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), PS-EXE stayed `145408`, `jcreborn.elf` stayed `722828`, and watched helper sizes/addresses stayed unchanged. Artifact: high `scratch/ps1-perf-iterate/20260428-054235/summary.json`. | Do not retry this source-only guard prune without a compiler change; current codegen already eliminated or folded the checks. |
| 2026-04-28 | `fg3-window-contains-payload-guard-prune` | `ps1: prune window-contains payload guard` | Remove the local `fgEntryHasPayload()` guard from `fgRuntimeWindowContainsEntry()`. The current callers either pass entries that already satisfied `fgRuntimeEntryFitsWindow()` or pass the active current/staged entry from a known-payload path. | Removed the duplicate guard, then validated high tide and low tide against the accepted refill zero-byte guard baseline. | Promoted as a flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `722828 -> 722756`. Artifacts: high `scratch/ps1-perf-iterate/20260428-060216/summary.json`, low `20260428-060445/summary.json`. | Promote as work reduction, not a VBlank win. The helper now assumes its callers have proven payload validity; restore the guard if a null/empty-entry caller is introduced. |
| 2026-04-28 | `fg3-copy-window-source-select-inline` | `ps1: inline copy-window source selection` | Remove the extra `fgRuntimeWindowContainsEntry()` call from `fgRuntimeCopyEntryFromWindow()` by selecting the setup-segment source first, then checking/copying the stream window directly. This avoids a containment helper call and duplicate setup-segment test during copy operations. | Inlined the source-selection checks in the copy helper, then validated high tide and low tide against the accepted window-contains payload-guard baseline. | Promoted as a tiny flat-timing code-size cleanup. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), low stayed exact-flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `FISH3LOW.FG2 LBA=1549`), and PS-EXE stayed `145408`. `jcreborn.elf` shrank `722756 -> 722752`. Artifacts: high `scratch/ps1-perf-iterate/20260428-060922/summary.json`, low `20260428-061153/summary.json`. | Promote as work reduction, not a VBlank win. The tiny size win still counts because it removes duplicated source-selection work without moving timing or layout. |
| 2026-04-28 | `fg3-entry-fit-window-size-guard-prune` | uncommitted/reverted | Remove the `streamWindowSize == 0` check from `fgRuntimeEntryFitsWindow()`, assuming active playback with a non-null stream-window buffer and nonzero configured prefetch window cannot have a zero-sized window. | Ran the high-tide FISHING3 gate against the accepted copy-window source-selection baseline. | Rejected due code-size growth without timing benefit. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`) and PS-EXE stayed `145408`, but `jcreborn.elf` grew `722752 -> 722768`. Artifact: high `scratch/ps1-perf-iterate/20260428-061555/summary.json`. | Keep the guard. The source invariant is plausible, but current codegen/layout is worse without it. |
| 2026-04-28 | `fg3-entry-fit-boolean-return` | uncommitted/reverted | Replace the final `fgRuntimeEntryFitsWindow()` ternary with the boolean expression directly, checking whether GCC emits a smaller predicate return after the latest window-helper cleanups. | Ran the high-tide FISHING3 gate against the accepted copy-window source-selection baseline. | Rejected as an exact binary no-op. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), PS-EXE stayed `145408`, `jcreborn.elf` stayed `722752`, and watched symbol size/address data stayed unchanged. Artifact: high `scratch/ps1-perf-iterate/20260428-064242/summary.json`. | Do not retry this source-shape-only return simplification without a compiler/codegen change. |
| 2026-04-28 | `fg3-load-frame-staged-result-boolean-return` | uncommitted/reverted | Replace `return stagedResult > 0 ? 1 : 0` in `fgRuntimeLoadSceneFrame()` with `return stagedResult > 0`, checking whether the staged-frame fast path emits smaller code after the recent helper cleanups. | Ran the high-tide FISHING3 gate against the accepted copy-window source-selection baseline. | Rejected as an exact binary no-op. High stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `FISHING3.FG2 LBA=1195`), PS-EXE stayed `145408`, `jcreborn.elf` stayed `722752`, and `fgRuntimeLoadSceneFrame` stayed at `696` bytes. Artifact: high `scratch/ps1-perf-iterate/20260428-064606/summary.json`. | Do not retry this return-shape-only simplification without a compiler/codegen change. |
| 2026-04-28 | `fg3-foreground-read-plan-tool` | tooling after `ps1: inline copy-window source selection` | Build a host-side read-plan analyzer so the next CD/setup experiment is based on existing summary/log/pack evidence rather than more runtime source-shape guesses. | Added `scripts/ps1-foreground-read-plan.py`, then ran it against the accepted FISHING3 high and low summaries plus `FISHING3.FG2`/`FISH3LOW.FG2`. | Accepted as tooling. The speed binary was untouched. High reports current setup coverage `[3..73)` and `43` uncovered post-locate read segments; top candidates include `348:354` at `6` sectors, `222:234` at `12`, `338:354` at `16`, and `216:240` at `24`. Low reports coverage `[2..152)` and `20` uncovered segments, confirming low tide is already much less exposed (`blocking_vb=3`). | Promote as analysis infrastructure, not a VBlank win. Use the report to choose the next generated setup/read-segment experiment; do not continue blind source-shape edits until the CD target is explicit. |
| 2026-04-28 | `fg3-retain-setup-segment-after-hit` | uncommitted/reverted | Keep `setupSegmentPrimed` true after a setup-segment copy, relying on existing direct-read paths to clear it only when the backing buffer is actually overwritten. This tested whether one setup segment could serve multiple adjacent entries. | Removed the clear in `fgRuntimeCopyEntryFromWindow()`, rebuilt, ran high and low FISHING3 gates with `--require-improvement`, then reverted the source. | Rejected as exact-flat with no promotable work win. High stayed `loop_vb=2093`, `blocking_vb=16`, `prefetch_overrun_vb=11`; low stayed `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`. Layout stayed stable, but watched hot symbols shifted by `-24` bytes and no key metric improved. Artifacts: high `scratch/ps1-perf-iterate/20260428-073120/summary.json`, low `20260428-073301/summary.json`. | Do not promote. Retaining the existing single segment is not enough; the next CD experiment needs a new generated segment/read-group placement rather than only keeping the old one alive longer. |
| 2026-04-28 | `fg3-high-read-group-223-234` | accepted after visual signoff | Add one FISHING3 high-tide read group for file sectors `223..234`, chosen from the foreground read-plan analyzer, and allow grouped contiguous extension when the previous window ends exactly at the group start. | Added the group to the existing stream-window grouping path, ran high once with `--require-improvement` to inspect key metrics, ran high again without the strict key-metric requirement for the accepted work-reduction gate, ran low tide for regression coverage, and launched a visual high-tide run for human signoff. | Promoted as work reduction. High timing stayed flat (`loop_vb=2093`, `blocking_vb=16`, `prefetch_overrun_vb=11`) while CD work improved (`loop_reads 44 -> 43`, `loop_read_vb 188 -> 185`, `hidden_reads 43 -> 42`). Low tide stayed exact-flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`). Layout stayed stable (`FISHING3.FG2 LBA=1195`, `FISH3LOW.FG2 LBA=1549`, PS-EXE `145408`). Artifacts: high `scratch/ps1-perf-iterate/20260428-074246/summary.json`, low `20260428-073903/summary.json`; visual run accepted by user. | Promote. This does not reduce visible VBlanks yet, but it removes one active-loop CD transaction with no visual regression. New baseline uses this grouped-read shape; continue with more analyzer-ranked groups or generated metadata. |
| 2026-04-28 | `fg3-high-read-group-338-354` | log-only after `fg3-high-read-group-223-234` | Add a second FISHING3 high-tide tail group for file sectors `338..354`, targeting the analyzer-ranked late read cluster after the accepted `223..234` group. | Added `{338,354}` beside the accepted FISHING3 high group, rebuilt, and ran the high-tide FISHING3 gate against `scratch/ps1-perf-iterate/current-fishing3-baseline.json`. | Rejected as a structural failure. The per-case summary completed and showed lower CD transaction work (`loop_reads 43 -> 41`, `loop_read_vb 185 -> 182`, `hidden_reads 42 -> 40`), but active pressure regressed (`loop_vb 2093 -> 2094`, `blocking_vb 16 -> 18`, `prefetch_overrun_vb 11 -> 13`) and the headless process then spammed invalid reads from `pc=0x00000CEC` until the log cap killed it. Artifact: `scratch/ps1-perf-iterate/20260428-074612/fishing3/perf-summary.json`; the oversized raw log was deleted after inspection. | Do not promote. Broad tail grouping can remove reads, but this range violates the clean-exit/correctness bar and likely crosses an unsafe stream-window/lifetime boundary. Retry tail grouping only with narrower ranges that fit the proven retained-window shape or generated metadata with explicit capacity proof. |
| 2026-04-28 | `fg3-high-read-group-345-354` | accepted after `fg3-high-read-group-338-354` rejection | Retry the late FISHING3 high-tide tail opportunity as a narrower group, combining only sectors `345..354` instead of the unsafe broad `338..354` range. | Added `{345,354}` beside the accepted `223..234` group, built, ran the high-tide FISHING3 gate against the current high baseline, then ran low tide against the current low baseline. | Promoted as work reduction. High timing stayed flat (`loop_vb=2093`, `blocking_vb=16`, `prefetch_overrun_vb=11`) and exited cleanly while CD work improved (`loop_reads 43 -> 42`, `loop_read_vb 185 -> 183`, `hidden_reads 42 -> 41`, `setloc 50 -> 49`, `sectors 490 -> 489`). Low tide stayed exact-flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`). Layout stayed stable (`FISHING3.FG2 LBA=1195`, `FISH3LOW.FG2 LBA=1549`, PS-EXE `145408`). Artifacts: high `scratch/ps1-perf-iterate/20260428-075123/summary.json`, low `20260428-075357/summary.json`. | Promote. This confirms the tail cluster is valuable only when split into capacity-safe groups. Continue with narrow/generated groups; do not restore the rejected broad tail range. |
| 2026-04-28 | `fg3-high-read-group-149-161` | log-only after `fg3-high-read-group-345-354` | Combine two adjacent mid-scene FISHING3 high-tide reads, sectors `149..161`, to test whether the narrow grouped-read win generalizes outside the tail cluster. | Added `{149,161}` beside the two accepted high-tide groups, built, and ran the high-tide FISHING3 gate against the current high baseline. Source was reverted after the failed gate. | Failed/no promotion. It reduced transaction count (`loop_reads 42 -> 41`, `hidden_reads 41 -> 40`, `setloc 49 -> 48`) but worsened active pressure and total read time: `loop_vb 2093 -> 2096`, `overrun_vb 139 -> 145`, `blocking_vb 16 -> 19`, `prefetch_overrun_vb 11 -> 14`, and `loop_read_vb 183 -> 187`. Layout stayed stable. Artifact: `scratch/ps1-perf-iterate/20260428-075720/summary.json`. | Do not promote. Not every adjacent-read merge is useful; this one shifts CD ownership into visible pressure. Future groups need slack/phase scoring, not only sector adjacency. |
| 2026-04-28 | `fg3-high-read-group-277-289` | log-only after `fg3-high-read-group-149-161` | Test a later two-read FISHING3 high-tide merge, sectors `277..289`, to see whether a smaller/later adjacent group avoids the mid-scene pressure regression. | Added `{277,289}` beside the two accepted high-tide groups, built, and ran the high-tide FISHING3 gate against the current high baseline. Source was reverted after the failed gate. | Failed/no promotion. It reduced transaction count (`loop_reads 42 -> 41`, `hidden_reads 41 -> 40`, `setloc 49 -> 48`) and lowered aggregate loop read time (`loop_read_vb 183 -> 177`), but worsened visible pressure: `loop_vb 2093 -> 2095`, `overrun_vb 139 -> 143`, `blocking_vb 16 -> 19`, and `prefetch_overrun_vb 11 -> 14`. Artifact: `scratch/ps1-perf-iterate/20260428-080106/summary.json`. | Do not promote. Lower aggregate read time is insufficient when the grouped read lands in the wrong held-slice phase. Next read-group scoring must prioritize visible-blocking ownership, not only total CD work. |
| 2026-04-28 | `fg3-high-read-group-161-174` | log-only after `fg3-high-read-group-277-289` | Test a phase-safer-looking FISHING3 high-tide group, sectors `161..174`, chosen because the first read starts after a long held gap rather than inside the tightest mid-scene sequence. | Added `{161,174}` beside the two accepted high-tide groups, built, and ran the high-tide FISHING3 gate against the current high baseline. Source was reverted after the failed gate. | Failed/no promotion. It reduced transaction count (`loop_reads 42 -> 41`, `hidden_reads 41 -> 40`, `setloc 49 -> 48`) but worsened timing and read cost: `loop_vb 2093 -> 2096`, `overrun_vb 139 -> 144`, `blocking_vb 16 -> 18`, `prefetch_overrun_vb 11 -> 13`, and `loop_read_vb 183 -> 188`. Artifact: `scratch/ps1-perf-iterate/20260428-080457/summary.json`. | Do not promote. A long preceding gap alone does not make a group safe. The accepted groups appear cluster-specific; further FISHING3 high work should move to generated setup/preload ownership or a scheduler change. |
| 2026-04-28 | `fg3-read-plan-phase-risk` | tooling after FISHING3 read-group failures | Teach the host-side foreground read-plan analyzer to show estimated saved reads, observed read-gap ranges, and a phase-risk hint so candidates are not ranked only by transaction count. | Updated `scripts/ps1-foreground-read-plan.py`, then reran it against the current FISHING3 high baseline. | Accepted as tooling. The report now marks the recently rejected `149..161`, `277..289`, and tight 24-sector groups as `high-tight-cluster`, while preserving raw estimates for lower-risk candidates. This changes no PS1 code and no speed binary. | Promote as methodology. Use the risk hint to avoid blind adjacent-read merges; runtime candidates still require the normal headless gate because the hint is advisory, not a correctness proof. |
| 2026-04-28 | `fg3-high-read-group-80-96` | log-only after `fg3-read-plan-phase-risk` | Test a post-setup FISHING3 high-tide group, sectors `80..96`, chosen because it starts on the actual append boundary and the planner marked its observed read gaps as medium rather than tight-cluster risk. | Added `{80,96}` beside the two accepted high-tide groups, built, and ran the high-tide FISHING3 gate against the current high baseline. Source was reverted after the failed gate, and the oversized raw log was deleted after inspection. | Failed/no promotion. It reduced transaction count (`loop_reads 42 -> 41`, `hidden_reads 41 -> 40`, `setloc 49 -> 48`) but regressed timing and visible pressure: `loop_vb 2093 -> 2098`, `overrun_vb 139 -> 146`, `blocking_vb 16 -> 20`, `prefetch_overrun_vb 11 -> 15`, and `loop_read_vb 183 -> 186`. The run also emitted invalid BIOS-address writes after scene metrics, growing the log to `719 MB`; the process exited success but this still fails the clean-run bar. Artifact: `scratch/ps1-perf-iterate/20260428-080916/summary.json`. | Do not promote. The advisory phase-risk hint is not sufficient for early high-tide grouping; avoid further manual FISHING3 high adjacent-group probes until generated setup/preload ownership or scheduler attribution changes the conditions. |
| 2026-04-28 | `fg3-fill-window-function-o2` | log-only after `fg3-high-read-group-80-96` | Test whether compiling only `fgRuntimeFillWindowForEntry()` at `O2` improves the refill hot path without changing pack layout or runtime policy. | Added `__attribute__((optimize("O2")))` to `fgRuntimeFillWindowForEntry()`, built, and ran the FISHING3 high-tide gate against the current high baseline. Source was reverted after the no-improvement gate. | Failed/no promotion. Timing and CD pressure were exact-flat: `loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, and `prefetch_overrun_vb=11`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE bucket `145408`), but symbol shape shifted (`fgRuntimeFillWindowForEntry` size `884 -> 468`, address `+628`; `foregroundPilotPlay` size `+4`) without a key-metric win. Artifact: `scratch/ps1-perf-iterate/20260428-081315/summary.json`. | Do not promote. Function-scoped compiler attributes need a timing win or a clean size/phase objective; this O2 shape change is noise for the current baseline. Revisit toolchain work only with padding/phase control and an explicit shrink criterion. |
| 2026-04-28 | `fg3-fill-window-function-os` | log-only after `fg3-fill-window-function-o2` | Test whether compiling only `fgRuntimeFillWindowForEntry()` at `Os` can produce a safe size/hot-symbol win where the `O2` probe only changed phase. | Added `__attribute__((optimize("Os")))`, built, and ran the FISHING3 high-tide gate against the current high baseline. Source was reverted after the exact no-op result. | Failed/no promotion as exact no-op. High-tide timing and CD pressure stayed flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`), layout stayed fixed (`FISHING3.FG2 LBA=1195`, PS-EXE bucket `145408`), and there was no size/symbol benefit: ELF stayed `723324`, `fgRuntimeFillWindowForEntry` stayed size `884`, and tracked hot symbol addresses stayed identical. Artifact: `scratch/ps1-perf-iterate/20260428-081905/summary.json`. | Do not promote. The file is already effectively at the `Os` shape for this function; retire function-scoped `Os` on `fgRuntimeFillWindowForEntry()` unless the global build flags change. |
| 2026-04-28 | `fg3-high-setup-settle-vblank` | log-only after `fg3-fill-window-function-os` | Test whether a setup-only one-VBlank settle after FISHING3 high setup-prime work can realign active CD/prefetch cadence without moving pack layout. | Added a high-tide FISHING3-only `fgRuntimeWaitHeldVBlank()` after first-frame setup/prime work, built, and ran the high-tide FISHING3 gate with improvement required. Source was reverted after the failed gate. | Failed/no promotion. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE bucket `145408`), but active cadence worsened: `loop_vb 2093 -> 2096`, `target_vb 1954 -> 1949`, `overrun_vb 139 -> 147`, `blocking_vb 16 -> 24`, and `prefetch_overrun_vb 11 -> 20`. `foregroundPilotPlay` grew by `52` bytes and shifted later hot symbols by `52` bytes. Artifact: `scratch/ps1-perf-iterate/20260428-082212/summary.json`. | Do not promote. A blind setup barrier is the wrong phase move for FISHING3 high; future setup/preload experiments need per-read ownership proof rather than extra settle VBlanks. |
| 2026-04-28 | `perf-harness-read-plan-artifacts` | `tools: emit foreground read plans from perf runs` | Make every headless perf run emit the host-side foreground read-plan report so the next CD/setup experiment is chosen from the same log/pack evidence as the gate. | Updated `scripts/ps1-perf-iterate.sh` to resolve the active foreground pack from each case summary, run `scripts/ps1-foreground-read-plan.py`, save `foreground-read-plan.json` and `.txt` in the case artifact directory, attach those paths to the case summary, and print the compact report. Gated FISHING3 high against the current high baseline. | Promoted as tooling. Runtime metrics stayed exact-flat (`loop_vb=2093`, `overrun_vb=139`, `blocking_vb=16`, `prefetch_overrun_vb=11`), layout stayed fixed (`FISHING3.FG2 LBA=1195`, PS-EXE bucket `145408`), and the run now prints ranked remaining ranges such as `234..258`, `240..264`, `216..240`, and `73..97` with read counts, estimated saved reads, gap timing, and phase-risk hints. Artifact: `scratch/ps1-perf-iterate/20260428-084104/summary.json`. | Promote. This changes no PS1 speed binary, but it makes the optimization loop more deterministic: future runtime changes should cite the per-run read plan rather than manually rerunning the analyzer. |
| 2026-04-28 | `fg3-high-read-group-234-246-cap13` | `ps1: add fishing3 follow-on read group` | Use the just-emitted read-plan artifact to test the next FISHING3 high-tide cluster, sectors `234..246`, and raise retained group capacity from `11` to `13` sectors so the grouped window can hold it. | Added `{234,246}` between the accepted `223..234` and `345..354` groups, increased group window capacity to `13` sectors, ran FISHING3 high with improvement required, then validated FISHING3 low and FISHING1 high against their baselines. | Promoted as small work reduction. FISHING3 high kept `loop_vb=2093` while improving `target_vb 1954 -> 1955`, `overrun_vb 139 -> 138`, `blocking_vb 16 -> 15`, `prefetch_overrun_vb 11 -> 10`, and `loop_reads 42 -> 41`. FISHING3 low stayed exact-flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`). FISHING1 stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`). Layout stayed stable (`FISHING3.FG2 LBA=1195`, `FISH3LOW.FG2 LBA=1549`, `FISHING1.FG2 LBA=397`, PS-EXE `145408`). Artifacts: high `scratch/ps1-perf-iterate/20260428-084422/summary.json`, low `20260428-084602/summary.json`, fishing1 `20260428-084745/summary.json`. | Promote. This is the first post-read-plan group that moves visible CD-pressure counters, not only transaction count. Continue with evidence-driven FISHING3 groups, but require cross-scene validation whenever retained group capacity changes. |
| 2026-04-28 | `fg3-high-read-group-246-258` | log-only after `fg3-high-read-group-234-246-cap13` | Test whether the next contiguous FISHING3 high-tide read-plan group, sectors `246..258`, can stack after the accepted `223..234` and `234..246` groups. | Added `{246,258}` beside the accepted groups and ran FISHING3 high against the current high baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion. It reduced transaction count (`loop_reads 41 -> 40`) but regressed visible pressure: `loop_vb 2093 -> 2096`, `overrun_vb 138 -> 143`, `blocking_vb 15 -> 17`, `prefetch_overrun_vb 10 -> 12`, and `target_vb 1955 -> 1953`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-085130/summary.json`. | Do not promote. The accepted `234..246` group appears to be the end of the safe contiguous extension for this phase. Further FISHING3 high CD wins should use generated segmented setup/preload metadata or a scheduler ownership change, not another adjacent manual group. |
| 2026-04-28 | `fg3-high-persistent-setup-segment-67-97` | log-only after `fg3-high-read-group-246-258` | Test a larger FISHING3 high setup-segment strategy: make the setup segment owned/persistent instead of scratch-backed/single-use, then widen high tide from sectors `67..73` to `67..97` so the next uncovered cluster is paid during setup. | Added owned setup-segment allocation, stopped invalidating the segment after copies or unrelated scratch reads, widened the high-tide segment to `30` sectors, and ran FISHING3 high against the current high baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion. It reduced active-loop read count (`loop_reads 41 -> 38`) but broke the stream-window cadence and regressed active pressure: `loop_vb 2093 -> 2102`, `overrun_vb 138 -> 152`, `blocking_vb 15 -> 32`, `prefetch_overrun_vb 10 -> 17`, and `due_misses 1 -> 3`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-085542/summary.json`. | Do not promote. A persistent side segment can hide reads but delays normal window seeding and causes later visible stalls. Retry this class only if setup bytes seed the main stream window or if the pack emits scheduler-aware preload metadata. |
| 2026-04-28 | `fg3-high-setup-prime-192k` | log-only after `fg3-high-persistent-setup-segment-67-97` | Test whether a larger contiguous FISHING3 high setup-prime window can seed the main stream window through the cluster that the persistent side segment mishandled. | Raised the FISHING3 high setup-prime budget from `128 KB` to `192 KB` and ran FISHING3 high against the current high baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion. Active reads fell (`loop_reads 41 -> 36`), but timing and visible CD pressure regressed sharply: `loop_vb 2093 -> 2102`, `overrun_vb 138 -> 153`, `blocking_vb 15 -> 29`, `prefetch_overrun_vb 10 -> 25`, and `target_vb 1955 -> 1949`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-085856/summary.json`. | Do not promote. FISHING3 high has a setup-prime knee: larger contiguous coverage can remove transactions but worsens active cadence. Future setup work needs segmented scheduling/ownership, not simply a larger prime window. |
| 2026-04-28 | `fg3-high-setup-prime-160k` | log-only after `fg3-high-setup-prime-192k` | Narrow the failed FISHING3 high contiguous setup-prime sweep to test whether `160 KB` is below the active-cadence knee that rejected `192 KB`. | Raised the FISHING3 high setup-prime budget from `128 KB` to `160 KB` and ran FISHING3 high against the current high baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion. Active reads fell (`loop_reads 41 -> 39`), but active pressure still regressed: `loop_vb 2093 -> 2100`, `overrun_vb 138 -> 149`, `blocking_vb 15 -> 26`, `prefetch_overrun_vb 10 -> 11`, and `due_misses 1 -> 3`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-090126/summary.json`. | Do not promote. The knee is below `160 KB`; stop blind contiguous high-prime sweeps unless a new scheduler metric explains why the larger setup read lands safely. |
| 2026-04-28 | `read-plan-source-policy-parser` | `tools: derive read-plan setup policy from source` | Prevent the foreground read-plan analyzer from ranking candidates against stale hard-coded setup-prime/segment policy while source-policy probes are in flight. | Updated `scripts/ps1-foreground-read-plan.py` to parse current setup-prime constants and FISHING3 setup-segment ranges from `src/foreground_pilot.c`, then re-ran the analyzer on the current FISHING3 high baseline. | Promoted as tooling. The current baseline still reports the expected coverage (`prime=[3,67]`, `segments=[[67,73]]`), and `python3 -m py_compile scripts/ps1-foreground-read-plan.py` passes. No PS1 binary or runtime behavior changed. | Promote. Future setup-prime/segment probes should get read-plan output matching the source under test, reducing false confidence from stale analyzer assumptions. Historical artifacts remain historical; rerun the analyzer when inspecting old summaries after source reverts. |
| 2026-04-28 | `fg3-high-setup-segment-seed-window-67-97` | log-only after `read-plan-source-policy-parser` | Retry the larger FISHING3 high setup-segment idea, but seed the main stream window from the setup segment on first use so the side segment does not bypass normal window cadence. | Added owned variable setup-segment storage, widened high tide to `67..97`, copied the segment into the stream window on first segment hit, and stopped invalidating the owned segment on unrelated scratch reads. Ran FISHING3 high against the current high baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion, but it improved over the persistent-side-buffer failure. Active reads fell (`loop_reads 41 -> 38`) and the analyzer correctly reported `segments=[[67,97]]`, but active pressure still regressed: `loop_vb 2093 -> 2097`, `overrun_vb 138 -> 146`, `blocking_vb 15 -> 24`, and `due_misses 1 -> 3`. `prefetch_overrun_vb` stayed flat at `10`. Artifact: `scratch/ps1-perf-iterate/20260428-090730/summary.json`. | Do not promote. Seeding the stream window is directionally better than a persistent side buffer, but `67..97` is still too broad. If retried, use a smaller sector span around the first uncovered read instead of a 30-sector setup jump. |
| 2026-04-28 | `fg3-high-setup-segment-seed-window-67-80` | log-only after `fg3-high-setup-segment-seed-window-67-97` | Retry the stream-window-seeded setup segment with only the first post-prime uncovered read, sectors `67..80`, instead of the broad `67..97` span. | Added the same owned/seeded setup-segment mechanism, set high tide to `13` sectors, and ran FISHING3 high against the current high baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion, though it narrowed the damage. Active reads fell (`loop_reads 41 -> 40`) but the gate still regressed: `loop_vb 2093 -> 2094`, `overrun_vb 138 -> 140`, `blocking_vb 15 -> 17`, and `prefetch_overrun_vb 10 -> 13`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-091052/summary.json`. | Do not promote. Expanding the accepted high setup segment beyond `67..73` is not safe even when seeding the stream window. Treat this family as exhausted until the scheduler can reserve the resulting read/copy cost explicitly. |
| 2026-04-28 | `macro-present-prepared-predicate` | `ps1: macro-expand prepared-present predicate` | Remove the callable `fgRuntimeCanPresentPreparedOnNextVBlank()` helper and expand its single held-loop call site as a macro, matching the earlier accepted predicate macro-expansion cleanups. | Macro-expanded the predicate, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key timing and CD metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed in the `145408` byte sector bucket and FISHING pack LBAs stayed fixed. `jcreborn.elf` shrank `723380 -> 723228`. Artifacts: high `scratch/ps1-perf-iterate/20260428-092324/summary.json`, low `20260428-092502/summary.json`, fishing1 `20260428-092645/summary.json`. | Promote. This is not a VBlank win, but it removes another foreground scheduler helper from the speed binary with no measurable regression. |
| 2026-04-28 | `macro-can-stage-next-frame` | log-only after `macro-present-prepared-predicate` | Test whether macro-expanding `fgRuntimeCanStageNextFrame()` removes useful call overhead from the setup/active staging paths. | Macro-expanded the two-call-site predicate and ran FISHING3 high against the current high baseline. Source was reverted after the size-only failure. | Failed/no promotion. Timing and CD pressure stayed exact-flat (`loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`), but the change grew the hot loop and binary: `foregroundPilotPlay 8936 -> 9032` and `jcreborn.elf 723228 -> 723308`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-093027/summary.json`. | Do not promote. Two-call-site predicate macro expansion is not automatically beneficial; keep this helper callable unless later call-site count or compiler layout changes. |
| 2026-04-28 | `fg3-high-move-setup-segment-168-174` | log-only after `macro-can-stage-next-frame` | Test whether moving the fixed FISHING3 high 6-sector setup segment from `67..73` to the read-plan's top remaining single-read candidate `168..174` improves active CD pressure without adding memory. | Changed only `FG_FISHING3_HIGH_SETUP_SEGMENT_START` from sector `67` to `168`, ran FISHING3 high against the current baseline with improvement required, then reverted the source. The oversized raw log was deleted after inspection. | Failed/no promotion. Losing the accepted early segment hurt more than preloading `168..174` helped: `loop_vb 2093 -> 2098`, `overrun_vb 138 -> 146`, `blocking_vb 15 -> 21`, and `prefetch_overrun_vb 10 -> 16`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact summary: `scratch/ps1-perf-iterate/20260428-093408/summary.json`; raw log was `719 MB` and removed. | Do not promote. The accepted `67..73` setup segment is high-value even though later single reads look expensive in isolation. Future segment changes should add generated multi-segment ownership, not replace the existing early segment. |
| 2026-04-28 | `fg3-low-read-group-159-171` | log-only after `fg3-high-move-setup-segment-168-174` | Test whether FISHING3 low tide can use the read-plan's top 12-sector cluster, `159..171`, through the same grouped-read mechanism that helped high tide. | Added a low-tide read-group table, enabled `streamReadGroupsEnabled=3` for FISHING3 low, and ran FISHING3 low against the current low baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion. It reduced `loop_reads 21 -> 20`, but all key metrics stayed flat (`loop_vb=2090`, `overrun_vb=130`, `blocking_vb=3`, `prefetch_overrun_vb=3`) and code grew (`foregroundPilotPlay +44`, `fgRuntimeFillWindowForEntry +24`). Layout stayed stable (`FISH3LOW.FG2 LBA=1549`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-093726/summary.json`. | Do not promote. Low-tide read grouping needs a visible pressure win to justify the extra table/branch overhead. Transaction-count-only wins are insufficient when source size grows. |
| 2026-04-28 | `prefetch-candidate-threading` | log-only after `fg3-low-read-group-159-171` | Remove duplicate next-payload scanning by threading the candidate returned by the prefetch-would-read check into `fgRuntimeTryPrefetchWindow()`. | Replaced `fgRuntimeWindowPrefetchWouldRead()` with a candidate helper, changed prefetch-window call sites to pass the precomputed entry where available, and ran FISHING3 high against the current baseline. Source was reverted after the no-benefit gate. | Failed/no promotion. Timing and CD pressure stayed exact-flat (`loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`), but the code grew: `fgRuntimeTryPrefetchWindow 392 -> 352`, `foregroundPilotPlay 8936 -> 8948`, and `jcreborn.elf 723228 -> 723260`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-100349/summary.json`. | Do not promote. Avoid threading candidates through the held-loop call sites unless a finer CPU counter proves duplicate scans matter; the current shape trades helper shrink for larger scheduler code. |
| 2026-04-28 | `macro-held-wait-helper` | `ps1: inline held-vblank wait helper` | Inline `fgRuntimeWaitHeldVBlank()` so the hottest held-frame wait path does not call a 32-byte helper. | Macro-expanded the helper, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408` and pack LBAs stayed fixed. `foregroundPilotPlay` grew `8936 -> 8944`, but `jcreborn.elf` shrank `723228 -> 723024`. Artifacts: high `scratch/ps1-perf-iterate/20260428-100720/summary.json`, low `20260428-100906/summary.json`, fishing1 `20260428-101041/summary.json`. | Promote. This is not a VBlank win, but it removes the callable wait helper and reduces total ELF size without timing or layout regression. |
| 2026-04-28 | `macro-read-tick-counter` | `ps1: macro-expand foreground tick counter` | Remove the tiny `fgReadTickCounter()` wrapper around `VSync(-1)` from the foreground timing/perf paths. | Macro-expanded the wrapper, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408` and pack LBAs stayed fixed. `foregroundPilotPlay` grew `8944 -> 8948`, but `jcreborn.elf` shrank `723024 -> 722940`. Artifacts: high `scratch/ps1-perf-iterate/20260428-101358/summary.json`, low `20260428-101541/summary.json`, fishing1 `20260428-101717/summary.json`. | Promote. This removes one tiny callable wrapper and keeps deterministic playback flat; count it as code-size/call-overhead cleanup, not a VBlank speed win. |
| 2026-04-28 | `macro-frame-rendered-helper` | `ps1: inline frame-rendered marker` | Inline `fgRuntimeMarkFrameRendered()` so render completion does not call a tiny two-use helper. | Macro-expanded the helper, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408` and pack LBAs stayed fixed. `foregroundPilotPlay` grew `8948 -> 9016`, but `jcreborn.elf` shrank `722940 -> 722792`. Artifacts: high `scratch/ps1-perf-iterate/20260428-104319/summary.json`, low `20260428-104507/summary.json`, fishing1 `20260428-104643/summary.json`. | Promote. This is another code-size/call-overhead cleanup with flat deterministic playback; do not count it as a VBlank speed win. |
| 2026-04-28 | `inline-elapsed-vblank-accounting` | `ps1: inline foreground elapsed ticks` | Remove the one-call `fgElapsedVBlanksSince()` helper and inline the same scene-clock update in `foregroundPilotRuntimeAdvance()`, where the pointer argument is always `&gFgRuntime.sceneClockTick`. | Deleted the helper, kept the same wrap-safe elapsed calculation and zero-to-one VBlank clamp, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408` and pack LBAs stayed fixed. `jcreborn.elf` shrank `722792 -> 722660`; `foregroundPilotPlay` stayed `9016`. Artifacts: high `scratch/ps1-perf-iterate/20260428-105110/summary.json`, low `20260428-105634/summary.json`, fishing1 `20260428-105252/summary.json`. | Promote. This is a small helper-removal/code-size win with flat deterministic playback; do not count it as a VBlank speed win. |
| 2026-04-28 | `compose-entry-prechecked-guard-prune` | `ps1: prune compose prechecked guard` | Remove the defensive `entry == NULL || frameData == NULL` guard from `fgRuntimeComposeEntryToBackground()`. Both call sites pass runtime-owned entries and buffers after scene-pack startup or staged-frame prep succeeds. | Deleted the duplicate guard, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408` and pack LBAs stayed fixed. `foregroundPilotPlay` shrank `9016 -> 8996`, `foregroundPilotRuntimeCompose` shrank `56 -> 44`, and `jcreborn.elf` shrank `722660 -> 722376`. Artifacts: high `scratch/ps1-perf-iterate/20260428-110000/summary.json`, low `20260428-110137/summary.json`, fishing1 `20260428-110314/summary.json`. | Promote. This removes a duplicated safety check from an internal compose helper; do not add external callers without restoring equivalent preconditions. |
| 2026-04-28 | `prefetch-window-output-clear-prune` | `ps1: prune prefetch output clear` | Remove the initial nullable output-clear from `fgRuntimeTryPrefetchWindow()`. All current callers pass a real stack pointer, and false-return paths do not consume the elapsed output. | Removed the redundant output clear, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408` and pack LBAs stayed fixed. `fgRuntimeTryPrefetchWindow` shrank `392 -> 388`, and `jcreborn.elf` shrank `722376 -> 722364`. Artifacts: high `scratch/ps1-perf-iterate/20260428-112418/summary.json`, low `20260428-112555/summary.json`, fishing1 `20260428-112734/summary.json`. | Promote as a tiny hot-helper cleanup. The call-site contract is now part of the helper precondition; restore a clear if a future caller needs false-return output normalization. |
| 2026-04-28 | `stage-next-output-guard-prune` | `ps1: prune stage-next output guards` | Remove nullable elapsed-output guards from `fgRuntimeTryStageNextFrame()`. The active-loop caller always passes a stack pointer and ignores elapsed output when the helper returns false. | Removed the initial clear plus three nullable writes, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408`, pack LBAs stayed fixed, and `jcreborn.elf` shrank `722364 -> 722348`. Artifacts: high `scratch/ps1-perf-iterate/20260428-113036/summary.json`, low `20260428-113225/summary.json`, fishing1 `20260428-113401/summary.json`. | Promote as a tiny scheduler-helper cleanup. The non-null output pointer is now an explicit call-site precondition for this internal helper. |
| 2026-04-28 | `prepare-output-guard-prune` | `ps1: prune prepare output guards` | Remove nullable elapsed-output guards from `fgRuntimePrepareStagedFrameForPresent()`. The helper has one active-loop caller, and that caller always passes `&prepareElapsedVBlanks`. | Removed the initial clear and final nullable write, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408`, pack LBAs stayed fixed, and `jcreborn.elf` shrank `722348 -> 722336`. Artifacts: high `scratch/ps1-perf-iterate/20260428-120348/summary.json`, low `20260428-120719/summary.json`, fishing1 `20260428-120854/summary.json`. | Promote as a tiny prepared-frame helper cleanup. The non-null output pointer is now an explicit call-site precondition for this internal helper. |
| 2026-04-28 | `fill-window-output-clear-prune` | `ps1: prune fill-window output clear` | Remove the initial nullable elapsed-output clear from `fgRuntimeFillWindowForEntry()`. Non-null callers initialize their local elapsed value to zero before calling; NULL callers do not need output normalization. | Removed the redundant output clear, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. | Promoted as work reduction. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408`, pack LBAs stayed fixed, `fgRuntimeFillWindowForEntry` shrank `900 -> 888`, and `jcreborn.elf` shrank `722336 -> 722304`. Artifacts: high `scratch/ps1-perf-iterate/20260428-121135/summary.json`, low `20260428-121313/summary.json`, fishing1 `20260428-121454/summary.json`. | Promote as a small refill-helper cleanup. Keep caller-side elapsed initialization if future edits rely on false-return elapsed values. |
| 2026-04-28 | `setup-prime-entry-null-guard-prune` | log-only after `fill-window-output-clear-prune` | Remove the `entry == NULL` guard from `fgRuntimePrimeNextFrameForSetup()` after the caller has already proven `frameIndex + 1 < header.frameCount`. Valid generated FG2 packs should have an entry for every frame, so this looked like a removable malformed-pack fallback. | Removed the guard, then gated FISHING3 high, FISHING3 low, and FISHING1 high against the rolling baselines. Source was reverted after red-team review. | Failed/no promotion. All key metrics stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408`, pack LBAs stayed fixed, and `jcreborn.elf` stayed `722304`; tracked hot symbols only shifted by `+4` bytes. Artifacts: high `scratch/ps1-perf-iterate/20260428-122032/summary.json`, low `20260428-122212/summary.json`, fishing1 `20260428-122349/summary.json`. | Do not promote. A fallback-removal change needs a size, symbol, or timing win; this source shape only perturbed addresses without reducing the binary. |
| 2026-04-28 | `fgp3-static-upload-before-loop` | log-only after `setup-prime-entry-null-guard-prune` | Move the FGP3-required full static framebuffer upload out of the active playback loop: after clean-rect setup, call `VSync(0)`, upload the current background with `grDrawBackground()`, then clear residual dirty state before `ps1PerfMarkLoopStart()`. This preserves the full first presentation that fixed the FGP3 visual regression while testing whether the first residual frame can avoid a loop-time full upload. | Added the setup-time upload and residual dirty clear, then ran FISHING1 against the rolling baseline with improvement required. Source was reverted after the failed gate. | Failed/no promotion. Key timing stayed exact-flat (`loop_vb=1207`, `overrun_vb=131`, `blocking_vb=0`, `prefetch_overrun_vb=0`), but setup grew by one VBlank and gfx work increased (`upload_calls 155 -> 156`, `upload_bytes 8584960 -> 8699520`, `dirty_rows 13414 -> 13593`). `foregroundPilotPlay` grew by `40` bytes and `jcreborn.elf` grew `722304 -> 722400` while PS-EXE stayed `145408`. Artifact: `scratch/ps1-perf-iterate/20260428-122916/summary.json`. | Do not promote. The full first FGP3 upload cannot be shifted this way; it adds a second static upload instead of replacing the loop upload. Future first-frame work needs an explicit framebuffer-presented state in the graphics dirty tracker, not an extra foreground setup call. |
| 2026-04-28 | `dirty-upload-band-gap-zero` | `ps1: tighten dirty upload bands` | Tighten `grDrawBackground()` vertical upload banding from a one-row clean-gap merge to a zero-row merge. This should upload fewer clean rows inside residual dirty bands, trading more `LoadImage` rectangles for lower DMA byte volume. | Changed `GR_UPLOAD_BAND_MERGE_GAP` from `1` to `0`, gated FISHING1, FISHING3 high, and FISHING3 low against the rolling baselines, then tested a cap-6 variant and kept the existing cap-8 because cap-6 gave back part of the high-tide byte win. | Promoted as render/upload work reduction. All key metrics stayed exact-flat: FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`. Upload bytes/dirty rows fell on all three gates: FISHING1 `8584960 -> 8533120`, FISHING3 high `16771200 -> 16665600`, and FISHING3 low `11838720 -> 11820800`. Rectangles rose (`355 -> 436`, `693 -> 858`, `412 -> 440`), `grDrawBackground` shrank by `32` bytes, `jcreborn.elf` moved `722304 -> 722312`, and PS-EXE stayed `145408`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-123326/summary.json`, high `20260428-123506/summary.json`, low `20260428-123643/summary.json`, cap-6 probes `20260428-123848`, `20260428-124027`, `20260428-124202`. | Promote as a byte-volume reduction checkpoint, not a VBlank win. Keep watching rectangle pressure; if future GPU/CPU counters prove per-rect command overhead dominates, retry a one-row or adaptive gap. |
| 2026-04-28 | `dirty-upload-usebands-prune` | `ps1: prune dirty upload band flag` | Remove the separate `useBands` local in `grDrawBackground()` and branch directly on the authoritative scan state: `!capped && bandCount > 0`. This should preserve upload behavior while reducing upload-path code shape. | Replaced `useBands` with a function-scope `capped` flag, then gated FISHING1, FISHING3 high, and FISHING3 low against the zero-gap rolling baselines. | Promoted as upload-path code cleanup. All key metrics and upload work stayed exact-flat: FISHING1 `loop_vb=1207`, `upload_bytes=8533120`, `upload_rects=436`; FISHING3 high `loop_vb=2093`, `upload_bytes=16665600`, `upload_rects=858`; FISHING3 low `loop_vb=2090`, `upload_bytes=11820800`, `upload_rects=440`. PS-EXE stayed `145408`, pack LBAs stayed fixed, `grDrawBackground` shrank by `56` bytes, and `jcreborn.elf` shrank `722312 -> 722044`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-124657/summary.json`, high `20260428-124823/summary.json`, low `20260428-124959/summary.json`. | Promote. This removes one redundant upload-band decision variable without changing pixels, CD cadence, or upload work; count it as code-size/work-shape cleanup, not a VBlank win. |
| 2026-04-28 | `dirty-upload-single-branch-prune` | `ps1: prune single dirty upload fallback` | Remove the special `dirtyCount == 1` fallback in `grDrawBackground()`. The band path already handles uncapped single-tile uploads, and the generic fallback emits the same whole-tile upload when banding caps. | Deleted the single-tile branch and its now-dead `singleIndex` bookkeeping, then gated FISHING1, FISHING3 high, and FISHING3 low against the rolling baselines. | Promoted as upload-path code cleanup. All key timing, CD pressure, and upload work stayed exact-flat: FISHING1 `loop_vb=1207`, `upload_bytes=8533120`, `upload_rects=436`; FISHING3 high `loop_vb=2093`, `upload_bytes=16665600`, `upload_rects=858`; FISHING3 low `loop_vb=2090`, `upload_bytes=11820800`, `upload_rects=440`. PS-EXE stayed `145408`, pack LBAs stayed fixed, `grDrawBackground` shrank by `160` bytes, and `jcreborn.elf` shrank `722044 -> 721456`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-133219/summary.json`, high `20260428-133342/summary.json`, low `20260428-133518/summary.json`. | Promote. This removes one redundant upload fallback and its index tracking without changing pixels, CD cadence, or upload work; count it as code-size/work-shape cleanup, not a VBlank win. |
| 2026-04-28 | `dirty-upload-scan-guard-prune` | `ps1: prune dirty upload scan guard` | Remove the outer `dirtyCount > 0` wrapper around the upload-band scan. Each tile still exits through `minYs[i] < 0`, so the extra wrapper only duplicated the clean-frame decision. | Deleted the outer guard, then gated FISHING1, FISHING3 high, and FISHING3 low against the rolling baselines. | Promoted as upload-path code cleanup. All key timing, CD pressure, and upload work stayed exact-flat: FISHING1 `loop_vb=1207`, `upload_bytes=8533120`, `upload_rects=436`; FISHING3 high `loop_vb=2093`, `upload_bytes=16665600`, `upload_rects=858`; FISHING3 low `loop_vb=2090`, `upload_bytes=11820800`, `upload_rects=440`. PS-EXE stayed `145408`, pack LBAs stayed fixed, `grDrawBackground` shrank by `16` bytes, and `jcreborn.elf` shrank `721456 -> 721400`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-133951/summary.json`, high `20260428-134114/summary.json`, low `20260428-134250/summary.json`. | Promote. This is another small source-shape cleanup in the upload scanner; it does not change the active-loop VBlank baseline. |
| 2026-04-28 | `dirty-upload-drawsync-guard-prune` | `ps1: prune band upload sync guard` | Remove the `uploadRects > 0` guard before `DrawSync(0)` in the banded upload path. Entry to that path already requires `bandCount > 0`, and the loop emits one upload per band. | Deleted the redundant guard, then gated FISHING1, FISHING3 high, and FISHING3 low against the rolling baselines. | Promoted as upload-path code cleanup. All key timing, CD pressure, and upload work stayed exact-flat: FISHING1 `loop_vb=1207`, `upload_bytes=8533120`, `upload_rects=436`; FISHING3 high `loop_vb=2093`, `upload_bytes=16665600`, `upload_rects=858`; FISHING3 low `loop_vb=2090`, `upload_bytes=11820800`, `upload_rects=440`. PS-EXE stayed `145408`, pack LBAs stayed fixed, and `jcreborn.elf` shrank `721400 -> 721396`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-134546/summary.json`, high `20260428-134714/summary.json`, low `20260428-134850/summary.json`. | Promote as a tiny proven cleanup. The sync invariant is now tied to the band-path precondition: do not enter that path with zero bands. |
| 2026-04-28 | `dirty-upload-fallback-clean-guard` | log-only after `dirty-upload-drawsync-guard-prune` | Test whether making the fallback path explicit as `dirtyCount > 0` and removing its inner `DrawSync` guard shrinks clean-frame handling. | Changed the fallback `else` to `else if (dirtyCount > 0)`, removed the fallback's inner sync guard, and ran FISHING1 against the rolling baseline. Source was reverted after artifact review. | Failed/no promotion. Timing, CD pressure, layout, and upload work stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `upload_bytes=8533120`, `upload_rects=436`), but `grDrawBackground` grew by `72` bytes and `jcreborn.elf` regressed `721396 -> 721680`. Artifact: `scratch/ps1-perf-iterate/20260428-135329/summary.json`. | Do not promote. The compiler's current guarded fallback shape is smaller; retry only if the surrounding branch structure changes materially. |
| 2026-04-28 | `dirty-upload-coordinate-arithmetic` | `ps1: compute upload tile coordinates` | Replace the local `screenX[4]` and `screenY[4]` arrays in `grDrawBackground()` with direct coordinate arithmetic from the tile index. This tests a different shape than the earlier rejected static-coordinate-table probe. | Removed the local coordinate arrays, computed X/Y in both upload paths, then gated FISHING1, FISHING3 high, and FISHING3 low against the rolling baselines. | Promoted as upload-path code cleanup. All key timing, CD pressure, and upload work stayed exact-flat: FISHING1 `loop_vb=1207`, `upload_bytes=8533120`, `upload_rects=436`; FISHING3 high `loop_vb=2093`, `upload_bytes=16665600`, `upload_rects=858`; FISHING3 low `loop_vb=2090`, `upload_bytes=11820800`, `upload_rects=440`. PS-EXE stayed `145408`, pack LBAs stayed fixed, `grDrawBackground` shrank by `8` bytes, and `jcreborn.elf` shrank `721396 -> 721380`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-140427/summary.json`, high `20260428-140550/summary.json`, low `20260428-140726/summary.json`. | Promote. The formula shape wins where static tables lost; keep using measured artifact results rather than assuming table hoists help on this compiler. |
| 2026-04-28 | `dirty-upload-tile-pointer-macro` | log-only after `dirty-upload-coordinate-arithmetic` | Test whether removing the local four-pointer `tiles` array and selecting the background tile directly from the loop index shrinks `grDrawBackground()` stack/setup work. | Replaced the local `tiles` array with a local macro and ran FISHING1 against the rolling baseline. Source was reverted after artifact review. | Failed/no promotion. Timing, CD pressure, layout, and upload work stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `upload_bytes=8533120`, `upload_rects=436`), but `grDrawBackground` grew by `248` bytes and `jcreborn.elf` regressed `721380 -> 722372`. Artifact: `scratch/ps1-perf-iterate/20260428-141025/summary.json`. | Do not promote. The stack-local pointer table is much cheaper than repeated ternary tile selection in this compiler/code shape. |
| 2026-04-28 | `dirty-upload-hasdirty-flag` | log-only after `dirty-upload-coordinate-arithmetic` | Replace the upload-path `dirtyCount` integer with a boolean `hasDirty` flag because the value is only tested for nonzero after the dirty-tile scan. | Changed the incrementing counter to a one-shot flag and ran FISHING1 against the rolling baseline. Source was reverted after artifact review. | Failed/no promotion. Timing, CD pressure, layout, and upload work stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `upload_bytes=8533120`, `upload_rects=436`), but `jcreborn.elf` regressed `721380 -> 721400`. Artifact: `scratch/ps1-perf-iterate/20260428-141651/summary.json`. | Do not promote. The incrementing counter emits the smaller code shape in this compiler path; avoid boolean rewrites unless surrounding upload logic changes. |
| 2026-04-28 | `dirty-upload-derived-work-state` | log-only after `dirty-upload-hasdirty-flag` | Remove `dirtyCount` entirely and derive upload-work existence from `bandCount > 0 || capped`, since the band scan already proves whether the dirty upload path will do work. | Removed the counter and rewired perf/sync guards to the derived band state, then ran FISHING1 against the rolling baseline. Source was reverted after artifact review. | Failed/no promotion. Timing, CD pressure, layout, and upload work stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `upload_bytes=8533120`, `upload_rects=436`), but `grDrawBackground` grew by `84` bytes and `jcreborn.elf` regressed `721380 -> 721544`. Artifact: `scratch/ps1-perf-iterate/20260428-141916/summary.json`. | Do not promote. Deriving dirty state from banding is logically valid but codegen-expensive here; keep the explicit counter until the upload scanner is structurally rewritten. |
| 2026-04-28 | `fg3-high-read-group-134-150` | log-only after `dirty-upload-derived-work-state` | Add the FISHING3 high-tide read-plan candidate `134..150`, a medium-risk two-read cluster outside the previously rejected tight adjacent group family. | Added `{134,150}` to the high-tide read group table and ran FISHING3 high against the rolling baseline with `--require-improvement`. Source was reverted after artifact review. | Failed/no promotion. Key timing, CD pressure, and read counts stayed exact-flat (`loop_vb=2093`, `overrun_vb=138`, `blocking_vb=15`, `prefetch_overrun_vb=10`, `loop_reads=41`, `loop_read_vb=180`), while `jcreborn.elf` grew `721380 -> 721396`. Artifact: `scratch/ps1-perf-iterate/20260428-142224/summary.json`. | Do not promote. This candidate did not produce even a work-volume win under the current append boundaries; keep following the read-plan artifact, but require actual read-count or visible-pressure movement. |
| 2026-04-28 | `window-read-size-fallback-prune` | `ps1: prune window read-size fallback` | Remove the fallback from `fgRuntimeWindowReadSize()`. Active FG2 playback initializes `streamWindowReadSize` before any window-fit/fill helper can run, so repeatedly falling back to `streamWindowSize` is obsolete in the hot path. | Replaced the ternary macro with a direct `gFgRuntime.streamWindowReadSize` read, then gated FISHING1, FISHING3 high, and FISHING3 low against the rolling baselines. | Promoted as hot-helper cleanup. Key timing and CD pressure stayed exact-flat: FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`. PS-EXE stayed `145408`; `fgRuntimeFillWindowForEntry` shrank `888 -> 872`, `fgRuntimeLoadSceneFrame` `696 -> 688`, `fgRuntimeTryPrefetchWindow` `388 -> 384`, and `fgRuntimeWindowPrefetchWouldRead` `64 -> 60`. Aggregate ELF moved `721380 -> 721412`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-142526/summary.json`, high `20260428-143043/summary.json`, low `20260428-143220/summary.json`. | Promote as instruction-cache/hot-helper cleanup, not as an aggregate ELF-size or VBlank win. The invariant is now explicit: active window helpers require initialized `streamWindowReadSize`. |
| 2026-04-28 | `window-slack-eligible-boolean-macro` | log-only after `window-read-size-fallback-prune` | Simplify `fgRuntimeWindowSlackEligible()` from an explicit `? 1 : 0` macro to a direct comparison because callers consume it as a boolean. | Changed the macro, ran FISHING1 against the rolling baseline, then reverted the source after artifact review. | Failed/no promotion as exact binary no-op. Timing, CD pressure, layout, PS-EXE, ELF, and watched hot symbols stayed unchanged (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `jcreborn.elf=721412`). Artifact: `scratch/ps1-perf-iterate/20260428-143548/summary.json`. | Do not promote. The compiler already emits the same code for this boolean source shape; retry only after surrounding prefetch predicate code changes. |
| 2026-04-28 | `fg3-low-read-group-159-171` | `ps1: add fishing3 low read group` | Add a low-tide-specific FISHING3 read group for sectors `159..171`, chosen from the low-tide read-plan artifact as a medium-risk two-read cluster. | Added a FISHING3 low read-group table and enabled it only for `fishing3` low tide, then ran low tide first with `--require-improvement`, reran low tide without the key-only gate for the accepted work-reduction artifact, and cross-gated FISHING3 high plus FISHING1. | Promoted as CD work reduction. FISHING3 low kept key timing flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`) while active CD work improved: `loop_reads 21 -> 20`, `hidden_reads 21 -> 20`, `loop_read_vb 85 -> 81`, and `setloc 28 -> 27`. FISHING3 high and FISHING1 stayed exact-flat. PS-EXE stayed `145408`; aggregate ELF moved `721412 -> 721488`. Artifacts: low strict probe `scratch/ps1-perf-iterate/20260428-144435/summary.json`, accepted low `20260428-144638/summary.json`, high `20260428-144815/summary.json`, FISHING1 `20260428-144955/summary.json`. | Promote as a small CD transaction/read-time win, not a VBlank win. The code growth is acceptable because it removes one active-loop low-tide read without changing visible cadence or validated high/canary paths. |
| 2026-04-28 | `read-group-active-table-pointer` | `ps1: cache active read group table` | Store the active read-group table pointer/count during scene startup so `fgRuntimeGroupedAppendTargetEnd()` no longer switches between fishing1, fishing3 high, and fishing3 low tables on every grouped-append probe. | Replaced the mode byte with an active table pointer and count, then gated FISHING3 low, FISHING3 high, and FISHING1 against the rolling baselines. | Promoted as hot-helper cleanup. All key timing and CD work stayed exact-flat: FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. `fgRuntimeFillWindowForEntry` shrank `892 -> 844`, `foregroundPilotPlay` grew `8992 -> 9016`, PS-EXE stayed `145408`, and aggregate ELF moved `721488 -> 721564`. Artifacts: low `scratch/ps1-perf-iterate/20260428-145407/summary.json`, high `20260428-145555/summary.json`, FISHING1 `20260428-145732/summary.json`. | Promote as a hot grouped-append helper shrink. This trades setup code and a larger runtime struct for less append-time branching; do not count it as a VBlank or aggregate ELF win. |
| 2026-04-28 | `read-group-byte-count` | `ps1: narrow read group count` | Store the active read-group count and loop index as bytes because the generated group tables are tiny. This should keep the table-pointer hot-helper shape while reducing runtime-state/code footprint. | Changed the count field and grouped-append scan locals to `uint8`, then gated FISHING3 low, FISHING3 high, and FISHING1 against the rolling baselines. | Promoted as a small size cleanup. All key timing and CD work stayed exact-flat: FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. `fgRuntimeFillWindowForEntry` stayed at `844`, `foregroundPilotPlay` grew `9016 -> 9020`, PS-EXE stayed `145408`, and aggregate ELF shrank `721564 -> 721504`. Artifacts: low `scratch/ps1-perf-iterate/20260428-150019/summary.json`, high `20260428-150212/summary.json`, FISHING1 `20260428-150351/summary.json`. | Promote. This partially claws back the table-pointer aggregate ELF cost while preserving the hot helper shrink and the low-tide read reduction. |
| 2026-04-28 | `read-group-reset-owned-default-prune` | log-only after `read-group-byte-count` | Remove the no-read-group `else` assignments in startup because `fgRuntimeReset()` already clears the active read-group pointer and count before scene configuration. | Deleted the redundant-looking `streamReadGroups=NULL` and `streamReadGroupCount=0` assignments, ran FISHING1 against the rolling baseline, then reverted source after artifact review. | Failed/no promotion. Timing and CD pressure stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`), but `foregroundPilotPlay` grew `9020 -> 9036` and aggregate ELF regressed `721504 -> 721696`. Artifact: `scratch/ps1-perf-iterate/20260428-152443/summary.json`. | Do not promote. The explicit else block gives the compiler a smaller startup shape than relying on reset-owned defaults; keep it unless the surrounding startup selector is rewritten. |
| 2026-04-28 | `fg3-low-read-group-169-181` | log-only after `read-group-byte-count` | Add the next FISHING3 low-tide read-plan candidate, sectors `169..181`, to see whether the accepted low-tide grouping can stack into the following high-tight cluster. | Added `{169,181}` after the accepted `{159,171}` group and ran FISHING3 low against the rolling baseline with `--require-improvement`. Source was reverted after artifact review. | Failed/no promotion. Key timing and all measured CD work stayed exact-flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`, `hidden_reads=20`, `loop_read_vb=81`, `setloc=27`), while `foregroundPilotPlay` grew `9020 -> 9028` and aggregate ELF regressed `721504 -> 721544`. Artifact: `scratch/ps1-perf-iterate/20260428-152655/summary.json`. | Do not promote. This candidate does not fire under the current append boundaries and only adds table/code weight; continue low-tide grouping only with candidates that change actual read counters. |
| 2026-04-28 | `read-group-pointer-null-guard` | log-only after `fg3-low-read-group-169-181` | Use `streamReadGroups == NULL` instead of `streamReadGroupCount == 0` as the no-group guard in `fgRuntimeGroupedAppendTargetEnd()`, testing whether the cached table pointer is cheaper to check than the byte count. | Changed the guard, ran FISHING3 low against the rolling baseline, then reverted source after artifact review. | Failed/no promotion. Timing and CD work stayed exact-flat (`loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`), but `fgRuntimeFillWindowForEntry` grew `844 -> 856` and aggregate ELF regressed `721504 -> 721556`. Artifact: `scratch/ps1-perf-iterate/20260428-152925/summary.json`. | Do not promote. The byte count guard is the cheaper emitted shape in the current helper; keep the pointer only as the scan table. |
| 2026-04-28 | `read-group-direct-scan` | `ps1: scan active read groups directly` | Remove the local table/count copies in `fgRuntimeGroupedAppendTargetEnd()` and scan the cached runtime table directly. The test is whether the compiler emits a smaller grouped-append shape without changing read behavior. | Removed the locals, then gated FISHING3 low, FISHING3 high, and FISHING1 against the rolling baselines. | Promoted as aggregate size cleanup. Timing and CD work stayed exact-flat: FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. PS-EXE stayed `145408`, `fgRuntimeFillWindowForEntry` stayed at `844`, and aggregate ELF shrank `721504 -> 721376`. Artifacts: low `scratch/ps1-perf-iterate/20260428-153215/summary.json`, high `20260428-153359/summary.json`, FISHING1 `20260428-153535/summary.json`. | Promote as a tiny source-shape cleanup. It is not a VBlank win, but it makes the accepted grouped-read path smaller while preserving the current FISHING3 low read reduction. |
| 2026-04-28 | `fg3-high-read-group-246-258` | log-only after `read-group-direct-scan` | Add the next contiguous FISHING3 high-tide read-plan candidate, sectors `246..258`, after the accepted `234..246` group. This should save one additional active-loop read if the held slack can absorb the longer grouped append. | Added `{246,258}` to the high-tide read-group table and ran FISHING3 high against the rolling baseline with `--require-improvement`. Source was reverted after artifact review. | Failed/no promotion. It did save one read (`loop_reads 41 -> 40`), but the grouped read crossed the visible-pressure knee: `loop_vb 2093 -> 2096`, `overrun_vb 138 -> 143`, `blocking_vb 15 -> 17`, `prefetch_overrun_vb 10 -> 12`, and `due_misses 0 -> 1`. Layout stayed stable (`FISHING3.FG2 LBA=1195`, PS-EXE `145408`). Artifact: `scratch/ps1-perf-iterate/20260428-154111/summary.json`. | Do not promote. Transaction-count savings are not enough here; this contiguous cluster needs either a read-cost predictor, setup/inter-scene priming, or a different group boundary that does not create a due miss. |
| 2026-04-28 | `read-group-pack-end-clamp-prune` | `ps1: trust generated read group bounds` | Remove the pack-end clamp inside `fgRuntimeGroupedAppendTargetEnd()`. Read-group bounds are generated from validated pack sectors, so a group end beyond the pack should be treated as invalid generation data rather than clamped every runtime append probe. | Deleted the clamp and gated FISHING3 low, FISHING3 high, and FISHING1 against their rolling baselines. | Promoted as deterministic hot-helper cleanup. All three gates stayed exact-flat on timing and CD work: FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`, `loop_reads=41`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`. `fgRuntimeFillWindowForEntry` shrank `844 -> 820`, PS-EXE stayed `145408`, and aggregate ELF shrank `721376 -> 721232`. Artifacts: low `scratch/ps1-perf-iterate/20260428-154429/summary.json`, high `20260428-154607/summary.json`, FISHING1 `20260428-154742/summary.json`. | Promote. This removes runtime fallback logic from a generated-data path and makes the grouped-read code smaller without changing playback cadence. |
| 2026-04-28 | `stream-read-size-startup-fallback-prune` | `ps1: require initialized stream read size` | Remove the startup fallback that copied stream-window capacity into `streamWindowReadSize` when the read size was zero. Active foreground playback initializes the read size when prefetch is enabled, and the window helpers reject the disabled-prefetch path before reading it. | Deleted the fallback and gated FISHING1, FISHING3 high, and FISHING3 low against their rolling baselines. | Promoted as deterministic startup cleanup. All three gates stayed exact-flat: FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads=6`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`, `loop_reads=41`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`. `foregroundPilotPlay` shrank `9020 -> 8996`, PS-EXE stayed `145408`, and aggregate ELF shrank `721232 -> 721200`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-155035/summary.json`, high `20260428-155157/summary.json`, low `20260428-155334/summary.json`. | Promote. This is fallback removal and code-size cleanup only; do not count it as a VBlank win. |
| 2026-04-28 | `extend-window-valid-store-prune` | `ps1: prune redundant extend-window valid store` | Remove the successful-append `streamWindowValid = 1` store. `fgRuntimeTryExtendWindow()` only enters with a valid window, and its failure path explicitly invalidates the window before returning. | Deleted the redundant store and gated FISHING1, FISHING3 high, and FISHING3 low against their rolling baselines. | Promoted as hot-helper work-shape cleanup. All three gates stayed exact-flat: FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads=6`; FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`, `loop_reads=41`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`. `fgRuntimeFillWindowForEntry` shrank `820 -> 816`, PS-EXE stayed `145408`, and aggregate ELF moved `721200 -> 721228`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-155628/summary.json`, high `20260428-155751/summary.json`, low `20260428-155926/summary.json`. | Promote as a hot append-extension store removal, not as an aggregate binary-size or VBlank win. The current linker layout absorbs the source shrink as a small ELF increase while the PS-EXE sector bucket stays flat. |
| 2026-04-28 | `nonprefetch-elapsed-probe-skip` | log-only after `extend-window-valid-store-prune` | Skip the foreground tick/elapsed measurement around window reads when the read is not counted as prefetch and the caller passes no elapsed output. CD metrics already time the read in `cdrom_ps1.c`, so the wrapper measurement looks redundant for blocking reads. | Added a `trackElapsed` guard around the foreground tick and elapsed writes in both normal and append window reads, then ran FISHING3 high against the rolling baseline with `--require-improvement`. Source was reverted after artifact review. | Failed/no promotion. Key timing and CD pressure stayed flat (`loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`), but the extra branch shape grew `fgRuntimeFillWindowForEntry 816 -> 928` and aggregate ELF `721228 -> 721656`; PS-EXE stayed `145408`. Artifact: `scratch/ps1-perf-iterate/20260428-160529/summary.json`. | Do not promote. Avoid conditional elapsed guards in this helper; the branch/code cost is worse than the occasional unused foreground tick. |
| 2026-04-28 | `extend-window-capacity-guard-prune` | `ps1: prune redundant extend-window guards` | Remove two append-extension guards already covered by earlier conditions: `targetEnd <= currentEnd` proves `appendBytes` is nonzero, and `preserveBytes + appendBytes > streamWindowSize` covers any too-large preserved prefix. | Deleted the redundant guards and gated FISHING3 high, FISHING3 low, and FISHING1 against their rolling baselines. | Promoted as hot-helper cleanup. All three gates stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`, `loop_reads=41`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads=6`. `fgRuntimeFillWindowForEntry` shrank `816 -> 812`, PS-EXE stayed `145408`, and aggregate ELF moved `721228 -> 721232`. Artifacts: high `scratch/ps1-perf-iterate/20260428-160825/summary.json`, low `20260428-161003/summary.json`, FISHING1 `20260428-161139/summary.json`. | Promote as source-invariant cleanup in the append-extension path, not as a VBlank or aggregate binary-size win. |
| 2026-04-28 | `extend-window-align-hoist` | log-only after `extend-window-capacity-guard-prune` | Move the append-alignment rejection before the grouped-read helper so `fgRuntimeGroupedAppendTargetEnd()` can assume sector-aligned append starts and drop its own alignment check. | Hoisted the current/end alignment check in `fgRuntimeTryExtendWindow()`, removed the helper alignment guard, and ran FISHING3 high against the rolling baseline. Source was reverted after artifact review. | Failed/no promotion. Timing and CD pressure stayed exact-flat (`loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`), but `fgRuntimeFillWindowForEntry` grew `812 -> 820` and aggregate ELF moved `721232 -> 721276`; PS-EXE stayed `145408`. Artifact: `scratch/ps1-perf-iterate/20260428-161425/summary.json`. | Do not promote. The original helper-local alignment guard is the smaller emitted shape in this compiler layout. |
| 2026-04-28 | `contiguous-window-fill-via-extend` | `ps1: fill contiguous windows through extend path` | Let `fgRuntimeTryExtendWindow()` handle the case where the next window starts exactly at the current window end instead of rejecting it and falling through to the normal fill path. The CD read is the same aligned read into the same buffer; the difference is avoiding a second helper path for contiguous reads. | Removed the no-preserve/no-group rejection, then gated FISHING3 high, FISHING3 low, and FISHING1 against their rolling baselines. The first high run used `--require-improvement` and failed only because key timing/CD counters were flat, so it was treated as a work-reduction candidate and cross-gated. | Promoted as hot-helper and aggregate size cleanup. All three gates stayed exact-flat: FISHING3 high `loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`, `loop_reads=41`; FISHING3 low `loop_vb=2090`, `blocking_vb=3`, `prefetch_overrun_vb=3`, `loop_reads=20`; FISHING1 `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads=6`. `fgRuntimeFillWindowForEntry` shrank `812 -> 788`, PS-EXE stayed `145408`, and aggregate ELF shrank `721232 -> 721144`. Artifacts: high `scratch/ps1-perf-iterate/20260428-164504/summary.json`, low `20260428-164653/summary.json`, FISHING1 `20260428-164829/summary.json`. | Promote. This is not a VBlank win, but it removes a redundant fill-path split and lowers the hot streaming helper and aggregate ELF size. |
| 2026-04-28 | `read-group-zero-count-guard-prune` | log-only after `contiguous-window-fill-via-extend` | Remove the explicit `streamReadGroupCount == 0` guard from `fgRuntimeGroupedAppendTargetEnd()` because the loop naturally returns `targetEnd` when there are no groups. | Removed the guard and ran FISHING3 high against the rolling baseline. Source was reverted after artifact review. | Failed/no promotion. Timing and CD pressure stayed exact-flat (`loop_vb=2093`, `blocking_vb=15`, `prefetch_overrun_vb=10`), and aggregate ELF moved `721144 -> 721092`, but `fgRuntimeFillWindowForEntry` grew `788 -> 792`. It also adds unnecessary helper work for future scenes without active read groups. Artifact: `scratch/ps1-perf-iterate/20260428-165122/summary.json`. | Do not promote. Keep the zero-count guard because the grouped-read helper is hot and most future scene variants may not have generated read groups yet. |
| 2026-04-28 | `host-retry-manifest-read-cost` | `docs/tooling after read-group-zero-count-guard-prune` | Mine the failed/no-promotion experiment ledger into grouped retry classes and attach current read-cost profiles without adding PS1 runtime counters. | Added `scripts/ps1-perf-retry-manifest.py`, generated FISHING3 high/low read-plan artifacts with `scripts/ps1-foreground-read-plan.py`, then ran `scripts/ps1-perf-retry-manifest.py --summary scratch/ps1-perf-iterate/current-branch-baseline.json --summary scratch/ps1-perf-iterate/current-fishing3-baseline.json --summary scratch/ps1-perf-iterate/current-fishing3-low-baseline.json --read-plan scratch/ps1-perf-iterate/current-fishing3-read-plan.json --read-plan scratch/ps1-perf-iterate/current-fishing3-low-read-plan.json`. | Promoted as host-only evidence tooling. The manifest analyzed `322` failed/rejected rows, classifying `98` conditional retries, `67` near misses, `24` diagnostic-only rows, and `19` dead ends. Current read-cost profiles say FISHING1 is `cd-clean`, FISHING3 high has `visible_pressure_vb=25`, `loop_reads=41`, `due_misses=1`, and FISHING3 low has `visible_pressure_vb=6`, `loop_reads=20`, `due_misses=0`. Artifacts: `scratch/ps1-perf-iterate/current-retry-manifest.json`, `scratch/ps1-perf-iterate/current-retry-manifest.md`. | Promote. This does not change runtime speed, but it completes the first evidence-foundation step and makes generated CD/setup work the next informed implementation target. |
| 2026-04-28 | `host-retry-manifest-scope` | docs/tooling after `host-retry-manifest-read-cost` | Re-prioritize the retry manifest around all-scene/generalizable fixes instead of one-off scene patches, and verify the current over-target percentages before assuming every scene is still about `12%` slow. | Added scope classification to `scripts/ps1-perf-retry-manifest.py`, regenerated the manifest with FISHING1, FISHING2 high/low, and FISHING3 high/low summaries, and updated the optimization plan's all-scene targeting rule. | Promoted as host-only evidence tooling. Current measured canaries are: FISHING1 high/low `12.17%` over target, FISHING2 high `7.54%`, FISHING2 low `7.41%`, FISHING3 high `7.06%`, and FISHING3 low `6.63%`. The manifest now reports scope counts: `global-runtime=238`, `generated-all-scene=4`, `scene-family-canary=53`, `one-off-scene=10`, and `unknown=17`, with global/generalizable rows sorted first. Artifacts: refreshed `scratch/ps1-perf-iterate/current-retry-manifest.json` and `.md`. | Promote. FISHING3 remains a useful pressure canary, but mainline optimization should target shared runtime, shared pack format/generation, or generated per-scene policy for all routed variants rather than hand-authored scene fixes. |
| 2026-04-28 | `fishing2-high-setup-segment-185-191` | log-only after `host-retry-manifest-scope` | Test whether the read-plan's top FISHING2 high single-read candidate, sectors `185..191`, can remove the remaining two blocking/refill VBlanks through the existing setup-segment mechanism. | Tried the segment first as scratch-owned setup coverage, then retried as persistent-owned setup coverage. Both source variants were reverted after artifact review. | Failed/no promotion. The scratch-owned variant was timing-flat and did not reduce loop reads (`loop_vb=1898`, `blocking_vb=2`, `prefetch_overrun_vb=2`, `loop_reads=14`) while growing `foregroundPilotPlay` by about `1040` bytes. The persistent-owned variant was worse: `loop_vb 1898 -> 1900`, `overrun_vb 133 -> 135`, `blocking_vb/prefetch_overrun_vb 2 -> 4`, and `loop_reads=14`. Artifacts: scratch-owned `scratch/ps1-perf-iterate/20260428-172941/summary.json`, persistent-owned `20260428-173150/summary.json`. | Do not promote. A single setup segment that looks valuable offline can still add active pressure or fail to fire under current ownership. Retry FISHING2 high only with generated multi-segment policy plus scheduler ownership/cost proof, not another hand-coded segment. |
| 2026-04-28 | `fishing1-stale-read-group-prune` | `ps1: remove stale fishing1 read group` | Remove the old FISHING1 high-tide read group `{396,406}` now that the current FGP3 pack is only `195` sectors and the group cannot fire. | Deleted the stale table and startup branch, then gated FISHING1 high, FISHING3 high/low, and FISHING2 high/low against their rolling baselines. | Promoted as generated-policy cleanup. All tested canaries stayed exact-flat on timing/CD pressure: FISHING1 `loop_vb=1207`, FISHING2 high/low `loop_vb=1898`, FISHING3 high `loop_vb=2093`, and FISHING3 low `loop_vb=2090`. PS-EXE stayed `145408`, pack LBAs stayed fixed, and `foregroundPilotPlay` shrank by `44` bytes; aggregate ELF moved to `720964`. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-173532/summary.json`, FISHING3 high/low `20260428-173658/summary.json`, FISHING2 high/low `20260428-174029/summary.json`. | Promote. This removes stale generated-data cruft from the runtime selector and keeps grouped-read support focused on active FISHING3 policies until the all-scene generated policy pass exists. |
| 2026-04-28 | `all-scene-fgpilot-matrix-routing` | `ps1: wire fgpilot all-scene matrix` | Wire every generated FG2 pack into the active fgpilot path and add a performance matrix so future optimization work can measure all `63` scenes instead of extrapolating from FISHING canaries. | Added a generated scene manifest tool, regenerated the CD `FG/` directory to include all `126` high/low packs, added `scripts/ps1-perf-all-scenes.sh`, and kept the default random loop limited to `kProvenScenes`. Built the PS1 executable, built the all-FG CD image, then ran FISHING1 high and ACTIVITY1 high through the headless perf harness. | Promoted as all-scene routing/tooling. The full CD image fits at `411981024` bytes / `175162` sectors (`393M`). FISHING1 high still passes on the all-FG layout with `loop_vb=1207`, `target_vb=1076`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `pack_lba=401`. ACTIVITY1 high proves a newly routed non-FISHING scene boots and completes: `loop_vb=4383`, `target_vb=4241`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `due_misses=0`, `pack_lba=1822`. Artifacts: `scratch/ps1-perf-iterate/20260428-175648/summary.json`, `scratch/ps1-perf-iterate/20260428-175814/summary.json`. | Promote. This does not claim scene certification for the pending scenes; it gives the performance loop a generated matrix and a durable CSV ledger for every scene/tide variant. |
| 2026-04-28 | `read-plan-cd-alias-resolution` | `ps1: resolve foreground aliases for read plans` | Fix the headless read-plan hook for all-scene packs whose CD filename is an 8.3 alias, e.g. `FG\\ACTV1.FG2`, but whose generated source pack remains `ACTIVITY1.FG2`. Without this, non-FISHING scene runs passed but skipped the foreground read-plan artifact needed for generated CD/setup policy. | Updated `scripts/ps1-perf-iterate.sh` to resolve missing CD basenames through `scripts/ps1-foreground-scene-manifest.py`, then reran ACTIVITY1 high through `./scripts/ps1-perf-all-scenes.sh --limit 1 --tides high --frames 7200 --timeout 240 --no-sheet`. | Promoted as evidence tooling. The run now emits `foreground-read-plan.json` for ACTIVITY1 high and reports uncovered candidate ranges. Timing stayed unchanged: `loop_vb=4383`, `target_vb=4241`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `due_misses=0`, `loop_reads=137`. Artifact: `scratch/ps1-perf-iterate/20260428-180655/summary.json`. | Promote. No VBlank speed claim; this makes the generated read-cost path work for every CD alias family and keeps the next optimization target data-driven. |
| 2026-04-28 | `activity1-general-setup-prime-128k` | log-only after `read-plan-cd-alias-resolution` | Test whether a generalized non-FISHING setup-prime window can remove visible ACTIVITY1 high CD/refill pressure now that the all-scene matrix and alias-aware read-plan hook expose candidates outside FISHING. | Added a temporary default `128 KB` setup-prime for scenes without a specific policy, then ran ACTIVITY1 high against `scratch/ps1-perf-iterate/20260428-180655/summary.json` with `--require-improvement`. Source was reverted after artifact review. | Failed/no promotion. The run reduced active-loop reads (`137 -> 129`) but crossed the visible-pressure knee: `loop_vb 4383 -> 4384`, `overrun_vb 142 -> 143`, `blocking_vb 4 -> 5`, and `prefetch_overrun_vb 4 -> 6`. `due_misses` stayed zero and layout stayed fixed, so the failure is scheduler/read-cost pressure, not correctness. Artifact: `scratch/ps1-perf-iterate/20260428-181109/summary.json`. | Do not promote. Blind all-scene setup-prime sizes repeat earlier FISHING lessons: fewer reads is not enough. Retry only with generated per-scene coverage/cost metadata and full scene timing checks. |
| 2026-04-28 | `all-scene-pending-matrix-resume` | `ps1: resume pending scene matrix rows` | Make the all-scene performance sheet safe to fill incrementally across long-running sessions, and prevent failed dirty experiments from becoming the current measured row. | Added `--skip-measured-from` to `scripts/ps1-foreground-scene-manifest.py`, added `--skip-measured` / `--only-pending` to `scripts/ps1-perf-all-scenes.sh`, taught the sheet importer to ignore failed-gate cases, then ran `./scripts/ps1-perf-all-scenes.sh --only-pending --limit 2 --tides high --frames 7200 --timeout 240`. | Promoted as matrix tooling and data collection. The run added ACTIVITY4 high (`loop_vb=1215`, `target_vb=1065`, `blocking_vb=8`, `due_misses=0`) and ACTIVITY5 high (`loop_vb=1953`, `target_vb=1677`, `blocking_vb=158`, `prefetch_overrun_vb=47`, `due_misses=25`). The matrix is now `9 / 126` measured, and ACTIVITY5 high is the highest measured gap at `16.46%` over target. Artifact: `scratch/ps1-perf-iterate/20260428-181531/summary.json`. | Promote. No runtime speed claim; this makes the all-scene matrix resumable and identifies ACTIVITY5 high as the next generated CD/scheduler pressure target. |
| 2026-04-28 | `activity5-stream-window-sweep-20-32k` | log-only after `all-scene-pending-matrix-resume` | Test whether ACTIVITY5 high's `158` blocking VBlanks and `25` due misses can be reduced by larger stream-window coverage before adding generated read-group policy. | Ran ACTIVITY5 high against `scratch/ps1-perf-iterate/20260428-181531/summary.json` with `prefetch-window` values `20480`, `22528`, `24576`, and `32768`, each under `--require-improvement`. | Failed/no promotion, but the sweep produced a useful signal. `20 KB` regressed all key pressure (`loop_vb=1965`, `blocking_vb=173`, `prefetch_overrun_vb=63`). `22 KB` improved `loop_vb 1953 -> 1921` but regressed visible pressure (`blocking_vb 158 -> 167`, `prefetch_overrun_vb 47 -> 52`). `24 KB` improved `loop_vb 1953 -> 1946`, reduced `blocking_vb 158 -> 127`, and reduced due misses `25 -> 9`, but regressed `prefetch_overrun_vb 47 -> 75`. `32 KB` reduced due misses to `6` but regressed loop and `prefetch_overrun_vb=102`. Artifacts: `20260428-182421`, `20260428-182922`, `20260428-182154`, `20260428-182650`. | Do not promote a scalar window. ACTIVITY5 high needs generated group/segment policy that captures the `22/24 KB` loop-read reduction while explicitly covering the frames that became visible pressure. |
| 2026-04-28 | `activity5-read-plan-group-465-477` | log-only after `activity5-stream-window-sweep-20-32k` | Test the top ACTIVITY5 high 12-sector read-plan candidate, `465..477`, as a grouped append to see whether observed hot sector ranges can immediately translate to runtime policy. | Added a temporary `kActivity5HighReadGroups12` table with `{465,477}` and routed it for ACTIVITY5 high under the default `16 KB` window, then ran against `scratch/ps1-perf-iterate/20260428-181531/summary.json`. Source was reverted after artifact review. | Failed/no promotion. All key metrics were exact-flat (`loop_vb=1953`, `blocking_vb=158`, `prefetch_overrun_vb=47`, `due_misses=25`) and `group_hits=0`, while the source reshuffle grew `foregroundPilotPlay` by `8` bytes. Artifact: `scratch/ps1-perf-iterate/20260428-183331/summary.json`. | Do not promote. The read-plan range by itself is not enough; the generator must model the runtime append-start sector and retained-window capacity before emitting group tables. |
| 2026-04-28 | `activity5-grouped-fill-direct-bypass` | log-only after `activity5-read-plan-group-465-477` | Test whether the same ACTIVITY5 high group can fire if grouped coverage is applied to fresh aligned window fills, and then if entries that start a generated group bypass the 3-VBlank direct-stage shortcut. | First added grouped-fill expansion for normal window fills, then added a second variant that routes group-start entries away from direct-stage into window fill. Both ran ACTIVITY5 high against `scratch/ps1-perf-iterate/20260428-181531/summary.json` with `--require-improvement`; source was reverted after artifact review. | Failed/no promotion. Both variants stayed exact-flat on all key metrics: `loop_vb=1953`, `overrun_vb=276`, `blocking_vb=158`, `prefetch_overrun_vb=47`, and `due_misses=25`. CD-log comparison showed the sector sequence also stayed identical (`145` total reads, `139` loop reads, no missing or new file-sector starts). Artifacts: grouped-fill `scratch/ps1-perf-iterate/20260428-184055/summary.json`, direct-bypass `scratch/ps1-perf-iterate/20260428-184443/summary.json`, CD comparison `scratch/ps1-perf-iterate/20260428-184443/activity5-high/cd-summary.json`. | Do not promote. The ACTIVITY5 hot cluster is not reachable through the current append/window-prefetch hooks. The next attempt should target the blocking due-frame load path or generate scheduler-owned setup/coverage metadata rather than adding more group checks around prefetch fills. |
| 2026-04-28 | `activity5-retained-group-cap18` | log-only after `activity5-grouped-fill-direct-bypass` | Test whether the `465..477` ACTIVITY5 group only needs enough retained stream-window capacity to preserve the preceding `459..465` bytes while appending through the generated range. | Added the ACTIVITY5 high group and raised `FG_PREFETCH_GROUP_WINDOW_BYTES` from `13` to `18` sectors, then ran ACTIVITY5 high against `scratch/ps1-perf-iterate/20260428-181531/summary.json`. Source was reverted after artifact review. | Failed/no promotion. The group finally changed read work, but not in a promotable direction: `loop_reads 139 -> 137`, `due_misses 25 -> 24`, and `hidden_reads 114 -> 113`, while visible pressure worsened (`blocking_vb 158 -> 162`, `prefetch_overrun_vb 47 -> 50`, `overrun_vb 276 -> 277`). The run emitted a valid per-case JCPERF2 summary, but the wrapper exited `137` after invalid-read log spam grew the headless log past the harness cap. Artifact: `scratch/ps1-perf-iterate/20260428-184948/activity5-high/perf-summary.json`. | Do not promote. Raw retained capacity can save transactions, but it steals held slack and repeats the scalar-window failure. Retry this class only with scheduler-owned grouped reads that prove the larger append lands in hidden time. |
| 2026-04-28 | `activity5-narrow-read-group-465-471` | log-only after `activity5-retained-group-cap18` | Test a smaller ACTIVITY5 high read group that fits the existing `13`-sector retained capacity, preserving the preceding `459..465` window while merging only the first clustered follow-on read. | Added a temporary ACTIVITY5 high group `{465,471}` with the normal group capacity and ran ACTIVITY5 high against `scratch/ps1-perf-iterate/20260428-181531/summary.json` with `--require-improvement`. Source was reverted after artifact review. | Failed/no promotion. The narrow group reduced loop reads by one (`139 -> 138`) but worsened every visible pressure metric: `loop_vb 1953 -> 1958`, `overrun_vb 276 -> 283`, `blocking_vb 158 -> 170`, `prefetch_overrun_vb 47 -> 50`, and `due_misses 25 -> 26`. Artifact: `scratch/ps1-perf-iterate/20260428-185343/summary.json`. | Do not promote. Manual boundary trimming still steals the wrong slack on ACTIVITY5. Further ACTIVITY5 CD work needs scheduler-owned grouped reads, not another hand-authored sector range in this cluster. |
| 2026-04-28 | `all-scene-matrix-activity6-7-high` | `ps1: extend all-scene perf matrix rows` | Continue filling the generated all-scene performance sheet after ACTIVITY5 manual grouping proved too scheduler-sensitive, so the next high-impact target can be selected from measured data instead of a single canary. | Ran `./scripts/ps1-perf-all-scenes.sh --only-pending --limit 2 --tides high --frames 7200 --timeout 240`, fixed the sheet writer to keep `last_summary` repo-relative when `OUTPUT_ROOT` is absolute, and regenerated `docs/ps1/performance-scene-matrix.csv`. | Promoted as measurement/tooling. ACTIVITY6 high measured `loop_vb=1054`, `target_vb=910`, `15.82%` over, `blocking_vb=6`, `due_misses=0`. ACTIVITY7 high measured `loop_vb=762`, `target_vb=599`, `27.21%` over, `blocking_vb=26`, `prefetch_overrun_vb=27`, `due_misses=0`. The matrix is now `11 / 126` measured, and ACTIVITY7 high is the highest measured percent-over-target row. Artifact: `scratch/ps1-perf-iterate/20260428-185717/summary.json`. | Promote. No runtime speed claim; this redirects the next optimization pass toward classifying non-CD overhead on high-percent scenes while keeping ACTIVITY5 as the due-miss/prefetch stress case. |
| 2026-04-28 | `activity7-high-setup-prime-384k` | log-only after `all-scene-matrix-activity6-7-high` | Test whether ACTIVITY7 high's zero-due-miss, high-percent gap is mostly active CD stream pressure that can be paid during setup with scene-specific prime coverage. | First tried `448 KB`, which produced a strong local CD reduction but hit invalid-read log spam. Then tried `384 KB`, which completed ACTIVITY7 high cleanly, followed by FISHING1 high, ACTIVITY5 high, and FISHING3 high cross-gates. Source was reverted after the FISHING3 cross-gate failure. | Failed/no promotion despite a real local win. `384 KB` improved ACTIVITY7 high from `loop_vb=762`, `overrun_vb=163`, `blocking_vb=26`, `prefetch_overrun_vb=27`, `loop_reads=52` to `loop_vb=751`, `overrun_vb=139`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `loop_reads=21`. FISHING1 high and ACTIVITY5 high stayed exact-flat, but FISHING3 high regressed to `loop_vb=2095`, `blocking_vb=19`, `prefetch_overrun_vb=14`, and emitted invalid-read log spam under the shifted code layout. Artifacts: ACTIVITY7 `scratch/ps1-perf-iterate/20260428-190624/summary.json`, FISHING1 `20260428-190857`, ACTIVITY5 `20260428-191123`, FISHING3 per-case `scratch/ps1-perf-iterate/20260428-191408/fishing3-high/perf-summary.json`. | Do not promote in this source shape. The idea is worth retrying through layout-preserving generated setup-policy plumbing or a cold policy table, because the local ACTIVITY7 reduction is large and clean before cross-scene phase fallout. |
| 2026-04-28 | `activity7-setup-prime-wrapper-policy` | log-only after `activity7-high-setup-prime-384k` | Retry the ACTIVITY7 high `384 KB` setup-prime through a wrapper policy so the original setup-prime function body remains in place as ballast, testing whether that avoids the FISHING3 phase/log-spam failure. | Added a temporary `fgRuntimeSetupPrimeWindowBytesWithGeneratedPolicy()` wrapper that delegates to the existing policy first and returns `384 KB` only for ACTIVITY7 high under the default window. Ran ACTIVITY7 high with `--require-improvement`, then cross-gated FISHING1 high, ACTIVITY5 high, and FISHING3 high. Source was reverted after artifact review; oversized generated DuckStation logs were deleted after preserving summaries/read-plans. | Failed/no promotion. ACTIVITY7 reproduced the local win (`loop_vb 762 -> 751`, `overrun_vb 163 -> 139`, `blocking_vb 26 -> 4`, `prefetch_overrun_vb 27 -> 4`, `loop_reads 52 -> 21`). FISHING1 high and ACTIVITY5 high stayed exact-flat. FISHING3 high still regressed (`loop_vb 2093 -> 2095`, `blocking_vb 15 -> 19`, `prefetch_overrun_vb 10 -> 14`, `due_misses=1`) and hit the invalid-read log cap. Artifacts: ACTIVITY7 `scratch/ps1-perf-iterate/20260428-192907/summary.json`, FISHING1 `20260428-193238`, ACTIVITY5 `20260428-193502`, FISHING3 per-case `scratch/ps1-perf-iterate/20260428-193733/fishing3/perf-summary.json`. | Do not promote. A wrapper call is not layout-preserving enough; next retry should use truly cold/generated policy data, hot-symbol padding with proof, or a scheduler/pack-format change that makes the ACTIVITY7 setup coverage independent of FISHING3 foreground phase. |
| 2026-04-28 | `stage-next-zero-slack-perf-gate-coalesce` | log-only after `activity7-setup-prime-wrapper-policy` | Coalesce the two adjacent `ps1PerfEnabled` checks in `fgRuntimeTryStageNextFrame()`'s zero-slack branch, mirroring the earlier accepted prefetch no-slack gate cleanup. | Changed the two single-line guards into one block and rebuilt, then inspected `build-ps1/jcreborn.map` before running headless. Source was reverted after the map preflight. | Failed/no promotion. The shape grew `foregroundPilotPlay` from the current all-FG baseline by `8` bytes and did not shrink any tracked helper; no timing run was warranted because the hypothesis was code-size/work-shape only. | Do not promote. The compiler emits the original duplicate-guard shape smaller in this branch; avoid repeating this local coalesce unless a broader scheduler rewrite changes the surrounding code. |
| 2026-04-28 | `setup-prime-next-entry-null-guard-prune` | log-only after `stage-next-zero-slack-perf-gate-coalesce` | Remove the setup-prime next-frame `entry == NULL` guard after an explicit `frameIndex + 1 < frameCount` check, testing whether generated entry-table invariants can shrink setup code safely. | Deleted the guard, rebuilt, ran FISHING1 high against `current-allfg-fishing1-high`, then ran FISHING3 high against `current-fishing3-baseline`. Source was reverted after FISHING3 failed; oversized generated DuckStation logs were deleted after preserving summaries/read-plans. | Failed/no promotion. The map showed aggregate ELF `725648 -> 725536` and PS-EXE still `147456`; FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`). FISHING3 high regressed to the known bad phase (`loop_vb 2093 -> 2095`, `blocking_vb 15 -> 19`, `prefetch_overrun_vb 10 -> 14`, `due_misses=1`) and hit the invalid-read log cap. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-194738/summary.json`, FISHING3 per-case `scratch/ps1-perf-iterate/20260428-195002/fishing3/perf-summary.json`. | Do not promote. Even setup-only guard pruning can perturb the FISHING3 high phase; require FISHING3 high as a mandatory canary for hot-file cleanup until phase-control tooling exists. |
| 2026-04-28 | `retry-manifest-phase-risk-ranking` | tooling after `setup-prime-next-entry-null-guard-prune` | Mine the recent FISHING3 phase failures and current all-scene summaries into machine-readable retry signals, so the next runtime experiments are ranked by actual pressure and canary risk instead of just by read count or intuition. | Added phase-risk classification and pressure-score sorting to `scripts/ps1-perf-retry-manifest.py`, ran `python3 -m py_compile scripts/ps1-perf-retry-manifest.py`, then regenerated `scratch/ps1-perf-iterate/current-retry-manifest.json` and `.md` from the current FISHING and ACTIVITY summaries. | Promoted as host-side tooling. The manifest now reports `333` rows analyzed, phase-risk buckets of `22` `fishing3-high-required`, `5` `invalid-read-log-risk`, `90` `layout-sensitive`, and `216` `normal`, and ranks read pressure as ACTIVITY5 high (`score=2826`), ACTIVITY7 high (`score=693`), then FISHING3 high (`score=408`). Runtime speed is unchanged: FISHING1 remains `loop_vb=1207`, `target_vb=1076`, `12.17%` over target. | Keep. This does not count as a VBlank improvement, but it changes the operating rule: any layout-sensitive or FISHING3-tagged optimization must include FISHING3 high before promotion, and current read-cost targeting should start from pressure score. |
| 2026-04-28 | `single-parse-scene-identity-startup` | log-only after `retry-manifest-phase-risk-ranking` | Parse compact FG scene family/tag once at scene startup and use that identity for setup-prime and read-group selection, testing a generated-policy enabler that removes repeated scene-name checks before later policy-table work. | Reworked startup identity locally, ran FISHING1 high against `current-allfg-fishing1-high`, then ran FISHING3 high against `current-fishing3-baseline` with zero allowed regression. Source was reverted after FISHING3 failed; oversized generated logs were deleted after preserving summaries/read-plans. | Failed/no promotion. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, PS-EXE `147456`, `FISHING1.FG2` LBA `401`) but hot symbols shifted. FISHING3 high reproduced the known bad phase (`loop_vb 2093 -> 2095`, `blocking_vb 15 -> 19`, `prefetch_overrun_vb 10 -> 14`, `due_misses=1`) and hit the log cap. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-201640/summary.json`, FISHING3 per-case `scratch/ps1-perf-iterate/20260428-201904/fishing3/perf-summary.json`. | Do not promote. Even generated-policy plumbing that is setup-only is layout-sensitive in `foreground_pilot.c`; next policy work needs offset-stable generated data, cold-section isolation, or a hot-symbol/address padding harness before touching this file again. |
| 2026-04-28 | `restore-clean-span-tile-select-hoist` | log-only after `single-parse-scene-identity-startup` | Move tile-row selection in `grRestoreCleanBgSpanFromRects()` out of the clean-rect loop, testing a graphics-side FGP3 cleanup-span work reduction that avoids foreground scheduler code. | Hoisted the tile pointer/local-Y selection, ran FISHING1 high against `current-allfg-fishing1-high`, then ran FISHING3 high against `current-fishing3-baseline` with zero allowed regression. Source was reverted after FISHING3 failed; oversized generated logs were deleted after preserving summaries/read-plans. | Failed/no promotion. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, PS-EXE `147456`, `FISHING1.FG2` LBA `401`) but helper addresses shifted by `+4`. FISHING3 high again reproduced the bad phase (`loop_vb 2093 -> 2095`, `blocking_vb 15 -> 19`, `prefetch_overrun_vb 10 -> 14`) and hit the log cap. Artifacts: FISHING1 `scratch/ps1-perf-iterate/20260428-202422/summary.json`, FISHING3 per-case `scratch/ps1-perf-iterate/20260428-202652/fishing3/perf-summary.json`. | Do not promote. The phase sensitivity is not limited to `foreground_pilot.c`; even a tiny graphics helper address shift can perturb FISHING3. Prioritize a padding/phase-control harness before more source-shape micro-optimizations. |
| 2026-04-28 | `perf-harness-symbol-address-gate` | tooling after `restore-clean-span-tile-select-hoist` | Add a cheap phase-control gate so source-shape experiments can fail on hot-symbol address movement before spending a FISHING3 high run and risking log-cap spam. | Added `--max-symbol-address-delta N` and `PS1_PERF_MAX_SYMBOL_ADDRESS_DELTA` support to `scripts/ps1-perf-iterate.sh`; validated with `bash -n scripts/ps1-perf-iterate.sh` and `./scripts/ps1-perf-iterate.sh --help`. | Promoted as host-side harness tooling. Default behavior is unchanged, but future runs can use `--max-symbol-address-delta 0` to require exact tracked hot-symbol addresses against a baseline. This would have flagged both latest misses after the FISHING1 canary because they moved helper symbols before FISHING3 failed. | Keep. This is not a VBlank speed win, but it reduces iteration cost and makes phase-sensitive experiments safer. Use it for source-shape cleanups before running FISHING3 high. |
| 2026-04-28 | `activity7-padded-fgp3-pack` | `ps1: convert activity7 foreground to padded fgp3` | Convert ACTIVITY7 high from FGP2 to the existing FGP3 temporal-residual pack format, then pad the file back to the original byte size so the CD artifact remains size-stable while runtime payload/read pressure drops. | Converted `generated/ps1/foreground/ACTIVITY7.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, padded it back to `645455` bytes, ran ACTIVITY7 high against `scratch/ps1-perf-iterate/20260428-185717/summary.json` with improvement required, and ran FISHING1 high against `current-allfg-fishing1-high`. FISHING3 high was also probed, but the current branch reproduces the same `loop_vb=2095`/log-cap failure with the original ACTIVITY7 FGP2 pack restored, so that stale baseline is not a discriminator for this pack-only change. | Promoted as a layout-stable asset-format win. ACTIVITY7 high improves `loop_vb 762 -> 751`, `target_vb 599 -> 612`, `overrun_vb 163 -> 139`, `blocking_vb 26 -> 8`, `prefetch_overrun_vb 27 -> 8`, `loop_reads 52 -> 10`, and `loop_read_vb 262 -> 46`; payload drops `643211 -> 145381` while the CD file remains `645455` bytes and `FG\\ACTV7.FG2` stays at LBA `5634`. FISHING1 high stays exact-flat at `loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `FISHING1.FG2` LBA `401`. Artifacts: ACTIVITY7 `scratch/ps1-perf-iterate/20260428-205438/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-205825/summary.json`, FISHING3 control with original pack `scratch/ps1-perf-iterate/20260428-210420/fishing3/perf-summary.json`. | Promote. This turns the rejected ACTIVITY7 setup-prime pressure into a pack-format win without touching hot source code. Next pack-format conversions should start with the highest read-pressure FGP2 packs, keep physical size stable until CD phase is deliberately controlled, and refresh the stale FISHING3 high baseline separately before using it as a promotion gate again. |
| 2026-04-28 | `activity5-padded-fgp3-pack` | `ps1: convert activity5 foreground to padded fgp3` | Apply the same layout-stable FGP3 conversion to ACTIVITY5 high, the current measured read-pressure bottleneck, to test whether host-side residual packing can beat the failed scalar-window/read-group experiments without touching runtime source. | Converted `generated/ps1/foreground/ACTIVITY5.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, padded it back to `1640247` bytes, ran ACTIVITY5 high against `scratch/ps1-perf-iterate/20260428-181531/summary.json` with `--work-identity-min 0` because FGP3 intentionally changes restore-call identity, then cross-gated FISHING1 high and ACTIVITY7 high. | Promoted as the first large all-scene pack-format win. ACTIVITY5 high improves `loop_vb 1953 -> 1866`, `target_vb 1677 -> 1707`, `overrun_vb 276 -> 159`, `blocking_vb 158 -> 25`, `prefetch_overrun_vb 47 -> 8`, `due_misses 25 -> 4`, `loop_reads 139 -> 48`, and `loop_read_vb 637 -> 212`. Payload drops `1634015 -> 632152`, restore bytes drop `4544388 -> 463322`, upload bytes drop `34086400 -> 17074560`, and `FG\\ACTV5.FG2` stays at LBA `4050` with PS-EXE `147456`. FISHING1 high and ACTIVITY7 high stayed exact-flat. Artifacts: ACTIVITY5 `scratch/ps1-perf-iterate/20260428-211141/summary.json`, FISHING1 `20260428-211410`, ACTIVITY7 `20260428-211630`. | Promote. Padded FGP3 is now a proven host-side optimization path, not just a Fishing-specialized format. Continue converting high-pressure FGP2 packs one at a time with local scene improvement plus unchanged-layout canaries before returning to source-shape work. |
| 2026-04-28 | `activity6-padded-fgp3-pack` | `ps1: convert activity6 foreground to padded fgp3` | Continue the padded-FGP3 host preprocessing lane with ACTIVITY6 high, a short measured scene that was still `15.82%` over target with low but visible CD pressure. | Converted `generated/ps1/foreground/ACTIVITY6.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, padded it back to `388077` bytes, ran ACTIVITY6 high against `scratch/ps1-perf-iterate/20260428-185717/summary.json` with `--work-identity-min 0`, then cross-gated FISHING1 high against `current-allfg-fishing1-high`. | Promoted. ACTIVITY6 high improves `loop_vb 1054 -> 1043`, `target_vb 910 -> 911`, `overrun_vb 144 -> 132`, `blocking_vb 6 -> 0`, `prefetch_overrun_vb 6 -> 0`, `loop_reads 29 -> 6`, and `loop_read_vb 128 -> 25`. Payload drops `385869 -> 98412`, restore bytes drop `1056104 -> 58972`, upload bytes drop `11419520 -> 3275520`, and `FG\\ACTV6.FG2` stays at LBA `5254` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: ACTIVITY6 `scratch/ps1-perf-iterate/20260428-212058/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-212331/summary.json`. | Promote. This confirms the padded-FGP3 path works on small and medium FGP2 packs too. Keep the next tests in this lane scene-local and data-driven: convert, pad to original size, require local improvement, then cross-gate FISHING1 and at least one recently converted ACTIVITY scene when practical. |
| 2026-04-28 | `activity4-padded-fgp3-pack` | `ps1: convert activity4 foreground to padded fgp3` | Continue the padded-FGP3 conversion lane with ACTIVITY4 high, a measured FGP2 scene still `14.08%` over target. | Converted `generated/ps1/foreground/ACTIVITY4.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, padded it back to `839971` bytes, ran ACTIVITY4 high against `scratch/ps1-perf-iterate/20260428-181531/summary.json` with `--work-identity-min 0`, then cross-gated FISHING1 high. | Promoted. ACTIVITY4 high improves `loop_vb 1215 -> 1205`, `target_vb 1065 -> 1067`, `overrun_vb 150 -> 138`, `blocking_vb 8 -> 5`, `prefetch_overrun_vb 8 -> 5`, `loop_reads 63 -> 20`, and `loop_read_vb 274 -> 108`. Payload drops `836087 -> 291214`, restore bytes drop `2494474 -> 184546`, upload bytes drop `15255680 -> 8030720`, and `FG\\ACTV4.FG2` stays at LBA `3474` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: ACTIVITY4 `scratch/ps1-perf-iterate/20260428-212747/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-213012/summary.json`. | Promote. The repeated pattern is now strong enough to prioritize converting remaining measured FGP2 high scenes before more source-shape micro-optimizations. |
| 2026-04-28 | `activity1-padded-fgp3-pack` | `ps1: convert activity1 foreground to padded fgp3` | Convert the last currently measured ACTIVITY high-tide FGP2 row to padded FGP3. ACTIVITY1 had a low percent gap but a large payload and measurable CD work, so this tests whether the conversion still pays off when visible pressure is already small. | Converted `generated/ps1/foreground/ACTIVITY1.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, padded it back to `1691566` bytes, ran ACTIVITY1 high against `scratch/ps1-perf-iterate/20260428-180655/summary.json` with `--work-identity-min 0`, then cross-gated FISHING1 high. | Promoted. ACTIVITY1 high improves `loop_vb 4383 -> 4373`, `target_vb 4241 -> 4244`, `overrun_vb 142 -> 129`, `blocking_vb 4 -> 0`, `prefetch_overrun_vb 4 -> 0`, `loop_reads 137 -> 50`, and `loop_read_vb 589 -> 205`. Payload drops `1683850 -> 685922`, restore bytes drop `5802012 -> 542042`, upload bytes drop `29205120 -> 13253760`, and `FG\\ACTV1.FG2` stays at LBA `1822` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: ACTIVITY1 `scratch/ps1-perf-iterate/20260428-213402/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-213625/summary.json`. | Promote. Even low-pressure FGP2 packs benefit from FGP3 conversion through fewer reads and less render/upload volume. The next step is to measure more pending scene rows or batch-convert only after each scene/tide has a baseline gate. |
| 2026-04-28 | `activity7-low-padded-fgp3-pack` | `ps1: convert activity7 low foreground to padded fgp3` | Extend the padded-FGP3 lane to low-tide variants, starting with ACTIVITY7 low because its high-tide counterpart had the largest measured percent gap and the low pack showed a `74.50%` conversion payload reduction. | Captured an FGP2 baseline for `fgpilot activity7 lowtide 1`, converted `generated/ps1/foreground/ACTV7LOW.FG2`, padded it back to `358545` bytes, ran against the fresh baseline with `--work-identity-min 0`, then cross-gated FISHING1 high. | Promoted. ACTIVITY7 low improves `loop_vb 754 -> 747`, `target_vb 615 -> 614`, `overrun_vb 139 -> 133`, `blocking_vb 4 -> 1`, `prefetch_overrun_vb 4 -> 1`, `loop_reads 27 -> 6`, and keeps `FG\\ACTV7LOW.FG2` at LBA `5950` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: baseline `scratch/ps1-perf-iterate/20260428-214036/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260428-214307/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-214529/summary.json`. | Promote. Low-tide variants follow the same conversion pattern; continue with ACTIVITY6/5/4/1 low rather than leaving half of a scene family on FGP2. |
| 2026-04-28 | `activity6-low-padded-fgp3-pack` | log-only after `activity7-low-padded-fgp3-pack` | Test whether ACTIVITY6 low can use the same padded-FGP3 conversion as ACTIVITY6 high. The offline conversion saved `74.50%` payload and the fresh FGP2 baseline still had `5` visible CD/refill VBlanks. | Captured an FGP2 baseline for `fgpilot activity6 lowtide 1`, converted `generated/ps1/foreground/ACTV6LOW.FG2`, padded it back to `388077` bytes, and ran against the fresh baseline with `--work-identity-min 0`. The FGP3 pack was restored after the run. | Failed/no promotion as a structural failure. The per-case summary showed the expected speed signal (`loop_vb 1054 -> 1043`, `overrun_vb 143 -> 132`, `blocking_vb 5 -> 0`, `prefetch_overrun_vb 5 -> 0`, `loop_reads 29 -> 6`) with correctness counters clean, but the headless run hit invalid-read log spam and exited `137`. Oversized logs were deleted after preserving the summary/read-plan. Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260428-214907/summary.json`, failed FGP3 per-case `scratch/ps1-perf-iterate/20260428-215135/activity6-low/perf-summary.json`. | Do not promote. The speed signal is real, but low-tide ACTIVITY6 needs the post-scene invalid-read/lifetime issue fixed before FGP3 is acceptable. Keep the FGP2 baseline in the matrix and retry after low-tide FGP3 cleanup ownership is understood. |
| 2026-04-28 | `activity5-low-padded-fgp3-pack` | `ps1: convert activity5 low foreground to padded fgp3` | Continue low-tide FGP3 conversion with ACTIVITY5 low, which has a large FGP2 pack and meaningful visible CD pressure. | Captured an FGP2 baseline for `fgpilot activity5 lowtide 1`, converted `generated/ps1/foreground/ACTV5LOW.FG2`, padded it back to `823522` bytes, ran against the fresh baseline with `--work-identity-min 0`, then cross-gated FISHING1 high. | Promoted. ACTIVITY5 low improves `loop_vb 1879 -> 1861`, `target_vb 1696 -> 1706`, `overrun_vb 183 -> 155`, `blocking_vb 47 -> 29`, `prefetch_overrun_vb 29 -> 16`, `due_misses 4 -> 3`, `loop_reads 62 -> 36`, and `loop_read_vb 298 -> 172`. Payload drops `818130 -> 480744`, restore bytes drop `2028970 -> 353918`, upload bytes drop `24435840 -> 13203840`, and `FG\\ACTV5LOW.FG2` stays at LBA `4851` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: baseline `scratch/ps1-perf-iterate/20260428-215502/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260428-215732/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-215957/summary.json`. | Promote. This is the strongest low-tide conversion so far and leaves ACTIVITY6 low as the only known low-tide structural miss in this family. |
| 2026-04-28 | `activity4-low-padded-fgp3-pack` | `ps1: convert activity4 low foreground to padded fgp3` | Continue low-tide padded-FGP3 conversion with ACTIVITY4 low, a smaller FGP2 pack that still had measurable CD pressure and a `41.07%` offline payload reduction. | Captured an FGP2 baseline for `fgpilot activity4 lowtide 1`, converted `generated/ps1/foreground/ACTV4LOW.FG2`, padded it back to `337542` bytes, ran against the fresh baseline with `--work-identity-min 0`, then cross-gated FISHING1 high. | Promoted. ACTIVITY4 low improves `loop_vb 1213 -> 1203`, `overrun_vb 144 -> 134`, `blocking_vb 2 -> 1`, `prefetch_overrun_vb 2 -> 1`, `loop_reads 24 -> 13`, and `loop_read_vb 111 -> 59`. Payload drops `334338 -> 197018`, restore bytes drop `967512 -> 116696`, upload bytes drop `9402240 -> 5064960`, and `FG\\ACTV4LOW.FG2` stays at LBA `3885` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: baseline `scratch/ps1-perf-iterate/20260428-220350/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260428-220621/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-220846/summary.json`. | Promote. The low-tide lane remains profitable even for smaller packs; continue with ACTIVITY1 low unless it shows structural cleanup issues like ACTIVITY6 low. |
| 2026-04-28 | `activity1-low-padded-fgp3-pack` | `ps1: convert activity1 low foreground to padded fgp3` | Convert the remaining ACTIVITY low-tide FGP2 candidate that was not blocked by structural cleanup issues. ACTIVITY1 low has a large FGP2 payload and only small visible CD pressure, matching the high-tide profile. | Captured an FGP2 baseline for `fgpilot activity1 lowtide 1`, converted `generated/ps1/foreground/ACTV1LOW.FG2`, padded it back to `1691566` bytes, ran against the fresh baseline with `--work-identity-min 0`, then cross-gated FISHING1 high. | Promoted. ACTIVITY1 low improves `loop_vb 4382 -> 4373`, `target_vb 4241 -> 4242`, `overrun_vb 141 -> 131`, `blocking_vb 3 -> 2`, `prefetch_overrun_vb 3 -> 2`, `loop_reads 137 -> 50`, and `loop_read_vb 586 -> 216`. Payload drops `1683850 -> 685922`, restore bytes drop `5802012 -> 542042`, upload bytes drop `29205120 -> 13253760`, and `FG\\ACTV1LOW.FG2` stays at LBA `2648` with PS-EXE `147456`. FISHING1 high stayed exact-flat. Artifacts: baseline `scratch/ps1-perf-iterate/20260428-221249/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260428-221522/summary.json`, FISHING1 `scratch/ps1-perf-iterate/20260428-221748/summary.json`. | Promote. ACTIVITY high/low conversion is now complete except for the blocked ACTIVITY6 low structural miss; next pack-format work should move to another family or first fix the low-tide FGP3 invalid-read lifetime issue. |
| 2026-04-28 | `activity9-high-padded-fgp3-pack` | log-only after `activity1-low-padded-fgp3-pack` | Stress-test the padded-FGP3 lane on the largest PAL4 ACTIVITY pack. ACTIVITY9 high is a `29.7 MB` FGP2 pack and required an `18000`-frame baseline window before emitting `JCPERF2`. | Captured an FGP2 baseline for `fgpilot activity9 lowtide 0`, converted `generated/ps1/foreground/ACTIVITY9.FG2`, padded it back to `29738739` bytes, and ran against the fresh baseline with `--work-identity-min 0`. The FGP3 pack was restored after the run and oversized logs were deleted after preserving the per-case summary/read-plan. | Failed/no promotion as a structural failure despite a huge speed signal. FGP2 baseline was `loop_vb=11819`, `target_vb=1868`, `blocking_vb=7301`, `prefetch_overrun_vb=52`, `due_misses=260`, and `loop_reads=271`. FGP3 improved the per-case summary to `loop_vb=2267`, `target_vb=2040`, `blocking_vb=92`, `prefetch_overrun_vb=61`, `due_misses=7`, and `loop_reads=145`, with payload `29732775 -> 1746749`, but the run hit invalid-read log spam and exited `137`. Artifacts: baseline `scratch/ps1-perf-iterate/20260428-222434/summary.json`, failed FGP3 per-case `scratch/ps1-perf-iterate/20260428-223058/activity9-high/perf-summary.json`. | Do not promote. This is the largest known pack-format opportunity, but it is blocked by the same post-scene invalid-read/lifetime class seen on ACTIVITY6 low. Fix that class before retrying ACTIVITY9 FGP3; the FGP2 baseline is now recorded in the matrix. |
| 2026-04-28 | `activity9-high-padded-fgp3-loop-validation` | `ps1: convert activity9 foreground to padded fgp3` | Re-test ACTIVITY9 high FGP3 under the real screensaver loop after the failed one-shot probe suggested a post-scene shutdown artifact rather than active playback corruption. | Reinstalled the padded FGP3 pack, ran `fgpilot activity9 ... loop` for `9000` frames, verified three complete scene-end summaries with zero correctness failures and no invalid-write spam, then cross-gated FISHING1 high for layout identity. | Promoted as a loop-validated pack-format win. ACTIVITY9 high improves from the FGP2 baseline `loop_vb=11819`, `blocking_vb=7301`, `prefetch_overrun_vb=52`, `due_misses=260`, and `loop_reads=271` to loop-validated FGP3 `loop_vb=2268`, `target_vb=2039`, `overrun_vb=229`, `blocking_vb=97`, `prefetch_overrun_vb=60`, `due_misses=9`, and `loop_reads=144`. Payload drops `29732775 -> 1746749`; the CD file remains `29738739` bytes and `FG\\ACTV9.FG2` stays at LBA `6532`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`); its gate failed only because `--require-improvement` was intentionally still set. Artifacts: loop validation `scratch/ps1-perf-iterate/20260428-225008/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260428-225403/summary.json`. | Promote. For ACTIVITY9 FGP3, use the real `loop` path for perf validation until the one-shot `noloop` shutdown/exit artifact is handled separately. This is now the largest accepted host-preprocessing win in the all-scene lane. |
| 2026-04-28 | `activity6-low-padded-fgp3-loop-validation` | `ps1: convert activity6 low foreground to padded fgp3` | Retry ACTIVITY6 low FGP3 using the real screensaver loop, applying the ACTIVITY9 finding that the one-shot shutdown path can produce log spam after otherwise clean playback. | Reinstalled the saved padded FGP3 pack for `generated/ps1/foreground/ACTV6LOW.FG2`, padded it back to `388077` bytes, ran `fgpilot activity6 lowtide 1 ... loop` against the prior FGP2 baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. ACTIVITY6 low improves `loop_vb 1054 -> 1043`, `overrun_vb 143 -> 132`, `blocking_vb 5 -> 0`, `prefetch_overrun_vb 5 -> 0`, `loop_reads 29 -> 6`, and `loop_read_vb 132 -> 31`. Payload drops `385869 -> 98412`; the CD file remains `388077` bytes and `FG\\ACTV6LOW.FG2` stays at LBA `5444`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: loop validation `scratch/ps1-perf-iterate/20260428-230011/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260428-230251/summary.json`. | Promote. The earlier ACTIVITY6 low rejection is reclassified as a one-shot shutdown artifact, not active playback corruption; keep using explicit `loop` validation for this class until the shutdown path is fixed. |
| 2026-04-28 | `activity9-low-padded-fgp3-loop-validation` | `ps1: convert activity9 low foreground to padded fgp3` | Continue the ACTIVITY9 host-preprocessing lane on the low-tide variant, which was still a `29.4 MB` FGP2 pack with very high CD blocking. | Captured a fresh FGP2 baseline for `fgpilot activity9 lowtide 1`, converted `generated/ps1/foreground/ACTV9LOW.FG2` to padded FGP3, ran the real `loop` path against the baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. ACTIVITY9 low improves `loop_vb 12101 -> 2271`, `overrun_vb 10229 -> 227`, `blocking_vb 7444 -> 92`, `prefetch_overrun_vb 59 -> 56`, `due_misses 250 -> 8`, `loop_reads 264 -> 134`, and `loop_read_vb 7491 -> 641`. Payload drops `29424855 -> 1666401`; the CD file remains `29430619` bytes and `FG\\ACTV9LOW.FG2` stays at LBA `21053`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260428-230708/summary.json`, loop validation `scratch/ps1-perf-iterate/20260428-231329/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260428-231634/summary.json`. | Promote. ACTIVITY9 high and low are now both padded-FGP3, proving the loop-validation path can safely promote large residual packs while preserving LBA layout. |
| 2026-04-28 | `building4-high-padded-fgp3-pack` | `ps1: convert building4 foreground to padded fgp3` | Move the padded-FGP3 lane beyond ACTIVITY scenes with BUILDING4 high, a `5.4 MB` PAL4 pack whose baseline showed heavy CD blocking and due misses. | Captured an FGP2 baseline for `fgpilot building4 lowtide 0`, converted `generated/ps1/foreground/BUILDING4.FG2` to padded FGP3, tested against the fresh baseline with `--work-identity-min 0`, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. BUILDING4 high improves `loop_vb 4094 -> 3141`, `target_vb 2704 -> 2792`, `overrun_vb 1390 -> 349`, `blocking_vb 1591 -> 326`, `prefetch_overrun_vb 50 -> 43`, `due_misses 274 -> 39`, `loop_reads 354 -> 78`, and `loop_read_vb 1986 -> 484`. Payload drops `5417481 -> 1139499`; the CD file remains `5425309` bytes and `FG\\BUIL4.FG2` stays at LBA `45522`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260428-232751/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260428-233151/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260428-233534/summary.json`. | Promote. This confirms padded FGP3 is profitable outside the ACTIVITY family; continue with large PAL4 BUILDING/JOHNNY/MARY candidates and handle indexed8 MARY3/WALKSTUF separately. |
| 2026-04-28 | `building4-low-padded-fgp3-pack` | `ps1: convert building4 low foreground to padded fgp3` | Complete BUILDING4 by applying the same padded-FGP3 conversion to the low-tide pack after the high-tide variant produced a clean win. | Captured an FGP2 baseline for `fgpilot building4 lowtide 1`, converted `generated/ps1/foreground/BUIL4LOW.FG2` to padded FGP3, tested against the fresh baseline with `--work-identity-min 0`, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. BUILDING4 low improves `loop_vb 4111 -> 3135`, `target_vb 2705 -> 2790`, `overrun_vb 1406 -> 345`, `blocking_vb 1572 -> 335`, `prefetch_overrun_vb 87 -> 46`, `due_misses 265 -> 40`, `loop_reads 354 -> 78`, and `loop_read_vb 1998 -> 490`. Payload drops `5417481 -> 1139499`; the CD file remains `5425309` bytes and `FG\\BUIL4LOW.FG2` stays at LBA `48172`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260428-233936/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260428-234330/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260428-234710/summary.json`. | Promote. BUILDING4 high/low now both use padded FGP3; continue with the next largest PAL4 scene family and keep indexed8/full-loop coverage gaps separate. |
| 2026-04-28 | `johnny1-high-padded-fgp3-pack` | `ps1: convert johnny1 foreground to padded fgp3` | Test padded FGP3 on JOHNNY1 high, a `7.75 MB` PAL4 pack with severe CD blocking from large repeated full-width frames. | Captured an FGP2 baseline for `fgpilot johnny1 lowtide 0`, converted `generated/ps1/foreground/JOHNNY1.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted by manual acceptance. JOHNNY1 high improves `loop_vb 4311 -> 2128`, `target_vb 1866 -> 1943`, `overrun_vb 2445 -> 185`, `blocking_vb 1976 -> 31`, `due_misses 106 -> 0`, `loop_reads 109 -> 16`, and `loop_read_vb 2066 -> 109`. Payload drops `7753614 -> 446058`; the CD file remains `7755926` bytes and `FG\\JOHNNY1.FG2` stays at LBA `63124`. The strict baseline gate failed only because `prefetch_overrun_vb` rose from `0 -> 31`; the higher-level loop/overrun/blocking metrics improved sharply and FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260428-235127/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260428-235452/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260428-235800/summary.json`. | Promote. The harness needs an override or composite-score mode for zero-baseline submetrics when all user-facing timing metrics improve; do not treat this as a rejected experiment. |
| 2026-04-29 | `johnny1-low-padded-fgp3-pack` | `ps1: convert johnny1 low foreground to padded fgp3` | Complete JOHNNY1 by applying the same padded-FGP3 conversion to the low-tide pack after the high-tide variant produced a large manual-accepted win. | Captured an FGP2 baseline for `fgpilot johnny1 lowtide 1`, converted `generated/ps1/foreground/JOHN1LOW.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted by manual acceptance. JOHNNY1 low improves `loop_vb 4314 -> 2132`, `target_vb 1866 -> 1942`, `overrun_vb 2448 -> 190`, `blocking_vb 1989 -> 37`, `due_misses 106 -> 0`, `loop_reads 109 -> 16`, and `loop_read_vb 2079 -> 114`. Payload drops `7753614 -> 446058`; the CD file remains `7755926` bytes and `FG\\JOHN1LOW.FG2` stays at LBA `66912`. The strict baseline gate failed only because `prefetch_overrun_vb` rose from `0 -> 37`; the higher-level loop/overrun/blocking metrics improved sharply and FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-000228/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-000546/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-000843/summary.json`. | Promote. JOHNNY1 high/low now both use padded FGP3; track the zero-baseline prefetch-overrun gate issue as harness policy debt rather than runtime rejection. |
| 2026-04-29 | `suzy1-high-baseline-coverage-gap` | log-only after `ps1: convert johnny1 low foreground to padded fgp3` | Probe SUZY1 high before attempting padded FGP3 because it is a `20.6 MB` PAL4 pack and nominally one of the largest remaining payload targets. | Ran `fgpilot suzy1 lowtide 0 ... perf-log noloop` for `18000` frames without modifying the pack. | Failed/no promotion as a coverage gap, not as a pack-format result. The pack loaded (`FG\\SUZY1.FG2`, `20634648` bytes, `fgp2_pal4`, `177` entries), but playback never entered the foreground loop: `loop_start=0`, `loop_vb=0`, `advances=0`, `last_frame=0`, `expected_frames=177`, and only the first non-empty payload was read during setup. Artifact: probe `scratch/ps1-perf-iterate/20260428-232055/summary.json`. | Do not convert SUZY until fgpilot/runtime coverage for SUZY scenes is understood. Keep it as a large future target, but do not compare FGP3 against a zero-loop baseline. |
| 2026-04-29 | `mary1-high-padded-fgp3-pack` | `ps1: convert mary1 foreground to padded fgp3` | Continue padded-FGP3 conversion on MARY1 high, a `6.58 MB` PAL4 pack with a long scene and heavy CD pressure. | Captured an FGP2 baseline for `fgpilot mary1 lowtide 0`, converted `generated/ps1/foreground/MARY1.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. MARY1 high improves `loop_vb 6070 -> 5028`, `target_vb 4651 -> 4813`, `overrun_vb 1419 -> 215`, `blocking_vb 2362 -> 87`, `prefetch_overrun_vb 169 -> 75`, `due_misses 487 -> 2`, `loop_reads 707 -> 119`, and `loop_read_vb 3272 -> 652`. Payload drops `6564891 -> 1600043`; the CD file remains `6581511` bytes and `FG\\MARY1.FG2` stays at LBA `77353`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-001436/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260429-001838/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-002222/summary.json`. | Promote. Large long-running PAL4 scenes benefit strongly, though MARY1 remains mostly target-bound after CD pressure is removed; continue with MARY1 low or the next largest PAL4 scene. |
| 2026-04-29 | `mary1-low-padded-fgp3-pack` | `ps1: convert mary1 low foreground to padded fgp3` | Complete MARY1 by applying padded FGP3 to the lower-pressure low-tide pack after the high-tide variant produced a clean win. | Captured an FGP2 baseline for `fgpilot mary1 lowtide 1`, converted `generated/ps1/foreground/MARY1LOW.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. MARY1 low improves `loop_vb 5087 -> 5011`, `target_vb 4789 -> 4830`, `overrun_vb 298 -> 181`, `blocking_vb 166 -> 50`, `prefetch_overrun_vb 126 -> 45`, `due_misses 10 -> 1`, `loop_reads 346 -> 89`, and `loop_read_vb 1791 -> 486`. Payload drops `3944246 -> 1164115`; the CD file remains `3958106` bytes and `FG\\MARY1LOW.FG2` stays at LBA `80567`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-002610/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260429-003011/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-003354/summary.json`. | Promote. This is a smaller local win because the FGP2 baseline was already near target, but the CD/read reduction is real and stacks with the all-scene conversion work. |
| 2026-04-29 | `building2-high-padded-fgp3-pack` | `ps1: convert building2 foreground to padded fgp3` | Continue the padded-FGP3 lane with BUILDING2 high, a `4.52 MB` PAL4 pack whose baseline was dominated by CD stalls. | Captured an FGP2 baseline for `fgpilot building2 lowtide 0`, converted `generated/ps1/foreground/BUILDING2.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. BUILDING2 high improves `loop_vb 3142 -> 1572`, `target_vb 1272 -> 1291`, `overrun_vb 1870 -> 281`, `blocking_vb 1562 -> 173`, `prefetch_overrun_vb 73 -> 66`, `due_misses 220 -> 20`, `loop_reads 262 -> 70`, and `loop_read_vb 1725 -> 363`. Payload drops `4514780 -> 862297`; the CD file remains `4521444` bytes and `FG\\BUIL2.FG2` stays at LBA `40278`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-004724/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260429-005142/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-005522/summary.json`. | Promote. This is another major CD-bound scene win; continue with BUILDING2 low to complete the family. |
| 2026-04-29 | `building2-low-padded-fgp3-pack` | `ps1: convert building2 low foreground to padded fgp3` | Complete BUILDING2 by applying the same padded-FGP3 conversion to the low-tide pack after the high-tide variant produced a large clean win. | Captured an FGP2 baseline for `fgpilot building2 lowtide 1`, converted `generated/ps1/foreground/BUIL2LOW.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. BUILDING2 low improves `loop_vb 3219 -> 1570`, `target_vb 1273 -> 1293`, `overrun_vb 1946 -> 277`, `blocking_vb 1592 -> 172`, `prefetch_overrun_vb 68 -> 62`, `due_misses 214 -> 20`, `loop_reads 262 -> 69`, and `loop_read_vb 1789 -> 358`. Payload drops `4514780 -> 862297`; the CD file remains `4521444` bytes and `FG\\BUIL2LOW.FG2` stays at LBA `42486`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-005920/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260429-010317/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-010659/summary.json`. | Promote. BUILDING2 high/low now both use padded FGP3; continue with the next largest PAL4 family, likely BUILDING6 or JOHNNY6. |
| 2026-04-29 | `building6-high-padded-fgp3-pack` | `ps1: convert building6 foreground to padded fgp3` | Continue the padded-FGP3 lane with BUILDING6 high, a `4.40 MB` PAL4 pack with measurable CD stalls. | Captured an FGP2 baseline for `fgpilot building6 lowtide 0`, converted `generated/ps1/foreground/BUILDING6.FG2` to padded FGP3, tested against the fresh baseline, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted. BUILDING6 high improves `loop_vb 3414 -> 2754`, `target_vb 2376 -> 2435`, `overrun_vb 1038 -> 319`, `blocking_vb 1111 -> 317`, `prefetch_overrun_vb 60 -> 43`, `due_misses 170 -> 39`, `loop_reads 254 -> 72`, and `loop_read_vb 1520 -> 451`. Payload drops `4391520 -> 1052803`; the CD file remains `4397312` bytes and `FG\\BUIL6.FG2` stays at LBA `53292`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-011113/summary.json`, accepted FGP3 `scratch/ps1-perf-iterate/20260429-011517/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-011858/summary.json`. | Promote. Continue with BUILDING6 low to complete the family. |
| 2026-04-29 | `building6-low-padded-fgp3-loop-validation` | `ps1: convert building6 low foreground to padded fgp3` | Complete BUILDING6 by applying padded FGP3 to the low-tide pack, using the explicit loop-validation path if the one-shot runner repeats the known post-scene invalid-read artifact. | Captured an FGP2 baseline for `fgpilot building6 lowtide 1`, converted `generated/ps1/foreground/BUIL6LOW.FG2` to padded FGP3, observed the one-shot run emit good scene metrics then hit invalid-read log spam at `0xFFFFBF80`, re-ran the same artifact with explicit `loop`, then cross-gated FISHING1 high with a strict no-regression gate. | Promoted via loop validation. BUILDING6 low improves `loop_vb 3474 -> 2746`, `target_vb 2370 -> 2433`, `overrun_vb 1104 -> 313`, `blocking_vb 1175 -> 312`, `prefetch_overrun_vb 88 -> 55`, `due_misses 169 -> 38`, `loop_reads 254 -> 72`, and `loop_read_vb 1578 -> 445`. Payload drops `4391520 -> 1052803`; the CD file remains `4397312` bytes and `FG\\BUIL6LOW.FG2` stays at LBA `55440`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-012343/summary.json`, loop validation `scratch/ps1-perf-iterate/20260429-012952/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-013340/summary.json`; failed one-shot log `scratch/ps1-perf-iterate/20260429-012746/building6-low/headless-regtest.log`. | Promote. BUILDING6 high/low now both use padded FGP3; keep validating low-tide packs with `loop` when one-shot shutdown artifacts appear. |
| 2026-04-29 | `johnny6-high-padded-fgp3-loop-validation` | `ps1: convert johnny6 foreground to padded fgp3` | Continue padded-FGP3 conversion with JOHNNY6 high, a `4.31 MB` PAL4 pack with a short frame table and heavy CD blocking. | The one-shot FGP2 probe emitted valid scene metrics then hit the known `0xFFFFBF80` invalid-read shutdown spam, so the accepted baseline used explicit `loop`. Converted `generated/ps1/foreground/JOHNNY6.FG2` to padded FGP3, validated the same loop case against the baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted by manual acceptance. JOHNNY6 high improves `loop_vb 3680 -> 2901`, `target_vb 2755 -> 2799`, `overrun_vb 925 -> 102`, `blocking_vb 873 -> 33`, `due_misses 69 -> 0`, `loop_reads 79 -> 12`, and `loop_read_vb 1140 -> 90`. Payload drops `4309554 -> 400778`; the CD file remains `4311274` bytes and `FG\\JOHNNY6.FG2` stays at LBA `73141`. The generic gate failed only on the small local tradeoff `prefetch_overrun_vb 27 -> 33`; the aggregate timing win is large and FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-014336/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-014620/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-014852/summary.json`; failed one-shot probe `scratch/ps1-perf-iterate/20260429-014110/johnny6-high/headless-regtest.log`. | Promote. Treat zero/low-baseline prefetch-overrun submetric bumps as manual-review policy debt when loop/overrun/blocking all improve materially; continue with JOHNNY6 low. |
| 2026-04-29 | `johnny6-low-padded-fgp3-loop-validation` | `ps1: convert johnny6 low foreground to padded fgp3` | Complete JOHNNY6 by applying padded FGP3 to the matching low-tide pack using the explicit-loop path established by high tide. | Captured an FGP2 loop baseline for `fgpilot johnny6 lowtide 1`, converted `generated/ps1/foreground/JOHN6LOW.FG2` to padded FGP3, validated the same loop case against the baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted by manual acceptance. JOHNNY6 low improves `loop_vb 3675 -> 2905`, `target_vb 2755 -> 2798`, `overrun_vb 920 -> 107`, `blocking_vb 865 -> 37`, `due_misses 69 -> 0`, `loop_reads 79 -> 12`, and `loop_read_vb 1124 -> 94`. Payload drops `4309554 -> 400778`; the CD file remains `4311274` bytes and `FG\\JOHN6LOW.FG2` stays at LBA `75247`. The generic gate failed only on `prefetch_overrun_vb 26 -> 37`; the aggregate timing win is large and FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-015304/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-015545/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-015809/summary.json`. | Promote. JOHNNY6 high/low now both use padded FGP3; continue the large PAL4 lane with BUILDING7 or BUILDING5, and preserve the manual-review note for small prefetch-overrun tradeoffs. |
| 2026-04-29 | `building7-high-padded-fgp3-loop-validation` | `ps1: convert building7 foreground to padded fgp3` | Continue the building-family padded-FGP3 lane with BUILDING7 high, a `3.54 MB` PAL4 pack with a narrow high-tide union and measurable CD stalls. | Captured an FGP2 loop baseline for `fgpilot building7 lowtide 0`, converted `generated/ps1/foreground/BUILDING7.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. BUILDING7 high improves `loop_vb 4067 -> 3861`, `target_vb 3594 -> 3659`, `overrun_vb 473 -> 202`, `blocking_vb 383 -> 66`, `prefetch_overrun_vb 159 -> 47`, `due_misses 47 -> 3`, `loop_reads 284 -> 77`, and `loop_read_vb 1469 -> 424`. Payload drops `3530087 -> 1047635`; the CD file remains `3544407` bytes and `FG\\BUIL7.FG2` stays at LBA `57588`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-020216/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-020456/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-020726/summary.json`. | Promote. BUILDING7 high is now in the padded-FGP3 lane; continue with BUILDING7 low to complete the family. |
| 2026-04-29 | `building7-low-padded-fgp3-loop-validation` | `ps1: convert building7 low foreground to padded fgp3` | Complete BUILDING7 by applying padded FGP3 to the smaller low-tide pack after high tide passed cleanly. | Captured an FGP2 loop baseline for `fgpilot building7 lowtide 1`, converted `generated/ps1/foreground/BUIL7LOW.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. BUILDING7 low improves `loop_vb 3882 -> 3837`, `target_vb 3653 -> 3672`, `overrun_vb 229 -> 165`, `blocking_vb 139 -> 23`, `prefetch_overrun_vb 71 -> 23`, `due_misses 13 -> 0`, `loop_reads 128 -> 50`, and `loop_read_vb 661 -> 260`. Payload drops `1735855 -> 723937`; the CD file remains `1748515` bytes and `FG\\BUIL7LOW.FG2` stays at LBA `59319`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-021118/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-021358/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-021625/summary.json`. | Promote. BUILDING7 high/low now both use padded FGP3; continue the large PAL4 lane with BUILDING5 high/low. |
| 2026-04-29 | `building5-high-padded-fgp3-loop-validation` | `ps1: convert building5 foreground to padded fgp3` | Continue the building-family padded-FGP3 lane with BUILDING5 high, a `3.34 MB` PAL4 pack with the same narrow high-tide union pattern as BUILDING7. | Captured an FGP2 loop baseline for `fgpilot building5 lowtide 0`, converted `generated/ps1/foreground/BUILDING5.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. BUILDING5 high improves `loop_vb 3653 -> 3514`, `target_vb 3275 -> 3334`, `overrun_vb 378 -> 180`, `blocking_vb 339 -> 65`, `prefetch_overrun_vb 161 -> 36`, `due_misses 38 -> 6`, `loop_reads 276 -> 69`, and `loop_read_vb 1397 -> 341`. Payload drops `3329888 -> 966157`; the CD file remains `3343300` bytes and `FG\\BUIL5.FG2` stays at LBA `50822`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-022022/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-022318/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-022549/summary.json`. | Promote. BUILDING5 high is now in the padded-FGP3 lane; continue with BUILDING5 low to complete the family. |
| 2026-04-29 | `building5-low-padded-fgp3-loop-validation` | `ps1: convert building5 low foreground to padded fgp3` | Complete BUILDING5 by applying padded FGP3 to the smaller low-tide pack after high tide passed cleanly. | Captured an FGP2 loop baseline for `fgpilot building5 lowtide 1`, converted `generated/ps1/foreground/BUIL5LOW.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. BUILDING5 low improves `loop_vb 3534 -> 3508`, `target_vb 3316 -> 3346`, `overrun_vb 218 -> 162`, `blocking_vb 79 -> 33`, `prefetch_overrun_vb 67 -> 23`, `loop_reads 124 -> 47`, and `loop_read_vb 662 -> 240`. Payload drops `1701266 -> 671445`; the CD file remains `1713518` bytes and `FG\\BUIL5LOW.FG2` stays at LBA `52455`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-023005/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-023245/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-023513/summary.json`. | Promote. BUILDING5 high/low now both use padded FGP3; continue with VISITOR3 or the next large PAL4 pending row. |
| 2026-04-29 | `visitor3-high-padded-fgp3-loop-validation` | `ps1: convert visitor3 foreground to padded fgp3` | Test padded FGP3 outside the building-family shape with VISITOR3 high, a `2.96 MB` PAL4 pack with a taller full-width active region and heavy CD blocking. | Captured an FGP2 loop baseline for `fgpilot visitor3 lowtide 0`, converted `generated/ps1/foreground/VISITOR3.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. VISITOR3 high improves `loop_vb 1864 -> 1581`, `target_vb 987 -> 1013`, `overrun_vb 877 -> 568`, `blocking_vb 743 -> 424`, `prefetch_overrun_vb 192 -> 145`, `due_misses 43 -> 24`, `loop_reads 122 -> 73`, and `loop_read_vb 1056 -> 602`. Payload drops `2957520 -> 1529778`; the CD file remains `2961424` bytes and `FG\\VISITOR3.FG2` stays at LBA `161093`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-023925/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-024208/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-024434/summary.json`. | Promote. VISITOR3 high proves padded FGP3 still pays off on taller full-width PAL4 scenes, though it leaves significant CD pressure; continue with VISITOR3 low and then consider read-plan/prefetch follow-up for this family. |
| 2026-04-29 | `visitor3-low-padded-fgp3-loop-validation` | `ps1: convert visitor3 low foreground to padded fgp3` | Complete VISITOR3 by applying padded FGP3 to the low-tide pack and checking whether the tall full-width PAL4 pattern repeats. | Captured an FGP2 loop baseline for `fgpilot visitor3 lowtide 1`, converted `generated/ps1/foreground/VIST3LOW.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. VISITOR3 low improves `loop_vb 1997 -> 1611`, `target_vb 1009 -> 1017`, `overrun_vb 988 -> 594`, `blocking_vb 713 -> 404`, `prefetch_overrun_vb 145 -> 99`, `due_misses 27 -> 26`, `loop_reads 77 -> 63`, and `loop_read_vb 919 -> 552`. Payload drops `2935011 -> 1452700`; the CD file remains `2938815` bytes and `FG\\VIST3LOW.FG2` stays at LBA `162540`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-024835/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-025112/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-025337/summary.json`. | Promote. VISITOR3 high/low now both use padded FGP3, but both remain CD-bound after conversion; add a follow-up class for pack-emitted read grouping or scene-specific prefetch windows on tall full-width PAL4 packs. |
| 2026-04-29 | `suzy2-high-baseline-coverage-gap` | log-only after `ps1: convert visitor3 low foreground to padded fgp3` | Probe SUZY2 high before attempting padded FGP3 because it is a `13.7 MB` PAL4 pack and nominally one of the largest remaining payload targets. | Ran `fgpilot suzy2 lowtide 0 ... loop` for `18000` frames without modifying the pack. | Failed/no promotion as a coverage gap, not as a pack-format result. The pack loaded (`FG\\SUZY2.FG2`, `13706356` bytes, `fgp2_pal4`, `133` entries), but playback never entered the foreground loop: `loop_start=0`, `loop_vb=0`, `advances=0`, `render=0`, `last_frame=0`, `expected_frames=133`, and only setup CD reads occurred (`loop_reads=0`). Artifact: probe `scratch/ps1-perf-iterate/20260429-025754/summary.json`. | Do not convert SUZY2 high until fgpilot/runtime coverage for SUZY scenes is understood. This matches the earlier SUZY1 high coverage gap class and should be fixed before using SUZY packs in the padded-FGP3 lane. |
| 2026-04-29 | `walkstuf3-high-padded-fgp3-loop-validation` | `ps1: convert walkstuf3 foreground to padded fgp3` | Test padded FGP3 on WALKSTUF3 high, a `2.13 MB` PAL4 walking-family pack with moderate CD pressure. | Captured an FGP2 loop baseline for `fgpilot walkstuf3 lowtide 0`, converted `generated/ps1/foreground/WALKSTUF3.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. WALKSTUF3 high improves `loop_vb 2704 -> 2512`, `target_vb 2237 -> 2275`, `overrun_vb 467 -> 237`, `blocking_vb 345 -> 130`, `prefetch_overrun_vb 212 -> 90`, `due_misses 19 -> 6`, `loop_reads 166 -> 74`, and `loop_read_vb 1007 -> 475`. Payload drops `2116871 -> 1017926`; the CD file remains `2125867` bytes and `FG\\WALK3.FG2` stays at LBA `172729`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-030502/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-030807/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-031109/summary.json`. | Promote. WALKSTUF3 high confirms padded FGP3 still pays off on PAL4 walking scenes; continue with WALKSTUF3 low or the next large v1 PAL4 candidate. |
| 2026-04-29 | `visitor5-high-padded-fgp3-loop-validation` | `ps1: convert visitor5 foreground to padded fgp3` | Test padded FGP3 on VISITOR5 high, a `2.05 MB` PAL4 pack whose baseline is dominated by CD blocking. | Captured an FGP2 loop baseline for `fgpilot visitor5 lowtide 0`, converted `generated/ps1/foreground/VISITOR5.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted by manual acceptance. VISITOR5 high improves `loop_vb 2140 -> 1295`, `target_vb 1026 -> 1079`, `overrun_vb 1114 -> 216`, `blocking_vb 1546 -> 107`, `due_misses 215 -> 9`, `loop_reads 223 -> 43`, and `loop_read_vb 1592 -> 262`. Payload drops `2041304 -> 576996`; the CD file remains `2045968` bytes and `FG\\VISITOR5.FG2` stays at LBA `164212`. The generic gate failed only on `prefetch_overrun_vb 11 -> 52`; the aggregate timing and blocking win is large and FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-031520/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-031833/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-032128/summary.json`. | Promote. Keep using manual-review policy for small prefetch-overrun regressions when loop/overrun/blocking all improve materially; continue with VISITOR5 low or the next large v1 PAL4 candidate. |
| 2026-04-29 | `activity10-low-padded-fgp3-loop-validation` | `ps1: convert activity10 low foreground to padded fgp3` | Continue the padded-FGP3 lane on ACTIVITY10 low, a `1.87 MB` PAL4 activity pack with heavy CD pressure. | Captured an FGP2 loop baseline for `fgpilot activity10 lowtide 1`, converted `generated/ps1/foreground/ACTV10L.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. ACTIVITY10 low improves `loop_vb 1702 -> 1408`, `target_vb 1217 -> 1255`, `overrun_vb 485 -> 153`, `blocking_vb 495 -> 23`, `prefetch_overrun_vb 43 -> 14`, `due_misses 118 -> 2`, `loop_reads 186 -> 22`, and `loop_read_vb 781 -> 115`. Payload drops `1866208 -> 297646`; the CD file remains `1871440` bytes and `FG\\ACTV10L.FG2` stays at LBA `36202`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-032530/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-032840/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-033139/summary.json`. | Promote. ACTIVITY10 low now uses padded FGP3; continue with ACTIVITY10 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `activity12-low-padded-fgp3-loop-validation` | `ps1: convert activity12 low foreground to padded fgp3` | Continue the padded-FGP3 lane on ACTIVITY12 low, another large PAL4 activity pack with heavy CD pressure. | Captured an FGP2 loop baseline for `fgpilot activity12 lowtide 1`, converted `generated/ps1/foreground/ACTV12L.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. ACTIVITY12 low improves `loop_vb 1749 -> 1551`, `target_vb 1366 -> 1408`, `overrun_vb 383 -> 143`, `blocking_vb 433 -> 20`, `prefetch_overrun_vb 62 -> 14`, `due_misses 98 -> 1`, `loop_reads 184 -> 25`, and `loop_read_vb 780 -> 140`. Payload drops `1860209 -> 358097`; the CD file remains `1865361` bytes and `FG\\ACTV12L.FG2` stays at LBA `38620`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-033607/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-033920/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-034220/summary.json`. | Promote. ACTIVITY12 low now uses padded FGP3; continue with ACTIVITY12 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `mary5-high-padded-fgp3-loop-validation` | `ps1: convert mary5 foreground to padded fgp3` | Continue the padded-FGP3 lane on MARY5 high, a `1.84 MB` PAL4 pack with moderate CD pressure. | Captured an FGP2 loop baseline for `fgpilot mary5 lowtide 0`, converted `generated/ps1/foreground/MARY5.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. MARY5 high improves `loop_vb 1759 -> 1698`, `target_vb 1544 -> 1581`, `overrun_vb 215 -> 117`, `blocking_vb 119 -> 20`, `prefetch_overrun_vb 99 -> 20`, `due_misses 4 -> 0`, `loop_reads 116 -> 37`, and `loop_read_vb 718 -> 211`. Payload drops `1832429 -> 495145`; the CD file remains `1835521` bytes and `FG\\MARY5.FG2` stays at LBA `118121`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-034659/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-035010/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-035310/summary.json`. | Promote. MARY5 high now uses padded FGP3; continue with MARY5 low or the next large v1 PAL4 pending row. |
| 2026-04-29 | `mary5-low-padded-fgp3-loop-validation` | `ps1: convert mary5 low foreground to padded fgp3` | Complete MARY5 with the low-tide pack after high tide passed cleanly. | Captured an FGP2 loop baseline for `fgpilot mary5 lowtide 1`, converted `generated/ps1/foreground/MARY5LOW.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. MARY5 low improves `loop_vb 1768 -> 1694`, `target_vb 1546 -> 1583`, `overrun_vb 222 -> 111`, `blocking_vb 139 -> 14`, `prefetch_overrun_vb 106 -> 14`, `due_misses 6 -> 0`, `loop_reads 116 -> 37`, and `loop_read_vb 693 -> 230`. Payload drops `1832429 -> 495145`; the CD file remains `1835521` bytes and `FG\\MARY5LOW.FG2` stays at LBA `119018`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-035723/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-040029/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-040327/summary.json`. | Promote. MARY5 high/low now both use padded FGP3; continue with the next large v1 PAL4 pending row. |
| 2026-04-29 | `fishing8-high-padded-fgp3-loop-validation` | `ps1: convert fishing8 foreground to padded fgp3` | Continue the padded-FGP3 lane on FISHING8 high, a `1.83 MB` PAL4 fishing-family pack with heavy CD blocking and no setup-prime coverage. | Captured a clean explicit-loop FGP2 baseline for `fgpilot fishing8 lowtide 0`, converted `generated/ps1/foreground/FISHING8.FG2` to padded FGP3, validated against the fresh baseline, then cross-gated FISHING1 high with strict no-regression. | Promoted. FISHING8 high improves `loop_vb 1691 -> 1400`, `target_vb 1203 -> 1245`, `overrun_vb 488 -> 155`, `blocking_vb 756 -> 21`, `prefetch_overrun_vb 33 -> 21`, `due_misses 139 -> 0`, `loop_reads 200 -> 35`, and `loop_read_vb 1004 -> 185`. Payload drops `1825136 -> 477210`; the CD file remains `1829692` bytes and `FG\\FISHING8.FG2` stays at LBA `61940`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-041405/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-041949/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-042515/summary.json`. | Promote. FISHING8 high now uses padded FGP3; continue with MARY2 high/low or the next large v1 PAL4 pending row. |
| 2026-04-29 | `mary2-high-padded-fgp3-loop-validation` | `ps1: convert mary2 foreground to padded fgp3` | Continue the padded-FGP3 lane on MARY2 high, a `1.70 MB` PAL4 pack with visible CD blocking and a matching low-tide variant queued behind it. | Captured a clean explicit-loop FGP2 baseline for `fgpilot mary2 lowtide 0`, converted `generated/ps1/foreground/MARY2.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled because residual cleanup intentionally changes restore-call counts, then cross-gated FISHING1 high with strict no-regression. | Promoted. MARY2 high improves `loop_vb 2399 -> 2286`, `target_vb 2226 -> 2248`, `overrun_vb 173 -> 38`, `blocking_vb 248 -> 8`, `prefetch_overrun_vb 23 -> 8`, `due_misses 50 -> 0`, `loop_reads 144 -> 32`, and `loop_read_vb 705 -> 159`. Payload drops `1692166 -> 457249`; the CD file remains `1696554` bytes and `FG\\MARY2.FG2` stays at LBA `82500`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-042904/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-044025/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-044549/summary.json`. | Promote. MARY2 high now uses padded FGP3; continue with MARY2 low to complete the pair. |
| 2026-04-29 | `mary2-low-padded-fgp3-loop-validation` | `ps1: convert mary2 low foreground to padded fgp3` | Complete MARY2 by applying the same padded-FGP3 conversion to the low-tide pack after high tide passed cleanly. | Captured a clean explicit-loop FGP2 baseline for `fgpilot mary2 lowtide 1`, converted `generated/ps1/foreground/MARY2LOW.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. MARY2 low improves `loop_vb 2410 -> 2286`, `target_vb 2221 -> 2248`, `overrun_vb 189 -> 38`, `blocking_vb 254 -> 8`, `prefetch_overrun_vb 38 -> 8`, `due_misses 48 -> 0`, `loop_reads 144 -> 32`, and `loop_read_vb 724 -> 160`. Payload drops `1692166 -> 457249`; the CD file remains `1696554` bytes and `FG\\MARY2LOW.FG2` stays at LBA `83329`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-044954/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-045527/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-050051/summary.json`. | Promote. MARY2 high/low now both use padded FGP3; continue with ACTIVITY10 high, ACTIVITY12 high, or the next large v1 PAL4 pending row. |
| 2026-04-29 | `activity10-high-padded-fgp3-loop-validation` | `ps1: convert activity10 foreground to padded fgp3` | Complete ACTIVITY10 by applying padded FGP3 to the high-tide pack after the low-tide variant had already produced a clean large win. | Captured a clean explicit-loop FGP2 baseline for `fgpilot activity10 lowtide 0`, converted `generated/ps1/foreground/ACTIVITY10.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. ACTIVITY10 high improves `loop_vb 1543 -> 1408`, `target_vb 1236 -> 1254`, `overrun_vb 307 -> 154`, `blocking_vb 237 -> 18`, `prefetch_overrun_vb 43 -> 13`, `due_misses 39 -> 1`, `loop_reads 127 -> 19`, and `loop_read_vb 624 -> 95`. Payload drops `1588080 -> 275651`; the CD file remains `1593312` bytes and `FG\\ACTV10.FG2` stays at LBA `35424`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-050451/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-051031/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-051555/summary.json`. | Promote. ACTIVITY10 high/low now both use padded FGP3; continue with ACTIVITY12 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `activity12-high-padded-fgp3-loop-validation` | `ps1: convert activity12 foreground to padded fgp3` | Complete ACTIVITY12 by applying padded FGP3 to the high-tide pack after the low-tide variant had already produced a clean large win. | Captured a clean explicit-loop FGP2 baseline for `fgpilot activity12 lowtide 0`, converted `generated/ps1/foreground/ACTIVITY12.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. ACTIVITY12 high improves `loop_vb 1611 -> 1551`, `target_vb 1382 -> 1411`, `overrun_vb 229 -> 140`, `blocking_vb 193 -> 12`, `prefetch_overrun_vb 44 -> 12`, `due_misses 33 -> 0`, `loop_reads 132 -> 23`, and `loop_read_vb 621 -> 119`. Payload drops `1571016 -> 333162`; the CD file remains `1576168` bytes and `FG\\ACTV12.FG2` stays at LBA `37850`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-052002/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-052538/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-053106/summary.json`. | Promote. ACTIVITY12 high/low now both use padded FGP3; continue with JOHNNY3 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `johnny3-high-padded-fgp3-loop-validation` | `ps1: convert johnny3 foreground to padded fgp3` | Continue the padded-FGP3 lane on JOHNNY3 high, a `1.57 MB` PAL4 pack with heavy CD blocking and no setup-prime coverage. | Captured a clean explicit-loop FGP2 baseline for `fgpilot johnny3 lowtide 0`, converted `generated/ps1/foreground/JOHNNY3.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. JOHNNY3 high improves `loop_vb 1425 -> 1308`, `target_vb 1130 -> 1160`, `overrun_vb 295 -> 148`, `blocking_vb 247 -> 20`, `prefetch_overrun_vb 94 -> 16`, `due_misses 28 -> 1`, `loop_reads 103 -> 23`, and `loop_read_vb 565 -> 122`. Payload drops `1568749 -> 317406`; the CD file remains `1573029` bytes and `FG\\JOHNNY3.FG2` stays at LBA `71310`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-053625/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-054222/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-054755/summary.json`. | Promote. JOHNNY3 high now uses padded FGP3; continue with VISITOR6 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `visitor6-high-padded-fgp3-loop-validation` | `ps1: convert visitor6 foreground to padded fgp3` | Continue the padded-FGP3 lane on VISITOR6 high, a `1.35 MB` PAL4 pack that was already prefetch-complete but still spent visible VBlanks in CD/refill blocking. | Captured a clean explicit-loop FGP2 baseline for `fgpilot visitor6 lowtide 0`, converted `generated/ps1/foreground/VISITOR6.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. VISITOR6 high improves `loop_vb 2281 -> 2198`, `target_vb 2013 -> 2044`, `overrun_vb 268 -> 154`, `blocking_vb 119 -> 14`, `prefetch_overrun_vb 119 -> 14`, `due_misses 0 -> 0`, `loop_reads 105 -> 32`, and `loop_read_vb 656 -> 164`. Payload drops `1345696 -> 452898`; the CD file remains `1351348` bytes and `FG\\VISITOR6.FG2` stays at LBA `165588`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-055246/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-055839/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-060404/summary.json`. | Promote. VISITOR6 high now uses padded FGP3; continue with STAND12 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `stand12-high-padded-fgp3-loop-validation` | `ps1: convert stand12 foreground to padded fgp3` | Continue the padded-FGP3 lane on STAND12 high, a `1.18 MB` PAL4 pack with moderate CD/refill pressure. | Captured a clean explicit-loop FGP2 baseline for `fgpilot stand12 lowtide 0`, converted `generated/ps1/foreground/STAND12.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND12 high improves `loop_vb 1624 -> 1597`, `target_vb 1431 -> 1456`, `overrun_vb 193 -> 141`, `blocking_vb 56 -> 8`, `prefetch_overrun_vb 56 -> 8`, `due_misses 0 -> 0`, `loop_reads 102 -> 18`, and `loop_read_vb 550 -> 99`. Payload drops `1173091 -> 270953`; the CD file remains `1177923` bytes and `FG\\STAND12.FG2` stays at LBA `125057`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-060811/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-061345/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-061909/summary.json`. | Promote. STAND12 high now uses padded FGP3; continue with BUILDING3 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `building3-high-padded-fgp3-loop-validation` | `ps1: convert building3 foreground to padded fgp3` | Continue the padded-FGP3 lane on BUILDING3 high, a `1.16 MB` PAL4 pack with modest but measurable CD/refill pressure. | Captured a clean explicit-loop FGP2 baseline for `fgpilot building3 lowtide 0`, converted `generated/ps1/foreground/BUILDING3.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. BUILDING3 high improves `loop_vb 1583 -> 1565`, `target_vb 1420 -> 1431`, `overrun_vb 163 -> 134`, `blocking_vb 29 -> 4`, `prefetch_overrun_vb 22 -> 4`, `due_misses 1 -> 0`, `loop_reads 88 -> 27`, and `loop_read_vb 451 -> 135`. Payload drops `1152474 -> 386802`; the CD file remains `1156938` bytes and `FG\\BUIL3.FG2` stays at LBA `44694`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-062315/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-062900/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-063431/summary.json`. | Promote. BUILDING3 high now uses padded FGP3; continue with STAND5 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `stand5-high-padded-fgp3-loop-validation` | `ps1: convert stand5 foreground to padded fgp3` | Continue the padded-FGP3 lane on STAND5 high, a `1.12 MB` PAL4 pack with moderate CD/refill pressure. | Captured a clean explicit-loop FGP2 baseline for `fgpilot stand5 lowtide 0`, converted `generated/ps1/foreground/STAND5.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND5 high improves `loop_vb 1635 -> 1600`, `target_vb 1432 -> 1459`, `overrun_vb 203 -> 141`, `blocking_vb 65 -> 8`, `prefetch_overrun_vb 65 -> 8`, `due_misses 0 -> 0`, `loop_reads 79 -> 18`, and `loop_read_vb 442 -> 96`. Payload drops `1112379 -> 264280`; the CD file remains `1117211` bytes and `FG\\STAND5.FG2` stays at LBA `122163`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-063851/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-064433/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-065002/summary.json`. | Promote. STAND5 high now uses padded FGP3; continue with STAND6 high or the next large v1 PAL4 pending row. |
| 2026-04-29 | `stand6-high-padded-fgp3-loop-validation` | `ps1: convert stand6 foreground to padded fgp3` | Continue the padded-FGP3 lane on STAND6 high, a `1.07 MB` PAL4 stand-family pack with remaining CD/refill blocking. | Captured a clean explicit-loop FGP2 baseline for `fgpilot stand6 lowtide 0`, converted `generated/ps1/foreground/STAND6.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND6 high improves `loop_vb 1535 -> 1503`, `target_vb 1336 -> 1360`, `overrun_vb 199 -> 143`, `blocking_vb 59 -> 8`, `prefetch_overrun_vb 59 -> 8`, `due_misses 0 -> 0`, `loop_reads 79 -> 18`, and `loop_read_vb 451 -> 107`. Payload drops `1063672 -> 264261`; the CD file remains `1068204` bytes and `FG\\STAND6.FG2` stays at LBA `122973`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-065516/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-070048/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-070625/summary.json`. | Promote. STAND6 high now uses padded FGP3; continue with VISITOR7 high, STAND4 high, or the next large v1 PAL4 pending row. |
| 2026-04-29 | `visitor7-high-padded-fgp3-loop-validation` | `ps1: convert visitor7 foreground to padded fgp3` | Continue the padded-FGP3 lane on VISITOR7 high, a `1.04 MB` PAL4 visitor-family pack with stronger CD/refill pressure than the recent stand packs. | Captured a clean explicit-loop FGP2 baseline for `fgpilot visitor7 lowtide 0`, converted `generated/ps1/foreground/VISITOR7.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. VISITOR7 high improves `loop_vb 1837 -> 1777`, `target_vb 1605 -> 1621`, `overrun_vb 232 -> 156`, `blocking_vb 83 -> 15`, `prefetch_overrun_vb 83 -> 15`, `due_misses 0 -> 0`, `loop_reads 79 -> 26`, and `loop_read_vb 473 -> 140`. Payload drops `1034782 -> 367617`; the CD file remains `1039246` bytes and `FG\\VISITOR7.FG2` stays at LBA `166535`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-071102/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-071636/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-072200/summary.json`. | Promote. VISITOR7 high now uses padded FGP3; continue with STAND4 high, BUILDING1 high, or the next large v1 PAL4 pending row. |
| 2026-04-29 | `stand4-high-padded-fgp3-loop-validation` | `ps1: convert stand4 foreground to padded fgp3` | Continue the padded-FGP3 lane on STAND4 high, a just-under-`1 MB` PAL4 stand-family pack with broad foreground read pressure. | Captured a clean explicit-loop FGP2 baseline for `fgpilot stand4 lowtide 0`, converted `generated/ps1/foreground/STAND4.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND4 high improves `loop_vb 1405 -> 1365`, `target_vb 1191 -> 1216`, `overrun_vb 214 -> 149`, `blocking_vb 71 -> 12`, `prefetch_overrun_vb 71 -> 12`, `due_misses 0 -> 0`, `loop_reads 89 -> 16`, and `loop_read_vb 503 -> 83`. Payload drops `994023 -> 241352`; the CD file remains `998095` bytes and `FG\\STAND4.FG2` stays at LBA `121433`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-072559/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-073130/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-073656/summary.json`. | Promote. STAND4 high now uses padded FGP3; continue with BUILDING1 high, WALKSTUF3 low, or the next large v1 PAL4 pending row. |
| 2026-04-29 | `building1-high-padded-fgp3-loop-validation` | `ps1: convert building1 foreground to padded fgp3` | Continue the padded-FGP3 lane on BUILDING1 high, a `955 KB` PAL4 pack with severe baseline blocking and due misses. | Captured a clean explicit-loop FGP2 baseline for `fgpilot building1 lowtide 0`, converted `generated/ps1/foreground/BUILDING1.FG2` to padded FGP3, validated once against the default gate, repeated with bounded `5%` tolerance for the local prefetch-overrun tradeoff, then cross-gated FISHING1 high with strict no-regression. | Promoted. BUILDING1 high improves `loop_vb 1019 -> 966`, `target_vb 758 -> 769`, `overrun_vb 261 -> 197`, `blocking_vb 184 -> 76`, `due_misses 27 -> 6`, `loop_reads 80 -> 44`, and `loop_read_vb 397 -> 227`; `prefetch_overrun_vb` regresses slightly `50 -> 52`, which is accepted here because the loop saves `53` VBlanks and due misses drop by `21`. Payload drops `951375 -> 526376`; the CD file remains `954555` bytes and `FG\\BUIL1.FG2` stays at LBA `39531`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-074047/summary.json`, default-gate FGP3 run `scratch/ps1-perf-iterate/20260429-074628/summary.json`, accepted bounded FGP3 metrics `scratch/ps1-perf-iterate/20260429-075219/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-075746/summary.json`. | Promote with documented tradeoff. BUILDING1 high now uses padded FGP3; continue with WALKSTUF3 low, low-tide pairs for accepted high scenes, or the next large v1 PAL4 pending row. |
| 2026-04-29 | `walkstuf3-low-padded-fgp3-loop-validation` | Log-only reject; no pack commit. | Test whether the WALKSTUF3 low-tide pair benefits from padded FGP3 after WALKSTUF3 high had already promoted. | Captured a clean explicit-loop FGP2 baseline for `fgpilot walkstuf3 lowtide 1`, converted `generated/ps1/foreground/WALK3LOW.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then restored the original FGP2 pack after the gate failed. | Rejected. WALKSTUF3 low barely improves `loop_vb 2494 -> 2485`, `target_vb 2282 -> 2282`, `overrun_vb 212 -> 203`, `blocking_vb 97 -> 86`, `due_misses 5 -> 3`, `loop_reads 72 -> 61`, and `loop_read_vb` remains materially high, while `prefetch_overrun_vb` regresses `65 -> 69` and fails the default gate. Payload only drops `986873 -> 811444`, much weaker than the stand/building wins. Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-080152/summary.json`, rejected FGP3 metrics `scratch/ps1-perf-iterate/20260429-080733/summary.json`. | Do not promote plain padded FGP3 for WALKSTUF3 low. Revisit only with pack-time grouping, selective direct16/residual metadata, or a scheduler that can prevent the extra refill overrun. |
| 2026-04-29 | `johnny3-low-padded-fgp3-loop-validation` | `ps1: convert johnny3 low foreground to padded fgp3` | Continue the padded-FGP3 lane on JOHNNY3 low after the high-tide variant had already promoted cleanly. | Captured a clean explicit-loop FGP2 baseline for `fgpilot johnny3 lowtide 1`, converted `generated/ps1/foreground/JOHN3LOW.FG2` to padded FGP3, validated against the fresh baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. JOHNNY3 low improves `loop_vb 1346 -> 1305`, `target_vb 1147 -> 1163`, `overrun_vb 199 -> 142`, `blocking_vb 71 -> 10`, `prefetch_overrun_vb 35 -> 10`, `due_misses 7 -> 0`, `loop_reads 60 -> 16`, and `loop_read_vb 346 -> 81`. Payload drops `884828 -> 213873`; the CD file remains `888428` bytes and `FG\\JOHN3LOW.FG2` stays at LBA `72079`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 loop baseline `scratch/ps1-perf-iterate/20260429-085011/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-085548/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-090112/summary.json`. | Promote. JOHNNY3 high/low now both use padded FGP3; continue with VISITOR1 high, FISHING7 high, STAND16 high, or the next large v1 PAL4 pending row. |
| 2026-04-29 | `visitor1-high-padded-fgp3-loop-validation` | `ps1: convert visitor1 foreground to padded fgp3` | Continue the padded-FGP3 lane on VISITOR1 high, a still-pending PAL4 visitor-family pack with severe FGP2 CD blocking. | Captured a fresh FGP2 baseline for `fgpilot visitor1 lowtide 0`, converted `generated/ps1/foreground/VISITOR1.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. VISITOR1 high improves `loop_vb 1051 -> 822`, `target_vb 639 -> 671`, `overrun_vb 412 -> 151`, `blocking_vb 401 -> 22`, `prefetch_overrun_vb 29 -> 22`, `due_misses 59 -> 0`, `loop_reads 88 -> 23`, and `loop_read_vb 547 -> 129`. Payload drops `864434 -> 301238`; the CD file remains `866910` bytes and `FG\\VISITOR1.FG2` stays at LBA `160434`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-093605/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-093848/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-094239/summary.json`. | Promote. VISITOR1 high now uses padded FGP3; continue with FISHING7 high, STAND16 high, or the next PAL4 pending row. |
| 2026-04-29 | `fishing7-high-padded-fgp3-loop-validation` | `ps1: convert fishing7 foreground to padded fgp3` | Continue the padded-FGP3 lane on FISHING7 high, the next large PAL4 pending pack after VISITOR1 high promoted. | Captured a fresh FGP2 baseline for `fgpilot fishing7 lowtide 0`, converted `generated/ps1/foreground/FISHING7.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. FISHING7 high improves `loop_vb 940 -> 863`, `target_vb 711 -> 723`, `overrun_vb 229 -> 140`, `blocking_vb 175 -> 7`, `prefetch_overrun_vb 12 -> 7`, `due_misses 32 -> 0`, `loop_reads 89 -> 26`, and `loop_read_vb 431 -> 132`. Payload drops `837063 -> 335542`; the CD file remains `839235` bytes and `FG\\FISHING7.FG2` stays at LBA `61389`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-100210/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-100449/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-100714/summary.json`. | Promote. FISHING7 high now uses padded FGP3; continue with STAND16 high, FISHING4 high, or the next eligible PAL4 pending row. |
| 2026-04-29 | `stand16-high-padded-fgp3-loop-validation` | `ps1: convert stand16 foreground to padded fgp3` | Continue the padded-FGP3 lane on STAND16 high, a stand-family PAL4 pending pack with modest but measurable CD pressure. | Captured a fresh FGP2 baseline for `fgpilot stand16 lowtide 0`, converted `generated/ps1/foreground/STAND16.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND16 high improves `loop_vb 1340 -> 1328`, `target_vb 1180 -> 1189`, `overrun_vb 160 -> 139`, `blocking_vb 26 -> 7`, `prefetch_overrun_vb 21 -> 7`, `due_misses 1 -> 0`, `loop_reads 68 -> 19`, and `loop_read_vb 378 -> 103`. Payload drops `785019 -> 279058`; the CD file remains `788179` bytes and `FG\\STAND16.FG2` stays at LBA `126338`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-101057/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-101331/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-101556/summary.json`. | Promote. STAND16 high now uses padded FGP3; continue with FISHING4 high, ACTIVITY11 high, or the next eligible PAL4 pending row. |
| 2026-04-29 | `fishing4-high-padded-fgp3-loop-validation` | `ps1: convert fishing4 foreground to padded fgp3` | Continue the padded-FGP3 lane on FISHING4 high, a mid-sized PAL4 pending pack with visible CD/refill pressure. | Captured a fresh FGP2 baseline for `fgpilot fishing4 lowtide 0`, converted `generated/ps1/foreground/FISHING4.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. FISHING4 high improves `loop_vb 997 -> 978`, `target_vb 830 -> 838`, `overrun_vb 167 -> 140`, `blocking_vb 35 -> 15`, `prefetch_overrun_vb 35 -> 15`, `loop_reads 60 -> 18`, and `loop_read_vb 217 -> 98`. Payload drops `770186 -> 261315`; the CD file remains `772954` bytes and `FG\\FISHING4.FG2` stays at LBA `60173`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-102052/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-102332/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-102556/summary.json`. | Promote. FISHING4 high now uses padded FGP3; continue with ACTIVITY11 high/low, FISHING6 high, VIST5LOW, or the next eligible PAL4 pending row. |
| 2026-04-29 | `activity11-high-padded-fgp3-loop-validation` | `ps1: convert activity11 foreground to padded fgp3` | Continue the padded-FGP3 lane on ACTIVITY11 high, a mid-sized ACTIVITY-family PAL4 pack with visible CD/refill pressure. | Captured a fresh FGP2 baseline for `fgpilot activity11 lowtide 0`, converted `generated/ps1/foreground/ACTIVITY11.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. ACTIVITY11 high improves `loop_vb 1891 -> 1864`, `target_vb 1715 -> 1724`, `overrun_vb 176 -> 140`, `blocking_vb 35 -> 9`, `prefetch_overrun_vb 32 -> 5`, `loop_reads 58 -> 13`, and `loop_read_vb 291 -> 64`; `due_misses` stays at `1`. Payload drops `747178 -> 197913`; the CD file remains `751210` bytes and `FG\\ACTV11.FG2` stays at LBA `37116`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-103206/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-103438/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-103701/summary.json`. | Promote. ACTIVITY11 high now uses padded FGP3; continue with ACTIVITY11 low, FISHING6 high, VIST5LOW, or another eligible PAL4 pending row. |
| 2026-04-29 | `activity11-low-padded-fgp3-loop-validation` | `ps1: convert activity11 low foreground to padded fgp3` | Complete the ACTIVITY11 pair by testing the low-tide PAL4 pack under the same padded-FGP3 method that promoted high tide. | Captured a fresh FGP2 baseline for `fgpilot activity11 lowtide 1`, converted `generated/ps1/foreground/ACTV11L.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. ACTIVITY11 low improves `loop_vb 1883 -> 1866`, `target_vb 1713 -> 1723`, `overrun_vb 170 -> 143`, `blocking_vb 29 -> 10`, `prefetch_overrun_vb 26 -> 5`, `loop_reads 57 -> 13`, and `loop_read_vb 285 -> 66`; `due_misses` stays at `1`. Payload drops `747178 -> 197913`; the CD file remains `751210` bytes and `FG\\ACTV11L.FG2` stays at LBA `37483`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-104546/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-104817/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-105050/summary.json`. | Promote. ACTIVITY11 high/low now both use padded FGP3; continue with FISHING6 high, VIST5LOW, BUILDING1 low, or another eligible PAL4 pending row. |
| 2026-04-29 | `visitor5-low-padded-fgp3-loop-validation` | `ps1: convert visitor5 low foreground to padded fgp3` | Complete the VISITOR5 pair by testing the low-tide PAL4 pack under the padded-FGP3 method. | Captured a fresh FGP2 baseline for `fgpilot visitor5 lowtide 1`, converted `generated/ps1/foreground/VIST5LOW.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. VISITOR5 low improves `loop_vb 1302 -> 1250`, `target_vb 1075 -> 1085`, `overrun_vb 227 -> 165`, `blocking_vb 118 -> 62`, `prefetch_overrun_vb 61 -> 27`, `due_misses 10 -> 6`, `loop_reads 59 -> 29`, and `loop_read_vb 351 -> 166`. Payload drops `765113 -> 400594`; the CD file remains `769297` bytes and `FG\\VIST5LOW.FG2` stays at LBA `165212`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-105443/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-105715/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-105940/summary.json`. | Promote. VISITOR5 high/low now both use padded FGP3; continue with FISHING6 high, BUILDING1 low, MISCGAG1 high, or another eligible PAL4 pending row. |
| 2026-04-29 | `fishing6-high-padded-fgp3-loop-validation` | `ps1: convert fishing6 foreground to padded fgp3` | Test FISHING6 high, a large FISHING-family PAL4 pack, using loop validation because the one-shot `noloop` baseline hit post-scene invalid-read log spam after emitting usable scene metrics. | First ran `fgpilot fishing6 lowtide 0 ... noloop`, which emitted `loop_vb=915` but exceeded the log cap with `0xFFFFBF80` invalid reads. Re-ran the baseline as `fgpilot fishing6 lowtide 0 ... loop perf-log`, converted `generated/ps1/foreground/FISHING6.FG2` to padded FGP3 while preserving the original CD file size, validated against the loop baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. FISHING6 high improves `loop_vb 914 -> 893`, `target_vb 744 -> 749`, `overrun_vb 170 -> 144`, `blocking_vb 23 -> 7`, `prefetch_overrun_vb 23 -> 7`, `loop_reads 56 -> 22`, and `loop_read_vb 293 -> 117`; `due_misses` stays at `0`. Payload drops `716273 -> 311985`; the CD file remains `718609` bytes and `FG\\FISHING6.FG2` stays at LBA `60850`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: failed noloop baseline `scratch/ps1-perf-iterate/20260429-110301/fishing6-high/perf-summary.json`, loop FGP2 baseline `scratch/ps1-perf-iterate/20260429-110454/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-110736/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-111002/summary.json`. | Promote. FISHING6 high now uses padded FGP3; continue with BUILDING1 low, MISCGAG1 high, FISHING6 low, or another eligible PAL4 pending row. |
| 2026-04-29 | `building1-low-padded-fgp3-loop-validation` | `ps1: convert building1 low foreground to padded fgp3` | Complete the BUILDING1 pair by testing the low-tide PAL4 pack under explicit loop validation. | Captured a loop FGP2 baseline for `fgpilot building1 lowtide 1`, converted `generated/ps1/foreground/BUIL1LOW.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted with a documented due-miss tradeoff. BUILDING1 low improves `loop_vb 968 -> 953`, `target_vb 767 -> 767`, `overrun_vb 201 -> 186`, `blocking_vb 69 -> 62`, `prefetch_overrun_vb 57 -> 46`, `loop_reads 45 -> 40`, and `loop_read_vb 245 -> 199`, while `due_misses` regresses `2 -> 4`. Payload drops only `570502 -> 457201`; the CD file remains `573382` bytes and `FG\\BUIL1LOW.FG2` stays at LBA `39998`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-111333/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-111606/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-111838/summary.json`. | Promote with tradeoff. BUILDING1 high/low now both use padded FGP3; keep BUILDING1 low on the grouped-read/direct16 follow-up list because due misses rose and blocking remains high. |
| 2026-04-29 | `miscgag1-high-padded-fgp3-loop-validation` | `ps1: convert miscgag1 foreground to padded fgp3` | Test whether padded FGP3 continues to pay on a MISCGAG-family PAL4 pack outside the larger activity/fishing/building/visitor clusters. | Captured a loop FGP2 baseline for `fgpilot miscgag1 lowtide 0`, converted `generated/ps1/foreground/MISCGAG1.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. MISCGAG1 high improves `loop_vb 1127 -> 1115`, `target_vb 942 -> 954`, `overrun_vb 185 -> 161`, `blocking_vb 42 -> 26`, `prefetch_overrun_vb 42 -> 26`, `loop_reads 62 -> 16`, and `loop_read_vb 334 -> 97`; `due_misses` stays at `0`. Payload drops `716388 -> 231214`; the CD file remains `719332` bytes and `FG\\MISCGAG1.FG2` stays at LBA `119915`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-112211/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-112447/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-112717/summary.json`. | Promote. MISCGAG1 high now uses padded FGP3; continue with MISCGAG1 low, FISHING6 low, STAND low-tide rows, or another eligible PAL4 pending row. |
| 2026-04-29 | `miscgag1-low-padded-fgp3-loop-validation` | `ps1: convert miscgag1 low foreground to padded fgp3` | Complete the MISCGAG1 pair by testing the low-tide PAL4 pack under the same padded-FGP3 method. | Captured a loop FGP2 baseline for `fgpilot miscgag1 lowtide 1`, converted `generated/ps1/foreground/MISC1LOW.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. MISCGAG1 low improves `loop_vb 1108 -> 1099`, `target_vb 959 -> 959`, `overrun_vb 149 -> 140`, `blocking_vb 8 -> 5`, `prefetch_overrun_vb 8 -> 5`, `loop_reads 23 -> 10`, and `loop_read_vb 131 -> 48`; `due_misses` stays at `0`. Payload drops `328994 -> 146040`; the CD file remains `331438` bytes and `FG\\MISC1LOW.FG2` stays at LBA `120267`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-113229/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-113500/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-113723/summary.json`. | Promote. MISCGAG1 high/low now both use padded FGP3; continue with FISHING6 low, FISHING4 low, MISCGAG2, or another eligible PAL4 pending row. |
| 2026-04-29 | `fishing6-low-padded-fgp3-loop-validation` | `ps1: convert fishing6 low foreground to padded fgp3` | Complete the FISHING6 pair by testing the low-tide PAL4 pack under explicit loop validation, matching the high-tide path that avoided the one-shot post-scene log artifact. | Captured a loop FGP2 baseline for `fgpilot fishing6 lowtide 1`, converted `generated/ps1/foreground/FISH6LOW.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. FISHING6 low improves `loop_vb 903 -> 897`, `target_vb 748 -> 750`, `overrun_vb 155 -> 147`, `blocking_vb 12 -> 10`, `prefetch_overrun_vb 12 -> 10`, `loop_reads 28 -> 18`, and `loop_read_vb 146 -> 100`; `due_misses` stays at `0`. Payload drops `382073 -> 250428`; the CD file remains `384269` bytes and `FG\\FISH6LOW.FG2` stays at LBA `61201`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-114151/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-114420/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-114644/summary.json`. | Promote. FISHING6 high/low now both use padded FGP3; continue with FISHING4 low, MISCGAG2, or the next eligible PAL4 pending row. |
| 2026-04-29 | `fishing4-low-padded-fgp3-loop-validation` | Log-only reject; no pack commit. | Test whether the low-tide FISHING4 pair benefits from the same padded-FGP3 conversion that promoted high tide. | Captured a loop FGP2 baseline for `fgpilot fishing4 lowtide 1`, converted `generated/ps1/foreground/FISH4LOW.FG2` to padded FGP3 while preserving the original CD file size, and validated against the baseline with FGP3 work-identity disabled. The converted pack was restored after the strict gate failed. | Rejected. FISHING4 low improves `loop_vb 976 -> 972`, `target_vb 842 -> 843`, `overrun_vb 134 -> 129`, `loop_reads 17 -> 12`, and payload `236700 -> 167356`, but regresses visible CD pressure: `blocking_vb 3 -> 5` and `prefetch_overrun_vb 3 -> 5`. `due_misses` stays at `0`, and `FG\\FISH4LOW.FG2` would have stayed at LBA `60551`, but this is not a clean enough tradeoff for the plain padded-FGP3 lane. Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-115129/summary.json`, rejected FGP3 metrics `scratch/ps1-perf-iterate/20260429-115407/summary.json`. | Do not promote plain padded FGP3 for FISHING4 low. Retry only with grouped-read/direct16 metadata or scene-specific scheduling that can keep the already-small CD pressure from rising. |
| 2026-04-29 | `fishing8-low-padded-fgp3-loop-validation` | `ps1: convert fishing8 low foreground to padded fgp3` | Complete the FISHING8 pair by testing the low-tide PAL4 pack under the same padded-FGP3 method that promoted high tide. | Captured a loop FGP2 baseline for `fgpilot fishing8 lowtide 1`, converted `generated/ps1/foreground/FISH8LOW.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. FISHING8 low improves `loop_vb 1393 -> 1387`, `target_vb 1247 -> 1249`, `overrun_vb 146 -> 138`, `blocking_vb 13 -> 11`, `prefetch_overrun_vb 13 -> 11`, `loop_reads 43 -> 20`, and `loop_read_vb 228 -> 97`; `due_misses` stays at `0`. Payload drops `588958 -> 281162`; the CD file remains `593074` bytes and `FG\\FISH8LOW.FG2` stays at LBA `62834`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-115812/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-120044/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-120311/summary.json`. | Promote. FISHING8 high/low now both use padded FGP3; continue with JOHNNY2, MARY4, STAND15, or another larger eligible PAL4 pending row. |
| 2026-04-29 | `johnny2-high-padded-fgp3-loop-validation` | `ps1: convert johnny2 foreground to padded fgp3` | Test the padded-FGP3 lane on JOHNNY2 high, a larger remaining JOHNNY-family PAL4 pack with high read count but low visible blocking. | Captured a loop FGP2 baseline for `fgpilot johnny2 lowtide 0`, converted `generated/ps1/foreground/JOHNNY2.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. JOHNNY2 high improves `loop_vb 1889 -> 1878`, `target_vb 1748 -> 1750`, `overrun_vb 141 -> 128`, `blocking_vb 4 -> 1`, `prefetch_overrun_vb 5 -> 1`, `loop_reads 48 -> 13`, and `loop_read_vb 267 -> 72`; `due_misses` stays at `0`. Payload drops `620755 -> 187518`; the CD file remains `623707` bytes and `FG\\JOHNNY2.FG2` stays at LBA `70700`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-120629/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-120901/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-121126/summary.json`. | Promote. JOHNNY2 high now uses padded FGP3; continue with JOHNNY2 low or another larger eligible PAL4 pending row. |
| 2026-04-29 | `johnny2-low-padded-fgp3-loop-validation` | `ps1: convert johnny2 low foreground to padded fgp3` | Complete the JOHNNY2 pair by testing the low-tide PAL4 pack under the same padded-FGP3 method that promoted high tide. | Captured a loop FGP2 baseline for `fgpilot johnny2 lowtide 1`, converted `generated/ps1/foreground/JOHN2LOW.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. JOHNNY2 low improves `loop_vb 1887 -> 1878`, `target_vb 1748 -> 1750`, `overrun_vb 139 -> 128`, `blocking_vb 3 -> 1`, `prefetch_overrun_vb 3 -> 1`, `loop_reads 48 -> 13`, and `loop_read_vb 259 -> 71`; `due_misses` stays at `0`. Payload drops `620755 -> 187518`; the CD file remains `623707` bytes and `FG\\JOHN2LOW.FG2` stays at LBA `71005`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-121445/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-121715/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-121941/summary.json`. | Promote. JOHNNY2 high/low now both use padded FGP3. Next tests should include previously untouched scene slugs, not only low-tide pair completions, to keep the 63-scene perf suite coverage growing. |
| 2026-04-29 | `perf-matrix-last-run-at-column` | `ps1: add perf matrix freshness timestamps` | Make scene/tide measurement freshness explicit as the perf suite expands past the canary scenes. | Added a `last_run_at` CSV column populated from the headless summary path (`scratch/ps1-perf-iterate/YYYYMMDD-HHMMSS`), updated the manifest writer to emit it for future runs, and added a `Latest Run` column to the rendered scene battle card. | Promoted as tooling. The latest matrix run is now surfaced by the battle-card rollups; pending rows remain blank, and measured/blocked rows carry their own run timestamp. | Keep this column updated with every accepted matrix row so stale measurements are visible during broad 63-scene coverage. |
| 2026-04-29 | `stand15-high-padded-fgp3-loop-validation` | `ps1: convert stand15 foreground to padded fgp3` | Expand coverage to a previously untouched stand-family scene while continuing the padded-FGP3 conversion lane. | Captured a loop FGP2 baseline for `fgpilot stand15 lowtide 0`, converted `generated/ps1/foreground/STAND15.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND15 high improves `loop_vb 1164 -> 1135`, `target_vb 973 -> 985`, `overrun_vb 191 -> 150`, `blocking_vb 51 -> 17`, `prefetch_overrun_vb 51 -> 17`, `loop_reads 46 -> 14`, and `loop_read_vb 300 -> 87`; `due_misses` stays at `0`. Payload drops `634174 -> 205538`; the CD file remains `637046` bytes and `FG\\STAND15.FG2` stays at LBA `125899`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-122256/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-123059/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-123329/summary.json`. | Promote. STAND15 high now uses padded FGP3; continue with another new scene slug such as MARY4, ACTIVITY8, MISCGAG2, JOHNNY4, or STAND7 before doing more pair-only coverage. |
| 2026-04-29 | `mary4-high-padded-fgp3-loop-validation` | Log-only reject; keep FGP2 baseline measurement. | Test a new MARY-family scene for coverage and see whether plain padded FGP3 helps a pack that is already faster than target but still has due misses and visible CD pressure. | Captured a loop FGP2 baseline for `fgpilot mary4 lowtide 0`, converted `generated/ps1/foreground/MARY4.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then restored FGP2 after the gate failed. | Rejected as a pack conversion, but retained the FGP2 baseline in the scene matrix. Baseline: `loop_vb=1969`, `target_vb=2019`, `over_target_vb=-50`, `blocking_vb=27`, `prefetch_overrun_vb=10`, `due_misses=4`, `loop_reads=42`. FGP3 improved `loop_vb 1969 -> 1960` and `blocking_vb 27 -> 20`, but payload barely changed `547571 -> 541622` and `prefetch_overrun_vb` regressed `10 -> 15`, failing the default gate. Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-123706/summary.json`, rejected FGP3 metrics `scratch/ps1-perf-iterate/20260429-123948/summary.json`. | Do not promote plain padded FGP3 for MARY4 high. Keep the measured FGP2 row for coverage; retry with grouped-read/direct16 metadata or a scheduler that targets due misses without increasing prefetch overrun. |
| 2026-04-29 | `stand7-high-padded-fgp3-loop-validation` | `ps1: convert stand7 foreground to padded fgp3` | Use the host-side residual-size scan to pick a high-savings untouched scene slug, then test whether STAND7 high benefits from plain padded FGP3 without changing CD layout. | Captured a loop FGP2 baseline for `fgpilot stand7 lowtide 0`, converted `generated/ps1/foreground/STAND7.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND7 high improves `loop_vb 684 -> 676`, `target_vb 531 -> 536`, `overrun_vb 153 -> 140`, `blocking_vb 15 -> 5`, `prefetch_overrun_vb 15 -> 5`, `loop_reads 28 -> 6`, and `loop_read_vb 155 -> 36`; `due_misses` stays at `0`. Payload drops `386843 -> 94333`; the CD file remains `388655` bytes and `FG\\STAND7.FG2` stays at LBA `123747`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-125137/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-125414/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-125640/summary.json`. | Promote. STAND7 high now uses padded FGP3 and adds another new stand-family scene to the timed matrix. Continue with high-savings untouched packs such as STAND3 high, STAND9 high, STAND10 high, STAND11 high, or ACTIVITY8 before doing more pair-only coverage. |
| 2026-04-29 | `stand3-high-padded-fgp3-loop-validation` | `ps1: convert stand3 foreground to padded fgp3` | Continue the high-savings STAND-family lane with another untouched scene slug selected by the host-side residual-size scan. | Captured a loop FGP2 baseline for `fgpilot stand3 lowtide 0`, converted `generated/ps1/foreground/STAND3.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND3 high improves `loop_vb 713 -> 693`, `target_vb 546 -> 555`, `overrun_vb 167 -> 138`, `blocking_vb 28 -> 2`, `prefetch_overrun_vb 28 -> 2`, `loop_reads 29 -> 6`, and `loop_read_vb 167 -> 38`; `due_misses` stays at `0`. Payload drops `393898 -> 96203`; the CD file remains `395790` bytes and `FG\\STAND3.FG2` stays at LBA `121156`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-133113/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-133350/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-133618/summary.json`. | Promote. STAND3 high now uses padded FGP3 and adds another new stand-family scene to the timed matrix. Continue with STAND9 high, STAND10 high, STAND11 high, STAND2 high, or ACTIVITY8 to keep broad-scene coverage moving. |
| 2026-04-29 | `stand9-high-padded-fgp3-loop-validation` | `ps1: convert stand9 foreground to padded fgp3` | Continue the host-side residual-size lane with STAND9 high, another untouched stand-family pack with large temporal-residual savings. | Captured a loop FGP2 baseline for `fgpilot stand9 lowtide 0`, converted `generated/ps1/foreground/STAND9.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND9 high improves `loop_vb 684 -> 673`, `target_vb 530 -> 535`, `overrun_vb 154 -> 138`, `blocking_vb 15 -> 3`, `prefetch_overrun_vb 15 -> 3`, `loop_reads 27 -> 6`, and `loop_read_vb 148 -> 36`; `due_misses` stays at `0`. Payload drops `382132 -> 94439`; the CD file remains `383944` bytes and `FG\\STAND9.FG2` stays at LBA `124262`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-141119/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-141353/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-141831/summary.json`. | Promote. STAND9 high now uses padded FGP3 and adds another new stand-family scene to the timed matrix. Keep the accepted packs padded until the dedicated ISO compaction/layout pass can remove padding as a separate timing-gated experiment. |
| 2026-04-29 | `stand10-high-padded-fgp3-loop-validation` | `ps1: convert stand10 foreground to padded fgp3` | Continue the host-side residual-size lane with STAND10 high, another untouched stand-family pack with the same large temporal-residual profile as STAND9. | Captured a loop FGP2 baseline for `fgpilot stand10 lowtide 0`, converted `generated/ps1/foreground/STAND10.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND10 high improves `loop_vb 693 -> 673`, `target_vb 529 -> 535`, `overrun_vb 164 -> 138`, `blocking_vb 25 -> 3`, `prefetch_overrun_vb 25 -> 3`, `loop_reads 27 -> 6`, and `loop_read_vb 148 -> 36`; `due_misses` stays at `0`. Payload drops `380620 -> 94900`; the CD file remains `382432` bytes and `FG\\STAND10.FG2` stays at LBA `124530`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-145137/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-145405/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-145628/summary.json`. | Promote. STAND10 high now uses padded FGP3 and adds another new stand-family scene to the timed matrix. Continue with STAND11 high, STAND8 high, STAND2 high, ACTIVITY8 high, or MISCGAG2 high. |
| 2026-04-29 | `stand11-high-padded-fgp3-loop-validation` | `ps1: convert stand11 foreground to padded fgp3` | Continue the host-side residual-size lane with STAND11 high, the next untouched stand-family pack with large temporal-residual savings. | Captured a loop FGP2 baseline for `fgpilot stand11 lowtide 0`, converted `generated/ps1/foreground/STAND11.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND11 high improves `loop_vb 686 -> 674`, `target_vb 529 -> 536`, `overrun_vb 157 -> 138`, `blocking_vb 19 -> 3`, `prefetch_overrun_vb 19 -> 3`, `loop_reads 27 -> 6`, and `loop_read_vb` to `36`; `due_misses` stays at `0`. Payload drops `374354 -> 93694`; the CD file remains `376166` bytes and `FG\\STAND11.FG2` stays at LBA `124796`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-150126/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-150403/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-150627/summary.json`. | Promote. STAND11 high now uses padded FGP3 and adds another new stand-family scene to the timed matrix. Continue with STAND8 high, STAND2 high, ACTIVITY8 high, MISCGAG2 high, or JOHNNY4 high. |
| 2026-04-29 | `stand8-high-padded-fgp3-loop-validation` | `ps1: convert stand8 foreground to padded fgp3` | Continue the host-side residual-size lane with STAND8 high, another untouched stand-family pack with large temporal-residual savings and visible CD pressure. | Captured a loop FGP2 baseline for `fgpilot stand8 lowtide 0`, converted `generated/ps1/foreground/STAND8.FG2` to padded FGP3 while preserving the original CD file size, validated against the baseline with FGP3 work-identity disabled, then cross-gated FISHING1 high with strict no-regression. | Promoted. STAND8 high improves `loop_vb 649 -> 635`, `target_vb 493 -> 500`, `overrun_vb 156 -> 135`, `blocking_vb 18 -> 0`, `prefetch_overrun_vb 18 -> 0`, `loop_reads 25 -> 6`, and `loop_read_vb 133 -> 27`; `due_misses` stays at `0`. Payload drops `348399 -> 93771`; the CD file remains `350071` bytes and `FG\\STAND8.FG2` stays at LBA `124019`. FISHING1 high stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/20260429-151103/summary.json`, accepted FGP3 metrics `scratch/ps1-perf-iterate/20260429-151334/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/20260429-151558/summary.json`. | Promote. STAND8 high now uses padded FGP3 and adds another new stand-family scene to the timed matrix. Continue with STAND2 high, ACTIVITY8 high, MISCGAG2 high, JOHNNY4 high, or VISITOR4 high. |
| 2026-04-29 | `fgp3-compaction-stats-versioning` | `ps1: compact padded fgp3 packs and version stats` | Since emulator CD read speed is not physically location-dependent, remove the zero padding kept only to isolate earlier per-scene format experiments, and version battle-card statistics so old padded-layout numbers are not mistaken for compact-layout numbers. | Added `scripts/compact-fgp3-packs.py`, compacted every safe padded FGP3 pack, added `stats_version` to `performance-scene-matrix.csv`, backfilled existing measured/blocked rows as `padded-fgp3-v1`, and stamped compact reruns as `compact-fgp3-v1`. | Promoted. The compactor trimmed `73 / 79` FGP3 packs and removed `154427719` bytes from `generated/ps1/foreground`. FISHING1 high under `compact-fgp3-v1` stayed exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `FG\\FISHING1.FG2` LBA `401`). STAND8 high also stayed exact-flat after real compaction (`loop_vb=635`, `blocking_vb=0`, `prefetch_overrun_vb=0`) while pack bytes changed `350071 -> 95443` and LBA changed `124019 -> 53943`. Artifacts: FISHING1 compact canary `scratch/ps1-perf-iterate/20260429-152913/summary.json`, STAND8 compact rerun `scratch/ps1-perf-iterate/20260429-153139/summary.json`. | Promote. Treat `compact-fgp3-v1` as the new stats lane. Rerun the remaining battle-card rows under this version over time; old `padded-fgp3-v1` rows are historical until refreshed. |
| 2026-04-29 | `compact-fgp3-v2-fullmatrix` | `ps1: complete compact full-matrix perf pass` | Run the complete compact-layout performance matrix after FGP3 padding removal, then make the harness fast enough to keep rerunning all `126` variants without spending hours on artificial frame budgets. | Added early-stop-on-`JCPERF2`, `--skip-build`, `--case-local-cd`, `--resume-output`, `--retries`, and `--jobs` to the matrix harness, then ran `PS1_PERF_STATS_VERSION=compact-fgp3-v2-fullmatrix ./scripts/ps1-perf-all-scenes.sh --tides both --order list --frames 7200 --timeout 220 --output scratch/ps1-perf-iterate/compact-fgp3-v2-fullmatrix --stats-version compact-fgp3-v2-fullmatrix --continue-on-fail --skip-build --retries 2 --resume-output --jobs 2`. | Promoted as tooling/measurement. The matrix now has `126 / 126` routed rows, all stamped `compact-fgp3-v2-fullmatrix`; `120 / 126` rows carry active-loop timing. Timing-bearing average is `+17.4%` over target / `87.1%` target speed. Latest row is `2026-04-29T18:13:13`. Four transient failed attempt summaries were retried successfully and did not overwrite accepted rows. Heavy scratch logs/frames and ignored host-capture frame trees were pruned after preserving the summary JSONs and CSV. | Promote. This is the new performance status baseline. Future accepted optimizations must update the CSV, README, website battle card, and this log against `compact-fgp3-v2-fullmatrix`, and should keep full-matrix reruns case-local/early-stopped unless a test explicitly needs the old behavior. |
| 2026-04-29 | `stand12-low-compact-fgp3` | `ps1: convert stand12 low foreground to compact fgp3` | Continue the compact FGP3 host-preprocessing lane on the highest non-rejected PAL4 FGP2 row left in the matrix. `STND12L.FG2` had `loop_vb=1622`, `overrun_vb=172`, `blocking_vb=35`, and `loop_reads=38`; the high-tide pair was already FGP3. | Converted `generated/ps1/foreground/STND12L.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, then gated `fgpilot stand12 lowtide 1 ... perf-log noloop seed 1` against the `compact-fgp3-v2-fullmatrix` STAND12-low baseline with `--require-improvement --work-identity-min 0`. Cross-gated FISHING1 high against the compact full-matrix canary. | Promoted. STAND12 low improves `loop_vb 1622 -> 1595`, `target_vb 1450 -> 1459`, `overrun_vb 172 -> 136`, `blocking_vb 35 -> 3`, `prefetch_overrun_vb 35 -> 3`, `loop_reads 38 -> 9`, and `loop_read_vb 202 -> 49`; `due_misses` stays `0`. Payload drops `539229 -> 141812`, and the compact pack drops `543341 -> 145924` bytes. `FG\\STND12L.FG2` stays at LBA `54575`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: accepted STAND12 low `scratch/ps1-perf-iterate/stand12-low-fgp3-test/20260429-184529-1834971/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand12-low-fgp3-canary/20260429-184645-1842659/summary.json`. | Promote. The battle card now has mixed stats versions: `compact-fgp3-v3-stand12low` for the updated STAND12-low/canary rows and `compact-fgp3-v2-fullmatrix` for rows not rerun since the full matrix. Continue with ACTIVITY8 high/low, VISITOR4 high, VISITOR6 low, STAND5 low, or another high-pressure PAL4 FGP2 row; keep indexed8 outliers (`walkstuf1`, `mary3`) for a separate format path. |
| 2026-04-29 | `walkstuf3-low-compact-fgp3-retry` | Log-only reject; no pack commit. | Retry WALKSTUF3 low under the compact-layout lane because it was the largest remaining measured PAL4 FGP2 pressure row. The old `compact-fgp3-v2-fullmatrix` baseline was stale after STAND12-low compaction shifted later FG LBAs, so a current-layout FGP2 baseline was captured first. | Restored `generated/ps1/foreground/WALK3LOW.FG2` from the FGP2 backup, captured `fgpilot walkstuf3 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, then gated the compact FGP3 pack against that fresh baseline with `--require-improvement --work-identity-min 0 --allow-regression 3`. | Rejected. The fresh FGP2 baseline is `loop_vb=2467`, `target_vb=2286`, `overrun_vb=181`, `blocking_vb=64`, `prefetch_overrun_vb=37`, `loop_reads=72`, `loop_read_vb=407`, and `due_misses=5`. Compact FGP3 only shrinks payload `986873 -> 811444` and reduces `loop_reads 72 -> 61` plus `due_misses 5 -> 3`, but regresses visible timing: `loop_vb 2467 -> 2474`, `overrun_vb 181 -> 189`, `blocking_vb 64 -> 67`, and `prefetch_overrun_vb 37 -> 52`. Artifacts: stale-baseline failure `scratch/ps1-perf-iterate/walkstuf3-low-fgp3-test/20260429-185825-1909057/summary.json`, fresh FGP2 baseline `scratch/ps1-perf-iterate/walkstuf3-low-current-baseline/20260429-185955-1917977/summary.json`, rejected compact FGP3 run `scratch/ps1-perf-iterate/walkstuf3-low-fgp3-current-test/20260429-190119-1926386/summary.json`. | Do not promote plain compact FGP3 for WALKSTUF3 low. The weak residual ratio and worse refill slack match the earlier padded-FGP3 rejection; revisit only with pack-time read grouping, selective direct16, or a scheduler that can use the lower read count without increasing prefetch overrun. |
| 2026-04-29 | `visitor6-low-compact-fgp3` | `ps1: convert visitor6 low foreground to compact fgp3` | Continue the compact FGP3 host-preprocessing lane on the next high-pressure PAL4 FGP2 row with a materially better residual ratio than WALKSTUF3 low. Because STAND12-low compaction shifted later FG LBAs, capture a fresh current-layout VISITOR6-low FGP2 baseline before testing the format change. | Backed up `generated/ps1/foreground/VIST6LOW.FG2`, captured `fgpilot visitor6 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the compact full-matrix canary. | Promoted. VISITOR6 low improves `loop_vb 2205 -> 2190`, `target_vb 2041 -> 2048`, `overrun_vb 164 -> 142`, `blocking_vb 17 -> 1`, `prefetch_overrun_vb 17 -> 1`, `loop_reads 41 -> 19`, and `loop_read_vb 227 -> 101`; `due_misses` stays `0`. Payload drops `582114 -> 272050`, and the compact pack drops `586686 -> 276622` bytes. `FG\\VIST6LOW.FG2` stays at LBA `91514`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/visitor6-low-current-baseline/20260429-190535-1950756/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/visitor6-low-fgp3-current-test/20260429-190659-1959150/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/visitor6-low-fgp3-canary/20260429-190818-1967058/summary.json`. | Promote. The battle-card average moves from `+17.4%` to `+17.3%` over target while target speed remains `87.1%`. Latest rows now use `compact-fgp3-v4-visitor6low`; continue with STAND5 low, VISITOR4 high, ACTIVITY8 high/low, or another row with both visible pressure and at least ~50% residual savings. |
| 2026-04-29 | `visitor4-high-compact-fgp3` | `ps1: convert visitor4 high foreground to compact fgp3` | Continue the compact FGP3 host-preprocessing lane on VISITOR4 high, the highest remaining measured PAL4 FGP2 pressure row with no prior rejection and a predicted `~53%` residual-size cut. Capture a fresh current-layout FGP2 baseline first because older matrix rows predate recent compaction. | Backed up `generated/ps1/foreground/VISITOR4.FG2`, captured `fgpilot visitor4 lowtide 0 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. VISITOR4 high improves `loop_vb 591 -> 569`, `target_vb 422 -> 427`, `overrun_vb 169 -> 142`, `blocking_vb 27 -> 2`, `prefetch_overrun_vb 19 -> 2`, `due_misses 1 -> 0`, `loop_reads 26 -> 11`, and `loop_read_vb 128 -> 59`. Payload drops `319609 -> 149909`, and the compact pack drops `321041 -> 151341` bytes. `FG\\VISITOR4.FG2` stays at LBA `90570`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/visitor4-high-current-baseline/20260429-192434-2058334/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/visitor4-high-fgp3-current-test/20260429-192525-2063712/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/visitor4-high-fgp3-canary/20260429-192621-2069566/summary.json`. | Promote. The battle-card average stays rounded to `+17.3%` over target, but the exact timing-bearing average improves from `17.34%` to `17.29%`. Latest rows now use `compact-fgp3-v5-visitor4high`; continue with STAND5 low, ACTIVITY8 high/low, STAND4 low, STAND6 low, or another row with visible pressure and strong residual savings. |
| 2026-04-29 | `stand5-low-compact-fgp3` | `ps1: convert stand5 low foreground to compact fgp3` | Complete the STAND5 pair under the compact FGP3 lane. The high-tide pack had already promoted, while low tide remained a measured PAL4 FGP2 row with visible CD/refill pressure and a predicted `~74%` residual-size cut. | Backed up `generated/ps1/foreground/STND5LOW.FG2`, captured `fgpilot stand5 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND5 low improves `loop_vb 1607 -> 1595`, `target_vb 1449 -> 1458`, `overrun_vb 158 -> 137`, `blocking_vb 22 -> 4`, `prefetch_overrun_vb 22 -> 4`, `loop_reads 38 -> 9`, and `loop_read_vb 205 -> 47`; `due_misses` stays `0`. Payload drops `535922 -> 139360`, and the compact pack drops `540034 -> 143472` bytes. `FG\\STND5LOW.FG2` stays at LBA `53166`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand5-low-current-baseline/20260429-193206-2102531/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand5-low-fgp3-current-test/20260429-193319-2109845/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand5-low-fgp3-canary/20260429-193426-2116746/summary.json`. | Promote. STAND5 high/low are now both compact FGP3. The exact timing-bearing average improves again to `17.27%` over target. Latest rows now use `compact-fgp3-v6-stand5low`; continue with ACTIVITY8 high/low, STAND4 low, STAND6 low, STAND2 high, or another PAL4 row with strong residual savings. |
| 2026-04-29 | `activity8-high-compact-fgp3-retry` | Log-only reject; no pack commit. | Test ACTIVITY8 high because it was the highest remaining non-metadata PAL4 FGP2 row and had due misses (`4`) plus visible blocking under a fresh current-layout baseline. | Backed up `generated/ps1/foreground/ACTIVITY8.FG2`, captured a fresh FGP2 baseline for `fgpilot activity8 lowtide 0 ... perf-log noloop seed 1`, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, ran the default gate, then repeated the exact same pack under a bounded-tradeoff attempt before restoring the original FGP2 pack. | Rejected. Compact FGP3 gives tempting work reductions: `loop_vb 1050 -> 1045`, `overrun_vb 147 -> 142`, `blocking_vb 10 -> 4`, `due_misses 4 -> 0`, `loop_reads 35 -> 12`, and payload `412221 -> 171708`. However `prefetch_overrun_vb` regresses `1 -> 4`, and the harness treats that as a failed refill-pressure regression even though total loop improves. Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/activity8-high-current-baseline/20260429-200514-2287954/summary.json`, default-gate failure `scratch/ps1-perf-iterate/activity8-high-fgp3-current-test/20260429-200612-2293908/summary.json`, bounded rerun `scratch/ps1-perf-iterate/activity8-high-fgp3-bounded-test/20260429-200724-2301171/summary.json`. | Do not promote plain compact FGP3 for ACTIVITY8 high under the current gate. Revisit with absolute-VBlank tradeoff policy, setup prefetch for the late residual cluster, or pack-time grouping/direct16 so due-miss removal does not shift refill overrun above baseline. |
| 2026-04-29 | `stand4-low-compact-fgp3` | `ps1: convert stand4 low foreground to compact fgp3` | Continue the compact FGP3 host-preprocessing lane on STAND4 low after ACTIVITY8 high exposed a refill-overrun tradeoff. STAND4 low had visible CD/refill pressure, zero due misses, and a predicted strong residual-size cut. | Backed up `generated/ps1/foreground/STND4LOW.FG2`, captured `fgpilot stand4 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND4 low improves `loop_vb 1366 -> 1361`, `target_vb 1217 -> 1216`, `overrun_vb 149 -> 145`, `blocking_vb 8 -> 7`, `prefetch_overrun_vb 8 -> 7`, `loop_reads 37 -> 9`, and `loop_read_vb 170 -> 53`; `due_misses` stays `0`. Payload drops `491890 -> 137496`, and the compact pack drops `495282 -> 140888` bytes. `FG\\STND4LOW.FG2` stays at LBA `52792`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand4-low-current-baseline/20260429-201010-2317151/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand4-low-fgp3-current-test/20260429-201114-2323739/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand4-low-fgp3-canary/20260429-201343-2338151/summary.json`. | Promote. STAND4 high/low are now both compact FGP3. The exact timing-bearing average remains rounded at `17.27%` over target. Latest rows now use `compact-fgp3-v7-stand4low`; continue with STAND6 low, STAND2 high, ACTIVITY8 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `stand6-low-compact-fgp3` | `ps1: convert stand6 low foreground to compact fgp3` | Continue the compact FGP3 host-preprocessing lane on STAND6 low, the next remaining PAL4 FGP2 row with visible CD/refill pressure, no due misses, and a strong residual-size cut. | Backed up `generated/ps1/foreground/STND6LOW.FG2`, captured `fgpilot stand6 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND6 low improves `loop_vb 1505 -> 1500`, `target_vb 1359 -> 1362`, `overrun_vb 146 -> 138`, `blocking_vb 8 -> 3`, `prefetch_overrun_vb 8 -> 3`, `loop_reads 36 -> 10`, and `loop_read_vb 191 -> 46`; `due_misses` stays `0`. Payload drops `512094 -> 148441`, and the compact pack drops `515866 -> 152213` bytes. `FG\\STND6LOW.FG2` stays at LBA `53196`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand6-low-current-baseline/20260429-202033-2377283/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand6-low-fgp3-current-test/20260429-202140-2384045/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand6-low-fgp3-canary/20260429-202247-2391018/summary.json`. | Promote. STAND6 low is now compact FGP3. The exact timing-bearing average improves again to `17.2658%` over target. Latest rows now use `compact-fgp3-v8-stand6low`; continue with STAND2 high, ACTIVITY8 low, VISITOR1 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `stand2-high-compact-fgp3` | `ps1: convert stand2 high foreground to compact fgp3` | Continue the compact FGP3 lane on STAND2 high, a remaining stand-family FGP2 row with visible CD/refill pressure and a predicted strong residual-size cut. | Backed up `generated/ps1/foreground/STAND2.FG2`, captured `fgpilot stand2 lowtide 0 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND2 high improves `loop_vb 633 -> 625`, `target_vb 485 -> 490`, `overrun_vb 148 -> 135`, `blocking_vb 10 -> 0`, `prefetch_overrun_vb 10 -> 0`, `loop_reads 24 -> 6`, and `loop_read_vb 140 -> 31`; `due_misses` stays `0`. Payload drops `329025 -> 91549`, and the compact pack drops `330657 -> 93181` bytes. `FG\\STAND2.FG2` stays at LBA `52310`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand2-high-current-baseline/20260429-204357-2509401/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand2-high-fgp3-current-test/20260429-204449-2514823/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand2-high-fgp3-canary/20260429-204541-2520315/summary.json`. | Promote. STAND2 high now has zero visible CD/refill pressure. The exact timing-bearing average improves to `17.2410%` over target. Latest rows now use `compact-fgp3-v9-stand2high`; continue with VISITOR1 low, STAND2 low, ACTIVITY8 low, STAND16 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `visitor1-low-compact-fgp3` | `ps1: convert visitor1 low foreground to compact fgp3` | Continue the compact FGP3 lane on VISITOR1 low, a remaining VISITOR-family FGP2 row with visible CD/refill pressure and no prior plain-compact rejection. | Backed up `generated/ps1/foreground/VIST1LOW.FG2`, captured `fgpilot visitor1 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. VISITOR1 low improves `loop_vb 809 -> 798`, `target_vb 671 -> 674`, `overrun_vb 138 -> 124`, `blocking_vb 15 -> 7`, `prefetch_overrun_vb 15 -> 7`, `loop_reads 38 -> 19`, and `loop_read_vb 189 -> 99`; `due_misses` stays `0`. Payload drops `477657 -> 246225`, and the compact pack drops `479873 -> 248441` bytes. `FG\\VIST1LOW.FG2` stays at LBA `88215`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/visitor1-low-current-baseline/20260429-212411-2733834/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/visitor1-low-fgp3-current-test/20260429-212508-2739774/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/visitor1-low-fgp3-canary/20260429-212607-2745835/summary.json`. | Promote. VISITOR1 high/low are now both FGP3, with low tide still carrying some visible CD/refill pressure. The exact timing-bearing average improves to `17.2244%` over target. Latest rows now use `compact-fgp3-v10-visitor1low`; continue with STAND2 low, STAND16 low, VISITOR7 low, ACTIVITY8 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `stand2-low-compact-fgp3` | `ps1: convert stand2 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND2 low, the remaining STAND2 tide pair with visible CD/refill pressure and a predicted strong residual-size cut. | Backed up `generated/ps1/foreground/STND2LOW.FG2`, captured `fgpilot stand2 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND2 low improves `loop_vb 632 -> 625`, `target_vb 485 -> 490`, `overrun_vb 147 -> 135`, `blocking_vb 8 -> 0`, `prefetch_overrun_vb 8 -> 0`, `loop_reads 9 -> 3`, and `loop_read_vb 50 -> 13`; `due_misses` stays `0`. Payload drops `138883 -> 48669`, and the compact pack drops `140295 -> 50081` bytes. `FG\\STND2LOW.FG2` stays at LBA `52356`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand2-low-current-baseline/20260429-213142-2778078/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand2-low-fgp3-current-test/20260429-213235-2783629/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand2-low-fgp3-canary/20260429-213335-2789914/summary.json`. | Promote. STAND2 high/low are now both compact FGP3 with zero visible CD/refill pressure. The exact timing-bearing average improves to `17.2014%` over target / `87.1794%` target speed. Latest rows now use `compact-fgp3-v11-stand2low`; continue with STAND16 low, VISITOR7 low, ACTIVITY8 low, STAND15 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `stand16-low-compact-fgp3` | `ps1: convert stand16 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND16 low, a remaining stand-family PAL4 FGP2 row with visible CD/refill pressure and a predicted `~51%` residual-size cut. | Backed up `generated/ps1/foreground/STND16L.FG2`, captured `fgpilot stand16 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND16 low improves `loop_vb 1332 -> 1326`, `target_vb 1189 -> 1191`, `overrun_vb 143 -> 135`, `blocking_vb 14 -> 3`, `prefetch_overrun_vb 7 -> 3`, `due_misses 1 -> 0`, `loop_reads 25 -> 12`, and `loop_read_vb 131 -> 54`. Payload drops `351424 -> 173715`, and the compact pack drops `353984 -> 176275` bytes. `FG\\STND16L.FG2` stays at LBA `54311`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand16-low-current-baseline/20260429-220450-2964439/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand16-low-fgp3-current-test/20260429-220553-2970906/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand16-low-fgp3-canary/20260429-220657-2977491/summary.json`. | Promote. STAND16 high/low are now both FGP3, with low tide still carrying small visible CD/refill pressure. The exact timing-bearing average improves to `17.1950%` over target / `87.1846%` target speed. Latest rows now use `compact-fgp3-v12-stand16low`; continue with VISITOR7 low, ACTIVITY8 low, STAND15 low, VISITOR5 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `stand15-low-compact-fgp3` | `ps1: convert stand15 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND15 low, a remaining stand-family PAL4 FGP2 row with visible CD/refill pressure and a predicted `~54%` residual-size cut. | Backed up `generated/ps1/foreground/STND15L.FG2`, captured `fgpilot stand15 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND15 low improves `loop_vb 1136 -> 1125`, `target_vb 988 -> 986`, `overrun_vb 148 -> 139`, `blocking_vb 11 -> 6`, `prefetch_overrun_vb 11 -> 6`, `loop_reads 19 -> 8`, and `loop_read_vb 105 -> 47`; `due_misses` stays `0`. Payload drops `255955 -> 117968`, and the compact pack drops `258187 -> 120200` bytes. `FG\\STND15L.FG2` stays at LBA `54046`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand15-low-current-baseline/20260429-224518-3190275/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand15-low-fgp3-current-test/20260429-224623-3196911/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand15-low-fgp3-canary/20260429-224723-3203193/summary.json`. | Promote. STAND15 high/low are now both FGP3, with low tide still carrying small visible CD/refill pressure. The exact timing-bearing average improves to `17.1885%` over target / `87.1895%` target speed. Latest rows now use `compact-fgp3-v13-stand15low`; continue with VISITOR7 low, ACTIVITY8 low with explicit tradeoff rules, STAND11 low, STAND7 low, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-29 | `visitor7-low-compact-fgp3` | `ps1: convert visitor7 low foreground to compact fgp3` | Continue the compact FGP3 lane on VISITOR7 low, the next non-rejected PAL4 FGP2 row with visible CD/refill pressure and a predicted `~50%` residual-size cut. | Backed up `generated/ps1/foreground/VIST7LOW.FG2`, captured `fgpilot visitor7 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. VISITOR7 low improves `loop_vb 1780 -> 1768`, `target_vb 1622 -> 1626`, `overrun_vb 158 -> 142`, `blocking_vb 10 -> 1`, `prefetch_overrun_vb 10 -> 1`, `loop_reads 31 -> 16`, and `loop_read_vb 171 -> 77`; `due_misses` stays `0`. Payload drops `449974 -> 223959`, and the compact pack drops `453658 -> 227643` bytes. `FG\\VIST7LOW.FG2` stays at LBA `90779`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/visitor7-low-current-baseline/20260429-232441-3410049/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/visitor7-low-fgp3-current-test/20260429-232557-3417643/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/visitor7-low-fgp3-canary/20260429-232708-3424984/summary.json`. | Promote. VISITOR7 low still carries one visible CD/refill VBlank, but the row is largely cleaned up. The exact timing-bearing average improves to `17.1801%` over target / `87.1966%` target speed. Latest rows now use `compact-fgp3-v14-visitor7low`; continue with STAND11 low, STAND7 low, VISITOR4 low, ACTIVITY8 low with explicit tradeoff rules, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-30 | `stand11-low-compact-fgp3` | `ps1: convert stand11 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND11 low, the next non-rejected PAL4 FGP2 row with visible CD/refill pressure and a predicted `~70%` residual-size cut. | Backed up `generated/ps1/foreground/STND11L.FG2`, captured `fgpilot stand11 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND11 low improves `loop_vb 683 -> 673`, `target_vb 537 -> 538`, `overrun_vb 146 -> 135`, `blocking_vb 6 -> 0`, `prefetch_overrun_vb 6 -> 0`, `loop_reads 10 -> 3`, and `loop_read_vb 55 -> 13`; `due_misses` stays `0`. Payload drops `154846 -> 46264`, and the compact pack drops `156398 -> 47816` bytes. `FG\\STND11L.FG2` stays at LBA `53660`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand11-low-current-baseline/20260430-000518-3636614/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand11-low-fgp3-current-test/20260430-000616-3642615/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand11-low-fgp3-canary/20260430-000709-3648162/summary.json`. | Promote. STAND11 high/low are now both FGP3, with low tide carrying zero visible CD/refill pressure. The exact timing-bearing average improves to `17.1606%` over target / `87.2088%` target speed. Latest rows now use `compact-fgp3-v15-stand11low`; continue with STAND7 low, VISITOR4 low, ACTIVITY8 low with explicit tradeoff rules, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-30 | `stand7-low-compact-fgp3` | `ps1: convert stand7 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND7 low, another remaining stand-family PAL4 FGP2 row with visible CD/refill pressure and a predicted `~71%` residual-size cut. | Backed up `generated/ps1/foreground/STND7LOW.FG2`, captured `fgpilot stand7 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND7 low improves fresh current-layout metrics `loop_vb 678 -> 673`, `target_vb 537 -> 538`, `overrun_vb 141 -> 135`, `blocking_vb 2 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 11 -> 3`, and `loop_read_vb 48 -> 14`; `due_misses` stays `0`. Payload drops `166360 -> 46903`, and the compact pack drops `167912 -> 48455` bytes. `FG\\STND7LOW.FG2` stays at LBA `53158`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand7-low-current-baseline/20260430-004607-3865292/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand7-low-fgp3-current-test/20260430-004701-3870966/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand7-low-fgp3-canary/20260430-004755-3876606/summary.json`. | Promote. STAND7 high/low are now both FGP3, with low tide carrying zero visible CD/refill pressure. The exact timing-bearing average improves to `17.1458%` over target / `87.2181%` target speed. Latest rows now use `compact-fgp3-v16-stand7low`; continue with VISITOR4 low, STAND8 low, ACTIVITY8 low with explicit tradeoff rules, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-30 | `visitor4-low-compact-fgp3` | `ps1: convert visitor4 low foreground to compact fgp3` | Continue the compact FGP3 lane on VISITOR4 low, the remaining VISITOR4 tide pair with visible CD/refill pressure and a predicted `~30%` residual-size cut. | Backed up `generated/ps1/foreground/VIST4LOW.FG2`, captured `fgpilot visitor4 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. VISITOR4 low improves fresh current-layout metrics `loop_vb 584 -> 569`, `target_vb 424 -> 429`, `overrun_vb 160 -> 140`, `blocking_vb 10 -> 0`, `prefetch_overrun_vb 10 -> 0`, `loop_reads 12 -> 8`, and `loop_read_vb 67 -> 41`; `due_misses` stays `0`. Payload drops `162248 -> 112815`, and the compact pack drops `163580 -> 114147` bytes. `FG\\VIST4LOW.FG2` stays at LBA `89563`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/visitor4-low-current-baseline/20260430-012525-4082868/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/visitor4-low-fgp3-current-test/20260430-012616-4088177/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/visitor4-low-fgp3-canary/20260430-012709-4093794/summary.json`. | Promote. VISITOR4 high/low are now both FGP3, with low tide carrying zero visible CD/refill pressure. The exact timing-bearing average improves to `17.1032%` over target / `87.2413%` target speed. Latest rows now use `compact-fgp3-v17-visitor4low`; continue with STAND8 low, STAND1 high, ACTIVITY8 low with explicit tradeoff rules, or another PAL4 row with visible pressure and strong residual savings. |
| 2026-04-30 | `stand1-high-compact-fgp3` | `ps1: convert stand1 high foreground to compact fgp3` | Continue the compact FGP3 lane on STAND1 high, a remaining PAL4 FGP2 row with visible CD/refill pressure and a predicted `~76%` residual-size cut. | Backed up `generated/ps1/foreground/STAND1.FG2`, captured `fgpilot stand1 lowtide 0 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND1 high improves fresh current-layout metrics `loop_vb 347 -> 343`, `target_vb 202 -> 203`, `overrun_vb 145 -> 140`, `blocking_vb 2 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 10 -> 2`, and `loop_read_vb 60 -> 12`; `due_misses` stays `0`. Payload drops `152900 -> 36093`, and the compact pack drops `153672 -> 36865` bytes. `FG\\STAND1.FG2` stays at LBA `52200`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand1-high-current-baseline/20260430-020547-112622/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand1-high-fgp3-current-test/20260430-020633-117615/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand1-high-fgp3-canary/20260430-020719-122408/summary.json`. | Promote. STAND1 high now carries zero visible CD/refill pressure, though it remains heavily over target from non-CD work. The exact timing-bearing average improves to `17.0643%` over target / `87.2546%` target speed. Latest rows now use `compact-fgp3-v18-stand1high`; continue with STAND8 low, STAND9 low, STAND10 low, or ACTIVITY8 low with explicit tradeoff rules. |
| 2026-04-30 | `stand8-low-compact-fgp3` | `ps1: convert stand8 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND8 low, a remaining stand-family PAL4 FGP2 row with visible CD/refill pressure and a predicted `~66%` residual-size cut. | Backed up `generated/ps1/foreground/STND8LOW.FG2`, captured `fgpilot stand8 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND8 low improves fresh current-layout metrics `loop_vb 640 -> 635`, `target_vb 498 -> 499`, `overrun_vb 142 -> 136`, `blocking_vb 3 -> 1`, `prefetch_overrun_vb 3 -> 1`, `loop_reads 10 -> 3`, and `loop_read_vb 54 -> 15`; `due_misses` stays `0`. Payload drops `144793 -> 49688`, and the compact pack drops `146205 -> 51100` bytes. `FG\\STND8LOW.FG2` stays at LBA `53172`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand8-low-current-baseline/20260430-021450-165005/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand8-low-fgp3-current-test/20260430-021547-170990/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand8-low-fgp3-canary/20260430-021640-176488/summary.json`. | Promote. STAND8 high/low are now both FGP3, with low tide still carrying one visible CD/refill VBlank. The exact timing-bearing average improves to `17.0538%` over target / `87.2610%` target speed. Latest rows now use `compact-fgp3-v19-stand8low`; continue with STAND9 low, STAND10 low, or ACTIVITY8 low with explicit tradeoff rules. |
| 2026-04-30 | `stand9-low-compact-fgp3` | `ps1: convert stand9 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND9 low, a remaining stand-family PAL4 FGP2 row with visible CD/refill pressure and a predicted `~71%` residual-size cut. | Backed up `generated/ps1/foreground/STND9LOW.FG2`, captured `fgpilot stand9 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND9 low improves fresh current-layout metrics `loop_vb 679 -> 673`, `target_vb 536 -> 538`, `overrun_vb 143 -> 135`, `blocking_vb 3 -> 0`, `prefetch_overrun_vb 3 -> 0`, `loop_reads 11 -> 3`, and `loop_read_vb 65 -> 14`; `due_misses` stays `0`. Payload drops `161526 -> 47009`, and the compact pack drops `163078 -> 48561` bytes. `FG\\STND9LOW.FG2` stays at LBA `53244`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand9-low-current-baseline/20260430-022224-209464/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand9-low-fgp3-current-test/20260430-022323-215574/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand9-low-fgp3-canary/20260430-022422-221665/summary.json`. | Promote. STAND9 high/low are now both FGP3, with low tide carrying zero visible CD/refill pressure. The exact timing-bearing average improves to `17.0441%` over target / `87.2672%` target speed. Latest rows now use `compact-fgp3-v20-stand9low`; continue with STAND10 low or ACTIVITY8 low with explicit tradeoff rules. |
| 2026-04-30 | `stand10-low-compact-fgp3` | `ps1: convert stand10 low foreground to compact fgp3` | Continue the compact FGP3 lane on STAND10 low as a work-reduction candidate even though the fresh current baseline had no visible refill VBlank. | Backed up `generated/ps1/foreground/STND10L.FG2`, captured `fgpilot stand10 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND10 low improves fresh current-layout metrics `loop_vb 678 -> 673`, `target_vb 537 -> 538`, `overrun_vb 141 -> 135`, `blocking_vb 0 -> 0`, `prefetch_overrun_vb 0 -> 0`, `loop_reads 11 -> 3`, and `loop_read_vb 64 -> 16`; `due_misses` stays `0`. Payload drops `160234 -> 47470`, and the compact pack drops `161786 -> 49022` bytes. `FG\\STND10L.FG2` stays at LBA `53316`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand10-low-current-baseline/20260430-022900-248603/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand10-low-fgp3-current-test/20260430-022959-254666/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand10-low-fgp3-canary/20260430-023050-260060/summary.json`. | Promote. STAND10 high/low are now both FGP3, and low tide remains free of visible CD/refill pressure. The exact timing-bearing average improves to `17.0324%` over target / `87.2745%` target speed. Latest rows now use `compact-fgp3-v21-stand10low`; continue by re-ranking candidates because the obvious stand low-tide FGP2 cleanup lane is nearly exhausted. |
| 2026-04-30 | `activity8-low-compact-fgp3-retry` | Log-only reject; no pack commit. | Test ACTIVITY8 low because it was the next remaining PAL4 FGP2 row with visible CD/refill pressure after the stand low-tide lane, while treating it as high-risk because ACTIVITY8 high already failed plain compact FGP3 on prefetch overrun. | Backed up `generated/ps1/foreground/ACTV8LOW.FG2`, captured `fgpilot activity8 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, ran the default `--require-improvement --work-identity-min 0` gate, then restored the original FGP2 pack. | Rejected. Compact FGP3 gives useful work reductions: `loop_vb 1050 -> 1045`, `target_vb=903`, `overrun_vb 147 -> 142`, `blocking_vb 10 -> 4`, `due_misses 4 -> 0`, `loop_reads 35 -> 12`, and `loop_read_vb 143 -> 44`. Payload drops `412221 -> 171708`, and compact pack bytes would drop `414077 -> 173564`. The failure is the same refill-shape problem as high tide: `prefetch_overrun_vb` regresses `1 -> 4`, so the strict gate rejected it. Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/activity8-low-current-baseline/20260430-024559-344828/summary.json`, default-gate failure `scratch/ps1-perf-iterate/activity8-low-fgp3-current-test/20260430-024658-350931/summary.json`. | Do not promote plain compact FGP3 for ACTIVITY8 low. ACTIVITY8 high/low both need absolute-VBlank tradeoff policy, generated setup/group coverage, or direct16/grouped pack layout before retrying. |
| 2026-04-30 | `fishing7-low-compact-fgp3` | `ps1: convert fishing7 low foreground to compact fgp3` | Continue the compact FGP3 lane on FISHING7 low, a smaller PAL4 FGP2 row with mild visible CD/refill pressure and no prior plain-compact rejection. | Backed up `generated/ps1/foreground/FISH7LOW.FG2`, captured `fgpilot fishing7 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. FISHING7 low improves fresh current-layout metrics `loop_vb 866 -> 859`, `target_vb=725`, `overrun_vb 141 -> 134`, `blocking_vb 2 -> 1`, `prefetch_overrun_vb 2 -> 1`, `loop_reads 21 -> 16`, and `loop_read_vb 100 -> 76`; `due_misses` stays `0`. Payload drops `285009 -> 222034`, and the compact pack drops `287021 -> 224046` bytes. `FG\\FISH7LOW.FG2` stays at LBA `12927`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/fishing7-low-current-baseline/20260430-025023-370952/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/fishing7-low-fgp3-current-test/20260430-025126-377524/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/fishing7-low-fgp3-canary/20260430-025224-383536/summary.json`. | Promote. FISHING7 low still carries one visible CD/refill VBlank, but loop time and read volume both improve. The exact timing-bearing average improves to `17.0243%` over target / `87.2802%` target speed. Latest rows now use `compact-fgp3-v22-fishing7low`; continue with JOHNNY5 high, FISHING4 low, WALKSTUF2 high/low, or a non-plain ACTIVITY8/direct16/grouping follow-up. |
| 2026-04-30 | `johnny5-high-compact-fgp3` | `ps1: convert johnny5 high foreground to compact fgp3` | Continue the compact FGP3 lane on JOHNNY5 high, a mild-pressure PAL4 FGP2 row with enough predicted residual savings to test before work-reduction-only rows. | Backed up `generated/ps1/foreground/JOHNNY5.FG2`, captured `fgpilot johnny5 lowtide 0 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. JOHNNY5 high improves fresh current-layout metrics `loop_vb 955 -> 947`, `target_vb 819 -> 820`, `overrun_vb 136 -> 127`, `blocking_vb 2 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 19 -> 10`, and `loop_read_vb 90 -> 48`; `due_misses` stays `0`. Payload drops `269509 -> 148312`, and the compact pack drops `271369 -> 150172` bytes. `FG\\JOHNNY5.FG2` stays at LBA `14666`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/johnny5-high-current-baseline/20260430-025733-413197/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/johnny5-high-fgp3-current-test/20260430-025838-419794/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/johnny5-high-fgp3-canary/20260430-025938-426012/summary.json`. | Promote. JOHNNY5 high now carries zero visible CD/refill pressure. The exact timing-bearing average improves to `17.0161%` over target / `87.2864%` target speed. Latest rows now use `compact-fgp3-v23-johnny5high`; continue with FISHING4 low, WALKSTUF2 high/low, JOHNNY5 low as work reduction, or a non-plain ACTIVITY8/direct16/grouping follow-up. |
| 2026-04-30 | `fishing4-low-compact-fgp3-retry` | Log-only reject; no pack commit. | Test FISHING4 low because it was the next mild-pressure PAL4 FGP2 row after JOHNNY5 high, with a small visible refill profile and no prior plain-compact rejection. | Backed up `generated/ps1/foreground/FISH4LOW.FG2`, captured `fgpilot fishing4 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, ran the default `--require-improvement --work-identity-min 0` gate, then restored the original FGP2 pack. | Rejected. Compact FGP3 improves `loop_vb 976 -> 970`, `target_vb 842 -> 843`, `overrun_vb 134 -> 127`, `loop_reads 17 -> 12`, and `loop_read_vb 77 -> 54`; `due_misses` stays `0`. Payload drops `236700 -> 167356`, and compact pack bytes would drop `238608 -> 169264`. The strict gate rejects it because visible refill pressure regresses: `blocking_vb 2 -> 3` and `prefetch_overrun_vb 2 -> 3`. Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/fishing4-low-current-baseline/20260430-030444-455404/summary.json`, default-gate failure `scratch/ps1-perf-iterate/fishing4-low-fgp3-current-test/20260430-030550-462104/summary.json`. | Do not promote plain compact FGP3 for FISHING4 low. It needs setup/group coverage or direct16/grouped layout if revisited; do not accept lower loop time by worsening visible refill pressure. |
| 2026-04-30 | `walkstuf2-high-low-compact-fgp3-retry` | Log-only reject; no pack commit. | Test WALKSTUF2 high/low as a pair because `generated/ps1/foreground/WALKSTUF2.FG2` and `WALK2LOW.FG2` were byte-identical FGP2 packs, both measured `loop_vb=596`, and both still had one visible CD/refill VBlank. | Backed up both packs, captured a paired current-layout baseline for `fgpilot walkstuf2 lowtide 0/1 ... perf-log noloop seed 1`, converted both with `scripts/build-fg3-temporal-residual-pack.py`, ran the default paired `--require-improvement --work-identity-min 0` gate, then restored both original FGP2 packs. | Rejected. High tide improves `loop_vb 596 -> 593` and `overrun_vb 136 -> 133` but keeps `blocking_vb/prefetch_overrun_vb=1`, raises reads `9 -> 12`, and changes pack sectors `68 -> 80`. Low tide improves `loop_vb 596 -> 595`, `overrun_vb 136 -> 135`, and `blocking_vb/prefetch_overrun_vb 1 -> 0`, but also raises reads `9 -> 12`, changes sectors `68 -> 80`, and shifts the low-tide pack LBA `95847 -> 95859`. Payload grows `137337 -> 161895`, and each pack would grow `138609 -> 163167` bytes. Artifacts: fresh paired FGP2 baseline `scratch/ps1-perf-iterate/walkstuf2-current-baseline/20260430-032748-583373/summary.json`, default-gate failure `scratch/ps1-perf-iterate/walkstuf2-fgp3-current-test/20260430-032932-594145/summary.json`. | Do not promote plain compact FGP3 for WALKSTUF2. Retry only with generated grouping/direct16 or a residual policy that first proves lower pack bytes and lower read count; this pair is a poor fit for naive temporal residual encoding. |
| 2026-04-30 | `johnny5-low-compact-fgp3` | `ps1: convert johnny5 low foreground to compact fgp3` | Continue the compact FGP3 lane on JOHNNY5 low after re-ranking the remaining FGP2 rows. The high-tide pack already proved this art compresses cleanly, and the fresh low-tide current-layout baseline still had mild CD/refill pressure. | Backed up `generated/ps1/foreground/JOHN5LOW.FG2`, captured `fgpilot johnny5 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. JOHNNY5 low improves fresh current-layout metrics `loop_vb 955 -> 947`, `target_vb 819 -> 820`, `overrun_vb 136 -> 127`, `blocking_vb 2 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 19 -> 10`, and `loop_read_vb 53 -> 44`; `due_misses` stays `0`. Payload drops `269509 -> 148312`, and the compact pack drops `271369 -> 150172` bytes. `FG\\JOHN5LOW.FG2` stays at LBA `14740`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/johnny5-low-current-baseline/20260430-033350-619079/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/johnny5-low-fgp3-current-test/20260430-033451-625278/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/johnny5-low-fgp3-canary/20260430-033548-631271/summary.json`. | Promote. JOHNNY5 high/low now both carry zero visible CD/refill pressure. The exact timing-bearing average improves to `17.0090%` over target / `87.2917%` target speed. Latest rows now use `compact-fgp3-v24-johnny5low`; continue by prioritizing remaining FGP2 rows with measurable pressure, then switch to grouped/direct16 or host-side preprocessing for rows where plain residuals fail or grow. |
| 2026-04-30 | `building3-low-compact-fgp3-retry` | Log-only reject; no pack commit. | Test BUILDING3 low after re-ranking remaining FGP2 rows. The host residual probe predicted `~51%` payload reduction, and the fresh current-layout baseline still had mild visible refill pressure despite the older matrix row showing none. | Backed up `generated/ps1/foreground/BUIL3LOW.FG2`, captured `fgpilot building3 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, ran the default `--require-improvement --work-identity-min 0` gate, then restored the original FGP2 pack. | Rejected. Compact FGP3 improves work volume and total loop shape: `loop_vb 1572 -> 1570`, `target_vb=1433`, `overrun_vb 139 -> 137`, `loop_reads 39 -> 18`, and `loop_read_vb 194 -> 94`; `due_misses` stays `0`. Payload drops `534237 -> 260324`, and compact pack bytes would drop `537821 -> 263908`. The strict gate rejects it because visible refill pressure regresses: `blocking_vb 2 -> 7` and `prefetch_overrun_vb 2 -> 7`. Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/building3-low-current-baseline/20260430-040641-801648/summary.json`, default-gate failure `scratch/ps1-perf-iterate/building3-low-fgp3-current-test/20260430-040749-808487/summary.json`. | Do not promote plain compact FGP3 for BUILDING3 low. Retry only with generated setup/group coverage, direct16/grouped layout, or a scheduler rule that can keep the now larger residual transaction from crossing held slack. |
| 2026-04-30 | `johnny4-high-compact-fgp3` | `ps1: convert johnny4 high foreground to compact fgp3` | Test JOHNNY4 after the host residual probe showed a `~53%` payload reduction and JOHNNY5 high/low had both promoted cleanly. Because `JOHNNY4.FG2` and `JOHN4LOW.FG2` started byte-identical, first test the pair, then keep only the safe half if one tide regresses. | Backed up both packs, captured a paired `fgpilot johnny4 lowtide 0/1 ... perf-log noloop seed 1` baseline, converted both packs, rejected the paired shape because low-tide FGP3 added one visible refill VBlank, restored `JOHN4LOW.FG2` to FGP2, reran the high-only layout with low-tide as a non-regressing dependent route, and cross-gated FISHING1 high against the latest canary. | Promoted high-only. JOHNNY4 high improves `loop_vb 1349 -> 1342`, `target_vb 1213 -> 1214`, `overrun_vb 136 -> 128`, `blocking_vb/prefetch_overrun_vb` stays `1`, `loop_reads 27 -> 12`, and `loop_read_vb 125 -> 50`; `due_misses` stays `0`. Payload drops `367255 -> 171863`, and `JOHNNY4.FG2` drops `369195 -> 173803` bytes. The dependent low route stays FGP2 and flat under the shifted layout (`loop_vb 1349 -> 1349`, `overrun_vb=135`, zero pressure, LBA `14485 -> 14389`). FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: paired baseline `scratch/ps1-perf-iterate/johnny4-current-baseline/20260430-041057-826981/summary.json`, rejected paired FGP3 run `scratch/ps1-perf-iterate/johnny4-fgp3-current-test/20260430-041300-839480/summary.json`, accepted high-only paired route run `scratch/ps1-perf-iterate/johnny4-high-only-fgp3-flat-test/20260430-041715-865473/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/johnny4-high-fgp3-canary/20260430-041916-877722/summary.json`. | Promote high-only. Do not promote plain compact FGP3 for JOHNNY4 low; it needs grouping/setup/direct16 if revisited. The exact timing-bearing average improves to `17.0042%` over target / `87.2956%` target speed. Latest rows now use `compact-fgp3-v25-johnny4high`; continue with remaining no-pressure FGP2 rows only if they have strong shrink and dependent-route checks, otherwise move to grouped/direct16/host-preprocessing work. |
| 2026-04-30 | `stand3-low-compact-fgp3-retry` | Log-only reject; no pack commit. | Test STAND3 low because it was the remaining STAND3 tide pair with a strong host residual ratio and the stand-family FGP3 lane had been reliable. The acceptance bar was stricter because the fresh baseline had zero visible refill pressure. | Backed up `generated/ps1/foreground/STND3LOW.FG2`, captured `fgpilot stand3 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, ran the default `--require-improvement --work-identity-min 0` gate, then restored the original FGP2 pack. | Rejected. Compact FGP3 improves `loop_vb 697 -> 693`, `target_vb 557 -> 556`, `overrun_vb 140 -> 137`, `loop_reads 11 -> 3`, and `loop_read_vb 64 -> 12`; `due_misses` stays `0`. Payload drops `167222 -> 47163`, and compact pack bytes would drop `168834 -> 48775`. The strict gate rejects it because it introduces visible refill pressure: `blocking_vb 0 -> 1` and `prefetch_overrun_vb 0 -> 1`. Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand3-low-current-baseline/20260430-042522-912379/summary.json`, default-gate failure `scratch/ps1-perf-iterate/stand3-low-fgp3-current-test/20260430-042621-918512/summary.json`. | Do not promote plain compact FGP3 for STAND3 low. It is a good candidate for generated setup/group coverage because the core work reduction is real, but zero-pressure rows must remain zero-pressure. |
| 2026-04-30 | `stand1-low-compact-fgp3` | `ps1: convert stand1 low foreground to compact fgp3` | Test STAND1 low as a small stand-family zero-pressure row after STAND3 low showed the risk. Because the baseline had no visible refill pressure, any new `blocking_vb` or `prefetch_overrun_vb` would reject the conversion. | Backed up `generated/ps1/foreground/STND1LOW.FG2`, captured `fgpilot stand1 lowtide 1 ... perf-log noloop seed 1` as the current baseline, converted the pack with `scripts/build-fg3-temporal-residual-pack.py`, gated it with `--require-improvement --work-identity-min 0`, and cross-gated FISHING1 high against the latest canary. | Promoted. STAND1 low improves `loop_vb 347 -> 343`, `target_vb=203`, `overrun_vb 144 -> 140`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads 4 -> 1`, and `loop_read_vb 20 -> 5`; `due_misses` stays `0`. Payload drops `68706 -> 18535`, and the compact pack drops `69358 -> 19187` bytes. `FG\\STND1LOW.FG2` stays at LBA `51974`, PS-EXE stays `147456`, and FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh FGP2 baseline `scratch/ps1-perf-iterate/stand1-low-current-baseline/20260430-042939-937986/summary.json`, accepted compact FGP3 run `scratch/ps1-perf-iterate/stand1-low-fgp3-current-test/20260430-043029-943311/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/stand1-low-fgp3-canary/20260430-043122-948853/summary.json`. | Promote. STAND1 high/low are now both compact FGP3 and zero-pressure. The exact timing-bearing average improves to `16.9877%` over target / `87.3013%` target speed. Latest rows now use `compact-fgp3-v26-stand1low`; continue with the remaining plain residual candidates only when zero-pressure can be preserved, otherwise move to generated grouping/setup metadata. |
| 2026-04-30 | `miscgag2-high-low-compact-fgp3` | `ps1: convert miscgag2 foregrounds to compact fgp3` | Test MISCGAG2 high/low as a paired plain-residual conversion because the source packs were byte-identical, the host probe predicted a `42.5%` size cut, and both routes were near or under target with little refill pressure. | Backed up `generated/ps1/foreground/MISCGAG2.FG2` and `MISC2LOW.FG2`, captured a paired current-layout FGP2 baseline, converted both packs with `scripts/build-fg3-temporal-residual-pack.py`, reran the paired gate with `--allow-layout-change` because the low-tide LBA shifts behind the shrunk high-tide pack, and cross-gated FISHING1 high against the latest canary. | Promoted. High tide improves `loop_vb 1358 -> 1350`, `target_vb=1354`, `overrun_vb 4 -> 0`, `blocking_vb 1 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 36 -> 16`, and `loop_read_vb 168 -> 91`. Low tide improves `loop_vb 1356 -> 1350`, `target_vb=1355`, `overrun_vb 1 -> 0`, `loop_reads 36 -> 16`, and `loop_read_vb 164 -> 74` while keeping zero visible pressure. Each pack shrinks `394112 -> 226762` bytes. FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh paired FGP2 baseline `scratch/ps1-perf-iterate/miscgag2-current-baseline/20260430-044659-1035882/summary.json`, accepted layout-aware FGP3 run `scratch/ps1-perf-iterate/miscgag2-fgp3-layout-test/20260430-045150-1064966/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/miscgag2-fgp3-canary/20260430-045344-1076685/summary.json`. | Promote. MISCGAG2 high/low now both run at or faster than target with zero visible refill pressure. The exact timing-bearing average improves to `16.9810%` over target / `87.3085%` target speed. Latest rows now use `compact-fgp3-v27-miscgag2`; continue prioritizing remaining FGP2 rows where plain residual conversion preserves zero-pressure rows, otherwise move to grouped/direct16 host-side formats. |
| 2026-04-30 | `fishing5-high-low-compact-fgp3` | `ps1: convert fishing5 foregrounds to compact fgp3` | Test FISHING5 high/low as a paired plain-residual conversion because the source packs were byte-identical and the host probe predicted a `29.8%` size cut. The row is already faster than target, so the strict bar was lower loop time plus no new visible refill pressure. | Backed up `generated/ps1/foreground/FISHING5.FG2` and `FISH5LOW.FG2`, captured a paired current-layout FGP2 baseline, converted both packs with `scripts/build-fg3-temporal-residual-pack.py`, reran the paired gate with `--allow-layout-change` because the low-tide LBA shifts behind the shrunk high-tide pack, and cross-gated FISHING1 high against the latest canary. | Promoted. High tide improves `loop_vb 809 -> 803`, `target_vb 889 -> 890`, `blocking_vb 2 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 13 -> 9`, and `loop_read_vb 62 -> 44`. Low tide improves `loop_vb 807 -> 803`, `target_vb=890`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads 13 -> 9`, and `loop_read_vb 55 -> 42`. Each pack shrinks `185593 -> 130636` bytes. FISHING1 high stays exact-flat (`loop_vb=1207`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`). Artifacts: fresh paired FGP2 baseline `scratch/ps1-perf-iterate/fishing5-current-baseline/20260430-050334-1132381/summary.json`, accepted layout-aware FGP3 run `scratch/ps1-perf-iterate/fishing5-fgp3-current-test/20260430-050524-1143746/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/fishing5-fgp3-canary/20260430-050722-1155867/summary.json`. | Promote. FISHING5 high/low now both run well under target with zero visible refill pressure. The exact timing-bearing average improves to `16.9725%` over target / `87.3189%` target speed. Latest rows now use `compact-fgp3-v28-fishing5`; continue with remaining FGP2 rows only when plain residual conversion preserves pressure, otherwise move to grouped/direct16 host-side formats. |
| 2026-04-30 | `small-fgp3-setup-prime` | `ps1: prime small fgp3 packs at setup` | Retry the STAND3 low residual win with generated residency instead of another plain compact conversion. The previous STAND3 low FGP3 attempt proved the work reduction was real but introduced one visible refill VBlank; the hypothesis was that any FGP3 payload window at or below 64 KiB can be setup-primed through the existing stream-window path, turning loop-time reads into setup-time reads while preserving zero-pressure rows. | Added runtime small-FGP3 setup priming, taught `scripts/ps1-foreground-read-plan.py` to report `auto:fgp3-small-pack`, converted `generated/ps1/foreground/STND3LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated STAND3 low with `--allow-layout-change`, cross-gated FISHING1 high, then refreshed the other small-FGP3 stand variants affected by the generic policy. | Promoted. STAND3 low improves `loop_vb 697 -> 694`, `overrun_vb 140 -> 136`, and `loop_reads 11 -> 0` while preserving `blocking_vb=0` and `prefetch_overrun_vb=0`; its pack shrinks `168834 -> 48775` bytes. The refresh removes loop reads for STAND1 high/low, STAND2 low, STAND7 low, STAND8 low, STAND9 low, STAND10 low, and STAND11 low; STAND8 low also drops visible pressure `1 -> 0`. FISHING1 high remains exact-flat at `1207/1076`, `blocking_vb=0`, `prefetch_overrun_vb=0`, LBA `401`. Artifacts: accepted STAND3 low `scratch/ps1-perf-iterate/stand3-low-smallprime-final/20260430-053336-1302142/summary.json`, canary `scratch/ps1-perf-iterate/stand3-low-smallprime-canary/20260430-053428-1307600/summary.json`, affected-row refresh `scratch/ps1-perf-iterate/small-fgp3-prime-refresh/20260430-053549-1315712/summary.json`. | Promote. Small FGP3 packs now use setup-time residency when the payload window fits in 64 KiB. The exact timing-bearing average improves to `16.9692%` over target / `87.3226%` target speed. Latest rows now use `compact-fgp3-v29-smallprime`; continue with larger failed FGP3 candidates only when generated setup/group coverage can preserve or reduce visible pressure. |
| 2026-04-30 | `medium-fgp3-setup-prime` | `ps1: setup-prime medium fgp3 packs` | Raise the generic FGP3 setup-prime ceiling from `64 KiB` to `192 KiB` after the small-pack lane proved setup-time residency could preserve zero-pressure rows. Hypothesis: medium residual packs can move CD work out of active loops without touching pixels. | Raised the runtime auto-pack setup-prime limit to `192 KiB`, renamed the policy constant/read-plan label away from the now-misleading small-pack wording, probed ACTIVITY7/STAND4/VISITOR4/JOHNNY2 plus FISHING canaries, then refreshed the 27 affected medium-FGP3 rows with `scripts/ps1-perf-iterate.sh --skip-build --frames 7200 --timeout 220`. | Promoted. Across the affected refresh, `loop_vb` totals improve by `19`, `blocking_vb` and `prefetch_overrun_vb` each drop by `54`, `loop_reads` drop by `232`, and `loop_read_vb` drops by `1142`; 13 rows improve, 9 stay flat, and 5 under-target rows regress by only `1..4` VBlanks. ACTIVITY7 high improves `750/612 -> 746/615`, STAND4 low `1361/1216 -> 1358/1221`, MISCGAG1 low `1101/959 -> 1096/961`, and FISHING1 high stays exact-flat at `1207/1076`. Artifacts: probe `scratch/ps1-perf-iterate/medium-fgp3-prime-probe/20260430-060749-1494632/summary.json`, affected-row refresh `scratch/ps1-perf-iterate/medium-fgp3-prime-refresh/20260430-061351-1531167/summary.json`, post-rename canary `scratch/ps1-perf-iterate/medium-fgp3-prime-postrename-canary/20260430-064608-1719301/summary.json`, post-rename generic sanity `scratch/ps1-perf-iterate/medium-fgp3-prime-postrename-generic/20260430-064727-1727224/summary.json`. | Promote. Medium FGP3 packs now use setup-time residency when the payload window fits in `192 KiB`. The exact timing-bearing average improves to `16.9139%` over target / `87.3593%` target speed. Latest rows now use `compact-fgp3-v30-mediumprime`; continue with generated setup/group coverage for larger FGP3 failures or direct16/host-side layouts. |
| 2026-04-30 | `auto-fgp3-prime-224` | `ps1: setup-prime 224k fgp3 packs` | Raise the generic FGP3 setup-prime ceiling from `192 KiB` to `224 KiB` to cover the next medium-pack band without adding a bespoke per-scene table. Hypothesis: these rows can move their remaining loop-time reads into setup while FISHING1 remains exact-flat and visible refill pressure does not increase. | Raised `FG_SETUP_PRIME_AUTO_PACK_BYTES` to `224 KiB`, probed STAND15 high, ACTIVITY11 high/low, JOHNNY3 low, and FISHING1 high, then refreshed the 9 newly affected rows with `scripts/ps1-perf-iterate.sh --skip-build --frames 7200 --timeout 220`. | Promoted. Across the affected refresh, `loop_vb` totals improve by `16`, `blocking_vb` drops by `31`, `prefetch_overrun_vb` drops by `21`, `loop_reads` drops by `133`, and `loop_read_vb` drops by `646`; 7 rows improve and 2 already-under-target MISCGAG2 rows regress by only `2` VBlanks while remaining faster than target with zero pressure. ACTIVITY11 high/low improve `1862/1724 -> 1859/1725`, JOHNNY3 low `1303/1165 -> 1298/1166`, STAND15 high `1127/986 -> 1123/990`, and FISHING1 high stays exact-flat at `1207/1076`. Artifacts: probe `scratch/ps1-perf-iterate/auto-fgp3-prime-224-probe/20260430-065255-1759146/summary.json`, affected-row refresh `scratch/ps1-perf-iterate/auto-fgp3-prime-224-refresh/20260430-065815-1791215/summary.json`. | Promote. FGP3 packs now use setup-time residency when the payload window fits in `224 KiB`. The exact timing-bearing average improves to `16.8958%` over target / `87.3736%` target speed. Latest rows now use `compact-fgp3-v31-auto224`; continue with generated setup/group coverage for larger FGP3 failures or direct16/host-side layouts. |
| 2026-04-30 | `auto-fgp3-prime-256` | `ps1: setup-prime 256k fgp3 packs` | Raise the generic FGP3 setup-prime ceiling from `224 KiB` to `256 KiB` to cover the next four compact FGP3 rows. The acceptance bar is zero visible-pressure regression and exact-flat FISHING1/FISHING3 canaries, because this is still a global threshold in the foreground hot file. | Raised `FG_SETUP_PRIME_AUTO_PACK_BYTES` to `256 KiB`, then probed FISHING6 low, MISCGAG1 high, STAND4 high, VISITOR1 low, FISHING1 high, and FISHING3 high with `scripts/ps1-perf-iterate.sh --frames 7200 --timeout 220`. | Promoted. The four newly covered rows improve by `26` loop VBlanks total, remove `31` blocking VBlanks, remove `31` prefetch-overrun VBlanks, remove `69` loop reads, and remove `339` loop-read VBlanks. FISHING6 low improves `894/753 -> 890/753`, MISCGAG1 high `1105/957 -> 1097/961`, STAND4 high `1361/1217 -> 1359/1221`, and VISITOR1 low `798/674 -> 794/677`; all four now have zero loop reads and zero visible pressure. FISHING1 high stays exact-flat at `1207/1076`, and FISHING3 high stays exact-flat at `2095/1952` with its existing `blocking_vb=20`. Artifact: `scratch/ps1-perf-iterate/auto-fgp3-prime-256-probe/20260430-071322-1877902/summary.json`. | Promote. FGP3 packs now use setup-time residency when the payload window fits in `256 KiB`. The exact timing-bearing average improves to `16.8667%` over target / `87.3953%` target speed. Latest rows now use `compact-fgp3-v32-auto256`; the next threshold band is larger and should be probed cautiously with canaries, or replaced by generated segmented setup plans if heap/setup cost climbs too far. |
| 2026-04-30 | `auto-fgp3-prime-288` | `ps1: setup-prime 288k fgp3 packs` | Raise the generic FGP3 setup-prime ceiling from `256 KiB` to `288 KiB` to cover the next eight compact FGP3 rows. The hypothesis remains that setup-time residency can remove loop-time CD pressure without pixel changes, but the canary bar stays strict because larger setup reads can still move timing phase. | Raised `FG_SETUP_PRIME_AUTO_PACK_BYTES` to `288 KiB`, then probed ACTIVITY10 high, FISHING4 high, FISHING8 low, STAND5 high, STAND6 high, STAND12 high, STAND16 high, VISITOR6 low, FISHING1 high, and FISHING3 high. | Promoted. The eight newly covered rows improve by `28` loop VBlanks total, remove `55` blocking VBlanks, remove `49` prefetch-overrun VBlanks, remove `149` loop reads, and remove `752` loop-read VBlanks. ACTIVITY10 high improves `1406/1257 -> 1399/1262`, FISHING8 low `1388/1248 -> 1380/1253`, STAND12 high `1599/1453 -> 1594/1461`, and VISITOR6 low `2190/2048 -> 2188/2048`; all newly covered rows now have zero loop reads and zero visible pressure. FISHING1 high stays exact-flat at `1207/1076`, and FISHING3 high stays exact-flat at `2095/1952`. Artifact: `scratch/ps1-perf-iterate/auto-fgp3-prime-288-probe/20260430-072355-1940117/summary.json`. | Promote. FGP3 packs now use setup-time residency when the payload window fits in `288 KiB`. The exact timing-bearing average improves to `16.8301%` over target / `87.4251%` target speed. Latest rows now use `compact-fgp3-v33-auto288`; continue threshold probing only with canaries and be ready to switch to generated segmented setup metadata before the next heap/setup knee. |
| 2026-04-30 | `auto-fgp3-prime-320` | Log-only reject; no source promotion. | Probe the next automatic setup-prime ceiling after the successful `288 KiB` band. The candidate would cover ACTIVITY4 high, ACTIVITY10 low, FISHING1 low, FISHING6 high, JOHNNY3 high, and VISITOR1 high, but must preserve normal active-loop timing and canary behavior. | Temporarily raised `FG_SETUP_PRIME_AUTO_PACK_BYTES` to `320 KiB` and started a multi-case probe. The run was stopped after ACTIVITY10 low emitted structurally invalid active-loop metrics; source was reverted to `288 KiB`. | Rejected. ACTIVITY4 high locally improved cleanly (`1206/1066 -> 1202/1069`, `blocking_vb/prefetch_overrun_vb 6 -> 0`, `loop_reads 20 -> 0`), but ACTIVITY10 low reported `loop_start=0`, `loop_vb=0`, `target_vb=10`, `advances=0`, and only `entries=2` despite `expected_frames=256`. That means the scene did not actually execute the active playback loop, so the metric is invalid. Artifacts: partial run `scratch/ps1-perf-iterate/auto-fgp3-prime-320-probe/20260430-073851-2028736/`, ACTIVITY10 low summary `scratch/ps1-perf-iterate/auto-fgp3-prime-320-probe/20260430-073851-2028736/activity10-low/perf-summary.json`. | Do not promote a blind `320 KiB` auto threshold. Retry this band only after adding a gate that rejects `loop_start=0` / `advances=0` for timing-bearing rows, and prefer generated per-scene setup metadata so ACTIVITY4 high can be captured without taking ACTIVITY10 low through the bad setup path. |
| 2026-04-30 | `perf-harness-active-loop-structural-gate` | `ps1: reject zero-loop perf summaries` | The `320 KiB` setup-prime reject exposed a harness gap: a case could emit `JCPERF2` with clean correctness counters while never entering the active foreground loop. Future experiments must fail that shape automatically instead of relying on manual review. | Added a structural gate in `scripts/ps1-perf-iterate.sh` requiring nonzero `loop_start`, `loop_vb`, and `advances`, plus complete entry and final-frame coverage for routed active scenes. Known metadata-only coverage gaps (`mary3`, `suzy1`, `suzy2`) remain warnings so the full-scene matrix can still record them without treating them as speed evidence. | Promoted as harness correctness. A replay scan of existing passing summaries found exactly one non-allowlisted case that the new rule would reject: the already-rejected `activity10-low` zero-loop summary from `auto-fgp3-prime-320`. Syntax checks passed for the updated harness. | Promote. This is not a runtime speed win, but it prevents invalid active-loop metrics from becoming a future baseline and keeps the next setup-prime/generation experiments honest. |
| 2026-04-30 | `visitor1-high-setup-prime` | `ps1: setup-prime visitor1 high foreground` | The rejected blind `320 KiB` threshold showed the next band contains useful wins, but ACTIVITY10 low cannot go through that path. VISITOR1 high was the highest-pressure safe row in the `288..320 KiB` FGP3 payload-window band, so test a single high-tide setup-prime budget instead of widening the global threshold. | Captured a fresh VISITOR1 high baseline, added a `296 KiB` high-tide setup-prime budget plus read-plan awareness, gated VISITOR1 high with `--require-improvement`, then cross-gated FISHING1 high and FISHING3 high against the `compact-fgp3-v33-auto288` canary summary. The CSV also refreshed ACTIVITY4 high, which is now covered by the accepted `288 KiB` automatic resident-pack rule. | Promoted. VISITOR1 high improves `loop_vb 812 -> 804`, `target_vb 671 -> 677`, `overrun_vb 141 -> 127`, `blocking_vb/prefetch_overrun_vb 13 -> 0`, `loop_reads 23 -> 0`, and `loop_read_vb 104 -> 0`; `due_misses` stays `0`. Layout stays stable (`FG\\VISITOR1.FG2` LBA `86996`, PS-EXE `147456`). FISHING1 high stays exact-flat at `1207/1076`, and FISHING3 high stays exact-flat at `2095/1952`. Artifacts: baseline `scratch/ps1-perf-iterate/visitor1-high-current-baseline/20260430-080941-2201406/summary.json`, accepted run `scratch/ps1-perf-iterate/visitor1-high-setupprime-test/20260430-081117-2210710/summary.json`, canaries `scratch/ps1-perf-iterate/visitor1-high-setupprime-canaries/20260430-081221-2217295/summary.json`, ACTIVITY4 refresh `scratch/ps1-perf-iterate/activity4-current-check/20260430-080806-2192058/summary.json`. | Promote. The exact timing-bearing average improves to `16.8055%` over target / `87.4427%` target speed. Latest rows now use `compact-fgp3-v34-visitor1high-prime`; continue the band with targeted/generated setup-prime coverage for FISHING6 high or JOHNNY3 high, not a blind threshold that re-includes ACTIVITY10 low. |
| 2026-04-30 | `fishing6-high-setup-prime` | `ps1: setup-prime fishing6 high foreground` | Continue the targeted `288..320 KiB` setup-prime band without taking the rejected blind `320 KiB` global threshold. FISHING6 high had a compact FGP3 pack just above the generic `288 KiB` ceiling and still carried loop-time CD pressure. | Captured a fresh explicit-loop FISHING6 high baseline, added a `312 KiB` high-tide setup-prime budget plus read-plan awareness, gated FISHING6 high with `--require-improvement`, then cross-gated FISHING1 high and FISHING3 high against the latest canary summary. | Promoted. FISHING6 high improves `loop_vb 892 -> 890`, `target_vb 751 -> 753`, `overrun_vb 141 -> 137`, `blocking_vb/prefetch_overrun_vb 4 -> 0`, `loop_reads 22 -> 0`, and `loop_read_vb 117 -> 0`; `due_misses` stays `0`. Layout stays stable (`FG\\FISHING6.FG2` LBA `12430`, PS-EXE `147456`). FISHING1 high stays exact-flat at `1207/1076`, and FISHING3 high stays exact-flat at `2095/1952`. Artifacts: baseline `scratch/ps1-perf-iterate/fishing6-high-current-baseline/20260430-082200-2270895/summary.json`, accepted run `scratch/ps1-perf-iterate/fishing6-high-setupprime-test/20260430-082327-2279587/summary.json`, canaries `scratch/ps1-perf-iterate/fishing6-high-setupprime-canaries/20260430-082430-2285971/summary.json`. | Promote. The exact timing-bearing average improves to `16.7982%` over target / `87.4479%` target speed. Latest rows now use `compact-fgp3-v35-fishing6high-prime`; continue the band with generated/targeted setup-prime coverage, with JOHNNY3 high as the next likely safe candidate. |
| 2026-04-30 | `johnny3-high-setup-prime` | `ps1: setup-prime johnny3 high foreground` | Continue the targeted setup-prime band with JOHNNY3 high, another compact FGP3 pack just above the generic `288 KiB` ceiling. The fresh baseline still had a due miss and visible CD pressure. | Captured a fresh JOHNNY3 high baseline, added a `312 KiB` high-tide setup-prime budget plus read-plan awareness, gated JOHNNY3 high with `--require-improvement`, then cross-gated FISHING1 high, FISHING3 high, and FISHING6 high. | Promoted. JOHNNY3 high improves `loop_vb 1299 -> 1298`, `target_vb 1165 -> 1166`, `overrun_vb 134 -> 132`, `blocking_vb 7 -> 0`, `prefetch_overrun_vb 1 -> 0`, `loop_reads 23 -> 0`, and `due_misses 1 -> 0`. Layout stays stable (`FG\\JOHNNY3.FG2` LBA `13985`, PS-EXE `147456`). FISHING1 and FISHING3 high stay exact-flat, and FISHING6 high remains zero-pressure. Artifacts: baseline `scratch/ps1-perf-iterate/johnny3-high-current-baseline/20260430-083131-2325724/summary.json`, accepted run `scratch/ps1-perf-iterate/johnny3-high-setupprime-test/20260430-083259-2334490/summary.json`, canaries `scratch/ps1-perf-iterate/johnny3-high-setupprime-canaries/20260430-083407-2341356/summary.json`. | Promote. The local current-baseline win is real; the rounded matrix average remains `+16.8%` over target / `87.4%` target speed, with exact values `16.7983%` and `87.4478%` after refreshing the stale row. Latest rows now use `compact-fgp3-v36-johnny3high-prime`; continue with generated/targeted setup coverage or switch to a larger host-side format experiment for bigger gains. |
| 2026-04-30 | `fishing7-high-setup-prime` | `ps1: setup-prime fishing7 high foreground` | Continue the targeted setup-prime band with FISHING7 high, a compact FGP3 pack above the generic `288 KiB` ceiling that still had visible refill pressure. The analyzer showed `328 KiB` covers the active-loop reads while `320 KiB` leaves a tail uncovered. | Captured a fresh FISHING7 high baseline, added a `328 KiB` high-tide setup-prime budget plus read-plan awareness, gated FISHING7 high with `--require-improvement`, then cross-gated FISHING1 high, FISHING3 high, FISHING6 high, and JOHNNY3 high. | Promoted. FISHING7 high improves `loop_vb 863 -> 858`, `target_vb=725`, `overrun_vb 138 -> 133`, `blocking_vb/prefetch_overrun_vb 5 -> 0`, `loop_reads 26 -> 0`, and `loop_read_vb 124 -> 0`; `due_misses` stays `0`. Layout stays stable (`FG\\FISHING7.FG2` LBA `12708`, PS-EXE `147456`). FISHING1/FISHING3 stay exact-flat, and FISHING6/JOHNNY3 high remain zero-pressure. Artifacts: baseline `scratch/ps1-perf-iterate/fishing7-high-current-baseline/20260430-084013-2376615/summary.json`, accepted run `scratch/ps1-perf-iterate/fishing7-high-setupprime-test/20260430-084140-2385327/summary.json`, canaries `scratch/ps1-perf-iterate/fishing7-high-setupprime-canaries/20260430-084240-2391553/summary.json`. | Promote. The exact timing-bearing average improves to `16.7912%` over target / `87.4529%` target speed. Latest rows now use `compact-fgp3-v37-fishing7high-prime`; remaining setup-prime candidates should be treated individually because ACTIVITY10 low is still the known bad case for a blind `320 KiB` threshold. |
| 2026-04-30 | `visitor5-low-setup-prime-396` | Log-only reject; no source promotion. | Test whether the targeted setup-prime lane can scale to a higher-pressure `396 KiB` low-tide pack. VISITOR5 low had a fresh baseline of `loop_vb=1248`, `target_vb=1086`, `blocking_vb=54`, `prefetch_overrun_vb=21`, `loop_reads=29`, and `due_misses=6`, so a successful resident setup path would be material. | Captured a fresh VISITOR5 low baseline, temporarily added a `396 KiB` low-tide setup-prime budget plus read-plan awareness, and gated it with `--require-improvement`. The source probe was reverted after the gate failed. | Rejected. The run reproduced the structural zero-loop failure shape: `loop_start=0`, `loop_vb=0`, `advances=0`, `render=0`, `entries=2` of `204`, `last_frame=0`, and `target_vb=11`. The read-plan confirmed the payload window was resident (`prime=[2,198]`), so the failure is not proof that the CD coverage is useless; it means this large setup window breaks active-loop execution before meaningful timing. Artifacts: baseline `scratch/ps1-perf-iterate/visitor5-low-current-baseline/20260430-085343-2454099/summary.json`, rejected run `scratch/ps1-perf-iterate/visitor5-low-setupprime-test/20260430-085513-2462956/visitor5-low/perf-summary.json`. | Do not promote. Retry VISITOR5 low only with segmented setup coverage, inter-scene preload, or a larger-buffer diagnosis that explains why this scene exits before active playback under a full-pack resident window. |
| 2026-04-30 | `activity12-high-setup-prime` | `ps1: setup-prime activity12 high foreground` | Continue the targeted setup-prime lane in a safer size band after VISITOR5 low showed the large full-pack window failure. ACTIVITY12 high had a `338314` byte compact FGP3 pack and visible refill pressure; the analyzer showed `328 KiB` covers the active-loop reads. | Captured a fresh ACTIVITY12 high baseline, added a `328 KiB` high-tide setup-prime budget plus read-plan awareness, gated ACTIVITY12 high with `--require-improvement`, then cross-gated FISHING1 high, FISHING3 high, FISHING6 high, JOHNNY3 high, and FISHING7 high. | Promoted. ACTIVITY12 high improves `loop_vb 1549 -> 1543`, `target_vb 1412 -> 1415`, `overrun_vb 137 -> 128`, `blocking_vb/prefetch_overrun_vb 9 -> 0`, `loop_reads 23 -> 0`, and `loop_read_vb 110 -> 0`; `due_misses` stays `0`. Layout stays stable (`FG\\ACTIVITY12.FG2` LBA `6075`, PS-EXE `147456`). The canary set stayed stable. Artifacts: baseline `scratch/ps1-perf-iterate/activity12-high-current-baseline/20260430-085747-2478051/summary.json`, accepted run `scratch/ps1-perf-iterate/activity12-high-setupprime-test/20260430-085942-2489301/summary.json`, canaries `scratch/ps1-perf-iterate/activity12-high-setupprime-canaries/20260430-090055-2496609/summary.json`. | Promote. The exact timing-bearing average improves to `16.7876%` over target / `87.4559%` target speed. Latest rows now use `compact-fgp3-v38-activity12high-prime`; continue with smaller targeted setup-prime rows or design segmented coverage for large rows that fail full residency. |
| 2026-04-30 | `activity12-low-setup-prime-352` | Log-only reject; no source promotion. | Test the low-tide pair for the accepted ACTIVITY12 high rule. The fresh low-tide baseline still had `loop_vb=1543`, `target_vb=1411`, `blocking_vb=7`, `prefetch_overrun_vb=4`, `loop_reads=25`, and `due_misses=1`; the analyzer showed `352 KiB` covers all active reads. | Captured a fresh ACTIVITY12 low baseline, temporarily added a `352 KiB` low-tide setup-prime budget plus read-plan awareness, and gated it with `--require-improvement`. The source probe was reverted after the gate failed. | Rejected. The run produced the structural zero-loop failure shape: `loop_start=0`, `loop_vb=0`, `advances=0`, `entries=2` of `252`, `last_frame=0`, and `target_vb=10`. The setup read did complete (`setup_reads=6`, `setup_read_vb=184`, resident buffer `367708` bytes), so the failed shape again points to scene startup/loop execution under full residency, not to missing CD coverage. Artifacts: baseline `scratch/ps1-perf-iterate/activity12-low-current-baseline/20260430-090833-2541266/summary.json`, rejected run `scratch/ps1-perf-iterate/activity12-low-setupprime-test/20260430-091004-2550300/activity12-low/perf-summary.json`. | Do not promote. Retry low-tide ACTIVITY12 with segmented setup coverage or an inter-scene preload design; do not widen the full-resident scene-specific setup path for this row. |
| 2026-04-30 | `activity12-low-setup-prime-320` | Log-only reject; no source promotion. | After the full `352 KiB` low-tide ACTIVITY12 resident attempt failed structurally, test whether a smaller `320 KiB` partial contiguous preload avoids the startup knee while still covering most active reads. | Temporarily added a `320 KiB` low-tide setup-prime budget plus read-plan awareness and gated it against the same fresh ACTIVITY12 low baseline. The source probe was reverted after the gate failed. | Rejected. The smaller preload still produced the zero-loop failure shape: `loop_start=0`, `loop_vb=0`, `advances=0`, `entries=2` of `252`, `last_frame=0`, and `target_vb=10`. The setup read completed (`setup_reads=6`, `setup_read_vb=178`, resident buffer `334940` bytes), and the read-plan showed the tail remained uncovered, so this is not just a full-pack boundary issue. Artifact: rejected run `scratch/ps1-perf-iterate/activity12-low-setupprime-320-test/20260430-091308-2568235/activity12-low/perf-summary.json`. | Do not promote. Treat broad contiguous low-tide ACTIVITY12 setup-prime as exhausted. The next low-tide attempt should be narrow segmented coverage, not another contiguous setup-prime size. |
| 2026-04-30 | `visitor7-high-setup-prime` | `ps1: setup-prime visitor7 high foreground` | Continue the targeted high-tide setup-prime lane above the generic `288 KiB` ceiling, without re-entering the broad low-tide residency failure class. The fresh VISITOR7 high baseline still had visible CD pressure: `loop_vb=1772`, `target_vb=1622`, `blocking_vb=9`, `prefetch_overrun_vb=9`, and `loop_reads=26`. | Captured a fresh VISITOR7 high baseline, added a `368 KiB` high-tide setup-prime budget plus read-plan awareness, gated VISITOR7 high with `--require-improvement`, then cross-checked FISHING1 high, FISHING3 high, and the dependent VISITOR7 low route. | Promoted. VISITOR7 high improves `loop_vb 1772 -> 1766`, `target_vb 1622 -> 1625`, `overrun_vb 150 -> 141`, removes visible pressure (`blocking_vb/prefetch_overrun_vb 9 -> 0`), and removes active-loop foreground reads (`loop_reads 26 -> 0`, `loop_read_vb 147 -> 0`). FISHING1 high stays exact-flat at `1207/1076`, FISHING3 high stays exact-flat at `2095/1952`, and VISITOR7 low remains normal at `1766/1625` with zero pressure. Artifacts: baseline `scratch/ps1-perf-iterate/visitor7-high-current-baseline/20260430-093044-2666090/summary.json`, accepted run `scratch/ps1-perf-iterate/visitor7-high-prime-test/20260430-093223-2675735/summary.json`, canaries `scratch/ps1-perf-iterate/visitor7-high-prime-canaries/20260430-093343-2683696/summary.json`. | Promote. The exact timing-bearing average improves to `16.7857%` over target / `87.4572%` target speed. Latest rows now use `compact-fgp3-v39-visitor7high-prime`; continue with high-tide or generated/segmented setup coverage, not broad contiguous low-tide setup-prime. |
| 2026-04-30 | `johnny6-high-setup-prime-400` | Log-only reject; no source promotion. | Test whether the targeted high-tide setup-prime lane can safely scale just beyond VISITOR7's accepted `368 KiB` point. The fresh JOHNNY6 high baseline was `loop_vb=2896`, `target_vb=2798`, `blocking_vb=29`, `prefetch_overrun_vb=29`, and `loop_reads=12`; the read plan showed `400 KiB` covers all active reads while `384 KiB` leaves a tail. | Temporarily added a `400 KiB` high-tide setup-prime budget plus read-plan awareness and gated JOHNNY6 high with `--require-improvement`. The source probe was reverted after the structural failure. | Rejected. The run produced the same invalid zero-loop shape as the broader low-tide failures: `loop_start=0`, `loop_vb=0`, `advances=0`, `entries=2` of `82`, `last_frame=0`, and `target_vb=6`. The setup window was resident (`prime=[0,197]`) and the analyzer showed no uncovered active reads, so the failure is a startup/loop execution knee, not missing coverage. Artifacts: baseline `scratch/ps1-perf-iterate/johnny6-high-current-baseline/20260430-094222-2732143/summary.json`, rejected run `scratch/ps1-perf-iterate/johnny6-high-prime-test/20260430-094418-2743412/johnny6-high-prime-test/perf-summary.json`. | Do not promote. Treat broad contiguous setup-prime at roughly `400 KiB` as unsafe even for high tide. Retry JOHNNY6 high only with segmented setup coverage, inter-scene preload, or a smaller generated plan that does not require one contiguous resident window. |
| 2026-04-30 | `johnny6-high-setup-prime-384` | Log-only reject; no source promotion. | After the full `400 KiB` JOHNNY6 high preload failed, test whether the smaller `384 KiB` contiguous window avoids the startup knee while still covering most active-loop reads. | Temporarily added a `384 KiB` high-tide setup-prime budget plus read-plan awareness and gated it against the same fresh JOHNNY6 high baseline. The source probe was reverted after the structural failure. | Rejected. The smaller contiguous window still produced zero-loop metrics: `loop_start=0`, `loop_vb=0`, `advances=0`, `entries=2/82`, `last_frame=0`, and `target_vb=6`. It leaves three entries uncovered by the read-plan, so this is not a full-pack-only failure. Artifact: rejected run `scratch/ps1-perf-iterate/johnny6-high-prime384-test/20260430-094703-2759381/johnny6-high-prime384-test/perf-summary.json`. | Do not promote. For JOHNNY6 high, broad contiguous setup-prime is exhausted at both `384 KiB` and `400 KiB`; next retries need segmented coverage or a startup-safe generated preload shape. |
| 2026-04-30 | `johnny6-high-setup-segment-168` | Log-only reject; no source promotion. | Replace broad contiguous JOHNNY6 high setup-prime with a narrow one-shot setup segment over sectors `168..174`, matching the existing FISHING3 segment mechanism and using only `12 KiB`. | Temporarily added a JOHNNY6 high setup segment and read-plan awareness, then gated against the same fresh baseline. The source probe was reverted after the run proved flat. | Rejected as a no-op. The scene stayed structurally valid, unlike the contiguous-prime probes, but key metrics were unchanged: `loop_vb=2896`, `target_vb=2798`, `blocking_vb=29`, `prefetch_overrun_vb=29`, and `loop_reads=12`. Artifact: `scratch/ps1-perf-iterate/johnny6-high-segment-test/20260430-094906-2771183/summary.json`. | Do not promote this one-shot segment. The narrow segment path is startup-safe, but this specific segment does not move VBlank-level metrics; retry only with generated multi-segment or persistent segment ownership. |
| 2026-04-30 | `walkstuf1-high-indexed8-fgp3` | `ps1: add indexed8 temporal residual foreground packs` | Switch from setup-prime sizing to a host-side pack-format win for the largest timing-bearing indexed8 row. The analyzer showed WALKSTUF1 high could drop most active payload/read volume if the existing FGP3 temporal residual idea worked for indexed8 spans. | Extended `scripts/build-fg3-temporal-residual-pack.py` to emit `FGP3/v2` indexed8 residual packs, added the PS1 runtime indexed8 residual compositor and perf format label, converted `generated/ps1/foreground/WALKSTUF1.FG2`, gated against the fresh FGP2 baseline with `--allow-layout-change --require-improvement --work-identity-min 0`, and cross-gated FISHING1 high. | Promoted. WALKSTUF1 high improves `loop_vb 3337 -> 2115`, `target_vb 1355 -> 1391`, `overrun_vb 1982 -> 724`, `blocking_vb 1829 -> 724`, `prefetch_overrun_vb 17 -> 37`, `loop_reads 330 -> 177`, and `loop_read_vb` to `839`; `due_misses 318 -> 147`. Pack bytes drop `5210316 -> 2128856`, and payload drops `5202588 -> 2121128`. FISHING1 high stays exact-flat at `1207/1076` with zero visible pressure. Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/walkstuf1-high-current-baseline/20260430-101605-2917570/summary.json`, accepted run `scratch/ps1-perf-iterate/walkstuf1-high-indexed8-fgp3/20260430-103142-3006155/summary.json`, canary `scratch/ps1-perf-iterate/walkstuf1-indexed8-fgp3-canary/20260430-103300-3013919/summary.json`. | Promote. This is the first accepted indexed8 FGP3 residual pack. The exact timing-bearing average improves from `16.7857%` to `16.0128%` over target, and target speed moves from `87.4572%` to `87.6649%`. Latest rows now use `compact-fgp3-v40-walkstuf1-indexed8-fgp3`; next high-impact follow-up is WALKSTUF1 low with the same format, then indexed8 compositor/read grouping if pressure remains. |
| 2026-04-30 | `walkstuf1-low-indexed8-fgp3` | `ps1: convert walkstuf1 low foreground to indexed8 fgp3` | Complete the WALKSTUF1 pair after high tide proved the indexed8 residual format. The fresh low-tide baseline was still one of the largest outliers (`loop_vb=3740`, `target_vb=1353`, `blocking_vb=2171`, `loop_reads=341`), so a pack-format reduction had enough room to absorb minor refill-shape tradeoffs. | Backed up `generated/ps1/foreground/WALK1LOW.FG2`, converted it with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide against the fresh baseline with `--allow-layout-change --require-improvement --work-identity-min 0`, then cross-gated FISHING1 high and the already-promoted WALKSTUF1 high. | Promoted. WALKSTUF1 low improves `loop_vb 3740 -> 2090`, `target_vb 1353 -> 1392`, `overrun_vb 2387 -> 698`, `blocking_vb 2171 -> 697`, `loop_reads 341 -> 177`, and `loop_read_vb` to `821`; `due_misses 328 -> 145`. `prefetch_overrun_vb` rises `9 -> 38`, but the row-level blocking/due/read reductions are much larger and the gate passed. Pack bytes drop `5698737 -> 2157403`, and payload drops `5691009 -> 2149675`. FISHING1 high stays exact-flat at `1207/1076`, while WALKSTUF1 high refreshes at `2115/1393` with `blocking_vb=712`. Artifacts: FGP2 baseline `scratch/ps1-perf-iterate/walkstuf1-low-current-baseline/20260430-104812-3096736/summary.json`, accepted run `scratch/ps1-perf-iterate/walkstuf1-low-indexed8-fgp3/20260430-105030-3109882/summary.json`, canaries `scratch/ps1-perf-iterate/walkstuf1-low-indexed8-fgp3-canaries/20260430-105152-3117930/summary.json`. | Promote. WALKSTUF1 high/low are now both indexed8 FGP3 residual packs. The exact timing-bearing average improves from `16.0128%` to `14.9511%` over target, and target speed moves from `87.6649%` to `87.9202%`. Latest rows now use `compact-fgp3-v41-walkstuf1low-indexed8-fgp3`; next work should attack the remaining indexed8 residual CD pressure with generated read grouping or setup segmentation. |
| 2026-04-30 | `walkstuf1-low-rg86-98` | log-only after `9edf1cd0` | Test whether the top 12-sector read-plan cluster for the new WALK1LOW FGP3 residual pack can save two loop reads via a hard-coded low-tide read group. | Added a temporary WALKSTUF1 low read group `{86, 98}`, then ran `./scripts/ps1-perf-iterate.sh --case "walkstuf1-low-rg86-98::fgpilot walkstuf1 lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --frames 7200 --timeout 360 --baseline scratch/ps1-perf-iterate/walkstuf1-low-indexed8-fgp3/20260430-105030-3109882/summary.json --allow-layout-change --work-identity-min 0 --require-improvement --output scratch/ps1-perf-iterate/walkstuf1-low-rg86-98`. Source was reverted after counter review. | Failed/no promotion. Timing and CD work stayed exact-flat (`loop_vb=2090`, `target_vb=1392`, `blocking_vb=697`, `prefetch_overrun_vb=38`, `loop_reads=177`, `due_misses=145`) and `group_hits=0`, while selecting a read-group table grew the prefetch buffer from `33842` to `44082` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg86-98/20260430-113056-3330954/summary.json`. | Do not promote. Read-plan sector clusters are not enough to hand-code group starts for this residual pack; the next retry needs actual append-start tracing/generated group metadata or scheduler-owned grouped reads that prove the larger append lands in hidden slack. |
| 2026-04-30 | `walkstuf1-local-window32` | `ps1: use wider walkstuf1 residual stream windows` | Test whether the indexed8 residual WALKSTUF1 packs have a different stream-window knee than the fishing canaries. The hard-coded group probe showed `group_hits=0`, but the residual packs still had more than `690` blocking VBlanks and `145+` due misses, so a scene-local scalar window was worth re-sweeping. | Swept WALKSTUF1 low with `prefetch-window` values `20480`, `22528`, `24576`, `26624`, `28672`, and `32768`; `32768` was best. Promoted a default 32 KiB stream window only when the pack is indexed8 temporal-residual `walkstuf1`, then gated low tide against the v41 baseline and cross-gated high tide plus FISHING1 high. | Promoted. WALKSTUF1 low improves `loop_vb 2090 -> 2054`, `target_vb 1392 -> 1400`, `blocking_vb 697 -> 532`, `loop_reads 177 -> 82`, `loop_read_vb 821 -> 643`, and `due_misses 145 -> 56`; `prefetch_overrun_vb 38 -> 94`. WALKSTUF1 high improves `loop_vb 2115 -> 2064`, `target_vb 1393 -> 1413`, `blocking_vb 712 -> 530`, `loop_reads 177 -> 81`, `loop_read_vb 829 -> 638`, and `due_misses 147 -> 54`; `prefetch_overrun_vb 38 -> 94`. FISHING1 high remains exact-flat at `1207/1076`, `blocking_vb=0`, `prefetch_overrun_vb=0`. Artifacts: low parameter sweep `scratch/ps1-perf-iterate/walkstuf1-low-window-sweep-adjacent/20260430-113608-3360855/summary.json`, accepted low `scratch/ps1-perf-iterate/walkstuf1-local-window32-low/20260430-114518-3413466/summary.json`, canaries `scratch/ps1-perf-iterate/walkstuf1-local-window32-canaries/20260430-114636-3421232/summary.json`. | Promote. This is a scene-local CD coverage win, not a global window retune. The exact timing-bearing average improves from `14.9511%` to `14.8745%` over target, and target speed moves from `87.9202%` to `87.9549%`. Latest rows now use `compact-fgp3-v42-walkstuf1-window32`; future WALKSTUF1 grouping/setup work must use this 32 KiB baseline. |
| 2026-04-30 | `walkstuf1-split-window` | `ps1: split walkstuf1 residual stream windows` | Sweep whether the indexed8 residual WALKSTUF1 high/low routes have separate stream-window knees after the 32 KiB scene-local promotion. Hypothesis: the high route can use a wider window to reduce due misses/read count, while low tide wants a smaller compromise because overrun rises sooner. | Swept explicit byte-count windows: `40 KiB` high/low, `48 KiB` high/low, and `64 KiB` high/low. Promoted default `48 KiB` for high tide and `40 KiB` for low tide only when the pack is indexed8 temporal-residual `walkstuf1`, then gated low and high defaults plus FISHING1 high. The `prefetch-window48` alias produced one structural missing-`JCPERF2` run, but the explicit `49152` retry and default high retry both passed. | Promoted. WALKSTUF1 high improves `loop_vb 2064 -> 2030`, `target_vb 1413 -> 1421`, `blocking_vb 530 -> 457`, `loop_reads 81 -> 50`, `loop_read_vb 638 -> 549`, and `due_misses 54 -> 31`; `prefetch_overrun_vb 94 -> 108`. WALKSTUF1 low improves `loop_vb 2054 -> 2048`, `target_vb 1400 -> 1414`, `blocking_vb 532 -> 460`, `loop_reads 82 -> 63`, `loop_read_vb 643 -> 572`, and `due_misses 56 -> 34`; `prefetch_overrun_vb 94 -> 139`. FISHING1 high remains exact-flat at `1207/1076`, `blocking_vb=0`, `prefetch_overrun_vb=0`. Artifacts: sweep `scratch/ps1-perf-iterate/walkstuf1-window-40-64-sweep/20260430-115411-3464186/summary.json`, explicit high `48 KiB` retry `scratch/ps1-perf-iterate/walkstuf1-window48-explicit-retry/20260430-115717-3483227/summary.json`, explicit low/high `48/64 KiB` sweep `scratch/ps1-perf-iterate/walkstuf1-window48-64-explicit/20260430-115836-3490949/summary.json`, accepted low `scratch/ps1-perf-iterate/walkstuf1-split-window-low/20260430-120255-3515869/summary.json`, accepted high `scratch/ps1-perf-iterate/walkstuf1-split-window-high-retry/20260430-120507-3529712/summary.json`, canary `scratch/ps1-perf-iterate/walkstuf1-split-window-fishing1-canary/20260430-120654-3539878/summary.json`. | Promote. This is still a scene-local indexed8 residual CD coverage win, not a global prefetch retune. The exact timing-bearing average improves from `14.8745%` to `14.8321%` over target, and target speed moves from `87.9549%` to `87.9751%`. Latest rows now use `compact-fgp3-v43-walkstuf1-split-window`; future WALKSTUF1 grouping/setup work must use the split-window baseline. |
| 2026-04-30 | `walkstuf1-high-window54` | `ps1: retune walkstuf1 high residual window` | Fine-tune the high-tide side of the split-window result without accepting a dirty low-tide trade. Hypothesis: high tide can move past the 48 KiB knee and reduce read count/due misses, but low tide should stay at 40 KiB unless larger windows improve loop time without adding visible blocking. | Swept high `44/52/56 KiB`, low `36/44/48 KiB`, then narrowed to high `54/58 KiB` and low `41/42/43 KiB`. Promoted only high `54 KiB`; low `43/44 KiB` improved loop time to `2035` but regressed `blocking_vb 460 -> 491` and `due_misses 34 -> 40`, so low stays at `40 KiB`. Gated the default high route against v43, then cross-gated low tide and FISHING1 high. | Promoted. WALKSTUF1 high improves `loop_vb 2030 -> 2024`, `target_vb 1421 -> 1423`, `blocking_vb 457 -> 449`, `loop_reads 50 -> 43`, `loop_read_vb 549 -> 512`, and `due_misses 31 -> 25`; `prefetch_overrun_vb 108 -> 133`. Low tide stays exact-flat at `2048/1414`, `blocking_vb=460`, `prefetch_overrun_vb=139`, `due_misses=34`. FISHING1 high stays exact-flat at `1207/1076`, `blocking_vb=0`, `prefetch_overrun_vb=0`. Artifacts: first fine sweep `scratch/ps1-perf-iterate/walkstuf1-split-window-fine-sweep/20260430-121155-3567785/summary.json`, second fine sweep `scratch/ps1-perf-iterate/walkstuf1-split-window-fine-sweep-2/20260430-121908-3609494/summary.json`, accepted high `scratch/ps1-perf-iterate/walkstuf1-high-window54/20260430-122517-3644855/summary.json`, canaries `scratch/ps1-perf-iterate/walkstuf1-high-window54-canaries/20260430-122637-3652825/summary.json`. | Promote. This is a narrow high-tide CD coverage win. The exact timing-bearing average improves from `14.8321%` to `14.8269%` over target, and target speed moves from `87.9751%` to `87.9776%`. Latest rows now use `compact-fgp3-v44-walkstuf1-high-window54`; the next WALKSTUF1 work should attack residual prefetch-overrun ownership rather than increasing low-tide window size. |
| 2026-04-30 | `walkstuf1-high-seg455-479` | log-only after `d84f21e5` | Test whether a retained setup segment for WALKSTUF1 high sectors `455..479` can remove one visible loop read after the `54 KiB` high-window retune. | Temporarily added a dedicated setup segment buffer for WALKSTUF1 high and preserved dedicated setup segments after copy, then gated `fgpilot walkstuf1 lowtide 0 ... perf-log noloop seed 1` against the v44 baseline. Source was reverted after review. | Failed/no promotion. Due misses improved by one (`25 -> 24`), but total loop time regressed `2024 -> 2029`, `prefetch_overrun_vb 133 -> 145`, and `loop_reads` stayed `43`; `blocking_vb` stayed `449`. The read-plan still showed the `455..479` region as a high-tight-cluster candidate after the probe. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-seg455-479/20260430-123232-3686533/summary.json`. | Do not promote. A one-off retained segment is not enough; the next retry needs scheduler-owned setup/segment metadata that proves the extra setup read is hidden and does not worsen active-loop refill overrun. |
| 2026-04-30 | `walkstuf1-high-window53` | log-only after `d84f21e5` | Verify whether the `53 KiB` boot-override result is promotable as the default high-tide residual window. | Boot overrides `53 KiB` and `55 KiB` were swept without source changes; `53 KiB` looked better than v44 (`loop_vb=2017`, `blocking_vb=441`, `prefetch_overrun_vb=136`). The source default was then temporarily changed from `54 KiB` to `53 KiB` and gated against v44. Source was reverted after the default-source run failed structurally. | Failed/no promotion. The boot override passed, but the source-shape validation hit the headless log cap with repeated invalid reads and never emitted `JCPERF2`; the summary is a hard `missing_jcperf2` failure. Artifacts: passing override sweep `scratch/ps1-perf-iterate/walkstuf1-high-window53-55-sweep/20260430-123604-3706735/summary.json`, failed default-source run `scratch/ps1-perf-iterate/walkstuf1-high-window53/20260430-123833-3721197/walkstuf1-high-local-window53/perf-summary.json`. | Do not promote. Treat `53 KiB` as a code-layout/source-shape hazard unless a later binary layout change makes the default-source path pass; keep v44's `54 KiB` accepted baseline. |
| 2026-04-30 | `walkstuf1-high-group256-320` | log-only after `d84f21e5` | Test whether a high-tide WALKSTUF1 append group for sectors `256..320` can combine three adjacent accepted-baseline reads under a larger retained window, without changing normal FISHING/FISHING3 paths. | Temporarily added a WALKSTUF1-high group table `{256, 320}` and a `64`-sector retained window only for indexed8 residual WALKSTUF1 high tide, then gated `fgpilot walkstuf1 lowtide 0 ... perf-log noloop seed 1` against the v44 baseline. Source was reverted after counter review. | Failed/no promotion. The run was structurally clean, but key metrics stayed flat: `loop_vb=2024`, `blocking_vb=449`, `prefetch_overrun_vb=133`, `loop_reads=43`, and `due_misses=25`. It also increased the prefetch buffer to `147964` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-group256-320/20260430-125220-3796464/summary.json`. | Do not promote. Observed sector ranges are not sufficient; WALKSTUF1 grouping needs append-start-aware host simulation or generated runtime metadata before another source group. |
| 2026-04-30 | `walkstuf3-window-sweep-24-32` | log-only after `1b0b4417` | Test whether WALKSTUF3 has a larger stream-window knee under the current compact/padded baseline. High tide is FGP3 with `74` loop reads; low tide remains FGP2 with `72` loop reads, so a scene-local `24 KiB` or `32 KiB` window might reduce pressure without a pack-format change. | Ran parameter-only high/low cases with `prefetch-window 24576` and `prefetch-window32`; no source or pack files changed. Compared against matrix baselines: high `2460/2276`, `blocking_vb=79`, `prefetch_overrun_vb=45`; low `2466/2285`, `blocking_vb=66`, `prefetch_overrun_vb=38`. | Failed/no promotion. High `24 KiB` regressed to `2518/2273`, `blocking_vb=124`, `prefetch_overrun_vb=100`; high `32 KiB` regressed to `2523/2283`, `blocking_vb=113`, `prefetch_overrun_vb=99`. Low `24 KiB` regressed to `2495/2280`, `blocking_vb=81`, `prefetch_overrun_vb=73`; low `32 KiB` regressed to `2539/2284`, `blocking_vb=117`, `prefetch_overrun_vb=108`. Artifact: `scratch/ps1-perf-iterate/walkstuf3-window-sweep-24-32/20260430-125717-3824210/summary.json`. | Do not promote raw larger windows for WALKSTUF3. Transaction count falls, but held slack is not sufficient; revisit with generated groups, direct16/selective preprocessing, or scheduler ownership. |
| 2026-04-30 | `activity8-fgp3-auto-prime` | `ps1: convert activity8 foreground to fgp3 auto-prime` | Retry ACTIVITY8 high/low FGP3 conversion because the earlier rejection happened before setup-resident auto-prime existed; at `173564` bytes each converted pack should become setup-resident and remove loop CD pressure instead of shifting it into refill overrun. | Backed up both FGP2 packs, converted `ACTIVITY8.FG2` and `ACTV8LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated high/low against the fresh current-layout baseline, then cross-gated FISHING1 high. | Promoted. High improves `loop_vb 1058 -> 1043`, `target_vb 902 -> 905`, `overrun_vb 156 -> 138`, `blocking_vb 22 -> 0`, `prefetch_overrun_vb 2 -> 0`, `loop_reads 35 -> 0`, and `due_misses 5 -> 0`. Low improves `1053 -> 1043`, `901 -> 905`, `152 -> 138`, `14 -> 0`, `2 -> 0`, `35 -> 0`, and `4 -> 0`. FISHING1 stayed exact-flat at `1207/1076`, `blocking_vb=0`, `prefetch_overrun_vb=0`. Artifacts: accepted ACTIVITY8 run `scratch/ps1-perf-iterate/activity8-fgp3-auto-prime-retry/20260430-130710-3881684/summary.json`, FISHING1 canary `scratch/ps1-perf-iterate/activity8-fgp3-auto-prime-canary/20260430-130855-3892418/summary.json`. | Promote. This validates the "retry old FGP3 rejects after auto-prime" category. The exact matrix average moves from `14.8269%` to `14.8002%` over target, and target speed moves from `87.9776%` to `87.9974%`; continue scanning old FGP3 rejects whose converted packs now fit under the setup-resident ceiling. |
| 2026-04-30 | `johnny4-low-fgp3-auto-prime` | `ps1: convert johnny4 low foreground to fgp3 auto-prime` | Apply the same setup-resident FGP3 retry rule to JOHNNY4 low. The high-tide side was already a zero-read FGP3 row; converting low should pair-complete the scene and remove its remaining loop reads without touching the global prefetch policy. | Captured a fresh low-tide FGP2 baseline, converted `JOHN4LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide against the fresh baseline, then cross-gated JOHNNY4 high and FISHING1 high. | Promoted. Low improves `loop_vb 1349 -> 1341`, `target_vb 1213 -> 1214`, `overrun_vb 136 -> 127`, `blocking_vb 1 -> 0`, `prefetch_overrun_vb 1 -> 0`, `loop_reads 27 -> 0`, and `loop_read_vb` to `0`; pack bytes drop `369195 -> 173803`. JOHNNY4 high stays `1341/1214`, and FISHING1 stays `1207/1076` with zero blocking/refill pressure. Artifacts: baseline `scratch/ps1-perf-iterate/johnny4-low-current-baseline/20260430-131618-3933559/summary.json`, accepted low `scratch/ps1-perf-iterate/johnny4-low-fgp3-auto-prime/20260430-131730-3940750/summary.json`, canaries `scratch/ps1-perf-iterate/johnny4-low-fgp3-auto-prime-canaries/20260430-131841-3947948/summary.json`. | Promote. This is another converted-pack-plus-auto-prime win. The exact matrix average moves from `14.8002%` to `14.7947%` over target, and target speed crosses to `88.0019%`. Continue with small resident candidates only when they shrink or pair-complete a scene; tiny CD-pressure rows that grow under FGP3 should be rejected quickly. |
| 2026-04-30 | `fishing4-low-fgp3-auto-prime` | `ps1: convert fishing4 low foreground to fgp3 auto-prime` | Apply the same small-resident pair-completion rule to FISHING4 low. High tide was already a zero-read FGP3 row, and low tide had only one visible CD/refill VBlank, but the conversion also shrinks the pack. | Captured a fresh low-tide FGP2 baseline, converted `FISH4LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide against the fresh baseline, then cross-gated FISHING4 high and FISHING1 high. | Promoted. Low improves `loop_vb 975 -> 967`, `overrun_vb 132 -> 124`, `blocking_vb 1 -> 0`, `prefetch_overrun_vb 1 -> 0`, `loop_reads 17 -> 0`, and `loop_read_vb` to `0`; pack bytes drop `238608 -> 169264`. FISHING4 high stays `967/843`, and FISHING1 stays `1207/1076` with zero blocking/refill pressure. Artifacts: baseline `scratch/ps1-perf-iterate/fishing4-low-current-baseline/20260430-132323-3974961/summary.json`, accepted low `scratch/ps1-perf-iterate/fishing4-low-fgp3-auto-prime/20260430-132428-3981592/summary.json`, canaries `scratch/ps1-perf-iterate/fishing4-low-fgp3-auto-prime-canaries/20260430-132526-3987489/summary.json`. | Promote. The exact matrix average moves from `14.7947%` to `14.7868%` over target, and target speed moves from `88.0019%` to `88.0079%`. This validates the pair-completion subset; keep ranking remaining resident candidates by shrink plus expected loop-read removal, not percent-over-target alone. |
| 2026-04-30 | `building3-low-fgp3-auto-prime` | `ps1: convert building3 low foreground to fgp3 auto-prime` | Test the last clear resident-shrink FGP2 candidate from the small-pack scan. BUILDING3 low had little visible CD pressure, but it could cut the pack roughly in half and move 39 loop reads out of active playback. | Captured a fresh low-tide FGP2 baseline, converted `BUIL3LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, gated low tide against the fresh baseline, then cross-gated BUILDING3 high and FISHING1 high. | Promoted. Low improves `loop_vb 1571 -> 1564`, `target_vb 1433 -> 1434`, `overrun_vb 138 -> 130`, `blocking_vb 1 -> 0`, `prefetch_overrun_vb 1 -> 0`, `loop_reads 39 -> 0`, and `loop_read_vb` to `0`; pack bytes drop `537821 -> 263908`. BUILDING3 high stays `1565/1430`, and FISHING1 stays `1207/1076` with zero blocking/refill pressure. Artifacts: baseline `scratch/ps1-perf-iterate/building3-low-current-baseline/20260430-133052-4018697/summary.json`, accepted low `scratch/ps1-perf-iterate/building3-low-fgp3-auto-prime/20260430-133208-4026462/summary.json`, canaries `scratch/ps1-perf-iterate/building3-low-fgp3-auto-prime-canaries/20260430-133314-4033228/summary.json`. | Promote. The exact matrix average moves from `14.7868%` to `14.7827%` over target, and target speed moves from `88.0079%` to `88.0113%`. The remaining FGP2 resident candidates from this scan grow under FGP3, so the next high-impact path should move back to grouped/direct16/selective preprocessing or a wider scan for new shrink classes. |
| 2026-04-30 | `walkstuf2-fgp3-auto-prime` | `ps1: convert walkstuf2 foregrounds to fgp3 auto-prime` | Bound-test the remaining small resident FGP2 pair even though conversion grows both packs. The acceptance bar is stricter here: promote only if the VBlank/read win justifies the size trade. | Captured a paired high/low FGP2 baseline, converted `WALKSTUF2.FG2` and `WALK2LOW.FG2`, gated the pair against the fresh baseline, then cross-gated FISHING1 high. | Promoted as a speed-over-size tradeoff. Both packs grow `138609 -> 163167` bytes, but both tides improve `loop_vb 596 -> 593`, raise target to `462`, clear `blocking_vb/prefetch_overrun_vb 1 -> 0`, and remove `loop_reads 9 -> 0`. FISHING1 stays exact-flat at `1207/1076`. Artifacts: baseline `scratch/ps1-perf-iterate/walkstuf2-current-baseline/20260430-133750-4059682/summary.json`, accepted pair `scratch/ps1-perf-iterate/walkstuf2-fgp3-auto-prime/20260430-133947-4071581/summary.json`, canary `scratch/ps1-perf-iterate/walkstuf2-fgp3-auto-prime-canary/20260430-134128-4081974/summary.json`. | Promote. The exact matrix average moves from `14.7827%` to `14.7626%` over target, and target speed moves from `88.0113%` to `88.0234%`. This closes the obvious small resident FGP2 lane; next work should find a new class rather than blindly converting packs that grow without a timing win. |
| 2026-04-30 | `activity10-low-contiguous-setup-prime` | Log-only reject; no source promotion. | Revisit ACTIVITY10 low after the small resident FGP2/FGP3 lane was exhausted. The row sits just above the `288 KiB` auto-prime ceiling and still has measurable CD pressure, so a scene/tide-specific setup-prime budget might remove active-loop reads without broadening the global threshold. | Captured a fresh current-layout baseline (`1404/1257`, `blocking_vb=18`, `prefetch_overrun_vb=8`, `loop_reads=22`), then temporarily tested contiguous low-tide setup-prime budgets of `304 KiB`, `288 KiB`, and `256 KiB` with read-plan awareness. Source and script edits were reverted after review. | Rejected. `304 KiB` and `288 KiB` both reproduced the known zero-loop failure shape (`loop_vb=0`, `target_vb=10`, one observed setup read). `256 KiB` was structurally valid and improved CD pressure (`blocking_vb 18 -> 10`, `prefetch_overrun_vb 8 -> 4`, `loop_reads 22 -> 3`, `overrun_vb 147 -> 145`) but regressed `loop_vb 1404 -> 1405` and shifted the FISHING3 high canary to `2098/1950`, `blocking_vb=23`, `prefetch_overrun_vb=18`. Artifacts: baseline `scratch/ps1-perf-iterate/activity10-low-current-baseline/20260430-141111-49583/summary.json`, failed full prime `scratch/ps1-perf-iterate/activity10-low-setupprime304/20260430-141250-59260/`, failed partial prime `scratch/ps1-perf-iterate/activity10-low-setupprime288/20260430-141353-65558/`, structurally valid but rejected `256 KiB` probe `scratch/ps1-perf-iterate/activity10-low-setupprime256/20260430-141444-70894/summary.json`, canaries `scratch/ps1-perf-iterate/activity10-low-setupprime256-canaries/20260430-141613-79687/summary.json`. | Do not promote contiguous setup-prime for ACTIVITY10 low. Retry this row only with generated segmented coverage, inter-scene preload, or offset-stable metadata that avoids broad source/layout shifts and proves FISHING3 high remains stable. |
| 2026-04-30 | `visitor3-window-sweep` | Log-only reject; no source promotion. | Test whether VISITOR3 has a scene-local scalar stream-window knee now that the foreground code and CD helpers are smaller. The row still has high visible CD pressure, so a larger window could reduce transaction count if held slack can absorb the larger reads. | Ran parameter-only high-tide sweeps at `24/32/48 KiB`, then `20/22/26/28 KiB`, and a fresh low-tide `24 KiB` check. No source changes were promoted. | Rejected. High fresh baseline was `1504/1014`, `blocking_vb=351`, `prefetch_overrun_vb=112`, `loop_reads=69`; high `24 KiB` in the stale sweep showed `1507/1017`, but the fresh baseline made it non-promotable. Low fresh baseline was `1536/1018`, `blocking_vb=332`, `prefetch_overrun_vb=99`, `loop_reads=61`; low `24 KiB` regressed to `1541/1021` despite lowering reads to `48`. Artifacts: high sweep `scratch/ps1-perf-iterate/visitor3-high-window-sweep-24-48/20260430-142248-117447/summary.json`, high narrow sweep `scratch/ps1-perf-iterate/visitor3-high-window-sweep-20-28/20260430-142557-136488/summary.json`, fresh high/low check `scratch/ps1-perf-iterate/visitor3-current-and-low-window24/20260430-143006-161490/summary.json`. | Do not promote VISITOR3 scalar window overrides. Retry VISITOR3 with generated grouping, direct16/selective preprocessing, or first-class scheduler ownership; do not spend more time on raw window sizes unless a later format change moves the baseline. |
| 2026-04-30 | `building4-scene-window` | `ps1: tune building4 foreground stream windows` | Test whether the BUILDING4 high-pressure row has a tide-specific scalar stream-window knee. Unlike global raw-window probes, this is scoped to one scene/tide pair and accepted only if fresh baselines show total loop, blocking, reads, and due misses moving in the right direction under a bounded refill tradeoff. | Swept BUILDING4 high and low at `24 KiB` and `32 KiB`, promoted default source policy of `24 KiB` for high tide and `32 KiB` for low tide, gated both defaults with bounded CD-regression allowance, then ran FISHING1/FISHING3 canaries and a no-patch A/B to prove FISHING3 high drift was current-layout state rather than caused by this patch. | Promoted. High improves `loop_vb 3126 -> 3073`, `target_vb 2790 -> 2793`, `overrun_vb 336 -> 280`, `blocking_vb 351 -> 240`, `loop_reads 78 -> 63`, and `due_misses 49 -> 34`, with `prefetch_overrun_vb 19 -> 27`. Low improves `3126 -> 3080`, `2789 -> 2795`, `337 -> 285`, `349 -> 168`, `78 -> 51`, and `48 -> 18`, with `prefetch_overrun_vb 25 -> 63`. FISHING1 remains exact-flat at `1207/1076` with zero blocking/refill pressure. Artifacts: high sweep `scratch/ps1-perf-iterate/building4-high-window-sweep-24-32/20260430-143328-181664/summary.json`, low sweep `scratch/ps1-perf-iterate/building4-low-window-sweep-24-32/20260430-143755-207737/summary.json`, accepted high `scratch/ps1-perf-iterate/building4-scene-window-high-default-tradeoff/20260430-144556-254946/summary.json`, accepted low `scratch/ps1-perf-iterate/building4-scene-window-low-default/20260430-144423-245810/summary.json`, canaries `scratch/ps1-perf-iterate/building4-scene-window-canaries/20260430-144730-264169/summary.json`, no-patch FISHING3 A/B `scratch/ps1-perf-iterate/fishing3-high-layout-ab-no-building4-window/20260430-145138-288571/summary.json`. | Promote. This is a scene-local CD coverage win with an explicit refill-overrun tradeoff. The exact matrix average moves from `14.7626%` to `14.7288%` over target, and target speed moves from `88.0234%` to `88.0508%`. Next similar attempts should start with BUILDING-family rows and fresh baselines, not global raw-window changes. |
| 2026-04-30 | `building6-scene-window` | `ps1: tune building6 foreground stream windows` | Continue the BUILDING-family scene-local window lane on BUILDING6. The row has high visible CD pressure and the same broad sequential read shape as BUILDING4, so a `24/32/48 KiB` sweep should reveal whether scalar windows still pay before moving to generated grouping. | Swept high and low tide at `24/32/48 KiB`, selected `24 KiB` for both tides because it gave the best absolute loop time, then rebuilt the default source policy and gated high/low against fresh no-window baselines. Canaries covered FISHING1, FISHING3 high/low, and BUILDING4 high/low. | Promoted. High improves `loop_vb 2746 -> 2687`, `target_vb 2432 -> 2431`, `overrun_vb 314 -> 256`, `blocking_vb 341 -> 215`, `loop_reads 72 -> 59`, and `due_misses 49 -> 32`; `prefetch_overrun_vb` rises `12 -> 17`. Low improves `2747 -> 2697`, `target_vb 2431 -> 2434`, `overrun_vb 316 -> 263`, `blocking_vb 337 -> 226`, `prefetch_overrun_vb 16 -> 15`, `loop_reads 72 -> 59`, and `due_misses 48 -> 33`. Canaries stayed stable: FISHING1 `1207/1076`, FISHING3 high `2098/1950`, FISHING3 low `2091/1958`, BUILDING4 high `3073/2793`, BUILDING4 low `3080/2795`. Artifacts: high sweep `scratch/ps1-perf-iterate/building6-high-window-sweep-24-48/20260430-145911-330696/summary.json`, low sweep `scratch/ps1-perf-iterate/building6-low-window-sweep-24-48/20260430-150442-362252/summary.json`, accepted high `scratch/ps1-perf-iterate/building6-scene-window-high-default-rebuild/20260430-151215-405358/summary.json`, accepted low `scratch/ps1-perf-iterate/building6-scene-window-low-default-vs-baseline/20260430-151513-422720/summary.json`, canaries `scratch/ps1-perf-iterate/building6-scene-window-canaries/20260430-151642-431273/summary.json`. | Promote. This is a second BUILDING-family scalar-window win. The exact matrix average moves from `14.7288%` to `14.6917%` over target, and target speed moves from `88.0508%` to `88.0804%`. Continue this lane on remaining high-pressure BUILDING rows before escalating to generated read groups. |
| 2026-04-30 | `building2-window-sweep` | Log-only reject; no source promotion. | Test whether BUILDING2 can use the same scene-local scalar-window lane that worked for BUILDING4 and BUILDING6. Both tides have about `150` blocking VBlanks and `70` loop reads, but their shorter loop and tighter cadence may make larger reads steal too much held slack. | Swept high and low tides at `20/24/32/48 KiB`. High completed all sizes and every window regressed total loop. Low `32 KiB` initially hit a pre-JCPERF2 `137`, then passed on retry; a temporary low-only default source policy was rebuilt and tested before being reverted. | Rejected. High baseline was `1555/1296`, `blocking_vb=150`, `prefetch_overrun_vb=47`, `loop_reads=70`; `20 KiB` regressed to `1557`, `24 KiB` to `1563`, `32 KiB` to `1567`, and `48 KiB` to `1566`, despite lower reads at larger sizes. Low baseline was `1557/1295`, `blocking_vb=157`, `prefetch_overrun_vb=41`, `loop_reads=71`; the retry `32 KiB` parameter point improved to `1554/1299`, `blocking_vb=108`, `prefetch_overrun_vb=66`, `loop_reads=35`, but the compiled default source shape regressed to `1563/1297`, `blocking_vb=119`, `prefetch_overrun_vb=76`, `loop_reads=36`. Artifacts: high sweep `scratch/ps1-perf-iterate/building2-high-window-sweep-20-48/20260430-152936-503393/summary.json`, partial low sweep `scratch/ps1-perf-iterate/building2-low-window-sweep-20-48/20260430-153443-533065/`, low retry `scratch/ps1-perf-iterate/building2-low-window32-48-retry/20260430-153853-557603/summary.json`, rejected default-source run `scratch/ps1-perf-iterate/building2-low-window32-default/20260430-154139-573370/summary.json`. | Do not promote raw BUILDING2 stream windows. The parameter-only low result is not stable as compiled source, and high tide is clearly slower. Retry BUILDING2 with generated read groups, direct16/selective preprocessing, or scheduler-owned grouping rather than another scalar window override. |
| 2026-04-30 | `building5-window-sweep` | Log-only reject; no source promotion. | Test whether BUILDING5's longer loop gives raw windows enough held slack to pay off, unlike BUILDING2. The high side has `69` loop reads but only moderate visible blocking, while low tide has lower pressure; promote only if total loop improves, not just transaction count. | Swept high tide at `20/24/32/48 KiB`, retrying `32/48 KiB` after the first `32 KiB` run died before metrics. Swept low tide at `20/24/32/48 KiB`; the first three completed and `48 KiB` died before metrics after the trend was already regressive. No source changes were made. | Rejected. High fresh baseline was `3506/3335`, `blocking_vb=55`, `prefetch_overrun_vb=29`, `loop_reads=69`; `20 KiB` regressed to `3509`, `24 KiB` to `3515`, `32 KiB` to `3524`, and `48 KiB` to `3566`, despite reads falling as low as `21`. Low baseline was `3498/3349`, `blocking_vb=16`, `prefetch_overrun_vb=11`, `loop_reads=47`; `20 KiB` regressed to `3499`, `24 KiB` to `3510`, and `32 KiB` to `3524`, while `48 KiB` failed structurally before JCPERF2. Artifacts: high partial sweep `scratch/ps1-perf-iterate/building5-high-window-sweep-20-48/20260430-154435-590154/`, high retry `scratch/ps1-perf-iterate/building5-high-window32-48-retry/20260430-155003-621883/summary.json`, low sweep `scratch/ps1-perf-iterate/building5-low-window-sweep-20-48/20260430-155323-640835/`. | Do not promote raw BUILDING5 stream windows. Transaction reduction is not enough when the scene already has modest blocking pressure; retry with generated read groups or host-side preprocessing that changes read placement/format instead of scalar window size. |
| 2026-04-30 | `resident-deadline-catchup-probe` | Log-only reject; no source promotion. | Test whether fully setup-resident FGP3 packs can safely consume all late elapsed VBlanks in the host-deadline accumulator instead of only one extra VBlank. The hypothesis was that zero-CD rows such as STAND1 and WALKSTUF2 are over target because deadline accounting remains conservative even when no future CD slack needs protection. | Temporarily tracked whether the setup window covered the full payload and changed the catch-up branch to add `frameVBlank` rather than `frameHoldVBlanks + 1` for that resident case. Gated STAND1 high, WALKSTUF2 high, FISHING1 high, and VISITOR3 high in one run, then reverted the source. | Rejected as exact-flat. STAND1 high stayed `342/202`, WALKSTUF2 high stayed `593/462`, FISHING1 high stayed `1207/1076`, and VISITOR3 high stayed `1504/1014` with `blocking_vb=351` and `prefetch_overrun_vb=112`. Artifact: `scratch/ps1-perf-iterate/resident-deadline-catchup-probe/20260430-160801-723996/summary.json`. | Do not retry resident-only catch-up as a local runtime flag. The over-target gap on zero-CD resident rows is not coming from this catch-up clamp; pursue direct16/upload-ready bands, present/input scheduler work, or pack-time frame coalescing instead. |
| 2026-04-30 | `resident-no-slack-advance-probe` | Log-only reject; no source promotion. | Retry the old no-slack wait-skip idea only for setup-resident packs. The global version was unsafe because it changed CD/prefetch cadence; resident packs have no active-loop reads, so advancing immediately when held slack is zero should be safe if the extra VBlank is truly idle. | Temporarily cached full-payload setup-window coverage and, in the held branch, called `foregroundPilotRuntimeAdvance()` instead of waiting when `heldSlackVBlanks == 0` for resident packs. Gated STAND1 high/low, WALKSTUF2 high/low, FISHING1 high, and VISITOR3 high, then reverted the source. | Rejected as exact-flat. STAND1 high/low stayed `342/202`, WALKSTUF2 high/low stayed `593/462`, FISHING1 high stayed `1207/1076`, and VISITOR3 high stayed `1504/1014` with the same CD pressure. Artifact: `scratch/ps1-perf-iterate/resident-no-slack-advance-probe/20260430-161533-767496/summary.json`. | Do not retry no-slack advance as a resident-only runtime branch. The zero-CD over-target rows are not spending measurable time in this idle state; move to pack-time frame coalescing, upload-ready/direct16 formats, or explicit present/input scheduler redesign. |
| 2026-04-30 | `visitor3-high-setup-prime-216` | `ps1: setup-prime visitor3 high foreground` | Retry VISITOR3 high after raw scalar windows failed. The fresh no-prime shape was still heavily CD-bound (`1504/1014`, `blocking_vb=351`, `prefetch_overrun_vb=112`, `loop_reads=69`), so the hypothesis was that a targeted contiguous setup-prime budget below the broad structural-failure range could move some reads out of active playback without disturbing the canaries. | Swept targeted high-tide setup-prime sizes: `128 KiB`, `160 KiB`, `192 KiB`, `208 KiB`, `216 KiB`, `220 KiB`, and `224 KiB`. Promoted a named `216 KiB` VISITOR3 high budget, then gated VISITOR3 high/low, FISHING1 high, and FISHING3 high/low. | Promoted. VISITOR3 high improves fresh current-layout metrics `loop_vb 1504 -> 1484`, `target_vb 1014 -> 1013`, `overrun_vb 490 -> 471`, `blocking_vb 351 -> 321`, `prefetch_overrun_vb 112 -> 88`, `loop_reads 69 -> 52`, and `loop_read_vb 546 -> 418`; `due_misses` stays `23`. The sweep found `224 KiB` structurally unsafe before complete metrics, and `220 KiB` slower than `216 KiB`. FISHING1 high stayed `1207/1076` with zero pressure; FISHING3 high/low and VISITOR3 low stayed within the current gate baselines. Artifacts: final accepted run `scratch/ps1-perf-iterate/visitor3-high-setupprime216-final/20260430-165534-1001994/summary.json`, canary gate `scratch/ps1-perf-iterate/visitor3-high-setupprime216-gate/20260430-164400-934827/summary.json`; edge probes `visitor3-high-setupprime128`, `visitor3-high-setupprime160`, `visitor3-high-setupprime192`, `visitor3-high-setupprime208`, `visitor3-high-setupprime216`, `visitor3-high-setupprime220`, and failed `visitor3-high-setupprime224`. | Promote. This is a targeted setup-prime/CD-pressure win, not a general full-residency rule. The exact timing-bearing average moves from `14.6917%` to `14.6579%` over target, and target speed moves from `88.0804%` to `88.0942%`. Continue with generated segmented coverage or similarly bounded scene/tide setup-prime knees; do not retry blind `224 KiB+` VISITOR3 contiguous windows. |
| 2026-04-30 | `visitor3-high-seg156-168` | Log-only reject; no source promotion. | Test whether a narrow one-shot retained setup segment can capture the top remaining VISITOR3 high post-prime read cluster after the accepted `216 KiB` contiguous budget. The read-plan ranked sectors `156..168` as a 12-sector cluster with three reads and two potential saved transactions. | Temporarily added a VISITOR3 high scratch-backed setup segment over relative sectors `156..168`, reusing the existing single-use FISHING3 high segment mechanism, and gated `fgpilot visitor3 lowtide 0 ... perf-log noloop seed 1`. The source probe was reverted after the run. | Rejected. The segment stayed structurally clean but regressed the accepted row: `loop_vb 1484 -> 1495`, `target_vb 1013 -> 1011`, `blocking_vb 321 -> 331`, `prefetch_overrun_vb 88 -> 96`, and `loop_reads` stayed `52`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-seg156-168/20260430-165911-1022430/summary.json`. | Do not promote a manual VISITOR3 side segment. Like earlier FISHING3/WALKSTUF1 segment failures, the offline cluster is real but the single-use setup-segment ownership steals active cadence. Retry only with generated segment metadata plus scheduler-owned preload/copy timing. |
| 2026-04-30 | `visitor3-low-setup-prime-208` | `ps1: setup-prime visitor3 low foreground` | Continue the targeted VISITOR3 contiguous setup-prime lane on low tide. The current low row was still CD-bound (`1536/1018`, `blocking_vb=332`, `prefetch_overrun_vb=99`, `loop_reads=61`), so the test swept the smallest budget that improved battle-card speed without repeating the high-tide structural edge. | Swept low-tide setup-prime sizes `192`, `200`, `204`, `206`, `208`, `212`, `216`, and `224 KiB`, then promoted a named `208 KiB` low-tide budget and gated VISITOR3 low/high plus FISHING1 high and FISHING3 high/low. | Promoted. `208 KiB` repeats at `loop_vb=1524`, `target_vb=1019`, `overrun_vb=505`, `blocking_vb=306`, `prefetch_overrun_vb=76`, `loop_reads=46`, and `due_misses=22`. `204/206 KiB` had a two-VBlank lower raw loop but worse target-relative speed and higher CD pressure; `224 KiB` was slower. Artifacts: accepted gate `scratch/ps1-perf-iterate/visitor3-low-setupprime208-gate/20260430-171506-1116478/summary.json`, repeated selected point `scratch/ps1-perf-iterate/visitor3-low-setupprime208-repeat/20260430-171347-1108590/summary.json`, and sweep points under `visitor3-low-setupprime192/200/204/206/208/212/216/224`. | Promote. This is another targeted setup-prime/CD-pressure win. The exact timing-bearing average moves from `14.6579%` to `14.6469%` over target, and target speed moves from `88.0942%` to `88.0991%`. Continue with generated segment metadata or the next high-pressure scene/tide; do not choose raw loop over battle-card ratio when target variance appears in repeated summaries. |
| 2026-04-30 | `walkstuf1-low-setup-prime-248` | `ps1: setup-prime walkstuf1 low foreground` | Probe whether WALKSTUF1 low can use a small contiguous setup-prime even though its indexed8 residual route uses a non-default `40 KiB` stream window. The row was still heavily CD-bound after the v44 split-window baseline (`2048/1414`, `blocking_vb=460`, `prefetch_overrun_vb=139`, `loop_reads=63`, `due_misses=34`). | Temporarily swept low-tide setup-prime sizes `768`, `512`, `288`, `256`, `248`, and `224 KiB`. Promoted a named low-tide `248 KiB` budget, then gated WALKSTUF1 low/high, FISHING1 high, FISHING3 high/low, and VISITOR3 high/low. | Promoted. `248 KiB` repeats at `loop_vb=2001`, `target_vb=1395`, `overrun_vb=606`, `blocking_vb=450`, `prefetch_overrun_vb=134`, `loop_reads=57`, and `due_misses=31`. `256 KiB` was lower than baseline but worse than `248 KiB` (`2014/1393`, `blocking_vb=464`); `224 KiB` worsened target-relative speed; `288/512 KiB` hit zero-loop structural failures; `768 KiB` failed allocation. Artifacts: accepted gate `scratch/ps1-perf-iterate/walkstuf1-low-setupprime248-gate/20260430-173447-1231646/summary.json`, sweep points under `walkstuf1-low-setupprime224/248/256/288/512/768`. | Promote. This is a scene/tide setup-prime exception for an indexed8 residual route, not a global policy. The exact timing-bearing average moves from `14.6469%` to `14.6352%` over target, and target speed moves from `88.0991%` to `88.1047%`. Next WALKSTUF1 work should use generated segment/read metadata; broad contiguous setup-prime above `248 KiB` is structurally unsafe. |
| 2026-04-30 | `walkstuf1-high-derived-setup-prime` | `ps1: derive walkstuf1 setup-prime budget` | Try to recover the large WALKSTUF1 high setup-prime win without paying a binary-layout penalty. The row still had `loop_vb=2024`, `blocking_vb=449`, `prefetch_overrun_vb=133`, `loop_reads=43`, and `due_misses=25` after split-window and low-prime work. | Swept high-tide setup-prime budgets `248`, `240`, `256`, `272`, `288`, `304`, `320`, and `312 KiB`. The explicit high/low branch made `304 KiB` fast but crossed the PS-EXE sector bucket and regressed FISHING3/VISITOR3 canaries. Replaced the branch with a compact derived budget `(normalWindowBytes << 2) + 88 KiB`, which preserves low tide's `248 KiB` and gives high tide `304 KiB`, then gated WALKSTUF1 high/low, FISHING1 high, FISHING3 high/low, and VISITOR3 high/low. | Promoted. WALKSTUF1 high improves `loop_vb 2024 -> 1970`, `target_vb 1423 -> 1404`, `overrun_vb 601 -> 566`, `blocking_vb 449 -> 400`, `prefetch_overrun_vb 133 -> 128`, `loop_reads 43 -> 39`, `loop_read_vb 533 -> 484`, and `due_misses 25 -> 21`. WALKSTUF1 low stays exact-flat at `2001/1395`; FISHING1, FISHING3, and VISITOR3 stay exact-flat. Artifacts: accepted gate `scratch/ps1-perf-iterate/walkstuf1-high-derived-setupprime-gate/20260430-181213-1451042/summary.json`; rejected explicit-branch gate `scratch/ps1-perf-iterate/walkstuf1-high-setupprime304-gate/20260430-180339-1400234/summary.json`; edge failures `walkstuf1-high-setupprime320` zero-loop and `walkstuf1-high-setupprime312` missing `JCPERF2`. | Promote. This is a phase-safe derived setup-prime policy for the indexed8 WALKSTUF1 pair. Exact timing-bearing average moves from `14.6352%` to `14.6192%` over target, and target speed moves from `88.1047%` to `88.1128%`. Next WALKSTUF1 work should use generated metadata or scheduler-owned grouping; broad one-off branches are too close to the executable sector boundary. |
| 2026-04-30 | `walkstuf1-derived-base92` | Log-only reject; no source promotion. | Test whether raising the compact derived WALKSTUF1 setup-prime base from `88 KiB` to `92 KiB` can squeeze a little more high-tide coverage while keeping the compact policy shape and PS-EXE bucket stable. This gives high tide `308 KiB` and low tide `252 KiB`. | Temporarily changed `FG_WALKSTUF1_SETUP_PRIME_BASE_BYTES` to `92 KiB` and gated WALKSTUF1 high/low plus FISHING1, FISHING3, and VISITOR3 canaries against v55. The source probe was reverted after the gate failed. | Rejected. High tide regressed immediately: `loop_vb 1970 -> 1986`, `target_vb 1404 -> 1403`, `blocking_vb 400 -> 407`, and `prefetch_overrun_vb 128 -> 156`, despite `loop_reads 39 -> 38`; low tide was not an offsetting win (`2001 -> 2000` but `blocking_vb 450 -> 451`). Artifact: `scratch/ps1-perf-iterate/walkstuf1-derived-base92/20260430-182537-1528218/summary.json`. | Do not promote. The derived setup-prime knee is sharp: `88 KiB` base is the accepted phase-safe point; larger base steals held slack even when read count drops. |
| 2026-04-30 | `walkstuf1-derived-base84` | Log-only reject; no source promotion. | Test the lower side of the compact derived WALKSTUF1 setup-prime knee. A base of `84 KiB` gives high tide `300 KiB` and low tide `244 KiB`; the hypothesis was that lower setup pressure might beat the accepted `88 KiB` base even if it covers slightly less payload. | Temporarily changed `FG_WALKSTUF1_SETUP_PRIME_BASE_BYTES` to `84 KiB` and gated WALKSTUF1 high/low plus FISHING1, FISHING3, and VISITOR3 canaries against v55. The source probe was reverted after the gate failed. | Rejected as a pair policy. High tide improved (`loop_vb 1970 -> 1965`, `overrun_vb 566 -> 564`, `blocking_vb` flat at `400`) but low tide regressed (`loop_vb 2001 -> 2002`, `overrun_vb 606 -> 607`, `blocking_vb 450 -> 451`). Artifact: `scratch/ps1-perf-iterate/walkstuf1-derived-base84/20260430-183504-1583933/summary.json`. | Do not promote as the shared derived base. The high-tide result is worth one compact high-only follow-up if it can preserve low tide's accepted `248 KiB` budget without growing the PS-EXE sector bucket. |
| 2026-04-30 | `walkstuf1-high-only-base84` | `ps1: trim walkstuf1 high setup-prime` | Promote the useful half of the rejected shared `84 KiB` base without disturbing low tide. The source keeps the compact derived formula but subtracts `4 KiB` only when `walkstuf1` is high tide, preserving low tide's accepted `248 KiB` setup-prime and giving high tide the `300 KiB` knee. | Added a compact high-only trim, gated WALKSTUF1 high/low plus FISHING1, FISHING3, and VISITOR3 canaries against v55, then repeated WALKSTUF1 high as a primary-only acceptance run so the CSV points at a clean passing summary. | Promoted. WALKSTUF1 high improves `loop_vb 1970 -> 1965`, `target_vb 1404 -> 1401`, and `overrun_vb 566 -> 564`; `blocking_vb` stays `400`, `prefetch_overrun_vb` rises within tolerance `128 -> 130`, `loop_reads` rises `39 -> 41`, and `due_misses` stays `21`. WALKSTUF1 low stays exact-flat at `2001/1395`, and FISHING1, FISHING3, and VISITOR3 stay exact-flat in the aggregate canary run. Artifacts: accepted primary `scratch/ps1-perf-iterate/walkstuf1-high-only-base84-accepted/20260430-185448-1699775/summary.json`; aggregate canary `scratch/ps1-perf-iterate/walkstuf1-high-only-base84/20260430-184345-1635399/summary.json`. | Promote. Exact timing-bearing average moves from `14.6192%` to `14.6187%` over target, and target speed moves from `88.1128%` to `88.1130%`. Continue with generated metadata or scheduler-owned grouping; setup-prime byte tuning is now at a very sharp knee. |
| 2026-04-30 | `visitor3-high-group156-168` | Log-only reject; no source promotion. | Test whether VISITOR3 high can use the existing grouped-append mechanism for the read-plan cluster `156..168`, avoiding the setup-cadence cost that made the earlier one-shot setup segment fail. | Temporarily added a VISITOR3 high read-group table `{156, 168}` and selected it for default-window high tide, then gated VISITOR3 high against v55. The source probe was reverted after the gate failed. | Rejected. The group did fire and reduced active loop reads `52 -> 50`, but the source shape grew the PS-EXE bucket `147456 -> 149504`, shifted `FG\\VISITOR3.FG2` by one LBA, and regressed visible timing: `loop_vb 1484 -> 1489`, `overrun_vb 471 -> 476`, `blocking_vb 321 -> 325`, and `prefetch_overrun_vb 88 -> 89`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group156-168-gate/20260430-185944-1728344/summary.json`. | Do not promote a manual VISITOR3 read group. The transaction win is real but currently smaller than the code/layout cost; retry only with generated group metadata that does not grow the hot source shape or after the executable bucket has spare room. |
| 2026-04-30 | `activity9-high-window20` | Log-only reject; no source promotion. | Test whether ACTIVITY9 high can use a scene-local `20 KiB` stream window. The parameter sweep showed a real row-local signal (`loop_vb 2270 -> 2260`, `overrun_vb 225 -> 212`, `blocking_vb 100 -> 85`, `reads 144 -> 108`, and `due_misses 10 -> 6`), so the follow-up compiled that window as the default high-tide policy. | Temporarily added `FG_ACTIVITY9_HIGH_WINDOW_BYTES` and selected it for `activity9` high tide, then gated the primary row against the fresh default sweep and ran FISHING1, FISHING3, and WALKSTUF1 canaries. The source probe was reverted after the canary/layout review. | Rejected. The primary row improved loop and blocking, but exceeded the strict refill tolerance (`prefetch_overrun_vb 51 -> 54`) and crossed the PS-EXE sector bucket (`147456 -> 149504`, ACTIVITY9 LBA `3682 -> 3683`). The canaries confirmed the layout cost: FISHING3 high regressed `overrun_vb 148 -> 149`, `blocking_vb 23 -> 26`, and `prefetch_overrun_vb 18 -> 20`; WALKSTUF1 high regressed `loop_vb 1970 -> 1972`, `blocking_vb 400 -> 403`. Artifacts: sweep `scratch/ps1-perf-iterate/activity9-high-window-sweep-20-32/20260430-190226-1743915/summary.json`, primary gate `scratch/ps1-perf-iterate/activity9-high-window20-default/20260430-190728-1773719/summary.json`, canaries `scratch/ps1-perf-iterate/activity9-high-window20-default-canaries/20260430-190900-1782755/summary.json`. | Do not promote a hand-coded ACTIVITY9 window. Keep the parameter result as evidence that ACTIVITY9 wants shorter reads, but retry through generated metadata, a code-size-neutral policy table, or after a binary-layout control pass creates spare room. |
| 2026-04-30 | `policy-table-refactor` | `ps1: table-drive foreground runtime policies` | Move the existing scene/tide foreground window and setup-prime exceptions out of long hand-coded branch chains and into compact policy tables. The hypothesis was not a VBlank win; it was a code-shape win that should preserve every accepted metric while reducing hot function size and making future generated metadata experiments less likely to cross the PS-EXE bucket. | Replaced the BUILDING4/6/WALKSTUF1 window branch chain and the FISHING/VISITOR setup-prime chain with two static tables, rebuilt, then gated FISHING1 high, FISHING3 high/low, BUILDING4 high/low, BUILDING6 high/low, VISITOR3 high/low, and WALKSTUF1 high/low. The matrix rows for those 11 variants were refreshed as `compact-fgp3-v57-policy-table-refactor`. | Promoted as a structural work-reduction win. All gated timing rows stayed exact-flat against their accepted metrics, including FISHING1 `1207/1076`, FISHING3 high `2098/1950`, BUILDING4 high `3073/2793`, VISITOR3 high `1484/1013`, WALKSTUF1 high `1965/1401`, and WALKSTUF1 low `2001/1395`. `foregroundPilotPlay` shrank `10108 -> 9576` bytes, the ELF shrank `732872 -> 731568` bytes, and the PS-EXE bucket stayed `147456`. Artifact: `scratch/ps1-perf-iterate/policy-table-refactor-canaries/20260430-191815-1835851/summary.json`. | Promote. This does not change the exact timing-bearing average (`14.6187%` over target / `88.1130%` target speed), but it creates a safer data-driven surface for retrying ACTIVITY9/BUILDING2 read-policy experiments without broad source-shape churn. |
| 2026-04-30 | `activity9-high-window20-table` | `ps1: tune activity9 high foreground window` | Retry the earlier ACTIVITY9 high `20 KiB` stream-window signal after `policy-table-refactor` removed enough hand-coded source shape to keep the executable bucket stable. Acceptance required the ACTIVITY9 row to improve, the PS-EXE bucket to stay at `147456`, and the layout-sensitive canaries to pass with zero regression. | Added ACTIVITY9 high to the foreground runtime window policy table with a `20 KiB` high-tide window and default low-tide window, gated against a fresh table baseline, then ran FISHING1 high, FISHING3 high/low, BUILDING4 high/low, BUILDING6 high/low, VISITOR3 high/low, and WALKSTUF1 high/low canaries with `--allow-regression 0`. | Promoted. ACTIVITY9 high improves `loop_vb 2270 -> 2259`, `target_vb 2045 -> 2047`, `overrun_vb 225 -> 212`, `blocking_vb 100 -> 84`, `loop_reads 144 -> 108`, `loop_read_vb 659 -> 548`, and `due_misses 10 -> 6`; `prefetch_overrun_vb` rises only `51 -> 53`. The canary set stayed exact-flat, including FISHING1 `1207/1076`, FISHING3 high `2098/1950`, BUILDING4 high `3073/2793`, VISITOR3 high `1484/1013`, WALKSTUF1 high `1965/1401`, and WALKSTUF1 low `2001/1395`. Artifacts: baseline `scratch/ps1-perf-iterate/activity9-high-current-table-baseline/20260430-193536-1934622/summary.json`, accepted primary `scratch/ps1-perf-iterate/activity9-high-window20-table/20260430-193706-1943777/summary.json`, canaries `scratch/ps1-perf-iterate/activity9-high-window20-table-canaries/20260430-193849-1954158/summary.json`. | Promote. This validates the policy-table surface as the right way to retry previously rejected scalar read-policy wins. Exact timing-bearing average moves from `14.6187%` to `14.6132%` over target, and target speed moves from `88.1130%` to `88.1175%`. Continue with generated metadata or similarly compact scene/tide policies; avoid hand-coded branches near the PS-EXE sector boundary. |

## Retry Queue

| Candidate | Reason to revisit |
|---|---|
| `40 KB` stream window | Failed structurally, not because metrics proved it slow. Retry only after the headless exit-137 failure mode is understood or memory pressure is reduced elsewhere. |
| `56 KB` stream window | Correct but slower than `32 KB`; retry only if later changes can hide larger refill reads instead of blocking on them. |
| `64 KB` stream window | Earlier run also exited `137` before `JCPERF2`; likely too much pressure for the current simple single-window strategy. |
| `16 KB` stream window | Produced partial metrics but regtest exited `137`; metrics were worse on `blocking_vb` and `due_misses`, so this is low priority. |
| `no-stage1` window-only path | Failed structurally before metrics; retry only after headless stability improves or after stage/window code is simplified. |
| Partial tail reads | Proved harmful in isolation because fewer bytes did not compensate for more transactions; retry only as part of pack/group layout work. |
| Same-iteration stage-copy fallthrough | Structural failure before metrics; retry only with explicit state that prevents repeated FG2 lookup/read churn. |
| `6` VBlank prefetch slack threshold | Correct but slower; retry only if later pack/layout changes reduce due-frame miss cost enough that stricter prefetch gating can win. |
| Short-slack direct staging | Correctness clean but slower; retry only if direct-read transaction overhead is reduced by grouped or physically adjacent pack reads. |
| Holiday overlap restamping | Correctness clean but no measured fishing1 benefit; retry only after per-scene/holiday overlap stats show sparse contact. |
| Skip FG2 `ClearOTagR` | Correctness clean but no VBlank-level movement; retry after adding a cheap CPU-work metric or if primitive-buffer cleanup becomes a code-size pass. |
| X-aware upload via one scratch rectangle | Booted but failed to finish before `JCPERF2`; retry only with 16-pixel-aligned strip batching and no extra per-partial `DrawSync` calls. |
| `22 KB`, `23 KB`, `26 KB`, and `28 KB` post-slack windows | All lost to the accepted smaller-window point or tied without improvement; retry only after pack grouping or async refill changes the CD cost model. |
| `1` VBlank post-20 KB refill guard | Correctness clean but slower; retry only if later CD grouping or async refill makes the short-slack read cost materially cheaper. |
| `4` VBlank staged-copy fallthrough guard after the post-merge baseline | Correctness clean but identical to the accepted `5` VBlank guard; retry only after another change alters staged-copy/window timing. |
| `4` VBlank staged-copy fallthrough guard after `16 KB`/`3` VBlank retune | Correctness clean and lower blocking, but total `loop_vb` regressed by `10`; retry only after refill/grouping changes make the extra fallthrough read cheaper. |
| Split immediate/lookahead refill guards | Correctness clean and recovered due misses, but total `loop_vb` regressed by `14`; retry only after grouped or sector-aligned refills reduce short-read cost. |
| Screenwide PAL4 row dirty marking | Correctness clean and faster, but widened restore across the tile boundary; retry only as per-tile row aggregation or pack-emitted per-tile extents. |
| `4` VBlank refill guard after per-tile row dirty marking | Correctness clean and slightly faster overall, but visible blocking regressed; retry only if grouped/pipelined coverage prevents due misses. |
| `15 KB` and `14 KB` windows after the `6` VBlank fallthrough guard | `15 KB` is a no-op under rounding; `14 KB` lowers overrun but starves due frames. Retry only after pack grouping makes smaller windows cover more useful entries. |
| PAL4 visible-row dirty-branch hoist | Correctness clean but no VBlank-level movement; retry only with finer CPU counters or if grouped with other compositor branch cleanup. |
| Direct `markTileDirtyRect()` from PAL4 row dirty aggregation | Correctness clean but slower than the generic wrapper in this build; retry only if dirty marking is refactored into a smaller shared inline path. |
| FG2 compose-before-VSync sequencing | Correctness clean but slower and worse for CD blocking; retry only with Detail-tier render counters or after separating render scheduling from prefetch cadence. |
| `7` VBlank staged-copy fallthrough guard after per-tile row dirty marking | Correctness clean but slower with no blocking improvement; retry only after the refill-cost knee moves. |
| Staged next-VBlank present scheduler | Correctness and visual-work identity stayed clean, but total loop time regressed by `71` VBlanks; retry only after render preparation and CD prefetch have separate slack budgets. |
| Prepared-present thresholds `2`, `3`, and `5` | `2` VBlanks reduced loop time but failed the strict blocking/refill gate; `3` VBlanks kept loop flat while regressing blocking/refill and adding prep work; `5` VBlanks added prep work without timing improvement. Retry only if prepared-frame selection becomes exact-use instead of extra speculative restore/compose work. |
| Prepared-stage decoupling | Correctness-clean only after blocking CD reads during prepared waits, which made fishing1 flat at `loop_vb=1221`; retry only with explicit prepared-present/CD-budget metrics or a real second visual buffer. |
| Exact-4 VBlank prepared-present gating alone | Earlier pre-pause probes reduced speculative restore/compose calls but regressed loop, blocking, and refill-overrun by one VBlank each. The post-pause combined exact-4 plus 4-row upload-gap checkpoint is accepted only as a flat-key-timing work-reduction pass. |
| 4-or-5 VBlank prepared-present gating | Regressed loop/blocking/refill and increased prep calls; do not retry max-slack caps without prepared-used counters. |
| Prepared-wait stream-window prefetch | Post-gap1 retry is now accepted as flat-timing work reduction (`restore_calls/compose_calls 166 -> 155`). Further retries should be part of a full prepared/CD scheduler, not the old opportunistic shape. |
| Prepared-present `>=4` threshold after prepared-wait prefetch | Exact timing and work no-op with only loop bookkeeping movement; keep exact-`4` unless a new scheduler signal changes the budget. |
| `5` VBlank staged-copy fallthrough after prepared-wait prefetch | Still exposes extra CD pressure; keep the accepted `6` VBlank guard unless grouped reads make the same-iteration lookahead cheaper. |
| Long-hold catch-up threshold `4` after prepared-wait prefetch | Still regresses target and visible CD pressure; keep threshold `5` until grouped/predictive prefetch changes the slack budget. |
| Prefetch would-read probe removal | Regressed by shifting CD phase (`blocking_vb 5 -> 6`); retry only as part of an explicit pacing/scheduler rewrite. |
| Prefetch candidate reuse | Kept exact cadence flat but grew the ELF and did not move any tracked work counter; retry only as part of a broader prefetch state-machine rewrite or with finer CPU counters. |
| Host-timing preconvert on load | Runtime-flat but code-growth/unstable; retry only if finer CPU counters prove `fgEntryHoldVBlanks()` is material. |
| PAL4 aligned pair store | Dynamic alignment branch regressed CD phase badly; retry only as generated/assembly compositor work without runtime branching. |
| Caption render default-off gate | Runtime-flat but grew the executable; retry only with an inline/global caption-active flag rather than another function call. |
| Automatic FGHEAP probe compile gate | Failed as a standalone cleanup on the old fishing1 phase, but is now accepted as layout recovery for the FISHING3 high segmented setup-prime probe. Keep it tied to an otherwise-gated speed/layout win; do not treat heap-probe removal alone as a VBlank optimization. |
| Prepared-current RAM background reuse | Removed all duplicate prep work but regressed the same one-VBlank timing shape; retry only with explicit replacement pacing. |
| Post-prefetch leftover prepare | More prep work after CD reads created five due misses and many more backward seeks; retry only with frame-level budgeting that proves future coverage is preserved. |
| Host-deadline one-VBlank latency offset | `loop_vb` did not move and `target_vb` dropped by one; retry only with a scheduler-state change that models render/present latency separately from source deadlines. |
| `5` VBlank fallthrough guard after prepared-present | Timing flat but no key improvement, and hidden CD read time worsened; keep `6` VBlanks until grouped reads or prepared-used counters change the slack model. |
| Direct-stage payload cap tuning | Post-gap8 and post-caption sweeps confirm `8 KB` as the fishing1 knee: `6 KB`, `7 KB`, and `10 KB` are no-ops, while `4 KB` regresses blocking/refill. Retry only after grouped reads or a second stage slot changes direct-stage coverage cost. |
| Split immediate/lookahead refill guards | Raising pure-lookahead refills to `4` VBlanks regressed loop/blocking/refill while preserving due misses; fixed scalar guard splitting is exhausted until read-cost prediction or pack groups exist. |
| Runtime read-size predictor for lookahead refills | An `8 KB` tight-read cap improved `loop_vb` by two but worsened blocking/refill and changed cadence; `12 KB` regressed outright. Needs per-read histograms or pack groups before retry. |
| Two-entry direct-stage queue | A second exact-payload slot still caused `due_misses 0 -> 3`; retry only when the second slot is fed by grouped coverage, not isolated direct reads. |
| Window-resident due-frame direct pointer | Correctness clean but slower than copying to `frameBuffer`; retry only if the stream-window/stage ownership model changes. |
| Circular FG2 stream window | Correctness clean but no VBlank-level movement; retry only with finer CPU counters or after grouped-window metadata changes the refill path. |
| Sequential aligned CD reads without explicit `Setloc` | Timing looked better but visual-work counters collapsed; do not retry unless a lower-level CD continuation API is proven correct with full frame hashes/work-identity gates. |
| Clean-rect restore intersection skip | Correctness stayed clean but CD/prefetch metrics regressed; retry only if clean-rect grouping changes enough that skipped restore scans are common and the check can be made pack-time or table-driven. |
| CD stream file-start LBA cache | Correctness clean but exact VBlank-level no-op; retry only with finer CPU counters or during a broader CD helper cleanup pass. |
| PAL4 cross-tile direct split | Correctness clean but slower after the tile-local fast path; retry only as pack-time tile-split spans, not as a runtime branch. |
| PAL4 zero-offset opaque-run helper | Exact VBlank-level no-op; retry only with finer CPU counters or a generated compositor where the helper can be folded into the row loop. |
| `5` VBlank fallthrough guard after tile-local PAL4 fast path | Correctness clean but slower total loop and worse blocking/refill overrun; keep `6` VBlanks unless a future grouped-read change moves the slack knee. |
| Restore full-row coalescing | Exact VBlank-level no-op; retry only if pack-time restore bands identify enough contiguous full-width rows to make the branch worthwhile. |
| PAL4 four-pixel loop unroll | Exact VBlank-level no-op; retry only if generated compositor code can combine unrolling with pack-known alignment/length classes. |
| `2` VBlank stream-window refill guard after tile-local PAL4 fast path | Correctness clean but slower and more visibly blocking; keep `3` VBlanks unless grouped reads make short-slack refills cheaper. |
| Single-call prefetch-window check | Exact VBlank-level no-op; retry only as part of a broader prefetch state-machine cleanup. |
| Vertical dirty-row upload band merge/capping | The post-pause baseline now uses a 1-row byte-trim gap (`upload_bytes 16499200 -> 16281600`) with flat key timing and more rects (`401 -> 502`). Retry wider or narrower thresholds only with a targeted sweep or pack-emitted/upload-ready layouts. |
| Zero-gap dirty upload bands | Lowered upload bytes further but raised upload rects to `515` and regressed loop/blocking/refill; the current boundary is between `1` and `0` rows. Do not retry exact splitting unless upload-rect setup cost is proven negligible. |
| Single reusable upload `RECT` | Timing and work identity stayed flat, but ELF grew by `64` bytes; retry only if stack pressure becomes a measured problem or the upload path is refactored structurally. |
| `12`-row dirty upload band merge gap | Reduced upload rectangles to `399` but increased uploaded bytes to `16514560` with no timing gain; retry only if a future metric proves per-rect CPU setup dominates DMA volume on target hardware. |
| Inline CD-read histogram metrics | Detail-gating did not save it; merely adding the code changed binary/code shape and regressed timing. Retry only as a compile-time separate diagnostic build or by post-processing existing logs. |
| Hot translation unit `-O3` | Less prepared RAM work but worse loop/blocking/refill timing; retry only as a targeted single-function/assembly change or after adding binary/map-size gates. |
| Direct-stage read into stream window | Avoided one RAM copy in theory but increased loop reads and backward seeks; retry only if direct-stage can merge with existing window coverage instead of replacing it. |
| Held-loop no-slack wait skip | Looked like a one-VBlank overshoot cleanup, but regressed loop/blocking/refill and added prepared work; retry only with an explicit frame-deadline scheduler that proves the wait is not needed for CD cadence. |
| Direct-stage scratch merge into stream window | Correctness clean but regressed loop/blocking/refill with extra prep calls; retry only after read histograms or pack groups show a specific overlap case worth preserving. |
| Proactive stream-window extension while next payload is resident | Preserved zero due misses but doubled visible CD pressure; retry only when read-cost/group metadata can distinguish cheap append opportunities from slack-stealing reads. |
| Inline PAL4 span compositor | Correctness and work identity were stable but total loop, blocking, and refill overrun regressed; retry only as generated scene-specialized code, not as a local helper-inline rewrite. |
| Slack-adaptive `8/12/16 KB` stream windows | Lowered refill overrun and total loop slightly but regressed visible blocking; retry only after pack grouping preserves due-frame coverage. |
| `12 KB` stream window after prepared-present | Cut refill overrun to `1` VBlank but produced `26` due misses and `blocking_vb=89`; do not retry smaller raw windows before grouped coverage exists. |
| `14/18/20 KB` stream windows after prepared-present | All regressed total loop versus the accepted `16 KB` default; retry window sizes only after grouped/pack-aware reads change useful coverage per transaction. |
| Leading empty setup consume | Saves `6` loop VBlanks by skipping a non-payload startup render, but raises blocking/refill overrun by one VBlank and changes render count; retry only with CD smoothing and visual signoff policy for empty capture artifacts. |
| Zero-delay `eventsWaitTick` fast path | Exact VBlank-level no-op; retry only with finer CPU counters or during event/pause cleanup. |
| `19 KB` requested window after the post-2VB sweep | Sector-rounds to the old `20 KB` default and is a no-op; use sector-sized buckets for future sweeps. |
| `125 Hz` / `65 Hz` SPI pad polling | Correctness clean but `loop_vb` regressed by one VBlank while only CD submetrics improved; retry only with finer CPU/IRQ counters or a pause-input validation harness. |
| Post-row-restore `16/20/24/32 KB` window sweep under the `2` VBlank guard | Correctness clean but all tested sizes regressed total loop versus the then-accepted `18 KB` default; the `16 KB` point became useful only when paired with the later `3` VBlank guard. |
| Targeted dirty-row state clearing | Correctness clean but no VBlank-level improvement; retry only with finer CPU counters or when dirty-state maintenance is being refactored anyway. |
| Targeted current dirty-row clearing | Exact key timing no-op with a small prepared-work identity shift; retry only if lower-level CPU counters can prove the row-clear cost matters. |
| Runtime PAL4 pair LUT and aligned word stores | Correctness clean but slower than direct halfword palette writes; retry as pack-time direct16 or with lower-overhead scene-palette specialization. |
| 16-pixel X-aware upload strips with per-strip sync | Failed to reach scene-end within the headless frame window; retry only with a bounded scratch arena that allows batching partial strips behind one final `DrawSync`. |
| 32-pixel X-aware upload arena | Correctness clean and byte volume improved, but rect count exploded to `11575`; retry only with deterministic rect-count caps or wide-band merging. |
| Runtime scratch-packed X upload | Even capped tile-band packing regressed because CPU copy cost dominated; retry only if the pack/runtime stores upload-ready contiguous bands or the GPU upload source becomes naturally contiguous. |
| FG2 sound-event table in metadata prefix | Setup reads improved, but moving the table ahead of payload shifted every payload by `36` bytes and badly worsened active CD phase; retry only with payload offsets/sector crossings preserved or with pack-group metadata that makes the new layout intentional. |
| FG2 resolve-once startup reads | Setup improved, but active playback regressed and speculative prep cadence changed; retry only after adding an explicit setup/loop cadence barrier or decoupling prepared-present from startup timing. |
| Trim stream-window reads to complete payloads | Reduced bytes/sectors while preserving hits and due misses, but increased elapsed loop CD time and visible blocking; retry only with per-read cost classes or emitted group metadata. |
| Prepare staged frame before lookahead prefetch | Reduced restore/compose work but regressed CD blocking and seek-backs; retry only with separate render-prep and prefetch slack budgets that prove future read coverage is preserved. |
| Prefetch during prepared-frame waits | Removed duplicate prepared work but regressed blocking/refill by one VBlank; retry only after group metadata or a scheduler cost model proves the read fits the remaining held cadence. |
| Prepared-present min slack 5 | Saved only three prep calls and regressed blocking/refill by two VBlanks; do not retry threshold-only prepared-present pruning without a new scheduler signal. |
| Pad diagnostics compile gate | Large binary-size win but active timing regressed badly; retry only with a layout-preserving/cold-section strategy or after the CD scheduler no longer depends on current code shape. |
| Visual debug screen compile gate with EXE layout pad | Preserving `FISHING1.FG2` at LBA `390` did not recover the earlier 4 KB code-size win; retry only after scheduler phase is explicitly controlled or cold debug code can be isolated without changing hot code placement. |
| Dirty-row clear via `memset` | Smaller code and a simpler source shape still regressed active cadence, including with the EXE padded back to the accepted LBA; retry only with a custom inline MIPS fill or a packed dirty-state layout that changes the operation count, not just the library call. |
| Setup-settle first-frame prerender | It improved active `loop_vb` by moving first-frame render work into setup, but full `scene_vb` stayed flat and visible CD pressure regressed; retry only if the scheduler can preserve CD phase or if the setup render actually lowers full-scene timing. |
| Compose-ever telemetry removal | Exact timing no-op and loadable text grew by `28` bytes despite removing a hot store; retry only as part of a broader foreground telemetry/API pruning pass with map/layout review. |
| Long-hold catch-up threshold `4` | No loop win and worse CD pressure; keep the accepted threshold `5` until grouped prefetch or a better slack budget can prove additional catch-up is free. |
| Prepared-present threshold `5` after catch-up | Reduced duplicate prep and nominal loop by one VBlank, but regressed visible CD pressure; retry only with grouped/read-cost-aware prefetch. |
| Long-hold extra catch-up for `>=9` VBlank holds | Exact no-op under fishing1; retry only if another scene's hold distribution shows actual late long-hold opportunities. |
| `4 KB` FG2 metadata prefix | Reduced setup bytes and one setup read VBlank but regressed active CD pressure; retry only after setup-to-loop phase is deliberately normalized or group metadata decouples playback cadence from startup read shape. |
| Lookahead-covered short-hold catch-up | Immediate next-frame staging plus following-window coverage still regressed visible CD pressure; retry only with an explicit render-prep/CD budget, not another timing guard. |
| Cached no-holiday compose gate | Exact timing no-op and larger ELF; retry only through generated per-variant compose code or broader overlay cleanup, not another runtime flag. |
| Dirty-row pointer swap | Saved one total loop VBlank and shrank the binary, but regressed visible CD/refill by three VBlanks; retry once CD scheduling can tolerate the changed render/prep phase. |
| Clean-rect dirty-Y narrowing | VBlank-level no-op that increased ELF size; retry only with finer CPU counters or when pack-emitted restore bands replace runtime row scans. |
| Adaptive high-slack `24 KB` stream window | Fewer reads and lower hidden loop read time still regressed visible blocking/refill; retry only when pack groups or a read-cost predictor can prove the larger read fits the held cadence. |
| High-slack `24 KB` capacity with `10+` VBlank guard | Even the conservative long-hold-only form kept `loop_vb` flat and regressed visible pressure; retry only with exact group boundaries plus append/preserve cost, not a raw held-slack threshold. |
| Adaptive high-slack `20 KB` stream window | Smaller than the `24 KB` probe but still regressed visible blocking/refill; do not retry raw extended windows without pack/read-cost metadata. |
| Prepared-buffer release | Removed duplicate prepared restore/compose work but either regressed visible CD or starved the due-frame path; retry only as a full scheduler redesign with explicit prepared-used and payload-buffer ownership states. |
| `3` VBlank refill guard after row-level restore | Correctness clean and faster in total loop, but blocking and due misses regressed too far; retry only after grouped/pipelined prefetch coverage prevents starvation. |
| `4` VBlank refill guard after setup-prime | Correctness clean and faster in total loop, but due misses returned and `blocking_vb` regressed from `13` to `19-20`; retry only after grouped or physically adjacent reads preserve zero due misses under the stricter guard. |
| `18 KB` stream window after setup-prime | Due misses stayed zero, but `loop_vb`, `blocking_vb`, and `prefetch_overrun_vb` all regressed; keep the sector-rounded `16 KB` default until grouped/pipelined reads change the CD cost model. |
| Cached base-diff runtime flag | Exact VBlank-level no-op; retry only with finer CPU counters or when doing broader runtime-state/code-size cleanup. |
| Single-row exact-width upload | Fishing1 never hit the single-row upload case under current dirty aggregation; retry only if another scene's dirty distribution shows one-row bands. |
| Narrow upload bookkeeping locals | ELF shrank but timing regressed sharply; retry only with map/register review or upload-path translation-unit isolation. |
| Staged-frame prep at `3` VBlanks | Improved nominal overrun but regressed visible CD pressure; retry only with explicit render-prep/CD slack budgeting. |
| Direct-stage read into stream window | `8 KB` aligned windows made the loop two VBlanks faster but added one visible CD VBlank; `6 KB` was a no-op. Retry only with grouped/predicted direct-stage windows. |
| `5` VBlank fallthrough guard after OT-clear skip | Regressed total loop and refill overrun; keep `6` VBlanks unless another CD-cost change moves the knee. |
| Async stream-window refill | Naive async polling regressed visible blocking badly; retry only after adding first-class async counters and CD-state ownership, not as an inline replacement for synchronous refill. |
| Prepared-present min slack 3 | Increased speculative prep and still regressed visible CD/refill by four VBlanks; do not retry threshold-only prepared-present tuning until a scheduler model can prove whether prep ballast or CD work should own each held slice. |
| Stream-window grow capacity | A `24 KB` retained window with `16 KB` normal reads improved total loop by one VBlank but reintroduced due misses and raised `blocking_vb` to `16`; retry only when pack-emitted read groups or per-read cost classes can cap transaction churn. |
| FG2 fixed 16 KB payload grouping | Lowered read count but increased sectors and visible blocking; retry only as selective group metadata that pads or groups around measured hot boundaries, not every arbitrary `16 KB` crossing. |
| FG2 payload-start sector alignment | A cheap `904`-byte global offset shift doubled visible blocking; do not globally align payload bases without preserving measured active-loop offset cadence. |
| FG2 pause-poll-only events | Reduced apparent work but regressed loop/blocking/refill by one VBlank; retry only with a full FG2-owned event/present scheduler, not a local event-tail removal. |
| Post-pause raw stream-window sizes `18 KB` and `14 KB` | Both lost against the exact no-holiday post-pause `16 KB` default: `18 KB` overran held slack, and `14 KB` introduced `12` due misses. Retry raw window sizes only after pack groups or a costed read scheduler change useful coverage per transaction. |
| `cdrom_ps1.c -O3` | Whole-TU optimization grew the executable by three sectors, shifted `FISHING1.FG2` three LBAs later, and raised visible CD/refill pressure from `5` to `13` VBlanks. Do not retry hot whole-TU `-O3` until code layout and CD phase can be held fixed. |
| Pure FG2 LBA shift | Adding `+1`, `+2`, `+3`, `+4`, or `+8` dummy sectors after the executable moved `FISHING1.FG2` as far as LBA `407` with exact flat timing. Do not spend more time on pack LBA alone; executable/code/startup phase is the remaining phase-control target. |
| Held-loop prefetch pre-check removal | Shrank code and removed a redundant lookup, but with pack LBA restored it still regressed visible CD pressure. Treat hot-loop source cleanups as phase-sensitive until hot-symbol padding or a scheduler barrier exists. |
| Pure executable padding | Adding `2 KB` of initialized executable payload shifted hot symbols by `+8` and kept timing flat. Padding is a control tool, not a speed win by itself. |
| `fgRuntimeFillWindowForEntry` alignment | `+4`, `+20`, and downstream `+36` byte hot-symbol shifts were timing-flat. Do not expect small positive alignment shifts alone to close the current gap. |
| Upload rect cap `6` | Re-tested cleanly but still provides no measured binary/timing win and removes cross-scene headroom. Keep cap `8` until matrix data says otherwise. |
| WALKSTUF3 low padded FGP3 | Payload only shrank `17.8%`, loop improved by `9` VBlanks, and `prefetch_overrun_vb` regressed by `4`. Retry only with generated grouping/direct16/residual metadata that changes read shape, not plain residual conversion. |
| Prepared-present event-wait removal | Regressed visible CD pressure and weakens input polling; keep the explicit poll until a broader present/input scheduler replaces it. |
| Owned-idle 4 VBlank catch-up | The new ownership counters showed no eligible idle catch-up slots in fishing1 (`catchup_idle=0`) and the prototype moved the executable/CD layout. Retry only after per-frame ownership analysis proves a real slot exists. |
| Deep trace pipeline counters | Done behind `PS1_PERF_DEEP_TRACE=ON`; do not compile into the accepted speed binary because the ungated version changed EXE size/layout and timing. |
| Prepared bridge slack `>=4` | Do not retry as a local threshold. It moved ownership buckets but did not increase useful prepared-present work or improve `loop_vb`. |
| Global catch-up threshold `4` | Do not retry globally; it regressed visible CD pressure to `16` VBlanks. Threshold `4` is only safe when setup-prime or future scheduler metadata proves coverage. |
| Setup-prime `192 KB` or `256 KB` windows | Do not retry blind sizes. `256 KB` was close but still raised visible CD pressure, and `192 KB` lost; future sizes need generated per-scene/tide coverage analysis. |
| Setup-prime `320 KB` window | Done for fishing1 high tide as an active-loop win with setup-cost trade. Extend only through explicit inter-scene preload or generated setup-prime policy, not by assuming setup time is free. |

| 2026-04-30 | `o2-audit-report` | `docs: add ps1 o2 audit surface` | Start the new `-O2`-first optimization path with evidence instead of guessing. The hypothesis was that an audit built from the real compile database and map file would expose the exact `-Os` overrides, function-scoped optimize attributes, PS-EXE bucket, and symbol sizes needed to run one-target `-O2` probes safely. | Added `scripts/ps1-o2-audit.py`, ran a clean PS1 build, then generated `docs/ps1/performance-o2-audit.md` and `docs/ps1/performance-o2-audit.csv`. Linked the audit from README, the PS1 docs entrypoint, the website performance page, and the active optimization queue. | Promoted as tooling. The audit reports `10` translation units at default `-O2`, `13` translation units at `-Os`, and `4` function-scoped `optimize("Os")` helpers. The first concrete probes are `grDrawBackground()` and `grUpdateDisplay()` at default `-O2`, followed by hot/semi-hot `-Os` TUs. Build snapshot: `jcreborn.exe=169984`, `jcreborn.elf=791808`, `jcreborn.map=45253`. | Promote as HP-061. This is not counted as a VBlank speed win; the timing-bearing average remains `+14.6132%` over target / `88.1175%` target speed until the next measured runtime optimization. |
| 2026-04-30 | `grdrawbackground-o2` | `docs: log grdrawbackground o2 rejection` | Test the first scoped graphics helper from the `-O2` audit by removing only the `grDrawBackground()` function-level `optimize("Os")` attribute. Acceptance required any VBlank or work-counter improvement, or at least exact-flat timing with a material code-size improvement. | Rebuilt clean with `grDrawBackground()` at default `-O2`, regenerated the O2 audit, then ran the four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with zero regression allowed. | Rejected. FISHING1 high, FISHING1 low, FISHING2, and FISHING3 stayed exact-flat at `1209/1073`, `1207/1075`, `1899/1764`, and `2092/1954`; `blocking_vb` and `prefetch_overrun_vb` also stayed exact-flat at `5/1/4/11`. The PS-EXE bucket stayed `169984`, but `grDrawBackground` grew by `504` bytes and the ELF grew `791808 -> 792376`. Artifact: `scratch/ps1-perf-iterate/o2-grdrawbackground-probe/20260430-214142-2622795/summary.json`. | Do not promote. Source was restored to scoped `-Os`, the audit was regenerated, and HP-062 continues with `grUpdateDisplay()` as the next isolated helper probe. |
| 2026-04-30 | `grupdatedisplay-o2` | `docs: log grupdatedisplay o2 rejection` | Test the second scoped graphics helper from the `-O2` audit by removing only the `grUpdateDisplay()` function-level `optimize("Os")` attribute. Acceptance required a timing/work win because the build snapshot already showed helper and ELF growth. | Rebuilt clean with `grUpdateDisplay()` at default `-O2`, regenerated the O2 audit, then ran the same four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with zero regression allowed. | Rejected. FISHING1 high, FISHING1 low, FISHING2, and FISHING3 again stayed exact-flat at `1209/1073`, `1207/1075`, `1899/1764`, and `2092/1954`; `blocking_vb` and `prefetch_overrun_vb` stayed exact-flat at `5/1/4/11`. The PS-EXE bucket stayed `169984`, but `grUpdateDisplay` grew by `40` bytes and the ELF grew `791808 -> 791976`. Artifact: `scratch/ps1-perf-iterate/o2-grupdatedisplay-probe/20260430-214910-2663322/summary.json`. | Do not promote. Source was restored to scoped `-Os`, the audit was regenerated, and the `-O2` sweep moves to HP-063 hot/semi-hot `-Os` translation units. |
| 2026-04-30 | `foreground-pilot-tu-o2` | `docs: log foreground pilot o2 rejection` | Test the first hot whole-translation-unit `-O2` candidate by removing only `src/foreground_pilot.c` from the forced `-Os` list. Acceptance required a measured runtime improvement large enough to justify code growth and any PS-EXE sector-bucket movement. | Rebuilt clean with `foreground_pilot.c` at default `-O2`, regenerated the O2 audit, then ran the four-case matrix and a single-case repeat against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with layout movement allowed but zero timing regression allowed. | Rejected structurally. The build grew `jcreborn.exe 169984 -> 172032`, `jcreborn.elf 791808 -> 806096`, and `foregroundPilotPlay 0x252c -> 0x3114`. Both runtime attempts failed before `JCPERF2` on `fishing1-high` with regtest exit `137`; the repeat log reached resource startup, located `RESOURCE.MAP` at LBA `92749` and `RESOURCE.001` at LBA `92750`, then emitted sustained reads around LBA `93015+` with no scene-end metrics. Artifacts: `scratch/ps1-perf-iterate/o2-foreground-pilot-tu-probe/20260430-215654-2701958/fishing1-high/headless-regtest.log` and `scratch/ps1-perf-iterate/o2-foreground-pilot-tu-probe-repeat/20260430-215732-2706357/fishing1-high/headless-regtest.log`. | Do not promote. Keep `foreground_pilot.c` at `-Os`; only retry foreground codegen as function-scoped, split-TU, or layout-padded experiments. Continue HP-063 with the remaining hot/semi-hot `-Os` translation units one at a time. |
| 2026-04-30 | `jc-reborn-tu-o2` | `docs: log jc_reborn o2 rejection` | Test the next hot/semi-hot whole-translation-unit `-O2` candidate by removing only `src/jc_reborn.c` from the forced `-Os` list. Acceptance required a timing or work-counter win because this file sits early in link order and can shift every downstream hot symbol. | Rebuilt clean with `jc_reborn.c` at default `-O2`, regenerated the O2 audit, then ran the same four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with layout movement allowed and zero timing regression allowed. | Rejected. The run passed correctness, but all four cases stayed exact-flat: FISHING1 high `1209/1073`, FISHING1 low `1207/1075`, FISHING2 `1899/1764`, and FISHING3 `2092/1954`; `blocking_vb` and `prefetch_overrun_vb` stayed `5/1/4/11`. The PS-EXE bucket stayed `169984`, but the ELF grew `791808 -> 795996`, the map shrank `45253 -> 45179`, and tracked hot symbols shifted by `+1300` bytes without a size or timing benefit. Artifact: `scratch/ps1-perf-iterate/o2-jc-reborn-tu-probe/20260430-220234-2728151/summary.json`. | Do not promote. Source was restored to `-Os`, the audit was regenerated, and HP-063 moves to the remaining candidates: `resource.c`, `sound_ps1.c`, and `events_ps1.c`. |
| 2026-04-30 | `resource-tu-o2` | `docs: log resource o2 rejection` | Test `src/resource.c` at default `-O2` because resource lookup/setup code can affect scene loading and the first-frame path without changing foreground composition. Acceptance required a timing or work-counter win because the build snapshot showed ELF growth and hot-symbol movement. | Removed only `src/resource.c` from the forced `-Os` list, rebuilt clean, regenerated the O2 audit, then ran the four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with layout movement allowed and zero timing regression allowed. | Rejected. FISHING1 high, FISHING1 low, FISHING2, and FISHING3 stayed exact-flat at `1209/1073`, `1207/1075`, `1899/1764`, and `2092/1954`; `blocking_vb` and `prefetch_overrun_vb` stayed `5/1/4/11`. The PS-EXE bucket stayed `169984`, but the ELF grew `791808 -> 793392`, the map shifted `45253 -> 45180`, and tracked foreground hot symbols moved by `+240` bytes without any measured work benefit. Artifact: `scratch/ps1-perf-iterate/o2-resource-tu-probe/20260430-221206-2776322/summary.json`. | Do not promote. Source was restored to `-Os`, the audit was regenerated against the accepted build, and HP-063 continues with `sound_ps1.c` and `events_ps1.c`. |
| 2026-04-30 | `sound-tu-o2` | `docs: log sound o2 rejection` | Test `src/sound_ps1.c` at default `-O2` because the fully working sound path runs during FISHING1 and could expose cheap audio/event-side speed without touching foreground composition. Acceptance required a timing or work-counter win because the probe grew ELF and shifted downstream helpers. | Removed only `src/sound_ps1.c` from the forced `-Os` list, rebuilt clean, regenerated the O2 audit, then ran the four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with layout movement allowed and zero timing regression allowed. | Rejected. FISHING1 high, FISHING1 low, FISHING2, and FISHING3 stayed exact-flat at `1209/1073`, `1207/1075`, `1899/1764`, and `2092/1954`; `blocking_vb` and `prefetch_overrun_vb` stayed `5/1/4/11`, and `loop_reads` stayed `20/14/19/33`. The PS-EXE bucket stayed `169984`, but the ELF grew `791808 -> 791988`, the map stayed `45253`, and tracked CD helper symbols shifted by `+64` bytes without any measured work benefit. Artifact: `scratch/ps1-perf-iterate/o2-sound-tu-probe/20260430-221830-2813721/summary.json`. | Do not promote. Source was restored to `-Os`, the audit was regenerated against the accepted build, and HP-063 continues with `events_ps1.c`. |
| 2026-04-30 | `events-tu-o2` | `docs: log events o2 rejection` | Test `src/events_ps1.c` at default `-O2` as the final hot/semi-hot whole-TU candidate from HP-063. Acceptance required a timing or work-counter win because event polling runs during playback and the probe already showed ELF growth. | Removed only `src/events_ps1.c` from the forced `-Os` list, rebuilt clean, regenerated the O2 audit, then ran the four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with layout movement allowed and zero timing regression allowed. | Rejected. FISHING1 high, FISHING1 low, FISHING2, and FISHING3 stayed exact-flat at `1209/1073`, `1207/1075`, `1899/1764`, and `2092/1954`; `blocking_vb` and `prefetch_overrun_vb` stayed `5/1/4/11`, and `loop_reads` stayed `20/14/19/33`. The PS-EXE bucket stayed `169984`, but the ELF grew `791808 -> 793048`, the map stayed `45253`, and tracked CD helper symbols shifted by `+208` bytes without any measured work benefit. Artifact: `scratch/ps1-perf-iterate/o2-events-tu-probe/20260430-222534-2855040/summary.json`. | Do not promote. Source was restored to `-Os`, the audit was regenerated against the accepted build, and HP-063 is complete with no whole-TU `-O2` promotions. |
| 2026-04-30 | `cleanrect-word-copy-v1` | `docs: log cleanrect word-copy rejection` | Test a safer C word-copy restore path for `grCleanRectCopyIn()`: use halfword edges plus 32-bit body copies only when row source/destination alignment is compatible, and fall back to `memcpy()` for small or odd-parity rows. Acceptance required a timing/work win because the probe added code and moved hot symbols. | Added the local row-copy helper, rebuilt clean, then ran the four-case matrix against `scratch/ps1-perf-baselines/o2-graphics-helper-baseline.json` with layout movement allowed, zero timing regression allowed, and work identity minimum `95`. | Rejected. FISHING1 high, FISHING1 low, FISHING2, and FISHING3 stayed exact-flat at `1209/1073`, `1207/1075`, `1899/1764`, and `2092/1954`; `blocking_vb`/`prefetch_overrun_vb` stayed `5/1/4/11`, and `loop_reads` stayed `20/14/19/33`. The PS-EXE bucket stayed `169984`, but the ELF grew `791808 -> 793372`, the map grew `45253 -> 45327`, and tracked graphics/CD symbols shifted by `+252` bytes without any measured work benefit. Artifact: `scratch/ps1-perf-iterate/cleanrect-word-copy-v1/20260430-223258-2897939/summary.json`. | Do not promote. Source was restored to accepted `memcpy()` row restores and a clean rebuild returned to `jcreborn.exe=169984`, `jcreborn.elf=791808`, and `jcreborn.map=45253`. Retry restore wins through generated restore bands, finer counters, or switchable ASM only if new evidence shows this sub-VBlank work matters. |
| 2026-04-30 | `indexed8-tile-local-compose-v1` | `ps1: optimize indexed8 foreground compose` | Indexed8 FGP3 spans are emitted from nonzero visible pixels, so the runtime can remove the per-pixel transparent-index branch and select the destination tile once per run instead of once per pixel. | Added an indexed8 opaque-run compositor in `src/graphics_ps1.c`, rebuilt clean, captured a fresh seven-case current-build baseline, ran the same seven-case probe with zero regression, then ran focused WALKSTUF1 high/low acceptance with `--allow-regression 15` because the strict pass showed a loop win with bounded CD-blocking tradeoff. | Promoted. PAL4 canaries stayed exact-flat in the seven-case probe: FISHING1 `1207/1074`, FISHING3 high `2096/1952`, FISHING3 low `2093/1952`, VISITOR3 high `1505/1005`, and VISITOR3 low `1532/1015`. Against the fresh current-build baseline, WALKSTUF1 high improves `loop_vb 2013 -> 2002`, `overrun_vb 608 -> 591`, and `prefetch_overrun_vb 115 -> 101` while `blocking_vb` rises `435 -> 438`; low improves `loop_vb 2028 -> 2014`, `overrun_vb 637 -> 621`, and `prefetch_overrun_vb 114 -> 95` while `blocking_vb` rises `483 -> 494`. Build size is `jcreborn.exe=169984`, `jcreborn.elf=792892`, `jcreborn.map=45253`. Artifacts: fresh baseline `scratch/ps1-perf-iterate/indexed8-tile-local-compose-baseline/20260430-224444-2965731/summary.json`, strict seven-case probe `scratch/ps1-perf-iterate/indexed8-tile-local-compose/20260430-225422-3012541/summary.json`, accepted focused pass `scratch/ps1-perf-iterate/indexed8-tile-local-compose-acceptance/20260430-230241-3060141/summary.json`. | Promote as an indexed8-specific loop/overrun win. The CSV rows now use `indexed8-tile-local-compose-v1`; exact timing-bearing rollup is `14.6360%` over target / `88.1061%` target speed. That rollup is slightly worse than stale `v58` because the refreshed WALKSTUF1 rows expose current-build drift; the source change still improves both rows relative to the fresh current-build baseline. Next indexed8 work should recover the new blocking tradeoff through generated read groups, direct16/upload-ready spans, or scheduler ownership rather than reverting the compositor win. |
| 2026-04-30 | `indexed8-unroll4-compose-v1` | `docs: log indexed8 unroll rejection` | Test whether the accepted indexed8 opaque-run compositor still pays enough loop-control overhead for a manual 4-pixel unroll to matter. Acceptance required a WALKSTUF1 loop/overrun improvement with PAL4 canaries unchanged and no new CD/refill regression. | Replaced the scalar indexed8 opaque-run loop with a 4-pixel unrolled loop, rebuilt clean, then ran the same seven-case gate against `scratch/ps1-perf-iterate/indexed8-tile-local-compose/20260430-225422-3012541/summary.json` with zero regression allowed and `--require-improvement`. | Rejected. PAL4/layout canaries were exact-flat but had no key metric improvement: FISHING1 high `1207/1074`, FISHING3 high `2096/1952`, FISHING3 low `2093/1952`, VISITOR3 high `1505/1005`, and VISITOR3 low `1532/1015`. WALKSTUF1 high stayed `loop_vb=2002` and `overrun_vb=591`; `blocking_vb` improved `438 -> 435`, but `prefetch_overrun_vb` regressed `101 -> 108`. WALKSTUF1 low stayed `loop_vb=2014`, improved `blocking_vb 494 -> 491`, but regressed target-relative overrun `621 -> 622`. The probe kept the PS-EXE bucket at `169984` but grew ELF `792892 -> 793420` and shifted tracked hot symbols by `+296` bytes. Artifact: `scratch/ps1-perf-iterate/indexed8-unroll4-probe/20260430-233159-3220676/summary.json`. | Do not promote. Source was restored to the accepted scalar opaque-run helper and a clean rebuild returned to `jcreborn.exe=169984`, `jcreborn.elf=792892`, and `jcreborn.map=45253`. Future indexed8 wins should come from direct16/upload-ready spans, generated read groups, or scheduler ownership, not another local unroll of this helper. |
| 2026-04-30 | `pal4-scratchpad-palette-v1` | `docs: log pal4 scratchpad palette rejection` | Test whether copying the active 16-entry PAL4 palette to PS1 scratchpad RAM once per foreground compose pass reduces palette-load stalls enough to move PAL4-heavy scenes. Acceptance required a dependable loop/overrun improvement with no new visible CD/refill regression across the seven-case canary set. | Added a scratchpad palette copy in `src/graphics_ps1.c`, routed PAL4 span composition through `0x1F800000`, rebuilt clean, then ran the same seven-case gate against `scratch/ps1-perf-iterate/indexed8-tile-local-compose/20260430-225422-3012541/summary.json` with zero regression allowed and `--require-improvement`. | Rejected. FISHING1 high, FISHING3 high, WALKSTUF1 high, and WALKSTUF1 low were loop-flat; FISHING3 low only moved because `target_vb 1952 -> 1953` made overrun appear one VBlank better. VISITOR3 high improved `prefetch_overrun_vb 109 -> 95` but regressed `blocking_vb 357 -> 360`; VISITOR3 low regressed materially (`loop_vb 1532 -> 1541`, `target_vb 1015 -> 1011`, `overrun_vb 517 -> 530`, `blocking_vb 314 -> 334`, `prefetch_overrun_vb 83 -> 101`). The probe shrank `grCompositePacked4SpansToBackground` by `24` bytes and ELF `792892 -> 792624`, but timing was unsafe. Artifact: `scratch/ps1-perf-iterate/pal4-scratchpad-palette-probe/20260430-234413-3290527/summary.json`. | Do not promote. Source was restored and a clean rebuild returned to `jcreborn.exe=169984`, `jcreborn.elf=792892`, and `jcreborn.map=45253`. Scratchpad palette should not be retried as a per-compose local copy; only revisit scratchpad use if a generated compositor or assembly path owns scratchpad layout and proves no scene-specific cadence regression. |
| 2026-05-01 | `fill-window-align16-v1` | `docs: log fill-window alignment rejection` | Test whether aligning the hot foreground window-fill helper to a 16-byte address bucket improves CD/window cadence. The accepted build placed `fgRuntimeFillWindowForEntry` at `0x800125a4`; the probe moved it to `0x800125b0` while keeping the same PS-EXE sector bucket. | Added `__attribute__((aligned(16)))` to `fgRuntimeFillWindowForEntry()`, rebuilt clean, then ran the same seven-case gate against `scratch/ps1-perf-iterate/indexed8-tile-local-compose/20260430-225422-3012541/summary.json` with zero regression allowed and `--require-improvement`. | Rejected as exact-flat. FISHING1 high stayed `1207/1074`, FISHING3 high `2096/1952`, FISHING3 low `2093/1952`, VISITOR3 high `1505/1005`, VISITOR3 low `1532/1015`, WALKSTUF1 high `2002/1411`, and WALKSTUF1 low `2014/1393`; blocking, prefetch-overrun, and loop-read metrics also stayed flat in the baseline comparison. The probe kept `jcreborn.exe=169984`, grew ELF `792892 -> 792940`, and shifted downstream foreground/CD symbols by `+24` bytes. Artifact: `scratch/ps1-perf-iterate/fill-window-align16-probe/20260501-000025-3382904/summary.json`. | Do not promote this local function-alignment bucket. Source was restored and a clean rebuild returned to `jcreborn.exe=169984`, `jcreborn.elf=792892`, and `jcreborn.map=45253`. Future alignment work should be a scripted multi-bucket sweep over several hot functions with map-address reporting, not one-off helper alignment. |
| 2026-05-01 | `nogprel-g0-v1` | `docs: log nogprel g0 rejection` | Test the queued small-data threshold idea at the only SDK-supported alternate executable mode: `NOGPREL` / `-G0` instead of the accepted `GPREL` / `-G8`. Acceptance required a timing win large enough to justify binary growth, hot-symbol movement, and foreground pack LBA shifts. | Temporarily changed `psn00bsdk_add_executable(jcreborn GPREL ...)` to `NOGPREL`, rebuilt clean, then ran a four-case canary gate against `scratch/ps1-perf-iterate/indexed8-tile-local-compose/20260430-225422-3012541/summary.json` with layout movement allowed and zero timing regression allowed. | Rejected. The build grew `jcreborn.exe 169984 -> 174080`, `jcreborn.elf 792892 -> 799148`, shifted foreground pack LBAs by `+2` sectors, moved `fgRuntimeFillWindowForEntry` by `+512` bytes, and grew `foregroundPilotPlay 0x252c -> 0x25b8`. FISHING1 high and FISHING3 high stayed loop-flat, VISITOR3 high only improved blocking `357 -> 353` with loop/overrun flat, and WALKSTUF1 high regressed badly (`loop_vb 2002 -> 2008`, `overrun_vb 591 -> 597`, `blocking_vb 438 -> 441`, `prefetch_overrun_vb 101 -> 109`). Artifact: `scratch/ps1-perf-iterate/nogprel-g0-probe/20260501-001016-3440160/summary.json`. | Do not promote `NOGPREL` / `-G0`. Source was restored to `GPREL` and a clean rebuild returned to `jcreborn.exe=169984`, `jcreborn.elf=792892`, and `jcreborn.map=45253`. Further `-G` threshold work would require a custom SDK/build harness for `-G4` or `-G16`; it is lower priority than generated read metadata and upload-ready/direct16 pack work. |

| 2026-05-01 | `visitor3-high-group72-84` | `ps1: group visitor3 high foreground reads` | Use the fixed read-plan case selector to target VISITOR3 high's `72..84` 12-sector append group. The hypothesis was that this collapses four adjacent active-loop read segments into one retained-window read without moving the PS-EXE bucket or pack LBA. | Added `--case-label` to `scripts/ps1-foreground-read-plan.py`, added a VISITOR3 high read-group table `{72,84}`, ran the seven-case canary aggregate against the accepted indexed8 baseline, then reran VISITOR3 high as a focused acceptance case with `--require-improvement`. | Promoted. VISITOR3 high improves `loop_vb 1505 -> 1503`, `target_vb 1005 -> 1007`, `overrun_vb 500 -> 496`, `blocking_vb 357 -> 350`, `prefetch_overrun_vb 109 -> 103`, `loop_reads 61 -> 58`, `loop_read_vb 481 -> 468`, and `due_misses 25 -> 24`. The seven-case aggregate kept FISHING1, FISHING3 high/low, VISITOR3 low, and WALKSTUF1 high/low exact-flat. Artifacts: accepted `scratch/ps1-perf-iterate/visitor3-high-group72-84-accepted/20260501-003201-3565963/summary.json`; canary aggregate `scratch/ps1-perf-iterate/visitor3-high-group72-84-probe/20260501-002419-3519974/summary.json`. | Promote. This is the first current VISITOR3 generated-read-metadata-style promotion after the case-selector fix. The CSV now stamps VISITOR3 high as `compact-fgp3-v59-visitor3high-group72-84`; exact timing-bearing rollup is `14.6590%` over target / `88.0956%` target speed because refreshing this stale row onto the current baseline exposes current-build drift despite the fresh-baseline win. Next VISITOR3 candidates are `110..122` or `110..126`, but they should be generated/costed, not broad scalar window changes. |
| 2026-05-01 | `visitor3-high-group110-122` | `docs: log visitor3 high group110 rejection` | Retry the next VISITOR3 high read-plan candidate after the accepted `72..84` group. The planner ranked `110..122` as a 12-sector capacity-safe cluster expected to collapse three reads into one while preserving the PS-EXE bucket and pack LBA. | Temporarily added `{110,122}` beside the accepted VISITOR3 high `{72,84}` group, rebuilt, then ran the seven-case canary gate against `scratch/ps1-perf-iterate/visitor3-high-group72-84-probe/20260501-002419-3519974/summary.json` with zero regression allowed and `95%` work identity. Source was reverted after the flat result. | Rejected as exact-flat. VISITOR3 high stayed `1503/1007`, `overrun_vb=496`, `blocking_vb=350`, `prefetch_overrun_vb=103`, `loop_reads=58`, `loop_read_vb=468`, and `due_misses=24`; `group_hits=0`, so this group did not fire in the retained-window path. FISHING1, FISHING3 high/low, VISITOR3 low, and WALKSTUF1 high/low also stayed exact-flat. The probe kept the PS-EXE bucket and pack LBAs fixed but grew hot symbols by `+8` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group110-122-probe/20260501-005202-3676517/summary.json`. | Do not promote a hand-coded VISITOR3 `110..122` group. Retry this sector only after the group planner models actual append-start eligibility, or after metadata can express groups from the read starts that the runtime will actually request. |
| 2026-05-01 | `visitor3-high-group112-124` | `docs: log visitor3 high group112 rejection` | Retest the same VISITOR3 high cluster with a start sector that appears in the actual runtime read sequence. The `110..122` probe was flat because no read started at `110`; the accepted log shows reads starting at `112`, so `{112,124}` should be eligible if the remaining cluster can be grouped safely. | Temporarily replaced the failed `110..122` candidate with `{112,124}`, rebuilt, then ran focused VISITOR3 high with `--require-improvement` against `scratch/ps1-perf-iterate/visitor3-high-group72-84-accepted/20260501-003201-3565963/summary.json`. Source was reverted after the gate failed. | Rejected as a real regression. The group fired enough to change the read sequence, but VISITOR3 high regressed `loop_vb 1503 -> 1509`, `overrun_vb 496 -> 502`, and `blocking_vb 350 -> 357`; `prefetch_overrun_vb` improved `103 -> 96`, but `loop_reads` worsened `58 -> 60` and `due_misses 24 -> 25`. The PS-EXE bucket and pack LBA stayed fixed, and hot symbols grew by `+8` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group112-124-probe/20260501-010215-3735152/summary.json`. | Do not promote. A start-aligned group can still steal visible cadence; future VISITOR3 grouping needs a cost model that predicts blocking-vs-prefetch tradeoffs, not just append-start eligibility. |
| 2026-05-01 | `visitor3-high-group230-242` | `ps1: group later visitor3 high foreground reads` | Continue the VISITOR3 high read-group lane after `110..122` failed to fire and `112..124` regressed. The later actual-read cluster around `230..242` has a longer slack window and should collapse one read without disturbing the tight early due-frame cadence. | Added `{230,242}` beside the accepted VISITOR3 high `{72,84}` group, rebuilt clean, ran focused VISITOR3 high with `--require-improvement` against `scratch/ps1-perf-iterate/visitor3-high-group72-84-accepted/20260501-003201-3565963/summary.json`, then ran the seven-case canary aggregate against `scratch/ps1-perf-iterate/visitor3-high-group72-84-probe/20260501-002419-3519974/summary.json`. | Promoted. VISITOR3 high improves `loop_vb 1503 -> 1496`, `overrun_vb 496 -> 489`, `blocking_vb 350 -> 345`, `prefetch_overrun_vb 103 -> 102`, `loop_reads 58 -> 57`, and `loop_read_vb 468 -> 458`; `due_misses` stays `24`. FISHING1, FISHING3 high/low, VISITOR3 low, and WALKSTUF1 high/low stayed exact-flat. Artifacts: focused accepted `scratch/ps1-perf-iterate/visitor3-high-group230-242-probe/20260501-010620-3758604/summary.json`; canary aggregate `scratch/ps1-perf-iterate/visitor3-high-group230-242-canaries/20260501-010733-3765808/summary.json`. | Promote. The CSV now stamps VISITOR3 high as `compact-fgp3-v60-visitor3high-group230-242`; exact timing-bearing rollup is `14.6532%` over target / `88.0982%` target speed. Continue with generated/read-costed VISITOR3 groups; the accepted pattern is later, start-aligned, and slack-safe, not broad early clustering. |
| 2026-05-01 | `visitor3-high-group242-254` | `docs: log visitor3 high group242 rejection` | Test the next actual-start VISITOR3 high cluster after the accepted `230..242` group. The accepted run still had two reads starting at `242`, so the hypothesis was that `{242,254}` might collapse a duplicate/tail read without moving layout. | Temporarily added `{242,254}` beside the accepted `{72,84}` and `{230,242}` VISITOR3 high groups, rebuilt, then ran focused VISITOR3 high with `--require-improvement` against `scratch/ps1-perf-iterate/visitor3-high-group230-242-probe/20260501-010620-3758604/summary.json`. Source was reverted after the gate failed. | Rejected as a real regression. VISITOR3 high regressed `loop_vb 1496 -> 1507`, `overrun_vb 489 -> 500`, `blocking_vb 345 -> 357`, and `prefetch_overrun_vb 102 -> 109`; `loop_reads` stayed `57`, and `due_misses` stayed `24`. The PS-EXE bucket and pack LBA stayed fixed; ELF only grew `793112 -> 793124`, so the regression is cadence/read-cost, not layout. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group242-254-probe/20260501-012053-3841649/summary.json`. | Do not promote. Even a duplicate-looking actual-start cluster can steal visible CD cadence. Keep `230..242` as the last accepted VISITOR3 group until generated cost modeling can explain why `242..254` regresses. |
| 2026-05-01 | `visitor3-high-group738-749` | `docs: log visitor3 high group738 rejection` | Test a late tail-group candidate after the adjacent `242..254` cluster regressed. The 12-sector planner marked `738..749` as a two-read replacement near the end of the pack, which seemed less likely to disturb early due-frame cadence. | Temporarily added `{738,749}` beside the accepted `{72,84}` and `{230,242}` VISITOR3 high groups, rebuilt, then ran focused VISITOR3 high with `--require-improvement` against `scratch/ps1-perf-iterate/visitor3-high-group230-242-probe/20260501-010620-3758604/summary.json`. Source was reverted after the gate failed. | Rejected as a visible-blocking tradeoff. VISITOR3 high regressed `loop_vb 1496 -> 1507`, `overrun_vb 489 -> 499`, `blocking_vb 345 -> 356`, and `due_misses 24 -> 26`; `prefetch_overrun_vb` improved `102 -> 83`, but the loop and visible blocking got worse. The PS-EXE bucket and pack LBA stayed fixed. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group738-749-probe/20260501-012352-3858970/summary.json`. | Do not promote. Tail grouping is not automatically safe; these results reinforce that VISITOR3 needs a generated cost model that ranks visible blocking and due misses above raw prefetch-overrun reductions. |
| 2026-05-01 | `setup-prime-cap192` | `docs: log setup-prime cap192 rejection` | Test whether raising the global resident setup-prime cap from `128 KiB` to `192 KiB` can recover part of the lost VISITOR3/WALKSTUF1 residency after the persistent walk-clean-buffer memory change, without jumping back to the old `248-300 KiB` per-scene windows. | Temporarily changed `FG_SETUP_PRIME_MAX_RESIDENT_BYTES` to `192 KiB`, rebuilt, and started the seven-case gate against `scratch/ps1-perf-iterate/visitor3-high-group230-242-canaries/20260501-010733-3765808/summary.json`. The run was stopped after the first case because FISHING1 high already violated the zero-regression gate. Source was restored to `128 KiB`. | Rejected. FISHING1 high regressed immediately: `loop_vb 1207 -> 1209`, `target_vb 1074 -> 1073`, `overrun_vb 133 -> 136`, `blocking_vb 2 -> 5`, and `prefetch_overrun_vb 2 -> 5`. It did reduce `loop_reads 20 -> 15` and `loop_read_vb 89 -> 61`, but the larger resident window raised visible CD pressure and grew retained prefetch heap `137048 -> 202584` bytes. Artifact: `scratch/ps1-perf-iterate/setup-prime-cap192-probe/20260501-013649-3930008/fishing1-high/perf-summary.json`. | Do not promote a global cap raise. Future residency recovery needs per-scene generated coverage, segmented/inter-scene preload, or a memory-neutral ownership change; raw cap increases can save reads while making the visible loop slower. |
| 2026-05-01 | `read-plan-residency-clamp` | `scripts: honor setup-prime cap in read plans` | The cap probe exposed that `scripts/ps1-foreground-read-plan.py` still modeled old branch-chain setup policies and ignored `FG_SETUP_PRIME_MAX_RESIDENT_BYTES`, causing FISHING1/VISITOR3/WALKSTUF1 candidate reports to rank sectors as if larger setup windows were resident. | Updated the host planner to parse the setup-prime cap, VISITOR3 policy, and WALKSTUF1 indexed8-derived policy, and to clamp all modeled setup-prime coverage the same way the runtime does. Verified with `python3 -m py_compile` plus focused read-plan runs for FISHING1 high, VISITOR3 low, and WALKSTUF1 high. | Promoted as harness/tooling. FISHING1 high now reports real current coverage `prime=[1,65]` instead of `prime=[1,161]`; VISITOR3 low reports `policy=auto:visitor3-low prime=[1,65]`; WALKSTUF1 high reports `policy=auto:walkstuf1-high prime=[3,67]`. This changes no PS1 runtime bytes or speed metrics, but it prevents future CD-group and segmented-preload experiments from chasing already-impossible coverage. | Promote. Future read-group candidates should be chosen from cap-aware plans; do not trust older `foreground-read-plan.txt` artifacts for setup-primed scenes unless they were regenerated after this fix. |
| 2026-05-01 | `visitor3-low-group182-194` | `docs: log visitor3 low group182 rejection` | Use the corrected cap-aware planner to test VISITOR3 low's top 12-sector candidate, `{182,194}`. The hypothesis was that two observed reads across a long slack gap could become one retained-window read without moving layout. | Temporarily added a VISITOR3 low read-group table `{182,194}` under the default foreground window, rebuilt, and ran focused VISITOR3 low with `--require-improvement` against `scratch/ps1-perf-iterate/visitor3-high-group230-242-canaries/20260501-010733-3765808/summary.json`. Source was restored after the focused gate failed. | Rejected. The group saved one read (`loop_reads 53 -> 52`) and improved `prefetch_overrun_vb 83 -> 77`, but visible timing regressed: `loop_vb 1532 -> 1539`, `overrun_vb 517 -> 524`, and `blocking_vb 314 -> 324`. The PS-EXE bucket and pack LBA stayed fixed, so the regression is group cadence/read cost rather than layout. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group182-194-probe/20260501-014519-3978079/summary.json`. | Do not promote. VISITOR3 low follows the same pattern as the rejected high-side groups: raw read savings can worsen visible blocking. Next read-group work needs generated cost modeling that scores loop/blocking first, not just saved reads. |
| 2026-05-01 | `setup-segment-persist-v1` | `docs: log setup segment persistence rejection` | Test whether retaining a setup-prime segment after the first entry copy lets the same setup-read buffer satisfy multiple consecutive entries. This should be safe in principle because later direct reads already clear `setupSegmentPrimed` before reusing scratch. | Removed the single-use invalidation in `fgRuntimeCopyEntryFromWindow()`, rebuilt, then started the seven-case gate against `scratch/ps1-perf-iterate/visitor3-high-group230-242-canaries/20260501-010733-3765808/summary.json`. The run was stopped after VISITOR3 high failed; source was restored and rebuilt. | Rejected as layout/cadence-sensitive. FISHING1 high and FISHING3 high stayed flat, but VISITOR3 high regressed `loop_vb 1496 -> 1507`, `overrun_vb 489 -> 499`, and `blocking_vb 345 -> 356`; `prefetch_overrun_vb` improved `102 -> 83`, which is the wrong tradeoff. The probe kept the PS-EXE bucket fixed and shrank ELF `793112 -> 793072`, so this is another case where tiny foreground code-shape changes disturb visible CD cadence. Artifacts: partial run directory `scratch/ps1-perf-iterate/setup-segment-persist-probe/20260501-015015-4006129/`; VISITOR3 high case `scratch/ps1-perf-iterate/setup-segment-persist-probe/20260501-015015-4006129/visitor3-high/perf-summary.json`. | Do not promote. Do not use segment-lifetime cleanup as an isolated source change; retry only if generated setup segments require it and the code-shape change is bundled with a measured loop/blocking win. |
| 2026-05-01 | `indexed8-run-aware-compose-v1` | `docs: log indexed8 run-aware compose rejection` | Test whether WALKSTUF1 indexed8 residual spans have enough same-index byte runs to amortize run detection. The host scan showed average byte runs near `1.77-1.78` with roughly `300 KB` in `4+` byte runs, so a one-lookup-per-run path might reduce palette loads without changing pack format. | Temporarily changed `grCompositeIndexed8OpaqueRun()` to detect consecutive identical indices and fill the destination with one palette lookup per run, rebuilt, then ran focused WALKSTUF1 high/low against `scratch/ps1-perf-iterate/visitor3-high-group230-242-canaries/20260501-010733-3765808/summary.json` with zero regression allowed. Source was restored after the gate failed. | Rejected. WALKSTUF1 high improved `loop_vb 2002 -> 1997`, `overrun_vb 591 -> 589`, and `blocking_vb 438 -> 431`, but regressed `prefetch_overrun_vb 101 -> 108`. WALKSTUF1 low regressed `loop_vb 2014 -> 2030`, `overrun_vb 621 -> 640`, and `prefetch_overrun_vb 95 -> 99`, with no key metric improvement. Artifact: `scratch/ps1-perf-iterate/indexed8-run-aware-compose-probe/20260501-015951-4061522/summary.json`. | Do not promote. Runtime run detection is too scene-sensitive for the current indexed8 residual data; future indexed8 wins should use pack-time direct16/upload-ready spans, generated read metadata, or scheduler ownership rather than another pixel-loop branch. |
| 2026-05-01 | `setup-prerender-first-frame-v1` | `docs: log setup prerender rejection` | Test whether rendering the first foreground frame during setup removes the first due-render cost from the active playback loop. This targeted zero-CD rows like STAND1, where `loop_reads=0` and `blocking_vb=0` but `loop_vb` still exceeded target by about `140` VBlanks. | Captured a fresh 11-case baseline (`stand1` high/low, `visitor4`, `walkstuf2`, FISHING/VISITOR3/WALKSTUF canaries), then temporarily rendered the current frame before `ps1PerfMarkLoopStart()`. Two variants were tried: one reset `sceneClockTick` after setup render, and one preserved the existing clock. Source was restored after both failed. | Rejected. With clock reset, STAND1 high stayed exact-flat at `342/202`, so it did not remove active-loop cost. Without clock reset, STAND1 high/low stayed flat and FISHING1 high regressed `loop_vb 1207 -> 1208`, `overrun_vb 133 -> 135`, `blocking_vb 2 -> 4`, and `prefetch_overrun_vb 2 -> 4`. Artifacts: baseline `scratch/ps1-perf-iterate/prerender-first-frame-baseline/20260501-020854-4112657/summary.json`; no-clock probe `scratch/ps1-perf-iterate/prerender-first-frame-noclock-probe/20260501-022332-4282/summary.json`. | Do not promote setup-time first-frame rendering. The zero-CD overrun is not simply first-render placement; next work should attack per-frame upload/restore/compose cost, present scheduling, or pack-time upload-ready data. |
| 2026-05-01 | `fishing3-low-group163-175` | `ps1: group fishing3 low foreground reads` | Use the cap-aware read plan after the accepted FISHING3 low `{159,171}` group to test a later medium-risk 12-sector group, `{163,175}`. The hypothesis was that the longer slack gap would collapse one active-loop read without disturbing the already-tight FISHING3 low cadence. | Added `{163,175}` to `kFishing3LowReadGroups12`, rebuilt clean, ran focused FISHING3 low with `--require-improvement` against `scratch/ps1-perf-iterate/visitor3-high-group230-242-canaries/20260501-010733-3765808/summary.json`, then ran the seven-case canary aggregate with zero-regression semantics. | Promoted. FISHING3 low improves `loop_vb 2093 -> 2092`, `target_vb 1952 -> 1954`, `overrun_vb 141 -> 138`, `blocking_vb 11 -> 7`, `prefetch_overrun_vb 11 -> 7`, and `loop_reads 33 -> 32`. Pack LBA and PS-EXE sector bucket stayed fixed. FISHING1 high, FISHING3 high, VISITOR3 high/low, and WALKSTUF1 high/low stayed exact-flat against the accepted seven-case baseline. Artifacts: focused `scratch/ps1-perf-iterate/fishing3-low-group163-175-probe/20260501-023257-57798/summary.json`; canaries `scratch/ps1-perf-iterate/fishing3-low-group163-175-canaries-noregress/20260501-024210-111686/summary.json`. | Promote. The CSV now stamps the refreshed canary rows as `compact-fgp3-v61-fishing3low-group163-175`. Exact matrix rollup is `14.6669%` over target / `88.0913%` target speed after replacing older stale canary rows with current measurements; the direct accepted change is the FISHING3 low row improvement above. |
| 2026-05-01 | `fishing3-low-group253-269` | `docs: log fishing3 low group253 rejection` | Test the highest-impact remaining medium-risk FISHING3 low group after `{163,175}`. The cap-aware planner ranked `{253,269}` as a later cluster that could replace three reads with one retained-window read while keeping layout stable. | Temporarily added `{253,269}` to `kFishing3LowReadGroups12`, rebuilt clean, then ran focused FISHING3 low with `--require-improvement` against `scratch/ps1-perf-iterate/fishing3-low-group163-175-canaries-noregress/20260501-024210-111686/summary.json`. Source was restored after the gate failed. | Rejected. The group reduced read volume (`loop_reads 32 -> 30`) but worsened accepted timing and visible CD pressure: `loop_vb 2092 -> 2093`, `overrun_vb 138 -> 139`, `blocking_vb 7 -> 8`, and `prefetch_overrun_vb 7 -> 8`. Layout stayed fixed (`pack_lba=1564`, PS-EXE bucket `169984`), so the regression is read cadence/cost rather than a layout shift. Artifact: `scratch/ps1-perf-iterate/fishing3-low-group253-269-probe/20260501-025751-200430/summary.json`. | Do not promote. This is another proof that read-count reduction alone is not enough; future FISHING3 low groups need a visible-cost model or should try narrower/later ranges only if they can improve loop/blocking, not just `loop_reads`. |
| 2026-05-01 | `fishing3-low-group253-265` | `ps1: group later fishing3 low foreground reads` | Retry the same later FISHING3 low cluster with the narrower `{253,265}` shape after `{253,269}` proved too broad. The hypothesis was that a one-read reduction could lower visible CD pressure without stealing the extra cadence that made the wide group regress. | Added `{253,265}` to `kFishing3LowReadGroups12`, rebuilt clean, ran focused FISHING3 low with `--require-improvement`, then ran the seven-case canary aggregate against `scratch/ps1-perf-iterate/fishing3-low-group163-175-canaries-noregress/20260501-024210-111686/summary.json`. | Promoted. FISHING3 low keeps `loop_vb=2092`, `target_vb=1954`, and `overrun_vb=138` while improving `blocking_vb 7 -> 6`, `prefetch_overrun_vb 7 -> 6`, and `loop_reads 32 -> 31`. FISHING1 high, FISHING3 high, VISITOR3 high/low, and WALKSTUF1 high/low stayed exact-flat; pack LBAs and PS-EXE sector bucket stayed fixed. Artifacts: focused `scratch/ps1-perf-iterate/fishing3-low-group253-265-probe/20260501-030100-218850/summary.json`; canaries `scratch/ps1-perf-iterate/fishing3-low-group253-265-canaries-noregress/20260501-030224-227085/summary.json`. | Promote. The CSV now stamps the refreshed canary rows as `compact-fgp3-v62-fishing3low-group253-265`. Exact matrix rollup remains `14.6669%` over target / `88.0913%` target speed because this is a visible-CD/work reduction with flat target-relative loop time. |
| 2026-05-01 | `fishing3-high-group79-95` | `docs: log fishing3 high group79 rejection` | Test the highest-ranked medium-phase FISHING3 high read group, `{79,95}`. The plan predicted one saved read across a roughly `1.3s` gap, outside the setup segment and away from the rejected tight later clusters. | Temporarily added `{79,95}` to `kFishing3HighReadGroups12`, rebuilt clean, then ran focused FISHING3 high with `--require-improvement` against `scratch/ps1-perf-iterate/fishing3-low-group253-265-canaries-noregress/20260501-030224-227085/summary.json`. Source was restored after the gate failed. | Rejected as exact-flat. FISHING3 high stayed `loop_vb=2096`, `target_vb=1952`, `overrun_vb=144`, `blocking_vb=18`, `prefetch_overrun_vb=13`, `loop_reads=41`, and `due_misses=1`. Layout stayed fixed, but hot symbols shifted by `+4` bytes with no measured work or speed benefit. Artifact: `scratch/ps1-perf-iterate/fishing3-high-group79-95-probe/20260501-031316-289871/summary.json`. | Do not promote. The medium-phase candidate did not fire in a useful way; future FISHING3 high work should move to generated cost modeling, segmented preload, or another candidate only when the planner can explain an actual key-metric improvement path. |
| 2026-05-01 | `indexed8-row-local-dirty-v1` | `ps1: aggregate indexed8 dirty rows` | Indexed8 FGP3 spans are generated from visible nonzero pixels like PAL4 spans, so row-local tile pointers plus one dirty mark per row/tile should reduce per-span overhead for WALKSTUF1 without changing PAL4 rows. | Changed `grCompositeIndexed8SpansToBackground()` to mirror the PAL4 row-local fast path, rebuilt, ran the seven-case canary (PAL4/VISITOR3 rows exact-flat; strict gate failed only because unchanged rows did not improve and WALKSTUF1 refill overrun exceeded the tight tolerance), then ran focused WALKSTUF1 high/low acceptance with `--allow-regression 50`. | Promoted. WALKSTUF1 high improves `loop_vb 2002 -> 1971`, `overrun_vb 591 -> 556`, `blocking_vb 438 -> 423`, and `due_misses 27 -> 23`; `prefetch_overrun_vb` rises `101 -> 139`. WALKSTUF1 low improves `loop_vb 2014 -> 1958`, `overrun_vb 621 -> 559`, `blocking_vb 494 -> 452`, and `due_misses 40 -> 34`; `prefetch_overrun_vb` rises `95 -> 140`. Pack LBAs and the PS-EXE sector bucket stayed fixed. Artifacts: strict canary `scratch/ps1-perf-iterate/indexed8-row-local-dirty-probe/20260501-033519-410157/summary.json`; focused acceptance `scratch/ps1-perf-iterate/indexed8-row-local-dirty-acceptance/20260501-034320-456410/summary.json`. | Promote as an indexed8-specific loop/blocking/due-miss win with an explicit refill-overrun tradeoff. The matrix rows now use `indexed8-row-local-dirty-v1`; exact timing-bearing rollup improves from `14.6669%` to `14.6068%` over target and target speed improves from `88.0913%` to `88.1213%`. Next WALKSTUF1 work should attack hidden refill ownership, generated read metadata, or upload-ready/direct16 data rather than more local pixel-loop branching. |
| 2026-05-01 | `walkstuf1-low-group180-204` | `docs: log walkstuf1 low group180 rejection` | Test the cap-aware planner's top WALKSTUF1 low 24-sector group after indexed8 row-local dirty. The hypothesis was that `{180,204}` plus enough stream-window capacity could collapse one medium-gap active-loop read and reduce visible blocking on the largest indexed8 low-tide offender. | Temporarily added a low-tide WALKSTUF1 read-group table `{180,204}` and generalized grouped-window capacity from the fixed 13-sector default to the largest active group span. Ran focused WALKSTUF1 low against `scratch/ps1-perf-iterate/indexed8-row-local-dirty-probe/20260501-033519-410157/summary.json` with `--require-improvement`, zero regression allowance, and `95%` work identity. Source was restored after the gate failed. | Rejected. The group traded in the wrong direction: `prefetch_overrun_vb 140 -> 133` improved, but visible timing regressed `loop_vb 1958 -> 1998`, `overrun_vb 559 -> 598`, and `blocking_vb 452 -> 477`; `due_misses 34 -> 35`. Pack LBA and the PS-EXE bucket stayed fixed, so this is cadence/read-cost rather than layout. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group180-204-probe/20260501-041549-634044/summary.json`. | Do not promote raw WALKSTUF1 low grouped appends selected only by saved reads. The next WALKSTUF1 low work needs generated cost modeling, scheduler ownership, or upload-ready/direct16 pack data; lowering hidden refill overrun is not enough when it steals visible loop cadence. |
| 2026-05-01 | `walkstuf1-low-direct16-v3` | `docs: log walkstuf1 low direct16 rejection` | Prototype a direct16 FGP3/v3 payload for WALKSTUF1 low to remove indexed8 palette lookups from the runtime compositor. The hypothesis was that pack-time 16bpp expansion might reduce CPU compose cost enough to offset larger CD reads on the biggest indexed8 offender. | Temporarily extended the FGP3 builder/runtime for direct16 temporal residual spans, converted `WALK1LOW.FG2` from FGP3/v2 to FGP3/v3, and ran focused WALKSTUF1 low with layout changes allowed but zero key-metric regression. The generated pack grew `2,157,403 -> 2,919,466` bytes. Code and pack were restored after the gate failed. | Rejected. The direct16 path improved only hidden refill pressure (`prefetch_overrun_vb 140 -> 114`) while badly regressing visible playback: `loop_vb 1958 -> 2144`, `overrun_vb 559 -> 765`, `blocking_vb 452 -> 712`, `loop_reads 62 -> 85`, and `due_misses 34 -> 61`. The code also moved the PS-EXE bucket `169984 -> 172032` and shifted the pack LBA by `+1`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-direct16-probe/20260501-042256-673482/summary.json`. | Do not promote direct16 as a whole-payload expansion for WALKSTUF1 low. If this lane is retried, it needs selective upload-ready spans, compression, or resident/preloaded direct16 segments; naive 2-byte pixels create too much CD pressure. |
| 2026-05-01 | `visitor3-setupprime192` | `docs: log visitor3 setup-prime rejection` | Test whether VISITOR3 could safely recover resident setup coverage without repeating the global cap failure that hurt FISHING1. The hypothesis was that a VISITOR3-only `192 KiB` cap would reduce active-loop read pressure on both high and low tide while leaving other scenes unchanged. | Temporarily raised only VISITOR3's table-driven setup-prime cap from `128 KiB` to `192 KiB`, rebuilt, and ran focused VISITOR3 high/low against `scratch/ps1-perf-iterate/indexed8-row-local-dirty-probe/20260501-033519-410157/summary.json` with `--require-improvement`, zero regression allowance, and `95%` work identity. Source was restored after the gate failed. | Rejected. VISITOR3 high improved `blocking_vb 345 -> 342` but regressed `loop_vb 1496 -> 1499`, `overrun_vb 489 -> 495`, and `prefetch_overrun_vb 102 -> 110`. VISITOR3 low regressed materially: `loop_vb 1532 -> 1549`, `overrun_vb 517 -> 539`, `blocking_vb 314 -> 341`, and `prefetch_overrun_vb 83 -> 105`. The PS-EXE bucket and pack LBAs stayed fixed. Artifact: `scratch/ps1-perf-iterate/visitor3-setupprime192-probe/20260501-042808-702587/summary.json`. | Do not promote larger contiguous VISITOR3 setup-prime residency. Future VISITOR3 work needs segmented/costed preload or a scheduler-owned read plan; raw extra setup coverage can improve one visible-CD counter while worsening total cadence. |
| 2026-05-01 | `building2-low-setupprime128` | `ps1: setup-prime building2 low foreground` | Test BUILDING2 because it is the next high absolute-gap family after WALKSTUF1/VISITOR3. A fresh baseline showed high `1560/1297`, `blocking_vb=156`, and low `1559/1297`, `blocking_vb=150`. The hypothesis was that low tide could safely use a small setup-prime window while high tide might not. | First tried `128 KiB` setup-prime for both high and low; high regressed overrun/blocking while low improved. Narrowed the policy to low tide only, reran low with `--require-improvement`, and reran high as a zero-regression canary. Updated the matrix with stats version `compact-fgp3-v63-building2low-prime`. | Promoted. BUILDING2 low improves `loop_vb 1559 -> 1542`, `target_vb 1297 -> 1290`, `overrun_vb 262 -> 252`, `blocking_vb 150 -> 139`, `prefetch_overrun_vb 44 -> 32`, and `loop_reads 70 -> 60`; due misses move `19 -> 20`. BUILDING2 high stays exact-flat at `1560/1297`, `blocking_vb=156`, `prefetch_overrun_vb=43`. Artifacts: fresh baseline `scratch/ps1-perf-iterate/building2-current-baseline/20260501-043117-721135/summary.json`; accepted low `scratch/ps1-perf-iterate/building2-low-setupprime128-acceptance/20260501-043751-760764/summary.json`; high canary `scratch/ps1-perf-iterate/building2-low-setupprime128-high-canary/20260501-043850-760750/summary.json`. | Promote as a scene/tide-specific setup-prime win, not a global policy. Exact timing-bearing rollup moves to `14.6030%` over target / `88.1239%` target speed. Continue testing BUILDING-family low/high independently; paired scene names can have opposite setup-prime knees. |
| 2026-05-01 | `building2-low-group318-330` | `ps1: group building2 foreground reads` | Continue BUILDING2 after the low-tide setup-prime win. The cap-aware planner's top remaining low-tide candidate, `{318,330}`, covered four observed active-loop reads and looked like a possible one-read reduction without changing pack layout. | Added a low-tide BUILDING2 read-group table `{318,330}`. The first strict run failed only the zero-address-delta phase gate because hot symbols shifted by `+56` bytes; a repeat with `--max-symbol-address-delta 64` passed. Ran BUILDING2 high as a canary and then ran the seven-case FISHING/FISHING3/VISITOR3/WALKSTUF canary set against the indexed8 row-local baseline. Updated the matrix with stats version `compact-fgp3-v64-building2-group318-330`. | Promoted as a layout-sensitive CD/work reduction. BUILDING2 low keeps `loop_vb=1542` while improving `target_vb 1290 -> 1297`, `overrun_vb 252 -> 245`, `blocking_vb 139 -> 138`, `prefetch_overrun_vb 32 -> 31`, `loop_reads 60 -> 59`, and `loop_read_vb 281 -> 279`. BUILDING2 high also improves under the same compiled layout: `loop_vb 1560 -> 1552`, `overrun_vb 263 -> 256`, `blocking_vb 156 -> 144`, `prefetch_overrun_vb 43 -> 39`, and `loop_reads 73 -> 69`. FISHING1, FISHING3 high/low, VISITOR3 high/low, and WALKSTUF1 high/low stayed exact-flat. Artifacts: low repeat `scratch/ps1-perf-iterate/building2-low-group318-330-repeat/20260501-045258-846515/summary.json`; high canary `scratch/ps1-perf-iterate/building2-low-group318-330-high-canary/20260501-045407-853672/summary.json`; broad canaries `scratch/ps1-perf-iterate/building2-low-group318-330-canaries/20260501-045530-861922/summary.json`. | Promote, but keep the caveat visible: this is not proof that read-count savings alone are safe. The zero-address gate caught code-shape movement, so future read-group promotions should either keep the address phase fixed or prove broad canaries. Exact timing-bearing rollup moves to `14.5936%` over target / `88.1307%` target speed. |
| 2026-05-01 | `building2-high-group318-330` | `docs: log building2 high group318 rejection` | Test whether the accepted BUILDING2 low grouped-read cluster `{318,330}` can be shared by high tide. The read plan ranks the same cluster highly for high tide, and the prior low-only commit improved high under a fixed layout, so the hypothesis was that enabling the actual high group might produce another visible CD win. | Temporarily removed the low-tide guard so BUILDING2 high and low both used the `{318,330}` read group. Ran focused BUILDING2 high against the accepted `compact-fgp3-v64-building2-group318-330` high baseline with `--require-improvement`, zero regression allowance, `95%` work identity, and a bounded `64` byte hot-symbol address allowance. Source was restored after the gate failed. | Rejected as exact-flat. BUILDING2 high stayed `loop_vb=1552`, `overrun_vb=256`, `blocking_vb=144`, `prefetch_overrun_vb=39`, and `loop_reads=69`; the only measured change was a `-24` byte foreground-pilot code-shape shift and matching hot-symbol address movement. Artifact: `scratch/ps1-perf-iterate/building2-high-group318-330-probe/20260501-053255-1069195/summary.json`. | Do not promote shared high/low BUILDING2 grouping for this cluster. The high-side improvement from the accepted low-only commit was layout-sensitive, not proof that the high group itself fires usefully. Next BUILDING2 work should test distinct high-side groups or generated visible-cost metadata, not simply sharing low-tide groups. |
| 2026-05-01 | `building2-high-group325-331` | `docs: log building2 high group325 rejection` | Test a narrower high-tide-only BUILDING2 group after the shared `{318,330}` group stayed flat. The planner ranked `{325,331}` as the top 6-sector high-side cluster, so the hypothesis was that a smaller append could fire without the cadence neutrality of the wider group. | Temporarily added a high-tide BUILDING2 read-group table `{325,331}` while keeping low tide on the accepted `{318,330}` group. Ran focused BUILDING2 high against the accepted `compact-fgp3-v64-building2-group318-330` high baseline with `--require-improvement`, zero regression allowance, `95%` work identity, and a bounded `96` byte hot-symbol address allowance. Source was restored after the gate failed. | Rejected as exact-flat. BUILDING2 high stayed `loop_vb=1552`, `target_vb=1296`, `overrun_vb=256`, `blocking_vb=144`, `prefetch_overrun_vb=39`, and `loop_reads=69`; hot symbols moved only `+4` bytes. Artifact: `scratch/ps1-perf-iterate/building2-high-group325-331-probe/20260501-053626-1089748/summary.json`. | Do not promote narrower high-side BUILDING2 read groups selected by the current read-count plan. The top wide and narrow high-side clusters both fail to move metrics; next high-side work needs generated visible-cost metadata, a scheduler-owned read plan, or a different optimization family. |
| 2026-05-01 | `building46-setupprime64-128` | `docs: log building4/6 setup-prime rejection` | Test whether BUILDING4/6 could use contiguous setup-prime coverage like BUILDING2 low. The hypothesis was that a small broad setup-prime budget would remove enough active-loop reads to improve the remaining BUILDING-family CD pressure. | Temporarily enabled setup-prime policy for BUILDING4 and BUILDING6 at `128 KiB`, then repeated with `64 KiB`, and gated BUILDING4 high/low plus BUILDING6 high/low against `scratch/ps1-perf-iterate/building46-current-baseline/20260501-054243-1125287/summary.json`. Source was restored after both probes failed. | Rejected. The `128 KiB` probe saved some reads but worsened BUILDING4 high, BUILDING6 high, and BUILDING6 low while only making BUILDING4 low superficially better with higher refill pressure. The `64 KiB` probe still regressed BUILDING4 high/low and BUILDING6 low; BUILDING6 high's small loop/blocking hint came with higher prefetch overrun. Artifacts: `scratch/ps1-perf-iterate/building46-setupprime128-probe/20260501-054904-1162079/summary.json` and `scratch/ps1-perf-iterate/building46-setupprime64-probe/20260501-055502-1196889/summary.json`. | Do not promote raw BUILDING4/6 setup-prime windows. Future preload work for these scenes needs generated segment coverage or scheduler ownership, not a family-wide contiguous cap. |
| 2026-05-01 | `building46-window-retune-sweep` | `docs: log building4/6 window sweep rejection` | Sweep remaining BUILDING4/6 stream-window sizes after setup-prime failed. The hypothesis was that a larger retained window might lower visible blocking enough to offset refill pressure without a setup-time read. | Rebuilt the accepted source, then ran BUILDING6 `20 KiB`/`28 KiB` probes and BUILDING4 `20 KiB`/`28 KiB`/`36 KiB` probes against the same fresh BUILDING4/6 baseline. Only BUILDING4 low at `36 KiB` produced a promotable knee; other variants stayed source-clean. | Rejected for every non-BUILDING4-low shape. BUILDING6 `20 KiB` worsened both tides, `28 KiB` lowered blocking but regressed loop/refill, BUILDING4 high `20 KiB`/`28 KiB` regressed, and BUILDING4 low `28 KiB` was not enough. Artifacts: `scratch/ps1-perf-iterate/building6-window-retune-probe/20260501-060151-1236498/summary.json` and `scratch/ps1-perf-iterate/building4-window-retune-probe/20260501-060722-1267995/summary.json`. | Do not promote BUILDING stream-window sizes as a family. Keep this lane scene/tide-local with fresh baselines and explicit visible-CD/refill tradeoff rules. |
| 2026-05-01 | `building4-low-window36` | `ps1: retune building4 low foreground window` | Promote the one strong knee from the BUILDING4/6 window sweep: BUILDING4 low with a `36 KiB` retained foreground window. Acceptance required a focused low-tide improvement plus broad canary stability across BUILDING4 high, BUILDING6 high/low, FISHING1, FISHING3 high/low, VISITOR3 high/low, and WALKSTUF1 high/low. | Changed only `FG_BUILDING4_LOW_WINDOW_BYTES` from `32 KiB` to `36 KiB`, rebuilt, ran focused BUILDING4 low against the fresh baseline, then ran the 11-case canary gate and refreshed `docs/ps1/performance-scene-matrix.csv` as `compact-fgp3-v65-building4low-window36`. | Promoted. BUILDING4 low improves `loop_vb 3083 -> 3068`, `overrun_vb 287 -> 271`, `blocking_vb 180 -> 96`, `loop_reads 51 -> 40`, `loop_read_vb 331 -> 299`, and `due_misses 20 -> 2`; `prefetch_overrun_vb` rises `60 -> 90` as an accepted hidden-refill tradeoff. BUILDING4 high, BUILDING6 high/low, FISHING1, FISHING3 high/low, VISITOR3 high/low, and WALKSTUF1 high/low stayed stable in the canary run. Artifacts: focused acceptance `scratch/ps1-perf-iterate/building4-low-window36-acceptance/20260501-061315-1301338/summary.json`; canaries `scratch/ps1-perf-iterate/building4-low-window36-canaries/20260501-061457-1311308/summary.json`. | Promote as a scene/tide-specific window-retune win. Exact timing-bearing rollup moves from `14.5936%` to `14.5886%` over target and target speed from `88.1307%` to `88.1348%`. Future BUILDING work should target BUILDING4 high/BUILDING6 with generated read-cost metadata or preprocessing rather than broad setup-prime/window knobs. |
| 2026-05-01 | `building6-high-group505-517` | `docs: log building6 high group505 rejection` | Test the safest-looking BUILDING6 high read group after the low-window win. The planner ranked `{505,517}` as a 12-sector tail group that fits the existing `24 KiB` stream window and could collapse two observed reads without changing buffer capacity. | Temporarily added a BUILDING6 high read-group table `{505,517}` and enabled it only for high tide with the existing `FG_BUILDING6_WINDOW_BYTES` window. Rebuilt, then ran focused BUILDING6 high against `compact-fgp3-v65-building4low-window36` with zero regression allowed, `95%` work identity, `--require-improvement`, and a `128` byte hot-symbol movement ceiling. Source was restored after the gate failed. | Rejected as exact-flat. BUILDING6 high stayed `loop_vb=2692`, `target_vb=2433`, `overrun_vb=259`, `blocking_vb=223`, `prefetch_overrun_vb=16`, `loop_reads=59`, and `due_misses=33`; the only movement was a `+68` byte hot-symbol address shift from the added table/branch. Artifact: `scratch/ps1-perf-iterate/building6-high-group505-517-probe/20260501-064053-1459561/summary.json`. | Do not promote one-off BUILDING6 high 12-sector tail grouping. This confirms that a group can fit the retained window and still fail to alter runtime cadence; next BUILDING6 work needs generated visible-cost metadata, a scheduler-owned read plan, or a pack/preprocessing change that changes more than one append boundary. |
| 2026-05-01 | `pal4-pair-store-v1` | `docs: log pal4 pair-store rejection` | Test the first `HP-066` compose-width variant: in the PAL4 opaque span helper, use an aligned 32-bit store for two resolved 16-bit pixels when the destination is word-aligned and the source starts on an even nibble. The hypothesis was that fewer destination stores would reduce compose-side CPU without touching pack data or reads. | Temporarily changed only `grCompositePacked4OpaqueRun()`, rebuilt, then started the 11-case canary against `compact-fgp3-v65-building4low-window36` with zero key-metric regression allowed. The run was stopped after BUILDING4 high failed, and source was restored. | Rejected. BUILDING4 high regressed `loop_vb 3071 -> 3077`, `overrun_vb 279 -> 284`, `blocking_vb 234 -> 237`, and `loop_read_vb 378 -> 381` with unchanged reads/work (`loop_reads=63`, `restore/compose/upload_calls=382`). ELF shrank `796412 -> 795968` and upload bytes moved `14076160 -> 14075520`, but the active cadence got worse. Artifact: `scratch/ps1-perf-iterate/pal4-pair-store-probe/20260501-064800-1499735/building4-high/perf-summary.json`. | Do not promote aligned PAL4 pair stores as a local runtime branch. The small code-size win is not worth the cadence regression. Future compose-width work needs generated aligned command classes, a precomputed pair LUT owned by pack/runtime metadata, or an indexed8-only probe measured separately. |
| 2026-05-01 | `read-plan-visible-cost-v1` | `scripts: add visible-cost read-plan ranking` | Add the missing host-side discriminator that recent failures asked for: rank foreground read candidates by visible-cadence risk, not only by saved read count. The hypothesis is that first-gap, internal-gap, partial-overlap, and group-overread metadata can stop us from repeatedly promoting high-risk raw groups into headless probes. | Extended `scripts/ps1-foreground-read-plan.py` to annotate each candidate with `visible_safety_score`, `visible_risk_score`, `visible_risk_hint`, `visible_risk_reasons`, first/internal gap fields, partial-overlap count, observed sectors, and group overread. It also emits `visible_candidate_sets` and prints a compact visible-cost ranking beside the old raw ranking. Verified with `python3 -m py_compile` and sample BUILDING6/VISITOR3 plans. | Promoted as host-side tooling. BUILDING6 high now flags the rejected `505..517` group as `medium-risk:validate-phase` with `partial-read-overlap` and `tight-first-gap`, while VISITOR3 high top raw groups show `high-risk:visible-cadence` instead of looking safe solely because they save two reads. This changes no PS1 runtime bytes or timing. | Use visible-cost fields before choosing more manual read groups. This is not proof that a candidate will promote; it is a sharper filter before spending emulator time and source churn. |
| 2026-05-01 | `fishing3-high-group246-258-v65-retry` | `docs: log fishing3 high group246 retry rejection` | Retest the historically rejected FISHING3 high `{246,258}` group under the current `compact-fgp3-v65-building4low-window36` baseline after the read-plan visible-cost tooling made it look superficially low-risk again. Acceptance required zero key-metric regression and at least one key speed/CD metric improvement. | Temporarily re-added `{246,258}` to `kFishing3HighReadGroups12`, rebuilt, and ran focused FISHING3 high against `scratch/ps1-perf-iterate/building4-low-window36-canaries/20260501-061457-1311308/summary.json` with `--require-improvement`, zero regression allowance, `95%` work identity, and a `128` byte hot-symbol movement ceiling. Source was restored after the gate failed. | Rejected again. The current baseline regressed `loop_vb 2096 -> 2100`, `overrun_vb 144 -> 146`, `blocking_vb 18 -> 19`, and `prefetch_overrun_vb 13 -> 14`; it also failed `--require-improvement`. `loop_reads` fell `41 -> 40`, but `group_hits=0`, due misses stayed `1`, and the one-read reduction did not translate into visible speed. Artifact: `scratch/ps1-perf-iterate/fishing3-high-group246-258-probe/20260501-070056-1572394/summary.json`. | Do not retry `{246,258}` as a standalone hand-coded group. It has now failed in multiple layout eras; only revisit that sector range as part of generated setup/preload metadata or a scheduler-owned plan that changes the read cadence, not just the append boundary. |
| 2026-05-01 | `visitor3-low-group182-194` | `docs: log visitor3 low group182 rejection` | Test VISITOR3 low's best low-visible-risk 12-sector candidate from the v65 read plan, `{182,194}`. The hypothesis was that the long first gap and clean overread profile could remove one low-tide read without worsening the already-large visible blocking pressure. | Temporarily added `kVisitor3LowReadGroups12` with `{182,194}` and routed VISITOR3 low through it while leaving the accepted high-tide groups unchanged. Rebuilt, then ran focused VISITOR3 low against `compact-fgp3-v65-building4low-window36` with `--require-improvement`, zero regression allowance, `95%` work identity, and a `128` byte hot-symbol movement ceiling. Source was restored after the gate failed. | Rejected. Hidden refill pressure improved (`prefetch_overrun_vb 83 -> 77`), but active playback got worse: `loop_vb 1532 -> 1539`, `overrun_vb 517 -> 524`, and `blocking_vb 314 -> 324`. `loop_reads` fell `53 -> 52`, `due_misses` rose `22 -> 23`, and pack LBA/PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group182-194-probe/20260501-070730-1609892/summary.json`. | Do not promote `{182,194}` as a raw low-tide VISITOR3 group. Like the FISHING3 retry, this proves read-count and hidden-refill wins can steal visible cadence; VISITOR3 low needs generated cost/coverage metadata or a scheduler-owned plan, not one-off group appends selected by read count. |
| 2026-05-01 | `fg-preprocess-opportunity-matrix-v1` | `tools: add foreground preprocess opportunity matrix` | After repeated one-off read-group failures and the rejected whole-payload direct16 probe, build a current-format all-scene target sheet for selective pack-time graphics preprocessing. The hypothesis was that ranking FGP3 residual packs by measured timing pressure plus estimated upload-ready byte savings would identify higher-upside generated-format experiments without perturbing the PS1 speed binary. | Added `scripts/ps1-fg-preprocess-matrix.py`, parsed all `126` scene/tide rows from `docs/ps1/performance-scene-matrix.csv`, decoded current `FGP2` and `FGP3` packs, and generated `docs/ps1/performance-preprocess-opportunities.csv` plus `docs/ps1/performance-preprocess-opportunities.md`. Verified with `python3 -m py_compile scripts/ps1-fg-preprocess-matrix.py` and a full matrix generation pass. | Promoted as host-side planning tooling. The matrix includes `120` timing-bearing rows, keeps the accepted runtime baseline unchanged at about `14.588%` over target, and ranks the top upload-ready candidates as WALKSTUF1 low/high, VISITOR3 low/high, BUILDING4 high/low, BUILDING6 high/low, and BUILDING2 high/low. Estimated align4 x-band upload savings average about `67.5%` across timing-bearing measured rows, but many candidates need selective bands or compression because naive direct16 already proved too CD-heavy. | Promote the sheet, not a runtime speed win. The next pack-format experiment should start from this matrix and avoid whole-payload 16bpp expansion; target selective upload-ready/compressed bands or generated cleanup metadata for the highest-pressure rows. |
| 2026-05-01 | `building4-high-group537-561` | `docs: log building4 high group537 rejection` | Test BUILDING4 high's best 24-sector low-visible-risk candidate from the visible-cost planner, `{537,561}`. The hypothesis was that collapsing the late tail cluster could remove two active reads now that BUILDING4 low had proven a larger retained window can pay off. | Temporarily added a BUILDING4 high read-group table `{537,561}` and generalized group buffer capacity so the `24`-sector append would fit. Rebuilt, then ran focused BUILDING4 high against `compact-fgp3-v65-building4low-window36` with `--require-improvement`, zero regression allowance, `95%` work identity, and a bounded `256` byte hot-symbol movement ceiling. Source was restored after the gate failed. | Rejected. The group reduced reads `63 -> 61`, but active playback regressed: `loop_vb 3071 -> 3076`, `overrun_vb 279 -> 284`, `blocking_vb 234 -> 237`, and `prefetch_overrun_vb 24 -> 29`. The post-run read plan now ranks the remaining BUILDING4 high candidates as high-risk tight clusters, confirming the attempted tail group shifted cadence rather than freeing visible time. Artifact: `scratch/ps1-perf-iterate/building4-high-group537-561-probe/20260501-073205-1747852/summary.json`. | Do not promote raw 24-sector BUILDING4 high grouping. Variable group capacity may still be useful, but only alongside generated read scheduling or pack-time metadata that proves the larger append happens during safe slack; read-count savings alone remain insufficient. |
| 2026-05-01 | `final-frame-hold-v1` | `ps1: honor final foreground frame hold` | Scene-pack playback was replacing the captured final frame's own hold time with a hard-coded `150` VBlank tail. The hypothesis was that honoring the final entry deadline would remove a broad artificial active-loop tax without skipping frames or changing visual work identity. | Set the scene-pack startup tail counter to zero and moved final-frame scene shutdown until after the current frame's captured hold has elapsed. Ran the 11-case canary gate against `compact-fgp3-v65-building4low-window36`, then reran VISITOR3 high with an explicit bounded blocking allowance because the semantic tail removal exposed a small blocking tradeoff. | Promoted. The refreshed rows save `1455` active-loop VBlanks total: BUILDING4 high/low `-143/-143`, BUILDING6 high/low `-131/-131`, FISHING1 high `-140`, FISHING3 high/low `-136/-136`, VISITOR3 high/low `-80/-139`, and WALKSTUF1 high/low `-138/-138`. FISHING1 high moves under target (`1207/1074 -> 1067/1074`), FISHING3 is nearly target (`1960/1952`, `1956/1954`), and matrix rollup improves from `14.5886%` to `13.8440%` over target. VISITOR3 high is the explicit caveat: `blocking_vb 345 -> 356` while total loop improves `1496 -> 1416` and refill overrun falls `102 -> 83`. Artifacts: broad gate `scratch/ps1-perf-iterate/final-frame-hold-probe/20260501-074038-1796631/summary.json`; focused VISITOR3 gate `scratch/ps1-perf-iterate/final-frame-hold-visitor3-allow5/20260501-075742-1897496/summary.json`. | Promote as a timing-semantics correction, not a frame skip. The final frame is still displayed for its captured hold; only the artificial post-final `150` VBlank scene-pack tail is removed. Next target VISITOR3 high CD ownership because that row now has a known `+11` blocking tradeoff despite a large net loop win. |
| 2026-05-02 | `johnny2-v064-validation-refresh` | `ps1: validate johnny2 scene playback` | After JOHNNY2 visual signoff, refresh the performance matrix against the validated pinned island position and rebuilt high/low packs. | Ran `./scripts/ps1-perf-iterate.sh --case "johnny2-high::fgpilot johnny2 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -64 54 perf-log noloop seed 1" --case "johnny2-low::fgpilot johnny2 lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -64 54 perf-log noloop seed 1" --frames 7200 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/johnny2-v064-refresh`. | Refreshed only JOHNNY2. High is `1761/1751` (+0.57%, `blocking_vb=16`, `prefetch_overrun_vb=0`, `due_misses=3`); low is `1758/1750` (+0.46%, `blocking_vb=16`, `prefetch_overrun_vb=1`, `due_misses=3`). Both packs are `288637` bytes, correctness is clean, and visual signoff is complete. Matrix rollup moves to `13.7312%` over target / `88.7730%` target speed. Artifact: `scratch/ps1-perf-iterate/johnny2-v064-refresh/20260502-190601-677083/summary.json`. | Record as a validation refresh, not a performance optimization. The large row movement comes from the rebuilt validated pack/timing and pinned island position, not a runtime speed change. |
| 2026-05-02 | `fishing5-v065-current-ledger-overlay` | `ps1: validate fishing5 shark scene` | After FISHING5 visual/audible signoff, refresh the matrix against the correctness pack. The pack was rebuilt for shark cleanup, not speed, so acceptance required clean correctness and under-target loop timing for both tides. | Ran `./scripts/ps1-perf-iterate.sh --case-local-cd --case "fishing5-high::fgpilot fishing5 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --case "fishing5-low::fgpilot fishing5 lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --frames 7200 --timeout 240 --output scratch/ps1-perf-iterate/fishing5-v065-current-ledger-overlay`. | Refreshed only FISHING5. High and low are both `885/890` (-0.56%, `blocking_vb=0`, `prefetch_overrun_vb=0`, `due_misses=0`, `loop_reads=14`). Both packs are now `190765` bytes and `FGP2/pal4` after switching from compact FGP3 residuals to the full-frame keyed current-ledger overlay. Matrix rollup moves to `13.8803%` over target / `88.6078%` target speed. Artifact: `scratch/ps1-perf-iterate/fishing5-v065-current-ledger-overlay/20260502-223934-1842740/summary.json`. | Promote as a validation refresh, not a speed optimization. The correctness fix increases pack size and loses the old compact-FGP3 speed row, but the scene remains under target with clean correctness and no visible shark residue. |
| 2026-05-03 | `mary2-v068-wide-stitch` | `ps1: validate mary2 wide stitch scene` | After MARY2 visual/audible signoff, refresh the performance matrix against the validated wide scene-relative multi-view stitch packs. This records correctness-pack timing rather than claiming a runtime speed optimization. | Ran `./scripts/ps1-perf-iterate.sh --case "mary2-high::fgpilot mary2 lowtide 0 night 1 holiday 0 raft-stage 5 island-pos -154 54 perf-log noloop seed 1" --case "mary2-low::fgpilot mary2 lowtide 1 night 1 holiday 0 raft-stage 5 island-pos -154 54 perf-log noloop seed 1" --frames 7200 --timeout 240 --case-local-cd --output scratch/ps1-perf-iterate/mary2-v068-wide-stitch`. | Refreshed only MARY2. High is `2250/2246` (+0.18%, `blocking_vb=4`, `prefetch_overrun_vb=4`, `due_misses=0`); low is `2253/2246` (+0.31%, `blocking_vb=7`, `prefetch_overrun_vb=7`, `due_misses=0`). Both packs are `582189` bytes and `FGP2/pal4` after the wide stitch and full-host thought-bubble injection. Matrix rollup moves to `13.8570%` over target / `88.6303%` target speed. Artifact: `scratch/ps1-perf-iterate/mary2-v068-wide-stitch/20260503-142257-2557607/summary.json`. | Promote as a validation refresh. The new pack is random-position safe after far-right and true far-left visual stress, with production island placement still variable. |
| 2026-05-05 | `building2-high-group320-336-v072` | `docs: log building2 high group320 rejection` | Test the stale-matrix `BUILDING2` high 16-sector group candidate after the v0.7.2 scene-validation releases moved the pack layout. The hypothesis was that `{320,336}` might still collapse an active-loop read if the previous May 1 row was representative. | Temporarily raised grouped-window capacity to 16 sectors, added a high-tide `BUILDING2` read group `{320,336}`, rebuilt, and ran focused `building2-high`. Because the initial comparison used the stale May 1 baseline, also ran a fresh current-code v0.7.2 baseline from the same branch and pack layout. Source was restored after the gate. | Rejected/no-op. The probe and fresh v0.7.2 baseline both report `loop_vb=1476`, `target_vb=1285`, `overrun_vb=191`, `blocking_vb=286`, `prefetch_overrun_vb=66`, `loop_reads=96`, `due_misses=37`, and `group_hits=0`. The apparent regression versus May 1 came from stale layout data: old PS-EXE sector bucket `169984` / pack LBA `6678` versus current bucket `212992` / pack LBA `6179`. Artifacts: probe `scratch/ps1-perf-iterate/building2-high-group320-336-probe/20260505-221028-3368371/summary.json`; fresh baseline `scratch/ps1-perf-iterate/building2-high-current-v072-baseline/20260505-221205-3378183/summary.json`. | Do not promote or retry `{320,336}` on the current layout. Use current read-plan candidates such as `{538,550}` or `{531,543}` against the fresh v0.7.2 baseline instead of the stale matrix row. |
| 2026-05-05 | `building2-high-group538-550-v072` | `docs: log building2 high group538 rejection` | Test the current read-plan's lowest-risk `BUILDING2` high 12-sector candidate, `{538,550}`, after the stale `{320,336}` group never fired. The hypothesis was that a late-scene low-visible-risk group could collapse one active-loop read without perturbing visible cadence. | Temporarily added a high-tide `BUILDING2` read group `{538,550}` with the existing 13-sector grouped-window capacity and ran focused `building2-high` against the fresh v0.7.2 baseline `scratch/ps1-perf-iterate/building2-high-current-v072-baseline/20260505-221205-3378183/summary.json`. Source was restored after the gate failed. | Rejected/no-op. Metrics were exact-flat versus baseline: `loop_vb=1476`, `target_vb=1285`, `overrun_vb=191`, `blocking_vb=286`, `prefetch_overrun_vb=66`, `loop_reads=96`, `due_misses=37`, and `group_hits=0`. Layout stayed fixed at pack LBA `6179` and PS-EXE sector bucket `212992`; hot symbols shifted by `+4` bytes only because of the temporary table. Artifact: `scratch/ps1-perf-iterate/building2-high-group538-550-probe/20260505-221555-3400250/summary.json`. | Do not promote. Try the adjacent current read-plan candidate `{531,543}` next; if it also records `group_hits=0`, leave `BUILDING2` high raw grouping and move to generated read scheduling or setup-prime/inter-scene preload. |
| 2026-05-05 | `building2-high-group531-543-v072` | `docs: log building2 high group531 rejection` | Test the adjacent low-visible-risk `BUILDING2` high candidate `{531,543}` after `{538,550}` stayed exact-flat. The hypothesis was that this slightly earlier group might fire before the same late cluster and convert one active-loop read into useful visible timing relief. | Temporarily added high-tide `BUILDING2` read group `{531,543}` with the existing grouped-window capacity, rebuilt, and ran focused `building2-high` against the fresh v0.7.2 baseline. Source was restored after the gate failed. | Rejected as work-only/no timing win. `loop_reads` improved `96 -> 95`, but all promoted key metrics stayed flat: `loop_vb=1476`, `target_vb=1285`, `overrun_vb=191`, `blocking_vb=286`, `prefetch_overrun_vb=66`, and `due_misses=37`. `group_hits=1`; pack LBA stayed `6179` and PS-EXE sector bucket stayed `212992`. Artifact: `scratch/ps1-perf-iterate/building2-high-group531-543-probe/20260505-221833-3415780/summary.json`. | Do not promote raw `BUILDING2` high grouping for a one-read-only win. The row remains CD-bound, but this lane needs generated read scheduling, setup-prime/inter-scene preload, or a lower-level seek/cadence change to convert read-count savings into visible VBlank gains. |
| 2026-05-05 | `walkstuf1-prefetch-relief-v072` | `ps1: restore walkstuf1 prefetch under clean pressure` | After v0.7 scene validation, `WALKSTUF1` packs are validated `FGP2/pal4` with small frame payloads but large clean bboxes. The generic clean-snapshot pressure path disabled all optional prefetch, leaving both tides at `policy=none`, `hits=0`, `due_misses=214`, and roughly `900` visible blocking VBlanks. | Added a `WALKSTUF1`-only exception to the clean-memory relief gate so the small stage1/window buffers survive large clean-rect setup. Ran focused high/low against a fresh v0.7.2 baseline, repeated the acceptance run, then ran clean-source A/B canaries for FISHING1 high, FISHING3 low, VISITOR3 high, and BUILDING2 high. | Promoted with an explicit hidden-refill tradeoff. High improves `loop_vb 1753 -> 1637`, `overrun_vb 334 -> 227`, `blocking_vb 893 -> 297`, `loop_reads 214 -> 144`, and `due_misses 214 -> 54`; `prefetch_overrun_vb 0 -> 67`. Low improves `loop_vb 1755 -> 1634`, `overrun_vb 337 -> 225`, `blocking_vb 912 -> 304`, `loop_reads 214 -> 145`, and `due_misses 214 -> 55`; `prefetch_overrun_vb 0 -> 73`. FISHING1, FISHING3, VISITOR3, and BUILDING2 canaries stayed exact-flat versus clean source. Artifacts: baseline `scratch/ps1-perf-iterate/walkstuf1-current-v072-baseline/20260505-222229-3438693/summary.json`; accepted run `scratch/ps1-perf-iterate/walkstuf1-prefetch-relief-exception/20260505-222530-3456234/summary.json`; canaries `scratch/ps1-perf-iterate/walkstuf1-prefetch-relief-canaries/20260505-223534-3514957/summary.json`. | Promote as a current validated-pack prefetch recovery, not a generic clean-pressure relaxation. Matrix rollup moves from `13.8570%` to `13.6273%` over target and from `88.6303%` to `88.7828%` target speed. Next target: VISITOR3 high shows the same validated-pack `policy=none` risk in current canaries and now leads the preprocess opportunity sheet. |
| 2026-05-05 | `visitor3-prefetch-relief-v072` | `ps1: restore visitor3 prefetch under clean pressure` | Apply the same validated-pack memory-policy lesson to VISITOR3. Fresh current v0.7.2 baselines showed the old May 1 matrix rows were stale: both tides had fallen to `policy=none`, `hits=0`, `due_misses=144`, and about `900` visible blocking VBlanks. | Added a VISITOR3-only exception to the clean-memory relief gate so the stage1/window buffers survive large clean-rect setup. Ran focused high/low against the fresh current baseline, then gated unrelated canaries FISHING1 high, FISHING3 low, BUILDING2 high, plus WALKSTUF1 high/low. | Promoted with an explicit hidden-refill tradeoff. High improves against the fresh baseline `loop_vb 1642 -> 1455`, `overrun_vb 625 -> 445`, `blocking_vb 898 -> 363`, `loop_reads 144 -> 53`, and `due_misses 144 -> 31`; `prefetch_overrun_vb 0 -> 21`. Low improves `loop_vb 1654 -> 1453`, `overrun_vb 635 -> 444`, `blocking_vb 908 -> 365`, `loop_reads 144 -> 51`, and `due_misses 144 -> 32`; `prefetch_overrun_vb 0 -> 23`. All canaries stayed exact-flat. Artifacts: fresh baseline `scratch/ps1-perf-iterate/visitor3-current-v072-baseline/20260505-224859-3591131/summary.json`; accepted run `scratch/ps1-perf-iterate/visitor3-prefetch-relief-exception/20260505-225129-3605646/summary.json`; canaries `scratch/ps1-perf-iterate/visitor3-prefetch-relief-canaries-a/20260505-225400-3620302/summary.json` and `scratch/ps1-perf-iterate/visitor3-prefetch-relief-walkstuf1-canaries/20260505-225717-3639059/summary.json`. | Promote as a current-pack prefetch recovery and stale-row correction. The matrix aggregate moves from `13.6273%` to `13.7135%` over target because the old VISITOR3 rows were not current-layout measurements; direct current-code VISITOR3 active-loop performance still improves sharply. Next target: generated selective upload-ready/preprocess work for VISITOR3 remains top-ranked. |
| 2026-05-05 | `visitor3-generated-group163-low158` | `docs: log visitor3 generated group rejection` | After VISITOR3 prefetch recovery, the visible-cost read plan ranked high `163..175` and low `158..170` as low-visible-risk 12-sector candidates. Hypothesis: promoting only those generated ranges as tide-specific append groups would remove one active-loop read without changing visible cadence. | Added high group `{163,175}` and low group `{158,170}` as source-side read-group tables, then ran focused VISITOR3 high/low against `visitor3-v072-prefetch-relief` with `--require-improvement`. Source was reverted after the failed gate. | No promotion. High stayed key-timing flat (`loop_vb=1455`, `blocking_vb=363`, `prefetch_overrun_vb=21`) while only nominal `loop_reads` moved `53 -> 52`; low regressed `loop_vb 1453 -> 1454`, `blocking_vb 365 -> 366`, and `prefetch_overrun_vb 23 -> 25` despite nominal `loop_reads 51 -> 50`. Both tides reported `group_hits=0`, so the planned ranges did not actually match the runtime append starts. Artifact: `scratch/ps1-perf-iterate/visitor3-generated-group163-low158/20260505-231001-3709356/summary.json`. | Reject as another proof that visible-cost ranking is not sufficient by itself. Do not retry VISITOR3 high `163..175` or low `158..170` as standalone source tables; the next VISITOR3 work needs generated metadata that models actual append-start ownership, scheduler timing, or selective upload-ready/preprocessed graphics. |
| 2026-05-05 | `stand1-v072-current-refresh` | `docs: refresh stand1 performance baseline` | Refresh STAND1 before spending optimizer time on a stale zero-CD row. The battle card still measured the pre-validation `35`-frame stand pack at `342/202`, while the current validated source pack is an `18`-entry host-deadline loop. | Ran `./scripts/build-ps1.sh` and `./scripts/ps1-perf-iterate.sh --case "stand1-high::fgpilot stand1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --case "stand1-low::fgpilot stand1 lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/stand1-current-v072-refresh`. | Baseline correction only. High and low now both report `loop_vb=194`, `target_vb=202`, `over_target=-3.96%`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads=1`, and `due_misses=0`; the refreshed rows use `stand1-v072-current-refresh`. Artifact: `scratch/ps1-perf-iterate/stand1-current-v072-refresh/20260505-231725-3750628/summary.json`. | Record, but do not count as a runtime optimization. The aggregate moves from `13.7135%` to `12.4923%` over target and from `88.7395%` to `89.4905%` target speed because the stale STAND1 rows are no longer overstating current validated-pack cost. Next absolute gaps are VISITOR3, BUILDING2, and WALKSTUF1. |
| 2026-05-05 | `visitor4-v072-current-refresh` | `docs: refresh visitor4 performance baseline` | Refresh VISITOR4 after STAND1 proved stale validated-pack rows could distort the queue. The current generated VISITOR4 high/low packs are both `91205` bytes, while the battle card still reported earlier compact-FGP3 rows at `568/428`. | Ran `./scripts/build-ps1.sh` and `./scripts/ps1-perf-iterate.sh --case "visitor4-high::fgpilot visitor4 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --case "visitor4-low::fgpilot visitor4 lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/visitor4-current-v072-refresh`. | Baseline correction only. High and low now both report `loop_vb=424`, `target_vb=428`, `over_target=-0.93%`, `blocking_vb=0`, `prefetch_overrun_vb=0`, `loop_reads=6`, and `due_misses=0`; the refreshed rows use `visitor4-v072-current-refresh`. Artifact: `scratch/ps1-perf-iterate/visitor4-current-v072-refresh/20260505-232749-3808618/summary.json`. | Record, but do not count as a runtime optimization. The aggregate moves from `12.4923%` to `11.9317%` over target and from `89.4905%` to `89.9171%` target speed. VISITOR4 is no longer a zero-CD fixed-overhead target; next refresh/optimization queue starts with VISITOR3, WALKSTUF2, STAND2, and BUILDING2. |
| 2026-05-05 | `stand-family-v072-current-refresh` | `docs: refresh stand-family performance baseline` | Refresh the next stale zero-pressure stand-family rows before treating them as fixed-overhead optimizer targets. STAND2/7/8/9/10/11 still had April timing rows despite current generated pack changes. | Ran one 12-case `./scripts/ps1-perf-iterate.sh --skip-build` batch for STAND2, STAND7, STAND8, STAND9, STAND10, and STAND11 high/low with `--frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/stand-zero-pressure-current-v072-refresh`. | Baseline correction only. STAND2 high/low are `480/490`; STAND7 high/low `520/538`; STAND8 high/low `483/499` with `blocking_vb=2`; STAND9 high `520/538` and low `522/538`; STAND10 and STAND11 high/low `528/538`. All rows have `due_misses=0` and now use `stand-family-v072-current-refresh`. Artifact: `scratch/ps1-perf-iterate/stand-zero-pressure-current-v072-refresh/20260505-233406-3843826/summary.json`. | Record, but do not count as a runtime optimization. The aggregate moves from `11.9317%` to `9.0942%` over target and from `89.9171%` to `92.2322%` target speed. The refreshed stand rows are no longer top bottlenecks; next queue is VISITOR3, WALKSTUF2, STAND3, BUILDING1/2, and generated upload-ready/CD scheduling. |
| 2026-05-05 | `mismatch-top-v072-current-refresh` | `docs: refresh top mismatch performance baselines` | Continue removing stale validated-pack rows before chasing optimizer false positives. ACTIVITY7, VISITOR1, FISHING7, and ACTIVITY8 all had old rows that disagreed with current pack sizes/timing and still appeared high in the fixed-overhead queue. | Ran a 10-case current-pack batch for ACTIVITY7, VISITOR1, FISHING7, VISITOR5, and ACTIVITY8 high/low with `--frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/mismatch-top-current-v072-refresh`, then filtered VISITOR5 out of the promoted summary because it exposed a new current-pack due-miss failure shape. | Baseline correction only for the promoted eight rows. ACTIVITY7 is now `593/596` high and `594/596` low; VISITOR1 high/low are `672/677`; FISHING7 high/low are `715/725`; ACTIVITY8 is `898/904` high and `899/904` low. All promoted rows have `due_misses=0`; ACTIVITY8 retains `blocking_vb=1/2`. Artifacts: full batch `scratch/ps1-perf-iterate/mismatch-top-current-v072-refresh/20260505-234729-3920190/summary.json`; promoted summary `scratch/ps1-perf-iterate/mismatch-top-current-v072-refresh/20260505-234729-3920190/summary-promoted.json`. | Record, but do not count as a runtime optimization. The aggregate moves from `9.0942%` to `7.8267%` over target and from `92.2322%` to `93.3111%` target speed. VISITOR5 remains a targeted follow-up because current high/low came back `1196/1098` and `1207/1098` with `policy=none`, `loop_reads=178`, and `due_misses=178`. |
| 2026-05-06 | `visitor5-v072-prefetch-relief` | `ps1: restore visitor5 prefetch under clean pressure` | Fix the VISITOR5 current-pack due-miss failure exposed by the mismatch refresh. The validated high/low packs are `FGP2/pal4`, `354227` bytes, and have large clean bboxes; the generic clean-memory relief path disabled the small prefetch buffers that should fit, leaving both tides at `policy=none`, `loop_reads=178`, and `due_misses=178`. | Added a VISITOR5-only exception to the clean-memory relief gate and to the large clean-snapshot drop-prefetch path. Built, ran a focused VISITOR5 high/low probe, then repeated as a clean-source A/B: first captured fresh current-layout VISITOR5/WALKSTUF1/VISITOR3/BUILDING2/FISHING1 baselines, then reapplied the source patch and reran the same six cases. | Promoted with an explicit hidden-refill tradeoff. High improves `loop_vb 1196 -> 1111`, `target_vb 1098 -> 1090`, `overrun_vb 98 -> 21`, `blocking_vb 504 -> 12`, `loop_reads 178 -> 25`, and `due_misses 178 -> 0`; low improves `1207 -> 1112`, `1098 -> 1090`, `109 -> 22`, `523 -> 12`, `178 -> 25`, and `178 -> 0`. The only gate failure was hidden `prefetch_overrun_vb 0 -> 12`. WALKSTUF1 high, VISITOR3 high, BUILDING2 high, and FISHING1 high stayed exact-flat against the fresh current baseline. Artifacts: clean baseline `scratch/ps1-perf-iterate/visitor5-prefetch-relief-v072-ab-baseline/20260506-001645-4085958/summary.json`; accepted promoted summary `scratch/ps1-perf-iterate/visitor5-prefetch-relief-v072-ab-probe/20260506-002301-4122166/summary-promoted.json`; full probe with gate diagnostics `scratch/ps1-perf-iterate/visitor5-prefetch-relief-v072-ab-probe/20260506-002301-4122166/summary.json`. | Promote as a scene-local validated-pack prefetch recovery, not a generic memory-pressure relaxation. Matrix rollup improves from `7.8267%` to `7.5934%` over target and from `93.3111%` to `93.5083%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `9.8` percentage points of over-target gap and added about `6.4` percentage points of target speed. |
| 2026-05-06 | `stale-top-v072b-current-refresh` | `docs: refresh stale top performance baselines` | Continue clearing stale validated-pack rows before treating high-percent rows as optimizer targets. WALKSTUF2, STAND3, BUILDING1, and FISHING6 still had old active-timing rows that no longer matched current generated packs and made the priority queue look worse than it was. | Ran `./scripts/ps1-perf-iterate.sh --skip-build` with eight current-pack cases for WALKSTUF2, STAND3, BUILDING1, and FISHING6 high/low using `--frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/stale-top-current-v072b-refresh`, then promoted the summary with `PS1_PERF_STATS_VERSION=stale-top-v072b-current-refresh ./scripts/ps1-foreground-scene-manifest.py --write-sheet docs/ps1/performance-scene-matrix.csv --merge-existing-sheet docs/ps1/performance-scene-matrix.csv --summary scratch/ps1-perf-iterate/stale-top-current-v072b-refresh/20260506-003812-13674/summary.json --stats-version stale-top-v072b-current-refresh` and regenerated the preprocess opportunity sheet. | Baseline correction only. WALKSTUF2 high/low are now `451/461` with zero blocking/due misses; STAND3 high/low are `547/557` with zero blocking/due misses; FISHING6 high/low are `744/753` with zero blocking/due misses; BUILDING1 high/low are `794/778` and `794/779` with `blocking_vb=21`, `prefetch_overrun_vb=21`, and `due_misses=0`. Artifact: `scratch/ps1-perf-iterate/stale-top-current-v072b-refresh/20260506-003812-13674/summary.json`. | Record, but do not count as a runtime optimization. Matrix rollup improves from `7.5934%` to `5.9984%` over target and from `93.5083%` to `94.8106%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `11.4` percentage points of over-target gap and added about `7.7` percentage points of target speed. Next priority is current true bottlenecks: VISITOR3, BUILDING2, WALKSTUF1, ACTIVITY9, and generated selective preprocessing. |
| 2026-05-06 | `stale-zero-v072b-current-refresh` | `docs: refresh stale zero-pressure baselines` | Clear the next stale zero-pressure-looking rows before spending optimizer time on fixed-overhead experiments. JOHNNY5, FISHING4, ACTIVITY6, MISCGAG1, STAND15, and ACTIVITY4 still had April timing rows that made them look like large render/present overhead targets. | Ran a 12-case current-pack batch with `./scripts/ps1-perf-iterate.sh --skip-build` for JOHNNY5, FISHING4, ACTIVITY6, MISCGAG1, STAND15, and ACTIVITY4 high/low using `--frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/stale-zero-current-v072b-refresh`, then promoted the summary as `stale-zero-v072b-current-refresh` and regenerated the preprocess opportunity sheet. | Baseline correction only. JOHNNY5 is now `811/820` high and `810/820` low; FISHING4 is `835/842` high and `870/843` low; ACTIVITY6 high/low are `912/911`; MISCGAG1 high/low are `953/961`; STAND15 high/low are `444/452`; ACTIVITY4 is `1065/1065` high and `1102/1070` low. FISHING4 low and ACTIVITY4 low retain current CD pressure (`blocking_vb=251/361`, `due_misses=89/155`), but most of the old zero-CD fixed-overhead signal disappears. Artifact: `scratch/ps1-perf-iterate/stale-zero-current-v072b-refresh/20260506-005509-109010/summary.json`. | Record, but do not count as a runtime optimization. Matrix rollup improves from `5.9984%` to `4.5708%` over target and from `94.8106%` to `96.0653%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `12.8` percentage points of over-target gap and added about `9.0` percentage points of target speed. Next priority remains true current bottlenecks: VISITOR3, BUILDING2, WALKSTUF1, ACTIVITY9, and generated selective preprocessing. |
| 2026-05-06 | `stale-zero2-v072b-current-refresh` | `docs: refresh remaining stale zero-pressure baselines` | Finish clearing stale zero-pressure-looking rows before deeper runtime work. FISHING1 low, JOHNNY3, STAND4, STAND16, ACTIVITY10, JOHNNY4, FISHING8, STAND6, STAND5, and STAND12 still carried April timing rows or mixed current rows that could distort the fixed-overhead queue. | Ran a 20-case current-pack batch with `./scripts/ps1-perf-iterate.sh --skip-build` for FISHING1, JOHNNY3, STAND4, STAND16, ACTIVITY10, JOHNNY4, FISHING8, STAND6, STAND5, and STAND12 high/low using `--frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/stale-zero2-current-v072b-refresh`, then promoted the summary as `stale-zero2-v072b-current-refresh` and regenerated the preprocess opportunity sheet. | Baseline correction only. Most rows collapse to target or under target: FISHING1 low is `1067/1074`, STAND4 is `1202/1220` high and `1203/1218` low, STAND16 is `473/472`, JOHNNY4 is `1204/1214`, FISHING8 is `1243/1253`, STAND6 is `1346/1364`, STAND5 is `1442/1460`, and STAND12 is `1450/1459` high and `1450/1460` low. JOHNNY3 and ACTIVITY10 are now proven real current CD-pressure rows: JOHNNY3 has `blocking_vb=560/464`, `due_misses=209/175`; ACTIVITY10 has `blocking_vb=622/664`, `due_misses=255/255`. Artifact: `scratch/ps1-perf-iterate/stale-zero2-current-v072b-refresh/20260506-011422-217507/summary.json`. | Record, but do not count as a runtime optimization. Matrix rollup improves from `4.5708%` to `3.1299%` over target and from `96.0653%` to `97.3807%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `14.3` percentage points of over-target gap and added about `10.3` percentage points of target speed. Next priority is now true current bottlenecks: VISITOR3, BUILDING2, WALKSTUF1, ACTIVITY10, ACTIVITY9, JOHNNY3, and generated selective preprocessing. |
| 2026-05-06 | `activity10-johnny3-v072-prefetch-relief` | `ps1: restore activity10 johnny3 prefetch under clean pressure` | The stale-zero2 refresh proved ACTIVITY10 and JOHNNY3 were not fixed-overhead rows: large clean snapshots disabled optional prefetch and left both scenes at `policy=none` with hundreds of due misses. The packs are compact FGP3 residuals, so this looked like the same safe scene-local relief pattern used for WALKSTUF1, VISITOR3, and VISITOR5. | Added ACTIVITY10 and JOHNNY3 to the clean-memory-relief and large-clean drop-prefetch exception list, rebuilt the PS1 executable, then ran the four focused high/low cases with case-local CDs. The first probe was intentionally discarded because `--case-local-cd` implies `--skip-build` and used the old executable; the accepted run is the rebuilt probe. | Promoted. ACTIVITY10 high improves `loop_vb 1424 -> 1259`, `target_vb 1262 -> 1259`, `blocking_vb 622 -> 7`, `loop_reads 255 -> 11`, and `due_misses 255 -> 1`; low improves `1441 -> 1255`, `1260 -> 1256`, `664 -> 17`, `255 -> 22`, and `255 -> 2`. JOHNNY3 high improves `1258 -> 1158`, `1166 -> 1161`, `560 -> 10`, `209 -> 14`, and `209 -> 1`; low improves `1229 -> 1157`, `1166 -> 1166`, `464 -> 0`, `175 -> 7`, and `175 -> 0`. Hidden refill overrun is accepted (`4/4/6/0`) because visible timing/read pressure drops sharply and all four rows pass correctness. Artifact: `scratch/ps1-perf-iterate/activity10-johnny3-prefetch-relief-rebuilt-probe/20260506-014728-405578/summary.json`. | Promote as a scene-local validated-pack prefetch recovery. The four rows save `523` active-loop VBlanks total and the matrix rollup improves from `3.1299%` to `2.7833%` over target and from `97.3807%` to `97.6931%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `14.6` percentage points of over-target gap and added about `10.6` percentage points of target speed. Next priority is VISITOR3, BUILDING2, WALKSTUF1, ACTIVITY9, JOHNNY1, WALKSTUF3, and generated selective preprocessing. |
| 2026-05-06 | `stale-pressure-v072c-current-refresh` | `docs: refresh building2 pressure baselines` | Before optimizing the new top queue, refresh rows that predated the final validation pack/layout work. BUILDING2 still carried May 1 rows and JOHNNY1 still carried April full-matrix rows; ACTIVITY9 and WALKSTUF3 were also probed to see whether they were immediately refreshable. | Ran an eight-case current-pack batch for BUILDING2, ACTIVITY9, JOHNNY1, and WALKSTUF3 high/low with `--frames 3600 --timeout 180 --case-local-cd --output scratch/ps1-perf-iterate/stale-pressure-current-v072c-refresh`. Promoted only a filtered BUILDING2 high/low summary as `stale-pressure-v072c-current-refresh`; ACTIVITY9 and WALKSTUF3 were not promoted because the noloop window did not emit `JCPERF2`. | Baseline correction plus target discovery. BUILDING2 high/low refresh to `1476/1285` and `1465/1278`, with `blocking_vb=286/279`, `prefetch_overrun_vb=66/48`, `loop_reads=96/86`, and `due_misses=37/40`. JOHNNY1 high/low fresh baselines both report `2002/1947`, `policy=none`, `blocking_vb=365`, `loop_reads=111`, and `due_misses=111`, proving a current clean-pressure prefetch failure. Artifacts: full batch `scratch/ps1-perf-iterate/stale-pressure-current-v072c-refresh/20260506-015935-473122/summary.json`; promoted BUILDING2 summary `scratch/ps1-perf-iterate/stale-pressure-current-v072c-refresh/20260506-015935-473122/summary-building2-promoted.json`. | Record BUILDING2 as a baseline correction, not a runtime optimization. The corrected BUILDING2 rows plus the upcoming JOHNNY1 proof move the priority queue away from JOHNNY1 and toward VISITOR3, ACTIVITY9, WALKSTUF1, BUILDING2, and WALKSTUF3. |
| 2026-05-06 | `johnny1-v072c-prefetch-relief` | `ps1: restore johnny1 prefetch under clean pressure` | The current refresh showed JOHNNY1 high/low had the same validated-pack memory-pressure failure class as WALKSTUF1, VISITOR3, VISITOR5, ACTIVITY10, and JOHNNY3: compact packs, large clean snapshot, `policy=none`, and every payload due-missed. | Added JOHNNY1 to the clean-memory-relief and large-clean drop-prefetch exception list, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran JOHNNY1 high/low plus FISHING1 high, BUILDING2 high, and VISITOR3 high canaries. Promoted only the filtered JOHNNY1 high/low summary as `johnny1-v072c-prefetch-relief`. | Promoted. Both tides improve `loop_vb 2002 -> 1974`, `target_vb 1947 -> 1944`, `blocking_vb 365 -> 27`, `loop_reads 111 -> 16`, and `due_misses 111 -> 0`; `prefetch_overrun_vb` rises `0 -> 27` as an accepted hidden-refill tradeoff. FISHING1 high stayed `1068/1074`, BUILDING2 high stayed `1476/1285`, and VISITOR3 high stayed `1455/1010`. Artifacts: full run `scratch/ps1-perf-iterate/johnny1-prefetch-relief-v072c-probe/20260506-020850-526213/summary.json`; promoted JOHNNY1 summary `scratch/ps1-perf-iterate/johnny1-prefetch-relief-v072c-probe/20260506-020850-526213/summary-johnny1-promoted.json`. | Promote as a scene-local validated-pack prefetch recovery. The runtime win saves `56` active-loop VBlanks versus fresh JOHNNY1 current rows, while the matrix rollup improves from `2.7833%` to `2.5740%` over target and from `97.6931%` to `97.8684%` target speed after the paired BUILDING2 refresh. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `14.8` percentage points of over-target gap and added about `10.8` percentage points of target speed. Next priority is VISITOR3, ACTIVITY9, WALKSTUF1, BUILDING2, WALKSTUF3, and generated selective preprocessing. |
| 2026-05-06 | `stale-pressure2-v072c-current-refresh` | `docs: refresh activity9 walkstuf3 pressure baselines` | The previous 3600-frame current-pressure probe cut off ACTIVITY9 and WALKSTUF3 before `JCPERF2`, so rerun them with a longer window before deciding whether they are real runtime targets or stale-row cleanup. | Ran `./scripts/ps1-perf-iterate.sh --skip-build --case-local-cd` for ACTIVITY9 high/low and WALKSTUF3 high/low with `--frames 9000 --timeout 240 --output scratch/ps1-perf-iterate/activity9-walkstuf3-long-current-refresh`, then promoted only a filtered WALKSTUF3 high/low summary as `stale-pressure2-v072c-current-refresh`. | Baseline correction plus target discovery. ACTIVITY9 high/low fresh baselines are `2225/2056` and `2234/2054` with `policy=none`, `blocking_vb=884/871`, `loop_reads=251/251`, and `due_misses=251/251`, proving the clean-pressure failure class. WALKSTUF3 high/low refresh to `2321/2278` and `2321/2295` with `policy=stage1_window`, `blocking_vb=68/40`, `prefetch_overrun_vb=36/20`, `loop_reads=74/72`, and `due_misses=6/5`. Artifact: full batch `scratch/ps1-perf-iterate/activity9-walkstuf3-long-current-refresh/20260506-022347-609439/summary.json`; promoted WALKSTUF3 summary `scratch/ps1-perf-iterate/activity9-walkstuf3-long-current-refresh/20260506-022347-609439/summary-walkstuf3-promoted.json`. | Record WALKSTUF3 as a baseline correction, not a runtime optimization. The refresh drops WALKSTUF3 from a top current target to a near-target canary row and makes ACTIVITY9 the next clean-pressure prefetch-relief candidate. |
| 2026-05-06 | `activity9-v072c-prefetch-relief` | `ps1: restore activity9 prefetch under clean pressure` | The long ACTIVITY9 refresh showed the validated wide-stitch packs were due-missing every payload because large clean snapshots disabled optional prefetch. The hypothesis was that a scene-local clean-pressure exception would recover stage/window prefetch without perturbing unrelated canaries. | Added ACTIVITY9 to the clean-memory-relief and large-clean drop-prefetch exception list, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran ACTIVITY9 high/low plus FISHING1 high, BUILDING2 high, VISITOR3 high, and JOHNNY1 high canaries. Promoted only the filtered ACTIVITY9 high/low summary as `activity9-v072c-prefetch-relief`. | Promoted. High improves against the fresh long-window baseline `loop_vb 2225 -> 2194`, `target_vb 2056 -> 2050`, `blocking_vb 884 -> 139`, `loop_reads 251 -> 116`, and `due_misses 251 -> 25`; low improves `2234 -> 2218`, `2054 -> 2050`, `871 -> 175`, `251 -> 166`, and `251 -> 48`. Hidden refill overrun rises `0 -> 12/17`, accepted because visible blocking/read pressure drops sharply. FISHING1 high stayed `1068/1074`, BUILDING2 high stayed `1476/1285`, VISITOR3 high stayed `1455/1010`, and JOHNNY1 high stayed `1974/1944`. Artifacts: full run `scratch/ps1-perf-iterate/activity9-prefetch-relief-v072c-probe/20260506-022916-641019/summary.json`; promoted ACTIVITY9 summary `scratch/ps1-perf-iterate/activity9-prefetch-relief-v072c-probe/20260506-022916-641019/summary-activity9-promoted.json`. | Promote as a scene-local validated-pack prefetch recovery. The runtime win saves `47` active-loop VBlanks versus fresh ACTIVITY9 current rows, and the paired WALKSTUF3 refresh plus ACTIVITY9 win moves the matrix rollup from `2.5740%` to `2.4134%` over target and from `97.8684%` to `98.0111%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `15.0` percentage points of over-target gap and added about `10.9` percentage points of target speed. Next priority is VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9 generated scheduling/preprocessing, and stale-layout candidates such as MARY1/BUILDING7. |
| 2026-05-06 | `stale-layout-v072c-current-refresh` | `ps1: restore mary1 prefetch under clean pressure` | Clear stale validated-pack rows that were still distorting the target queue before applying the next clean-pressure exception. ACTIVITY5, BUILDING5, BUILDING7, VISITOR6, and VISITOR7 all had old timing rows from before the current validated pack layout. | Ran a 12-case current-pack refresh for ACTIVITY5, BUILDING5, BUILDING7, MARY1, VISITOR6, and VISITOR7 high/low with `--frames 9000 --timeout 240 --case-local-cd --output scratch/ps1-perf-iterate/stale-layout-current-v072c-refresh`, then promoted only the non-MARY1 rows as `stale-layout-v072c-current-refresh`. | Baseline correction only for the promoted ten rows. ACTIVITY5 is now under target (`1730/1749`, `1731/1749`), BUILDING5 is near target (`3359/3346`, `3359/3347`), BUILDING7 is at target (`3132/3133`, `3130/3133`), VISITOR6 is under target (`2043/2047` both tides), and VISITOR7 is under target (`1619/1625` both tides). MARY1 was not promoted from this refresh because it exposed a current `policy=none` prefetch failure (`5345/4838`, `5160/4842`). Artifact: full batch `scratch/ps1-perf-iterate/stale-layout-current-v072c-refresh/20260506-024800-745470/summary.json`; promoted non-MARY summary `scratch/ps1-perf-iterate/stale-layout-current-v072c-refresh/20260506-024800-745470/summary-promoted-nonmary.json`. | Record as baseline correction, not a runtime optimization. The ten-row refresh moves the matrix rollup from `2.4134%` to `1.8207%` over target and from `98.0111%` to `98.5648%` target speed, removing ACTIVITY5/BUILDING5/BUILDING7/VISITOR6/VISITOR7 from the high-priority list. |
| 2026-05-06 | `mary1-v072c-prefetch-relief` | `ps1: restore mary1 prefetch under clean pressure` | The stale-layout refresh proved current MARY1 high/low had the same validated-pack memory-pressure failure class as the earlier relief scenes: large clean snapshot, optional prefetch disabled, `policy=none`, hundreds of due misses, and visible blocking. | Added MARY1 to the clean-memory-relief and large-clean drop-prefetch exception list, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran MARY1 high/low plus FISHING1 high, BUILDING2 high, VISITOR3 high, WALKSTUF1 high, and ACTIVITY9 high canaries. Promoted only the filtered MARY1 high/low summary as `mary1-v072c-prefetch-relief`. | Promoted. MARY1 high improves from the fresh current baseline `loop_vb 5345 -> 4867`, `target_vb 4838 -> 4830`, `blocking_vb 2277 -> 47`, `loop_reads 820 -> 119`, and `due_misses 820 -> 2`; low improves `5160 -> 4860`, `4842 -> 4840`, `1831 -> 31`, `682 -> 89`, and `682 -> 1`. Hidden refill overrun rises to `37/24`, accepted because visible blocking/read pressure collapses. FISHING1, BUILDING2, VISITOR3, WALKSTUF1, and ACTIVITY9 canaries stayed on their accepted profiles. Artifacts: full run `scratch/ps1-perf-iterate/mary1-prefetch-relief-v072c-probe/20260506-030520-843485/summary.json`; promoted MARY1 summary `scratch/ps1-perf-iterate/mary1-prefetch-relief-v072c-probe/20260506-030520-843485/summary-mary1-promoted.json`. | Promote as a scene-local validated-pack prefetch recovery. The runtime win saves `778` active-loop VBlanks versus fresh MARY1 current rows, and the paired stale-layout refresh plus MARY1 win moves the matrix rollup from `2.4134%` to `1.7731%` over target and from `98.0111%` to `98.6106%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `15.6` percentage points of over-target gap and added about `11.5` percentage points of target speed. Next priority is VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9, BUILDING3/ACTIVITY12 current refresh or generated scheduling/preprocessing. |
| 2026-05-06 | `stale-next-v072c-current-refresh` | `ps1: restore activity11 activity12 prefetch under clean pressure` | Continue clearing stale validated-pack rows before optimizing. BUILDING3, FISHING2, and MARY5 still carried old rows that made them look like major current bottlenecks; ACTIVITY11/ACTIVITY12 were included to classify whether their old rows were stale or current prefetch failures. | Ran a 10-case current-pack refresh for BUILDING3, ACTIVITY12, ACTIVITY11, FISHING2, and MARY5 high/low with `--frames 9000 --timeout 240 --case-local-cd --output scratch/ps1-perf-iterate/stale-next-current-v072c-refresh`, then promoted only BUILDING3, FISHING2, and MARY5 as `stale-next-v072c-current-refresh`. | Baseline correction plus target discovery. BUILDING3 high/low refresh to `5460/5465` with zero blocking/due misses, FISHING2 high/low refresh to `1761/1763` and `1759/1765` with `blocking_vb=6/3` and zero due misses, and MARY5 high/low refresh to `1591/1582` and `1590/1582`. ACTIVITY11 high/low and ACTIVITY12 low exposed `policy=none` due-miss failure rows and were intentionally excluded from this promoted summary. Artifact: full batch `scratch/ps1-perf-iterate/stale-next-current-v072c-refresh/20260506-032610-959745/summary.json`; promoted clean summary `scratch/ps1-perf-iterate/stale-next-current-v072c-refresh/20260506-032610-959745/summary-promoted-clean.json`. | Record as baseline correction, not a runtime optimization. BUILDING3, FISHING2, and MARY5 drop out of the top queue; ACTIVITY11/ACTIVITY12 move to the targeted clean-pressure relief lane below. |
| 2026-05-06 | `activity11-12-v072c-prefetch-relief` | `ps1: restore activity11 activity12 prefetch under clean pressure` | The stale-next refresh proved ACTIVITY11 high/low and ACTIVITY12 low matched the validated-pack clean-pressure failure class: large clean snapshots disabled optional prefetch and caused every payload to due-miss. The hypothesis was that the same scene-local exception pattern used for MARY1 would restore stage/window prefetch without perturbing canaries. | Added ACTIVITY11 and ACTIVITY12 to the clean-memory-relief and large-clean drop-prefetch exception list, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran ACTIVITY11 high/low, ACTIVITY12 high/low, plus FISHING1 high, BUILDING2 high, VISITOR3 high, WALKSTUF1 high, and MARY1 high canaries. Promoted only ACTIVITY11/ACTIVITY12 rows as `activity11-12-v072c-prefetch-relief`. | Promoted. ACTIVITY11 high improves from the fresh current baseline `1835/1724 -> 1729/1720`, `blocking_vb 526 -> 10`, `loop_reads 207 -> 29`, and `due_misses 207 -> 1`; low improves `1830/1724 -> 1729/1717`, `blocking_vb 524 -> 14`, `loop_reads 207 -> 29`, and `due_misses 207 -> 1`. ACTIVITY12 low improves `1559/1415 -> 1409/1411`, `blocking_vb 653 -> 10`, `loop_reads 251 -> 25`, and `due_misses 251 -> 1`; high stays clean at `1411/1412`. FISHING1, BUILDING2, VISITOR3, WALKSTUF1, and MARY1 canaries stayed on accepted profiles. Artifacts: full run `scratch/ps1-perf-iterate/activity11-12-prefetch-relief-v072c-probe/20260506-034030-1041086/summary.json`; promoted summary `scratch/ps1-perf-iterate/activity11-12-prefetch-relief-v072c-probe/20260506-034030-1041086/summary-activity11-12-promoted.json`. | Promote as a scene-local validated-pack prefetch recovery. The runtime win saves `665` active-loop VBlanks versus fresh ACTIVITY11/12 failure rows, and the paired stale-next refresh plus relief moves the matrix rollup from `1.7731%` to `1.1131%` over target and from `98.6106%` to `99.2199%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.3` percentage points of over-target gap and added about `12.1` percentage points of target speed. Next priority is VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9, BUILDING4/6, and generated selective preprocessing. |
| 2026-05-06 | `building4-6-johnny6-v072c-prefetch-relief` | `ps1: restore building johnny prefetch relief` | Continue the clean-pressure relief path after a current refresh proved BUILDING4 high/low, BUILDING6 high/low, and JOHNNY6 high/low were real current `policy=none` rows. The same refresh showed ACTIVITY1 was stale-clean and safe to promote as a baseline correction. | Added BUILDING4, BUILDING6, and JOHNNY6 to the clean-memory-relief and large-clean drop-prefetch exception lists, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran affected high/low cases plus VISITOR3 high, WALKSTUF1 high, BUILDING2 high, ACTIVITY9 high, MARY1 high, and ACTIVITY11 high canaries. Promoted only BUILDING4/6/JOHNNY6 from the relief probe and ACTIVITY1 high/low from the stale refresh. | Promoted. Versus fresh failure baselines, BUILDING4 high improves `3286/2776 -> 2985/2774`, `blocking_vb 1519 -> 285`, `loop_reads 427 -> 93`, and `due_misses 427 -> 40`; low improves `3294/2779 -> 2981/2784`, `1510 -> 199`, `427 -> 62`, and `427 -> 14`. BUILDING6 high improves `2642/2460 -> 2520/2442`, `1035 -> 62`, `306 -> 74`, and `306 -> 1`; low improves `2641/2460 -> 2515/2437`, `1035 -> 70`, `306 -> 73`, and `306 -> 2`. JOHNNY6 high/low improve `2856/2804 -> 2832/2800`, `blocking_vb 275 -> 28`, and `due_misses 81 -> 0`. Hidden refill overrun rises on the promoted rows but visible blocking/read pressure collapses. Canaries stayed on accepted profiles. ACTIVITY1 refreshes to clean current rows `2754/2764` and `2754/2765`. Artifacts: stale/current refresh `scratch/ps1-perf-iterate/stale-remaining-current-v072c-refresh/20260506-040115-1157679/summary.json`; relief probe `scratch/ps1-perf-iterate/building4-6-johnny6-relief-v072c-probe/20260506-041506-1236594/summary.json`; promoted summaries beside each full run. | Promote as scene-local validated-pack prefetch recovery plus ACTIVITY1 baseline correction. The runtime win saves `1066` active-loop VBlanks versus fresh BUILDING4/6/JOHNNY6 failure rows, and the paired ACTIVITY1 refresh plus relief moves the matrix rollup from `1.1131%` to `1.0270%` over target and from `99.2199%` to `99.3047%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.4` percentage points of over-target gap and added about `12.2` percentage points of target speed. Next priority is VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9, BUILDING4 residual scheduling, FISHING4 low, BUILDING6 residual scheduling, ACTIVITY4 low, and generated selective preprocessing. |
| 2026-05-06 | `activity4-fishing4-v072c-prefetch-relief` | `ps1: restore activity fishing prefetch relief` | Finish the current clean-pressure relief lane by testing the two remaining small due-miss rows from the latest current matrix: ACTIVITY4 low and FISHING4 low. The hypothesis was that the same validated-pack clean-snapshot exception would restore stage/window prefetch without perturbing canaries. | Added ACTIVITY4 and FISHING4 to the clean-memory-relief and large-clean drop-prefetch exception lists, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran ACTIVITY4 high/low, FISHING4 high/low, plus FISHING1 high, VISITOR3 high, WALKSTUF1 high, BUILDING2 high, ACTIVITY9 high, BUILDING4 high, and BUILDING6 high canaries. Promoted only ACTIVITY4/FISHING4 rows as `activity4-fishing4-v072c-prefetch-relief`. | Promoted. ACTIVITY4 high stays under target at `1065/1066` with `blocking_vb=4`; low improves from the fresh current row `1102/1070 -> 1064/1068`, `blocking_vb 361 -> 1`, `loop_reads 155 -> 5`, and `due_misses 155 -> 0`. FISHING4 high stays under target at `835/842` with `blocking_vb=2`; low improves `870/843 -> 834/843`, `blocking_vb 251 -> 0`, `loop_reads 89 -> 3`, and `due_misses 89 -> 0`. Canaries stayed on accepted profiles: VISITOR3 high `1456/1010`, WALKSTUF1 high `1640/1406`, BUILDING2 high `1468/1285`, ACTIVITY9 high `2196/2051`, BUILDING4 high `2985/2774`, and BUILDING6 high `2520/2442`. Artifact: full run `scratch/ps1-perf-iterate/activity4-fishing4-relief-v072c-probe/20260506-044105-1381975/summary.json`; promoted summary `scratch/ps1-perf-iterate/activity4-fishing4-relief-v072c-probe/20260506-044105-1381975/summary-activity4-fishing4-promoted.json`. | Promote as scene-local validated-pack prefetch recovery. The runtime win saves `74` active-loop VBlanks versus fresh ACTIVITY4/FISHING4 current rows and collapses `244` due misses to zero. Matrix rollup moves from `1.0270%` to `0.9628%` over target and from `99.3047%` to `99.3677%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.4` percentage points of over-target gap and added about `12.3` percentage points of target speed. Next priority is VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9, BUILDING4 residual scheduling, BUILDING6 residual scheduling, and generated selective preprocessing. |
| 2026-05-06 | `activity9-window-v072c` | `ps1: retune activity9 stream windows` | After the clean-pressure due-miss lane was exhausted, ACTIVITY9 still had one of the larger remaining measured gaps. The binary question was whether a tide-specific stream-window knee could reduce active-loop blocking without broader policy risk. | Ran a parameter sweep for ACTIVITY9 high current/24/28/32 KiB and low current/20/24/28/32 KiB, promoted only high `32 KiB` and low `20 KiB`, then patched the source policy and rebuilt because `--case-local-cd` implies `--skip-build`. Source-mode confirmation ran ACTIVITY9 high/low plus exact-boot VISITOR3, WALKSTUF1, BUILDING2, BUILDING4, BUILDING6, and FISHING1 canaries. | Promoted. High improves `2194/2050 -> 2185/2049`, `blocking_vb 139 -> 117`, `loop_reads 116 -> 62`, and `due_misses 25 -> 14`. Low improves `2218/2050 -> 2197/2054`, `blocking_vb 175 -> 146`, `loop_reads 166 -> 114`, and `due_misses 48 -> 28`. Low `24/28/32 KiB` variants are rejected for now because they reduce reads but regress total loop VBlanks versus the `20 KiB` point. Exact-boot canaries stayed on accepted profiles: VISITOR3 `1456/1010`, WALKSTUF1 `1640/1406`, BUILDING2 `1468/1285`, BUILDING4 `2985/2774`, BUILDING6 `2520/2442`, and FISHING1 `1069/1072`. Artifacts: sweep `scratch/ps1-perf-iterate/activity9-window-sweep-v072c-probe/20260506-050444-1514883/summary.json`; source probe `scratch/ps1-perf-iterate/activity9-window-source-v072c-probe/20260506-051552-1578732/summary.json`; promoted summary `scratch/ps1-perf-iterate/activity9-window-source-v072c-probe/20260506-051552-1578732/summary-activity9-window-promoted.json`. | Promote as a scene-local stream-window policy win. The runtime win saves `30` active-loop VBlanks versus the accepted ACTIVITY9 relief baseline and removes `31` due misses. Matrix rollup moves from `0.9628%` to `0.9493%` over target and from `99.3677%` to `99.3794%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.5` percentage points of over-target gap and added about `12.3` percentage points of target speed. Next priority remains VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9 residual scheduling/preprocessing, BUILDING4 residual scheduling, BUILDING6 residual scheduling, and generated selective preprocessing. |

| 2026-05-06 | `building2-high-visible-groups-v072c` | Log-only; source reverted. | After ACTIVITY9 window retuning, BUILDING2 high remained a top current CD-pressure row and scalar windows were already rejected. The visible-cost planner suggested early high-side groups might be safer than the old rejected high `325..331` table. | Tested two source-side BUILDING2 high read-group probes against the exact matrix canary boot: `96..104` from the 8-sector visible-cost set, then `66..78` from the 12-sector set. Both probes rebuilt first because `--case-local-cd` implies `--skip-build`; both used the latest exact canary baseline `activity9-window-source-matrix-canaries-v072c/20260506-052635-1639477/summary.json`. | Failed/no promotion. `96..104` reduced `loop_reads 96 -> 95` and hidden refill `57 -> 49`, but active playback did not improve (`loop_vb=1468`) and visible pressure worsened (`blocking_vb 301 -> 303`, `due_misses 37 -> 43`). `66..78` left active playback and blocking flat (`1468`, `301`), did not reduce `loop_reads`, and only moved hidden refill `57 -> 56` while due pressure remained worse than the baseline. Artifacts: `scratch/ps1-perf-iterate/building2-high-group96-104-probe/20260506-054642-1751737/summary.json` and `scratch/ps1-perf-iterate/building2-high-group66-78-probe/20260506-054809-1760023/summary.json`. | Do not promote BUILDING2 high one-off visible-cost hand tables. They can improve local read/refill counters without reducing active-loop VBlanks, so BUILDING2 should move to scheduler-owned read timing or selective upload-ready/preprocessed pack data rather than more standalone group tables. |
| 2026-05-06 | `grdrawbackground-o2-v072c` | Log-only; source reverted. | The current `-O2` audit again ranked `grDrawBackground()` as the top scoped-helper retest because it was still compiled with `optimize("Os")`. The binary question was whether the fully validated v0.7 pack/layout baseline had changed enough for default `-O2` upload codegen to help. | Temporarily removed the function-scoped `optimize("Os")` attribute from `grDrawBackground()`, rebuilt first, then ran exact VISITOR3 high, WALKSTUF1 high, BUILDING2 high, and ACTIVITY9 high canaries against the accepted `activity9-window` baseline. Source was restored and rebuilt after the gate. | Failed/no promotion. VISITOR3, WALKSTUF1, and BUILDING2 stayed exactly flat on all key timing metrics; ACTIVITY9 high only moved `blocking_vb 117 -> 116` with `loop_vb=2185` and `prefetch_overrun_vb=14` unchanged. The helper grew by `504` bytes (`grDrawBackground size +504`) and shifted nearby hot symbols without improving active-loop timing. Artifact: `scratch/ps1-perf-iterate/grdrawbackground-o2-probe/20260506-055856-1819512/summary.json`. | Keep the scoped `grDrawBackground()` `-Os` attribute under this baseline. Retest default `-O2` only after generated upload-ready bands or a material dirty-upload rewrite changes the helper's hot path. |
| 2026-05-06 | `grupdatedisplay-o2-v072c` | Log-only; source reverted. | Continue the current `-O2` audit's scoped graphics-helper lane after the `grDrawBackground()` retest failed. The binary question was whether default `-O2` for the display wrapper could improve present/upload cadence without changing scene packs. | Temporarily removed the function-scoped `optimize("Os")` attribute from `grUpdateDisplay()`, rebuilt first, then ran exact VISITOR3 high, WALKSTUF1 high, BUILDING2 high, and ACTIVITY9 high canaries against the accepted `activity9-window` baseline. Source was restored and rebuilt after the gate. | Failed/no promotion. VISITOR3, WALKSTUF1, BUILDING2, and ACTIVITY9 stayed flat on key timing metrics. The helper grew by `40` bytes and shifted the CD helpers by `40` bytes without a speed win. Artifact: `scratch/ps1-perf-iterate/grupdatedisplay-o2-probe/20260506-060643-1863744/summary.json`. | Keep the scoped `grUpdateDisplay()` `-Os` attribute. Retest only after display/present scheduling or upload ownership changes materially. |
| 2026-05-06 | `activity9-hand-groups-v072c` | Log-only; source reverted. | After the ACTIVITY9 stream-window win, the cap-aware visible-cost planner suggested one high-tide `434..450` group and one low-tide `841..853` group as possible residual CD scheduling follow-ups. The binary question was whether a hand-coded grouped append could turn the residual blocking/refill pressure into another loop win without changing pack data. | Temporarily added ACTIVITY9 high/low read-group tables and routed them through the existing grouped-append path, rebuilt first, then ran ACTIVITY9 high/low exact boots against `scratch/ps1-perf-iterate/activity9-window-source-v072c-probe/20260506-051552-1578732/summary.json` with zero key-metric regression and `--require-improvement`. Source was restored and rebuilt after the gate. | Failed/no promotion. High tide fired the group but regressed active playback: `loop_vb 2185 -> 2209`, `overrun_vb 136 -> 159`, `blocking_vb 117 -> 123`, and `prefetch_overrun_vb 14 -> 17`. Low tide stayed exact-flat on key metrics (`loop_vb=2197`, `blocking_vb=146`, `prefetch_overrun_vb=8`) and failed `--require-improvement`. Artifact: `scratch/ps1-perf-iterate/activity9-groups-source-probe/20260506-061756-1927328/summary.json`. | Do not promote ACTIVITY9 standalone hand-coded grouped reads. Like BUILDING/VISITOR/WALKSTUF failures, saved-read candidates can steal visible cadence or stay flat; ACTIVITY9 residual work should move to scheduler-owned read timing, generated append-start ownership metadata, or selective preprocessing/upload-ready pack data. |
| 2026-05-06 | `walkstuf1-pal4-padded-fgp3-v072c` | Log-only; asset probe restored. | After ACTIVITY9 hand groups failed, WALKSTUF1 remained a top current gap. The binary question was whether the validated `FGP2/pal4` WALKSTUF1 high/low packs could be converted back to padded `FGP3/pal4` temporal residual packs, preserving the original file sizes and CD layout while reducing runtime foreground payload work. | Backed up `generated/ps1/foreground/WALKSTUF1.FG2` and `WALK1LOW.FG2`, ran `scripts/build-fg3-temporal-residual-pack.py` on each pack, then checked whether the generated `FGP3` data fit inside the original `1535263` byte files before benchmarking. The original `FGP2` assets were restored immediately after the size check. | Failed/no promotion before runtime measurement. Both current pal4 packs expand instead of shrink: payload `1530775 -> 1712687`, or `-11.88%` saved. Padding to the old CD footprint is impossible without truncating real pack data, and letting the larger packs move layout would conflate this data-shape test with a broad CD layout change. | Do not promote or benchmark truncated pal4 `FGP3` for current WALKSTUF1. Retry only with a new encoder that proves the residual payload fits, a selective/keyframed residual strategy that shrinks, or an explicit layout-changing experiment with canaries. The next WALKSTUF1 work should prefer selective preprocessing/upload-ready metadata, scheduler-owned read timing, or another generated cost model. |
| 2026-05-06 | `aligned-cd-helper-o2-v072c` | Log-only; source reverted. | Continue the `-O2` audit after the graphics helper retests. `ps1_streamReadAlignedFromCdFileInto()` is the active foreground stream-window helper and was still forced to function-scoped `-Os`; the binary question was whether default `-O2` would reduce active CD helper work now that all scenes are validated and the pack/layout baseline has changed. | Temporarily removed the function-scoped `optimize("Os")` attribute from `ps1_streamReadAlignedFromCdFileInto()`, rebuilt first because `--case-local-cd` implies `--skip-build`, then ran exact-boot canaries against `scratch/ps1-perf-iterate/activity9-window-source-v072c-probe/20260506-051552-1578732/summary.json`. The first partial run covered FISHING1, VISITOR3, WALKSTUF1, and BUILDING2; the tail run covered ACTIVITY9, BUILDING4, and BUILDING6. Source was restored and rebuilt after the gate. | Failed/no promotion. Key timing stayed flat across the exact-label comparisons except a one-VBlank BUILDING2 blocking-only movement in the partial run (`375 -> 374`), while the tail run showed ACTIVITY9, BUILDING4, and BUILDING6 exact-flat and failed `--require-improvement`. The helper grew materially (`ps1_streamReadAlignedIntoFile size +524`) and ELF grew `952312 -> 952488` while the PS-EXE bucket and pack LBAs stayed fixed. Artifacts: partial case summaries under `scratch/ps1-perf-iterate/aligned-cd-helper-o2-v072c-probe/20260506-063344-2015168/`; tail summary `scratch/ps1-perf-iterate/aligned-cd-helper-o2-v072c-probe-tail/20260506-063944-2049543/summary.json`. | Keep the aligned CD helper at function-scoped `-Os`. Default `-O2` is a code-size/layout cost with no real loop/blocking/refill win under the v0.7.2 baseline; retest only after CD helper splitting, async/scheduler ownership, or generated read metadata changes the active helper shape. |
| 2026-05-06 | `unbuffered-cd-helper-o2-v072c` | Log-only; source reverted. | Finish the current `-O2` audit function lane by retesting the setup-facing `ps1_streamReadFromCdFile()` helper without its accepted function-scoped `-Os`. The binary question was whether default `-O2` now wins under the v0.7.2 validated-scene baseline. | Temporarily removed `optimize("Os")` from `ps1_streamReadFromCdFile()`, rebuilt first, then started the exact canary gate against `scratch/ps1-perf-iterate/activity9-window-source-v072c-probe/20260506-051552-1578732/summary.json`. The run was stopped after FISHING1 failed the zero-regression gate and VISITOR3 gave enough secondary signal. Source was restored and rebuilt after the partial gate. | Failed/no promotion. FISHING1 regressed immediately: `loop_vb 1068 -> 1069`, `blocking_vb 4 -> 9`, `loop_reads 20 -> 21`, and `due_misses 0 -> 1`. VISITOR3 moved favorably on visible timing (`loop_vb 1494 -> 1490`, `blocking_vb 385 -> 380`, `prefetch_overrun_vb 5 -> 4`) but also increased `loop_reads 57 -> 61`, so it cannot override the FISHING1 failure. The helper grew `592 -> 660` bytes and ELF grew `952312 -> 952548`; PS-EXE stayed in the `215040` byte bucket. Artifacts: `scratch/ps1-perf-iterate/unbuffered-cd-helper-o2-v072c-probe/20260506-065055-2112868/`. | Keep the unbuffered CD stream helper at function-scoped `-Os`. Default `-O2` is not globally safe under current canaries; retry only after splitting setup/direct-read call sites or changing scheduler/read metadata enough that the FISHING1 phase is no longer comparable. |
| 2026-05-06 | `ps1-stubs-o2-v072c` | Log-only; source reverted. | Continue HP-064 by retesting the cold/default-off `src/ps1_stubs.c` translation unit at default `-O2`. The binary question was whether the old accepted stubs `-Os` cleanup now cost timing under the v0.7.2 validated-scene baseline. | Temporarily removed only `src/ps1_stubs.c` from the forced `-Os` list. An initial edit accidentally removed it from the source list too and failed link on libc stub symbols; that source-list mistake was corrected before the real gate. Rebuilt clean, then ran FISHING1 high, VISITOR3 high, WALKSTUF1 high, BUILDING2 high, and ACTIVITY9 high canaries against `scratch/ps1-perf-iterate/activity9-window-source-v072c-probe/20260506-051552-1578732/summary.json`. Source was restored and rebuilt after the exact-flat result. | Failed/no promotion. All five canaries stayed exact-flat: FISHING1 `1068/1073`, VISITOR3 `1494/1015`, WALKSTUF1 `1639/1416`, BUILDING2 `1519/1278`, and ACTIVITY9 `2163/2055`, with matching blocking, prefetch, loop-read, and due-miss counters. The PS-EXE bucket stayed `215040`, but ELF grew `952312 -> 952360` and the stubs object grew `16640 -> 16680`. Artifact: `scratch/ps1-perf-iterate/ps1-stubs-o2-v072c-probe/20260506-070025-2166635/summary.json`. | Keep `src/ps1_stubs.c` at whole-TU `-Os`. Default `-O2` is exact-flat plus code growth under the current phase, so do not retry it unless the startup/stub code shape or link layout changes materially. |
| 2026-05-06 | `activity9-pal4-fgp3-padded-v072c` | `ps1: convert activity9 to padded fgp3` | After the direct WALKSTUF1 pal4 FGP3 size gate failed, ACTIVITY9 remained a current residual-pack target with large payload and visible CD pressure. The binary question was whether its validated wide-stitched FGP2/pal4 high/low packs could be converted to FGP3/pal4 temporal residuals while preserving the exact old CD footprint and sector layout. | Converted `generated/ps1/foreground/ACTIVITY9.FG2` and `ACTV9LOW.FG2` with `scripts/build-fg3-temporal-residual-pack.py`, verified the generated FGP3 payload fit, then padded both files back to the original `1745484` bytes. Rebuilt first because asset changes affect the disc, then ran ACTIVITY9 high/low plus FISHING1, VISITOR3, WALKSTUF1, BUILDING2, BUILDING4, and BUILDING6 canaries against the accepted `activity9-window-v072c` baseline. | Promoted. ACTIVITY9 high improves `2185/2049 -> 2101/2056`, `blocking_vb 117 -> 44`, `loop_reads 62 -> 52`, and `due_misses 14 -> 2`; low improves `2197/2054 -> 2103/2053`, `blocking_vb 146 -> 60`, `loop_reads 114 -> 93`, and `due_misses 28 -> 7`. Runtime foreground payload drops `1740180 -> 1453793` bytes while each pack stays at `1745484` bytes and the LBAs/sectors stay fixed (`3108/3961`, `853` sectors each). Canaries were key-flat; the aggregate wrapper reported failure only because unchanged canaries were included under `--require-improvement`. Artifact: `scratch/ps1-perf-iterate/activity9-pal4-fgp3-padded-v072c-probe/20260506-071427-2245928/summary.json`; promoted summary `summary-activity9-fgp3-promoted.json`. | Promote as a data-shape win. Matrix rollup moves from `0.9493%` to `0.8745%` over target and from `99.3794%` to `99.4479%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.5` percentage points of over-target gap and added about `12.3` target-speed points. Next priority is VISITOR3, WALKSTUF1, BUILDING2, BUILDING4/6, and selective preprocessing/upload-ready metadata, with ACTIVITY9 residual scheduling now using this FGP3 baseline. |
| 2026-05-06 | `activity9-lowgroup-v072c` | `ps1: add activity9 low fgp3 read group` | After the padded FGP3 conversion, ACTIVITY9's read plan changed enough that the old rejected FGP2 hand-group result no longer applied. The binary question was whether a new low-visible-risk grouped append could reduce residual low-tide CD pressure without affecting high tide or other scene canaries. | First tested high `{447,463}` and low `{624,636}` FGP3 groups together; high was exact-flat while low improved, so the high table was removed. The promoted source keeps only the low-tide FGP3 group and reruns ACTIVITY9 low plus ACTIVITY9 high, FISHING1, VISITOR3, WALKSTUF1, and BUILDING2 canaries against the padded FGP3 baseline. | Promoted. ACTIVITY9 low improves `loop_vb 2103 -> 2093`, target accounting `2053 -> 2056`, `overrun_vb 50 -> 37`, `blocking_vb 60 -> 43`, `prefetch_overrun_vb 18 -> 14`, and `due_misses 7 -> 5`; `loop_reads` rises `93 -> 94`, accepted because visible timing and target-relative loop time improve. ACTIVITY9 high stays exact-flat at `2101/2056`, and FISHING1, VISITOR3, WALKSTUF1, and BUILDING2 canaries stayed key-flat. Artifact: `scratch/ps1-perf-iterate/activity9-low-fgp3-group-v072c-probe/20260506-074520-2420094/summary.json`; promoted summary `summary-activity9-lowgroup-promoted.json`. | Promote as a scoped low-tide CD scheduling win. Matrix rollup moves from `0.8745%` to `0.8692%` over target and from `99.4479%` to `99.4529%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.5` percentage points of over-target gap and added about `12.4` target-speed points. The rejected high `{447,463}` no-op should not be retried without a changed ACTIVITY9 high data shape or scheduler. |
| 2026-05-06 | `building4-high-group821-837-v072c` | Log-only; source reverted. | After the ACTIVITY9 lowgroup promotion, BUILDING4 high remained a top residual CD-pressure row. A fresh current-code baseline showed a new visible-cost candidate `{821,837}` that was distinct from the previously rejected `{537,561}` range. | First ran a current-code BUILDING4 high/low plus FISHING1, VISITOR3, WALKSTUF1, BUILDING2, and ACTIVITY9 canary refresh to anchor the exact baseline. Then tested BUILDING4 high `{821,837}` in source. The first source probe was accidentally gated on the default `16 KiB` window and stayed exact-flat; the corrected probe routed it under BUILDING4 high's `24 KiB` window and gave it `32 KiB` group capacity before rerunning the same gate. Source was restored after the corrected no-op. | Failed/no promotion. Even with the correct BUILDING4 high window/capacity, BUILDING4 high stayed exact-flat at `2985/2774`, `blocking_vb=285`, `prefetch_overrun_vb=51`, `loop_reads=93`, and `due_misses=40`. BUILDING4 low and all canaries stayed flat as well. The only material source effect was code growth/layout shift (`foregroundPilotPlay size +116`, hot symbols shifted `+116`) with no timing or work-volume win. Artifacts: baseline `scratch/ps1-perf-iterate/building4-current-v072c-baseline/20260506-081048-2562996/summary.json`; default-window no-op `scratch/ps1-perf-iterate/building4-high-group821-837-v072c-probe/20260506-081926-2612631/summary.json`; corrected-window no-op `scratch/ps1-perf-iterate/building4-high-group821-837-window32-v072c-probe/20260506-082817-2663165/summary.json`. | Do not promote or retry BUILDING4 high `{821,837}` as a standalone grouped append under the current pack/window scheduler. Like the older `{537,561}` attempt, the visible-cost planner can identify plausible saved-read windows that do not become runtime wins. BUILDING4 residual work should move to scheduler-owned append timing, generated append-start ownership metadata, or selective upload-ready/preprocessed pack data. |
| 2026-05-06 | `building2-high-group538-550-v072c` | Log-only; source reverted. | After the BUILDING4 `{821,837}` no-op, a fresh BUILDING2 high read-plan pass identified tail group `{538,550}` as a low-visible-risk candidate distinct from the older rejected early and mid-scene groups. The binary question was whether this later append window could reduce BUILDING2 high's residual CD pressure without perturbing canaries. | Temporarily added a high-tide BUILDING2 read-group table `{538,550}`, rebuilt first because `--case-local-cd` implies `--skip-build`, and ran BUILDING2 high plus BUILDING2 low, FISHING1 high, VISITOR3 high, WALKSTUF1 high, ACTIVITY9 low, and BUILDING4 high canaries against the fresh current-code baseline. Source was restored after the no-op gate. | Failed/no promotion. BUILDING2 high stayed exact-flat at `1519/1278`, `blocking_vb=375`, `prefetch_overrun_vb=42`, `loop_reads=96`, and `due_misses=53`. The other canaries stayed on their accepted profiles; the only material effect was code growth/layout shift (`foregroundPilotPlay size +44`, hot symbols shifted `+44`) with no runtime timing or read-count win. Artifact: `scratch/ps1-perf-iterate/building2-high-group538-550-v072c-probe/20260506-083951-2729116/summary.json`; baseline `scratch/ps1-perf-iterate/building4-current-v072c-baseline/20260506-081048-2562996/summary.json`. | Do not promote or retry BUILDING2 high `{538,550}` as a standalone hand-coded grouped append. The visible-cost planner can still find plausible tail ranges that the current runtime scheduler does not exploit; BUILDING2 residual work should move to append-start ownership metadata, scheduler-owned read timing, or selective upload-ready/preprocessed pack data. |
| 2026-05-06 | `building6-pal4-padded-fgp3-v072c` | Log-only; asset probe restored. | BUILDING6 remains a small residual gap and is still current `FGP2/pal4`, so the next binary data-shape question was whether the validated high/low packs could be converted to padded `FGP3/pal4` within the current CD footprint like the earlier large-pack BUILDING6 conversion did before later validation recaptures. | Generated temporary FGP3 packs from `generated/ps1/foreground/BUILDING6.FG2` and `BUIL6LOW.FG2` into `scratch/fgp3-size-probes/building6-v072c/`, then stopped at the size gate before touching runtime assets or benchmarking. | Failed/no promotion before runtime measurement. Both high and low expand: payload `1438098 -> 1595173`, and full file size `1444370 -> 1601445` (`-10.87%` saved, `fits=False`). Padding to the old CD footprint would require truncation, and allowing the larger files to move the disc layout would conflate this encoder test with a broad layout experiment. | Do not promote or benchmark current BUILDING6 pal4 temporal FGP3 as a same-layout padded pack. Retry BUILDING6 data-shape work only with selective/keyframed residual encoding, upload-ready preprocessing, or an explicit layout-changing gate with broad canaries. |
| 2026-05-06 | `pause-menu-o2-v072c` | Log-only; source reverted. | Continue the generated `-O2` audit queue after the hot helper and stubs retests. `src/pause_menu.c` was the highest-priority remaining whole-TU candidate, so the binary question was whether returning that cold-ish menu code to default `-O2` could improve phase/layout without harming accepted scene playback. | Temporarily removed `src/pause_menu.c` from the forced `-Os` source list, rebuilt first, and ran FISHING1, VISITOR3, WALKSTUF1, BUILDING2, ACTIVITY9, BUILDING4, and BUILDING6 canaries against the fresh current-code baseline. Source was restored and rebuilt after the gate failed. | Failed/no promotion. The PS-EXE sector bucket grew `215040 -> 217088` and shifted foreground LBAs by `+1`. FISHING1 regressed `loop_vb 1068 -> 1070`, `blocking_vb 4 -> 6`, and `prefetch_overrun_vb 4 -> 6`; WALKSTUF1 high regressed `1639 -> 1644` with refill `56 -> 69`; BUILDING2 high regressed `1519 -> 1521` with refill `42 -> 59`; ACTIVITY9 low regressed `2093 -> 2099` and `blocking_vb 43 -> 51`. VISITOR3 did not produce complete JCPERF2 metrics in the aggregate run. Artifact: `scratch/ps1-perf-iterate/pause-menu-o2-v072c-probe/20260506-085728-2828295/summary.json`. | Keep `src/pause_menu.c` at whole-TU `-Os`. Default `-O2` is a broad layout regression with no compensating speed win; do not retry unless the pause/menu source or link-phase padding changes materially. |
| 2026-05-06 | `ps1-captions-o2-v072c` | Log-only; source reverted. | Continue the cold/default-off `-O2` audit queue after `pause_menu.c` failed. `src/ps1_captions.c` is small and should not touch active scene playback, so the first binary gate was whether default `-O2` could at least stay FISHING1-clean without crossing the PS-EXE bucket. | Temporarily removed `src/ps1_captions.c` from the forced `-Os` source list, rebuilt, and ran the focused FISHING1 high canary against the fresh current-code baseline. Because the canary was exact-flat and the build grew, source was restored before broader canaries. | Failed/no promotion. FISHING1 stayed exact-flat at `1068/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, and `loop_reads=20`, with the PS-EXE bucket still `215040`. However the ELF grew `952268 -> 954084` and the captions object grew `28180 -> 31060` bytes with no speed or work-volume improvement. Artifact: `scratch/ps1-perf-iterate/ps1-captions-o2-v072c-probe/20260506-091107-2905726/summary.json`. | Keep `src/ps1_captions.c` at whole-TU `-Os`. Default `-O2` is exact-flat plus code growth under the current phase, so do not retry unless caption code shape or link layout changes materially. |
| 2026-05-06 | `memcard-o2-v072c` | Log-only; source reverted. | Continue the cold/default-off `-O2` audit queue after `ps1_captions.c` failed. `src/memcard.c` is menu/save-facing code, so the binary question was whether default `-O2` could stay phase-clean or expose a small helper win without moving foreground layout. | Temporarily removed `src/memcard.c` from the forced `-Os` source list, rebuilt first, and ran a focused FISHING1 high canary against the fresh current-code baseline. The first run used a mismatched case label and did not compare; the corrected rerun used `fishing1-high-canary` so the baseline gate enforced regressions. Source was restored and rebuilt after the failure. | Failed/no promotion. The PS-EXE bucket and foreground LBA stayed fixed (`215040` bytes, `FISHING1.FG2` LBA `434`), but FISHING1 regressed `loop_vb 1068 -> 1069`, `blocking_vb 4 -> 5`, and `prefetch_overrun_vb 4 -> 6` with no key metric improvement. The ELF grew `952268 -> 952888` and the memcard object grew `28716 -> 28992` bytes. Artifacts: comparison gate `scratch/ps1-perf-iterate/memcard-o2-v072c-probe-compare/20260506-091922-2953135/summary.json`; initial label-mismatch run `scratch/ps1-perf-iterate/memcard-o2-v072c-probe/20260506-091750-2944352/summary.json`. | Keep `src/memcard.c` at whole-TU `-Os`. Default `-O2` is both a code-growth and visible-CD-pressure regression under the current phase; do not retry unless memcard code shape, link layout, or the foreground scheduler changes materially. |
| 2026-05-06 | `holidays-o2-v072c` | Log-only; source reverted. | Continue the cold/default-off `-O2` audit queue after `memcard.c` failed. `src/holidays.c` is inactive in the no-holiday FISHING1 canary, so the binary question was whether default `-O2` could stay phase-clean enough to justify any later holiday-specific probe. | Temporarily removed `src/holidays.c` from the forced `-Os` source list, rebuilt first, and ran the focused `fishing1-high-canary` gate against the fresh current-code baseline. Source was restored and rebuilt after the gate failed. | Failed/no promotion. The PS-EXE bucket and foreground LBA stayed fixed (`215040` bytes, `FISHING1.FG2` LBA `434`), but FISHING1 regressed `loop_vb 1068 -> 1069`, `blocking_vb 4 -> 5`, and `prefetch_overrun_vb 4 -> 6` with no key metric improvement. The ELF grew `952268 -> 955740` and the holidays object grew `25440 -> 30008` bytes. Artifact: `scratch/ps1-perf-iterate/holidays-o2-v072c-probe/20260506-092338-2977642/summary.json`. | Keep `src/holidays.c` at whole-TU `-Os`. Default `-O2` is a large code-growth and visible-CD-pressure regression under the current phase; do not retry unless holiday code shape, link layout, or foreground scheduler phase changes materially. |
| 2026-05-06 | `visitor3-high-group-removal-v072c` | Log-only; source reverted. | Test whether the accepted VISITOR3 high read groups had become stale or harmful after the later pack/window work. The binary question was whether removing the old groups, or keeping branch shape while disabling them, could reduce the current top VISITOR3 gap. | First removed the VISITOR3 high read-group table/branch and ran VISITOR3 high plus FISHING1 against an older building4 canary baseline. That exposed a stale-baseline trap: VISITOR3 appeared to improve only because the baseline row was older than the current matrix. Then restored the branch shape but disabled the group pointers and reran against the current VISITOR3 baseline from `visitor3-prefetch-relief-exception`. Source was restored and rebuilt after the correct gate failed. | Failed/no promotion. Against the correct current VISITOR3 baseline, disabling the groups regressed VISITOR3 high `loop_vb 1455 -> 1456` and `overrun_vb 445 -> 446` with `blocking_vb=363`, `prefetch_overrun_vb=21`, and no key metric improved. The earlier branch-removal probe also shifted hot code and showed FISHING1 phase sensitivity (`1068 -> 1069`, `blocking_vb 4 -> 5`, `prefetch_overrun_vb 4 -> 6`) while comparing VISITOR3 against a stale row. Artifacts: stale-baseline branch removal `scratch/ps1-perf-iterate/visitor3-high-drop-old-groups-v072c-probe/20260506-092920-3009921/summary.json`; correct-baseline branch-shape disable `scratch/ps1-perf-iterate/visitor3-high-disable-groups-branchshape-v072c-probe/20260506-093305-3031357/summary.json`. | Keep the accepted VISITOR3 high groups. Future VISITOR3 probes must compare against `scratch/ps1-perf-iterate/visitor3-prefetch-relief-exception/20260505-225129-3605646/summary.json` or a freshly regenerated VISITOR3 baseline, not the older building4 current canary file. |
| 2026-05-06 | `read-plan-append-fireability-v1` | `tools: score foreground group fireability` | The VISITOR3 and BUILDING4 no-op probes showed that visible-cost ranking was still spending emulator time on groups that covered reads but could not fire in the runtime, because `fgRuntimeGroupedAppendTargetEnd()` only applies a group when the current append start equals the group's start sector. | Extended `scripts/ps1-foreground-read-plan.py` to compute `append_start_fireable`, `append_start_read_indices`, and nearest observed append-start deltas for each candidate. Non-fireable groups now receive `reject:no-observed-append-start` and an added visible-risk penalty before the visible shortlist is printed. Verified with `python3 -m py_compile`, current VISITOR3 high, and current BUILDING4 high plans. | Promoted as host-side tooling. VISITOR3 high still lists start-aligned candidates like `163..175` as fireable, but non-start-aligned raw windows are demoted. BUILDING4 high now demotes the previously probed `821..837` raw candidate because it is `fire=no`; the visible shortlist moves to start-aligned candidates such as `739..755`, `706..722`, and `763..779` before any source edit is attempted. This changes no PS1 runtime bytes or timing. | Use append-start fireability with visible-cost score before any future hand-coded grouped-read table. Raw read coverage is not enough; a candidate must match an observed append start or be implemented by a scheduler/metadata change that can create that append point. |
| 2026-05-06 | `building4-high-group57-69-v072c` | Log-only; source reverted. | After adding append-start fireability scoring, test BUILDING4 high's only useful fireable 12-sector candidate `{57,69}`. The hypothesis was that a fireable group might avoid the previous `{821,837}` no-op while staying inside the existing grouped-window capacity. | Temporarily added `kBuilding4HighReadGroups12 = {{57,69}}`, routed it only for BUILDING4 high under the existing `24 KiB` scene window, rebuilt first, and ran BUILDING4 high/low plus FISHING1, VISITOR3, WALKSTUF1, and ACTIVITY9 canaries against the current BUILDING4 baseline. Source was restored and rebuilt after the gate failed. | Failed/no promotion. BUILDING4 high stayed exact-flat at `2985/2774`, `blocking_vb=285`, `prefetch_overrun_vb=51`, `loop_reads=93`, and `due_misses=40`; BUILDING4 low also stayed flat. The source table grew `foregroundPilotPlay` by `96` bytes and shifted hot symbols, causing canary failures: FISHING1 regressed `1068 -> 1069`, `blocking_vb 4 -> 5`, `prefetch_overrun_vb 4 -> 6`; WALKSTUF1 high regressed `1639 -> 1640` and `prefetch_overrun_vb 56 -> 67`; ACTIVITY9 low regressed `2093 -> 2098`, `blocking_vb 43 -> 47`, `prefetch_overrun_vb 14 -> 19`. Artifact: `scratch/ps1-perf-iterate/building4-high-group57-69-v072c-probe/20260506-101242-3247684/summary.json`. | Do not promote or retry BUILDING4 high `{57,69}` as a standalone hand-coded group. Fireability is necessary but not sufficient; this lane needs scheduler-owned append timing or generated metadata that can change cadence without adding hot-code phase pressure. |
| 2026-05-06 | `ps1-debug-o2-v080` | Log-only; source reverted. | Continue the cold/default-off `-O2` audit queue after the v0.8.0 release. `src/ps1_debug.c` is debug-overlay code, so the binary question was whether returning it to default `-O2` could stay phase-clean or shrink enough to matter. | Temporarily removed only `src/ps1_debug.c` from the forced `-Os` source list, rebuilt first, and ran the focused FISHING1 high canary. An initial comparison used the stale pre-v0.8.0 building4 baseline; after regenerating `v080-current-fishing1-baseline`, the probe was rerun against the correct current baseline. Source was restored and rebuilt after the gate. | Failed/no promotion. Correct current-baseline rerun stayed exact-flat: FISHING1 `1069/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `loop_reads=20`, PS-EXE bucket `215040`, and tracked hot symbol addresses/sizes exact. The ELF still grew `954192 -> 954632` with no speed or work-volume win. Artifacts: stale comparison `scratch/ps1-perf-iterate/ps1-debug-o2-v080-probe/20260506-115605-3816866/summary.json`; current comparison `scratch/ps1-perf-iterate/ps1-debug-o2-v080-current-probe/20260506-120804-3885182/summary.json`; refreshed baseline `scratch/ps1-perf-iterate/v080-current-fishing1-baseline/20260506-120547-3872759/summary.json`. | Keep `src/ps1_debug.c` at whole-TU `-Os`. Default `-O2` is exact-flat plus code growth under the current phase, so do not retry unless debug overlay shape or link layout changes materially. |
| 2026-05-06 | `utils-o2-v080` | Log-only; source reverted. | Continue the cold/default-off `-O2` audit queue after `ps1_debug.c`. `src/utils.c` is tiny, so the binary question was whether default `-O2` could stay phase-clean enough to remove another forced `-Os` override. | Temporarily removed only `src/utils.c` from the forced `-Os` source list, rebuilt first, and ran the focused FISHING1 high canary. The initial comparison used the stale pre-v0.8.0 building4 baseline, then the probe was rerun against `v080-current-fishing1-baseline`. Source was restored and rebuilt after the gate. | Failed/no promotion. Correct current-baseline rerun stayed timing-flat: FISHING1 `1069/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `loop_reads=20`, and PS-EXE bucket `215040`. The ELF grew `954192 -> 955436`, and tracked hot symbols shifted by `+20` bytes with no speed or work-volume win. Artifacts: stale comparison `scratch/ps1-perf-iterate/utils-o2-v080-probe/20260506-120034-3842463/summary.json`; current comparison `scratch/ps1-perf-iterate/utils-o2-v080-current-probe/20260506-120935-3895004/summary.json`. | Keep `src/utils.c` at whole-TU `-Os`. Default `-O2` is exact-flat plus code growth and avoidable phase noise under the current baseline. |
| 2026-05-06 | `island-o2-v080` | Log-only; source reverted. | Finish the normal cold/default-off `-O2` audit queue with `src/island.c`. The binary question was whether island setup code could return to default `-O2` without perturbing the validated foreground playback phase. | Temporarily removed only `src/island.c` from the forced `-Os` source list, rebuilt first, and ran the focused FISHING1 high canary. The initial comparison used the stale pre-v0.8.0 building4 baseline, then the probe was rerun against `v080-current-fishing1-baseline`. Source was restored and rebuilt after the gate. | Failed/no promotion. Correct current-baseline rerun stayed timing-flat: FISHING1 `1069/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `loop_reads=20`, and PS-EXE bucket `215040`. The ELF grew `954192 -> 954492`, and tracked graphics/CD symbols shifted by `+48` bytes with no speed or work-volume win. Artifacts: stale comparison `scratch/ps1-perf-iterate/island-o2-v080-probe/20260506-120309-3857645/summary.json`; current comparison `scratch/ps1-perf-iterate/island-o2-v080-current-probe/20260506-121049-3901723/summary.json`. | Keep `src/island.c` at whole-TU `-Os`. Default `-O2` is exact-flat plus code growth and avoidable phase noise; the normal cold compiler-flag queue is exhausted, leaving only review-only/default-off surfaces unless the link layout changes materially. |
| 2026-05-06 | `ps1-pad-script-o2-v080` | Log-only; source reverted. | Drain the review-only `-O2` audit rows after the normal cold compiler-flag queue was exhausted. `src/ps1_pad_script.c` only drives scripted input/test paths, so the binary question was whether default `-O2` could shrink or stay completely inert. | Temporarily removed only `src/ps1_pad_script.c` from the forced `-Os` source list, rebuilt first, then ran the focused FISHING1 high canary against `v080-current-fishing1-baseline`. Source was restored and rebuilt after the gate. | Failed/no promotion. FISHING1 stayed exact-flat at `1069/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `loop_reads=20`, and PS-EXE bucket `215040`. The ELF grew `954192 -> 954248`, and tracked CD helper symbols shifted by `+36` bytes with no speed or work-volume win. Artifact: `scratch/ps1-perf-iterate/ps1-pad-script-o2-v080-probe/20260506-121502-3925782/summary.json`. | Keep `src/ps1_pad_script.c` at whole-TU `-Os`. Default `-O2` is exact-flat plus code growth and avoidable phase noise, so it should not be retried unless scripted-input code shape or link layout changes materially. |
| 2026-05-06 | `scene-freeplay-o2-v080` | Log-only; source reverted. | Drain the review-only `-O2` audit rows after the pad-script row failed. `src/scene_freeplay.c` is not active in the FISHING1 canary, so the binary gate was whether default `-O2` could stay layout-clean or shrink enough to matter. | Temporarily removed only `src/scene_freeplay.c` from the forced `-Os` source list, rebuilt first, then ran the focused FISHING1 high canary against `v080-current-fishing1-baseline`. Source was restored and rebuilt after the layout gate failed. | Failed/no promotion. The PS-EXE bucket grew `215040 -> 217088`, shifting `FISHING1.FG2` LBA `434 -> 435`; ELF grew `954192 -> 960236`. FISHING1 timing happened to stay flat at `1069/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, but the layout change is disqualifying with no compensating speed win. Artifact: `scratch/ps1-perf-iterate/scene-freeplay-o2-v080-probe/20260506-121822-3945227/summary.json`. | Keep `src/scene_freeplay.c` at whole-TU `-Os`. Default `-O2` is a broad layout/code-size regression; do not retry unless freeplay source shape or link padding changes materially. |
| 2026-05-06 | `scene-picker-o2-v080` | Log-only; source reverted. | Finish the review-only `-O2` audit rows with `src/scene_picker.c`. The binary gate was whether default `-O2` could reduce menu/selector code without affecting active scene playback. | Temporarily removed only `src/scene_picker.c` from the forced `-Os` source list, rebuilt first, then ran the focused FISHING1 high canary against `v080-current-fishing1-baseline`. Source was restored and rebuilt after the gate. | Failed/no promotion. FISHING1 stayed exact-flat at `1069/1073`, `blocking_vb=4`, `prefetch_overrun_vb=4`, `loop_reads=20`, PS-EXE bucket `215040`, and tracked hot symbols exact. The ELF grew `954192 -> 955976` with no speed or work-volume win. Artifact: `scratch/ps1-perf-iterate/scene-picker-o2-v080-probe/20260506-122113-3961896/summary.json`. | Keep `src/scene_picker.c` at whole-TU `-Os`. The `-O2` audit queue is now exhausted; next runtime work should move to generated metadata, scheduler-owned read timing, selective preprocessing, or targeted C/data-shape changes rather than more compiler-flag retests. |
| 2026-05-06 | `post-o2-baseline-fingerprints` | `perf: fingerprint baseline comparisons` | The v0.8.0 `-O2` sweep exposed a stale-baseline trap: exact-flat probes were initially compared to an older `1068/1074` FISHING1 row instead of the refreshed `1069/1073` canary. The hypothesis was that the perf harness should make stale source/layout baselines visible by default. | Added per-case `fingerprint` metadata to `scripts/ps1-perf-iterate.sh`: key timing/CD metrics, foreground pack LBA/sectors, PS-EXE bucket, ELF/map bytes, and hot symbol sizes. Baseline comparison now records fingerprint deltas and warns when a baseline lacks stored fingerprint metadata, lacks `run.git`, came from a dirty worktree, or was generated from a different commit. | Promoted as host-side tooling. `bash -n scripts/ps1-perf-iterate.sh` and `git diff --check` passed. No PS1 runtime bytes changed. | Keep. This does not count as a VBlank speed win, but it hardens the headless loop by making stale-baseline comparisons explicit before a false rejection or false promotion is logged. |
| 2026-05-06 | `read-plan-segment-metadata-v2` | `perf: include read segment metadata in plans` | After the `-O2` queue was exhausted, the next bottleneck moved to generated read metadata. Existing read-plan rows listed source read indices, but not the concrete observed segments needed to generate runtime metadata or audit append-start ownership without rereading DuckStation logs. | Extended `scripts/ps1-foreground-read-plan.py` candidate rows with `source_read_segments`, `touched_read_segments`, and `append_start_read_segments`. Smoked it on FISHING1 high with `scratch/ps1-perf-iterate/v080-current-fishing1-baseline/20260506-120547-3872759/summary.json`, producing `scratch/ps1-perf-iterate/read-plan-metadata-smoke.json`. | Promoted as host-side tooling. `python3 -m py_compile scripts/ps1-foreground-read-plan.py` passed, and the smoke JSON now embeds read lines, LBAs, file-sector ranges, inferred sectors, and timing gaps for candidate rows. No PS1 runtime bytes changed. | Keep. Future grouped-read or scheduler work should consume these self-contained plan rows instead of hand-transcribing sector ranges from logs. |
| 2026-05-06 | `read-plan-runtime-fit-v3` | Host-side tooling; no runtime promotion. | VISITOR3 high/low still dominate the residual gap, but previous standalone groups either no-op or regress. The binary question for the planner was whether it can distinguish candidates that the current `FG_PREFETCH_GROUP_WINDOW_BYTES` path can actually run from candidates that require scheduler or larger-window metadata. | Added runtime-fit metadata to `scripts/ps1-foreground-read-plan.py`: `runtime_group_capacity_sectors`, `runtime_current_group_fit`, and `runtime_metadata_class`. Smoked it on current VISITOR3 high against `scratch/ps1-perf-iterate/visitor3-prefetch-relief-exception/20260505-225129-3605646/summary.json`. | Promoted as host-side planner improvement. VISITOR3 high now marks 12-sector fireable candidates such as `163..175` as `current-read-group-compatible`, while higher-value 16/24-sector candidates such as `170..186` and `163..187` are correctly classified as `needs-scheduler-or-larger-group-window`. Artifact: `scratch/ps1-perf-iterate/visitor3-current-high-readplan-v3.json`. | Keep. Do not treat larger VISITOR3 groups as promotable by hand-coded tables under the current 13-sector group capacity; use this classifier to route them to scheduler-owned read timing or generated metadata work. |
| 2026-05-06 | `read-plan-visible-cd-class-v4` | Host-side tooling; no runtime promotion. | After runtime-fit classification, the next risk was choosing candidates that fit mechanically but repeat historical tight-cluster regressions. The binary question for the planner was whether it can label visible-CD risk in a durable way before any runtime source edit. | Added `visible_cd_cost_class`, `first_gap_slack_class`, `internal_gap_slack_class`, and `read_seek_direction` to `scripts/ps1-foreground-read-plan.py`. Smoked it on current VISITOR3 high against `scratch/ps1-perf-iterate/visitor3-prefetch-relief-exception/20260505-225129-3605646/summary.json`, producing `scratch/ps1-perf-iterate/visitor3-current-high-readplan-v4.json`. | Promoted as host-side planner improvement. VISITOR3 high now distinguishes the 12-sector `163..175` candidate as `safe:long-visible-gap` and current-fit, the 16-sector `170..186` candidate as `balanced:validate-overlap` but `needs-scheduler-or-larger-group-window`, and tight 16/24-sector clusters such as `102..118`, `128..144`, and `114..138` as `risky:short-visible-gap` or `unsafe:tight-visible-gap`. No PS1 runtime bytes changed. | Keep. Future generated read metadata should filter or heavily penalize `unsafe:tight-visible-gap` candidates; only long-gap or balanced candidates should advance to a same-baseline runtime probe. |
| 2026-05-06 | `visitor3-high-group170-186-v080` | Log-only; source reverted. | Use the new runtime-fit and visible-CD classifiers to test VISITOR3 high's best balanced larger candidate, `{170,186}`, by temporarily raising `FG_PREFETCH_GROUP_WINDOW_BYTES` from `13` to `16` sectors and adding the group to the high-tide VISITOR3 table. | First refreshed a same-HEAD VISITOR3 high baseline at `scratch/ps1-perf-iterate/visitor3-current-v080-baseline/20260506-124922-4117894/summary.json`, then ran VISITOR3 high with `--require-improvement --allow-regression 0`. Because the local row passed, started grouped-scene canaries and stopped after the first canary disqualified the source shape. Source was restored after the failed canary. | Failed/no promotion. VISITOR3 high improved locally (`loop_vb 1456 -> 1455`, `overrun_vb 446 -> 445`, `blocking_vb 363 -> 361`, `loop_reads 53 -> 52`) with fixed PS-EXE bucket and pack LBA. However, the FISHING1 high canary regressed under the same source shape: compared to the v0.8.0 baseline `1069/1073`, the partial canary reported `target_vb 1072`, `blocking_vb 5`, and `prefetch_overrun_vb 6` with `jcreborn.elf 954192 -> 954284` and `foregroundPilotPlay 10992 -> 11000`. Artifacts: local VISITOR3 pass `scratch/ps1-perf-iterate/visitor3-high-group170-186-v080-probe/20260506-125038-4125539/summary.json`; disqualifying FISHING1 canary `scratch/ps1-perf-iterate/visitor3-group170-186-v080-canaries/20260506-125209-4134517/fishing1-high-canary/perf-summary.json`. | Do not promote this as a global capacity/table source change. The candidate is real, but it needs scene-local/generated metadata or padding that avoids changing FISHING1 phase; do not retry by simply raising the global group window or adding another hand-coded hot table row. |
| 2026-05-06 | `visitor3-high-group170-186-v080-current` | `ps1: add visitor3 high 170 read group` | Correct the stale-canary rejection above by proving the candidate against a clean same-commit control. The binary question was whether the FISHING1 and broader canary deltas were caused by the VISITOR3 source change or by current baseline drift after the fingerprint/tooling commits. | Reapplied the `16`-sector retained group capacity plus VISITOR3 high `{170,186}` group, reran VISITOR3 high against `scratch/ps1-perf-iterate/visitor3-current-v080-baseline/20260506-124922-4117894/summary.json`, then ran VISITOR3 low, FISHING1 high, FISHING3 high/low, BUILDING2 low, ACTIVITY9 low, and WALKSTUF1 high canaries. Restored clean source and reran the same canary set as a control. | Promoted. VISITOR3 high improves current same-HEAD metrics `loop_vb 1456 -> 1455`, `overrun_vb 446 -> 445`, `blocking_vb 363 -> 361`, and `loop_reads 53 -> 52` with fixed PS-EXE bucket and pack LBA. The patch canaries matched the clean-source control on loop timing for VISITOR3 low (`1455/1010`), FISHING1 (`1069/1072`), FISHING3 high/low (`1962/1950`, `1957/1955`), BUILDING2 low (`1465/1276`), ACTIVITY9 low (`2098/2056`), and WALKSTUF1 high (`1640/1406`). Artifacts: accepted VISITOR3 run `scratch/ps1-perf-iterate/visitor3-high-group170-186-v080-current-probe/20260506-130521-17368/summary.json`; patched canaries `scratch/ps1-perf-iterate/visitor3-group170-186-v080-current-canaries-built/20260506-130818-34542/summary.json`; clean control `scratch/ps1-perf-iterate/visitor3-group170-186-v080-clean-canary-compare/20260506-131652-83639/summary.json`. | Keep. This is a small but real VISITOR3 high CD-pressure win and a process correction: current-source control canaries are required when fingerprint warnings show commit/layout drift. The headline battle-card average remains `0.8692%` over target / `99.4529%` target speed because the matrix already carried the `1455/1010` loop row, but VISITOR3 high blocking drops `363 -> 361` and active loop reads drop `53 -> 52`. |
| 2026-05-06 | `visitor3-low-group170-186-v080` | Log-only; source reverted. | After accepting the 16-sector retained window for VISITOR3 high, test the read-plan's low-tide `170..186` candidate, which was compatible and balanced but only promised one saved read. | Temporarily added a VISITOR3 low read-group table `{170,186}` and routed it only for low tide, then ran VISITOR3 low against the just-built current canary baseline. Source was restored after the failed gate. | Failed/no promotion. The group reduced nominal loop reads `51 -> 50`, but it regressed visible timing: `loop_vb 1455 -> 1458`, `overrun_vb 445 -> 448`, and `blocking_vb 366 -> 369`; `prefetch_overrun_vb` stayed `22`. The PS-EXE bucket and pack LBA stayed fixed, so this is a cadence/read-timing loss rather than a broad layout issue. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group170-186-v080-probe/20260506-133157-169526/summary.json`. | Do not promote or retry VISITOR3 low `170..186` as a hand-coded group. One saved read is not enough to offset the larger retained append cadence; low-tide VISITOR3 needs scheduler-owned timing, setup segmentation, or upload-ready/selective preprocessing instead. |
| 2026-05-06 | `visitor3-prepare-slack-ge4-v080` | Log-only; source reverted. | Test whether the prepared-visual scheduler can use any held slice with at least `4` VBlanks of slack instead of only the exact `4`-VBlank slot. The binary question was whether broader prepare eligibility hides more visual work after the accepted VISITOR3 high `170..186` read group without stealing CD cadence. | Temporarily changed `fgRuntimeCanPrepareStagedFrame()` from `heldSlack == FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` to `heldSlack >= FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS`, rebuilt, then ran VISITOR3 high and low with `--require-improvement --allow-regression 0` against the current accepted VISITOR3 high baseline. Source was restored after the no-improvement gate failed. | Failed/no promotion. VISITOR3 high stayed exact-flat at `1455/1010`, `blocking_vb=361`, `prefetch_overrun_vb=21`, `due_misses=31`, and `loop_reads=52`; VISITOR3 low stayed current-shape flat at `1455/1010`, `blocking_vb=366`, `prefetch_overrun_vb=22`, `due_misses=32`, and `loop_reads=51`. Artifact: `scratch/ps1-perf-iterate/visitor3-prepare-slack-ge4-v080-probe/20260506-133534-190260/summary.json`. | Do not promote threshold-only prepared-visual slack broadening. The local guard is not the remaining VISITOR3 bottleneck; retry only with an explicit CD-first scheduler budget, generated append ownership metadata, or separate prepared visual storage that proves it does not perturb read cadence. |
| 2026-05-06 | `preprocess-matrix-rect-pressure-v080` | Host-side tooling; no runtime promotion. | The next VISITOR3 lane is selective upload-ready preprocessing, but the prior matrix ranked byte savings without enough rect/cap-pressure evidence. The binary question was whether the planning sheet can distinguish blanket upload-ready candidates from rows that need selective banding before runtime work. | Extended `scripts/ps1-fg-preprocess-matrix.py` with x-band rect totals, cap-hit counts, max x-band bytes/rects, rects-per-frame pressure, exact-upload interval totals, and cap-pressure notes. Regenerated `docs/ps1/performance-preprocess-opportunities.csv` and `.md` from the current 126-row scene matrix. | Promoted as host-side planning tooling. The measured timing average remains `0.8692%` over target / `99.4529%` target speed. VISITOR3 remains the top upload-ready target (`56.97%` estimated align4 upload saving and `229.28%` payload growth), but both high and low now report `3` x-band cap hits, so the next VISITOR3 graphics probe must be selective/thresholded rather than a blanket upload-ready conversion. | Keep. Use the new rect/cap columns before any generated upload-ready pack experiment; byte savings alone are not enough when cap hits or rect pressure can recreate the old upload-rect regressions. |
| 2026-05-06 | `preprocess-detail-fgp3-hotspots-v080` | Host-side tooling; no runtime promotion. | The matrix proved VISITOR3 needs selective upload-ready preprocessing, but it did not identify which frames caused cap pressure versus which frames could profit from x-band data. The binary question was whether the existing per-pack analyzer could be made FGP3-aware and frame-specific without touching PS1 runtime bytes. | Extended `scripts/analyze-fg2-preprocess-plans.py` to accept `FGP3`, parse cleanup rows separately from draw rows, align upload-band modeling to the runtime zero-gap band splitter, emit `frame_cap_hotspots` plus `frame_saving_hotspots`, and write an optional per-frame CSV with a default thresholded selective plan. Smoked it on `generated/ps1/foreground/VISITOR3.FG2` and `FISHING1.FG2`. Artifacts: `scratch/ps1-perf-iterate/visitor3-preprocess-detail-v080.json`; `docs/ps1/performance-preprocess-visitor3-hotspots.csv`. | Promoted as host-side planning tooling. The VISITOR3 detail matches the matrix shape (`3` x-band cap hits, `56.97%` align4 saving). It identifies frames `134..136` / source `181..183` as cap-hit frames where blanket x-band saves `0%`, while source frames around `168..180` are high-savings non-cap candidates. The default threshold plan selects `96 / 144` frames, excludes `3` cap-hit frames, and estimates `6114568` selected-subset upload bytes saved. No runtime timing or matrix average changed: still `0.8692%` over target / `99.4529%` target speed. | Keep. The next VISITOR3 graphics experiment should store upload-ready bands selectively for the selected non-cap frames first, leaving the cap-hit crash frames on the current full-width path unless a new format avoids the rect cap. |
| 2026-05-06 | `visitor3-high-group144-156-v080` | Log-only; source reverted. | Test the best remaining current-fit VISITOR3 high standalone read group after accepting `170..186`. The planner ranked `{144,156}` as a one-read low-visible-risk candidate with actual append-start ownership, so the binary question was whether one more grouped append could reduce blocking without stealing cadence. | Temporarily added `{144,156}` to the VISITOR3 high read-group table, rebuilt, and ran VISITOR3 high with a case-local CD against `scratch/ps1-perf-iterate/visitor3-high-group170-186-v080-current-probe/20260506-130521-17368/summary.json` using `--require-improvement --allow-regression 0`. Source was restored after the gate failed. | Failed/no promotion. The group reduced nominal loop reads `52 -> 51`, but visibly regressed the same metrics we are optimizing: `loop_vb 1455 -> 1459`, `overrun_vb 445 -> 448`, `blocking_vb 361 -> 367`, and `prefetch_overrun_vb 21 -> 23`. The PS-EXE bucket and pack LBA stayed fixed, so this is a pure cadence/read-timing trap rather than a layout issue. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group144-156-v080-probe/20260506-141827-428275/summary.json`. | Do not promote or retry VISITOR3 high `144..156` as a hand-coded group. It proves current-fit append ownership is still insufficient; continue VISITOR3 through scheduler-owned timing or selective graphics preprocessing rather than more one-off high-tide read groups. |
| 2026-05-06 | `visitor3-narrow-upload-v080` | Log-only; source reverted. | Test a very strict VISITOR3-only x-aware dirty upload path after the FGP3 hotspot analyzer showed 21 non-cap frames with at least `88%` estimated x-band savings and max per-frame scratch under `7 KB`. The binary question was whether a gated runtime copy-to-scratch path could reduce upload work without reviving earlier broad x-aware upload regressions. | Temporarily added `grSetNarrowDirtyUploadEnabled()` and a VISITOR3-only `grDrawBackground()` branch that packed narrow per-band row extents into an `8 KB` static scratch buffer only when the whole frame saved at least `32768` bytes and kept narrow bytes at or below `12%` of full-band bytes. Built and ran VISITOR3 high with a case-local CD against `scratch/ps1-perf-iterate/visitor3-high-group170-186-v080-current-probe/20260506-130521-17368/summary.json` using `--require-improvement --allow-regression 0`. Source was restored after the gate failed. | Failed/no promotion. VISITOR3 high regressed `loop_vb 1455 -> 1461`, `overrun_vb 445 -> 451`, `blocking_vb 361 -> 367`, and `prefetch_overrun_vb 21 -> 22`; `loop_reads` stayed `52`. The PS-EXE bucket and pack LBA stayed fixed, but `grDrawBackground` grew by `1084` bytes and the scratch-copy cost perturbed the CD/present cadence. Artifact: `scratch/ps1-perf-iterate/visitor3-narrow-upload-v080-probe/20260506-142435-462825/summary.json`. | Do not promote runtime scratch-packed x-aware upload, even when VISITOR3-gated and aggressively thresholded. The remaining VISITOR3 graphics path needs pack/data-shape work that avoids per-frame scratch packing, or scheduler ownership that hides the copy cost explicitly. |
| 2026-05-06 | `visitor3-high-group102-118-v080` | Log-only; source reverted. | Test the highest-scoring remaining current-fit VISITOR3 high group after rejecting `144..156`. The planner ranked `{102,118}` as fireable/current-compatible and it covered three reads with two nominal saves, so this was the binary check for whether any more hand-coded current-fit VISITOR3 high groups are still worth probing. | Temporarily added `{102,118}` to the VISITOR3 high read-group table, rebuilt, and ran VISITOR3 high with a case-local CD against `scratch/ps1-perf-iterate/visitor3-high-group170-186-v080-current-probe/20260506-130521-17368/summary.json` using `--require-improvement --allow-regression 0`. Source was restored after the gate failed. | Failed/no promotion. The group reduced nominal loop reads `52 -> 50`, but regressed visible timing: `loop_vb 1455 -> 1457`, `overrun_vb 445 -> 446`, `blocking_vb 361 -> 366`, and `prefetch_overrun_vb 21 -> 24`; target also moved `1010 -> 1011`. The PS-EXE bucket and pack LBA stayed fixed. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group102-118-v080-probe/20260506-143242-507946/summary.json`. | Do not promote or retry adjacent VISITOR3 high current-fit read groups under the current scheduler. Even a two-read nominal save loses to cadence/visible-pressure cost, so the next VISITOR3 work must change scheduler ownership or pack/data shape first. |
| 2026-05-06 | `visitor3-high-perf-detail-v080` | Log-only diagnostic; no source change. | After the remaining current-fit VISITOR3 hand groups regressed, run a detail pass to identify whether the residual gap is still primarily CD-bound or whether upload/compose is the next local bottleneck. | Ran VISITOR3 high with `perf-detail` and a case-local CD from the clean current source: `./scripts/ps1-perf-iterate.sh --case "visitor3-high-detail::fgpilot visitor3 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-detail noloop seed 1" --frames 7200 --timeout 240 --case-local-cd --output scratch/ps1-perf-iterate/visitor3-high-perf-detail-v080`. | Diagnostic only. The run reports `loop_vb=1456`, `target_vb=1010`, `overrun_vb=446`, `blocking_vb=364`, `prefetch_overrun_vb=23`, `loop_reads=52`, and `loop_read_vb=442`. Pipeline detail shows VISITOR3 is still CD-first (`blocking_vb=364`, `loop_read_vb=442`) but has a non-trivial render lane (`render_vb=348`, `compose_vb=191`, `advance_vb=342`) while upload is small (`upload_vb=11`). Scheduler counters show `cd_stage=108`, `cd_window=10`, `visual_prepare=28`, `wait=530`, `prepared_used=28`, and `prepared_wasted=0`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-perf-detail-v080/20260506-143620-528426/summary.json`. | Use this as the VISITOR3 battle-card baseline. Do not spend more time on current-fit hand read groups. The next VISITOR3 runtime/data probe needs scheduler-owned CD timing, generated append/setup metadata, or pack data-shape work that reduces compose/advance without runtime scratch packing. |
| 2026-05-06 | `walkstuf1-high-group429-441-v080` | Log-only; source reverted. | After VISITOR3 current-fit groups were closed, test the top current WALKSTUF1 high 12-sector candidate that was fireable/current-compatible in the same-source read plan. The binary question was whether a tight-gap grouped append can still help WALKSTUF1 after the v0.8.0 validated pack updates. | Captured a same-source WALKSTUF1 high/low baseline at `scratch/ps1-perf-iterate/walkstuf1-current-v080-baseline/20260506-144418-572369/summary.json`, temporarily added a high-tide `walkstuf1` read group `{429,441}`, rebuilt, and ran WALKSTUF1 high with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the gate failed. | Failed/no promotion. WALKSTUF1 high stayed exact-flat at `loop_vb=1640`, `target_vb=1406`, `overrun_vb=234`, `blocking_vb=318`, `prefetch_overrun_vb=67`, `loop_reads=144`, and `due_misses=58`. Layout stayed fixed (`pack_lba=24744`, PS-EXE bucket `215040`), but the source table shifted hot symbols by `+156` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-group429-441-v080-probe/20260506-144710-588892/summary.json`. | Do not promote or retry WALKSTUF1 high `429..441` as a standalone table. The current WALKSTUF1 candidates are mostly `unsafe:tight-visible-gap`; future WALKSTUF1 work should use scheduler-owned grouping or selective indexed8/data-shape preprocessing, not more one-off table rows. |
| 2026-05-06 | `building2-low-group603-619-v080` | Log-only; source reverted. | Test BUILDING2 low's strongest fresh current-fit 16-sector read candidate after BUILDING4 and WALKSTUF1 standalone plans looked weak. The candidate was fireable and covered four reads, so it was the binary check for whether BUILDING2 low still has standalone read-group headroom. | Captured a same-source BUILDING2 low baseline at `scratch/ps1-perf-iterate/building2-low-current-v080-baseline/20260506-145117-611888/summary.json`, temporarily added `{603,619}` beside the accepted low-tide `{318,330}` group, rebuilt, and ran BUILDING2 low with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the gate failed. | Failed/no promotion. The group reduced loop reads `87 -> 84` but regressed user-facing timing: `loop_vb 1465 -> 1469`, `overrun_vb 189 -> 192`, with `blocking_vb=334` and `prefetch_overrun_vb=35` unchanged. Layout stayed fixed (`pack_lba=6817`, PS-EXE bucket `215040`). Artifact: `scratch/ps1-perf-iterate/building2-low-group603-619-v080-probe/20260506-145241-620124/summary.json`. | Do not promote or retry BUILDING2 low `603..619` as a standalone table. The read savings are real, but cadence cost consumes them; BUILDING2 needs scheduler-owned grouping or selective preprocessing, not another hand-coded tight cluster. |
| 2026-05-06 | `building2-low-group538-550-v080` | Log-only; source reverted. | Test the lower-risk BUILDING2 low 12-sector candidate after `603..619` failed. Unlike the tight tail cluster, `{538,550}` was current-fit, fireable, and classified as `balanced:medium-visible-gap`, so it was the final standalone BUILDING2 low read-table check before moving to scheduler-owned grouping. | Temporarily added `{538,550}` beside the accepted low-tide `{318,330}` group, rebuilt, and ran BUILDING2 low against `scratch/ps1-perf-iterate/building2-low-current-v080-baseline/20260506-145117-611888/summary.json` with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion under the strict gate. The probe improved primary timing (`loop_vb 1465 -> 1461`, `overrun_vb 189 -> 185`, `blocking_vb 334 -> 328`) with fixed layout, but regressed hidden refill (`prefetch_overrun_vb 35 -> 40`). Artifact: `scratch/ps1-perf-iterate/building2-low-group538-550-v080-probe/20260506-145549-638079/summary.json`. | Do not promote as a standalone table because it violates the no-regression baseline. This is a useful scheduler candidate: retry only if the scheduler can reserve/absorb the extra refill work while preserving the visible loop and blocking gains. |
| 2026-05-06 | `read-plan-scheduler-retry-class-v080` | Host-side tooling; no runtime promotion. | The BUILDING2 `538..550` probe exposed a candidate class the planner did not name: current-fit groups that may improve visible timing but fail strict promotion because hidden refill/overlap regresses. The binary question was whether the read-plan output can route those candidates away from standalone source tables before the runtime is edited. | Added `scheduler_retry_class` and `scheduler_retry_note` to `scripts/ps1-foreground-read-plan.py`, then smoked it with `python3 -m py_compile`, BUILDING2 low, and VISITOR3 high read plans. | Promoted as host-side tooling. BUILDING2 low now labels `{538,550}` and `{531,543}` as `scheduler-owned-candidate`, while the rejected `{603,619}` tight cluster is `high-risk:scheduler-only`. VISITOR3 high labels previously rejected current-fit candidates like `{144,156}` and `{102,118}` as scheduler-owned rather than standalone-safe. No PS1 runtime bytes or speed metrics changed. | Keep. Future read-group work should only test `standalone-candidate` rows as direct source tables; `scheduler-owned-candidate` rows need a scheduler/refill budget first, and `high-risk:scheduler-only` rows should not be hand-coded. |
| 2026-05-06 | `visitor3-high-group144-156-slack6-v080` | Log-only; source reverted. | Test whether a minimal scheduler-owned read-group shape can make the rejected VISITOR3 high `144..156` append safe by requiring at least `6` held VBlanks before the grouped append is allowed to extend the window. | Temporarily added a per-group `minSlackVBlanks` field, kept all accepted groups at `0`, added VISITOR3 high `{144,156,6}`, rebuilt, and ran VISITOR3 high against `scratch/ps1-perf-iterate/visitor3-high-group170-186-v080-current-probe/20260506-130521-17368/summary.json` with `--require-improvement --allow-regression 0`. Source was restored and rebuilt after the gate failed. | Failed/no promotion. The run stayed exact-flat on key metrics: `loop_vb=1455`, `target_vb=1010`, `overrun_vb=445`, `blocking_vb=361`, `prefetch_overrun_vb=21`, and `loop_reads=52`. The PS-EXE bucket and foreground LBA stayed fixed; hot foreground symbols shifted by `+24` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group144-156-slack6-v080-probe/20260506-150402-684451/summary.json`. | Do not promote or retry local min-slack gating as the scheduler-owned implementation. It proves the remaining VISITOR3 group candidates need first-class scheduler budget/ownership metadata or data-shape changes, not another per-table guard around the existing append path. |
| 2026-05-06 | `visitor3-setup-prime-192k-v080` | `ps1: prime visitor3 setup window deeper` | After the scheduler-owned slack guard no-oped, test a scene-local setup-prime residency cap instead of broadening global read grouping. VISITOR3 high and low still had visible CD pressure, but raising the global cap would risk unrelated scenes, so the binary check was whether only VISITOR3 could safely keep more setup-prime data resident. | Added `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES` at `192 KiB`, left the global `FG_SETUP_PRIME_MAX_RESIDENT_BYTES` at `128 KiB`, rebuilt, ran VISITOR3 high with strict same-baseline improvement, then gated VISITOR3 low plus FISHING1 high, BUILDING2 low, and WALKSTUF1 high canaries. Updated read-plan tooling so setup coverage reports the VISITOR3 cap correctly as `prime=[1, 97]`. | Promoted. VISITOR3 high improves `loop_vb 1455 -> 1450`, `target_vb 1010 -> 1015`, `overrun_vb 445 -> 435`, `blocking_vb 361 -> 355`, `prefetch_overrun_vb 21 -> 14`, `loop_reads 52 -> 45`, and `loop_read_vb 438 -> 412`. VISITOR3 low improves overrun `444 -> 441`, blocking `365 -> 362`, prefetch overrun `23 -> 19`, loop reads `51 -> 50`, and target `1009 -> 1012`. Canaries stayed within accepted shapes: FISHING1 high `1069/1072`, BUILDING2 low `1465/1276`, and WALKSTUF1 high `1640/1406`. Artifacts: high pass `scratch/ps1-perf-iterate/visitor3-high-setup-prime-192k-v080-probe/20260506-150907-712870/summary.json`; canaries/promoted matrix source `scratch/ps1-perf-iterate/visitor3-setup-prime-192k-v080-canaries/20260506-151017-720001/summary.json`. | Keep. This is scene-local VISITOR3 setup-prime residency, not a global cap raise. The matrix rollup moves from `0.8692%` to `0.8556%` over target and from `99.4529%` to `99.4595%` target speed. Remaining VISITOR3 work should target scheduler-owned CD timing or selective pack/data-shape preprocessing, not more one-off current-fit read groups. |
| 2026-05-06 | `visitor3-high-setup-prime-196k-v080` | Log-only; source reverted. | Final narrow cap bisection after `200 KiB` and `208 KiB` failed. The binary question was whether the accepted `192 KiB` cap had a small safe margin at `196 KiB`. | Temporarily changed `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES` from `192 KiB` to `196 KiB`, rebuilt, and ran VISITOR3 high with a case-local CD against the accepted 192 KiB baseline using `--require-improvement --allow-regression 0`. Source was restored after the strict gate failed. | Failed/no promotion. This was worse than both larger edge probes: `loop_vb 1450 -> 1454`, `overrun_vb 435 -> 440`, `blocking_vb 355 -> 356`, `prefetch_overrun_vb 14 -> 17`, and `loop_reads 45 -> 46`. Setup coverage moved to `prime=[1, 99]`, proving the cap took effect. Artifact: `scratch/ps1-perf-iterate/visitor3-high-setup-prime-196k-v080-probe/20260506-153057-836782/summary.json`. | Close VISITOR3 setup-prime cap bisection at `192 KiB`. Additional cap increases are cadence-negative under the current scheduler; move to scheduler-owned CD timing or selective pack/data-shape preprocessing. |
| 2026-05-06 | `visitor3-high-setup-prime-200k-v080` | Log-only; source reverted. | Bisect the failed `208 KiB` setup-prime cap against the accepted `192 KiB` VISITOR3 baseline. The binary question was whether a smaller increase could retain the blocking benefit without shifting visible loop cadence. | Temporarily changed `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES` from `192 KiB` to `200 KiB`, rebuilt, and ran VISITOR3 high with a case-local CD against the accepted 192 KiB baseline using `--require-improvement --allow-regression 0`. Source was restored after the strict gate failed. | Failed/no promotion. Blocking improved `355 -> 353`, but primary timing regressed: `loop_vb 1450 -> 1452` and `overrun_vb 435 -> 437`; `prefetch_overrun_vb` stayed `14`, `loop_reads` rose `45 -> 46`, and setup coverage moved to `prime=[1, 101]`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-setup-prime-200k-v080-probe/20260506-152846-823891/summary.json`. | Do not promote `200 KiB`. The cap knee is at or very near `192 KiB` under the strict high-tide gate; further cap bisection should be lower-risk only if it tests `196 KiB` specifically, otherwise move to scheduler-owned CD timing or selective pack/data-shape preprocessing. |
| 2026-05-06 | `visitor3-high-setup-prime-208k-v080` | Log-only; source reverted. | Test the next larger scene-local VISITOR3 setup-prime resident cap after `192 KiB` promoted. The binary question was whether keeping `208 KiB` resident would remove more active-loop reads without crossing the cadence/memory knee. | Temporarily changed `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES` from `192 KiB` to `208 KiB`, rebuilt, and ran VISITOR3 high with a case-local CD against the accepted 192 KiB baseline using `--require-improvement --allow-regression 0`. Source was restored after the strict gate failed. | Failed/no promotion. The larger cap kept `loop_vb=1450` and improved `prefetch_overrun_vb 14 -> 13`, but regressed primary gate metrics: `target_vb 1015 -> 1014`, `overrun_vb 435 -> 436`, and `blocking_vb 355 -> 359`; `loop_reads` rose `45 -> 49`. Read-plan setup coverage moved to `prime=[1, 105]`, proving the cap took effect. Artifact: `scratch/ps1-perf-iterate/visitor3-high-setup-prime-208k-v080-probe/20260506-152620-809632/summary.json`. | Do not promote `208 KiB`. The VISITOR3 setup-prime knee is below this point under the current scheduler; if probing further, bisect inside `192..208 KiB` with the same strict VISITOR3 high baseline before any canary expansion. |
| 2026-05-06 | `visitor3-high-group158-170-post192-v080` | Log-only; source reverted. | After accepting the `192 KiB` setup-prime cap, test the highest-ranked remaining VISITOR3 high current-fit group `{158,170}`. The read-plan classified it as scheduler-owned but low-visible-risk, so the binary check was whether the stronger setup-prime baseline made standalone append timing safe. | Temporarily added `{158,170}` to `kVisitor3HighReadGroups12`, rebuilt, and ran VISITOR3 high with a case-local CD against the accepted 192 KiB setup-prime baseline using `--require-improvement --allow-regression 0`. Source was restored after the strict gate failed. | Failed/no promotion. The group saved one nominal read (`loop_reads 45 -> 44`) but regressed every visible gate: `loop_vb 1450 -> 1453`, `overrun_vb 435 -> 438`, `blocking_vb 355 -> 357`, and `prefetch_overrun_vb 14 -> 16`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group158-170-post192-v080-probe/20260506-153425-856815/summary.json`. | Do not promote or retry `{158,170}` as a standalone table. This validates the planner's scheduler-owned warning after the setup-prime win: saved reads still lose if append cadence is not owned by the scheduler. Move to generated scheduler/refill budget or selective pack/data-shape work. |
| 2026-05-06 | `visitor3-high-setup-segment158-170-v080` | Log-only; source reverted. | Test whether the rejected VISITOR3 high `{158,170}` cluster becomes safe when moved out of active playback into a setup-owned retained segment. The hypothesis was that a 24 KiB setup read could satisfy the later yacht-photo entries without spending held active-loop slack. | Temporarily added a VISITOR3 high setup segment for sectors `158..170`, allocated it in the setup-segment buffer, retained it across multiple copies, rebuilt, and ran VISITOR3 high against the accepted 192 KiB setup-prime baseline using `--require-improvement --allow-regression 0`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. The extra segment kept layout fixed but regressed primary timing: `loop_vb 1450 -> 1451`, `target_vb 1015 -> 1013`, `overrun_vb 435 -> 438`, and `prefetch_overrun_vb 14 -> 19`; `blocking_vb` stayed `355`, `loop_reads` stayed `45`, and no key metric improved. Artifact: `scratch/ps1-perf-iterate/visitor3-high-setup-segment158-170-v080-probe/20260506-154159-899713/summary.json`. | Do not promote or retry VISITOR3 setup segments as a local fix. Moving this cluster into setup still perturbs cadence and does not reduce active read count under the current window path; remaining VISITOR3 work should use generated scheduler ownership or pack/data-shape preprocessing. |
| 2026-05-06 | `visitor3-high-group146-170-post192-v080` | Log-only; source reverted. | Test the highest-ranked post-192 VISITOR3 high 24-sector generated/larger-window candidate. The read-plan said `{146,170}` could save three reads but did not fit the normal 16-sector grouped capacity; because the accepted 192 KiB setup-prime allocation already leaves a larger stream buffer resident, the binary question was whether adding the group alone could exploit that capacity without new heap. | Temporarily added `{146,170}` to `kVisitor3HighReadGroups12`, rebuilt, and ran VISITOR3 high against the accepted 192 KiB setup-prime baseline using `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. The group reduced nominal reads `45 -> 42`, but visibly regressed timing: `loop_vb 1450 -> 1456`, `overrun_vb 435 -> 439`, `blocking_vb 355 -> 358`, and `prefetch_overrun_vb 14 -> 18`; target rose `1015 -> 1017`, so the saved reads still lost to cadence. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group146-170-post192-v080-probe/20260506-154754-933395/summary.json`. | Do not promote or retry 24-sector VISITOR3 high groups through the current append path. Larger generated groups need a scheduler budget/ownership change that can absorb the refill, not just more retained window capacity. |
| 2026-05-06 | `walkstuf1-fgp2-setup-prime-v080` | `ps1: prime walkstuf1 fgp2 setup window` | After VISITOR3 direct knobs were closed, WALKSTUF1 became the next current outlier. The existing WALKSTUF1 setup-prime formula only applied to indexed8 temporal-residual packs, but the validated v0.8.0 WALKSTUF1 packs are FGP2/PAL4, so the binary check was whether applying the same scene-local setup-prime coverage to PAL4 could reduce active-loop CD pressure without a global cap raise. | Removed the indexed8-only guard from the WALKSTUF1 setup-prime policy, rebuilt, ran high and low tide against the current v0.8.0 WALKSTUF1 baseline with fixed layout, then ran FISHING1 high, VISITOR3 low, and BUILDING2 low as exact-flat canaries. Updated `scripts/ps1-foreground-read-plan.py` so PAL4 WALKSTUF1 setup coverage reports `auto:walkstuf1-*` instead of `none`, refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `loop_vb 1640 -> 1595`, `target_vb 1406 -> 1403`, `overrun_vb 234 -> 192`, `blocking_vb 318 -> 278`, `prefetch_overrun_vb 67 -> 50`, `loop_reads 144 -> 136`, and `loop_read_vb 663 -> 601`. Low improves `loop_vb 1631 -> 1614`, `blocking_vb 296 -> 276`, `prefetch_overrun_vb 60 -> 59`, `loop_reads 147 -> 134`, `loop_read_vb 660 -> 605`, and `due_misses 55 -> 49`, with overrun flat at `217` after target accounting moved. Canaries stayed exact-flat on key metrics: FISHING1 high `1069/1072`, VISITOR3 low `1453/1012`, and BUILDING2 low `1465/1276`. Artifacts: high `scratch/ps1-perf-iterate/walkstuf1-fgp2-setup-prime-v080-regate/20260506-155523-975774/summary.json`, low `scratch/ps1-perf-iterate/walkstuf1-fgp2-setup-prime-v080-low/20260506-155638-982964/summary.json`, canaries `scratch/ps1-perf-iterate/walkstuf1-fgp2-setup-prime-v080-canaries/20260506-155805-991366/summary.json`. | Keep as a WALKSTUF1-local PAL4 setup-prime recovery, not a global policy. Matrix rollup moves from `0.8556%` to `0.8318%` over target and from `99.4595%` to `99.4775%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.6` percentage points of over-target gap and added about `12.4` target-speed points. Next targets are VISITOR3 scheduler/data-shape work, BUILDING4/B2 CD pressure, and selective preprocessing; avoid more raw WALKSTUF1 hand groups unless generated scheduler ownership changes. |
| 2026-05-06 | `visitor3-low-group122-138-v080` | Log-only; source reverted. | Test the strongest remaining VISITOR3 low current-capacity grouped append after the 192 KiB setup-prime baseline. The read-plan ranked `{122,138}` as a high-score 16-sector current-fit cluster that could save five nominal reads, so the binary question was whether the saved transactions outweighed its known tight visible gaps. | Temporarily added a low-tide VISITOR3 read-group table with `{122,138}`, rebuilt, and ran `visitor3-low` against `scratch/ps1-perf-iterate/visitor3-setup-prime-192k-v080-canaries/20260506-151017-720001/summary.json` using `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored after the strict gate failed. | Failed/no promotion. The group did reduce nominal CD pressure (`loop_reads 50 -> 44`, `blocking_vb 362 -> 360`), but the primary gate regressed `loop_vb 1453 -> 1457`; `overrun_vb` stayed flat at `441` and `prefetch_overrun_vb` stayed `19`. Layout stayed fixed (`pack_lba=23232`, PS-EXE bucket `215040`). Artifact: `scratch/ps1-perf-iterate/visitor3-low-group122-138-v080-probe/20260506-161706-1098045/summary.json`. | Do not promote `{122,138}` as a standalone low-tide group. This confirms that even high-save VISITOR3 low clusters with very tight gaps need scheduler ownership or generated preload timing; transaction count alone is not enough under the current append path. |
| 2026-05-06 | `visitor3-low-group170-186-v080b` | `ps1: group visitor3 low sectors 170-186` | Retry the previously rejected VISITOR3 low `170..186` group after the accepted `192 KiB` setup-prime and WALKSTUF1 PAL4 setup-prime changes moved the current baseline. The hypothesis was that this later balanced cluster might now save one visible read without the tight-gap cadence loss seen in `{122,138}`. | Added a low-tide-only VISITOR3 read-group table with `{170,186}`, rebuilt, ran VISITOR3 low against the accepted 192 KiB setup-prime baseline with zero regression allowance, then gated VISITOR3 high, FISHING1 high, and BUILDING2 low canaries. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. VISITOR3 low improves `loop_vb 1453 -> 1452`, `overrun_vb 441 -> 440`, `blocking_vb 362 -> 361`, and `loop_reads 50 -> 49`; `target_vb=1012`, `prefetch_overrun_vb=19`, and `due_misses=32` stay flat. VISITOR3 high stayed exact-flat at `1450/1015`, FISHING1 high stayed exact-flat at `1069/1072`, and BUILDING2 low stayed exact-flat at `1465/1276`. Artifacts: focused run `scratch/ps1-perf-iterate/visitor3-low-group170-186-v080-probe/20260506-162055-1120330/summary.json`, canaries `scratch/ps1-perf-iterate/visitor3-low-group170-186-v080-canaries/20260506-162202-1126817/summary.json`. | Keep as a narrow VISITOR3 low-tide append win, not a general retry of all previously rejected low groups. Matrix rollup moves from `0.8318%` to `0.8309%` over target and from `99.4775%` to `99.4779%` target speed; the headless methodology still totals about `16.6` over-target points removed and `12.4` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-06 | `visitor3-low-group126-132-v080` | Log-only; source reverted. | Test the smallest fireable VISITOR3 low high-risk cluster after the accepted `170..186` low-tide append. The read-plan said `{126,132}` could save one nominal visible read but was a tight visible-gap scheduler-only candidate, so the binary check was whether the small size avoided the cadence loss seen in `{122,138}`. | Temporarily added `{126,132}` ahead of the accepted `{170,186}` low-tide table, rebuilt, and ran VISITOR3 low against the accepted `visitor3-low-group170-186-v080b` focused baseline with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. The probe was exact-flat on every key metric: `loop_vb=1452`, `target_vb=1012`, `overrun_vb=440`, `blocking_vb=361`, `prefetch_overrun_vb=19`, `loop_reads=49`, and `due_misses=32`. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group126-132-v080-probe/20260506-164127-1235377/summary.json`. | Do not promote or retry `{126,132}` as a standalone low-tide source table. Small high-risk VISITOR3 low groups still do not buy measurable runtime improvement through the current append path; remaining VISITOR3 work should move to scheduler-owned timing or generated pack/data-shape preprocessing. |
| 2026-05-06 | `building4-high-group49-65-v080` | Log-only; source reverted. | Test BUILDING4 high's lowest-risk current-window-compatible 16-sector read-group candidate after BUILDING2 and VISITOR3 standalone groups were mostly closed. The read-plan labeled `{49,65}` as current-compatible with one saved read and a medium validation risk, so the binary check was whether a small high-tide-only table could reduce residual BUILDING4 blocking. | Temporarily added high-tide-only BUILDING4 `{49,65}`, rebuilt, first ran a probe with a mismatched baseline label, then reran as `building4-high-current` against `scratch/ps1-perf-iterate/building4-current-v072c-baseline/20260506-081048-2562996/summary.json` using `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. The strict run was exact-flat on key metrics: `loop_vb=2985`, `target_vb=2774`, `overrun_vb=211`, `blocking_vb=285`, `prefetch_overrun_vb=51`, and `loop_reads=93`. It also grew/shifted `foregroundPilotPlay` by `+540` bytes, so it is a size/layout cost with no runtime gain. Artifact: `scratch/ps1-perf-iterate/building4-high-group49-65-v080-strict/20260506-164842-1276635/summary.json`. | Do not promote or retry BUILDING4 high `{49,65}` as a standalone source table. BUILDING4 needs generated scheduler ownership, larger-window metadata, or data-shape preprocessing; one-off current-window tables do not move the active loop. |
| 2026-05-06 | `building2-low-group538-550-slack6-v080` | Log-only; source reverted. | Retest the previously useful-but-nonpromotable BUILDING2 low `{538,550}` candidate with explicit scheduler ownership: only allow the grouped append when at least `6` held VBlanks are available, aiming to keep the loop/blocking benefit while avoiding the hidden-refill regression that rejected the raw table. | Temporarily added a `minSlackVBlanks` field to read groups, left existing groups at zero, added BUILDING2 low `{538,550,6}` beside the accepted `{318,330}` table, rebuilt, and ran BUILDING2 low against `scratch/ps1-perf-iterate/building2-low-current-v080-baseline/20260506-145117-611888/summary.json` with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. The slack guard made the candidate safe but exact-flat: `loop_vb=1465`, `target_vb=1276`, `overrun_vb=189`, `blocking_vb=334`, `prefetch_overrun_vb=35`, and `loop_reads=87`. It also grew/shuffled hot code (`fgRuntimeFillWindowForEntry +24`, `foregroundPilotPlay +44`) with no runtime gain. Artifact: `scratch/ps1-perf-iterate/building2-low-group538-550-slack6-v080-probe/20260506-165515-1313600/summary.json`. | Do not promote local min-slack gating for BUILDING2 low grouped appends. The safe version no-ops and the raw version regresses hidden refill; BUILDING2 needs first-class scheduler budget/ownership metadata or selective preprocessing rather than another local group guard. |
| 2026-05-06 | `scene-vb-baseline-gate-v080` | Host tooling only; no runtime change. | Before more setup-prime or preload experiments, close the measurement hole where active-loop wins could hide cost by moving it into setup or teardown. | Added `timing.scene_vb` to the baseline-gated key metric list in `scripts/ps1-perf-iterate.sh`, included it in synthesized fingerprints, and printed it in both the case headline and `vs baseline` comparison. Smoke-tested against the current FISHING1 high canary baseline; an exact-zero run failed on unrelated current CD noise while proving the comparison output, then a tolerant pass-path run confirmed the aggregate gate still passes when allowed. | Promoted as harness guardrail. Smoke artifact: `scratch/ps1-perf-iterate/scene-vb-gate-smoke-pass/20260506-170240-1355711/summary.json`; proof line includes `timing.scene_vb 1343->1328 (-15)` beside `loop_vb`, `blocking_vb`, and prefetch overrun. | Keep. Future setup-prime, setup-segment, inter-scene preload, and setup-render probes must improve or preserve full-scene cost, not just active-loop accounting. This does not change the runtime matrix rollup: current accepted speed remains `0.8309%` over target / `99.4779%` target speed after `visitor3-low-group170-186-v080b`. |
| 2026-05-06 | `visitor3-direct-stage-cap16-v080` | Log-only; source reverted. | Test whether VISITOR3 can use a larger scene-local tight-slack direct-stage payload cap after setup-prime and low-group wins. The hypothesis was that VISITOR3's remaining CD-first pressure could benefit from exact-payload reads up to `16 KiB` without changing global FISHING1's proven `8 KiB` knee. | Temporarily added a runtime `directStageMaxBytes` field, kept the default cap at `8 KiB`, set VISITOR3 to `16 KiB`, rebuilt, and ran VISITOR3 high against the accepted `192 KiB` setup-prime baseline with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. High tide was exact-flat on key metrics and full-scene timing: `scene_vb=1746`, `loop_vb=1450`, `target_vb=1015`, `overrun_vb=435`, `blocking_vb=355`, `prefetch_overrun_vb=14`, `loop_reads=45`, and `due_misses=31`. Hot foreground code shifted by `+12` bytes with no win. Artifact: `scratch/ps1-perf-iterate/visitor3-direct-stage-cap16-v080-probe/20260506-170856-1391091/summary.json`. | Do not promote or retry a local VISITOR3 direct-stage cap without generated coverage metadata. The current `8 KiB` global knee is not the visible limiter for VISITOR3 under the accepted setup-prime baseline; remaining VISITOR3 work needs scheduler-owned refill timing or data-shape preprocessing. |
| 2026-05-06 | `canary-baseline-refresh-tool-v080` | Host tooling only; no runtime change. | Baseline drift has caused false starts and stale comparisons during the headless path. The next scheduler-owned probes need a single same-commit canary capture command instead of manually rebuilding ad hoc baseline batches. | Added `scripts/ps1-perf-canary-baseline.sh`, which runs the standard high-pressure canary set with case-local CDs, writes the case manifest, and records the latest summary path in `scratch/ps1-perf-iterate/latest-canary-baseline.txt`. Smoke-tested with `bash -n` and `--dry-run --skip-build`, proving the command emits all expected FISHING1, VISITOR3, WALKSTUF1, BUILDING2, BUILDING4, BUILDING6, and ACTIVITY9 tide canaries. | Promoted as harness guardrail. No PS1 runtime bytes or matrix timing changed. The dry-run manifest lives under `scratch/ps1-perf-iterate/canary-smoke/canary-baseline-smoke/cases.txt`; real runs should use the generated `latest-canary-baseline.txt` as the baseline pointer for subsequent probes. | Keep. This completes the planned automated canary baseline refresh step and reduces stale baseline risk before more scheduler/data-shape experiments. |
| 2026-05-06 | `building2-high-setupprime64-v080` | Log-only; source reverted. | Test whether BUILDING2 high can take a smaller high-tide setup-prime budget without repeating the previously rejected broad/shared `128 KiB` high+low policy. The binary check was whether a high-only `64 KiB` resident setup window could reduce active-loop reads while preserving full-scene timing and visible CD cadence. | Temporarily changed the BUILDING2 setup-prime policy from high `0` / low `128 KiB` to high `64 KiB` / low `128 KiB`, rebuilt, and ran BUILDING2 high against a same-commit clean baseline with `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored and rebuilt after the strict gate failed. | Failed/no promotion. The probe improved `scene_vb 1719 -> 1717`, `loop_vb 1468 -> 1457`, and `prefetch_overrun_vb 56 -> 42`, but it regressed primary pressure metrics: `overrun_vb 183 -> 186` and `blocking_vb 301 -> 324`; `target_vb` also moved `1285 -> 1271`. Artifact: `scratch/ps1-perf-iterate/building2-high-setupprime64-v080-probe/20260506-171909-1448894/summary.json`; baseline: `scratch/ps1-perf-iterate/building2-high-current-v080-headless-baseline/20260506-171552-1430178/summary.json`. | Do not promote high-tide BUILDING2 contiguous setup-prime at `64 KiB`. It is an active-loop win that buys speed by worsening visible blocking and target-relative overrun, so BUILDING2 high needs scheduler-owned refill timing, segmented/prepared coverage, or data-shape preprocessing rather than another contiguous setup-prime size. |
| 2026-05-06 | `walkstuf3-low-fgp3-v080` | Log-only; asset restored. | Continue the data-shape path after ACTIVITY9's same-layout FGP3 win and the WALKSTUF1/BUILDING6 size-gate failures. The host size gate showed current WALKSTUF3 low could shrink inside the existing CD footprint (`994669` bytes), so the binary check was whether padded PAL4 FGP3 temporal residuals improve runtime without moving layout. | Converted only `generated/ps1/foreground/WALK3LOW.FG2` to FGP3/v1, padded it back to exactly `994669` bytes, rebuilt, and ran WALKSTUF3 low against a same-commit FGP2 baseline with `--require-improvement --allow-regression 0 --work-identity-min 95`. The original FGP2 asset was restored and the PS1 binary rebuilt after the gate failed. | Failed/no promotion. The converted payload shrank `986873 -> 811444` bytes and runtime CD metrics improved (`loop_reads 72 -> 61`, `blocking_vb 45 -> 43`, `prefetch_overrun_vb 25 -> 19`), but full timing regressed by one VBlank (`scene_vb 2583 -> 2584`, `loop_vb 2320 -> 2321`). The format switch also changed restore work identity (`restore_calls 1134 -> 378`), so this is not a safe isolated promotion. Artifact: `scratch/ps1-perf-iterate/walkstuf3-low-fgp3-v080-probe/20260506-172658-1493646/summary.json`; baseline: `scratch/ps1-perf-iterate/walkstuf3-low-current-v080-baseline/20260506-172507-1482874/summary.json`. | Do not promote one-sided WALKSTUF3 low FGP3 conversion by itself. The data shape reduces CD pressure but loses total loop time, so retry only with paired high/low conversion, setup-prime/residency, or scheduler-owned timing that preserves the one-VBlank timing edge. |
| 2026-05-06 | `building5-fgp3-padded-v080` | `ps1: convert building5 to padded fgp3` | Continue the same-layout data-shape lane after ACTIVITY9's FGP3 win and the WALKSTUF3 low rejection. The host size gate showed both current BUILDING5 PAL4 packs could shrink from `809098` payload bytes to `592755` while preserving the existing `818670` byte CD footprint. | Captured a same-commit BUILDING5 high/low FGP2 baseline, converted `generated/ps1/foreground/BUILDING5.FG2` and `BUIL5LOW.FG2` to FGP3/v1, padded both files back to `818670` bytes, rebuilt, and ran high/low with `--require-improvement --allow-regression 0 --work-identity-min 0`. A detached visible DuckStation long-run was also started with `scripts/ps1-duckstation-longrun.sh` so memory/scene-change regressions can be monitored separately under `scratch/duckstation-longrun-*`. | Promoted. High improves `scene_vb 3622 -> 3606`, `loop_vb 3359 -> 3343`, `overrun_vb 13 -> 0`, `blocking_vb 20 -> 5`, `prefetch_overrun_vb 20 -> 5`, and `loop_reads 56 -> 41`; low improves `scene_vb 3621 -> 3609`, `loop_vb 3357 -> 3345`, `overrun_vb 10 -> 0`, `blocking_vb 17 -> 8`, `prefetch_overrun_vb 17 -> 8`, and `loop_reads 56 -> 41`. Same-commit pack LBAs and PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/building5-fgp3-padded-v080-probe/20260506-173807-1556865/summary.json`; baseline: `scratch/ps1-perf-iterate/building5-current-v080-baseline/20260506-173428-1535823/summary.json`. | Keep as a current-pack data-shape win. Matrix rollup moves from `0.8309%` to `0.8231%` over target and from `99.4779%` to `99.4858%` target speed; the headless methodology has removed about `16.6` over-target points and added about `12.4` target-speed points since the compact full-matrix baseline. Next same-layout FGP3 candidates are ACTIVITY11, JOHNNY2, MARY5/MARY2, and ACTIVITY6, while VISITOR3/BUILDING2/B4 need scheduler-owned timing or selective preprocessing. |
| 2026-05-06 | `visitor3-high-group170-194-v081` | Log-only; source reverted. | After the v0.8.1 clean-rect stability release, test whether the accepted VISITOR3 high `170..186` grouped append could be extended to the planner's next balanced larger-window candidate `170..194`. The binary question was whether the larger retained setup-prime-backed window could save another visible read without repeating the already rejected `146..170` cadence loss. | Temporarily changed the high-tide VISITOR3 read group from `{170,186}` to `{170,194}`, rebuilt, and ran VISITOR3 high plus low against the accepted post-cleanrect baseline with `--require-improvement --allow-layout-change`. Source was restored after the gate failed. | Failed/no promotion. High tide did not reduce active loop time (`loop_vb=1450`) and regressed target-relative pressure: `target_vb 1015 -> 1014`, `overrun_vb 435 -> 436`, `blocking_vb 355 -> 356`, and `prefetch_overrun_vb 14 -> 16`; `loop_reads` stayed `45`. Low tide stayed exact-flat and failed `--require-improvement`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-170-194-probe/20260506-202839-2499713/summary.json`. | Do not extend the accepted VISITOR3 high `170..186` table to `170..194` under the current runtime. Larger retained windows are now confirmed to require generated scheduler ownership or a different data shape; the current grouped-append path turns the extra coverage into visible refill pressure instead of a speed win. |
| 2026-05-06 | `activity11-fgp3-padded-v081` | `ps1: convert activity11 to padded fgp3` | Continue the same-layout data-shape lane after BUILDING5's FGP3 win. The host size gate showed both current ACTIVITY11 PAL4 packs could shrink from `429658` payload bytes to `278596` while preserving the existing `433970` byte CD footprint. | Captured a same-commit ACTIVITY11 high/low FGP2 baseline, converted `generated/ps1/foreground/ACTIVITY11.FG2` and `ACTV11L.FG2` to FGP3/v1, padded both files back to `433970` bytes, rebuilt, and ran high/low with `--require-improvement --allow-layout-change --work-identity-min 0`. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `loop_vb 1729 -> 1715`, `overrun_vb 9 -> 0`, `blocking_vb 10 -> 2`, `prefetch_overrun_vb 4 -> 2`, and `loop_reads 29 -> 11`; low improves `loop_vb 1729 -> 1717`, `overrun_vb 12 -> 0`, `blocking_vb 14 -> 4`, `prefetch_overrun_vb 9 -> 4`, and `loop_reads 29 -> 11`. Same-commit pack LBAs and PS-EXE bucket stayed fixed. Full scene setup grows by `8/11` VBlanks because more pack work is primed before active playback. Artifact: `scratch/ps1-perf-iterate/activity11-fgp3-padded-v081-probe/20260506-203609-2542521/summary.json`; baseline: `scratch/ps1-perf-iterate/activity11-current-v081-baseline/20260506-203340-2528142/summary.json`. | Keep as a current-pack active-loop data-shape win. Matrix rollup moves from `0.8231%` to `0.8071%` over target and from `99.4858%` to `99.5018%` target speed; the headless methodology has removed about `16.6` over-target points and added about `12.4` target-speed points since the compact full-matrix baseline. Next same-layout FGP3 candidates are JOHNNY2, MARY5/MARY2, and ACTIVITY6, while VISITOR3/BUILDING2/B4 need scheduler-owned timing or selective preprocessing. |
| 2026-05-06 | `mary5-fgp3-padded-v081` | `ps1: convert mary5 to padded fgp3` | Continue the same-layout data-shape lane on current regenerated FGP2 packs. The size gate showed both current MARY5 PAL4 packs could shrink from `646602` bytes to `549421` while preserving the existing CD footprint. VISITOR5 and BUILDING1 were size-rejected first because direct FGP3 expanded those packs. | Captured a same-commit MARY5 high/low FGP2 baseline, converted `generated/ps1/foreground/MARY5.FG2` and `MARY5LOW.FG2` to FGP3/v1, padded both files back to `646602` bytes, rebuilt, and ran high/low with `--require-improvement --allow-layout-change --work-identity-min 0`. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `loop_vb 1591 -> 1581`, `overrun_vb 9 -> 0`, `blocking_vb 8 -> 5`, `prefetch_overrun_vb 8 -> 0`, and `loop_reads 49 -> 42`; low improves `loop_vb 1592 -> 1581`, `overrun_vb 11 -> 0`, `blocking_vb 10 -> 6`, `prefetch_overrun_vb 10 -> 2`, and `loop_reads 49 -> 42`. Same-commit pack LBAs and PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/mary5-fgp3-padded-v081-probe/20260506-210737-2716502/summary.json`; baseline: `scratch/ps1-perf-iterate/mary5-current-v081-baseline/20260506-210518-2703056/summary.json`. | Keep as a current-pack active-loop data-shape win. Matrix rollup moves from `0.8071%` to `0.7939%` over target and from `99.5018%` to `99.5149%` target speed; the headless methodology has removed about `16.6` over-target points and added about `12.4` target-speed points since the compact full-matrix baseline. Next same-layout FGP3 candidates are JOHNNY2, MARY2, and ACTIVITY6; VISITOR3/BUILDING2/B4 remain scheduler/data-shape work. |
| 2026-05-06 | `johnny2-fgp3-padded-v081` | `ps1: convert johnny2 to padded fgp3` | Continue the same-layout data-shape lane after MARY5. The size gate showed both current JOHNNY2 PAL4 packs could shrink from `288637` bytes to `226374` while preserving the existing CD footprint. The existing matrix rows were old v0.6.4 validation refreshes, so a same-commit baseline was required before trusting the public row delta. | Captured a same-commit JOHNNY2 high/low FGP2 baseline, converted `generated/ps1/foreground/JOHNNY2.FG2` and `JOHN2LOW.FG2` to FGP3/v1, padded both files back to `288637` bytes, rebuilt, and ran high/low with `--require-improvement --allow-layout-change --work-identity-min 0`. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `scene_vb 2089 -> 2080`, `loop_vb 1833 -> 1801`, `overrun_vb 82 -> 50`, and `blocking_vb 401 -> 369`; low improves `scene_vb 2091 -> 2080`, `loop_vb 1833 -> 1800`, `overrun_vb 82 -> 49`, and `blocking_vb 399 -> 377`. Same-commit pack LBAs and PS-EXE bucket stayed fixed. The row still carries heavy current CD pressure: `loop_reads=144` and `blocking_vb=369/377`. Artifact: `scratch/ps1-perf-iterate/johnny2-fgp3-padded-v081-probe/20260506-212057-2791614/summary.json`; baseline: `scratch/ps1-perf-iterate/johnny2-current-v081-baseline/20260506-211816-2776214/summary.json`. | Keep as a current-pack active-loop data-shape win with a stale-row correction caveat. Public matrix rollup moves from `0.7939%` to `0.8324%` over target and from `99.5149%` to `99.4776%` target speed because the old JOHNNY2 rows understated current pressure. The headless methodology has still removed about `16.6` over-target points and added about `12.4` target-speed points since the compact full-matrix baseline. Next same-layout FGP3 candidates are MARY2 and ACTIVITY6; VISITOR3/BUILDING2/B4/JOHNNY2 need scheduler-owned timing or selective preprocessing for the remaining CD pressure. |
| 2026-05-06 | `mary2-fgp3-padded-v081` | `ps1: convert mary2 to padded fgp3` | Continue the same-layout data-shape lane after JOHNNY2. The size gate showed both current MARY2 PAL4 packs could shrink from `582189` bytes to `526408` while preserving the existing CD footprint. The existing matrix rows were old v0.6.8 validation-refresh timing, so a same-commit baseline was required before trusting the public row delta. | Captured a same-commit MARY2 high/low FGP2 baseline, converted `generated/ps1/foreground/MARY2.FG2` and `MARY2LOW.FG2` to FGP3/v1, padded both files back to `582189` bytes, rebuilt, and ran high/low with `--require-improvement --allow-layout-change --work-identity-min 0`. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `scene_vb 2638 -> 2583`, `loop_vb 2385 -> 2330`, and `overrun_vb 135 -> 78`; low improves `scene_vb 2638 -> 2581`, `loop_vb 2384 -> 2327`, and `overrun_vb 134 -> 75`. Same-commit pack LBAs and PS-EXE bucket stayed fixed. The row still carries heavy current CD pressure: `loop_reads=233`, `due_misses=233`, and `blocking_vb=668/662`. Artifact: `scratch/ps1-perf-iterate/mary2-fgp3-padded-v081-probe/20260506-213922-2894734/summary.json`; baseline: `scratch/ps1-perf-iterate/mary2-current-v081-baseline/20260506-213628-2878074/summary.json`. | Keep as a current-pack active-loop data-shape win with a stale-row correction caveat. Public matrix rollup moves from `0.8324%` to `0.8847%` over target and from `99.4776%` to `99.4269%` target speed because the old MARY2 rows understated current pressure. The headless methodology has still removed about `16.5` over-target points and added about `12.3` target-speed points since the compact full-matrix baseline. Next same-layout FGP3 candidate is ACTIVITY6; VISITOR3/MARY2/BUILDING2/BUILDING4/JOHNNY2 need scheduler-owned timing or selective preprocessing for the remaining CD pressure. |
| 2026-05-06 | `mary2-prefetch-relief-v081` | `ps1: restore mary2 prefetch under clean pressure` | The padded FGP3 MARY2 rows exposed a clean-memory relief failure class: the pack data improved, but setup pressure dropped optional prefetch entirely (`policy=none`), leaving both tides with `due_misses=233` and visible blocking above `660` VBlanks. | Added a MARY2-only exception to the clean-memory relief gate and to the large-clean snapshot drop-prefetch guard, preserving stage/window prefetch for this validated pack. Ran focused MARY2 high/low against the padded-FGP3 baseline, then gated FISHING1, VISITOR3 high/low, WALKSTUF1 high, BUILDING2 low, and BUILDING4 high canaries. | Promoted with the standard hidden-refill tradeoff. MARY2 high improves `2330/2252 -> 2241/2248`, `overrun_vb 78 -> 0`, `blocking_vb 668 -> 2`, `loop_reads 233 -> 37`, and `due_misses 233 -> 0`. Low improves `2327/2252 -> 2242/2250`, `overrun_vb 75 -> 0`, `blocking_vb 662 -> 2`, `loop_reads 233 -> 37`, and `due_misses 233 -> 0`. The focused strict gate failed only because hidden `prefetch_overrun_vb` moved `0 -> 2`, accepted because visible timing and due-miss pressure collapse. Canaries stayed on accepted profiles. Artifacts: focused run `scratch/ps1-perf-iterate/mary2-prefetch-relief-v081-probe/20260506-222902-3167590/summary.json`; canaries `scratch/ps1-perf-iterate/mary2-prefetch-relief-v081-canaries/20260506-223211-3185895/summary.json`. | Promote as MARY2-local clean-pressure relief, not a generic memory-policy relaxation. Matrix rollup improves from `0.8847%` to `0.8228%` over target and from `99.4269%` to `99.4872%` target speed; green/yellow/red moves to `96/22/2`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.6` over-target points and added about `12.4` target-speed points. Next priority remains VISITOR3, then BUILDING2/BUILDING4/JOHNNY2 via scheduler-owned timing or selective preprocessing. |
| 2026-05-06 | `visitor3-contained-large-stage-copy-v081` | Log-only; source reverted. | Test whether VISITOR3's accepted setup-prime/grouped windows could satisfy large non-16KiB frame payloads already resident in the stream window. The read-plan showed a duplicate high-tide `170..186`/`176..186` pattern, so the binary question was whether staging could copy a contained large payload instead of issuing an exact buffered CD read. | Temporarily let setup and held-loop staging call `fgRuntimeCopyEntryFromWindow()` before rejecting entries that exceed the nominal `16 KiB` window read size, rebuilt, captured a same-branch VISITOR3 high/low baseline, and reran both tides with matching labels under `--require-improvement --allow-regression 0 --work-identity-min 95`. Source was restored after the strict gate failed. | Failed/no promotion. The change reduced duplicate CD pressure but moved the cost into hidden refill: high `loop_reads 45 -> 44`, `blocking_vb 355 -> 341`, `hits 113 -> 120`, and `due_misses 31 -> 24`, but `loop_vb` stayed `1450` and `prefetch_overrun_vb 14 -> 75`. Low similarly moved `loop_reads 49 -> 48`, `blocking_vb 361 -> 351`, `hits 112 -> 119`, and `due_misses 32 -> 25`, while `loop_vb` stayed `1452` and `prefetch_overrun_vb 19 -> 79`. Artifact: `scratch/ps1-perf-iterate/visitor3-contained-stage-v081-gated/20260506-230400-3364431/summary.json`; same-branch baseline: `scratch/ps1-perf-iterate/visitor3-current-v081-baseline/20260506-225852-3334385/summary.json`. | Do not promote large resident-window staging through the current held-loop scheduler. It proves VISITOR3 still has duplicate-read work, but the copy/read ownership must be explicitly budgeted; simply turning contained large entries into prefetch hits hides the work and fails the accepted prefetch-overrun gate. |
| 2026-05-07 | `visitor3-fgp3-full-compact-v081` | Log-only; source/asset probe superseded. | After contained staging failed, test whether a more compact VISITOR3 residual pack could attack the same CD-first problem without changing frame timing. The first version compacted both cleanup metadata and draw metadata, cutting active payload from `1552446` to `981514`. | Built temporary FGP3/v3 full-compact high/low packs and a matching runtime compositor. Focused VISITOR3 looked strong, but the broader canary exposed code-size/code-shape sensitivity: the extra compositor pushed the PS-EXE to the next pressure point, and even after parser sharing or CD-layout absorption attempts, non-VISITOR canaries did not stay flat. | Failed/no promotion. Full compaction improved VISITOR3 high `loop_vb 1450 -> 1358` and low `1452 -> 1357`, but the win was not isolated enough to promote under the current strict canary bar. Failed/superseded artifacts: `scratch/ps1-perf-iterate/visitor3-fgp3-compact-metadata-v081-canaries/20260506-232040-3458655/summary.json`, `scratch/ps1-perf-iterate/visitor3-fgp3-compact-metadata-v081-refactor-canaries/20260506-233128-3521482/summary.json`, and `scratch/ps1-perf-iterate/visitor3-fgp3-compact-metadata-v081-layout-canaries/20260506-234417-3595241/summary.json`. | Do not promote the full compact draw+cleanup format until there is a lower-code-size compositor or a larger structural code-size plan. Keep the data insight: VISITOR3 benefits from pack-shape compression, but non-VISITOR phase stability matters more than the larger isolated win. |
| 2026-05-07 | `visitor3-fgp3-cleanup-compact-v081` | `ps1: compact visitor3 cleanup metadata` | Salvage the safe subset of the full-compaction result: VISITOR3 cleanup row/span metadata was large and small-valued, while the PAL4 draw payload path already has a mature compositor. The binary question was whether compacting cleanup metadata only could reduce active payload and restore/CD work while keeping draw code, PS-EXE bucket, and all non-VISITOR canaries stable. | Added `scripts/compact-fgp3-residual-metadata.py`, regenerated `VISITOR3.FG2` and `VIST3LOW.FG2` as FGP3/v3 cleanup-compact packs padded to the existing `1555450` byte footprint, added the compact cleanup parser in the PS1 runtime, left draw spans on the existing PAL4 compositor, and updated host analyzers to parse FGP3/v3. Focused VISITOR3 high/low passed, then the broad canary gated FISHING1 high, VISITOR3 high/low, WALKSTUF1 high, BUILDING2 low, and BUILDING4 high with stable pack LBAs and `215040` PS-EXE bucket. | Promoted. Active payload drops `1552446 -> 1265930`. VISITOR3 high improves `scene_vb 1746 -> 1702`, `loop_vb 1450 -> 1406`, `target_vb 1015 -> 1019`, `overrun_vb 435 -> 387`, `blocking_vb 355 -> 296`, `prefetch_overrun_vb 14 -> 7`, `loop_reads 45 -> 40`, and `loop_read_vb 412 -> 333`. Low improves `1749 -> 1702`, `1452 -> 1405`, `1012 -> 1015`, `440 -> 390`, `361 -> 301`, `19 -> 8`, `49 -> 44`, and `425 -> 341`. Canaries stayed key-flat. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-fgp3-cleanup-compact-v081-probe/20260507-000127-3694031/summary.json`; canaries `scratch/ps1-perf-iterate/visitor3-fgp3-cleanup-compact-v081-canaries/20260507-000513-3715437/summary.json`. | Keep as a VISITOR3-local data-shape win. Matrix rollup improves from `0.8228%` to `0.7400%` over target and from `99.4872%` to `99.5291%` target speed; green/yellow/red remains `96/22/2`, with VISITOR3 high/low still the only red rows. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.7` over-target points and added about `12.4` target-speed points. Next priority remains VISITOR3 scheduler-owned CD timing or selective preprocessing, then WALKSTUF1/BUILDING2/BUILDING4. |
| 2026-05-07 | `visitor3-generated-groups-v081-probe` | Log-only; source reverted. | Use the new visible-cost read-plan output on the post cleanup-compact VISITOR3 baseline instead of retrying raw read-count groups. The generated current-window-compatible candidates were high `144..156` and low `128..140` / `134..146`; the binary check was whether those low-risk append boundaries became measurable after FGP3/v3 cleanup compaction lowered the active payload. | Temporarily added `{144,156}` to `kVisitor3HighReadGroups12` and `{128,140}` plus `{134,146}` ahead of the accepted low-tide `{170,186}` table, then ran focused VISITOR3 high/low against `visitor3-fgp3-cleanup-compact-v081` with fixed layout and zero regression allowance. Source was restored after the probe. | Failed/no promotion. Both tides were exact-flat: high stayed `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=296`, `prefetch_overrun_vb=7`, and `loop_reads=40`; low stayed `1702`, `1405`, `1015`, `390`, `301`, `8`, and `44`. The generated candidates are safe but do not move runtime through the current append path. Artifact: `scratch/ps1-perf-iterate/visitor3-generated-groups-v081-probe/20260507-010929-4069122/summary.json`. | Do not promote these post-compaction VISITOR3 groups as source tables. The visible-cost model correctly avoided regressions, but append grouping alone is exhausted; remaining VISITOR3 work should target scheduler-owned refill timing, selective upload-ready data, or a lower-code-size compact draw compositor. |
| 2026-05-07 | `visitor3-fgp3-fullcompact-v4-v081-probe` | Log-only; source/assets reverted. | Revisit the earlier VISITOR3 full metadata compaction with a stricter format boundary: keep the promoted FGP3/v3 cleanup-only format intact, prototype full cleanup+draw metadata compaction as FGP3/v4, and share the PAL4 span compositor core to reduce the extra runtime code that made the first full-compact attempt unsafe. | Temporarily added `--compact-draw` to `scripts/compact-fgp3-residual-metadata.py`, generated FGP3/v4 `VISITOR3.FG2` and `VIST3LOW.FG2` from the original FGP3/v1 backups, added a v4 runtime parser/compositor path, and ran focused VISITOR3 high/low against `visitor3-fgp3-cleanup-compact-v081`. Because focused VISITOR3 passed, ran the broad FISHING1, VISITOR3 high/low, WALKSTUF1 high, BUILDING2 low, and BUILDING4 high canary. Source and packs were restored to the accepted FGP3/v3 cleanup-only baseline after the canary failed. | Failed/no promotion. The focused win was real: high improved `loop_vb 1406 -> 1373`, `overrun_vb 387 -> 350`, `blocking_vb 296 -> 250`, `prefetch_overrun_vb 7 -> 2`, and `loop_reads 40 -> 35`; low improved `1405 -> 1365`, `390 -> 342`, `301 -> 245`, `8 -> 2`, and `44 -> 35`. But the PS-EXE sector bucket grew `215040 -> 217088`, foreground LBAs shifted by `+1`, and broad canaries regressed: WALKSTUF1 high `loop_vb 1595 -> 1601`, `blocking_vb 278 -> 284`, `prefetch_overrun_vb 50 -> 52`; BUILDING2 low `1465 -> 1467`, `overrun_vb 189 -> 191`, `prefetch_overrun_vb 35 -> 36`; BUILDING4 high `overrun_vb 211 -> 212`, `blocking_vb 285 -> 288`. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-fgp3-fullcompact-v4-v081-probe/20260507-012224-4141024/summary.json`; canary `scratch/ps1-perf-iterate/visitor3-fgp3-fullcompact-v4-v081-canaries/20260507-012443-4154393/summary.json`. | Do not promote FGP3/v4 full draw metadata compaction under the current hot-code layout. The data shape is valuable, but the remaining VISITOR3 win must come from a zero-code-size or layout-neutral route: generated pack scheduling, selective upload-ready data, a build/layout padding plan that preserves non-VISITOR LBAs, or a compact draw parser that does not cross the PS-EXE bucket. |
| 2026-05-07 | `building2-fgp3-cleanup-compact-v081` | `ps1: compact building2 cleanup metadata` | Reuse the promoted FGP3/v3 cleanup-only metadata format on the next top FGP3/v1 outlier without adding runtime code. The size gate showed both current BUILDING2 packs could preserve the existing `1303332` byte CD footprint while active payload dropped `1296388 -> 1044638`. | Backed up `BUILDING2.FG2` and `BUIL2LOW.FG2`, generated padded FGP3/v3 cleanup-compact replacements with `scripts/compact-fgp3-residual-metadata.py`, rebuilt, captured a clean same-commit BUILDING2 high/low baseline, and reran focused high/low with stable LBAs and `215040` PS-EXE bucket. Then gated FISHING1 high, VISITOR3 high/low, WALKSTUF1 high, BUILDING2 high/low, and BUILDING4 high. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `scene_vb 1719 -> 1682`, `loop_vb 1468 -> 1430`, `target_vb 1285 -> 1289`, `overrun_vb 183 -> 141`, `blocking_vb 301 -> 212`, `prefetch_overrun_vb 56 -> 20`, and `loop_reads 96 -> 82`. Low improves `1739 -> 1703`, `1465 -> 1429`, `1276 -> 1286`, `189 -> 143`, `334 -> 193`, keeps `prefetch_overrun_vb=35`, and cuts `loop_reads 87 -> 68`. Canaries stayed key-flat. Artifacts: focused `scratch/ps1-perf-iterate/building2-fgp3-cleanup-compact-v081-clean-probe/20260507-014552-81488/summary.json`; canaries `scratch/ps1-perf-iterate/building2-fgp3-cleanup-compact-v081-canaries/20260507-014803-94262/summary.json`; clean baseline `scratch/ps1-perf-iterate/building2-current-v081-clean-baseline/20260507-014336-68038/summary.json`. | Keep as a no-new-runtime-code BUILDING2 data-shape win. Matrix rollup improves from `0.7400%` to `0.6781%` over target and from `99.5291%` to `99.5777%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.7` over-target points and added about `12.5` target-speed points. Next priority remains VISITOR3 scheduler-owned/selective preprocessing, then WALKSTUF1 and BUILDING4. |
| 2026-05-07 | `building4-fgp3-cleanup-compact-window-v081` | `ps1: compact building4 cleanup metadata` | Reuse the promoted FGP3/v3 cleanup-only metadata format on BUILDING4, the next top FGP3/v1 outlier after VISITOR3/WALKSTUF1. The size gate showed both current BUILDING4 packs could preserve the existing `1714154` byte CD footprint while active payload dropped `1705426 -> 1370198`; the first unretuned compact-pack probe improved loop time but failed hidden refill, so the binary follow-up was whether smaller scene-local stream windows could keep the win and satisfy the strict gate. | Backed up `BUILDING4.FG2` and `BUIL4LOW.FG2`, generated padded FGP3/v3 cleanup-compact replacements with `scripts/compact-fgp3-residual-metadata.py`, rebuilt, captured a clean same-commit BUILDING4 high/low baseline, tested the unretuned compact packs, then retuned BUILDING4 windows from `24/36 KiB` to `20/32 KiB` and reran focused high/low with stable LBAs and `215040` PS-EXE bucket. Then gated FISHING1 high, VISITOR3 high/low, WALKSTUF1 high, BUILDING2 high/low, BUILDING4 high/low, and ACTIVITY9 high/low. Refreshed `performance-scene-matrix.csv`, regenerated the preprocess opportunity sheet, and updated the website/readme/docs. | Promoted. High improves `scene_vb 3257 -> 3210`, `loop_vb 2985 -> 2939`, `target_vb 2774 -> 2786`, `overrun_vb 211 -> 153`, `blocking_vb 285 -> 240`, `prefetch_overrun_vb 51 -> 27`; low improves `3257 -> 3220`, `2981 -> 2945`, `2784 -> 2798`, `197 -> 147`, `199 -> 117`, and `119 -> 114`. Unretuned compact packs improved high/low loop to `2950/2944`, but failed strict promotion because hidden refill rose to `59/122`; the window retune fixed that. Canaries stayed on accepted profiles. Artifacts: focused `scratch/ps1-perf-iterate/building4-fgp3-cleanup-compact-v081-window-compiled-probe/20260507-021926-271599/summary.json`; canaries `scratch/ps1-perf-iterate/building4-fgp3-cleanup-compact-v081-window-canaries/20260507-022230-289170/summary.json`; clean baseline `scratch/ps1-perf-iterate/building4-current-v081-clean-baseline/20260507-020858-211510/summary.json`; rejected unretuned probe `scratch/ps1-perf-iterate/building4-fgp3-cleanup-compact-v081-clean-probe/20260507-021210-230084/summary.json`. | Keep as a BUILDING4 data-shape plus window-cadence win. Matrix rollup improves from `0.6781%` to `0.6452%` over target and from `99.5777%` to `99.6067%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.8` over-target points and added about `12.5` target-speed points. Next priority remains VISITOR3 scheduler-owned/selective preprocessing, then WALKSTUF1, residual BUILDING2/BUILDING4, and BUILDING6/JOHNNY2 CD pressure. |
| 2026-05-07 | `visitor3-high-144-156-group-v081` | Log-only; source reverted. | Recheck the only post-BUILDING4 VISITOR3 high read-plan candidate that was append-start fireable, fit the current 16-sector retained window, and carried a balanced visible-cost classification. The binary question was whether `{144,156}` became measurable after the cleanup-compact baseline and BUILDING4 window retune shifted nothing in VISITOR3's pack layout. | Captured a fresh same-source VISITOR3 high/low baseline at `scratch/ps1-perf-iterate/visitor3-current-v081-after-building4-baseline/20260507-030826-544104/summary.json`, temporarily added `{144,156}` to `kVisitor3HighReadGroups12`, rebuilt, and ran VISITOR3 high/low with fixed layout, zero regression allowance, and `--require-improvement`. Source was restored after the strict gate failed. | Failed/no promotion. Both tides were exact-flat: high stayed `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=296`, `prefetch_overrun_vb=7`, `loop_reads=40`, and `group_hits=0`; low stayed `1702`, `1405`, `1015`, `390`, `301`, `8`, `44`, and `group_hits=0`. PS-EXE bucket and pack LBAs stayed fixed, but hot symbols shifted by `+4` bytes with no work reduction. Artifact: `scratch/ps1-perf-iterate/visitor3-high-144-156-group-v081-probe/20260507-031044-557405/summary.json`. | Do not promote or retry VISITOR3 high `{144,156}` as a source-table append under the current runtime. The candidate is safe but non-firing/non-measurable; VISITOR3 needs true scheduler-owned refill metadata, selective upload-ready data, or a layout-neutral compact draw path rather than more current-window hand tables. |
| 2026-05-07 | `visitor3-resident-window-reuse-v081` | Log-only; source reverted. | Follow up the `{144,156}` exact-flat result by testing whether the widened read actually existed but was not being reused by staging. CD-log inspection showed the read changed from `144..151` to `144..156`, but the next overlapping `145..155` read still occurred, so the binary question was whether allowing staging to consume any already-resident stream-window entry could convert that duplicate read into a safe prefetch hit. | Temporarily added `{144,156}` to VISITOR3 high and tested two source shapes: a direct early resident-window copy in `fgRuntimeTryStageNextFrame()`, then a smaller `fgRuntimeEntryFitsWindow()` eligibility change. Both were rebuilt and gated against `visitor3-current-v081-after-building4-baseline` with VISITOR3 high/low, fixed layout allowance, zero regression allowance, and `--require-improvement`. Source was restored after both gates failed. | Failed/no promotion. Both variants saved the high-tide duplicate read but moved the cost into hidden refill and code/layout pressure. The smaller variant produced high `loop_vb 1406 -> 1405`, `blocking_vb 296 -> 285`, and `loop_reads 40 -> 39`, but `prefetch_overrun_vb 7 -> 40`; low regressed `loop_vb 1405 -> 1409`, `overrun_vb 390 -> 394`, and `blocking_vb 301 -> 309`. The larger variant crossed the PS-EXE bucket to `217088` and had the same metric shape. Artifacts: `scratch/ps1-perf-iterate/visitor3-resident-window-reuse-v081-probe/20260507-031926-606117/summary.json` and `scratch/ps1-perf-iterate/visitor3-resident-fits-v081-probe/20260507-032225-623372/summary.json`. | Do not promote broad resident-window staging under the current held-loop scheduler. The experiment proves VISITOR3 still has duplicate read work after grouped append, but ownership must include an explicit refill budget/cadence model; simply turning resident large entries into staged hits creates visible hidden-overrun pressure. |
| 2026-05-07 | `visitor3-high-97-113-group-v081` | Log-only; source reverted. | Test the current read-plan's top VISITOR3 high `16`-sector scheduler-compatible candidate after rejecting the `144..156` path. The candidate was append-start fireable and did not require a larger retained window, so the binary question was whether it could save early-cluster reads without source-code growth or layout movement. | Temporarily added `{97,113}` to `kVisitor3HighReadGroups12`, rebuilt, and ran VISITOR3 high/low against the post-BUILDING4 baseline with stable PS-EXE bucket and fixed pack LBAs. Source was restored after the strict gate failed. | Failed/no promotion. High saved nominal read count (`loop_reads 40 -> 38`) but regressed all visible timing: `scene_vb 1702 -> 1717`, `loop_vb 1406 -> 1421`, `overrun_vb 387 -> 400`, `blocking_vb 296 -> 309`, and `prefetch_overrun_vb 7 -> 9`. Low stayed exact-flat and did not provide a compensating win. Artifact: `scratch/ps1-perf-iterate/visitor3-high-97-113-group-v081-probe/20260507-032457-638045/summary.json`. | Do not retry VISITOR3 high `97..113` as a grouped-read table under the current cadence. Early VISITOR3 clusters are too visible-gap-sensitive; remaining VISITOR3 work should shift to generated scheduler-owned refill timing, zero-code-size compact draw/data-shape work, or selective upload-ready preprocessing rather than more blind group ranges. |
| 2026-05-07 | `activity9-fgp3-cleanup-compact-v081` | Log-only; assets restored. | Test whether the already-supported FGP3/v3 cleanup-only compaction could salvage more CD pressure from ACTIVITY9 without adding runtime code. The size gate was attractive: both wide stitched ACTIVITY9 packs preserve the existing `1745484` byte CD footprint while active payload drops `1453793 -> 1196583`. | Captured a fresh same-commit ACTIVITY9 high/low baseline, backed up `ACTIVITY9.FG2` and `ACTV9LOW.FG2`, generated padded FGP3/v3 cleanup-compact replacements with `scripts/compact-fgp3-residual-metadata.py`, rebuilt, and ran both tides with stable PS-EXE bucket and stable pack LBAs. Original packs were restored after the paired gate failed. | Failed/no promotion as a paired change. Low improves materially: `scene_vb 2346 -> 2335`, `loop_vb 2098 -> 2087`, `overrun_vb 42 -> 31`, `blocking_vb 47 -> 42`, and `prefetch_overrun_vb 19 -> 12`. High regresses: `scene_vb 2343 -> 2348`, `loop_vb 2094 -> 2099`, `overrun_vb 38 -> 42`, and `blocking_vb 37 -> 38`, despite `loop_reads 52 -> 43`. Artifacts: baseline `scratch/ps1-perf-iterate/activity9-current-v081-baseline/20260507-033040-670366/summary.json`; probe `scratch/ps1-perf-iterate/activity9-fgp3-cleanup-compact-v081-probe/20260507-033316-685616/summary.json`. | Do not promote ACTIVITY9 cleanup compaction for both tides under the current strict family gate. The low-tide win is real, but high-tide cadence loses more than the reduced read count buys; retry only if high gets a window/cadence retune or if a release decision allows tide-specific asset compaction with high held flat. |
| 2026-05-07 | `activity9-low-fgp3-cleanup-compact-v081` | `ps1: compact activity9 low cleanup metadata` | Promote the safe tide-specific subset from the paired ACTIVITY9 cleanup-compaction miss: leave high untouched, compact only `ACTV9LOW.FG2`, and require high to stay flat while low improves. | Restored high `ACTIVITY9.FG2`, generated only `ACTV9LOW.FG2` as a padded FGP3/v3 cleanup-compact pack, rebuilt, confirmed focused ACTIVITY9 high/low, and then ran the standard FISHING1, VISITOR3 high/low, WALKSTUF1 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low canaries. Refreshed `performance-scene-matrix.csv`, regenerated preprocess opportunities, and updated README/site/docs. | Promoted. High remains flat at `scene_vb=2343`, `loop_vb=2094`, `target_vb=2056`, `blocking_vb=37`, and `prefetch_overrun_vb=23`. Low improves `scene_vb 2346 -> 2335`, `loop_vb 2098 -> 2087`, `overrun_vb 42 -> 31`, `blocking_vb 47 -> 42`, `prefetch_overrun_vb 19 -> 12`, and `loop_reads 93 -> 78`; active payload drops `1453793 -> 1196583` while the CD footprint stays `1745484` bytes. Artifacts: focused `scratch/ps1-perf-iterate/activity9-low-fgp3-cleanup-compact-v081-probe/20260507-033753-712036/summary.json`; canaries `scratch/ps1-perf-iterate/activity9-low-fgp3-cleanup-compact-v081-canaries/20260507-034300-741029/summary.json`. | Keep as a no-new-runtime-code, tide-specific pack-shape win. Matrix rollup improves from `0.6452%` to `0.6400%` over target and from `99.6067%` to `99.6118%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.8` over-target points and added about `12.5` target-speed points. Next priority remains VISITOR3 scheduler-owned/selective preprocessing, then WALKSTUF1, BUILDING2, BUILDING4, BUILDING6, and JOHNNY2 residual pressure. |
| 2026-05-07 | `johnny2-prefetch-relief-v081` | `ps1: restore johnny2 prefetch under clean pressure` | Finish the residual JOHNNY2 pressure left by the padded FGP3 conversion. The actual clean-rect backup estimate is `338830` bytes, which crossed the large-clean drop-prefetch guard and forced `policy=none` even though keeping the existing prefetch buffer is safe for this validated pack. | Added `johnny2` to the clean-pressure preserve list and shared that list between `fgSceneNeedsCleanMemoryRelief()` and the large-clean snapshot drop-prefetch guard, avoiding the duplicated exception chain that initially grew the PS-EXE. Ran focused JOHNNY2 high/low against the padded-FGP3 baseline, then gated FISHING1, VISITOR3 high/low, WALKSTUF1 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low as strict canaries. | Promoted. High improves `scene_vb 2080 -> 2020`, `loop_vb 1801 -> 1741`, `overrun_vb 50 -> 0`, `blocking_vb 369 -> 0`, `loop_reads 144 -> 8`, and `due_misses 144 -> 0`; low improves `2080 -> 2021`, `1800 -> 1741`, `49 -> 0`, `377 -> 0`, `144 -> 8`, and `144 -> 0`. The refactor keeps `jcreborn.exe` in the `215040` byte sector bucket and preserves JOHNNY2 pack LBAs `14420/14561`. Artifacts: focused `scratch/ps1-perf-iterate/johnny2-prefetch-relief-v081-probe2/20260507-041816-940450/summary.json`; canaries `scratch/ps1-perf-iterate/johnny2-prefetch-relief-v081-proper-canaries/20260507-044903-1116004/summary.json`; baseline `scratch/ps1-perf-iterate/johnny2-fgp3-padded-v081-probe/20260506-212057-2791614/summary.json`. | Keep as a JOHNNY2-local clean-pressure relief, not a generic memory-policy relaxation. Matrix rollup improves from `0.6400%` to `0.5830%` over target and from `99.6118%` to `99.6672%` target speed; green/yellow/red moves to `98/20/2`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.8` over-target points and added about `12.6` target-speed points. Next priority returns to VISITOR3, then WALKSTUF1, BUILDING2, BUILDING4, and BUILDING6. |
| 2026-05-07 | `visitor3-upload-band-cap16-v081-probe` | Log-only; source reverted. | Test whether the offline VISITOR3 x-band rect-cap signal mapped to the runtime dirty uploader. The binary probe raised `GR_MAX_UPLOAD_RECTS` from `8` to `16` and required VISITOR3 high/low to improve with stable pack LBAs and the same `215040` byte PS-EXE bucket. | Temporarily raised the runtime upload band cap in `grDrawBackground()`, rebuilt, and ran VISITOR3 high/low against the post-JOHNNY2 strict canary baseline with zero regression allowance and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. Both tides were exact-flat: high stayed `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=296`, `prefetch_overrun_vb=7`, `loop_reads=40`; low stayed `1702`, `1405`, `1015`, `390`, `301`, `8`, and `44`. Graphics counters were also unchanged, including high `upload_rects=244`, `upload_bytes=13384960`, `cap_hits=0`, `full_fallbacks=0` and low `263`, `13758720`, `0`, `0`. Artifact: `scratch/ps1-perf-iterate/visitor3-upload-band-cap16-v081-probe/20260507-050732-1221660/summary.json`. | Do not retry dirty uploader band-cap changes for VISITOR3. The offline x-band model is about potential selective upload-ready metadata, not the live `GR_MAX_UPLOAD_RECTS` fallback. VISITOR3 remains blocked on CD/refill cadence or pack data-shape work: generated scheduler-owned refill metadata, selective upload-ready payloads, or a layout-neutral compact draw parser. |
| 2026-05-07 | `visitor3-fullcompact-v4-graphics-os-padded-v081` | Log-only; source not promoted. | Retry the valuable VISITOR3 FGP3/v4 full draw-metadata compaction by preserving the established `215040` byte PS-EXE sector bucket with deterministic padding and using whole-file `graphics_ps1.c` `-Os` to recover the code-size headroom. The binary question was whether the layout-fixed build could keep the focused VISITOR3 speed win without moving or slowing non-VISITOR canaries. | Generated padded FGP3/v4 compact-draw `VISITOR3.FG2` and `VIST3LOW.FG2` from the accepted v3 backups, added the compact draw parser path, compiled `graphics_ps1.c` with `-Os`, padded `jcreborn.exe` back to `215040`, ran focused VISITOR3 high/low, then started the standard broad canary set against `scratch/ps1-perf-iterate/johnny2-prefetch-relief-v081-proper-canaries/20260507-044903-1116004/summary.json`. The canary was stopped after multiple hard regressions made promotion impossible. | Failed/no promotion. Focused VISITOR3 improved with fixed pack LBAs: high `loop_vb 1406 -> 1386`, `blocking_vb 296 -> 241`, `prefetch_overrun_vb 7 -> 2`, `loop_reads 40 -> 35`; low `1405 -> 1394`, `301 -> 252`, `8 -> 2`, and `44 -> 35`. But whole-file graphics `-Os` regressed canaries: WALKSTUF1 high `1595 -> 1610`, BUILDING2 high/low `1430/1429 -> 1451/1441`, and BUILDING4 high/low `2939/2945 -> 2959/2971`. Artifacts: focused pass `scratch/ps1-perf-iterate/visitor3-fullcompact-v4-graphics-os-padded-v081-probe/20260507-051943-1291143/summary.json`; stopped canary root `scratch/ps1-perf-iterate/visitor3-fullcompact-v4-graphics-os-padded-v081-canaries/20260507-052228-1307027/`. | Do not promote whole-file `graphics_ps1.c` `-Os` as the layout-neutral compact draw solution. The VISITOR3 data shape is still worth pursuing, but size recovery must avoid the shared compositor: use a cold-file size shave, a compact-only parser, generated scheduler metadata, or another zero/non-hot-code layout plan. |
| 2026-05-07 | `visitor3-fullcompact-v4-cdrom-os-padded-v081` | Log-only; source not promoted. | Test the next layout-neutral hypothesis for FGP3/v4 full draw metadata: leave `graphics_ps1.c` at the accepted compiler shape, recover the single PS-EXE sector by compiling only the colder `cdrom_ps1.c` with `-Os`, and pad the executable back to the established `215040` byte bucket. | Removed whole-file graphics `-Os`, added `cdrom_ps1.c` to the size-optimized allowlist, rebuilt to `215040`, and ran focused VISITOR3 high/low plus a broad canary subset. The canary was stopped after WALKSTUF1 high showed a hard timing regression. | Failed/no promotion. Focused VISITOR3 improved more than the graphics-`Os` variant: high `loop_vb 1406 -> 1378`, `blocking_vb 296 -> 257`, `prefetch_overrun_vb 7 -> 2`, `loop_reads 40 -> 35`; low `1405 -> 1372`, `301 -> 251`, `8 -> 2`, and `44 -> 35`. But the broad canary regressed WALKSTUF1 high from the accepted `1595/1403` profile to `1609/1401`, with `blocking_vb 278 -> 283` and `prefetch_overrun_vb 50 -> 56`; FISHING1 also shifted `1069/1072 -> 1070/1073`. Artifacts: focused pass `scratch/ps1-perf-iterate/visitor3-fullcompact-v4-cdrom-os-padded-v081-probe/20260507-053620-1388560/summary.json`; stopped canary root `scratch/ps1-perf-iterate/visitor3-fullcompact-v4-cdrom-os-padded-v081-canaries/20260507-053834-1401585/`. | Do not use compiler-option sector recovery for this VISITOR3 compact-draw promotion. Both hot and cold `-Os` recovery perturb timing enough to fail canaries. Next retry should keep source compiler shape stable and solve layout through CD layout accounting, generated pack/scheduler data, or a compact parser that fits without crossing the PS-EXE sector bucket. |
| 2026-05-07 | `visitor3-fullcompact-v4-title-shrink-v081` | Log-only; source not promoted. | Remove compiler-option timing drift from the FGP3/v4 compact-draw attempt by letting `jcreborn.exe` grow naturally to `217088` bytes and shrinking the unused legacy diagnostic `TITLE.RAW` asset by one sector, keeping the pre-foreground CD footprint unchanged and preserving foreground pack LBAs. | Restored normal compiler options, removed executable padding, truncated `generated/ps1/foreground/TITLE.RAW` by `2048` bytes, rebuilt, and ran focused VISITOR3 high/low plus the broad canary subset. The canary was stopped after WALKSTUF1 high reproduced the same regression as the original v4 attempt. | Failed/no promotion. Focused VISITOR3 was strong with stable foreground LBAs: high `loop_vb 1406 -> 1373`, `blocking_vb 296 -> 250`, `prefetch_overrun_vb 7 -> 2`, `loop_reads 40 -> 35`; low `1405 -> 1365`, `301 -> 245`, `8 -> 2`, and `44 -> 35`. FISHING1 high improved slightly (`1069/1072 -> 1068/1074`, blocking `5 -> 2`), proving the CD-layout compensation itself was safe. But WALKSTUF1 high regressed `1595/1403 -> 1601/1403`, with `blocking_vb 278 -> 284` and `prefetch_overrun_vb 50 -> 52`. Artifacts: focused pass `scratch/ps1-perf-iterate/visitor3-fullcompact-v4-title-shrink-v081-probe/20260507-054542-1443032/summary.json`; stopped canary root `scratch/ps1-perf-iterate/visitor3-fullcompact-v4-title-shrink-v081-canaries/20260507-054803-1456648/`. | Do not promote the current compact-draw runtime refactor even with fixed CD layout. The failing surface is the shared PAL4 span compositor refactor: existing non-v4 packs now run through changed code. Retry only with the accepted `grCompositePacked4SpansToBackground()` path restored byte-for-byte for existing formats and a separate compact-draw compositor used only by FGP3/v4 VISITOR3. |
| 2026-05-07 | `visitor3-fullcompact-v4-scratch-expand-v081` | Log-only; source/assets restored. | Test whether FGP3/v4 compact draw metadata could avoid the non-VISITOR shared-compositor regression by expanding compact draw rows into the existing PAL4 span scratch format, preserving the accepted compositor path byte-for-byte for existing packs. | Removed the duplicate compact compositor, routed v4 draw metadata through a scratch expansion helper, increased v4 scratch allocation to cover the expanded span payload, regenerated padded VISITOR3/VIST3LOW FGP3/v4 packs, and rebuilt before running canaries. | Failed/no promotion at the binary identity gate. The scratch-expansion build still produced `build-ps1/jcreborn.exe` at `217088` bytes with `TITLE.RAW` shortened to `612352` bytes; the accepted baseline requires the `215040` byte PS-EXE sector bucket. The ELF did shrink versus the separate-compositor retry (`text=204572`, `data=9314`, `bss=155300`), but not enough to recover the sector, so broad timing canaries would still be polluted by layout drift. | Do not promote the scratch-expansion v4 route. FGP3/v4 remains data-attractive, but current runtime support cannot fit inside the accepted executable bucket without compiler/layout compensation that has already failed canaries. Move VISITOR3 work back to scheduler-owned refill timing, selective upload-ready metadata, or another zero-code-size data-shape path. |
| 2026-05-07 | `visitor3-low-group105-117-v081` | Log-only; source restored. | Probe the fresh low-tide VISITOR3 read-plan's largest current-window-compatible group after v4 compact draw was closed. `{105,117}` promised four saved reads, but the planner marked it `unsafe:tight-visible-gap`, so the binary question was whether the existing grouped-append table path could capture the cluster without stealing visible cadence. | Temporarily added `{105,117}` to `kVisitor3LowReadGroups16`, rebuilt with the PS-EXE still in the accepted `215040` byte bucket, and ran VISITOR3 high/low against the post-JOHNNY2 baseline. The run used short case labels, so the harness did not perform an automatic baseline comparison; metrics were compared manually against the accepted `visitor3-high-canary` and `visitor3-low-canary` rows. | Failed/no promotion. High stayed exact-flat at `1406/1019`, `blocking_vb=296`, `prefetch_overrun_vb=7`, and `loop_reads=40`; low stayed exact-flat at `1405/1015`, `blocking_vb=301`, `prefetch_overrun_vb=8`, and `loop_reads=44`. There was no visible timing win and no work-volume reduction to justify the table growth. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group105-117-v081-probe/20260507-063212-1708060/summary.json`. | Do not promote or retry the tight low-tide `105..117` cluster through the current grouped-append table. Even when the planner's estimated read count looks large, this path does not move accepted runtime metrics; VISITOR3 still needs scheduler-owned refill metadata or a pack/preprocess data-shape change rather than more hand-authored table ranges. |
| 2026-05-07 | `walkstuf1-low-primecap160-v081` | `ps1: prime more walkstuf1 low setup data` | After VISITOR3 hand groups and compact-draw routes were closed, test whether WALKSTUF1 low could safely use the natural derived setup-prime budget that was previously hidden by the shared `128 KiB` resident cap. The paired `160 KiB` cap made low faster but regressed high, so the promotable binary question was whether a low-tide-only cap could keep high exact-flat while extending low setup coverage. | Added `FG_WALKSTUF1_LOW_SETUP_PRIME_MAX_RESIDENT_BYTES` at `160 KiB` and used it only for `walkstuf1` low tide; high continues to use the shared `128 KiB` cap. Updated `scripts/ps1-foreground-read-plan.py` so PAL4 WALKSTUF1 low reports the wider `auto:walkstuf1-low` coverage. Ran a primary low gate against the post-JOHNNY2 baseline, then gated FISHING1, VISITOR3 high/low, WALKSTUF1 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low as canaries. | Promoted. WALKSTUF1 low improves `scene_vb 1900 -> 1895`, `loop_vb 1614 -> 1604`, `target_vb 1397 -> 1407`, `overrun_vb 217 -> 197`, `blocking_vb 276 -> 270`, `prefetch_overrun_vb 59 -> 55`, and `loop_reads 134 -> 132`; `due_misses` moves `49 -> 50`. WALKSTUF1 high stays exact-flat at `1595/1403`, `blocking_vb=278`, `prefetch_overrun_vb=50`, and `loop_reads=136`; all broad canaries stayed key-flat with fixed foreground LBAs and the accepted `215040` byte PS-EXE bucket. Artifacts: primary `scratch/ps1-perf-iterate/walkstuf1-low-primecap160-v081-primary/20260507-064625-1789404/summary.json`; canaries `scratch/ps1-perf-iterate/walkstuf1-low-primecap160-v081-canaries/20260507-064747-1797388/summary.json`; rejected paired cap `scratch/ps1-perf-iterate/walkstuf1-primecap160-v081-probe/20260507-064109-1758799/summary.json`. | Keep as a low-tide-only setup-prime cap retune, not a shared WALKSTUF1 policy. Matrix rollup improves from `0.5830%` to `0.5706%` over target and from `99.6672%` to `99.6769%` target speed. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.8` over-target points and added about `12.6` target-speed points. Next priority returns to VISITOR3 scheduler-owned/selective preprocessing, then WALKSTUF1 high, BUILDING2, BUILDING4, and BUILDING6. |
| 2026-05-07 | `visitor3-resident-stage-budget-v081` | Log-only; source restored. | Test whether VISITOR3 could reuse a large next-frame payload that was already fully resident in the stream window, then suppress lookahead CD while that resident-staged frame waited. This was a scheduler-budgeted retry of the earlier resident-window reuse idea, aimed at harvesting stage hits without the prior hidden-overrun spike. | Added a temporary `stagedFromResidentWindow` flag, allowed VISITOR3-only copy-from-window staging for oversized payloads already resident, and skipped lookahead window prefetch while such a staged frame was pending. Rebuilt and ran VISITOR3 high/low against the accepted WALKSTUF1-low canary baseline. The probe source was restored after manual comparison showed the harness baseline keys did not match the shorter case labels. | Failed/no promotion. The code grew `build-ps1/jcreborn.exe` to `217088` bytes and shifted VISITOR3 pack LBAs by one sector (`22472/23232 -> 22473/23233`), failing binary identity before speed comparison. Metrics were also not promotable: high moved `1406/1019 -> 1408/1021`, with blocking only `296 -> 294`; low regressed `1405/1015 -> 1409/1015`, `overrun_vb 390 -> 394`, `blocking_vb 301 -> 309`, and `loop_reads 44 -> 43`. Artifact: `scratch/ps1-perf-iterate/visitor3-resident-stage-budget-v081-probe/20260507-072437-2005656/summary.json`. | Do not promote or retry the resident-stage budget path in runtime code unless it is size-neutral and label-matched against the baseline. The low-tide cadence regression plus PS-EXE sector growth confirms VISITOR3 needs zero-code-size data-shape work, generated scheduler metadata, or a compact parser that preserves binary identity. |
| 2026-05-07 | `visitor3-xband-scratch-upload-v081` | Log-only; source restored. | Test the preprocessing sheet's top VISITOR3 signal with a narrow runtime probe: use the existing per-row dirty X metadata to upload align4 x-bands through the existing `gScrStreamRows` scratch buffer when a dirty band fits, otherwise fall back to the accepted full-width row upload path. | Temporarily added x-min/x-max tracking, scratch offsets, per-band row copies, and narrow `LoadImage` rectangles inside `grDrawBackground()`. The probe was stopped at the build gate and source was restored before any perf run. | Failed/no promotion at binary identity. The patch grew `build-ps1/jcreborn.exe` from the accepted `215040` byte sector bucket to `217088` bytes, which would shift foreground pack LBAs and pollute every canary. After restoring source and rebuilding, the executable returned to `215040` bytes. | Do not pursue runtime x-band uploading in the shared graphics path without a size-neutral implementation. The upload-ready signal remains valid as a pack/preprocess direction, but the live `grDrawBackground()` path has no spare sector for this logic under the current binary-identity gate. |
| 2026-05-07 | `building6-fgp3-padded-v081-size-check` | Log-only; assets untouched. | Check whether the remaining BUILDING6 high/low pressure rows could move from current PAL4 FGP2 packs to padded FGP3 as a data-only optimization, preserving the executable and CD layout. | Generated temporary FGP3/v1 residual packs under `scratch/ps1-perf-iterate/building6-fgp3-padded-v081-assets/` from the current `BUILDING6.FG2` and `BUIL6LOW.FG2` without modifying `generated/ps1/foreground`. | Failed/no promotion at the pack-size gate. Both current packs are `1,444,370` bytes, but the residual conversion produces `1,601,445` byte packs (`payload 1,438,098 -> 1,595,173`), so padding cannot preserve the CD footprint; promoting would shift downstream LBAs before any speed comparison. | Do not retry padded FGP3 for current BUILDING6 with the generic residual builder. BUILDING6 needs either FGP2 scheduler/window retuning or a compact/selective pack format that is smaller than the current FGP2 payload, not generic FGP3 residual conversion. |
| 2026-05-07 | `walkstuf1-fgp3-padded-v081-size-check` | Log-only; assets untouched. | Check whether the current WALKSTUF1 high/low PAL4 FGP2 packs could use the same data-only padded-FGP3 lane that helped earlier large PAL4 scenes, without changing executable size or CD layout. | Generated temporary FGP3/v1 residual packs under `scratch/ps1-perf-iterate/walkstuf1-fgp3-padded-v081-assets/` from current `WALKSTUF1.FG2` and `WALK1LOW.FG2` only. | Failed/no promotion at the pack-size gate. Both current packs are `1,535,263` bytes, while generic residual conversion produces `1,717,175` byte packs (`payload 1,530,775 -> 1,712,687`). This cannot be padded back to the original footprint and would shift downstream LBAs. | Do not use generic FGP3 residual conversion for WALKSTUF1. The scene remains a scheduler/window/setup-prime or future compact/selective-format target, not a current padded-FGP3 candidate. |
| 2026-05-07 | `building2-high-setup-prime128-v081` | Log-only; source restored. | Test whether BUILDING2 high could use the same `128 KiB` setup-prime residency budget already assigned to BUILDING2 low, with no executable-sector or pack-LBA movement. | Temporarily changed the BUILDING2 setup-prime policy from high `0` / low `128 KiB` to high/low `128 KiB`, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran BUILDING2 high/low against the current accepted canary baseline. Source was restored after the paired gate failed. | Failed/no promotion. High improved active pressure (`loop_vb 1430 -> 1428`, `overrun_vb 141 -> 139`, `blocking_vb 212 -> 194`, `loop_reads 82 -> 68`, `due_misses 39 -> 34`) with fixed pack LBA, but setup residency cost raised total `scene_vb 1682 -> 1701`. Low stayed exact-flat (`1429/1286`, `blocking_vb=193`, `prefetch_overrun_vb=35`, `loop_reads=68`). Artifact: `scratch/ps1-perf-iterate/building2-high-setup-prime128-v081-probe/20260507-074030-2095310/summary.json`. | Do not promote high setup-prime unless scene-total gating is relaxed or the setup cost is hidden elsewhere. BUILDING2 high needs a loop-visible win that does not add setup VBlanks, likely a scheduler-owned group or data-shape change. |
| 2026-05-07 | `visitor3-low-group134-150-v081` | Log-only; source restored. | Probe the current VISITOR3 low read-plan's low-risk-looking `134..150` append-start candidate after the high-tide `97..113` and `144..156` table candidates had already failed or gone exact-flat. The candidate was current-window-compatible and promised a possible read reduction without changing pack layout. | Temporarily added `{134,150}` to `kVisitor3LowReadGroups16`, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran VISITOR3 high/low against the accepted WALKSTUF1-low canary baseline with zero regression allowance and `--require-improvement`. Source was restored and rebuilt after the gate failed. | Failed/no promotion. High stayed exact-flat at `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=296`, `prefetch_overrun_vb=7`, and `loop_reads=40`. Low reduced `loop_reads 44 -> 43`, but regressed the real bottleneck: `scene_vb 1702 -> 1706`, `loop_vb 1405 -> 1409`, `overrun_vb 390 -> 394`, `blocking_vb 301 -> 309`, and `due_misses 31 -> 33`; `group_hits=0`, foreground LBAs stayed fixed (`22472/23232`), and the PS-EXE bucket stayed `215040`. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group134-150-v081-probe/20260507-074838-2141746/summary.json`. | Do not promote or hand-author more VISITOR3 low table ranges from this tight cluster. A nominal read-count reduction can still steal visible cadence; VISITOR3 needs scheduler-owned grouped reads or zero-code-size data-shape work, not standalone table entries. |
| 2026-05-07 | `building2-low-group67-79-v081` | Log-only; source restored. | Test a current BUILDING2 low read-plan candidate outside the already accepted `{318,330}` group. The `67..79` append-start candidate had no overread, fit the existing grouped-window capacity, and looked safer than the high-tide tight clusters. | Temporarily added `{67,79}` ahead of `{318,330}` in `kBuilding2LowReadGroups12`, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran BUILDING2 high/low against the accepted WALKSTUF1-low canary baseline with zero regression allowance and `--require-improvement`. Source was restored and rebuilt after the gate failed. | Failed/no promotion. High stayed exact-flat (`1682`, `1430/1289`, `overrun_vb=141`, `blocking_vb=212`, `prefetch_overrun_vb=20`, `loop_reads=82`). Low cut hidden refill (`prefetch_overrun_vb 35 -> 14`) but regressed user-facing timing and visible CD pressure: `scene_vb 1703 -> 1709`, `loop_vb 1429 -> 1435`, `target_vb 1286 -> 1289`, `overrun_vb 143 -> 146`, `blocking_vb 193 -> 214`, and `due_misses 32 -> 38`; `loop_reads` stayed `68`, `group_hits=0`, pack LBAs stayed `6180/6817`, and the PS-EXE bucket stayed `215040`. Artifact: `scratch/ps1-perf-iterate/building2-low-group67-79-v081-probe/20260507-075432-2175611/summary.json`. | Do not promote. The candidate shifts refill work but does not reduce reads or timing; more hand-authored BUILDING2 low groups should wait for scheduler-owned metadata or a generated cost model that treats visible blocking as the primary objective. |
| 2026-05-07 | `read-candidate-matrix-v081` | Host-side tooling; no runtime promotion. | After repeated hand-authored VISITOR3 and BUILDING2 groups either regressed visible timing or went exact-flat, add a durable queue report that ranks current read-plan candidates and marks known bad exact ranges before source edits. The binary question was whether the planner output plus experiment log can steer future probes toward scheduler-owned candidates instead of stale standalone table ranges. | Added `scripts/ps1-read-candidate-matrix.py`, generated `docs/ps1/performance-read-candidate-matrix.csv` and `.md` from the accepted `walkstuf1-low-primecap160-v081` canary root, linked the report from the README and performance website docs, regenerated the source-library wrappers, and rebuilt/red-teamed the static site. | Promoted as host-side planning tooling. The initial report had `128` candidate rows, `0` standalone-safe rows, `8` scheduler-or-guarded rows, `58` scheduler-owned-only rows, `6` exact ranges already closed by the experiment log, and `12` under-target rows deferred. No PS1 runtime bytes, CSV timing rollup, or matrix baseline changed: accepted speed remains `0.5706%` over target / `99.6769%` target speed. | Keep. This makes VISITOR3's next path explicit: direct source-table grouping is exhausted under the current scheduler. Probe only scheduler-owned or guarded CD timing, selective data-shape/preprocess changes, or other layout-neutral approaches; do not spend more iterations on raw hand-authored table ranges without a new ownership model. |
| 2026-05-07 | `visitor3-high-group144-160-slack4-v081` | `ps1: guard visitor3 high read group` | Use the new read-candidate matrix's scheduler-or-guarded lane instead of another raw VISITOR3 hand table. The target candidate was high-tide `144..160`, but only when the current scheduler has at least four VBlanks of slack, so the binary question was whether a minimal guard could lower visible CD blocking without stealing loop cadence or shifting layout. | Added `minSlackVBlanks` to grouped read entries, left existing groups unguarded, and added VISITOR3 high `{144,160,4}`. Ran focused VISITOR3 high/low against the accepted WALKSTUF1-low canary baseline, then gated FISHING1 high, FISHING3 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low with fixed foreground LBAs and the accepted `215040` byte PS-EXE bucket. | Promoted. VISITOR3 high keeps `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `prefetch_overrun_vb=7`, and `loop_reads=40`, while lowering visible blocking `296 -> 294`; `loop_read_vb` moves `333 -> 335`, an acceptable hidden-CD trade for lower visible wait. VISITOR3 low stays flat at `1702`, `1405/1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, and `loop_reads=44`. All broad canaries stayed key-flat with fixed layout. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-high-group144-160-slack4-v081-probe/20260507-080924-2259561/summary.json`; canaries `scratch/ps1-perf-iterate/visitor3-high-group144-160-slack4-v081-canaries/20260507-081217-2276264/summary.json`. The refreshed read-candidate report now has `127` rows, `1` standalone-safe row, `11` scheduler-or-guarded rows, and VISITOR3 high `144..160` remains scheduler/guarded rather than raw standalone. | Keep as the first VISITOR3 scheduler/slack-owned group after cleanup compaction, not a general hand-table pattern. The matrix speed rollup remains `0.5706%` over target / `99.6769%` target speed because `loop_vb` did not move, but visible CD blocking improves on the largest outlier. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology still has removed about `16.8` over-target points and added about `12.6` target-speed points, plus this blocking-only reduction. |
| 2026-05-07 | `visitor3-low-group105-121-slack6-v081` | Log-only; source restored. | Test whether the top VISITOR3 low scheduler-only cluster could become safe and useful with explicit slack ownership after the high-tide guarded group proved the path could move visible blocking. The candidate was low-tide `{105,121}` with `minSlackVBlanks=6`. | Temporarily added `{105,121,6}` ahead of the accepted low `{170,186}` group, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran VISITOR3 high/low against the newly promoted guarded high baseline with zero regression allowance and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. High stayed exact-flat at `1702`, `1406/1019`, `blocking_vb=294`, `prefetch_overrun_vb=7`, and `loop_reads=40`; low stayed exact-flat at `1702`, `1405/1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, and `loop_reads=44`. The only measurable change was hot-symbol drift (`foregroundPilotPlay +4`) from the extra table row. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group105-121-slack6-v081-probe/20260507-083804-2423380/summary.json`. | Do not promote or retry low `105..121` as a local slack-guarded table. This tight low cluster still does not fire into a tracked runtime win; low-tide VISITOR3 needs first-class generated scheduler ownership, a different data shape, or selective preprocessing rather than another local guarded append. |
| 2026-05-07 | `visitor3-high-group104-120-slack4-v081` | Log-only; source restored. | Test the next VISITOR3 high scheduler-owned candidate from the read-candidate matrix after the accepted high `{144,160,4}` guard. The candidate was high-tide `{104,120}` with `minSlackVBlanks=4`, using the same strict high/low paired gate. | Temporarily added `{104,120,4}` to `kVisitor3HighReadGroups12`, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran VISITOR3 high/low against the newly promoted guarded high baseline with zero regression allowance and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. High saved nominal reads (`loop_reads 40 -> 38`) but regressed every primary timing pressure metric: `scene_vb 1702 -> 1706`, `loop_vb 1406 -> 1410`, `target_vb 1019 -> 1020`, `overrun_vb 387 -> 390`, `blocking_vb 294 -> 300`, and `prefetch_overrun_vb 7 -> 11`; `due_misses` stayed `31` and pack LBA stayed `22472`. Low stayed exact-flat at `1702`, `1405/1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`, and pack LBA `23232`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group104-120-slack4-v081-probe/20260507-084406-2458173/summary.json`. | Do not promote or retry high `104..120` as a local slack-guarded table. This candidate proves that saved reads can still steal visible cadence under the current grouped append path; remaining VISITOR3 work should move to generated scheduler ownership, data-shape preprocessing, or a different hidden-CD budget rather than adjacent local high-table ranges. |
| 2026-05-07 | `visitor3-high-group138-162-slack4-v081` | `ps1: widen visitor3 high guarded read group` | Promote the generated-window retry after the narrow VISITOR3 high guarded group proved slack ownership could move visible CD blocking but adjacent hand ranges regressed. The candidate widens the high-tide guarded append to `{138,162}` with `minSlackVBlanks=4`, allowing the scheduler to collapse one additional active read only when the due-frame gap can absorb it. | Added `{138,162,4}` before the existing VISITOR3 high `{144,160,4}` group, rebuilt at the accepted `215040` byte PS-EXE bucket, ran a focused VISITOR3 high/low probe against the guarded-high baseline, then gated FISHING1 high, FISHING3 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low with fixed foreground LBAs. | Promoted as a work/CD-pressure win. VISITOR3 high keeps `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, and `prefetch_overrun_vb=7`, while lowering `blocking_vb 294 -> 293`, `loop_reads 40 -> 39`, and `loop_read_vb 335 -> 332`; `due_misses` stays `31`, pack LBA stays `22472`, and the PS-EXE bucket stays `215040`. VISITOR3 low stays exact-flat at `1702`, `1405/1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`, and pack LBA `23232`. All broad canaries stayed key-flat. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-high-group138-162-slack4-v081-probe/20260507-085014-2493254/summary.json`; canaries `scratch/ps1-perf-iterate/visitor3-high-group138-162-slack4-v081-canaries/20260507-085249-2508519/summary.json`. | Promote. The matrix speed rollup remains `0.5706%` over target / `99.6769%` target speed because `loop_vb` is flat, but VISITOR3 high visible CD blocking and active read count both improve on the top outlier. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.8` over-target points and added about `12.6` target-speed points, plus this additional blocking/read reduction. |
| 2026-05-07 | `visitor3-low-group99-115-slack8-v082` | Log-only; source restored. | Test the highest-scoring remaining VISITOR3 low scheduler-owned range after the `138..162` high-tide promotion. The binary question was whether a strict `minSlackVBlanks=8` guard could make low-tide `{99,115}` safe enough to collapse the tight early read cluster without stealing visible cadence. | Temporarily added `{99,115,8}` ahead of the existing VISITOR3 low `{170,186}` group, rebuilt, and ran paired VISITOR3 high/low against the accepted `visitor3-high-group138-162-slack4-v081` canary baseline with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored immediately after the gate failed. | Failed/no promotion. The single extra table row grew the PS-EXE sector bucket `215040 -> 217088` and shifted VISITOR3 pack LBAs by one sector, failing layout identity before considering speed. It also regressed both tides: high `scene_vb 1702 -> 1705`, `loop_vb 1406 -> 1409`, `overrun_vb 387 -> 388`, `blocking_vb 293 -> 296`; low `1702 -> 1706`, `1405 -> 1409`, `390 -> 394`, `blocking_vb 301 -> 309`, while only reducing low `loop_reads 44 -> 43`. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group99-115-slack8-v082-probe/20260507-100707-2923426/summary.json`. | Do not promote or retry VISITOR3 low `99..115` as a local guarded read table. The range is now closed unless a generated scheduler-owned metadata path can add it without executable growth and with explicit visible-CD ownership; the next VISITOR3 path should avoid raw low-cluster table rows and focus on size-neutral data-shape or first-class scheduler metadata. |
| 2026-05-07 | `visitor3-low-group105-117-slack8-v082` | Log-only; source restored. | After removing the redundant high-tide table row recovered source headroom, test a narrower VISITOR3 low guarded cluster before promoting the cleanup. The binary question was whether low-tide `{105,117}` with `minSlackVBlanks=8` could use that recovered headroom without moving layout and still produce a measurable timing or work-volume win. | Temporarily added `{105,117,8}` to `kVisitor3LowReadGroups16` while keeping the high-tide `{144,160,4}` row removed, rebuilt, and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-group138-162-slack4-v081-canaries/20260507-085249-2508519/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored to the high-row cleanup after the strict gate failed. | Failed/no promotion. Layout stayed fixed and both tides were exact-flat: VISITOR3 high `1702`, `1406/1019`, `blocking_vb=293`, `prefetch_overrun_vb=7`, `loop_reads=39`; VISITOR3 low `1702`, `1405/1015`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`. Because no key timing or work-volume metric improved, the `--require-improvement` gate rejected it. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group105-117-slack8-v082-probe/20260507-101456-2968856/summary.json`. | Do not promote or retry low `105..117` as a local guarded table. It is safe but inert under the current append path; low-tide VISITOR3 still needs generated scheduler ownership or data-shape work rather than another raw cluster row. |
| 2026-05-07 | `visitor3-high-remove-144-160-v082` | `ps1: drop redundant visitor3 high read group` | The accepted high-tide `{138,162,4}` guarded window fully subsumes the earlier `{144,160,4}` guarded row. The binary question was whether removing the older row preserves timing/layout while recovering source-table headroom for future VISITOR3 scheduler work. | Removed `{144,160,4}` from `kVisitor3HighReadGroups12`, rebuilt, ran a focused VISITOR3 high/low gate against the accepted `visitor3-high-group138-162-slack4-v081` canary baseline, then gated FISHING1 high, FISHING3 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low with fixed foreground LBAs and the accepted `215040` byte PS-EXE bucket. | Promoted as a work/code-headroom cleanup. Focused and broad canaries stayed exact-flat: VISITOR3 high remains `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=293`, `prefetch_overrun_vb=7`, `loop_reads=39`, `loop_read_vb=332`; VISITOR3 low remains `1702`, `1405/1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`. Pack LBAs and the PS-EXE sector bucket stayed fixed. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-probe/20260507-101226-2953837/summary.json`; canaries `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json`. | Keep. This is not a VBlank speed win and does not change the aggregate `0.5706%` over target / `99.6769%` target speed, but it removes a redundant guarded table row from the largest outlier while preserving every measured gate. Future VISITOR3 probes should spend recovered headroom only on candidates that show a real key-metric win or on generated scheduler/data-shape work. |
| 2026-05-07 | `visitor3-high-group124-140-slack8-v082` | Log-only; source restored. | Continue VISITOR3 high scheduler-owned probing after the redundant-row cleanup by testing the next high-tide candidate from the read-candidate matrix. The candidate was `{124,140}` with a stricter `minSlackVBlanks=8`, intended to prove whether a nearby short-visible-gap cluster could be made safe without moving layout or stealing cadence. | Temporarily added `{124,140,8}` to `kVisitor3HighReadGroups12`, rebuilt, and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored immediately after the gate failed. | Failed/no promotion. The extra row grew the PS-EXE sector bucket `215040 -> 217088` and shifted VISITOR3 pack LBAs by one sector (`22472 -> 22473`, `23232 -> 23233`). It also regressed both tides: high `scene_vb 1702 -> 1705`, `loop_vb 1406 -> 1409`, `overrun_vb 387 -> 388`, `blocking_vb 293 -> 296`; low `1702 -> 1706`, `1405 -> 1409`, `390 -> 394`, `blocking_vb 301 -> 309`, while only reducing low `loop_reads 44 -> 43`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group124-140-slack8-v082-probe/20260507-103858-3109886/summary.json`. | Do not promote or retry high `124..140` as a local guarded table. Like the prior low-cluster probes, it consumes binary/layout headroom and regresses visible cadence; VISITOR3 needs size-neutral generated scheduler metadata or data-shape/preprocess work rather than more hand-authored table rows. |
| 2026-05-07 | `visitor3-prime224-v082` | Log-only; source restored. | After the guarded-table lane closed, test whether the next VISITOR3 improvement could come from moving more early payload coverage into setup instead of adding hot read-table rows. The probe raised VISITOR3 high setup prime to `224 KiB` and the VISITOR3 resident cap to `224 KiB`, so high coverage would extend from sectors `1..97` to `1..113` and low to `1..105`. | Temporarily changed `FG_VISITOR3_HIGH_SETUP_PRIME_WINDOW_BYTES` and `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES`, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. Layout stayed fixed and active reads dropped, but playback cadence worsened. VISITOR3 high regressed `scene_vb 1702 -> 1712`, `loop_vb 1406 -> 1410`, `overrun_vb 387 -> 389`, and `blocking_vb 293 -> 301` while `loop_reads 39 -> 37`; low regressed `scene_vb 1702 -> 1714`, `loop_vb 1405 -> 1413`, `overrun_vb 390 -> 395`, and `blocking_vb 301 -> 304` while `loop_reads 44 -> 41`. Foreground LBAs and the PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/visitor3-prime224-v082-probe/20260507-104735-3160494/summary.json`. | Do not promote or retry larger VISITOR3 setup-prime caps as a blind extension. The 224 KiB point proves that moving more reads into setup can still steal the active cadence even when layout and read count improve. Keep the accepted `192 KiB` VISITOR3 resident cap; further VISITOR3 work needs first-class scheduler ownership or a size-neutral data-shape/preprocess path. |
| 2026-05-07 | `visitor3-prepare-threshold5-v082` | Log-only; source restored. | After the guarded-table and setup-prime lanes failed to move loop timing, test whether delaying prepared-present work from held slack `4` to `5` would reserve more CD slack for VISITOR3 without changing data or read groups. | Temporarily changed `FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` from `4` to `5`, rebuilt, and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. Both tides were exact-flat on all key metrics and therefore failed `--require-improvement`: high stayed `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=293`, `prefetch_overrun_vb=7`, `loop_reads=39`, `due_misses=31`; low stayed `scene_vb=1702`, `loop_vb=1405`, `target_vb=1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`, `due_misses=33`. Artifact: `scratch/ps1-perf-iterate/visitor3-prepare-threshold5-v082-probe/20260507-105314-3192972/summary.json`. | Do not promote or retry this as a local threshold-only tweak. The current scheduler already reserves enough VISITOR3 CD slack at threshold `4`; remaining VISITOR3 work needs generated ownership metadata, a different CD-first scheduler budget, or size-neutral pack/data-shape preprocessing. |
| 2026-05-07 | `visitor3-primecap184-v082` | Log-only; source restored. | Test the opposite side of the VISITOR3 resident-cap knee after the `224 KiB` cap-up retry failed. The binary question was whether lowering the cap from `192 KiB` to `184 KiB` would reduce setup/residency overhead while preserving active-loop CD coverage. | Temporarily changed only `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES` from `192 KiB` to `184 KiB`, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. Layout stayed fixed, but reducing coverage to sectors `1..93` regressed both tides. High moved `loop_vb 1406 -> 1408`, `overrun_vb 387 -> 388`, and `blocking_vb 293 -> 295` while only lowering `prefetch_overrun_vb 7 -> 6`; low regressed `scene_vb 1702 -> 1715`, `loop_vb 1405 -> 1419`, `overrun_vb 390 -> 402`, and `blocking_vb 301 -> 311`, with `prefetch_overrun_vb` flat at `8`. Artifact: `scratch/ps1-perf-iterate/visitor3-primecap184-v082-probe/20260507-110016-3233218/summary.json`. | Do not promote or retry lower VISITOR3 resident caps as a local knob. The current `192 KiB` cap is a measured knee: less coverage exposes tight active reads, while more coverage steals cadence. Further VISITOR3 work should move to generated scheduler ownership or size-neutral pack/data-shape preprocessing. |
| 2026-05-07 | `visitor3-packtrim-v082` | Log-only; assets restored. | Test whether removing trailing zero padding from both VISITOR3 FGP3/v3 packs can improve CD footprint or cadence now that local scheduler and prime-cap knobs are exhausted. The trim is pack-only and saves bytes, but it intentionally permits layout movement. | Backed up `generated/ps1/foreground/VISITOR3.FG2` and `VIST3LOW.FG2`, ran `scripts/compact-fgp3-packs.py` on those two files (`1555450 -> 1268934` bytes each, `573032` bytes total saved), rebuilt, and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, bounded hot-symbol drift, and explicit `--allow-layout-change`. Original padded assets were restored after the strict gate failed. | Failed/no promotion. The high pack LBA stayed fixed but cadence regressed: high `scene_vb 1702 -> 1704`, `loop_vb 1406 -> 1409`, `overrun_vb 387 -> 394`, `blocking_vb 293 -> 307`, and `prefetch_overrun_vb 7 -> 11`. The low pack LBA moved `23232 -> 23092` and also regressed: low `scene_vb 1702 -> 1709`, `loop_vb 1405 -> 1412`, `overrun_vb 390 -> 397`, `blocking_vb 301 -> 308`, and `prefetch_overrun_vb 8 -> 11`. Artifact: `scratch/ps1-perf-iterate/visitor3-packtrim-v082-probe/20260507-110435-3259108/summary.json`. | Do not promote VISITOR3 pack-only padding trim. The shorter files change sector cadence/adjacency without reducing active payload and make both tides worse; keep the same-layout padded VISITOR3 packs until a pack/data-shape change reduces runtime work, not just trailing bytes. |
| 2026-05-07 | `read-candidate-closed-range-v082` | `scripts: close read candidates from all scenes` | The regenerated read-candidate matrix still surfaced FISHING3 low `253..269` as a standalone probe because the closed-range scanner used a short hard-coded scene list and omitted `fishing3`. The binary question was whether the host-side matrix should derive its closed-range scene vocabulary from the current validated scene matrix instead. | Updated `scripts/ps1-read-candidate-matrix.py` to load `scene_slug` values from `docs/ps1/performance-scene-matrix.csv`, retain a fallback list for missing inputs, and match scene names with slug boundaries while scanning `performance-experiment-log.md`; regenerated the read-candidate CSV/Markdown from the current v0.8.2 VISITOR3 baseline. | Promoted as host-side tooling. Standalone probes dropped `1 -> 0`, scheduler-or-guarded probes dropped `10 -> 9`, scheduler-owned-only rows dropped `52 -> 50`, and closed exact ranges rose `8 -> 12`. The rejected FISHING3 low `253..269` row no longer appears as a safe direct table candidate. No PS1 binary/runtime metrics changed; the speed rollup remains `0.5706%` over target / `99.6769%` target speed. | Keep. Future matrix regenerations must close ranges for every validated scene, not just the historical hot subset, before direct source-table probes are considered. With zero standalone probes left, the next performance work should target scheduler/guarded candidates, generated metadata, or data-shape preprocessing rather than raw direct read tables. |
| 2026-05-07 | `visitor3-high-group111-127-slack8-v082` | Log-only; source restored. | Test the only remaining non-closed VISITOR3 high current-window candidate from the refreshed read-candidate matrix. The candidate was high-tide `{111,127}` with `minSlackVBlanks=8`, intended to prove whether a stricter guard could collapse one short visible-gap read without stealing cadence. | Temporarily added `{111,127,8}` to `kVisitor3HighReadGroups12` and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored immediately after the strict gate failed. | Failed/no promotion. The extra row grew the PS-EXE sector bucket `215040 -> 217088` and shifted VISITOR3 pack LBAs by one sector (`22472 -> 22473`, `23232 -> 23233`). It also regressed both tides: high `scene_vb 1702 -> 1705`, `loop_vb 1406 -> 1409`, `overrun_vb 387 -> 388`, `blocking_vb 293 -> 296`, while only lowering `prefetch_overrun_vb 7 -> 6`; low `scene_vb 1702 -> 1706`, `loop_vb 1405 -> 1409`, `overrun_vb 390 -> 394`, `blocking_vb 301 -> 309`, and `prefetch_overrun_vb` stayed `8`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-group111-127-slack8-v082-probe/20260507-111837-3337966/summary.json`. | Do not promote or retry high `111..127` as a local guarded table. This closes the last remaining VISITOR3 high current-window table candidate; VISITOR3 needs generated scheduler ownership, x-band/upload-ready preprocessing, or another size-neutral data-shape change instead of more hand-authored read rows. |
| 2026-05-07 | `visitor3-xband-upload96-v082` | Log-only; source restored. | Test a runtime-only alternative to full-width dirty-row uploads before building a generated upload-ready pack format. The probe added a bounded `96 KiB` scratch-packed x-band path inside `grDrawBackground()`, using narrow `LoadImage` rectangles when row bands could be packed contiguously and falling back to the current full-width path otherwise. | Temporarily added per-band min/max-x packing and a static upload scratch buffer, then ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the structural gate failed. | Failed/no promotion. Both cases failed before emitting complete `JCPERF2` metrics, so this is a structural rejection rather than a speed comparison. The patch grew `grDrawBackground` by `+1036` bytes, crossed the PS-EXE sector bucket `215040 -> 217088`, and shifted downstream hot symbols by `+1036`, including `grUpdateDisplay` and CD streaming helpers. Artifact: `scratch/ps1-perf-iterate/visitor3-xband-upload96-v082-probe/20260507-112416-3370433/summary.json`. | Do not promote runtime scratch-packed x-band uploads. The CPU copy and code-size/layout cost is too high for the current hot path; the upload-ready lane should move x-band packing to pack generation, with runtime consuming pre-contiguous bands instead of packing tile rows every frame. |
| 2026-05-07 | `visitor3-high-remove-72-84-v082` | `ps1: drop setup-covered visitor3 high read group` | The refreshed VISITOR3 high read plan showed setup coverage `1..97`, which fully owns the old `{72,84}` grouped-read row before active playback. The binary question was whether removing that dead high-tide table row preserves all timing/layout while recovering a little source-table headroom. | Removed `{72,84,0}` from `kVisitor3HighReadGroups12`, ran a focused VISITOR3 high/low gate against `scratch/ps1-perf-iterate/visitor3-high-remove-144-160-v082-canaries/20260507-101742-2985323/summary.json`, then gated FISHING1 high, FISHING3 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low with fixed foreground LBAs and the accepted `215040` byte PS-EXE bucket. | Promoted as a source-table cleanup. Focused and broad canaries stayed exact-flat: VISITOR3 high remains `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `overrun_vb=387`, `blocking_vb=293`, `prefetch_overrun_vb=7`, `loop_reads=39`, `loop_read_vb=332`; VISITOR3 low remains `1702`, `1405/1015`, `overrun_vb=390`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`, and `loop_read_vb=341`. Pack LBAs and the PS-EXE sector bucket stayed fixed, while `foregroundPilotPlay` shrank by `4` bytes. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-high-remove-72-84-v082-probe/20260507-113129-3411340/summary.json`; canaries `scratch/ps1-perf-iterate/visitor3-high-remove-72-84-v082-canaries/20260507-113352-3426056/summary.json`. | Keep. This is not a VBlank speed win and does not change the aggregate `0.5706%` over target / `99.6769%` target speed, but it proves the old `72..84` group is setup-covered dead weight under the current `192 KiB` VISITOR3 resident cap. The current VISITOR3 high table is now `{138,162,4}`, `{170,186,0}`, and `{230,242,0}`; spend further VISITOR3 effort on generated scheduler ownership or pre-contiguous data-shape work, not more local rows. |
| 2026-05-07 | `visitor3-low-group106-122-slack8-v082` | Log-only; source restored. | Use the table-row headroom recovered by the `72..84` cleanup to test the highest-ranked remaining VISITOR3 low scheduler-owned cluster. The candidate was low-tide `{106,122}` with `minSlackVBlanks=8`, intended to prove whether a strict local guard could collapse the early low-tide read burst without visible cadence loss. | Temporarily added `{106,122,8}` before the accepted low `{170,186}` group and ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-72-84-v082-canaries/20260507-113352-3426056/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. Layout stayed fixed and both tides were exact-flat, so the `--require-improvement` gate rejected the row. High stayed `scene_vb=1702`, `loop_vb=1406`, `target_vb=1019`, `blocking_vb=293`, `prefetch_overrun_vb=7`, `loop_reads=39`, and `due_misses=31`; low stayed `scene_vb=1702`, `loop_vb=1405`, `target_vb=1015`, `blocking_vb=301`, `prefetch_overrun_vb=8`, `loop_reads=44`, and `due_misses=33`. The only measurable change was the extra row shifting hot foreground symbols by `+4` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group106-122-slack8-v082-probe/20260507-115353-3543580/summary.json`. | Do not promote or retry low `106..122` as a local guarded table. It is safe but inert under the current append path; the remaining low-tide early cluster needs first-class generated scheduler metadata or a pack/data-shape change, not another hand-authored table row. |

| 2026-05-07 | `building4-high-group398-414-v082` | Log-only; source restored. | After the VISITOR3 hand-table lane closed, test the highest-ranked non-VISITOR scheduler-or-guarded matrix row. The candidate was BUILDING4 high-tide `{398,414}` with no extra slack guard, intended to collapse the two medium-gap reads at sectors `398..406` and `406..414` under the existing BUILDING4 high `20 KiB` window policy. | Temporarily added a BUILDING4 high read-group table containing `{398,414,0}` and wired it only for high-tide BUILDING4 when the scene-local `20 KiB` stream window is active. Ran paired BUILDING4 high/low against `scratch/ps1-perf-iterate/visitor3-high-remove-72-84-v082-canaries/20260507-113352-3426056/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. The extra table/branch grew `foregroundPilotPlay` by `+108` bytes, crossed the PS-EXE bucket `215040 -> 217088`, and shifted BUILDING4 pack LBAs by one sector (`8280 -> 8281`, `9117 -> 9118`). High improved visible CD pressure (`blocking_vb 240 -> 236`, `prefetch_overrun_vb 27 -> 26`) but regressed timing (`scene_vb 3210 -> 3211`, `loop_vb 2939 -> 2940`, `overrun_vb 153 -> 154`) and left `loop_reads=94`. Low regressed more broadly: `scene_vb 3220 -> 3224`, `loop_vb 2945 -> 2949`, `overrun_vb 147 -> 151`, `blocking_vb 117 -> 125`, and `prefetch_overrun_vb 114 -> 120`. Artifact: `scratch/ps1-perf-iterate/building4-high-group398-414-v082-probe/20260507-120131-3587070/summary.json`. | Do not promote or retry BUILDING4 high `398..414` as a direct hand-authored table. Even the top balanced non-VISITOR row is not safe when added through the current source-table path because executable growth and LBA phase erase the visible-blocking benefit. Future BUILDING4 work needs size-neutral generated scheduler metadata, a table-selector shrink that creates enough binary headroom first, or pack/data-shape preprocessing rather than another direct branch. |
| 2026-05-07 | `read-group-selector-single-assign-v082` | `ps1: shrink foreground read-group selector` | After direct read-table probes started failing on executable growth and LBA phase, shrink the accepted selector shape before trying more scheduler/read metadata. The binary question was whether selecting the stream read-group table into locals and assigning the runtime fields once could reduce hot code without changing timing. | Reworked `foregroundPilotRuntimeStart()` to accumulate `streamReadGroups` and `streamReadGroupCount` in locals, then assign `gFgRuntime.streamReadGroups` and `gFgRuntime.streamReadGroupCount` once after policy selection. Ran the 13-case broad canary set against `scratch/ps1-perf-iterate/visitor3-high-remove-72-84-v082-canaries/20260507-113352-3426056/summary.json` with zero regression allowance, `95%` work identity, and bounded hot-symbol drift; refreshed the scene, preprocess, and read-candidate matrices from the accepted summary. | Promoted as code-headroom work, not a VBlank speed win. All 13 canaries stayed exact-flat on timing, pack LBAs, and the `215040` byte PS-EXE bucket: FISHING1 high, FISHING3 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low. `foregroundPilotPlay` shrank `11408 -> 11372` (`-36`), and downstream hot symbols shifted by `-36` within the gate. Artifact: `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json`. | Keep. The refreshed public matrix rollup is `0.5746%` over target / `99.6729%` target speed across `120` timing-bearing rows after replacing older canary rows with the current-layout summary; the headless methodology has still removed about `16.8` over-target points and added about `12.6` target-speed points since the compact full-matrix baseline. Next probes should use this code headroom for size-neutral scheduler/guarded metadata or generated data-shape work, not unguarded direct tables. |
| 2026-05-07 | `building4-high-group390-406-slack4-v082` | Log-only; source restored. | After the selector cleanup recovered 36 bytes of hot-path code, retry the new top read-candidate matrix row as a guarded BUILDING4 high probe instead of the previously rejected unguarded `{398,414}` direct branch. The candidate was high-tide `{390,406}` with `minSlackVBlanks=4`, intended to collapse the balanced `390..406` reads only when scheduler slack could absorb them. | Temporarily added a BUILDING4 high read-group table containing `{390,406,4}` and wired it only for high-tide BUILDING4 when the current `20 KiB` stream window is active. Ran paired BUILDING4 high/low against `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. Even with the guard, the table/branch grew `foregroundPilotPlay` by `+112`, crossed the PS-EXE bucket `215040 -> 217088`, and shifted BUILDING4 pack LBAs by one sector (`8280 -> 8281`, `9117 -> 9118`). High improved visible CD pressure (`blocking_vb 240 -> 236`, `prefetch_overrun_vb 27 -> 26`) but regressed timing (`scene_vb 3210 -> 3211`, `loop_vb 2939 -> 2940`, `overrun_vb 153 -> 154`) and left `loop_reads=94`. Low regressed `scene_vb 3220 -> 3224`, `loop_vb 2945 -> 2949`, `overrun_vb 147 -> 151`, `blocking_vb 117 -> 125`, and `prefetch_overrun_vb 114 -> 120`. Artifact: `scratch/ps1-perf-iterate/building4-high-group390-406-slack4-v082-probe/20260507-123738-3796511/summary.json`. | Do not promote or retry BUILDING4 high `390..406` as a local guarded source table. The candidate has a real visible-blocking signal but the current source-table path consumes too much binary/layout headroom and destabilizes low tide; future BUILDING4 work should be generated/size-neutral scheduler metadata or pack/data-shape preprocessing. |
| 2026-05-07 | `read-group-policy-table-v082` | Log-only; source restored. | Replace the remaining read-group selector branch chain with a compact read-group policy table, testing whether a generated/size-neutral metadata surface can add future BUILDING4/ACTIVITY9 rows without direct scene branches. | Temporarily added `TFgRuntimeReadGroupPolicy`, moved the existing ACTIVITY9, FISHING3, VISITOR3, and BUILDING2 read-group selections into `kFgRuntimeReadGroupPolicies`, and ran the 13-case broad canary set against `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json` with zero regression allowance, fixed layout identity, `95%` work identity, and bounded hot-symbol drift. Source was restored after the gate failed. | Failed/no promotion. The table shape crossed the PS-EXE sector bucket `215040 -> 217088`, shifted every tested foreground pack LBA by one sector, and regressed multiple timing rows despite some visible-CD submetric wins. VISITOR3 high regressed `1406 -> 1409` and `blocking_vb 293 -> 296`; VISITOR3 low regressed `1405 -> 1409` and `301 -> 309`; BUILDING2 high regressed `1430 -> 1438`; BUILDING4 low regressed `2945 -> 2949`; BUILDING6 high regressed `2520 -> 2521`. Artifact: `scratch/ps1-perf-iterate/read-group-policy-table-v082-canaries/20260507-124631-3847233/summary.json`. | Do not promote this C policy-table shape. The generated metadata idea is still valid, but it must be genuinely size-neutral/cold-sectioned or layout-padded; this hot C table adds enough data/code shape to erase submetric wins through LBA phase changes. |
| 2026-05-07 | `pal4-word-pair-store-v082` | Log-only; source restored. | Test whether replacing PAL4 span compositing's two halfword stores with an aligned 32-bit pair store can reduce VISITOR3 compose cost without waiting for the fuller generated upload-ready pack format. | Temporarily added an aligned word-store fast path to `grCompositePacked4OpaqueRun()`, built, then ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, bounded symbol drift, and explicit `--allow-layout-change` so the CPU signal could be measured despite the build growth. Source was restored after the strict gate failed. | Failed/no promotion. The C word-store shape grew `grCompositePacked4SpansToBackground` by `+436` bytes, crossed the PS-EXE bucket `215040 -> 217088`, and shifted VISITOR3 pack LBAs by `+1`. It also regressed both tides: high `scene_vb 1702 -> 1703`, `loop_vb 1406 -> 1407`, `overrun_vb 387 -> 388`, `blocking_vb 293 -> 295`; low `1702 -> 1709`, `1405 -> 1412`, `390 -> 397`, `blocking_vb 301 -> 310`. Artifact: `scratch/ps1-perf-iterate/pal4-word-pair-store-v082-probe/20260507-130909-3980465/summary.json`. | Do not retry this local C word-store implementation. If PAL4 pair stores are revisited, do it as generated/assembly-owned compositor work with fixed code size and pack/layout identity; the compiler-expanded C version costs more phase and code than it saves. |
| 2026-05-07 | `activity9-compact-readgroup-v082` | Log-only; source restored. | Test whether the existing ACTIVITY9 low read-group table should also apply to the current cleanup-compact FGP3/v3 pack. The baseline read plan showed `policy=none` for `ACTV9LOW.FG2` because the selector was still gated on the older FGP3/v1 pack format. | Temporarily removed the ACTIVITY9 low pack-format check so the existing `{624,636,0}` group could activate for compact packs, rebuilt at the accepted `215040` byte PS-EXE bucket, and ran ACTIVITY9 high/low against `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. Layout stayed fixed, but no key metric improved. High stayed exact-flat at `scene_vb=2343`, `loop_vb=2094`, `overrun_vb=38`, `blocking_vb=37`, `prefetch_overrun_vb=23`, and `loop_reads=52`; low stayed exact-flat at `2335`, `2087`, `31`, `42`, `12`, and `loop_reads=78`. The selector tweak grew `foregroundPilotPlay` by `+44` bytes and merely shifted downstream hot symbols. Artifact: `scratch/ps1-perf-iterate/activity9-compact-readgroup-v082-probe/20260507-131733-4028341/summary.json`. | Do not promote this stale gate removal by itself. The current ACTIVITY9 compact pack does not benefit from the old `{624,636}` row; future ACTIVITY9 work should target the current read-plan candidates such as low `514..530` or high `447..463` through a size-neutral/generated scheduler path, not this old v1-era group. |
| 2026-05-07 | `activity9-low-group514-530-v082` | Log-only; source restored. | Test the current ACTIVITY9 low read-plan's highest-ranked 16-sector scheduler-owned candidate after the stale compact-pack group proved inert. The candidate was `{514,530}` plus the existing old `{624,636}` row, with the table enabled for the current compact FGP3/v3 low pack. | Temporarily added `{514,530,0}` to `kActivity9LowFgp3ReadGroups`, removed the old v1-only pack-format gate, rebuilt, and ran ACTIVITY9 low against `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the gate failed. | Failed/no promotion. The perf build crossed the PS-EXE sector bucket `215040 -> 217088`, shifted `ACTV9LOW.FG2` LBA `3961 -> 3962`, and did not improve any key metric. Low stayed `scene_vb=2335`, `loop_vb=2087`, `blocking_vb=42`, `prefetch_overrun_vb=12`, and `loop_reads=78`, while `target_vb 2056 -> 2055` made `overrun_vb 31 -> 32`. The table/selector change grew `foregroundPilotPlay` by `+60`. Artifact: `scratch/ps1-perf-iterate/activity9-low-group514-530-v082-probe/20260507-132138-4052730/summary.json`. | Do not promote or retry ACTIVITY9 low `514..530` through the local C read-group path. The row is plausible as generated scheduler metadata, but the current source-table implementation spends too much binary/layout headroom and does not fire into a read or timing win. |
| 2026-05-07 | `activity9-dead-readgroup-prune-v082` | `ps1: prune stale activity9 read group` | Convert the failed ACTIVITY9 compact read-group probes into code headroom by deleting the stale low-tide FGP3/v1 `{624,636}` selector/table. Current `ACTV9LOW.FG2` is FGP3/v3 compact and the accepted read plan already reports `policy=none`, so the branch was dead under current assets. | Removed `kActivity9LowFgp3ReadGroups` and the ACTIVITY9 low pack-format selector branch from `foregroundPilotRuntimeStart()`. Ran the 13-case broad canary set against `scratch/ps1-perf-iterate/read-group-selector-single-assign-v082-canaries/20260507-120859-3629908/summary.json` with zero regression allowance, `95%` work identity, and bounded hot-symbol drift; refreshed the scene, preprocess, and read-candidate matrices from the accepted summary. | Promoted as code-headroom work, not a VBlank speed win. FISHING1 high, FISHING3 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, BUILDING6 high/low, and ACTIVITY9 high/low stayed exact-flat on timing, pack LBAs, and the `215040` byte PS-EXE bucket. `foregroundPilotPlay` shrank `11372 -> 11356` (`-16`), and downstream hot symbols shifted by `-16` within the gate. Artifact: `scratch/ps1-perf-iterate/activity9-dead-readgroup-prune-v082-canaries/20260507-132441-4070377/summary.json`. | Keep. Public rollup remains `0.5746%` over target / `99.6729%` target speed across `120` timing-bearing rows; the headless methodology has still removed about `16.8` over-target points and added about `12.6` target-speed points since the compact full-matrix baseline. Treat the recovered headroom as budget for generated VISITOR3 scheduler/data-shape work, not proof that more local ACTIVITY9 tables are useful. |
| 2026-05-07 | `visitor3-low-window32-v082` | Log-only; source restored. | Test whether VISITOR3 low tide can use a `32 KiB` stream window while preserving the accepted `192 KiB` setup-prime resident cap. The binary question was whether wider low-tide window refills could collapse the tight early low-tide read cluster without adding a local read table or moving pack layout. | Temporarily added a VISITOR3 low-tide `32 KiB` stream-window policy and made VISITOR3 setup-prime selection independent of the normal window size, then ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/activity9-dead-readgroup-prune-v082-canaries/20260507-132441-4070377/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored immediately after the strict gate failed. | Failed/no promotion. High tide stayed exact-flat at `scene_vb=1702`, `loop_vb=1406`, `overrun_vb=387`, `blocking_vb=293`, `prefetch_overrun_vb=7`, and `loop_reads=39`. Low tide reduced read pressure (`loop_reads 44 -> 36`, `loop_read_vb 341 -> 317`, `blocking_vb 301 -> 290`, `due_misses 33 -> 30`) but regressed visible cadence: `scene_vb 1702 -> 1710`, `loop_vb 1405 -> 1413`, `target_vb 1015 -> 1021`, `overrun_vb 390 -> 392`, and `prefetch_overrun_vb 8 -> 19`. Pack LBAs and the PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/visitor3-low-window32-v082-probe/20260507-135546-56309/summary.json`. | Do not promote VISITOR3 low `32 KiB` window sizing. The wider refill does save transactions, but it overruns held slack and loses more cadence than it gains. Keep VISITOR3 on the accepted `16 KiB` stream window; remaining VISITOR3 work should be generated scheduler ownership or data-shape/pre-contiguous payload work rather than wider runtime windows. |
| 2026-05-07 | `fgp3v4-drawcompact-all-v082` | `ps1: compact fgp3 draw metadata` | After local read-table/window/prime probes closed, test the size-neutral data-shape lane from the earlier full-compact VISITOR3 result: compact PAL4 draw row/span metadata in every current FGP3/v3 compact residual pack while preserving padded file size, CD sectors, and LBAs. | Added `scripts/compact-fgp3-pal4-draw-metadata.py`, converted `ACTV9LOW`, `BUILDING2`/`BUIL2LOW`, `BUILDING4`/`BUIL4LOW`, and `VISITOR3`/`VIST3LOW` to FGP3/v4 with input-size padding, taught the runtime and host analyzers to decode compact v4 draw tails, and removed the legacy `JCPERF scene-start` print to keep the PS-EXE sector bucket fixed. Ran VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high broad canaries against `scratch/ps1-perf-iterate/activity9-dead-readgroup-prune-v082-canaries/20260507-132441-4070377/summary.json`. | Promoted. VISITOR3 high improved `loop_vb 1406 -> 1369`, `target_vb 1019 -> 1023`, `blocking_vb 293 -> 244`, `prefetch_overrun_vb 7 -> 1`, and `loop_reads 39 -> 35`; VISITOR3 low improved `1405 -> 1376`, `1015 -> 1023`, `301 -> 253`, `8 -> 2`, and `44 -> 35`. BUILDING2 high improved `1430 -> 1405`, `1289 -> 1298`, and `blocking_vb 212 -> 176`; low improved `1429 -> 1395`, `1286 -> 1294`, and `193 -> 144`. ACTIVITY9 low improved `2087 -> 2085`, `2056 -> 2058`, and `blocking_vb 42 -> 28`; FISHING1 high control stayed under target at `1068/1074` with `blocking_vb=2`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/fgp3v4-drawcompact-all-v082-broad/20260507-143445-284827/summary.json`. | Keep as the new compact-residual baseline. Public rollup is now `0.4533%` over target / `99.7548%` target speed across 120 timing-bearing rows; since the compact full-matrix baseline, headless optimization has removed about `16.9` over-target points and added about `12.7` target-speed points. Next VISITOR3 work should target remaining v4 read-plan candidates or selective upload-ready bands; the v3 compact draw-metadata lane is exhausted because all current FGP3/v3 compact packs are now v4. |
| 2026-05-07 | `visitor3-group111-123-slack4-v083` | Log-only; source restored. | Test the top post-FGP3/v4 read-candidate row for VISITOR3: add a guarded `111..123` retained read group to both high and low tide with `minSlackVBlanks=4`, matching the prior high-tide guarded-table shape. The binary question was whether the new compact draw metadata made this balanced candidate safe enough to reduce the remaining VISITOR3 CD pressure. | Temporarily added `{111,123,4}` to `kVisitor3HighReadGroups12` and `kVisitor3LowReadGroups16`, then ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/fgp3v4-drawcompact-all-v082-broad/20260507-143445-284827/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. High tide showed a real visible/timing signal but violated the hidden refill gate: `scene_vb 1669 -> 1661`, `loop_vb 1369 -> 1361`, `overrun_vb 346 -> 338`, and `blocking_vb 244 -> 235`, but `prefetch_overrun_vb 1 -> 28`; low tide was exact-flat at `1677`, `1376/1023`, `overrun_vb=353`, `blocking_vb=253`, `prefetch_overrun_vb=2`, `loop_reads=35`, and failed `--require-improvement`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed; `foregroundPilotPlay` grew by `8` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-group111-123-slack4-v083-probe/20260507-150054-434498/summary.json`. | Do not promote VISITOR3 `111..123` as a shared local guarded table with 4-VBlank slack. The high-tide speed signal is useful, but it spends too much hidden refill slack; low tide does not benefit. Retry only as high-tide-only with a stricter slack threshold, generated scheduler-owned timing, or a pre-contiguous data-shape change that avoids the refill-overrun spike. |
| 2026-05-07 | `visitor3-high111-123-slack8-v083` | Log-only; source restored. | Follow up the shared `111..123` rejection by testing only the high-tide row with a stricter `minSlackVBlanks=8`, because high tide had the real speed/blocking signal and low tide was inert. The binary question was whether a higher local slack guard could keep the speed win while suppressing the hidden refill-overrun spike. | Temporarily added `{111,123,8}` only to `kVisitor3HighReadGroups12`, then ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/fgp3v4-drawcompact-all-v082-broad/20260507-143445-284827/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Source was restored after the strict gate failed. | Failed/no promotion. High tide repeated the same tradeoff as the shared probe: `scene_vb 1669 -> 1661`, `loop_vb 1369 -> 1361`, `overrun_vb 346 -> 338`, and `blocking_vb 244 -> 235`, but `prefetch_overrun_vb 1 -> 28`; low tide stayed exact-flat at `1677`, `1376/1023`, `overrun_vb=353`, `blocking_vb=253`, `prefetch_overrun_vb=2`, and `loop_reads=35`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed; `foregroundPilotPlay` grew by `4` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-high111-123-slack8-v083-probe/20260507-150439-456738/summary.json`. | Do not keep raising local slack for high `111..123`; the guard does not stop the hidden-overrun failure once the row fires. Close this local-table variant. The high-tide win now needs generated scheduler ownership, a different refill budget, or pre-contiguous/data-shape work that preserves the reduced loop without converting the cost into hidden prefetch overrun. |
| 2026-05-07 | `visitor3-align114-high111-123-v083` | Log-only; assets/source restored. | Test whether the rejected VISITOR3 high `111..123` row was failing because frame 114 began 77 bytes before the next sector boundary, causing the static analyzer to partially touch the following `122..135` read. The binary question was whether sector-aligning entry 114 would preserve the high-tide speed signal while eliminating the hidden refill-overrun spike. | Temporarily inserted 77 bytes of zero padding before FGP3/v4 entry 114 in `VISITOR3.FG2` and `VIST3LOW.FG2`, shifted later entry offsets and the sound table while preserving each pack's `1555450` byte file size/LBA, and added `{111,123,4}` only to `kVisitor3HighReadGroups12`. Ran paired VISITOR3 high/low against `scratch/ps1-perf-iterate/fgp3v4-drawcompact-all-v082-broad/20260507-143445-284827/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded hot-symbol drift. Binary packs and source were restored after the strict gate failed. | Failed/no promotion. The analyzer risk did improve: high and low `111..123` became `partial=0`, `overread=0`, `class=current-read-group-compatible`, and `scheduler=standalone-candidate`. Runtime behavior did not improve enough to pass: high repeated the same tradeoff (`scene_vb 1669 -> 1661`, `loop_vb 1369 -> 1361`, `blocking_vb 244 -> 235`) but still regressed `prefetch_overrun_vb 1 -> 28`; low stayed exact-flat at `1677`, `1376/1023`, `blocking_vb=253`, `prefetch_overrun_vb=2`, and failed `--require-improvement`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed; `foregroundPilotPlay` grew by `4` bytes. Artifact: `scratch/ps1-perf-iterate/visitor3-align114-high111-123-v083-probe/20260507-151202-499569/summary.json`. | Close the static-sector-overlap hypothesis for local high `111..123`. Removing the partial-touch/overread artifact makes the candidate cleaner on paper, but the runtime still overruns held refill slack once the row fires. Next VISITOR3 attempts should avoid local C read groups for `111..123` and instead test generated scheduler timing, upload/restore work reduction, or pack-side data-shape changes that lower active work without adding a held refill. |
| 2026-05-07 | `compact-u16-inline-os-v083` | Log-only; source restored. | Test whether forcing the compact FGP3/v4 span metadata reader to inline while keeping `optimize("Os")` would capture the call-overhead win without growing the hot compositor as much as a default inline helper. | Temporarily changed `grReadCompactSpanU16()` to `static inline` with `always_inline` and `optimize("Os")`, then ran VISITOR3 high/low and BUILDING2 high/low against `scratch/ps1-perf-iterate/fgp3v4-drawcompact-all-v082-broad/20260507-143445-284827/summary.json`. Source was restored to the default-inline candidate after the probe. | Failed/no promotion. The variant produced some loop wins but worse tradeoffs than default inline: VISITOR3 high only moved `1369 -> 1364` while blocking regressed `244 -> 261`; VISITOR3 low moved `1376 -> 1357` with blocking `253 -> 252`; BUILDING2 high moved `1405 -> 1388` and blocking `176 -> 126` but hidden prefetch regressed `19 -> 22`; BUILDING2 low moved `1395 -> 1389` but blocking regressed `144 -> 149`. Artifact: `scratch/ps1-perf-iterate/compact-u16-inline-os-v083-probe/20260507-152622-585672/summary.json`. | Do not promote the `always_inline` plus `-Os` shape. The compiler layout and branch costs are inferior to the plain default-inline helper under the current broad canary set. |
| 2026-05-07 | `compact-u16-inline-v083` | `ps1: inline compact span metadata decode` | Remove per-field helper-call overhead from the FGP3/v4 compact metadata path. The binary question was whether a normal inline `grReadCompactSpanU16()` can speed compact residual draw decode without moving pack LBAs or crossing the PS-EXE sector bucket. | Changed `grReadCompactSpanU16()` from a noinline `optimize("Os")` helper to default inline code. Ran a focused promotion gate for VISITOR3 high/low and BUILDING2 high/low against `scratch/ps1-perf-iterate/fgp3v4-drawcompact-all-v082-broad/20260507-143445-284827/summary.json` with `--require-improvement`, `--allow-regression 8`, `95%` work identity, and bounded hot-symbol drift; then ran broad stability with ACTIVITY9 low and FISHING1 high included. | Promoted. VISITOR3 high improves `scene_vb 1669 -> 1657`, `loop_vb 1369 -> 1357`, and `overrun_vb 346 -> 334` with blocking `244 -> 246` and prefetch overrun flat at `1`; low improves `1677 -> 1662`, `1376 -> 1361`, `353 -> 338`, and `253 -> 250`. BUILDING2 high improves `1656 -> 1645`, `1405 -> 1394`, `107 -> 93`, and `176 -> 138` with prefetch overrun `19 -> 20`; low improves `1669 -> 1659`, `1395 -> 1385`, `101 -> 82`, `144 -> 121`, and prefetch overrun `19 -> 8`. ACTIVITY9 low is timing-flat at `2085/2058` with blocking `28 -> 29`; FISHING1 high remains exact-flat under target. Pack LBAs and the `215040` byte PS-EXE bucket stay fixed; `grCompositePacked4SpansToBackground` grows `+108` bytes with downstream hot-symbol addresses within the `192` byte gate. Artifacts: focused `scratch/ps1-perf-iterate/compact-u16-inline-v083-promote-focused/20260507-153046-612345/summary.json`; broad stability `scratch/ps1-perf-iterate/compact-u16-inline-v083-broad-stability/20260507-153511-639350/summary.json`. | Keep as the new compact residual runtime baseline. Public row-average rollup improves `0.4533% -> 0.4096%` over target and `99.7548% -> 99.7860%` target speed across `120` timing-bearing rows; weighted over-target VBlanks improve `1323 -> 1263`. Since the compact full-matrix baseline, headless optimization has removed about `17.0` over-target points and added about `12.7` target-speed points. Next work should focus on generated scheduler ownership or upload-ready/selective preprocessing for VISITOR3 and BUILDING2 rather than more local C read rows for the closed `111..123` lane. |
| 2026-05-07 | `building2-group371-383-slack4-v084` | Log-only; source restored. | Test the top post-inline read-candidate row: BUILDING2 high and low `371..383` with a 4-VBlank retained-read slack guard. The row was ranked balanced/low-visible-risk in both tides and fit the current 12-sector group window, so the question was whether a guarded local row could collapse one late BUILDING2 read without the VISITOR3-style hidden-refill spike. | Temporarily added `{371,383,4}` to a new high-tide BUILDING2 read-group table and to the existing low-tide BUILDING2 table after `{318,330,0}`. Ran paired BUILDING2 high/low against `scratch/ps1-perf-iterate/compact-u16-inline-v083-broad-stability/20260507-153511-639350/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, fixed pack LBA/PS-EXE bucket, and bounded hot-symbol drift. Source was restored after the gate failed. | Failed/no promotion. Both tides were exact-flat on key metrics and therefore failed `--require-improvement`: high stayed `scene_vb=1645`, `loop_vb=1394`, `target_vb=1301`, `overrun_vb=93`, `blocking_vb=138`, `prefetch_overrun_vb=20`, and `loop_reads=68`; low stayed `1659`, `1385/1303`, `overrun_vb=82`, `blocking_vb=121`, `prefetch_overrun_vb=8`, and `loop_reads=57`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed; the extra table/selector shape actually shrank `foregroundPilotPlay` by `12` bytes but did not change runtime behavior. Artifact: `scratch/ps1-perf-iterate/building2-group371-383-slack4-v084-probe/20260507-155549-759395/summary.json`. | Do not promote BUILDING2 `371..383` as a local guarded table. It is safe but inert under the current append path. Close the exact range for both tides; remaining BUILDING2 read work should be generated scheduler ownership or larger-window/generated metadata rather than another hand-authored local row. |
| 2026-05-07 | `activity9-low-group229-245-slack4-v084` | Log-only; source restored. | Test the last post-inline scheduler-or-guarded local row: ACTIVITY9 low `229..245` with a 4-VBlank slack guard. The read plan ranked it as low-visible-risk, balanced, zero partial/overread, and current-window compatible, so this probe checked whether it could collapse one low-tide read without moving layout. | Temporarily added `kActivity9LowReadGroups16` with `{229,245,4}` and selected it only for ACTIVITY9 low under the existing `20 KiB` scene-local stream window. Ran ACTIVITY9 low plus FISHING1 high control against `scratch/ps1-perf-iterate/compact-u16-inline-v083-broad-stability/20260507-153511-639350/summary.json` with zero regression allowance, `--require-improvement`, `95%` work identity, fixed layout identity, and bounded hot-symbol drift. Source was restored after the gate failed. | Failed/no promotion. The local table crossed the PS-EXE sector bucket `215040 -> 217088`, shifted pack LBAs by one sector (`ACTV9LOW 3961 -> 3962`, `FISHING1 434 -> 435`), and did not improve any key metric. ACTIVITY9 low stayed `scene_vb=2333`, `loop_vb=2085`, `blocking_vb=29`, `prefetch_overrun_vb=12`, and `loop_reads=59`; target accounting moved `2058 -> 2057`, so overrun regressed `27 -> 28`. FISHING1 high stayed timing-flat but failed fixed-layout identity because of the executable bucket/LBA shift. Artifact: `scratch/ps1-perf-iterate/activity9-low-group229-245-slack4-v084-probe/20260507-160004-784169/summary.json`. | Do not promote ACTIVITY9 low `229..245` as a local guarded table. This closes the final scheduler-or-guarded local row in the current read-candidate matrix. Future CD work needs generated scheduler metadata, larger-window/generated ownership, or data-shape/upload preprocessing rather than more source-table rows. |
| 2026-05-07 | `compact-u16-pair-v084` | Log-only; source restored. | Test whether reading adjacent compact FGP3/v4 metadata fields as a pair could reduce branch/offset overhead beyond the promoted inline single-field reader. The binary question was whether pairing `relY/spanCount` and `relX/pixelCount` would improve VISITOR3 or BUILDING2 without changing layout. | Temporarily added `grReadCompactSpanPairU16()` and changed both compact draw and compact temporal residual restore paths to consume adjacent metadata fields through it. Ran VISITOR3 high/low and BUILDING2 high/low against `scratch/ps1-perf-iterate/compact-u16-inline-v083-broad-stability/20260507-153511-639350/summary.json` with `--require-improvement`, `--allow-regression 6`, `95%` work identity, fixed pack LBA/PS-EXE bucket, and bounded hot-symbol drift. Source was restored after the focused gate failed. | Failed/no promotion. VISITOR3 did not improve: high stayed `scene_vb=1657`, `loop_vb=1357`, and `overrun_vb=334` while blocking regressed `246 -> 250`; low regressed `scene_vb 1662 -> 1663`, `loop_vb 1361 -> 1362`, `overrun_vb 338 -> 339`, and blocking `250 -> 253`. BUILDING2 improved timing (`loop_vb 1394 -> 1390` high, `1385 -> 1383` low) and low also improved `blocking_vb 121 -> 120`, but the extra pair-reader code crossed the PS-EXE bucket `215040 -> 217088` and shifted BUILDING2 pack LBAs by `+1`. Artifact: `scratch/ps1-perf-iterate/compact-u16-pair-v084-probe/20260507-160606-819072/summary.json`. | Do not promote pair-reading in C. It is a BUILDING2 speed signal, but not layout-neutral and not a VISITOR3 improvement. Future compact metadata work needs a code-size-neutral compiler/layout shape or pack/data preprocessing that avoids adding another hot helper. |
| 2026-05-07 | `pal4-pair-store-v084` | Log-only; source restored. | Test whether aligned 32-bit two-pixel stores in the PAL4 packed span compositor can reduce palette/write overhead without changing the FGP3/v4 compact metadata format. The C-shape was intentionally small: only pair writes inside `grCompositePacked4OpaqueRun()`, with scalar fallback for odd alignment or tail pixels. | Temporarily changed the PAL4 packed-span inner loop to emit two 16-bit pixels through one aligned 32-bit store when possible, then ran VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high against `scratch/ps1-perf-iterate/compact-u16-inline-v083-broad-stability/20260507-153511-639350/summary.json` with `--require-improvement`, `--allow-regression 6`, `95%` work identity, fixed layout identity, and bounded hot-symbol drift. Source was restored after the focused/broad gate failed. | Failed/no promotion. The high-tide VISITOR3 signal was only `scene_vb 1657 -> 1656`, `loop_vb 1357 -> 1356`, `overrun_vb 334 -> 333`, with blocking flat at `246`; VISITOR3 low regressed `1662 -> 1670`, `1361 -> 1369`, `338 -> 346`, and `blocking_vb 250 -> 257`. BUILDING2 high regressed `loop_vb 1394 -> 1396`; BUILDING2 low improved `1385 -> 1383`, `blocking_vb 121 -> 119`, and `prefetch_overrun_vb 8 -> 7`. ACTIVITY9 low and FISHING1 high were effectively flat. The actual harness build crossed the PS-EXE sector bucket `215040 -> 217088`, shifted every gated pack LBA by `+1`, and moved `grCompositePacked4SpansToBackground` by `+376` bytes, exceeding the `192` byte hot-symbol drift gate even though the symbol body shrank `2888 -> 2664`. Artifact: `scratch/ps1-perf-iterate/pal4-pair-store-v084-probe/20260507-161531-874905/summary.json`. | Do not promote this PAL4 pair-store C shape. It has a small high-tide VISITOR3 and BUILDING2-low signal, but it is not layout-neutral, regresses VISITOR3 low and BUILDING2 high, and crosses both executable-bucket and hot-symbol gates. If revisited, it needs layout/padding compensation or a pack-side data-shape route that preserves the `215040` byte PS-EXE bucket. |
| 2026-05-07 | `visitor3-high-readgroup-prune-v084` | `ps1: prune stale visitor3 high read groups` | Convert exhausted VISITOR3 high retained-read rows into code headroom. The refreshed read plans after compact-u16 inline show the old high-tide table no longer changes runtime metrics; local VISITOR3 rows are closed unless generated scheduler ownership or data-shape work changes the cost model. | Removed `kVisitor3HighReadGroups12` (`138..162`, `170..186`, `230..242`) and the non-lowtide VISITOR3 selector branch, rebuilt, ran focused VISITOR3 high/low and then broad VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high against `scratch/ps1-perf-iterate/compact-u16-inline-v083-broad-stability/20260507-153511-639350/summary.json` with zero regression, `95%` work identity, fixed layout, and bounded hot symbols. Refreshed scene/read/preprocess matrices and docs/site. | Promoted as code headroom, not VBlank speed. All six broad cases stayed exact-flat. VISITOR3 high remains `1657/1357/1023`, blocking `246`, prefetch `1`, reads `35`; low remains `1662/1361/1023`, blocking `250`, prefetch `2`, reads `35`; BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high are exact-flat. Pack LBAs and the `215040` PS-EXE bucket stay fixed; `foregroundPilotPlay` shrinks by `48` bytes and downstream hot symbols shift `-48` within gate. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-high-readgroup-prune-v084-probe/20260507-162836-951815/summary.json`; broad `scratch/ps1-perf-iterate/visitor3-high-readgroup-prune-v084-broad/20260507-163049-965277/summary.json`. | Keep. This does not change the `0.4096%` over-target / `99.7860%` target-speed rollup, but recovers hot foreground code/layout budget for generated scheduler metadata or pack-side upload-ready/preprocess work. Do not re-add VISITOR3 high local tables without new generated ownership evidence. |
| 2026-05-07 | `upload-rectcap24-v085` | Log-only; source restored. | Test the smallest runtime upload-cap hypothesis from the VISITOR3 selective-preprocess lane before building a new pack format. The analyzer showed VISITOR3 only hits the 8-rect dirty-upload cap on frames `134..136`; raising `GR_MAX_UPLOAD_RECTS` to `24` should remove those cap fallbacks and save about `90,880` uploaded bytes over the loop, at the cost of more `LoadImage` rects. | Temporarily changed `GR_MAX_UPLOAD_RECTS` in `grDrawBackground()` from `8` to `24`, rebuilt, ran an initial mislabeled VISITOR3 high/low probe, then reran baseline-compatible `visitor3-high-canary` and `visitor3-low-canary` against `scratch/ps1-perf-iterate/visitor3-high-readgroup-prune-v084-broad/20260507-163049-965277/summary.json` with `--require-improvement`, `--allow-regression 6`, `95%` work identity, fixed layout, and bounded hot-symbol drift. Source was restored after the gate failed and the clean build returned to the `215040` byte PS-EXE bucket. | Failed/no promotion. Both VISITOR3 tides were exact-flat and failed `--require-improvement`: high stayed `scene_vb=1657`, `loop_vb=1357`, `target_vb=1023`, `blocking_vb=246`, `prefetch_overrun_vb=1`; low stayed `1662`, `1361/1023`, `blocking_vb=250`, and `prefetch_overrun_vb=2`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. Artifacts: mislabeled exploratory run `scratch/ps1-perf-iterate/upload-rectcap24-v085-probe/20260507-165123-1083585/summary.json`; strict baseline run `scratch/ps1-perf-iterate/upload-rectcap24-v085-probe2/20260507-165338-1096710/summary.json`. | Do not promote or retry simple dirty-upload rect-cap raises. The byte saving is too small to move VISITOR3 timing and extra rects do not pay off. Continue toward generated selective upload-ready payloads, restore-skip/coalescing, or scheduler-owned CD/render timing rather than another global upload cap tweak. |
| 2026-05-07 | `compact-restore-skip-v085` | Log-only; source restored. | Test the runtime version of the restore-minus-current idea: have the compact FGP3/v4 C parser skip restoring cleanup intervals that are covered by the current draw spans. The question was whether this could win without changing pack data. | Temporarily added runtime compact cleanup/draw interval ownership logic and ran VISITOR3 high/low against `scratch/ps1-perf-iterate/visitor3-high-readgroup-prune-v084-broad/20260507-163049-965277/summary.json`, allowing layout movement only for diagnosis. Source was restored after the binary/layout gate failed. | Failed/no promotion. VISITOR3 improved, but the route was not layout-neutral: high moved `1357 -> 1344`, low `1361 -> 1337`, while `build-ps1/jcreborn.exe` grew `215040 -> 217088`, VISITOR3 pack LBAs shifted `22472/23232 -> 22473/23233`, and hot symbols moved by about `+1000/+1316` bytes. High blocking also regressed `246 -> 250`; low blocking improved `250 -> 245`. Artifact: `scratch/ps1-perf-iterate/compact-restore-skip-v085-probe/20260507-165933-1130662/summary.json`. | Do not promote the runtime C skip path. The signal is real, but code growth and LBA drift violate the binary-identity gate. Use a pack-side transform that removes the same redundant cleanup work without changing executable layout. |
| 2026-05-07 | `visitor3-restore-minus-current-v086` | `ps1: remove redundant visitor3 restore spans` | Promote the pack-side form of restore-minus-current: subtract each frame's current PAL4 draw intervals from that frame's compact cleanup prefix in the VISITOR3 FGP3/v4 packs. This keeps draw pixels byte-identical, keeps the padded `1555450` byte pack footprint, and avoids the runtime C code growth that failed in `compact-restore-skip-v085`. | Added `scripts/compact-fgp3-restore-minus-current.py`, regenerated `VISITOR3.FG2` and `VIST3LOW.FG2` from the saved V084 baseline packs with input-size padding, verified the script reproduces the committed binary packs byte-for-byte, ran focused VISITOR3 high/low promotion, then ran broad VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high with fixed layout identity. Refreshed scene/read/preprocess matrices, README, docs, and site sources. | Promoted. VISITOR3 high improves `scene_vb 1657 -> 1439`, `loop_vb 1357 -> 1139`, `target_vb 1023 -> 1024`, `overrun_vb 334 -> 115`, `blocking_vb 246 -> 191`, `prefetch_overrun_vb 1 -> 0`, `loop_reads 35 -> 33`, and `loop_read_vb 266 -> 201`; low improves `1662 -> 1441`, `1361 -> 1140`, `1023 -> 1024`, `338 -> 116`, `250 -> 194`, `2 -> 0`, `35 -> 33`, and `271 -> 203`. Active payload drops `981514 -> 737600` per tide; runtime `restore_bytes` drops `973290 -> 498676`. BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high stayed exact-flat. Pack LBAs stay fixed at `22472/23232`, and the PS-EXE bucket stays `215040`. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-restore-minus-current-v086-probe/20260507-171044-1193221/summary.json`; broad `scratch/ps1-perf-iterate/visitor3-restore-minus-current-v086-broad/20260507-171251-1205609/summary.json`. | Keep as the new baseline. Public rollup improves from `0.4096%` over target / `99.7860%` target speed to `0.0502%` / `100.0292%` across `120` timing-bearing rows, eliminating the last red rows. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.35` over-target points and added about `12.93` target-speed points. Next targets are WALKSTUF1 high/low, BUILDING4 high/low, remaining VISITOR3 scheduler-owned groups, BUILDING2, and BUILDING6. |
| 2026-05-07 | `visitor3-group97-109-v087` | Log-only; source restored. | Close the refreshed top VISITOR3 read-candidate range after the V086 pack shrink. The current read matrix ranked `97..109` for both tides as current-window compatible and scheduler/guarded, so the binary question was whether a guarded or unguarded local read-group append could now collapse one duplicate read without regressing the new baseline. | Temporarily added `97..109` to VISITOR3 high and low read groups, first with a `4` VBlank slack guard and then unguarded. Each variant was rebuilt and run against `scratch/ps1-perf-iterate/visitor3-restore-minus-current-v086-broad/20260507-171251-1205609/summary.json` with VISITOR3 high/low, fixed pack LBAs, same `215040` PS-EXE bucket, zero regression allowance, and `--require-improvement`. Source was restored after both gates failed. | Failed/no promotion. Both variants were exact-flat on key timing and read count: high stayed `scene_vb=1439`, `loop_vb=1139`, `target_vb=1024`, `blocking_vb=191`, `prefetch_overrun_vb=0`, and `loop_reads=33`; low stayed `1441`, `1140/1024`, `blocking_vb=194`, `prefetch_overrun_vb=0`, and `loop_reads=33`. The grouped append increased uncovered sectors `534 -> 539`, loop read time `201/203 -> 203/205`, left `group_hits=0`, and grew `foregroundPilotPlay` by `20` bytes without consuming the matrix candidate. Artifacts: guarded `scratch/ps1-perf-iterate/visitor3-group97-109-slack4-v087-probe/20260507-181026-1524344/summary.json`; unguarded `scratch/ps1-perf-iterate/visitor3-group97-109-unguarded-v087-probe/20260507-181248-1538359/summary.json`. | Do not promote or retry VISITOR3 `97..109` through the current local grouped-append path. The candidate remains a planner signal but needs generated scheduler ownership or a pack-layout/data-shape change; local table rows add CD sectors and code size without reducing visible blocking. |
| 2026-05-07 | `building4-restore-minus-current-v087` | `ps1: remove redundant building4 restore spans` | Apply the successful VISITOR3 pack-side restore-minus-current transform to the remaining BUILDING4 restore-heavy outlier. The hypothesis was that subtracting each current frame's draw spans from its compact cleanup prefix would reduce visible restore/CD pressure without adding runtime code or moving the padded pack footprint. | Saved same-commit BUILDING4 high/low pack baselines, transformed `generated/ps1/foreground/BUILDING4.FG2` and `generated/ps1/foreground/BUIL4LOW.FG2` with `scripts/compact-fgp3-restore-minus-current.py --pad-to-input-size`, verified focused BUILDING4 high/low against `scratch/ps1-perf-iterate/building4-current-v087-baseline/20260507-181725-1564538/summary.json`, then ran VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high broad canaries. Refreshed the scene, read-candidate, and preprocess matrices plus README/docs/site. | Promoted. Both packs keep the `1714154` byte CD footprint and fixed LBAs (`8280/9117`) while active payload drops `1032442 -> 855284`, cleanup spans `148456 -> 61263`, cleanup pixels `614939 -> 273475`, and pack restore bytes `1229878 -> 546950`. BUILDING4 high improves `loop_vb 2939 -> 2844`, `target_vb 2786 -> 2816`, `overrun_vb 153 -> 28`, `blocking_vb 240 -> 37`, `prefetch_overrun_vb 27 -> 30`, and `loop_reads 81 -> 49`; low improves `2945 -> 2855`, `2798 -> 2815`, `147 -> 40`, `117 -> 46`, `114 -> 38`, and `39 -> 30`. VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, and FISHING1 high controls stayed on accepted timing/layout profiles. Artifacts: focused `scratch/ps1-perf-iterate/building4-restore-minus-current-v087-probe/20260507-182043-1583655/summary.json`; broad `scratch/ps1-perf-iterate/building4-restore-minus-current-v087-broad/20260507-183004-1637867/summary.json`. | Keep as the new baseline. Public rollup improves from `0.0502%` over target / `100.0292%` target speed to `-0.0193%` / `100.0943%` across `120` timing-bearing rows. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.42` over-target points and added about `12.99` target-speed points. Next true outliers are WALKSTUF1 high/low, VISITOR3 high/low, BUILDING2 high/low, BUILDING6 high/low, and selective upload-ready preprocessing. |
| 2026-05-07 | `visitor3-carry-forward-draw-v088` | Log-only; source restored. | Test whether VISITOR3 still had pack-side draw pixels that could be carried forward from the previous foreground frame after the V086 restore-minus-current promotion. The binary question was whether same-coordinate, same-palette PAL4 pixels in adjacent frames could be removed from the current draw tail while the previous framebuffer/tile value remained valid, reducing compose payload without runtime code or LBA movement. | Built a temporary host-only FGP3/v4 analyzer/transform and ran it against same-commit VISITOR3 high/low pack copies saved under `scratch/ps1-perf-iterate/visitor3-carry-forward-draw-v088-assets/`. The transform compared each current draw pixel against the previous frame's foreground pixel at the same screen coordinate, split surviving runs, and padded outputs to the original `1555450` byte pack size. No emulator run was needed because the transform produced no binary payload delta. | Failed/no promotion. Both VISITOR3 packs were deterministic no-ops: active payload stayed `737600 -> 737600`, draw spans stayed `129323 -> 129323`, draw pixels stayed `652876 -> 652876`, and draw payload bytes stayed `673318 -> 673318` for high and low. The current FGP3/v4 draw stream is already not carrying adjacent-frame identical foreground pixels at the same coordinate. | Close this pack-side carry-forward lane for current VISITOR3 assets. Remaining VISITOR3 work should stay on scheduler-owned CD timing, upload-ready/selective x-band data, or broader scene outliers; do not spend runtime code on carry-forward detection. |
| 2026-05-07 | `visitor3-24group-fill-v088` | Log-only; source restored. | Test the generated/larger-window version of the remaining VISITOR3 `97..121` read-plan signal. The local `97..109` grouped append was inert because the grouped window never fired, so this probe let read groups expand fresh window fills as well as append extensions and raised the group capacity to 24 sectors. Follow-up variants tested a stricter slack guard and a due-only mode that disabled speculative prefetch grouping. | Temporarily changed `FG_PREFETCH_GROUP_WINDOW_BYTES` from `16` to `24` sectors, added a shared VISITOR3 `{97,121}` read group, and let `fgRuntimeFillWindowForEntry()` apply group expansion to fresh fills. Ran VISITOR3 high/low against the BUILDING4 v087 broad baseline. Then retried the same group with `minSlackVBlanks=16`, and finally a due-only split where grouped expansion ran only for non-prefetch fills. Source was restored after all three variants failed. | Failed/no promotion. The full grouped-fill variants had a real speed signal but converted the cost into hidden-overrun failure: high improved `loop_vb 1139 -> 1134` and `blocking_vb 191 -> 142`, low improved `1140 -> 1133` and `194 -> 141`, but `prefetch_overrun_vb` regressed `0 -> 79` high and `0 -> 78` low. The slack-16 variant was identical, proving the guard did not avoid the overrun. Due-only kept `prefetch_overrun_vb=0` but regressed high `loop_vb 1139 -> 1152` and `blocking_vb 191 -> 197`, left low timing flat at `1140`, and crossed the PS-EXE bucket `215040 -> 217088`. Artifacts: `scratch/ps1-perf-iterate/visitor3-24group-fill-v088-probe/20260507-190309-1823611/summary.json`, `scratch/ps1-perf-iterate/visitor3-24group-fill-slack16-v088-probe/20260507-190527-1837721/summary.json`, and `scratch/ps1-perf-iterate/visitor3-24group-due-only-v088-probe/20260507-190750-1852270/summary.json`. | Do not promote VISITOR3 `97..121` through this runtime grouped-fill shape. The read-saving signal is real, but it must be scheduler-owned so the long refill is placed where it does not create `78+` VBlanks of overrun; due-only grouping loses cadence and grows code. Next VISITOR3 read probes should try smaller grouped fills such as `97..113`, or a generated schedule rather than simply widening the runtime window. |
| 2026-05-07 | `visitor3-16group-fill-v088` | Log-only; source restored. | Test the smaller VISITOR3 `97..113` read-plan candidate after the 24-sector grouped-fill probe proved the read-saving signal but created hidden prefetch overrun. The binary question was whether staying inside the existing 16-sector group capacity would collapse one duplicate read without widening the runtime window. | Temporarily added a VISITOR3 high `{97,113,4}` read group, prepended the same guarded row to the existing low-tide VISITOR3 group table, selected VISITOR3 groups for both tides, and let `fgRuntimeFillWindowForEntry()` expand fresh fills through read-group metadata. Ran VISITOR3 high/low against the BUILDING4 v087 broad baseline with `--require-improvement`, `--allow-regression 6`, fixed layout identity, `95%` work identity, and bounded hot-symbol drift. Source was restored after the gate failed. | Failed/no promotion. The smaller fill avoided the `78+` VBlank prefetch-overrun spike, but high tide regressed `loop_vb 1139 -> 1152`, `overrun_vb 115 -> 128`, and `blocking_vb 191 -> 197` with no key metric improved. Low tide stayed timing-flat at `loop_vb=1140` while blocking improved only `194 -> 189`. The added source crossed the PS-EXE bucket `215040 -> 217088`, shifted VISITOR3 pack LBAs by `+1`, and failed fixed-layout identity. Artifact: `scratch/ps1-perf-iterate/visitor3-16group-fill-v088-probe/20260507-191328-1884839/summary.json`. | Do not promote VISITOR3 `97..113` through runtime grouped fresh-fill expansion. This closes the smaller local-group version too: the remaining VISITOR3 CD signal needs generated scheduler ownership or a pack/layout data-shape change, not more C table rows that alter cadence and executable layout. |
| 2026-05-07 | `visitor3-low-readgroup-prune-v088` | `ps1: prune stale visitor3 low read group` | Convert the last stale VISITOR3 low local read-table row into code headroom after the current restore-minus-current baseline and grouped-fill probes proved local VISITOR3 C rows are exhausted. The goal was zero timing/layout movement while shrinking hot foreground code for future generated scheduler or pack-side work. | Removed `kVisitor3LowReadGroups16` (`170..186`) and the VISITOR3 low selector branch, rebuilt, ran a focused VISITOR3/BUILDING2/ACTIVITY9/FISHING1 gate, then ran a broad VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high gate against the BUILDING4 v087 baseline. Refreshed scene, read-candidate, and preprocess matrices plus README/docs/site. | Promoted as code headroom, not a VBlank speed win. All eight broad cases stayed exact-flat on loop timing, blocking, hidden prefetch-overrun, pack LBAs, and the `215040` byte PS-EXE bucket. VISITOR3 high stays `1139/1024`, blocking `191`, prefetch `0`; VISITOR3 low stays `1140/1024`, blocking `194`, prefetch `0`; BUILDING2, BUILDING4, ACTIVITY9 low, and FISHING1 high all stayed on accepted profiles. `foregroundPilotPlay` shrinks by `36` bytes and downstream hot symbols shift `-36` within gate. Artifacts: focused `scratch/ps1-perf-iterate/visitor3-low-readgroup-prune-v088-probe/20260507-191959-1922260/summary.json`; broad `scratch/ps1-perf-iterate/visitor3-low-readgroup-prune-v088-broad/20260507-192704-1964869/summary.json`. | Keep. No public rollup change: the matrix remains `-0.0193%` over target / `100.0943%` target speed across `120` timing-bearing rows. Since the compact full-matrix baseline was about `+17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.42` over-target points and added about `12.99` target-speed points. This closes stale VISITOR3 local read-table state; next VISITOR3 work needs scheduler-owned generated metadata or selective pack/preprocess data. |
| 2026-05-07 | `visitor3-pal4-pairlut-v089` | Log-only; source restored. | Test whether the existing PAL4 byte-pair LUT can reduce VISITOR3 compact-residual compose cost without the rejected aligned pair-store branch shape. Unlike `pal4-pair-store-v084`, this probe only replaces the two palette-index loads in `grCompositePacked4OpaqueRun()` with one `palLutSierra` lookup and keeps scalar halfword writes. | Temporarily used `palLutSierra[packed]` for two-pixel PAL4 spans in `src/graphics_ps1.c`, rebuilt, and ran VISITOR3 high/low against the current `visitor3-low-readgroup-prune-v088` baseline. Source was restored after manual comparison showed the gate was non-promotable; the run labels did not match the baseline labels, so the harness reported clean case gates but did not enforce baseline comparison. | Failed/no promotion. High only moved visible blocking `191 -> 188` while `loop_vb` stayed `1139/1024`; low regressed `scene_vb 1441 -> 1443` and `loop_vb 1140 -> 1142` with blocking flat at `194`. Prefetch overrun remained `0` and loop reads stayed `33` for both tides. Artifact: `scratch/ps1-perf-iterate/visitor3-pal4-pairlut-v089-probe/20260507-195042-2101813/summary.json`. | Do not promote this PAL4 pair-LUT halfword-write shape. The existing pair table is not enough to buy a loop win and it regresses low tide; future PAL4 compose work still needs generated aligned command classes, pack-side direct16/selective data, or layout-neutral assembly/codegen rather than another local C inner-loop swap. |
| 2026-05-07 | `walkstuf1-high-primecap144-v089` | `ps1: prime more walkstuf1 high setup data` | Promote a high-tide-only midpoint setup-prime cap after WALKSTUF1 became the top outlier and generic padded-FGP3/direct local group lanes were closed. The binary question was whether high tide could use more resident setup coverage without repeating the rejected shared `160 KiB` cap or stealing low-tide cadence. | Added `FG_WALKSTUF1_HIGH_SETUP_PRIME_MAX_RESIDENT_BYTES` at `144 KiB`, used it only for `walkstuf1` high tide, and updated `scripts/ps1-foreground-read-plan.py` so host planning mirrors the new cap. Ran a focused high/low detail probe against `scratch/ps1-perf-iterate/walkstuf1-current-detail-v089/20260507-195607-2133504/summary.json`, then gated VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high against the current broad baseline. | Promoted. WALKSTUF1 high keeps full scene time flat at `1880`, improves `loop_vb 1595 -> 1592`, `target_vb 1402 -> 1406`, `overrun_vb 193 -> 186`, `blocking_vb 277 -> 275`, `loop_reads 136 -> 134`, `loop_read_vb 602 -> 586`, and `due_misses 56 -> 55`; setup coverage increases to sectors `2..74` and setup bytes rise only `396852 -> 413236`. Low tide stays exact-flat at `1895`, `1604/1407`, blocking `271`, prefetch `54`, and `loop_reads=132`. All broad controls stayed exact-flat with fixed pack LBAs and the `215040` byte PS-EXE bucket. Artifacts: focused `scratch/ps1-perf-iterate/walkstuf1-high-primecap144-v089-probe/20260507-200051-2161059/summary.json`; controls `scratch/ps1-perf-iterate/walkstuf1-high-primecap144-v089-controls/20260507-200356-2179194/summary.json`. | Keep as a high-tide-only setup-prime cap retune. Public rollup improves from `-0.0193%` over target / `100.0943%` target speed to `-0.0231%` / `100.0972%` across `120` timing-bearing rows. Since the compact full-matrix baseline was about `+17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.42` over-target points and added about `13.00` target-speed points. Next targets remain WALKSTUF1 low/high scheduler/data-shape work, VISITOR3 generated scheduling or selective preprocessing, BUILDING2, and BUILDING6. |
| 2026-05-07 | `walkstuf1-high-primecap152-v090` | Log-only; source restored. | Test whether the successful high-tide-only WALKSTUF1 setup-prime cap had more headroom above the promoted `144 KiB` midpoint. The binary question was whether `152 KiB` could capture another read/setup segment before the previously rejected shared `160 KiB` cadence problem reappeared. | Temporarily changed `FG_WALKSTUF1_HIGH_SETUP_PRIME_MAX_RESIDENT_BYTES` from `144 KiB` to `152 KiB` and ran WALKSTUF1 high/low against the promoted `walkstuf1-high-primecap144-v089` focused baseline with zero regression allowance and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. High setup coverage extended to sectors `2..76` and nominal loop reads dropped `134 -> 133`, but cadence regressed: `scene_vb 1880 -> 1884`, `loop_vb 1592 -> 1595`, `target_vb 1406 -> 1405`, `overrun_vb 186 -> 190`, `blocking_vb 275 -> 286`, and `due_misses 55 -> 57`; prefetch overrun improved only `51 -> 49`. Low stayed exact-flat at `1895`, `1604/1407`, `blocking_vb=271`, `prefetch_overrun_vb=54`, and `due_misses=50`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-primecap152-v090-probe/20260507-202938-2326606/summary.json`. | Keep the `144 KiB` high cap as the measured knee. A wider high setup-prime window trades one read for worse visible blocking and loop cadence, so the next WALKSTUF1 work should move to scheduler-owned grouped reads, selective data-shape/preprocessing, or a shrinking encoder rather than raising the high setup cap again. |
| 2026-05-07 | `walkstuf1-low-primecap168-v090` | Log-only; source restored. | Test whether the promoted low-tide-only WALKSTUF1 setup-prime cap had more headroom above `160 KiB` after high tide found its own `144 KiB` knee. The binary question was whether low tide could absorb one more setup segment without changing the validated pack layout or active-loop cadence. | Temporarily changed `FG_WALKSTUF1_LOW_SETUP_PRIME_MAX_RESIDENT_BYTES` from `160 KiB` to `168 KiB` and ran WALKSTUF1 low against the promoted `walkstuf1-high-primecap144-v089` focused baseline with zero regression allowance and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. Setup coverage extended to sectors `2..78`, but the run was key-flat and failed `--require-improvement`: `scene_vb=1895`, `loop_vb=1604`, `target_vb=1407`, `overrun_vb=197`, `blocking_vb=271`, `prefetch_overrun_vb=54`, `loop_reads=132`, and `due_misses=50`. Pack LBA `25494` and the `215040` byte PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-primecap168-v090-primary/20260507-203954-2385459/summary.json`. | Keep the `160 KiB` low cap as the measured low-tide setup-prime ceiling for the current data shape. More contiguous cap does not buy another visible/read win, so WALKSTUF1 now needs scheduler-owned grouped reads, selective upload-ready/data-shape preprocessing, or a shrinking encoder rather than more scalar setup-prime cap raises. |
| 2026-05-07 | `walkstuf1-low-no-direct-stage-v091` | Log-only; source restored. | Test whether WALKSTUF1 low-tide's remaining blocking comes from the tight-slack direct-stage shortcut bypassing the resident stream window for small entries. The binary question was whether forcing WALKSTUF1 through the stream-window path at the minimum slack point would reduce duplicate visible CD work. | Temporarily excluded `walkstuf1` from the `slack == FG_PREFETCH_WINDOW_MIN_SLACK_VBLANKS && dataSize <= 8 KiB` direct-stage branch, then ran WALKSTUF1 low against the promoted `walkstuf1-high-primecap144-v089` focused baseline with zero regression allowance, fixed-layout identity, and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion, but useful signal. Low improved visible cadence metrics: `scene_vb 1895 -> 1892`, `loop_vb 1604 -> 1601`, `overrun_vb 197 -> 194`, `blocking_vb 271 -> 214`, `loop_reads 132 -> 129`, and `due_misses 50 -> 34`. The cost was non-promotable: hidden prefetch overrun regressed `54 -> 79`, the PS-EXE bucket crossed `215040 -> 217088`, pack LBA shifted `25494 -> 25495`, and `foregroundPilotPlay` grew by `116` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-no-direct-stage-v091-primary/20260507-204733-2428726/summary.json`. | Do not promote this branch as-is. It proves the remaining WALKSTUF1 low gap is partly scheduler ownership, not just cap size, but the fix must be code-size-neutral and overrun-aware: generated scheduler metadata, a narrower no-direct-stage guard, or selective pack/data-shape work that reduces refill cost before moving these reads into the hidden path. |
| 2026-05-07 | `direct-stage-cap4-v092` | Log-only; source restored. | Test whether the no-direct-stage WALKSTUF1 signal could be captured without a hot scene-name branch by lowering the global direct-stage shortcut threshold from `8 KiB` to `4 KiB`. The binary question was whether fewer small direct reads would reduce visible blocking while preserving executable layout and avoiding the broad no-direct-stage hidden-overrun spike. | Temporarily changed `FG_PREFETCH_DIRECT_STAGE_MAX_BYTES` from `8 KiB` to `4 KiB` and ran WALKSTUF1 low against the promoted `walkstuf1-high-primecap144-v089` focused baseline with zero regression allowance, fixed layout, and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. Layout stayed fixed, but the timing trade was worse than the broad scene-specific probe: `scene_vb 1895 -> 1898`, `loop_vb 1604 -> 1607`, `target_vb 1407 -> 1405`, and `overrun_vb 197 -> 202` all regressed. Visible blocking improved `271 -> 233`, loop reads dropped `132 -> 129`, and due misses improved `50 -> 36`, but hidden prefetch overrun regressed `54 -> 81`. Artifact: `scratch/ps1-perf-iterate/direct-stage-cap4-v092-walkstuf1-low-primary/20260507-205133-2451713/summary.json`. | Keep the global direct-stage cap at `8 KiB`. The scalar threshold moves work from visible blocking into hidden refill and worsens active-loop cadence; the next WALKSTUF1 scheduler attempt needs frame/range-specific generated ownership or pack/data-shape cost reduction, not a smaller global byte cap. |
| 2026-05-07 | `visitor3-selective-upload-footprint-v093` | Log-only; no runtime source. | Size-gate the current default VISITOR3 selective upload-ready/x-band plan before adding a new runtime format. The binary question was whether the selected non-cap frame payload could fit inside the existing padded VISITOR3 pack footprint, preserving FG LBAs and avoiding a layout-moving experiment. | Regenerated the FGP3 preprocess detail sheets for `VISITOR3.FG2` and `VIST3LOW.FG2` with `scripts/analyze-fg2-preprocess-plans.py`, refreshed `docs/ps1/performance-preprocess-visitor3-hotspots.csv` from the current high-tide pack, and measured each pack's padded zero-tail slack. No emulator run was needed because the footprint gate failed before a runtime probe. Artifacts: `scratch/visitor3-high-preprocess-v093.json`, `scratch/visitor3-low-preprocess-v093.json`, and `scratch/visitor3-high-preprocess-doc-refresh-v093.json`. | Failed/no promotion as a layout-neutral default plan. High and low are symmetric: the threshold plan selects `96 / 144` frames, keeps cap-hit frames `134..136` on the old path, estimates `8575360` selected full-width upload bytes, `2460792` selected x-band bytes, `6114568` bytes saved (`71.3%`), `160` selected x-band rects, and `2462072` payload+rect metadata bytes. Each current pack is `1555450` bytes with last nonzero byte at `740603`, leaving only `814847` bytes of zero-tail slack, so the default upload-ready payload exceeds layout-neutral slack by `1647225` bytes per pack. | Do not implement the default VISITOR3 selected upload-ready plan as a same-footprint append. The next VISITOR3 graphics lane needs a smaller budgeted subset, compressed upload-ready payloads, a shrinking pack transform that buys at least `1.65 MiB` of slack per tide, or an explicit layout-moving experiment with full canaries. The public battle-card baseline remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-budgeted-upload-target-v094` | `ps1: add budgeted preprocess target` | Convert the failed default VISITOR3 upload-ready footprint gate into an exact same-footprint implementation target before touching runtime code. The binary question was whether the profitable selected rows could be reduced enough to fit the existing pack padding while preserving most of the modeled upload-byte win. | Added pack zero-tail slack reporting and an exact 0/1 knapsack option to `scripts/analyze-fg2-preprocess-plans.py` via `--selective-budget-from-zero-tail` / `--selective-max-payload-bytes`, refreshed the VISITOR3 high/low preprocess JSON outputs, regenerated `docs/ps1/performance-preprocess-visitor3-hotspots.csv` with a new `budgeted_selective_plan` column, and refreshed the preprocess matrix plus README/docs/site. Artifacts: `scratch/visitor3-high-preprocess-budget-v094.json`, `scratch/visitor3-low-preprocess-budget-v094.json`, and `scratch/visitor3-high-preprocess-doc-refresh-v094.json`. | Promoted as host-side tooling/target, not runtime behavior. High and low are symmetric: the exact budget selects `74 / 96` default-selected frames, uses `814184` payload+rect bytes inside the `814847` byte zero-tail slack, leaves `663` bytes free, excludes `22` default-selected rows by budget, and retains `3858104` modeled upload bytes saved (`63.1%` of the default threshold plan's savings). | Keep this as the next VISITOR3 same-footprint upload-ready target. Runtime promotion still needs generated pre-contiguous upload rows or an equivalent safe pack format, and must pass VISITOR3 high/low plus broad canaries before changing the speed baseline. Public battle-card rollup is unchanged at `-0.0231%` over target / `100.0972%` target speed; since the compact full-matrix baseline was about `+17.4%` over target / `87.1%` target speed, headless has still removed about `17.42` over-target points and added about `13.00` target-speed points. |
| 2026-05-07 | `visitor3-noop-prune-v095` | Log-only; source restored. | Test whether pruning visually no-op VISITOR3 FGP3/v4 entries could reduce active frame count and compose payload without changing pack size or executable layout. The binary question was whether removing entries that draw no visible pixels, while preserving sound-trigger sentinels, would convert the remaining VISITOR3 gap into a layout-neutral speed win. | Temporarily pruned VISITOR3 high/low entries in host-visible no-op ranges `42..48`, `50..55`, and `160..164`, preserving sound-event sentinels at sources `44` and `161`, then padded the packs back to their original `1555450` byte size. The transform removed `16` entries, changing active frames `144 -> 128`, render entries `145 -> 129`, and payload `737600 -> 659318` while keeping pack LBAs `22472/23232` and the `215040` byte PS-EXE bucket fixed. Source and packs were restored after the gate failed. Artifact: `scratch/ps1-perf-iterate/visitor3-noop-prune-v095-probe/20260507-212259-2627789/summary.json`. | Failed/no promotion. High improved `scene_vb 1439 -> 1415`, `loop_vb 1139 -> 1115`, `overrun_vb 115 -> 87`, `blocking_vb 191 -> 123`, `loop_reads 33 -> 29`, and `due_misses 31 -> 12`, but hidden prefetch overrun regressed `0 -> 56`. Low improved only modestly, `1441 -> 1436`, `1140 -> 1135`, `116 -> 107`, `194 -> 159`, `33 -> 29`, and `31 -> 22`, while hidden prefetch overrun regressed `0 -> 17`. | Do not promote entry pruning as a standalone VISITOR3 optimization. The visible-work signal is real, but shortening the cadence removes slack that the current hidden prefetch schedule was using, so it trades active work for refill debt. Keep the budgeted upload-ready target and generated scheduler ownership as the next VISITOR3 lanes. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-upload-draw-covered-v096` | Log-only; analyzer guard promoted. | Before implementing the budgeted upload-ready runtime path, test the core safety requirement for raw pack-emitted upload pixels: an aligned x-band payload can be emitted from foreground data alone only if every pixel in the upload rectangle is covered by current opaque draw spans. Otherwise the payload would have to bake restored background/cleanup pixels, which are dynamic at runtime. | Added draw-covered x-band feasibility accounting to `scripts/analyze-fg2-preprocess-plans.py`, regenerated the VISITOR3 high/low preprocess JSON outputs, refreshed `docs/ps1/performance-preprocess-visitor3-hotspots.csv` with `xband_exact_align4_draw_covered_*` columns, and refreshed the preprocess matrix plus README/docs/site. Artifacts: `scratch/visitor3-high-preprocess-safety-v096.json` and `scratch/visitor3-low-preprocess-safety-v096.json`. | Failed/no runtime promotion. High and low are symmetric: the default `96`-frame selective plan and the same-footprint `74`-frame budgeted plan both report `0` selected draw-covered x-band bytes, `0` selected draw-covered x-band rects, and `0` all-draw-covered selected frames. The apparent `3858104` modeled upload-byte win therefore depends on restored background/cleanup pixels, not just foreground draw pixels available in the pack. | Do not implement VISITOR3 raw foreground-only upload-ready payloads under the current FGP3 data. The budgeted target remains useful as a byte/cost ceiling, but runtime promotion needs a safe pixel source, compression plus explicit background ownership, generated scheduler ownership, or a different data shape. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-zero-noop-empty-hold-v097` | Log-only; tooling guard promoted. | Test whether the rejected VISITOR3 no-op entry-prune speed signal could be recast safely by preserving entry/deadline cadence and turning only truly visual-empty FGP3/v4 payloads into empty holds. The binary question was whether any current high/low payload entries have zero cleanup pixels and zero draw pixels. | Added `scripts/compact-fgp3-zero-noop-entries.py`, which parses FGP3/v4 compact residual payloads, counts cleanup and draw pixels, and can emit a same-size pack with visual-empty payload entries zeroed. Ran it on `generated/ps1/foreground/VISITOR3.FG2` and `generated/ps1/foreground/VIST3LOW.FG2` with `--pad-to-input-size`; generated packs were verified byte-identical to the saved baseline before logging. Scratch outputs: `scratch/visitor3-zero-noop-v097-high.FG2` and `scratch/visitor3-zero-noop-v097-low.FG2`. | Failed/no runtime promotion. Both current VISITOR3 packs have `0` eligible entries: high and low stayed `1555450 -> 1555450` bytes, `active_payload 737600 -> 737600`, `zeroed_entries=0`, `zeroed_bytes=0`, and empty `zeroed_sources`. No emulator run was needed because the transform produced no binary payload delta. | Do not pursue VISITOR3 no-op empty holds under the current data shape. The earlier prune win removed entries that still carry real cleanup/draw pixels, so making that idea safe requires generated scheduler ownership, budgeted upload-ready data with a safe pixel source, or a different pack transform. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-setupseg97-109-v098` | Log-only; source restored. | Test whether the top VISITOR3 `97..109` scheduler-owned read-plan candidate could fire as a setup-owned persistent segment instead of the already-rejected local grouped-append path. The binary question was whether preloading those `12` sectors into an owned setup cache would collapse the two observed active reads without moving foreground LBAs or the executable sector bucket. | Temporarily added a VISITOR3-only `97..109` setup segment, a dedicated `24 KiB` segment buffer, and persistent segment lifetime so later direct scratch reads would not invalidate the owned cache. Ran VISITOR3 high/low against the current `walkstuf1-high-primecap144-v089` control baseline with zero regression allowance and `--require-improvement`. Source was restored after the gate failed. | Failed/no promotion. Both tides were exact-flat with fixed layout: high stayed `scene_vb=1439`, `loop_vb=1139/1024`, `overrun_vb=115`, `blocking_vb=191`, `prefetch_overrun_vb=0`, `loop_reads=33`, and `due_misses=31`; low stayed `1441`, `1140/1024`, `overrun_vb=116`, `blocking_vb=194`, `prefetch_overrun_vb=0`, `loop_reads=33`, and `due_misses=31`. Pack LBAs stayed `22472/23232` and the PS-EXE bucket stayed `215040`. Artifact: `scratch/ps1-perf-iterate/visitor3-setupseg97-109-v098-probe/20260507-220852-2886344/summary.json`. | Do not retry VISITOR3 `97..109` as either a local grouped read or a setup-owned segment under the current runtime. The planner candidate is still visible in read reports, but the measured paths are inert; VISITOR3 now needs generated scheduler ownership that changes actual refill timing, a safe data-shape/payload reduction, or a different pack transform. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-sector-align-large-v099` | Log-only; packs restored. | Test whether the current VISITOR3 padded zero-tail slack could be spent on sector-aligning the large late FGP3/v4 payloads, reducing unaligned active read sectors without changing pack file sizes, foreground LBAs, executable bytes, runtime code, or entry cadence. | Temporarily rewrote `VISITOR3.FG2` and `VIST3LOW.FG2` in place, preserving the original `1555450` byte footprint, inserting `38957` bytes of internal zero padding, and moving the `32` large late payload entries onto sector boundaries. Saved and restored byte-identical pack baselines under `scratch/visitor3-sector-align-v099/`. Ran VISITOR3 high/low against the current `walkstuf1-high-primecap144-v089` control baseline with zero regression allowance and `--require-improvement`. | Failed/no promotion. The transform did reduce read-plan uncovered sectors `306 -> 294`, but it worsened actual cadence: high regressed `scene_vb 1439 -> 1443`, `loop_vb 1139 -> 1143`, and `overrun_vb 115 -> 119` with blocking flat at `191`; low regressed `1441 -> 1452`, `1140 -> 1151`, `116 -> 127`, and `blocking_vb 194 -> 197`. Prefetch overrun stayed `0`, loop reads stayed `33`, pack LBAs stayed `22472/23232`, and the PS-EXE bucket stayed `215040`. Artifact: `scratch/ps1-perf-iterate/visitor3-sector-align-v099-probe/20260507-221859-2944029/summary.json`. | Do not use blanket sector alignment for VISITOR3 large payloads. Byte/sector savings are not enough; the current measured payload offset phase is part of the CD cadence. Future payload-shape work needs a costed scheduler/pack planner that preserves timing phase, or a real payload reduction that lowers read duration without inserting phase-shifting padding. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-packed-draw-v100` | Log-only; source and packs restored. | Test a real payload-shrinking VISITOR3 data shape after local read grouping, setup segments, no-op pruning, and sector padding were exhausted. The probe encoded FGP3/v4 PAL4 draw spans as a proposed FGP3/v7 delta-from-previous-span-end stream, packing most `(gap,count)` pairs into one byte while preserving cleanup metadata, pixels, sound events, and the outer pack footprint. | Temporarily added a v7 runtime decoder and converted only `VISITOR3.FG2` / `VIST3LOW.FG2`, preserving each `1555450` byte file. The transform reduced active payload `737600 -> 659455` per tide and draw metadata `303124 -> 224979`, with `101552` packed spans and `27771` escaped spans. The focused fixed-layout gate failed because the PS-EXE bucket crossed `215040 -> 217088` and VISITOR3 pack LBAs shifted by `+1`; the layout-allowed focused gate then passed, so a broad layout-allowed canary was run. Source and packs were restored afterward, and the rebuild returned to the `215040` byte bucket. | Failed/no promotion. VISITOR3 itself was strong: high improved `1439 -> 1427`, `1139 -> 1127`, overrun `115 -> 103`, blocking `191 -> 166`, reads `33 -> 32`, and prefetch overrun stayed `0`; low improved `1441 -> 1425`, `1140 -> 1124`, overrun `116 -> 100`, blocking `194 -> 163`, reads `33 -> 32`, and prefetch overrun stayed `0`. But the broad canary rejected the layout-moving version: BUILDING2 high regressed `1394 -> 1402`, blocking `138 -> 147`, prefetch overrun `20 -> 31`; BUILDING2 low regressed `1385 -> 1389` and prefetch `8 -> 10`; BUILDING4 high regressed `2844 -> 2847` and prefetch `30 -> 33`. Artifacts: focused fixed-layout `scratch/ps1-perf-iterate/visitor3-packed-draw-v100-probe/20260507-223025-3008423/summary.json`, layout-allowed focused `scratch/ps1-perf-iterate/visitor3-packed-draw-v100-layout-probe/20260507-223535-3038424/summary.json`, broad `scratch/ps1-perf-iterate/visitor3-packed-draw-v100-broad/20260507-223756-3052207/summary.json`. | Do not promote this v7 C-runtime shape. The payload-reduction signal is real, but spending an executable sector shifts every following foreground LBA and perturbs CD phase enough to regress other top outliers. A retry needs to be layout-neutral: reclaim at least one PS-EXE sector first, make the decoder size-neutral/asm/codegen, or apply an equivalent pack shrink without moving preceding disc layout. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-packeddelta-v101` | Log-only; source and packs restored. | Retry the packed-draw byte-reduction signal as a layout-neutral FGP3/v4 variant instead of a new v7 pack format. The binary question was whether a payload-local high-bit marker plus a small row-local `(gap,count)` draw-tail decoder could preserve pack LBAs and the `215040` byte PS-EXE bucket while keeping the VISITOR3 speed win. | Temporarily encoded only `VISITOR3.FG2` and `VIST3LOW.FG2`, preserving each `1555450` byte file. The transform reduced active payload `737600 -> 659751` per tide, with `101552` packed spans and `27771` escaped spans. The first runtime shape still crossed `215040 -> 217088`; a narrower payload-marker shape plus function-scoped `-Os` on `grCompositePacked4SpansToBackground()` restored the executable bucket to `215040`, so VISITOR3 high/low were run against the current `walkstuf1-high-primecap144-v089` baseline with fixed layout, zero regression allowance, and `--require-improvement`. Source and packs were restored after the gate failed. | Failed/no promotion. Layout identity was fixed (`VISITOR3` LBA `22472`, `VIST3LOW` LBA `23232`, PS-EXE bucket `215040`), and low tide improved strongly: `scene_vb 1441 -> 1436`, `loop_vb 1140 -> 1135`, overrun `116 -> 111`, blocking `194 -> 154`, reads `33 -> 32`, prefetch overrun `0 -> 0`. High tide rejected the experiment despite lower CD pressure: `scene_vb 1439 -> 1448`, `loop_vb 1139 -> 1148`, overrun `115 -> 124`, while blocking improved `191 -> 164`, reads `33 -> 32`, and prefetch overrun stayed `0`. Artifact: `scratch/ps1-perf-iterate/visitor3-packeddelta-v101-probe/20260507-230236-3193558/summary.json`. | Do not promote the layout-neutral packed-draw delta shape with function-scoped PAL4 span `-Os`. The byte/CD signal is real, but the compiler/code-shape trade moves high-tide compose cadence enough to lose the accepted timing gate. Future VISITOR3 payload work needs a true size-neutral decoder, assembly/codegen that preserves the hot PAL4 span phase, or generated scheduler ownership rather than more C-side packed draw metadata. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-origin-shift-v102` | Log-only; no runtime source. | Try a zero-runtime-code VISITOR3 payload shrink after the C-side packed-draw routes failed. The idea was to shift each FGP3/v4 entry origin and subtract that shift from compact cleanup/draw coordinates, hoping to turn extended compact-u16 fields back into one-byte fields while preserving pixels, holds, pack sizes, LBAs, and PS-EXE bytes. | Ran a host-side parser over current `VISITOR3.FG2` and `VIST3LOW.FG2`, testing legal per-entry X/Y origin shifts against the existing compact cleanup and draw coordinate streams. No pack was written because the best legal shift saved no bytes. Artifact: `scratch/visitor3-origin-shift-v102/analysis.txt`. | Failed/no promotion before emulator. Both tides are already compact under this coordinate basis: high stayed `active_payload 737600 -> 737600`, `saved=0`, `changed_entries=0`; low stayed `737600 -> 737600`, `saved=0`, `changed_entries=0`. | Do not pursue VISITOR3 entry-origin recentering under the current FGP3/v4 packs. It does not buy payload or CD duration, so the battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. Continue with scheduler-owned refill timing, a truly size-neutral compact decoder, or a different pack data shape. |
| 2026-05-07 | `visitor3-dedupe-v103` | Log-only; packs restored. | Test whether exact duplicate FGP3/v4 payload bodies in VISITOR3 could be table-reused without runtime code. The first variant removed only four small duplicate bodies before the first uncovered `97..121` cluster and inserted equivalent padding before entry `113` to preserve the large ship-frame sector phase; the second variant removed all duplicate bodies and let later payloads shift earlier. | Saved byte-identical VISITOR3 high/low pack baselines under `scratch/visitor3-dedupe-v103/`, rewrote only `VISITOR3.FG2` and `VIST3LOW.FG2`, and kept the padded `1555450` byte pack footprints, foreground LBAs, sound offsets, PS-EXE bucket, and runtime source fixed. The phase-preserving variant removed `5006` active bytes and moved entries `111..112` into setup-prime coverage; the full variant removed `144068` active bytes and moved active payload end `740572 -> 596504`. Both variants were run against the current `walkstuf1-high-primecap144-v089` control baseline with zero regression allowance and `--require-improvement`, then the original packs were restored byte-for-byte. | Failed/no promotion. Phase-preserving dedupe cut one read but regressed high `scene_vb 1439 -> 1440`, `loop_vb 1139 -> 1140`, and overrun `115 -> 116` while blocking only improved `191 -> 190`; low regressed hard to `1464`, `1163/1024`, overrun `139`, and blocking `206`. Full dedupe also cut reads `33 -> 32` and uncovered sectors `306 -> 297`, but worsened high to `1458`, `1158/1024`, overrun `134`, blocking `208`, and low to `1466`, `1165/1024`, overrun `141`, blocking `213`. Artifacts: phase `scratch/ps1-perf-iterate/visitor3-dedupe-phase-v103-probe/20260507-232350-3313380/summary.json`; full `scratch/ps1-perf-iterate/visitor3-dedupe-full-v103-probe/20260507-232623-3328380/summary.json`. | Do not promote duplicate-payload table reuse for VISITOR3 under the current scheduler. Even layout-neutral byte reductions and one fewer read can destroy the measured CD cadence; this lane needs a scheduler-costed pack planner before retry. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-07 | `visitor3-offscreen-clip-v104` | Log-only; source and packs restored. | Test whether VISITOR3 could get a zero-runtime pack-side win by removing offscreen PAL4 compact residual work that the runtime compositor already clips at draw time. The binary question was whether data-size-preserving or data-size-shrinking clipping could reduce active draw/restore work without changing pixels, pack LBAs, the `215040` byte PS-EXE bucket, or hidden refill debt. | Saved byte-identical VISITOR3 high/low pack baselines under `scratch/visitor3-offscreen-clip-v104/`. The size-shrinking variant clipped cleanup and draw spans to the 640x480 screen, reducing logical payload `737600 -> 655911` per tide (`81689` bytes) while preserving pack file size and LBA; it then tried a VISITOR3 exact-stage runtime variant that stayed in the PS-EXE bucket. A safer data-size-preserving variant kept every entry offset/size unchanged and zero-filled the clipped tail, and a draw-only follow-up kept cleanup intact. Source and packs were restored after the gates failed. | Failed/no promotion. The size-shrinking pack had a strong visible signal but converted work into hidden refill debt: high improved `scene_vb 1439 -> 1426`, `loop_vb 1139 -> 1126`, and `blocking_vb 191 -> 132`, but `prefetch_overrun_vb 0 -> 72`; low improved only `1441 -> 1439` / `1140 -> 1138`, `blocking_vb 194 -> 143`, and `prefetch_overrun_vb 0 -> 77`. Exact-stage did not fix it (`prefetch_overrun_vb 73/78`). Data-size-preserving compose-only avoided hidden overrun and improved high `1139 -> 1137`, but low deterministically regressed `scene_vb 1441 -> 1452`, `loop_vb 1140 -> 1151`, and `blocking_vb 194 -> 200`; draw-only repeated the same low regression. Artifacts: shrink `scratch/ps1-perf-iterate/visitor3-offscreen-clip-phase-v104-probe/20260507-233614-3384165/summary.json`; exact-stage `scratch/ps1-perf-iterate/visitor3-offscreen-clip-exactstage-v104-probe/20260507-233957-3405980/summary.json`; compose-only `scratch/ps1-perf-iterate/visitor3-offscreen-clip-composeonly-v104-probe/20260507-234642-3444510/summary.json`; draw-only `scratch/ps1-perf-iterate/visitor3-offscreen-drawonly-v104-probe/20260507-234945-3462339/summary.json`; repeat `scratch/ps1-perf-iterate/visitor3-offscreen-drawonly-v104-repeat/20260507-235155-3475099/summary.json`. | Do not promote VISITOR3 offscreen clipping as a standalone pack transform. Runtime clipping already makes these pixels invisible, but changing payload bytes or entry sizes perturbs the measured low-tide CD/compositor phase enough to fail the strict two-tide gate. Revisit only with a scheduler-costed pack planner that can preserve low-tide phase, or with a visual/data-shape change that proves both tides and broad canaries. Public battle-card rollup remains `-0.0231%` over target / `100.0972%` target speed. |
| 2026-05-08 | `visitor3-high-offscreen-drawclip-v105` | `ps1: clip visitor3 high offscreen draw spans` | Promote the safe half of the v104 signal: apply the data-size-preserving offscreen draw clip only to high-tide `VISITOR3.FG2`, leaving low tide untouched after the both-tide variant repeatedly regressed low. The binary question was whether high could keep the `1139 -> 1137` win without requiring low to accept the bad phase. | Rewrote only `generated/ps1/foreground/VISITOR3.FG2`, preserving the `1555450` byte pack footprint, all entry sizes and offsets, foreground LBA `22472`, and the `215040` byte PS-EXE bucket. The transform trims offscreen draw work in `17` entries, leaves every source frame and sound offset in place, changes logical parsed payload `737600 -> 661437`, and records `91271` draw-trim pixels while zero-filling clipped tails. Artifacts: analysis `scratch/visitor3-high-offscreen-drawclip-v105/analysis.txt`, focused high gate `scratch/ps1-perf-iterate/visitor3-high-offscreen-drawclip-v105-highpass/20260508-000023-3523514/summary.json`, and broad controls `scratch/ps1-perf-iterate/visitor3-high-offscreen-drawclip-v105-broad/20260508-000136-3530675/summary.json`. | Promoted. VISITOR3 high improves `scene_vb 1439 -> 1437`, `loop_vb 1139 -> 1137`, `overrun_vb 115 -> 113`, and `blocking_vb 191 -> 190`; `prefetch_overrun_vb` stays `0`, loop reads stay `33`, pack LBA stays `22472`, and the PS-EXE bucket stays `215040`. VISITOR3 low stays exact-flat at `1441`, `1140/1024`, `blocking_vb=194`, `prefetch_overrun_vb=0`, and LBA `23232`. BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high controls stayed exact-flat. | Keep this as the new VISITOR3 high baseline and keep low-side offscreen clipping closed until a scheduler-costed pack planner can preserve low-tide phase. Public battle-card rollup moves to `-0.0249%` over target / `100.0985%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.42` over-target points and added about `13.00` target-speed points. |
| 2026-05-08 | `visitor3-low-offscreen-exitright-v106` | `ps1: clip visitor3 low exit-right draw spans` | Localize the v104/v105 low-side regression by splitting the low-tide draw-only offscreen trim into early-left, ship-left, exit-right, and ship-plus-exit subsets. The binary question was whether any low-side subset could safely keep the entry-size-preserving draw-tail trim without reproducing the bad `1151/1024` low cadence. | Generated size-preserving `VIST3LOW.FG2` variants under `scratch/visitor3-low-offscreen-subsets-v106/`, preserving the `1555450` byte pack footprint, all entry sizes and offsets, foreground LBA `23232`, and the `215040` byte PS-EXE bucket. Promoted only exit-right entries `139..143`, changing logical parsed payload `737600 -> 698335`, zero-filling `39265` tail bytes, and recording `44911` draw-trim pixels. Artifacts: analysis `scratch/visitor3-low-offscreen-subsets-v106/analysis.txt`; subset sweeps `scratch/ps1-perf-iterate/visitor3-low-offscreen-subset-v106-early-left/20260508-002348-3656657/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-offscreen-subset-v106-ship-left/20260508-002442-3662084/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-offscreen-subset-v106-exit-right/20260508-002535-3667483/summary.json`, and `scratch/ps1-perf-iterate/visitor3-low-offscreen-subset-v106-ship-and-exit/20260508-002629-3672902/summary.json`; high check `scratch/ps1-perf-iterate/visitor3-low-offscreen-exitright-v106-highcheck/20260508-002751-3680960/summary.json`; broad controls `scratch/ps1-perf-iterate/visitor3-low-offscreen-exitright-v106-broad/20260508-002928-3690399/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1441 -> 1439`, `loop_vb 1140 -> 1138`, `overrun_vb 116 -> 114`, and `blocking_vb 194 -> 191`; `prefetch_overrun_vb` stays `0`, loop reads stay `33`, pack LBA stays `23232`, and the PS-EXE bucket stays `215040`. VISITOR3 high stays exact-flat at `1437`, `1137/1024`, `blocking_vb=190`, `prefetch_overrun_vb=0`, and LBA `22472`. BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high controls stayed exact-flat. Early-left was exact-flat and failed `--require-improvement`; ship-left and ship-plus-exit both failed by reproducing the low `1151/1024` regression. | Keep the low exit-right subset as the new VISITOR3 low baseline. Keep low `ship-left` closed until a scheduler-costed pack planner can preserve low-tide phase. Public battle-card rollup moves to `-0.0266%` over target / `100.0998%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.43` over-target points and added about `13.00` target-speed points. |
| 2026-05-08 | `visitor3-cleanup-offscreen-v107` | Log-only; packs restored. | Test the remaining half of the offscreen-cleanup signal after high draw tails and low exit-right draw tails were promoted. The binary question was whether clipping only offscreen FGP3/v4 cleanup spans, while keeping draw pixels and every entry size/offset fixed, could reduce restore parser work without touching the fragile low `ship-left` draw phase. | Generated same-footprint cleanup-only variants under `scratch/visitor3-cleanup-offscreen-v107/`. The all-entry transform preserved both `1555450` byte pack footprints, foreground LBAs `22472/23232`, and the `215040` byte PS-EXE bucket, zero-filling `5526` bytes per pack while trimming `58513` cleanup pixels and `1299` cleanup spans. Also generated ship-focused subsets for later reference, but the all-entry high/low focused gates were enough to decide promotion. Artifacts: analysis `scratch/visitor3-cleanup-offscreen-v107/analysis.txt`; high gate `scratch/ps1-perf-iterate/visitor3-cleanup-offscreen-v107-high/20260508-005408-3830031/summary.json`; low gate `scratch/ps1-perf-iterate/visitor3-cleanup-offscreen-v107-low/20260508-005523-3837281/summary.json`. | Failed/no promotion. VISITOR3 high was exact-flat against v106: `scene_vb=1437`, `loop_vb=1137/1024`, `overrun_vb=113`, `blocking_vb=190`, `prefetch_overrun_vb=0`, `loop_reads=33`, LBA `22472`. VISITOR3 low was exact-flat against v106: `scene_vb=1439`, `loop_vb=1138/1024`, `overrun_vb=114`, `blocking_vb=191`, `prefetch_overrun_vb=0`, `loop_reads=33`, LBA `23232`. Both strict runs failed only the `--require-improvement` gate. | Do not promote or retry cleanup-only offscreen clipping under the current FGP3/v4 VISITOR3 packs. It is safe but inert at VBlank/work-volume resolution, so the baseline remains `visitor3-low-offscreen-exitright-v106` with public rollup `-0.0266%` over target / `100.0998%` target speed. Continue with scheduler-owned refill timing, a truly size-neutral compact draw/parser shape, or safe pack-time upload/restore metadata. |
| 2026-05-08 | `building2-high-restore-minus-current-v108` | `ps1: compact building2 high cleanup spans` | Apply the already-promoted cleanup-minus-current-draw transform to BUILDING2 after VISITOR3 standalone lanes were exhausted and BUILDING2 remained a top outlier. The binary question was whether BUILDING2 could get the same pack-side restore/data-shape win while preserving pack footprint, foreground LBAs, and hidden prefetch cadence. | Generated same-footprint high/low variants under `scratch/building2-restore-minus-current-v108/` with `scripts/compact-fgp3-restore-minus-current.py --pad-to-input-size`. Both candidate packs preserve the `1303332` byte file size; active payload drops `789906 -> 674798`, cleanup spans `109076 -> 53563`, cleanup pixels `468636 -> 237286`, and modeled restore bytes `937272 -> 474572`. The both-tide focused gate rejected low because hidden prefetch overrun regressed, so low was restored and the high-only split was gated. Artifacts: both-tide focused gate `scratch/ps1-perf-iterate/building2-restore-minus-current-v108-focused/20260508-010159-3874442/summary.json`; high-only focused pass `scratch/ps1-perf-iterate/building2-restore-minus-current-v108-highonly-pass/20260508-010624-3900616/summary.json`; broad controls `scratch/ps1-perf-iterate/building2-restore-minus-current-v108-highonly-broad/20260508-010735-3907687/summary.json`. | Promoted high-only. BUILDING2 high improves `scene_vb 1645 -> 1603`, `loop_vb 1394 -> 1353`, `target_vb 1301 -> 1311`, `overrun_vb 93 -> 42`, `blocking_vb 138 -> 56`, and `loop_reads 68 -> 62`; `prefetch_overrun_vb` stays `20`, pack LBA stays `6180`, and the PS-EXE bucket stays `215040`. BUILDING2 low stays exact-flat at `1659`, `1385/1303`, `blocking_vb=121`, `prefetch_overrun_vb=8`, and LBA `6817`. VISITOR3 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high controls stayed exact-flat. | Keep only the high-tide BUILDING2 restore-minus-current pack. The both-tide transform is closed until scheduler ownership can absorb low-tide hidden refill (`prefetch_overrun_vb 8 -> 13`). Public battle-card rollup moves to `-0.0595%` over target / `100.1296%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.46` over-target points and added about `13.03` target-speed points. |
| 2026-05-08 | `building2-high-group60-72-v109` | `ps1: add building2 high read group` | Promote the only current read-matrix row that still qualified as `scheduler-or-guarded-probe` after the v108 BUILDING2 pack pass. The binary question was whether a high-tide-only retained stream group for file sectors `60..72` could collapse one append read without repeating the visible-cadence failures from earlier hand-authored read groups. | Added `kBuilding2HighReadGroups12[] = {60, 72, 0}` and selected it only for non-lowtide BUILDING2 while leaving the existing low-tide `{318,330}` table intact. Ran a focused strict gate, then a broad VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high gate against the current v108 broad baseline. Artifacts: focused `scratch/ps1-perf-iterate/building2-high-group60-72-v109-focused/20260508-013349-4056267/summary.json`; broad controls `scratch/ps1-perf-iterate/building2-high-group60-72-v109-broad/20260508-013506-4063937/summary.json`. | Promoted. BUILDING2 high improves `scene_vb 1603 -> 1599`, `loop_vb 1353 -> 1349`, `target_vb 1311 -> 1316`, `overrun_vb 42 -> 33`, `blocking_vb 56 -> 48`, `prefetch_overrun_vb 20 -> 12`, and `loop_reads 62 -> 61`. Pack LBA stays `6180`, the PS-EXE bucket stays `215040`, and `foregroundPilotPlay` grows only `12` bytes. VISITOR3 high/low, BUILDING2 low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high controls stayed exact-flat. | Keep this as the new BUILDING2 high baseline and mark the `60..72` read-matrix row closed by promotion. Public battle-card rollup moves to `-0.0650%` over target / `100.1351%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.47` over-target points and added about `13.04` target-speed points. Remaining direct read candidates are now scheduler-owned only; next targets are WALKSTUF1 low/high, remaining VISITOR3 scheduler/data-shape work, BUILDING2 low, and BUILDING6. |
| 2026-05-08 | `building2-low-group365-381-v110` | `ps1: add building2 low read group` | Promote the top remaining BUILDING2 low candidate after v109 exhausted guarded rows and left only scheduler-owned direct read candidates. The binary question was whether the low-tide `365..381` retained read group could collapse two append reads despite one-sector overread and partial overlap risk without changing broad canaries. | Added `{365, 381, 0}` to `kBuilding2LowReadGroups12[]` beside the existing `{318, 330, 0}` low-tide table, leaving the high-tide `60..72` table unchanged. Ran a focused strict BUILDING2 low gate, then a broad VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high gate against the v109 broad baseline. Artifacts: focused `scratch/ps1-perf-iterate/building2-low-group365-381-v110-focused/20260508-015214-4161565/summary.json`; broad controls `scratch/ps1-perf-iterate/building2-low-group365-381-v110-broad/20260508-015344-4170683/summary.json`. | Promoted. BUILDING2 low improves `scene_vb 1659 -> 1657`, `loop_vb 1385 -> 1383`, `target_vb 1303 -> 1304`, `overrun_vb 82 -> 79`, `blocking_vb 121 -> 118`, `prefetch_overrun_vb 8 -> 5`, `loop_reads 57 -> 55`, and `due_misses 23 -> 22`. Pack LBA stays `6817`, the PS-EXE bucket stays `215040`, and `foregroundPilotPlay` grows only `8` bytes versus v109. VISITOR3 high/low, BUILDING2 high, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high controls stayed exact-flat. | Keep this as the new BUILDING2 low baseline and treat the remaining BUILDING2 read candidates as scheduler-owned unless a fresh matrix row proves standalone safety. Public battle-card rollup moves to `-0.0670%` over target / `100.1368%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.47` over-target points and added about `13.04` target-speed points. Next targets remain WALKSTUF1 low/high, VISITOR3 scheduler/data-shape work, remaining BUILDING2 scheduler-owned rows, and BUILDING6. |
| 2026-05-08 | `graphics-composite-os-v111` | `ps1: size-optimize background composite helpers` | Test whether cold/non-FGP3 background composite helpers can be scoped to `-Os` to reclaim binary/code-shape headroom without touching the active FGP3 packed-span compositor. The binary question was whether layout-preserved code shrink could improve VISITOR3 without moving pack LBAs or the `215040` byte PS-EXE bucket. | Marked `grCompositeToBackground()` and `grCompositeToBackgroundFlip()` with `__attribute__((optimize("Os")))`; built and ran a VISITOR3 high/low plus BUILDING2, BUILDING4, ACTIVITY9 low, and FISHING1 high broad gate against v110. Artifacts: focused precheck `scratch/ps1-perf-iterate/graphics-composite-os-v111-focused/20260508-022323-144294/summary.json`; broad controls `scratch/ps1-perf-iterate/graphics-composite-os-v111-broad/20260508-022537-157560/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1439 -> 1436`, `loop_vb 1138 -> 1135`, `overrun_vb 114 -> 111`, `blocking_vb 191 -> 184`, and `loop_read_vb 200 -> 194`; `prefetch_overrun_vb=0`, loop reads stay `33`, pack LBA stays `23232`, and PS-EXE bucket stays `215040`. VISITOR3 high, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high stayed exact-flat. ELF shrinks `960556 -> 951708`; helper sizes shrink `0xbf4 -> 0x5b0` and `0xc60 -> 0x63c`; packed FGP3 compositor size stays unchanged. | Keep this as the new VISITOR3 low baseline and a code-headroom win. The strict script reported flat controls as `--require-improvement` failures, but no canary regressed and the only timing delta was VISITOR3 low. Public battle-card rollup moves to `-0.0694%` over target / `100.1388%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.47` over-target points and added about `13.04` target-speed points. |
| 2026-05-08 | `clean-copy-word-v112` | Log-only; source reverted. | Test the next restore-loop code-shape idea after the VISITOR3 read-group lane closed: replace hot clean-background span `memcpy()` calls with a bounded helper that uses 32-bit copies only when source and destination have matching 4-byte alignment, falling back to unrolled halfword copies otherwise. The binary question was whether VISITOR3's many FGP3 residual clean-rect span restores would improve without moving pack LBAs or the `215040` byte PS-EXE bucket. | Added a temporary inline `grCopyCleanSpan16()` and routed `grCleanRectCopyIn()` plus `grRestoreCleanBgSpanFromRects()` through it. Built and ran a strict VISITOR3 high/low focused gate against the v111 broad baseline with zero regression allowance and `--require-improvement`, then reverted the source. Artifact: `scratch/ps1-perf-iterate/clean-copy-word-v112-focused/20260508-025531-327513/summary.json`. | Failed/no promotion. VISITOR3 high stayed exact-flat at `scene_vb=1437`, `loop_vb=1137/1024`, `overrun_vb=113`, `blocking_vb=190`, `prefetch_overrun_vb=0`, `loop_reads=33`, `loop_read_vb=200`, LBA `22472`. VISITOR3 low stayed exact-flat at `scene_vb=1436`, `loop_vb=1135/1024`, `overrun_vb=111`, `blocking_vb=184`, `prefetch_overrun_vb=0`, `loop_reads=33`, `loop_read_vb=194`, LBA `23232`. The PS-EXE bucket stayed `215040`, but ELF grew `951708 -> 957328`. | Do not promote local word-stride clean-span copies in this form. The current libc/compiler `memcpy()` shape is already VBlank-flat for VISITOR3, and the helper only adds code size/address movement. Continue VISITOR3 with scheduler-owned CD timing or generated selective/compressed upload-ready data rather than local restore-copy microcode. |
| 2026-05-08 | `graphics-clean-restore-os-v112` | Log-only; source reverted. | Test whether the clean-background restore helpers could be scoped to `-Os` after the background composite helper win. The binary question was whether shrinking `grCleanRectCopyIn()` and `grRestoreCleanBgSpanFromRects()` would preserve layout while reducing VISITOR3 restore/code-shape pressure. | Temporarily added function-scoped `__attribute__((optimize("Os")))` to both helpers, built, and ran a strict VISITOR3 high/low focused gate against the v111 broad baseline with zero regression allowance and `--require-improvement`, then reverted the source. Artifact: `scratch/ps1-perf-iterate/graphics-clean-restore-os-v112-focused/20260508-030350-374571/summary.json`. | Failed/no promotion. The build shrank PS-EXE `215040 -> 212992` and ELF `951708 -> 949300`, but that moved foreground pack LBAs by `-1` sector and regressed timing. VISITOR3 high moved `scene_vb 1437 -> 1438`, `loop_vb 1137 -> 1138`, and `overrun_vb 113 -> 114` while only improving blocking `190 -> 189`. VISITOR3 low regressed `scene_vb 1436 -> 1440`, `loop_vb 1135 -> 1139`, `overrun_vb 111 -> 115`, and `blocking_vb 184 -> 191`. | Do not scope these clean-restore helpers to `-Os` as a standalone change. The shrink crosses an executable-sector boundary and shifts CD phase enough to hurt VISITOR3, so future clean-restore/code-shape work needs explicit layout pinning or a larger same-bucket speed signal. Continue VISITOR3 with scheduler-owned CD timing or generated selective/compressed data-shape work. |
| 2026-05-08 | `graphics-clean-restore-os-pinned-v113` | Log-only; source reverted. | Isolate whether the failed clean-restore `-Os` probe was caused by foreground pack LBA movement or by the helper/code-address shape itself. The binary question was whether manually padding the smaller executable back to the v111 `215040` byte sector bucket, preserving foreground LBAs, would recover the timing while retaining the smaller helper code shape. | Re-applied function-scoped `optimize("Os")` to `grCleanRectCopyIn()` and `grRestoreCleanBgSpanFromRects()`, built, manually padded `build-ps1/jcreborn.exe` from `212992` back to `215040`, and ran a strict VISITOR3 high/low gate with `--skip-build` against the v111 broad baseline. Source was reverted after the gate failed. Artifact: `scratch/ps1-perf-iterate/graphics-clean-restore-os-pinned-v113-focused/20260508-030954-410000/summary.json`. | Failed/no promotion. Layout pinning worked: VISITOR3 high LBA stayed `22472`, low LBA stayed `23232`, and PS-EXE bucket stayed `215040`. Timing still regressed exactly enough to fail: high moved `1437 -> 1438`, `1137/1024 -> 1138/1024`, and overrun `113 -> 114` while blocking improved only `190 -> 189`; low moved `1436 -> 1440`, `1135/1024 -> 1139/1024`, overrun `111 -> 115`, and blocking `184 -> 191`. | Close clean-restore helper `-Os` even with layout pinning. The failure is hot-code address/codegen sensitivity, not just CD sector placement. Do not retry this lane unless a later rewrite gives a direct restore-work reduction large enough to dominate VISITOR3 low's four-VBlank regression. |
| 2026-05-08 | `visitor3-no-direct-stage-v114` | Log-only; source reverted. | Test whether VISITOR3's exact-3-VBlank small-payload direct-stage path is masking a better stream-window schedule. The binary question was whether forcing VISITOR3 payloads under `8 KiB` through the normal window-fill path would reduce active blocking or loop read time without moving pack LBAs or the `215040` byte PS-EXE bucket. | Temporarily guarded the direct-stage branch in `fgRuntimeTryStageNextFrame()` with `!fgSceneEquals(gFgRuntime.sceneName, "visitor3")`, built, and ran a strict VISITOR3 high/low focused gate against the v111 broad baseline with zero regression allowance and `--require-improvement`. Source was reverted after the gate failed. Artifact: `scratch/ps1-perf-iterate/visitor3-no-direct-stage-v114-focused/20260508-031646-449029/summary.json`. | Failed/no promotion. Both tides stayed exact-flat with fixed layout: high remained `scene_vb=1437`, `loop_vb=1137/1024`, `overrun_vb=113`, `blocking_vb=190`, `prefetch_overrun_vb=0`, `loop_reads=33`, `loop_read_vb=200`, LBA `22472`; low remained `scene_vb=1436`, `loop_vb=1135/1024`, `overrun_vb=111`, `blocking_vb=184`, `prefetch_overrun_vb=0`, `loop_reads=33`, `loop_read_vb=194`, LBA `23232`. The PS-EXE bucket stayed `215040`, while `foregroundPilotPlay` grew by `128` bytes and ELF grew `951708 -> 951900`. | Do not promote or retry VISITOR3 direct-stage disable as a local scene guard. The current exact-3-VBlank direct-stage path is not the source of VISITOR3's remaining blocking; further VISITOR3 work should move to generated scheduler ownership, data-shape/upload-ready payloads, or a measured restore/composite work reduction with a real timing signal. |
| 2026-05-08 | `visitor3-packed-draw-v115-v122` | Log-only; source and packs restored. | Retry VISITOR3 packed draw metadata after v111 reclaimed code headroom. The binary question was whether a compact FGP3/v7 `(gap,count)` draw-span stream, plus setup-prime and scheduler-guard variants, could reduce VISITOR3 payload reads without moving foreground LBAs or the `215040` byte PS-EXE bucket. | Temporarily converted only `VISITOR3.FG2` and `VIST3LOW.FG2` to FGP3/v7 while preserving each `1555450` byte file. High active payload dropped `737600 -> 592605`; low dropped `737600 -> 626088`. Tested direct-stage v115, no-direct-stage v116, setup-prime caps v117/v118/v119, slack guards v120/v121, and the v122 non-window stage guard. Key artifacts: `scratch/ps1-perf-iterate/visitor3-packed-draw-v115-focused/20260508-032753-512248/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-nodirect-v116-focused/20260508-033112-532014/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-prime216-v117-focused/20260508-033617-561656/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-prime232-v118-focused/20260508-033856-577823/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-prime232h208l-v119-focused/20260508-034114-592084/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-prime232h208l-slack4-v120-focused/20260508-034356-608334/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-prime232h208l-slack5-v121-focused/20260508-034639-624714/summary.json`, `scratch/ps1-perf-iterate/visitor3-packed-draw-prime232h208l-stageguard-v122-focused/20260508-034912-640259/summary.json`, and broad `scratch/ps1-perf-iterate/visitor3-packed-draw-prime232h208l-stageguard-v122-broad/20260508-035118-653016/summary.json`. | Failed/no promotion. The best focused v122 variant fixed hidden prefetch and improved VISITOR3 high `loop_vb 1137 -> 1104`, `overrun_vb 113 -> 76`, and `blocking_vb 190 -> 127`; low moved `1135 -> 1118`, `111 -> 94`, and `184 -> 153`, with `prefetch_overrun_vb=0` on both tides. Broad controls rejected it: BUILDING2 high regressed `overrun_vb 33 -> 34` and `prefetch_overrun_vb 12 -> 13`; BUILDING2 low regressed `scene_vb 1657 -> 1663`, `loop_vb 1383 -> 1389`, `overrun_vb 79 -> 88`, and `prefetch_overrun_vb 5 -> 13`; BUILDING4 high regressed `scene_vb 3115 -> 3117` and `loop_vb 2844 -> 2846`. A split decoder attempt preserved v4 hot-loop shape but grew PS-EXE `215040 -> 217088`, so it was closed before an emulator gate. | Do not promote the v7 runtime shape. The VISITOR3 byte-reduction signal is strong, but the C decoder and scheduler guard perturb control timing enough to break broad canaries. Retry only with a layout-neutral/generated decoder, pack-only reduction, or scene-owned scheduler metadata that can isolate VISITOR3 without BUILDING2/BUILDING4 drift. |
| 2026-05-08 | `visitor3-tail-trim-v123` | Log-only; packs restored. | Test whether the payload-reduction portion of packed draw could be captured without any runtime source by trimming all-zero draw-tail bytes inside existing FGP3/v4 payloads, preserving entry cadence, pack sizes, foreground LBAs, and executable bytes. | Restored the v111 packs, rewrote only VISITOR3 high/low FGP3/v4 payload lengths, and padded each pack back to its original footprint. High active payload dropped `737600 -> 661437` by trimming `76163` bytes across `17` entries; low dropped `737600 -> 698335` by trimming `39265` bytes across `5` entries. Build size stayed exact at PS-EXE `215040` and ELF `951708`. Artifact: `scratch/ps1-perf-iterate/visitor3-tail-trim-v123-focused/20260508-040711-745590/summary.json`. | Failed/no promotion. High improved visible work but moved too much refill into hidden debt: `scene_vb 1437 -> 1416`, `loop_vb 1137 -> 1116`, `overrun_vb 113 -> 91`, and `blocking_vb 190 -> 116`, but `prefetch_overrun_vb 0 -> 67`. Low passed cleanly with `scene_vb 1436 -> 1426`, `loop_vb 1135 -> 1125`, `overrun_vb 111 -> 100`, `blocking_vb 184 -> 167`, and `prefetch_overrun_vb=0`. | Do not promote standalone v4 draw-tail trimming. The high-tide speed signal needs explicit scheduler budget or setup-prime ownership to avoid hidden prefetch debt; keep this as the next low-risk VISITOR3 retry lane because it avoids v7 runtime/code-shape drift. |
| 2026-05-08 | `visitor3-tail-trim-prime-v124-v126` | Log-only; source and packs restored. | Follow up on the v123 tail-trim failure by testing whether VISITOR3 setup-prime budget alone could absorb high-tide hidden refill debt while keeping the v4 pack-only payload shrink and avoiding the v7 decoder/code-shape risk. The binary search covered the nearby high-tide resident caps before closing the lane. | Reapplied the v4 draw-tail trim from fresh pack backups, preserving both `1555450` byte footprints and foreground LBAs, then tested high/low with high `232 KiB` / low `208 KiB` prime and high-only probes at `256 KiB` and `224 KiB`. The transform report is `scratch/visitor3-tail-trim-prime232-v124/transform.json`; gate artifacts are `scratch/ps1-perf-iterate/visitor3-tail-trim-prime232-v124-focused/20260508-041452-789921/summary.json`, `scratch/ps1-perf-iterate/visitor3-tail-trim-prime256-v125-high/20260508-041716-804711/summary.json`, and `scratch/ps1-perf-iterate/visitor3-tail-trim-prime224-v126-high/20260508-041838-813031/summary.json`. Source and packs were restored afterward and the build returned to PS-EXE `215040` / ELF `951708`. | Failed/no promotion. The `232 KiB` focused variant reduced high hidden debt compared with v123 but still failed strict: high `loop_vb 1137 -> 1131`, `overrun_vb 113 -> 104`, `blocking_vb 190 -> 149`, and `prefetch_overrun_vb 0 -> 22`; low passed with `loop_vb 1135 -> 1126`, `overrun_vb 111 -> 101`, `blocking_vb 184 -> 170`, and `prefetch_overrun_vb=0`. More or less high prime was non-monotonic and worse for hidden debt: `256 KiB` high improved `loop_vb 1114` and `blocking_vb 113` but regressed `prefetch_overrun_vb=69`; `224 KiB` high improved `loop_vb 1112` and `blocking_vb 109` but regressed `prefetch_overrun_vb=66`. | Close scalar setup-prime retries for standalone v4 tail trimming. The byte/work signal is strong, but high tide needs generated scheduler ownership or a targeted stage guard that can preserve broad canaries; prime bytes alone trade visible blocking for hidden overrun instead of producing a promotable cadence. |
| 2026-05-08 | `visitor3-tail-trim-stageguard-v127` | `ps1: trim visitor3 draw tails with stage guard` | Promote the v4 draw-tail reduction signal only after adding explicit VISITOR3 scheduler protection. The binary question was whether the pack-side trim plus a narrow hidden-stage slack guard could preserve the v123/v124 visible win while eliminating high-tide hidden prefetch debt and keeping broad controls flat. | Reapplied FGP3/v4 zero draw-tail trimming to `VISITOR3.FG2` and `VIST3LOW.FG2`, preserving both `1555450` byte footprints and foreground LBAs. High active payload dropped `737600 -> 661435` by trimming `76165` bytes across `18` entries; low dropped `737600 -> 698333` by trimming `39267` bytes across `6` entries. Raised the VISITOR3 high setup-prime window and resident max to `232 KiB`, and skipped VISITOR3 non-window hidden large-stage reads when held slack is below `FG_PREFETCH_FALLTHROUGH_MIN_SLACK_VBLANKS`. Artifacts: transform `scratch/visitor3-tail-trim-stageguard-v127/transform.json`; focused `scratch/ps1-perf-iterate/visitor3-tail-trim-stageguard-v127-focused/20260508-042255-837812/summary.json`; broad controls `scratch/ps1-perf-iterate/visitor3-tail-trim-stageguard-v127-broad/20260508-042455-850028/summary.json`. | Promoted. VISITOR3 high improves `scene_vb 1437 -> 1422`, `loop_vb 1137 -> 1118`, `target_vb 1024 -> 1028`, `overrun_vb 113 -> 90`, `blocking_vb 190 -> 150`, `prefetch_overrun_vb=0`, `loop_reads 33 -> 27`, and `loop_read_vb 200 -> 153`. VISITOR3 low improves `1436 -> 1426`, `1135 -> 1126`, `1024 -> 1025`, `111 -> 101`, `184 -> 170`, `prefetch_overrun_vb=0`, `33 -> 31`, and `194 -> 179`. BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high controls stayed exact-flat; PS-EXE stayed `215040`, and foreground LBAs stayed fixed. | Keep this as the new VISITOR3 baseline. The broad script reported exact-flat controls as `--require-improvement` failures, but no canary regressed and the VISITOR3 rows improved. Public battle-card rollup moves to `-0.0968%` over target / `100.1613%` target speed; since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.50` over-target points and added about `13.06` target-speed points. |
| 2026-05-08 | `visitor3-readgroup-315331-333349-v128` | Log-only; source reverted. | Retest the top v127 read-candidate matrix rows after draw-tail trimming shifted VISITOR3's remaining CD pressure to late tail clusters. The binary question was whether tide-specific retained read groups for high `315..331` and low `333..349` could collapse two append reads without repeating the historical VISITOR3 visible-cadence regressions. | Temporarily added VISITOR3 default-window read groups for high `{315,331}` and low `{333,349}` and selected them only by tide. Ran an unguarded strict high/low gate, changed both groups to require `8` held VBlanks and reran, then tested a scheduler-owned variant where VISITOR3 groups applied only to prefetch reads and were disabled on visible due reads. Source was reverted after all variants failed. Artifacts: raw `scratch/ps1-perf-iterate/visitor3-readgroup-315331-333349-v128-focused/20260508-045502-1022084/summary.json`; guarded `scratch/ps1-perf-iterate/visitor3-readgroup-315331-333349-minslack8-v128-focused/20260508-045820-1041906/summary.json`; prefetch-only `scratch/ps1-perf-iterate/visitor3-prefetchonly-readgroup-v128-focused/20260508-050410-1075650/summary.json`. | Failed/no promotion. The raw groups did save CD reads but regressed visible cadence: high moved `scene_vb 1422 -> 1426`, `loop_vb 1118 -> 1122`, `overrun_vb 90 -> 94`, and `blocking_vb 150 -> 151` while loop reads dropped `27 -> 25`; low moved `1426 -> 1430`, `1126 -> 1130`, `101 -> 105`, and `170 -> 171` while loop reads dropped `31 -> 29`. The `8` VBlank guard and prefetch-only scheduler restriction both prevented the regression but became exact-flat on both tides and failed only `--require-improvement`. | Close these exact VISITOR3 ranges as standalone source-table changes. The matrix was right to mark them scheduler-owned: without explicit CD ownership they trade fewer transactions for worse visible timing, and the safe restrictions do not fire enough to improve. Keep the v127 baseline and continue with generated scheduler metadata or data-shape work rather than hand-authored VISITOR3 read groups. |
| 2026-05-08 | `visitor3-low-prime200-216-v132` | Log-only; source restored. | Recheck whether the promoted low-tide `208 KiB` setup-prime knee moved after the v127 draw-tail trim plus stage guard changed VISITOR3's remaining read shape. The binary question was whether low tide now preferred one `8 KiB` step above or below the accepted cap while high tide stayed flat. | Temporarily changed `FG_VISITOR3_LOW_SETUP_PRIME_WINDOW_BYTES` from `208 KiB` to `216 KiB` and ran VISITOR3 high/low against the v127 focused baseline, then changed it to `200 KiB` and ran the low case. Source was restored afterward. Artifacts: `scratch/ps1-perf-iterate/visitor3-low-prime216-v132-focused/20260508-053529-1254959/summary.json` and `scratch/ps1-perf-iterate/visitor3-low-prime200-v132-focused/20260508-053737-1268113/summary.json`. | Failed/no promotion. At `216 KiB`, high stayed exact-flat (`1422`, `1118/1028`, `blocking_vb=150`, `prefetch_overrun_vb=0`), but low regressed `scene_vb 1426 -> 1429`, `loop_vb 1126 -> 1127`, `overrun_vb 101 -> 102`, and `blocking_vb 170 -> 173`. At `200 KiB`, low regressed much harder: `1426 -> 1455`, `1126 -> 1152`, `overrun_vb 101 -> 128`, `blocking_vb 170 -> 191`, and `prefetch_overrun_vb 0 -> 3`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Keep VISITOR3 low at `208 KiB`; the v127 low-tide setup-prime knee did not move. Further VISITOR3 work should use generated scheduler ownership or pack/data-shape changes, not another scalar low-prime retry. The public battle-card baseline remains `-0.0968%` over target / `100.1613%` target speed. |
| 2026-05-08 | `building2-low-restore-minus-current-v133-v135` | Log-only; source and pack restored. | Retry the BUILDING2 low half of the restore-minus-current data-shape transform after the later low read-group promotions reduced low-tide CD pressure. The binary question was whether the same size-preserving cleanup-minus-current pack transform that promoted high tide could now pass strict low tide without hidden refill debt, and whether a small setup-prime or stage-guard salvage would absorb any remaining debt. | Generated a size-preserving `BUIL2LOW.FG2` candidate with `scripts/compact-fgp3-restore-minus-current.py --pad-to-input-size`, reducing active payload `789906 -> 674798`, cleanup spans `109076 -> 53563`, cleanup pixels `468636 -> 237286`, and modeled restore bytes `937272 -> 474572` while keeping the pack at `1303332` bytes. Tested the low pack alone, then retried with a temporary low `144 KiB` setup-prime cap and a temporary BUILDING2-low non-window stage guard. Source and pack were restored afterward. Artifacts: `scratch/ps1-perf-iterate/building2-low-restore-minus-current-v133-focused/20260508-054126-1289936/summary.json`, `scratch/ps1-perf-iterate/building2-low-restore-minus-current-prime144-v134-low/20260508-054405-1306021/summary.json`, and `scratch/ps1-perf-iterate/building2-low-restore-stageguard-v135-low/20260508-054549-1316262/summary.json`. | Failed/no promotion under the strict hidden-refill gate. The raw low pack produced a large visible win, improving low `scene_vb 1657 -> 1625`, `loop_vb 1383 -> 1351`, `overrun_vb 79 -> 40`, `blocking_vb 118 -> 55`, and `loop_reads 55 -> 53`, with high tide exact-flat, but hidden prefetch overrun regressed `5 -> 13`. The `144 KiB` setup-prime and BUILDING2-low stage-guard variants improved the visible result further to `1623`, `1346/1311`, `overrun_vb=35`, `blocking_vb=50`, and `loop_reads=52`, but hidden refill remained `13`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Do not promote BUILDING2 low restore-minus-current until scheduler ownership can absorb the hidden refill debt. The transform is promising because it removes `32..37` loop VBlanks and `63..68` visible-blocking VBlanks, but the current strict baseline requires `prefetch_overrun_vb` to stay at `5`. Retry only with generated scheduler timing, a targeted refill-budget guard that actually moves hidden overrun, or another data-shape change that reduces active CD pressure before the shortened render cadence. The public battle-card baseline remains `-0.0968%` over target / `100.1613%` target speed. |

| 2026-05-08 | `walkstuf1-direct-stage-cap6-cap7-v129` | Log-only; source restored. | Close the remaining scalar WALKSTUF1 direct-stage threshold gap after the low no-direct-stage probe and global `4 KiB` cap both showed visible-blocking relief but non-promotable cadence/hidden-overrun costs. The binary question was whether an intermediate global cap at `6 KiB` or `7 KiB` could retain enough exact-payload direct staging to avoid the hidden-refill spike while still reducing WALKSTUF1 active blocking. | Temporarily changed `FG_PREFETCH_DIRECT_STAGE_MAX_BYTES` from `8 KiB` to `6 KiB`, ran WALKSTUF1 high/low against the promoted `walkstuf1-high-primecap144-v089` paired baseline with zero regression allowance and `--require-improvement`, then repeated at `7 KiB`. Source was restored afterward; pack LBAs and the `215040` byte PS-EXE bucket stayed fixed during both gates. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-direct-stage-cap6-v129-focused/20260508-051532-1139989/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-direct-stage-cap7-v129-focused/20260508-051757-1154740/summary.json`. | Failed/no promotion. At `6 KiB`, high reduced blocking `275 -> 209`, loop reads `134 -> 130`, and due misses `55 -> 35`, but regressed `scene_vb 1880 -> 1882`, `loop_vb 1592 -> 1594`, `overrun_vb 186 -> 187`, and `prefetch_overrun_vb 51 -> 75`; low reduced blocking `271 -> 229`, loop reads `132 -> 129`, and due misses `50 -> 36`, but regressed `1895 -> 1898`, `1604 -> 1607`, `197 -> 203`, and `54 -> 81`. At `7 KiB`, hidden overrun stayed flat (`51/54`) and loop time stayed flat, but the tiny blocking wins (`275 -> 274`, `271 -> 267`) came with target drift and overrun regressions (`186 -> 187`, `197 -> 198`). | Keep the global direct-stage cap at `8 KiB` and close scalar direct-stage threshold tuning for current WALKSTUF1. The measured trade is monotonic: smaller caps move work out of visible blocking but into hidden debt or target-relative drift. Further WALKSTUF1 work needs generated scheduler ownership, frame/range-specific direct-stage policy, or a pack/data-shape reduction that lowers refill cost before changing direct-stage coverage. |
| 2026-05-08 | `walkstuf1-low-rg297-313-minslack8-v130` | Log-only; source restored. | Test the top current WALKSTUF1 low read-matrix row as a guarded scheduler-owned group instead of a raw table. The binary question was whether range `297..313` could collapse a three-read low-tide cluster only when at least `8` held VBlanks were available, avoiding the visible-cadence regressions seen in raw VISITOR3/WALKSTUF1 table work. | Temporarily added a low-tide-only `walkstuf1` read group `{297,313,8}` and ran high/low against the promoted `walkstuf1-high-primecap144-v089` paired baseline with zero regression allowance and `--require-improvement`. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg297-313-minslack8-v130-focused/20260508-052535-1198528/summary.json`. | Failed/no promotion. The guard prevented the group from firing (`group_hits=0` on both tides). High stayed exact-flat at `1880`, `1592/1406`, `overrun_vb=186`, `blocking_vb=275`, `prefetch_overrun_vb=51`, and `loop_reads=134`. Low kept `scene_vb=1895`, `loop_vb=1604`, `prefetch_overrun_vb=54`, and `loop_reads=132`, but code-shape/phase drift lowered `target_vb 1407 -> 1406`, so overrun regressed `197 -> 198` despite a small blocking shift `271 -> 267`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Do not promote guarded source-table WALKSTUF1 `297..313`. The current hot-path branch grows `foregroundPilotPlay` and still does not fire under the safe slack guard; raw/no-guard versions remain too risky by matrix classification. Keep WALKSTUF1 read work in generated scheduler metadata or data-shape lanes rather than another hand-authored group table. |
| 2026-05-08 | `walkstuf1-selective-upload-footprint-v131` | Log-only; no runtime source. | Size- and safety-gate the top current WALKSTUF1 selective upload-ready/x-band preprocessing lane before adding a PAL4/FGP2 runtime format. The binary question was whether either tide could fit a useful upload-ready subset inside the existing pack footprint, and whether raw pack-emitted foreground pixels were safe for the selected bands. | Ran `scripts/analyze-fg2-preprocess-plans.py --selective-budget-from-zero-tail` on `generated/ps1/foreground/WALKSTUF1.FG2` and `generated/ps1/foreground/WALK1LOW.FG2`, emitting per-frame sheets in scratch. No emulator run was needed because the same-footprint and raw-pixel safety gates failed before a runtime probe. Artifacts: `scratch/walkstuf1-high-preprocess-v131.json`, `scratch/walkstuf1-low-preprocess-v131.json`, `scratch/walkstuf1-high-preprocess-v131-frames.csv`, and `scratch/walkstuf1-low-preprocess-v131-frames.csv`. | Failed/no promotion. Both current WALKSTUF1 PAL4/FGP2 packs are `1535263` bytes with the last nonzero byte at `1535262`, leaving only `1` byte of zero-tail slack. The default threshold plan is large but not layout-neutral: it selects `153 / 216` frames, estimates `15581440` runtime full-width upload bytes, `5680448` x-band bytes, `9900992` modeled upload bytes saved (`63.54%`), `456` x-band rects, and `5684096` payload-plus-rect metadata bytes per tide. The exact zero-tail budget therefore selects `0` frames and retains `0` saved bytes. The raw foreground-only safety gate also reports `0` selected draw-covered x-band bytes and `0` all-draw-covered selected frames. | Do not implement WALKSTUF1 selective upload-ready as a same-footprint raw append under the current packs. There is essentially no padded slack, and the modeled x-band win depends on pixels outside current opaque draw coverage. WALKSTUF1 now needs a shrinking/compressed pack transform, explicit layout-moving experiment, safe background-owned pixel source, or generated scheduler metadata rather than a raw upload-ready append. The public battle-card baseline remains `-0.0968%` over target / `100.1613%` target speed. |
| 2026-05-08 | `building6-window48-group15-39-v136` | Log-only; source restored. | Test the best shared BUILDING6 high/low read-plan candidate after confirming the current packs are `FGP2`/PAL4 and cannot use the FGP3 restore-minus-current transform. The binary question was whether a `48 KiB` retained stream window plus read group `15..39` could collapse one early append cluster on both tides without repeating the old BUILDING6 scalar-window regressions. | Temporarily changed `FG_BUILDING6_WINDOW_BYTES` from `24 KiB` to `48 KiB`, added a shared BUILDING6 read group `{15,39,0}`, and selected it for both tides. Ran paired BUILDING6 high/low against `scratch/ps1-perf-iterate/activity9-dead-readgroup-prune-v082-canaries/20260507-132441-4070377/summary.json` with zero regression allowance and `--require-improvement`. Source was restored after the strict gate failed. Artifact: `scratch/ps1-perf-iterate/building6-window48-group15-39-v136-focused/20260508-055330-1359380/summary.json`. | Failed/no promotion. The larger retained window cut loop reads heavily (`74 -> 32` high, `73 -> 31` low) and due misses did not worsen (`1 -> 0` high, low stayed `2`), but the refill cost became visible and hidden debt. High regressed `scene_vb 2784 -> 2837`, `loop_vb 2520 -> 2568`, `target_vb 2442 -> 2443`, `overrun_vb 78 -> 125`, `blocking_vb 62 -> 115`, and `prefetch_overrun_vb 64 -> 117`. Low regressed `2780 -> 2835`, `2515 -> 2565`, `2437 -> 2445`, `78 -> 120`, `70 -> 115`, and `66 -> 96`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Do not retry BUILDING6 with scalar window enlargement or hand-authored larger-window groups. The saved-read signal is real, but the window is too expensive under the current scheduler. BUILDING6 needs generated scheduler ownership, a shrinking/selective FGP2 data-shape encoder, or an explicit layout-moving pack experiment before another read-count group. The public battle-card baseline remains `-0.0968%` over target / `100.1613%` target speed. |
| 2026-05-08 | `visitor3-prepare-slack3-slack5-v137` | Log-only; source restored. | Close the remaining cheap VISITOR3 prepared-present scheduler threshold after the v127 draw-tail trim plus stage guard. The binary question was whether preparing the staged frame one VBlank later (`5`) or earlier (`3`) than the accepted `4` would move VISITOR3 cadence without adding CD/refill debt. | Temporarily changed `FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS` from `4` to `5` and ran VISITOR3 high/low against the v127 focused baseline, then repeated with the threshold at `3`. Source was restored to `4` afterward. Artifacts: `scratch/ps1-perf-iterate/visitor3-prepare-slack5-v137-focused/20260508-060833-1442529/summary.json` and `scratch/ps1-perf-iterate/visitor3-prepare-slack3-v137-focused/20260508-061037-1455113/summary.json`. | Failed/no promotion. Both thresholds were exact-flat on both tides. High stayed `scene_vb=1422`, `loop_vb=1118`, `target_vb=1028`, `overrun_vb=90`, `blocking_vb=150`, `prefetch_overrun_vb=0`, and `loop_reads=27`; low stayed `1426`, `1126/1025`, `overrun_vb=101`, `blocking_vb=170`, `prefetch_overrun_vb=0`, and `loop_reads=31`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Keep `FG_PREPARE_PRESENT_MIN_SLACK_VBLANKS=4`. VISITOR3's remaining `90/101` VBlank overrun is not sensitive to this local prepare threshold; continue with generated scheduler ownership, safe upload-ready/precomposed payloads, or another pack/data-shape reduction. The public battle-card baseline remains `-0.0968%` over target / `100.1613%` target speed. |
| 2026-05-08 | `visitor3-high-prime256-stageguard-v139` | Log-only; source restored. | Retest high-tide `256 KiB` setup-prime residency after the accepted v127 VISITOR3 stage guard, because the earlier `256 KiB` scalar probe was run before that guard existed. The binary question was whether the guard made the larger high-tide prime safe enough to remove another read. | Temporarily changed VISITOR3 high setup-prime and max-resident caps from `232 KiB` to `256 KiB`, then ran VISITOR3 high against the v127 focused baseline with zero regression allowance and `--require-improvement`. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/visitor3-high-prime256-stageguard-v139-high/20260508-061718-1494975/summary.json`. | Failed/no promotion. The larger high setup-prime extended setup coverage from sectors `1..117` to `1..129` and reduced loop reads `27 -> 26`, but it regressed the actual cadence: `scene_vb 1422 -> 1439`, `loop_vb 1118 -> 1131`, `target_vb 1028 -> 1027`, `overrun_vb 90 -> 104`, `blocking_vb 150 -> 155`, and `prefetch_overrun_vb 0 -> 3`. Pack LBA and the `215040` byte PS-EXE bucket stayed fixed. | Keep VISITOR3 high at `232 KiB`. Larger setup residency still perturbs CD/refill phase even with the stage guard; remaining VISITOR3 work should not be scalar prime tuning. Continue only with generated scheduler ownership or pack/data-shape work that reduces active CD cost without moving hidden refill into visible cadence. |
| 2026-05-08 | `visitor3-dirty-upload-readplan-v140` | Log-only; no source change. | Make a current-baseline determination on the remaining VISITOR3 dirty-upload/read-plan lane before moving attention to the next top outlier. The binary question was whether the v127 tail-trim slack created a source-side, same-footprint, safe runtime path through dirty-upload narrowing or current-window read groups. | Inspected the live dirty-upload path in `src/graphics_ps1.c`, refreshed VISITOR3 high/low preprocessing with `scripts/analyze-fg2-preprocess-plans.py --selective-budget-from-zero-tail`, and regenerated read-plan reports from the v127 focused logs. Artifacts: `scratch/visitor3-high-preprocess-v140.json`, `scratch/visitor3-low-preprocess-v140.json`, `scratch/visitor3-high-preprocess-v140-frames.csv`, `scratch/visitor3-low-preprocess-v140-frames.csv`, `scratch/visitor3-high-readplan-v140.json`, and `scratch/visitor3-low-readplan-v140.json`. | Failed/no runtime promotion. The live uploader already tracks row-level dirty X extents, but submitting exact narrow intervals would explode to about `131996` upload rects over the loop; prior scratch-packed x-band probes are still the applicable rejection because they either crossed the PS-EXE bucket or regressed timing. The current budgeted upload-ready model fits more after v127 slack, selecting high `75` frames with `888880 / 891012` bytes and `6290232` modeled upload bytes saved, and low `74` frames with `853848 / 854114` bytes and `6166528` saved, but both tides still report `0` draw-covered selected x-band bytes and `0` all-draw-covered selected frames, so raw foreground-only payloads remain unsafe. The refreshed read-plan reports no candidate that is simultaneously append-start fireable, current-window-sized, and low-risk; the fireable current-fit rows are the known late tight-cluster class, including high `315..331` and low `333..349`, and are marked `high-risk:scheduler-only`. | Do not implement runtime dirty-upload narrowing or another hand-authored VISITOR3 source-table group from the current v127 baseline. VISITOR3 remains blocked on generated scheduler ownership or a safe pack/data-shape change: precomposed/background-owned upload payloads, compression/shrinking that can carry owned pixels, or a generated refill budget that can absorb the tight late clusters. Public battle-card baseline remains `-0.0968%` over target / `100.1613%` target speed, with about `17.50` over-target points removed and `13.06` target-speed points added since the compact full-matrix baseline. |

| 2026-05-08 | `walkstuf1-compact-fgp3-v141` | `ps1: compact walkstuf1 foreground packs` | Promote the first successful WALKSTUF1 pack/data-shape reduction after the raw FGP3 conversion expanded past the original footprint and the scalar/read-table lanes closed. The binary question was whether the full compaction chain could shrink both tides into compact FGP3/v4 restore-minus-current packs, keep the original CD layout, and improve the top WALKSTUF1 timing rows without causing broad canary regressions. | Generated raw temporal residual packs, then ran cleanup metadata compaction, PAL4 draw metadata compaction, restore-minus-current, draw-tail trim, and zero-noop compaction. Updated `scripts/compact-fgp3-zero-noop-entries.py` so cleanup-only residual payloads with no draw trailer are treated as no-visual-draw candidates instead of aborting the pass. Both `WALKSTUF1.FG2` and `WALK1LOW.FG2` were padded back to `1535263` bytes with header `FGP3/v4`; active candidate size is `923959` bytes. Artifacts: high gate `scratch/ps1-perf-iterate/walkstuf1-compact-fgp3-v141-high/20260508-063526-1596231/summary.json`, low gate `scratch/ps1-perf-iterate/walkstuf1-compact-fgp3-v141-low/20260508-063632-1602886/summary.json`, broad canary `scratch/ps1-perf-iterate/walkstuf1-compact-fgp3-v141-broad/20260508-063809-1612125/summary.json`, VISITOR3 retry `scratch/ps1-perf-iterate/walkstuf1-compact-fgp3-v141-visitor3-high-retry/20260508-064701-1663448/summary.json`, and original-FGP2 control `scratch/ps1-perf-iterate/walkstuf1-compact-fgp3-v141-visitor3-high-control-fgp2/20260508-064823-1671329/summary.json`. | Promoted. WALKSTUF1 high improves `scene_vb 1880 -> 1779`, `loop_vb 1592 -> 1491`, `target_vb 1406 -> 1426`, `overrun_vb 186 -> 65`, `blocking_vb 275 -> 85`, `prefetch_overrun_vb 51 -> 32`, `loop_reads 134 -> 69`, `loop_read_vb 586 -> 300`, and `due_misses 55 -> 13`. Low improves `1895 -> 1780`, `1604 -> 1489`, `1407 -> 1427`, `197 -> 62`, `270 -> 86`, `55 -> 27`, `132 -> 69`, `604 -> 305`, and `50 -> 12`. Pack LBAs stay `24744/25494`, pack bytes stay `1535263`, and the PS-EXE bucket stays `215040`. The broad non-WALKSTUF canaries were exact-flat except VISITOR3 high; that same VISITOR3 high drift reproduced with the original FGP2 WALKSTUF packs restored, so it is unrelated current-control drift. | Keep as the new WALKSTUF1 baseline. The battle card moves from `-0.0968%` to `-0.2497%` average over target and from `100.1613%` to `100.2899%` target speed across `120` timing-bearing rows. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `17.65` over-target points and added about `13.19` target-speed points. Next targets are VISITOR3 low/high, BUILDING2 low, WALKSTUF1 residual work, and BUILDING6. |
| 2026-05-08 | `visitor3-fallthrough5-v142` | Log-only; source restored. | Retest the VISITOR3 non-window fallthrough guard against a fresh current control after the WALKSTUF1 compact pass. The binary question was whether lowering `FG_PREFETCH_FALLTHROUGH_MIN_SLACK_VBLANKS` from `6` to `5` would let the stage-guarded VISITOR3 scheduler do useful extra held-loop work without adding visible or hidden CD debt. | First refreshed the exact current VISITOR3 high/low control at `scratch/ps1-perf-iterate/visitor3-current-v142-control/20260508-073802-1946196/summary.json`, then temporarily set the fallthrough guard to `5` and ran paired VISITOR3 high/low with zero regression allowance and `--require-improvement`. Source was restored to `6` afterward. Artifact: `scratch/ps1-perf-iterate/visitor3-fallthrough5-v142-focused/20260508-074006-1958740/summary.json`. | Failed/no promotion. Both tides stayed exact-flat and therefore failed `--require-improvement`: high stayed `scene_vb=1422`, `loop_vb=1118`, `target_vb=1028`, `overrun_vb=90`, `blocking_vb=150`, `prefetch_overrun_vb=0`, and `loop_reads=27`; low stayed `1426`, `1126/1025`, `overrun_vb=101`, `blocking_vb=170`, `prefetch_overrun_vb=0`, and `loop_reads=31`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Keep the fallthrough guard at `6`. This local threshold is exhausted for the v127/v141 VISITOR3 baseline; remaining VISITOR3 work needs generated scheduler ownership, safe upload/precomposed payloads, or another pack/data-shape reduction rather than tighter held-loop fallthrough. |
| 2026-05-08 | `visitor3-window20-slack12-v143` | Log-only; source restored. | Test whether a VISITOR3-only `20 KiB` retained stream window could stage the current `16-18 KiB` yacht-motion entries through the window path if refills required `12` held VBlanks, avoiding the earlier blind scalar-window failures. | Temporarily added a compact-residual-only VISITOR3 stream-window policy at `20 KiB` and routed window refill eligibility through a VISITOR3 `12` VBlank guard. Ran high/low against the fresh v142 control with zero regression allowance, `--require-improvement`, `95%` work identity, fixed layout, and bounded symbol drift. Source was restored after the strict gate failed. Artifact: `scratch/ps1-perf-iterate/visitor3-window20-slack12-v143-focused/20260508-082340-2201826/summary.json`. | Failed/no promotion. High improved total `scene_vb 1422 -> 1396` but regressed active `loop_vb 1118 -> 1131`, `overrun_vb 90 -> 102`, `blocking_vb 150 -> 210`, `loop_reads 27 -> 39`, `loop_read_vb 153 -> 223`, and `due_misses 26 -> 37`; low improved total `1426 -> 1406` but regressed `1126 -> 1139`, `101 -> 106`, `170 -> 212`, `31 -> 41`, `179 -> 228`, and `29 -> 38`. Hidden `prefetch_overrun_vb` stayed `0`, foreground LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Do not retry VISITOR3 scalar retained-window tuning, even with a high slack guard. The failure is active-stage scheduling/ownership: larger windows make more payloads technically stageable but steal visible CD cadence and increase due misses. Continue with generated scheduler ownership or a pack/data-shape change, not another local window size. |
| 2026-05-08 | `building2-low-restore-window-slack5-v144` | Log-only; source and pack restored. | Test whether the rejected BUILDING2 low restore-minus-current pack can pass if low-tide window refills require `5` held VBlanks instead of the global `3`, targeting the hidden refill blocker that the non-window stage guard did not affect. | First refreshed a clean BUILDING2 high/low control at `scratch/ps1-perf-iterate/building2-current-v144-control/20260508-083211-2251304/summary.json`, then generated a size-preserving `BUIL2LOW.FG2` restore-minus-current candidate (`active_payload 789906 -> 674798`, `restore_bytes 937272 -> 474572`) and temporarily routed BUILDING2 low window-refill eligibility through a `5` VBlank guard. Ran high/low against the fresh control with zero regression allowance, `--require-improvement`, `95%` work identity, fixed layout, and bounded symbol drift. Source and pack were restored after the strict gate failed. Artifact: `scratch/ps1-perf-iterate/building2-low-restore-window-slack5-v144-focused/20260508-083528-2270638/summary.json`. | Failed/no promotion. High stayed exact-flat and failed only `--require-improvement`: `1599`, `1349/1316`, `overrun_vb=33`, `blocking_vb=48`, `prefetch_overrun_vb=12`, and `loop_reads=61`. Low fixed hidden refill and improved headline loop metrics (`scene_vb 1657 -> 1634`, `loop_vb 1383 -> 1360`, `target_vb 1304 -> 1313`, `overrun_vb 79 -> 47`, `prefetch_overrun_vb 5 -> 0`, `loop_reads 55 -> 52`, `loop_read_vb 251 -> 214`), but visibly regressed CD cadence: `blocking_vb 118 -> 180` and `due_misses 22 -> 43`. Pack LBA and PS-EXE bucket stayed fixed. | Do not promote or retry BUILDING2 low restore-minus-current with a local window slack guard. The guard solves the hidden-refill symptom by refusing low-slack window work, but that starves presentation and increases due misses. The next BUILDING2 low retry needs generated refill placement or a data-shape change that lowers visible CD demand without simply delaying window reads. |
| 2026-05-08 | `walkstuf1-preprocess-refresh-v145` | Log-only; no runtime change. | Refresh WALKSTUF1 upload/preprocess feasibility after the compact FGP3/v4 baseline created zero-tail pack slack. The binary question was whether the compact pack turns the earlier selective upload-ready append failure into a same-footprint runtime candidate, and whether the raw foreground-only safety gate is now satisfied. | Ran `scripts/analyze-fg2-preprocess-plans.py --selective-budget-from-zero-tail` on `generated/ps1/foreground/WALKSTUF1.FG2` and `generated/ps1/foreground/WALK1LOW.FG2`, writing scratch JSON and per-frame CSV artifacts. Artifacts: `scratch/walkstuf1-high-preprocess-v145.json`, `scratch/walkstuf1-low-preprocess-v145.json`, `scratch/walkstuf1-high-preprocess-v145-frames.csv`, and `scratch/walkstuf1-low-preprocess-v145-frames.csv`. | Failed/no runtime promotion. The budgeted selective x-band model now fits inside the compact pack zero tail (`609192 / 611305` bytes), selecting `39` frames, saving `1991904` modeled upload bytes from `2600320` selected runtime bytes (`76.6%` saved), and retaining `59.45%` of the default selected savings. However, selected draw-covered x-band bytes remain `0` and selected all-draw-covered frames remain `0`, so raw foreground-only payloads are still unsafe. Restore-minus-current has no remaining modeled cleanup win (`restore_runtime_bytes == restore_minus_current_exact_bytes == 525826`). | Do not append raw foreground-only WALKSTUF1 upload-ready payloads. The next runtime candidate needs precomposed/background-owned pixel data, compression with ownership metadata, generated scheduler ownership, or MoveImage-safe motion data; compact slack is a byte budget, not a safety proof. |
| 2026-05-08 | `building6-upload-motion-refresh-v146` | Log-only; no runtime change. | Refresh BUILDING6 data-shape feasibility after VISITOR3, BUILDING2 low, and WALKSTUF1 scalar/source lanes closed. The binary question was whether BUILDING6 has a same-footprint selective upload candidate or a simple motion-comp candidate worth implementing before generated scheduler/format work. | Ran `scripts/analyze-fg2-preprocess-plans.py --selective-budget-from-zero-tail` and `scripts/analyze-fg2-motion-comp.py` on both `generated/ps1/foreground/BUILDING6.FG2` and `generated/ps1/foreground/BUIL6LOW.FG2`, writing scratch JSON/CSV artifacts. Artifacts: `scratch/building6-high-preprocess-v146.json`, `scratch/building6-low-preprocess-v146.json`, `scratch/building6-high-preprocess-v146-frames.csv`, `scratch/building6-low-preprocess-v146-frames.csv`, `scratch/building6-high-motion-v146.json`, and `scratch/building6-low-motion-v146.json`. | Failed/no runtime promotion. Both packs remain `1444370` bytes with `1` byte of zero-tail slack, while the default selective x-band plan needs `3354208` payload-plus-rect bytes for `92` frames and `5708192` modeled upload bytes saved; the exact zero-tail budget selects `0` frames. Raw foreground-only safety also remains blocked with `0` selected draw-covered x-band bytes and `0` all-draw-covered selected frames. Motion analysis is strong but host-only: `242 / 305` candidate pairs, `64.85%` modeled pair-payload savings, and a zero-shift runtime model saving `680717` compose payload bytes plus `11701120` upload bytes. | Do not implement raw BUILDING6 upload append or direct MoveImage from the analyzer alone. BUILDING6 now needs generated zero-shift/motion residual format work, RAM-mirror/dirty-state proof, scheduler ownership, or an explicit layout-moving pack experiment; no low-risk same-footprint runtime probe is available from v146. |
| 2026-05-08 | `missing-scenes-current-v147` | `ps1: refresh missing scene perf rows` | Determine whether SUZY1, SUZY2, and MARY3 were missing from the timing-bearing matrix because of scene bugs or stale/short perf runs. The binary question was whether current builds emit clean `JCPERF2` active-loop timing for all six high/low rows. | Ran MARY3 high/low at `7200` frames, SUZY1/SUZY2 high/low at `7200` frames, then SUZY1 high/low at `12000` frames after the default budget ended before scene-end. Updated `performance-scene-matrix.csv`, regenerated preprocess opportunities, removed the SUZY metadata-only structural-gate allowlist, and taught `ps1-perf-all-scenes.sh` to raise SUZY1 cases to `PS1_PERF_SUZY1_FRAMES` or `12000` when the global frame budget is lower. Artifacts: MARY3 `scratch/ps1-perf-iterate/missing-scenes-current-v001/20260508-092931-2576782/summary.json`, SUZY2 plus short SUZY1 miss `scratch/ps1-perf-iterate/missing-scenes-current-v001/20260508-093210-2592030/summary.json`, and SUZY1 long `scratch/ps1-perf-iterate/missing-scenes-current-v001/20260508-094012-2637681/summary.json`. | Promoted as measurement/harness correctness, not a runtime speed win. None of the scenes is a playback bug. MARY3 high/low now measure `2402/2295` and `2402/2296` with `blocking_vb=690/693` and `due_misses=255/255`, making MARY3 a real top outlier. SUZY1 high/low measure `5763/5738` at the longer frame budget with `blocking_vb=21` and `due_misses=0`; SUZY2 high/low measure `2655/2633` with `blocking_vb=19` and `due_misses=0`. | Keep. The battle card now has `126 / 126` timing-bearing rows and `63 / 63` timed scenes. Public-capped rollup is `+0.6248%` over target / `99.4019%` target speed; raw signed rollup is `-0.1436%` / `100.1857%`. Since the compact full-matrix baseline, headless optimization has removed about `16.78` public over-target points and added about `12.30` public target-speed points. Next runtime target after VISITOR3/BUILDING2 remains MARY3 clean-pressure CD/data-shape work, not SUZY. |
| 2026-05-08 | `mary3-prefetch-preserve-prime-v147` | Log-only; source restored. | Test the obvious MARY3 clean-pressure escape hatch after the current matrix refresh showed `policy=none`, `blocking_vb=690/693`, and `due_misses=255/255`. The binary question was whether preserving stage/window prefetch under MARY3 clean-memory relief, with or without a `128 KiB` setup-prime window, could collapse visible blocking without violating the strict hidden-refill gate. | First temporarily added `mary3` to `fgScenePreservesPrefetchUnderCleanPressure()`, rebuilt, and ran MARY3 high/low against the fresh baseline. Then kept that preservation and added a `128 KiB` MARY3 setup-prime policy before rerunning. Source was restored after both strict gates failed. Artifacts: preserve-only `scratch/ps1-perf-iterate/mary3-prefetch-preserve-v001/20260508-094714-2677819/summary.json`; preserve plus prime `scratch/ps1-perf-iterate/mary3-prefetch-prime128-v002/20260508-095044-2698985/summary.json`. | Failed/no promotion. Preserve-only produced a large visible win but violated hidden refill: high `scene_vb 2661 -> 2556`, `loop_vb 2402 -> 2295`, `blocking_vb 690 -> 32`, `prefetch_overrun_vb 0 -> 9`; low `2662 -> 2558`, `2402 -> 2296`, `693 -> 32`, `0 -> 9`. The `128 KiB` setup-prime variant remained non-promotable and weaker: high `loop_vb 2402 -> 2302`, `blocking_vb 690 -> 32`, `prefetch_overrun_vb 0 -> 7`; low `2402 -> 2304`, `693 -> 32`, `0 -> 8`. | Do not promote raw MARY3 prefetch preservation or blind setup-prime. The speed signal is real, but strict hidden-overrun policy rejects moving work from visible due reads into held-slack debt. Retry MARY3 only with generated scheduler ownership, a data-shape reduction, or a clean-pressure-specific refill budget that keeps `prefetch_overrun_vb` at zero while reducing due misses. |
| 2026-05-08 | `mary3-preserve-window-slack8-v149` | `ps1: preserve mary3 prefetch safely` | Promote the strict-safe half of the MARY3 prefetch signal. The prior preserve-only probe proved visible blocking could collapse but failed because the default `3` VBlank window-refill guard moved cost into hidden refill debt. The binary question was whether a MARY3-local higher window slack guard could keep prefetch enabled while keeping `prefetch_overrun_vb=0`. | Added MARY3 to `fgScenePreservesPrefetchUnderCleanPressure()`, added a scene-local `FG_MARY3_WINDOW_MIN_SLACK_VBLANKS=8`, and routed window eligibility/direct-stage fallback through that scene-aware threshold. Ran high/low against the fresh MARY3 current rows and confirmed from current source. A `7` VBlank adjacent retry was also tested and restored because high tide regressed against the slack-8 baseline. Artifacts: promoted confirm `scratch/ps1-perf-iterate/mary3-preserve-window-slack8-v149-confirm/20260508-111257-3163258/summary.json`; slack-7 rejection `scratch/ps1-perf-iterate/mary3-preserve-window-slack7-v150-probe/20260508-111002-3145980/summary.json`. | Promoted. MARY3 high improves `loop_vb 2402 -> 2296`, `target_vb 2295 -> 2294`, `overrun_vb 107 -> 2`, `blocking_vb 690 -> 53`, `loop_reads 255 -> 44`, `loop_read_vb 690 -> 206`, and `due_misses 255 -> 13`; low improves `2402 -> 2297`, `2296 -> 2295`, `106 -> 2`, `693 -> 51`, `255 -> 44`, `693 -> 208`, and `255 -> 13`. Both tides keep `prefetch_overrun_vb=0`, use `policy=stage1_window`, and move to about `99.9%` public target speed. | Keep as the new MARY3 baseline. The public battle-card rollup moves from `+0.6248%` to `+0.5526%` over target and from `99.4019%` to `99.4709%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.2159%` / `100.2547%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.85` public over-target points and added about `12.37` public target-speed points. MARY3 is now green; next targets are VISITOR3 low/high, BUILDING2 low, WALKSTUF1 high/low, and BUILDING6. |
| 2026-05-08 | `visitor3-low-late-cluster-v148` | Log-only; source restored. | Attack the current largest public outlier, VISITOR3 low (`1126/1025`, `+9.85%`, `blocking_vb=170`), with the top current-compatible implementation idea from the read-plan analyzer. The binary question was whether the low-tide late cluster around sectors `333..349` could be promoted either as a retained read group or as setup-segment residency without stealing visible cadence. | First added a low-tide VISITOR3 read group `{333,349,0}` and ran the focused strict gate. Then restored the group path and tested two setup-segment variants: a one-shot high-cost frame segment at `159..171`, and a persistent late-cluster segment at `333..349`. Source was restored after all gates failed. Artifacts: group `scratch/ps1-perf-iterate/visitor3-low-group333-349-v148-probe/20260508-102807-2907784/summary.json`; one-shot segment `scratch/ps1-perf-iterate/visitor3-low-setupseg159-171-v148-probe/20260508-103019-2921038/summary.json`; persistent segment `scratch/ps1-perf-iterate/visitor3-low-setupseg333-349-v148-probe/20260508-103141-2929188/summary.json`. | Failed/no promotion. The read group saved loop reads (`31 -> 29`) but regressed `scene_vb 1426 -> 1430`, `loop_vb 1126 -> 1130`, `overrun_vb 101 -> 105`, and `blocking_vb 170 -> 171`. The one-shot `159..171` setup segment was exact-flat in active loop (`1126/1025`, `blocking_vb=170`) and failed `--require-improvement` while adding setup cost (`scene_vb 1426 -> 1434`). The persistent `333..349` setup segment regressed `scene_vb 1426 -> 1442`, `loop_vb 1126 -> 1129`, `target_vb 1025 -> 1024`, `overrun_vb 101 -> 105`, `blocking_vb 170 -> 174`, and `prefetch_overrun_vb 0 -> 2`. | Do not retry VISITOR3 late clusters through hand-authored read groups or setup segments. The saved-read signal is real but scheduler ownership is the blocker: local tables either move the cost into visible cadence or perturb hidden refill. Continue VISITOR3 with generated scheduler ownership, safe precomposed/background-owned upload payloads, compression/ownership metadata, or a data-shape reduction rather than another local source-table probe. |
| 2026-05-08 | `visitor3-low-late-cluster-guard-v150` | Log-only; source restored. | Close the last local-table variant for the current top outlier after the unguarded VISITOR3 low `333..349` group regressed active cadence. The binary question was whether adding a held-slack guard would allow the scheduler-style saved-read signal to fire only when spare VBlanks exist. | Temporarily added a low-tide VISITOR3 retained read group `{333,349,8}` and ran the focused strict gate against the v127 VISITOR3 low baseline. After it was exact-flat, lowered the guard to `{333,349,7}` and reran the same gate. Source was restored afterward. Artifacts: slack-8 `scratch/ps1-perf-iterate/visitor3-low-group333-349-minslack8-v150-probe/20260508-113717-3299347/summary.json`; slack-7 `scratch/ps1-perf-iterate/visitor3-low-group333-349-minslack7-v150-probe/20260508-113830-3306806/summary.json`. | Failed/no promotion. Both guarded variants were exact-flat and failed `--require-improvement`: low stayed `scene_vb=1426`, `loop_vb=1126`, `target_vb=1025`, `overrun_vb=101`, `blocking_vb=170`, `prefetch_overrun_vb=0`, `loop_reads=31`, and `due_misses=29`. Pack LBA `23232` and the `215040` byte PS-EXE bucket stayed fixed. | Close VISITOR3 local source-table late-cluster work. Unguarded grouping saves reads but regresses cadence; guarded grouping avoids regression but does not fire usefully. The next VISITOR3 attempt needs generated scheduler ownership or a pack/data-shape/precomposed-payload route rather than another hand-authored read group. |
| 2026-05-08 | `building2-low-group381-397-v151` | Log-only; source restored. | Rotate from exhausted VISITOR3 local groups to the next under-99 outlier, BUILDING2 low (`1383/1304`, `+6.06%`, `blocking_vb=118`). The read-plan matrix marked `381..397` as a low-visible-risk, fireable, current-window-compatible chained group after the accepted `365..381` group. The binary question was whether the next tail append could remove another read without stealing active cadence. | Temporarily appended `{381,397,0}` to `kBuilding2LowReadGroups12[]` after the accepted `{318,330,0}` and `{365,381,0}` groups, then ran BUILDING2 low against the v110 broad baseline with zero regression allowance and `--require-improvement`. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/building2-low-group381-397-v151-focused/20260508-114204-3327409/summary.json`. | Failed/no promotion. The candidate reduced `loop_reads 55 -> 54`, but regressed active timing: `scene_vb 1657 -> 1661`, `loop_vb 1383 -> 1387`, `overrun_vb 79 -> 83`, and `blocking_vb 118 -> 122`; `prefetch_overrun_vb` stayed `5`, `due_misses` stayed `22`, pack LBA `6817` and the `215040` byte PS-EXE bucket stayed fixed. | Do not retry BUILDING2 low `381..397` as a local table group. It proves the post-`365..381` tail read can be coalesced, but the extra append still costs more active cadence than it saves. Remaining BUILDING2 low read work should use generated scheduler placement or a data-shape change that lowers active CD pressure first. |
| 2026-05-08 | `building2-low-group204-220-v152` | Log-only; source restored. | Continue BUILDING2 low after the tail-group miss with the next distinct current-window candidate. The read-plan matrix ranked `204..220` as a fireable 16-sector group saving two reads with no overread or partial touches, but with tight internal gaps. The binary question was whether a held-slack guard could make it safe, and whether the unguarded version had a promotable visible-blocking signal. | Temporarily appended `{204,220,8}` to `kBuilding2LowReadGroups12[]`, then lowered the guard to `{204,220,7}` after an exact-flat result, then tested unguarded `{204,220,0}`. Each run used BUILDING2 low against the v110 broad baseline with zero regression allowance and `--require-improvement`; source was restored afterward. Artifacts: slack-8 `scratch/ps1-perf-iterate/building2-low-group204-220-minslack8-v152-focused/20260508-114515-3345865/summary.json`; slack-7 `scratch/ps1-perf-iterate/building2-low-group204-220-minslack7-v152-focused/20260508-114638-3354157/summary.json`; unguarded `scratch/ps1-perf-iterate/building2-low-group204-220-unguarded-v152-focused/20260508-114749-3361518/summary.json`. | Failed/no promotion. The guarded `8` and `7` VBlank variants were exact-flat at `1657`, `1383/1304`, `overrun_vb=79`, `blocking_vb=118`, `prefetch_overrun_vb=5`, and `loop_reads=55`. The unguarded variant improved visible CD pressure (`blocking_vb 118 -> 108`, `loop_reads 55 -> 53`, `due_misses 22 -> 18`) but regressed target-relative overrun `79 -> 82`, target cadence `1304 -> 1301`, and hidden refill `prefetch_overrun_vb 5 -> 13`. Pack LBA `6817` and the `215040` byte PS-EXE bucket stayed fixed. | Close `204..220` as a local BUILDING2 low table. The read saving is real but not strict-safe: guarded variants do not fire, and unguarded grouping creates the same hidden-refill debt class as the rejected low restore-minus-current transform. Continue BUILDING2 low only with generated scheduler/refill placement or a data-shape change that lowers active CD pressure without hidden debt. |
| 2026-05-08 | `walkstuf1-group201-213-v153` | Log-only; source restored. | Rotate from VISITOR3 and BUILDING2 local-table closures to the next under-99 rows, WALKSTUF1 high/low. Both compact-pack read plans ranked `201..213` as a fireable current-window-compatible 12-sector candidate with low visible risk, distinct from the earlier rejected `86..98`, `180..204`, and `297..313` groups. The binary question was whether it could reduce active loop time without worsening visible or hidden CD pressure. | Temporarily added a WALKSTUF1 retained read group `{201,213,0}` selected only for temporal-residual WALKSTUF1 packs, then reran low tide against the v141 compact-pack low baseline. Because the unguarded run was mixed, retried with `minSlack=8` and `minSlack=12`. Source was restored afterward. Artifacts: unguarded `scratch/ps1-perf-iterate/walkstuf1-group201-213-v153-low/20260508-115230-3388166/summary.json`; slack-8 `scratch/ps1-perf-iterate/walkstuf1-group201-213-minslack8-v153-low/20260508-115355-3397356/summary.json`; slack-12 `scratch/ps1-perf-iterate/walkstuf1-group201-213-minslack12-v153-low/20260508-115520-3405929/summary.json`. | Failed/no promotion. All three variants produced the same mixed low-tide profile: `scene_vb 1780 -> 1777`, `loop_vb 1489 -> 1486`, `overrun_vb 62 -> 59`, and `loop_reads 69 -> 67`, but visible blocking regressed `86 -> 89` and hidden refill regressed `prefetch_overrun_vb 27 -> 28`. Pack LBA `25494` and the `215040` byte PS-EXE bucket stayed fixed. | Do not promote `201..213` as a local WALKSTUF1 table. The timing win is real, but the strict gate rejects converting it into higher visible/hidden CD pressure. Keep the range for generated scheduler ownership or a data-shape route that can preserve the loop win without blocking/refill regressions. |
| 2026-05-08 | `building6-direct-fgp3-v154` | Log-only; packs restored. | Close the explicit layout-moving version of the BUILDING6 direct PAL4 temporal-residual conversion. The earlier same-footprint gate already showed direct FGP3 expands both current packs; this run answered whether accepting that layout movement still produces enough active-loop win to justify promotion. | Built scratch FGP3 residual candidates with `scripts/build-fg3-temporal-residual-pack.py`, confirming both packs expand `1444370 -> 1601445` bytes. Backed up `generated/ps1/foreground/BUILDING6.FG2` and `BUIL6LOW.FG2`, temporarily replaced them with the FGP3 candidates, and ran paired BUILDING6 high/low against `scratch/ps1-perf-iterate/activity9-dead-readgroup-prune-v082-canaries/20260507-132441-4070377/summary.json` with layout movement allowed, work-identity disabled, zero regression allowance, and `--require-improvement`. Original packs were restored afterward. Artifact: `scratch/ps1-perf-iterate/building6-direct-fgp3-v154-focused/20260508-120136-3441207/summary.json`. | Failed/no promotion. Direct FGP3 made both tides materially slower. High regressed `scene_vb 2784 -> 2882`, `loop_vb 2520 -> 2618`, `target_vb 2442 -> 2418`, `overrun_vb 78 -> 200`, and `blocking_vb 62 -> 283`; low regressed `2780 -> 2886`, `2515 -> 2621`, `2437 -> 2419`, `78 -> 202`, and `70 -> 292`. Hidden refill improved (`64 -> 46` high, `66 -> 44` low), but the read count and visible CD cost worsened (`loop_reads 74/73 -> 88/88`, `loop_read_vb 423/420 -> 527/524`, `due_misses 1/2 -> 40/42`). High LBA stayed `10754`; low moved `11460 -> 11536`; the PS-EXE bucket stayed `215040`. | Close direct BUILDING6 FGP3 conversion, including explicit layout movement. The runtime residual format is the wrong shape for BUILDING6 as-is: it trades hidden refill debt for much larger visible CD pressure and slower targets. Continue BUILDING6 only with a genuinely shrinking/selective/keyframed encoder, generated scheduler ownership, or a motion format that preserves RAM-mirror/dirty-state invariants without raising active read pressure. |
| 2026-05-08 | `visitor5-compact-fgp3-v155` | Log-only; packs restored. | Try the successful compact-FGP3/v4 data-shape lane on the next small under-99 FGP2 outlier after the top rows were blocked on generated scheduler/safe upload ownership. The size gate showed both VISITOR5 packs can become compact FGP3/v4 restore-minus-current packs inside the original footprint, so the binary question was whether the active-loop win passes strict scene/CD gates. | Ran a fresh VISITOR5 high/low control first (`scratch/ps1-perf-iterate/visitor5-current-v155-control/20260508-120852-3483846/summary.json`). Built FGP3 residual candidates, then cleanup metadata compaction, PAL4 draw metadata compaction, restore-minus-current, draw-tail trim, and zero-noop compaction. Both active packs shrink to `279245` bytes and were padded back to `354227` bytes. Temporarily replaced `VISITOR5.FG2` and `VIST5LOW.FG2`, ran high/low with fixed layout, zero regression allowance, `--require-improvement`, and work-identity disabled, then restored the original packs. Artifact: `scratch/ps1-perf-iterate/visitor5-compact-fgp3-v155-focused/20260508-121059-3498041/summary.json`. | Failed/no promotion. The active-loop signal is real but strict scene/CD gates reject it. High improves `loop_vb 1112 -> 1102`, `target_vb 1090 -> 1093`, `overrun_vb 22 -> 9`, `blocking_vb 12 -> 8`, `prefetch_overrun_vb 12 -> 8`, and `loop_reads 25 -> 11`, but setup/scene cost regresses `scene_vb 1369 -> 1382`. Low improves `1111 -> 1104`, `1092 -> 1093`, `19 -> 11`, and `25 -> 11`, but regresses `scene_vb 1370 -> 1385`, `blocking_vb 7 -> 10`, and `prefetch_overrun_vb 7 -> 10`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Do not promote paired VISITOR5 compact FGP3/v4 under the current setup-prime policy. It is useful evidence that compact conversion can make the active loop green, but the auto-resident FGP3 setup cost and low-tide CD drift erase strict promotion. Retry only as high-tide-only, with a setup-prime cap/disable strategy, or after generated scheduler ownership can preserve the loop win without full-scene or low-tide CD regressions. |
| 2026-05-08 | `building1-compact-fgp3-v156` | Log-only; packs restored. | Try the compact-FGP3/v4 data-shape lane on BUILDING1 after VISITOR5 showed an active-loop win but a setup-prime full-scene regression. The size gate showed both BUILDING1 packs can become compact FGP3/v4 restore-minus-current packs inside the original footprint, so the binary question was whether the smaller active payload is enough to pass the strict scene and CD gates. | Ran a fresh BUILDING1 high/low control first (`scratch/ps1-perf-iterate/building1-current-v156-control/20260508-121504-3523685/summary.json`). Built FGP3 residual candidates, then cleanup metadata compaction, PAL4 draw metadata compaction, restore-minus-current, draw-tail trim, and zero-noop compaction. Both active packs shrink to `240140` bytes and were padded back to `318131` bytes. Temporarily replaced `BUILDING1.FG2` and `BUIL1LOW.FG2`, ran high/low with fixed layout, zero regression allowance, `--require-improvement`, and work-identity disabled, then restored the original packs. Artifact: `scratch/ps1-perf-iterate/building1-compact-fgp3-v156-focused/20260508-121659-3535426/summary.json`. | Failed/no promotion. The active-loop signal is strong, but setup/scene cost fails the strict gate. High improves `loop_vb 792 -> 781`, `target_vb 778 -> 788`, `overrun_vb 14 -> 0`, `blocking_vb 17 -> 2`, `prefetch_overrun_vb 17 -> 2`, and `loop_reads 23 -> 8`, but `scene_vb` regresses `1028 -> 1040`. Low improves `794 -> 781`, `779 -> 788`, `15 -> 0`, `21 -> 3`, `21 -> 3`, and `23 -> 8`, but `scene_vb` regresses `1032 -> 1041`. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. | Do not promote paired BUILDING1 compact FGP3/v4 under the current auto-resident setup-prime policy. This repeats the compact-lane pattern: active loops become green, but auto-resident FGP3 setup cost regresses full scene time. Retry only with setup-prime capped/disabled, generated scheduler ownership, or an explicit setup-only acceptance rule for under-target active loops. |
| 2026-05-08 | `building1-compact-fgp3-noautoprime-v157` | `ps1: compact building1 foreground packs` | Promote the BUILDING1 compact-FGP3 lane by removing the failed auto-resident setup-prime assumption from v156. The binary question was whether the same compact packs still improve the active loop and full scene time when BUILDING1 skips only the automatic FGP3 setup-prime path. | Added a BUILDING1-only skip for automatic FGP3 setup-prime and mirrored it in `scripts/ps1-foreground-read-plan.py`. Kept the compact FGP3/v4 restore-minus-current packs from the v156 size gate, padded both back to the original `318131` byte footprints, and ran focused high/low against the fresh BUILDING1 control. Then ran broad canaries across VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high. Artifacts: focused `scratch/ps1-perf-iterate/building1-compact-fgp3-noautoprime-v157-focused/20260508-122252-3570112/summary.json`; broad `scratch/ps1-perf-iterate/building1-compact-fgp3-noautoprime-v157-broad/20260508-122624-3591332/summary.json`. | Promoted. BUILDING1 high improves `scene_vb 1028 -> 1020`, `loop_vb 792 -> 784`, `target_vb 778 -> 782`, `overrun_vb 14 -> 2`, `blocking_vb 17 -> 15`, `prefetch_overrun_vb 17 -> 9`, `loop_reads 23 -> 17`, and `loop_read_vb 108 -> 76`. Low improves `1032 -> 1025`, `794 -> 787`, `779 -> 782`, `15 -> 5`, `21 -> 16`, `21 -> 14`, `23 -> 17`, and `113 -> 76`. Both rows move into the green band, pack LBAs stay fixed at `5868/6024`, pack bytes stay `318131`, and the PS-EXE bucket stays `215040`. Broad canaries stayed on their accepted profiles. | Keep as the new BUILDING1 baseline. The public battle-card rollup moves from `+0.5526%` to `+0.5281%` over target and from `99.4709%` to `99.4747%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.2404%` / `100.2410%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.87` public over-target points and added about `12.37` public target-speed points. Next targets are VISITOR3 low/high, BUILDING2 low, WALKSTUF1 high/low, BUILDING6 high/low, and remaining under-99 rows. |
| 2026-05-08 | `visitor5-high-compact-fgp3-noautoprime-v158` | `ps1: compact visitor5 high foreground pack` | Promote the safe half of the VISITOR5 compact-FGP3 lane after the paired v155 and no-autoprime retries showed that high tide benefits but low tide regresses CD debt. The binary question was whether high tide alone can keep the compact active-loop win with auto FGP3 setup-prime disabled while low tide stays on the prior FGP2 pack. | Ran a fresh VISITOR5 high/low control (`scratch/ps1-perf-iterate/visitor5-noautoprime-v158-control/20260508-125414-3753346/summary.json`), built high/low compact FGP3/v4 restore-minus-current assets, then restored `VIST5LOW.FG2` and kept only `VISITOR5.FG2` as a padded compact pack inside the original `354227` byte footprint. Added VISITOR5 to the auto-FGP3 setup-prime skip and mirrored that in `scripts/ps1-foreground-read-plan.py`. The paired compact/no-autoprime run rejected low tide (`scratch/ps1-perf-iterate/visitor5-compact-fgp3-noautoprime-v158-focused/20260508-125642-3769107/summary.json`); the high-only promotion gate passed at `scratch/ps1-perf-iterate/visitor5-high-compact-fgp3-noautoprime-v158-promote-high/20260508-130112-3797427/summary.json`; broad canaries passed at `scratch/ps1-perf-iterate/visitor5-high-compact-fgp3-noautoprime-v158-broad-strict/20260508-131142-3861593/summary.json`. | Promoted high tide only. High improves `scene_vb 1369 -> 1361`, `loop_vb 1111 -> 1104`, `target_vb 1090 -> 1092`, `overrun_vb 21 -> 12`, `blocking_vb 12 -> 11`, `prefetch_overrun_vb 12 -> 11`, `loop_reads 25 -> 19`, and `loop_read_vb 123 -> 91`; pack LBA stays fixed at `24082`, pack bytes stay `354227`, and the PS-EXE bucket stays `215040`. Low tide is not promoted because the paired compact/no-autoprime retry regressed `blocking_vb 7 -> 15` and `prefetch_overrun_vb 7 -> 15`; with the original FGP2 low pack restored, low stayed exact-flat as a canary. | Keep as the new VISITOR5 high baseline and keep VISITOR5 low on `visitor5-v072-prefetch-relief`. The public battle-card rollup moves from `+0.5281%` to `+0.5215%` over target and from `99.4747%` to `99.4812%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.2471%` / `100.2478%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.88` public over-target points and added about `12.38` public target-speed points. Next targets are VISITOR3 low/high, BUILDING2 low, WALKSTUF1 high/low, BUILDING6 high/low, VISITOR5 low, and remaining under-99 rows. |
| 2026-05-08 | `building2-low-group95-111-v159` | Log-only; source restored. | Try the next BUILDING2 low local read-group candidate after the `318..330` and `365..381` groups. The binary question was whether pulling entries `95..111` into a local group could reduce loop reads without worsening the already-tight low-tide timing profile. | Tested a guarded table entry `{95,111,8}` first, then an unguarded `{95,111,0}` retry after the guarded form proved inert. Artifacts: guarded `scratch/ps1-perf-iterate/building2-low-group95-111-minslack8-v159-focused/20260508-133835-4019912/summary.json`; unguarded `scratch/ps1-perf-iterate/building2-low-group95-111-unguarded-v159-focused/20260508-133959-4028853/summary.json`. The temporary read-group edit was restored before logging. | Failed/no promotion. The guarded form was exact-flat and did not activate the useful read movement. The unguarded form reduced `loop_reads 55 -> 53` and `loop_read_vb 251 -> 243`, but regressed `scene_vb 1657 -> 1659`, `loop_vb 1383 -> 1385`, `target_vb 1304 -> 1302`, `overrun_vb 79 -> 83`, `blocking_vb 118 -> 120`, and `prefetch_overrun_vb 5 -> 11`; it also moved code layout enough to trip symbol-address drift. | Keep BUILDING2 low on `building2-low-group365-381-v110`. Close `95..111` as a local table-group lane unless generated scheduler ownership can reserve enough slack around the moved reads; the read saving is real, but it is bought by worse user-facing timing. |
| 2026-05-08 | `building2-low-restore-window-slack4-v160` | `ps1: restore-trim building2 low with slack guard` | Promote the previously rejected BUILDING2 low restore-minus-current data-shape win after v144 proved a higher window guard could eliminate hidden refill but `5` VBlanks over-starved visible cadence. The binary question was whether a `4` VBlank BUILDING2-low window guard would keep the pack-side read/work reduction without visible or hidden regressions. | Generated a size-preserving `BUIL2LOW.FG2` restore-minus-current candidate with `scripts/compact-fgp3-restore-minus-current.py --pad-to-input-size`, reducing active payload `789906 -> 674798`, cleanup spans `109076 -> 53563`, cleanup pixels `468636 -> 237286`, and modeled restore bytes `937272 -> 474572` while keeping the pack at `1303332` bytes. Added a BUILDING2-low `FG_BUILDING2_LOW_WINDOW_MIN_SLACK_VBLANKS=4` branch to the scene-aware window slack threshold. Artifacts: focused high/low probe `scratch/ps1-perf-iterate/building2-low-restore-window-slack4-v160-focused/20260508-135004-4087501/summary.json`; broad canary pass `scratch/ps1-perf-iterate/building2-low-restore-window-slack4-v160-broad/20260508-135227-4102888/summary.json`. | Promoted. BUILDING2 low improves `scene_vb 1657 -> 1623`, `loop_vb 1383 -> 1349`, `target_vb 1304 -> 1316`, `overrun_vb 79 -> 33`, `blocking_vb 118 -> 83`, `prefetch_overrun_vb 5 -> 1`, `loop_reads 55 -> 54`, `loop_read_vb 251 -> 227`, and `due_misses 22 -> 19`. Pack LBA stays `6817`, pack sectors stay `637`, pack bytes stay `1303332`, and the PS-EXE bucket stays `215040`. VISITOR3 high/low, BUILDING2 high, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high broad canaries stayed exact-flat. | Keep as the new BUILDING2 low baseline. The public battle-card rollup moves from `+0.5215%` to `+0.4933%` over target and from `99.4812%` to `99.5091%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.2752%` / `100.2759%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.91` public over-target points and added about `12.41` public target-speed points. Next targets are VISITOR3 low/high, WALKSTUF1 high/low, BUILDING6 high/low, BUILDING2 high/low residual work, VISITOR5 low, and remaining under-99 rows. |
| 2026-05-08 | `visitor3-low-group317-341-v161` | Log-only; source restored. | Test the top VISITOR3 low 24-sector read candidate from the v160 read-plan after local 12/16-sector VISITOR3 groups were exhausted. The planner predicted `317..341` could save three reads if a larger grouped window could own the append cadence, so the binary question was whether a low-only runtime table plus the existing larger group-capacity allocation would produce any measurable win before building generated scheduler metadata. | Temporarily added a low-tide-only VISITOR3 read-group table with `{317,341,0}` and selected it only for `visitor3` low tide, then built and ran VISITOR3 low against the v160 broad baseline with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded symbol drift. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group317-341-v161-focused/20260508-141332-35310/summary.json`. | Failed/no promotion. The probe was exact-flat on timing and CD metrics: `scene_vb=1426`, `loop_vb=1126`, `target_vb=1025`, `overrun_vb=101`, `blocking_vb=170`, `prefetch_overrun_vb=0`, `loop_reads=31`, `loop_read_vb=179`, and `due_misses=29`. It only grew/shuffled hot code (`foregroundPilotPlay size +48`, downstream symbol addresses `+48`) with no key metric improvement. | Close `317..341` as a local VISITOR3 low table. The range remains evidence for generated scheduler/larger-window ownership, but the hand-authored runtime table does not activate a useful read movement on the current baseline. The public battle-card baseline remains `+0.4933%` over target / `99.5091%` target speed; next VISITOR3 work should use generated scheduler metadata, safe upload/precomposed payloads, or another pack-side data-shape reduction rather than local table groups. |
| 2026-05-08 | `visitor3-low-group317-341-window24-v162` | Log-only; source restored. | Close the remaining ambiguity from v161: that probe used a 24-sector table, but the shared grouped-window allocation still capped the stream buffer at the 16-sector `FG_PREFETCH_GROUP_WINDOW_BYTES`. The binary question was whether giving VISITOR3 low this specific 24-sector group capacity would let the predicted `317..341` read movement actually fire. | Temporarily added `FG_VISITOR3_GROUP_WINDOW_BYTES = 24 sectors`, selected `{317,341,0}` only for `visitor3` low tide, and raised only that grouped path's allocation capacity to `24` sectors while leaving Fishing3/Building2 groups at the existing 16-sector capacity. Built and ran VISITOR3 low against the v160 broad baseline with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded symbol drift. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group317-341-window24-v162-focused/20260508-143043-132631/summary.json`. | Failed/no promotion. The larger-capacity probe was still exact-flat: `scene_vb=1426`, `loop_vb=1126`, `target_vb=1025`, `overrun_vb=101`, `blocking_vb=170`, `prefetch_overrun_vb=0`, `loop_reads=31`, `loop_read_vb=179`, and `due_misses=29`. It only grew/shuffled hot code (`foregroundPilotPlay size +52`, downstream symbol addresses `+52`) with no key metric improvement. | Do not retry VISITOR3 low `317..341` through hand-authored local read groups, even with a 24-sector stream capacity. This confirms the next VISITOR3 read lane needs generated scheduler/refill ownership or a pack-side data-shape change that changes real active read timing, not another scalar table/capacity variant. Public battle-card baseline remains `+0.4933%` over target / `99.5091%` target speed. |
| 2026-05-08 | `walkstuf3-high-compact-fgp3-v163` | `ps1: compact walkstuf3 high foreground pack` | Move from exhausted VISITOR3 local-table probes to the next under-99 pack-side data-shape lane. WALKSTUF3 high was still FGP3/v1 and had enough internal metadata/cleanup redundancy to test whether a compact FGP3/v4 restore-minus-current transform could reduce CD and render pressure without changing the public CD footprint. | Generated a padded `WALKSTUF3.FG2` candidate through `compact-fgp3-residual-metadata`, `compact-fgp3-pal4-draw-metadata`, `compact-fgp3-restore-minus-current`, and `compact-fgp3-trim-draw-tails`, keeping the pack at `1026922` bytes while reducing active payload `1017926 -> 538132`, cleanup spans `79508 -> 45389`, cleanup pixels `340997 -> 192886`, and modeled restore bytes `681994 -> 385772`. Ran a fresh current-layout high/low control, a high-only strict promotion gate, and a broad canary run. Artifacts: control `scratch/ps1-perf-iterate/walkstuf3-current-v163-control/20260508-143927-182215/summary.json`; high gate `scratch/ps1-perf-iterate/walkstuf3-high-compact-fgp3-v163-promote-high/20260508-144448-213113/summary.json`; broad `scratch/ps1-perf-iterate/walkstuf3-high-compact-fgp3-v163-broad/20260508-144619-221837/summary.json`. | Promoted high tide only. Against current control, high improves `scene_vb 2593 -> 2578`, `loop_vb 2325 -> 2310`, `target_vb 2282 -> 2290`, `overrun_vb 43 -> 20`, `blocking_vb 65 -> 47`, `prefetch_overrun_vb 32 -> 18`, `loop_reads 74 -> 37`, and `loop_read_vb 378 -> 183`; due misses stay `6`. Pack bytes stay `1026922`, pack sectors stay `502`, current-layout pack LBA stays `26404`, and the PS-EXE bucket stays `215040`. The unchanged low-tide pack was refreshed as a current-layout canary at `2321/2293`; VISITOR3, BUILDING2, BUILDING4, ACTIVITY9 low, and FISHING1 high broad canaries stayed on their accepted profiles. | Keep as the new WALKSTUF3 high baseline and keep low tide on the existing pack. The public battle-card rollup moves from `+0.4933%` to `+0.4860%` over target and from `99.5091%` to `99.5164%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.2825%` / `100.2833%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.91` public over-target points and added about `12.42` public target-speed points. Next targets are VISITOR3 low/high, WALKSTUF1 high/low, BUILDING6 high/low, BUILDING2 residual work, VISITOR5 low, ACTIVITY9 high, and remaining under-99 rows. |
| 2026-05-08 | `visitor3-low-group325-341-v164` | Log-only; source restored. | Test the highest-ranked untried VISITOR3 low read-plan candidate after v161/v162 closed the `317..341` variants. The planner marked `325..341` as a current 16-sector fit that could save two reads if the append fired, so this checked whether the narrower range avoids the inert larger-window behavior. | Temporarily added a low-tide-only VISITOR3 retained read group `{325,341,0}` and ran VISITOR3 low against the v160 broad baseline with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded symbol drift. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group325-341-v164-focused/20260508-151457-381924/summary.json`. | Failed/no promotion. The run was exact-flat on every key metric: `scene_vb=1426`, `loop_vb=1126`, `target_vb=1025`, `overrun_vb=101`, `blocking_vb=170`, `prefetch_overrun_vb=0`, `loop_reads=31`, `loop_read_vb=179`, and `due_misses=29`. It only grew/shuffled hot code (`foregroundPilotPlay size +36`, downstream symbol addresses `+36`) with no key metric improvement. | Close `325..341` as another local VISITOR3 low table. Together with `317..341`, this confirms the current VISITOR3 low read-plan candidates are scheduler-owned-only: local tables either do not fire useful movement or need generated metadata to reserve/own the active CD phase. Public battle-card baseline remains `+0.4860%` over target / `99.5164%` target speed. |
| 2026-05-08 | `building6-compact-fgp3-v165` | `ps1: compact building6 foreground packs` | Move BUILDING6 from exhausted scalar/read-group/direct-FGP3 lanes into the compact FGP3/v4 restore-minus-current chain. The binary question was whether metadata/draw compaction plus restore-minus-current could turn the raw FGP3 expansion into a same-footprint shrinking pack and improve both tides. | Built raw FGP3 candidates that expanded `1444370 -> 1601445`, then compacted residual metadata (`1601445 -> 1287041`), PAL4 draw metadata (`1287041 -> 970725`), and restore-minus-current payloads (`active_payload 964453 -> 794537`, restore bytes `1143878 -> 489702`) before padding both packs back to `1444370`. A strict baseline run failed only the intentional work-identity restore-call guard (`1530 -> 309/310`), so the accepted gate reran with work identity disabled for this data-shape promotion. Artifacts: accepted gate `scratch/ps1-perf-iterate/building6-compact-fgp3-v165-acceptance-workfree/20260508-152652-450497/summary.json`; broad canaries `scratch/ps1-perf-iterate/building6-compact-fgp3-v165-broad-canaries/20260508-152957-468470/summary.json`; work-identity guard evidence `scratch/ps1-perf-iterate/building6-compact-fgp3-v165-acceptance/20260508-152359-433535/summary.json`. | Promoted. BUILDING6 high improves `scene_vb 2784 -> 2746`, `loop_vb 2520 -> 2482`, `target_vb 2442 -> 2457`, `overrun_vb 78 -> 25`, `blocking_vb 62 -> 25`, `prefetch_overrun_vb 64 -> 27`, `loop_reads 74 -> 42`, `loop_read_vb 423 -> 243`, and `due_misses 1 -> 0`. Low improves `2780 -> 2751`, `2515 -> 2485`, `2437 -> 2456`, `78 -> 29`, `70 -> 28`, `66 -> 29`, `73 -> 42`, `420 -> 243`, and `2 -> 0`. Pack LBAs stay `10754/11460`, sectors stay `706`, and the PS-EXE bucket stays `215040`. VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high broad canaries stayed exact-flat. | Keep as the new BUILDING6 baseline. Public rollup moves from `+0.4860%` to `+0.4527%` over target and from `99.5164%` to `99.5662%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.3158%` / `100.3500%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.95` public over-target points and added about `12.47` public target-speed points. Next targets are VISITOR3 low/high, WALKSTUF1 high/low, BUILDING2 high/low residual work, VISITOR5 low, ACTIVITY9 high, JOHNNY1 high/low, BUILDING4 low, BUILDING6 high/low residual work, and remaining under-99 rows. |
| 2026-05-08 | `walkstuf1-low-direct-stage-cap-v166` | Log-only; source restored. | Test whether WALKSTUF1 low's remaining scheduler-owned gap could be improved by reducing only the exact-min-slack direct-stage threshold, forcing medium-small frames through the stream-window path while preserving the global `8 KiB` shortcut elsewhere. The binary question was whether the known scheduler signal could be narrowed enough to reduce blocking without moving hidden refill debt into visible timing. | Temporarily added a WALKSTUF1-low direct-stage max-byte override and tried `4 KiB` then `6 KiB` against the current `walkstuf1-low-canary` baseline with zero regression allowance and `--require-improvement`. The `4 KiB` run changed behavior; the `6 KiB` midpoint kept baseline behavior. Source was restored after both gates failed. Artifacts: `4 KiB` probe `scratch/ps1-perf-iterate/20260508-160045-641142/summary.json`; `6 KiB` probe `scratch/ps1-perf-iterate/20260508-160157-648315/summary.json`. | Failed/no promotion. The `4 KiB` cap reduced visible CD pressure (`blocking_vb 86 -> 74`, `loop_reads 69 -> 61`, `due_misses 12 -> 6`) but regressed user-facing timing and hidden refill debt (`overrun_vb 62 -> 64`, `prefetch_overrun_vb 27 -> 42`, `target_vb 1427 -> 1425`). The `6 KiB` cap was exact-flat on key metrics (`scene_vb=1780`, `loop_vb=1489`, `overrun_vb=62`, `blocking_vb=86`, `prefetch_overrun_vb=27`, `loop_reads=69`, `due_misses=12`) with only source/code-shape churn and no improvement. | Do not promote scalar direct-stage cap tuning for WALKSTUF1 low. The useful schedule movement exists, but the narrow cap either shifts debt into hidden prefetch overrun or does nothing; the next WALKSTUF1 lane needs generated scheduler/cost ownership, safe pack-side payload reduction, or a broader data-shape change rather than another threshold-only variant. Public battle-card baseline remains `+0.4527%` over target / `99.5662%` target speed. |
| 2026-05-08 | `activity9-high-compact-fgp3-v167` | `ps1: compact activity9 high foreground pack` | Move ACTIVITY9 high from the older padded FGP3/v1 shape into the compact FGP3/v4 restore-minus-current chain after the paired earlier cleanup-metadata attempt proved high needed tide-specific promotion. The binary question was whether high tide alone could shrink active payload and CD pressure while low tide stayed exact-flat as a canary. | Generated a same-footprint `ACTIVITY9.FG2` candidate through residual-metadata compaction (`1745484 -> 1201887`), PAL4 draw-metadata compaction (`1201887 -> 947523`), restore-minus-current (`947523 -> 782205`), and draw-tail trim (`782205 -> 782197`) before padding back to `1745484`. Active payload drops `1453793 -> 776893`; cleanup spans drop `112361 -> 36308`; cleanup pixels drop `697279 -> 194715`; modeled restore bytes drop `1394558 -> 389430`. Artifacts: focused high gate `scratch/ps1-perf-iterate/20260508-160629-673911/summary.json`; low-tide exact-flat canary `scratch/ps1-perf-iterate/20260508-160745-681573/summary.json`; broad canaries `scratch/ps1-perf-iterate/activity9-high-compact-fgp3-v167-broad/20260508-160931-691785/summary.json`; high row refresh `scratch/ps1-perf-iterate/activity9-high-compact-fgp3-v167-broad/20260508-161916-749341/summary.json`. | Promoted high tide only. ACTIVITY9 high improves `scene_vb 2343 -> 2331`, `loop_vb 2094 -> 2082`, `target_vb 2056 -> 2062`, `overrun_vb 38 -> 20`, `blocking_vb 37 -> 24`, `prefetch_overrun_vb 23 -> 17`, `loop_reads 52 -> 25`, `loop_read_vb 378 -> 187`, and `due_misses 2 -> 1`. Pack LBA stays `3108`, sectors stay `853`, and the PS-EXE bucket stays `215040`. ACTIVITY9 low stayed exact-flat (`2085/2058`, `blocking_vb=29`, `prefetch_overrun_vb=12`, `due_misses=3`); VISITOR3, BUILDING2, BUILDING4, and FISHING1 broad canaries stayed exact-flat. | Keep as the new ACTIVITY9 high baseline and keep low tide on its existing pack. Public rollup moves from `+0.4527%` to `+0.4457%` over target and from `99.5662%` to `99.5730%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.3228%` / `100.3568%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.95` public over-target points and added about `12.47` public target-speed points. Next targets are VISITOR3 low/high, WALKSTUF1 high/low, BUILDING2 high/low residual work, VISITOR5 low, JOHNNY1 high/low, BUILDING4 low, BUILDING6 residual work, ACTIVITY9 low, and remaining under-99 rows. |
| 2026-05-08 | `walkstuf1-setup-prime-cap-v168` | Log-only; source restored. | Retest WALKSTUF1 setup-prime resident caps after compact FGP3/v4 made the existing high `144 KiB` / low `160 KiB` policy suspect. The binary question was whether reducing residency would relieve active-loop pressure, or whether adding a little more coverage would recover loop time without visible blocking regression. | Temporarily tested high tide at reduced `128 KiB`, then increased caps `160 KiB` high / `176 KiB` low, then midpoint caps `152 KiB` high / `168 KiB` low. Each probe was built and run against the current `walkstuf1-compact-fgp3-v141` high baseline with zero regression allowance and `--require-improvement`. Source was restored to the accepted `144 KiB` high / `160 KiB` low caps afterward; pack LBAs and the `215040` byte PS-EXE bucket stayed fixed. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-setupprime128-v168-high/20260508-163915-859711/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-setupprime-plus-v168-high/20260508-164042-868243/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-setupprime-mid-v168-high/20260508-164213-877073/summary.json`. | Failed/no promotion. The `128 KiB` reduction improved hidden refill (`prefetch_overrun_vb 32 -> 22`) but regressed high `loop_vb 1491 -> 1494`, `overrun_vb 65 -> 66`, and `blocking_vb 85 -> 92`. Both larger-cap probes improved total loop by one VBlank (`1491 -> 1490`) and hidden refill (`32 -> 28`) but failed the strict gate because visible blocking regressed `85 -> 87`; the midpoint and larger cap produced the same high-tide metrics. | Keep WALKSTUF1 setup-prime caps at `144 KiB` high / `160 KiB` low. Scalar setup-prime cap tuning is closed for the current compact pack: less residency loses visible loop time, more residency trades a tiny loop win for higher blocking. The next WALKSTUF1 attempt needs generated scheduler/cost ownership, selective upload-ready data with safe pixels, or a pack/data-shape change rather than another resident-cap scalar. Public battle-card baseline remains `+0.4457%` over target / `99.5730%` target speed. |
| 2026-05-08 | `building2-high-window-slack4-v169` | Log-only; source restored. | Test whether BUILDING2 high can share the low-tide `4` VBlank window-refill slack guard that promoted `building2-low-restore-window-slack4-v160`. The binary question was whether the stricter high-tide guard would trade a little hidden refill for a net loop/blocking win, or whether high and low have different scheduler knees. | Temporarily removed the low-tide qualifier from the BUILDING2 window-min-slack branch so both tides used `FG_BUILDING2_LOW_WINDOW_MIN_SLACK_VBLANKS=4`. Built and ran BUILDING2 high against the accepted `building2-low-group365-381-v110` high baseline with zero regression allowance and `--require-improvement`. Source was restored afterward; BUILDING2 high pack LBA stayed `6180`, and the PS-EXE bucket stayed `215040`. Artifact: `scratch/ps1-perf-iterate/building2-high-window-slack4-v169/20260508-164806-910120/summary.json`. | Failed/no promotion. The stricter guard reduced hidden refill (`prefetch_overrun_vb 12 -> 4`) but badly regressed visible playback: `scene_vb 1599 -> 1611`, `loop_vb 1349 -> 1361`, `overrun_vb 33 -> 47`, `blocking_vb 48 -> 126`, `loop_reads 61 -> 69`, and `due_misses 7 -> 28`. | Keep BUILDING2 high on the default `3` VBlank window slack and BUILDING2 low on the promoted `4` VBlank guard. This closes the high-side scalar slack-guard reuse lane; future BUILDING2 high work needs generated scheduler/refill ownership or a different pack/data-shape change, not the low-tide window knee. Public battle-card baseline remains `+0.4457%` over target / `99.5730%` target speed. |
| 2026-05-08 | `visitor5-low-compact-slack-v170` | Log-only; source and pack restored. | Retry the rejected VISITOR5 low compact-FGP3/v4 data-shape lane with explicit low-tide window-slack variants. The fresh same-commit control showed the older matrix row was stale: current low is `1111/1092` with `blocking_vb=7` and `prefetch_overrun_vb=7`, not the public row's older `1112/1090` with `12/12`. The binary question was whether compact payload reduction plus a low-tide slack guard could keep the loop win without the v158 visible-CD regression. | Generated a compact low-tide candidate from `VIST5LOW.FG2`: raw FGP3 expanded `350523 -> 472905`, then cleanup metadata, draw metadata, restore-minus-current, and draw-tail trim reduced active payload to `275541` bytes; the candidate fits inside the original `354227` byte footprint. Ran a fresh current-control low case, then tested the padded compact pack with a low-tide `2` VBlank window slack and a low-tide `4` VBlank window slack, each against that fresh control with zero regression allowance and work identity disabled. Source and pack were restored afterward. Artifacts: control `scratch/ps1-perf-iterate/visitor5-low-current-v170-control/20260508-165133-929669/summary.json`; slack2 `scratch/ps1-perf-iterate/visitor5-low-compact-slack2-v170/20260508-165317-939749/summary.json`; slack4 `scratch/ps1-perf-iterate/visitor5-low-compact-slack4-v170/20260508-165442-948055/summary.json`; transform scratch root `scratch/visitor5-low-compact-v170/`. | Failed/no promotion. Slack2 reproduced the v158 failure shape: loop improved `1111 -> 1109`, scene improved `1370 -> 1368`, and reads dropped `25 -> 19`, but visible and hidden CD both regressed `7 -> 15`. Slack4 increased the loop win (`1111 -> 1103`, overrun `19 -> 11`) and slightly improved hidden refill (`7 -> 6`), but visible blocking regressed harder `7 -> 22`, loop reads rose to `21`, and due misses appeared (`0 -> 3`). Pack LBA stayed `24255` and the PS-EXE bucket stayed `215040`. | Do not promote VISITOR5 low compact FGP3/v4 with scalar window slack. The payload/work signal is real, but every scalar scheduler variant turns it into worse visible CD pressure. Keep low tide on the FGP2 `visitor5-v072-prefetch-relief` baseline; future work needs generated scheduler/read ownership or a different pack layout that preserves the compact loop win without `blocking_vb` regression. Public battle-card baseline remains `+0.4457%` over target / `99.5730%` target speed until the stale VISITOR5 low row is intentionally refreshed. |
| 2026-05-08 | `walkstuf3-low-compact-fgp3-v171` | `ps1: compact walkstuf3 low foreground pack` | Promote the low-tide half of WALKSTUF3 after the high compact-FGP3/v4 pass left low as an unchanged under-99 canary. | Generated a same-footprint `WALK3LOW.FG2` candidate through raw temporal residual FGP3, residual metadata compaction, PAL4 draw metadata compaction, restore-minus-current, draw-tail trim, and zero-noop check, padding back to `994669` bytes. Active payload drops `986873 -> 429436`, cleanup spans `62722 -> 35300`, cleanup pixels `266273 -> 149620`, and modeled restore bytes `532546 -> 299240`. Artifacts: transform root `scratch/walkstuf3-low-compact-fgp3-v171/`; focused gate `scratch/ps1-perf-iterate/walkstuf3-low-compact-fgp3-v171-focused/20260508-165936-975752/summary.json`; broad controls `scratch/ps1-perf-iterate/walkstuf3-low-compact-fgp3-v171-broad-norequire/20260508-171152-1045430/summary.json`; WALKSTUF3 high canary `scratch/ps1-perf-iterate/walkstuf3-low-compact-fgp3-v171-high-canary/20260508-172149-1102179/summary.json`. | Promoted. Low improves `scene_vb 2584 -> 2567`, `loop_vb 2321 -> 2310`, `target_vb 2293 -> 2295`, `overrun_vb 28 -> 15`, `blocking_vb 41 -> 26`, `prefetch_overrun_vb 21 -> 17`, `loop_reads 72 -> 29`, `loop_read_vb 359 -> 150`, and `due_misses 5 -> 2`. Pack LBA stays `26906`, sectors stay `486`, and the PS-EXE bucket stays `215040`. VISITOR3, BUILDING2, BUILDING4, ACTIVITY9 low, FISHING1 high, and WALKSTUF3 high canaries stayed exact-flat. | Keep as the new WALKSTUF3 low baseline. Public rollup moves from `+0.4457%` to `+0.4412%` over target and from `99.5730%` to `99.5774%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.3273%` / `100.3612%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.96` public over-target points and added about `12.48` public target-speed points. WALKSTUF3 low is now green at `99.35%`; next top outliers remain VISITOR3 low/high, WALKSTUF1 high/low, BUILDING2 high/low, VISITOR5 low, JOHNNY1 high/low, BUILDING4 low, ACTIVITY9 low, and BUILDING6/JOHNNY6 residual rows. |
| 2026-05-08 | `walkstuf1-high-direct-stage-range178-194-v172` | Log-only; source restored. | Test the documented WALKSTUF1 frame/range-specific direct-stage lane after scalar threshold probes failed. The read planner marked high-tide sector range `178..194` as a current-group-compatible scheduler-owned candidate that could save two reads with lower visible risk than the tighter `84..100` cluster. The binary question was whether a narrow local range policy could force that cluster through the retained-window path without taking the broader hidden-refill debt seen in scalar cap probes. | Temporarily added a WALKSTUF1-high-only direct-stage deny for entries overlapping file sectors `178..194`, leaving the global `8 KiB` exact-min-slack direct-stage shortcut intact elsewhere. Built successfully in the `215040` byte PS-EXE bucket and ran the focused high case against the current `walkstuf1-compact-fgp3-v141` baseline. The harness warned that the baseline label did not match, so the result was judged manually against the accepted high row. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-direct-stage-range178-194-v172/20260508-174223-1215520/summary.json`. | Failed/no promotion. The range policy reduced hidden refill (`prefetch_overrun_vb 32 -> 26`) but regressed the actual high-tide playback: `scene_vb 1779 -> 1783`, `loop_vb 1491 -> 1495`, `target_vb 1426 -> 1427`, `overrun_vb 65 -> 68`, `blocking_vb 85 -> 95`, `loop_reads 69 -> 70`, `loop_read_vb 300 -> 301`, and `due_misses 13 -> 14`. | Do not promote or retry local WALKSTUF1 direct-stage range deny-lists. The cluster can be moved, but not safely without generated scheduler ownership/cost metadata; otherwise the attempted hidden grouped read becomes visible loop and blocking debt. Public battle-card baseline remains `+0.4412%` over target / `99.5774%` target speed. |
| 2026-05-08 | `johnny1-compact-fgp3-v173` | `ps1: compact johnny1 foreground packs` | Promote the compact-FGP3/v4 restore-minus-current chain for JOHNNY1 after the public row proved stale and the fresh current-layout control still showed both tides under 99%. The binary question was whether pack-side data-shape reduction could lower read pressure without changing black-backdrop scene behavior, pack LBAs, or the `215040` byte PS-EXE bucket. | Generated same-footprint `JOHNNY1.FG2` and `JOHN1LOW.FG2` candidates through residual metadata compaction, PAL4 draw metadata compaction, restore-minus-current, and draw-tail trim, padding both back to `448370` bytes. Active payload drops `446058 -> 316608`, cleanup spans `17583 -> 484`, cleanup pixels `295908 -> 204436`, and modeled restore bytes `591816 -> 408872`. Because the public matrix row came from the older `212992` byte PS-EXE layout, a fresh control was captured first. Artifacts: transform root `scratch/johnny1-compact-fgp3-v173/`; current-layout control `scratch/ps1-perf-iterate/johnny1-current-v173-control/20260508-175139-1267982/summary.json`; focused acceptance `scratch/ps1-perf-iterate/johnny1-compact-fgp3-v173-acceptance/20260508-175407-1282247/summary.json`; broad canaries `scratch/ps1-perf-iterate/johnny1-compact-fgp3-v173-broad/20260508-175757-1303705/summary.json`. | Promoted. High and low both improve `scene_vb 2074 -> 2070`, `loop_vb 1977 -> 1974`, `target_vb 1943 -> 1945`, `overrun_vb 34 -> 29`, `blocking_vb 31 -> 26`, `prefetch_overrun_vb 31 -> 26`, `loop_reads 16 -> 7`, `loop_read_vb 95 -> 56`, and `due_misses=0`. Pack LBAs stay `13982/14201`, sectors stay `219`, and the PS-EXE bucket stays `215040`. VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, ACTIVITY9 low, and FISHING1 high broad canaries stayed exact-flat. | Keep as the new JOHNNY1 baseline. Public rollup moves from `+0.4412%` to `+0.4404%` over target and from `99.5774%` to `99.5782%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.3281%` / `100.3620%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.96` public over-target points and added about `12.48` public target-speed points. JOHNNY1 remains under 99% but now has lower CD pressure; the refreshed read matrix moves JOHNNY1 balanced `131..147`, `145..161`, and `138..154` groups to the top scheduler/guarded queue. |
| 2026-05-08 | `activity9-low-compact-fgp3-v174` | `ps1: compact activity9 low foreground pack` | Promote the low-tide half of ACTIVITY9 after the high compact-FGP3/v4 pass left low as an unchanged under-99 canary. The binary question was whether low could use the same compact restore-minus-current shape as high without changing pack LBA, sectors, or the `215040` byte PS-EXE bucket. | Generated a same-footprint `ACTV9LOW.FG2` candidate through restore-minus-current and draw-tail trim, padding back to `1745484` bytes. Active payload drops `942219 -> 776893`, cleanup spans drop `112361 -> 36308`, cleanup pixels drop `697279 -> 194715`, modeled restore bytes drop `1394558 -> 389430`, and zero-tail slack grows `797961 -> 963287`. Artifacts: transform root `scratch/activity9-low-compact-v174/`; focused low/high gate `scratch/ps1-perf-iterate/activity9-low-compact-fgp3-v174-focused/20260508-184053-1538935/summary.json`; broad canaries `scratch/ps1-perf-iterate/activity9-low-compact-fgp3-v174-broad/20260508-184351-1556182/summary.json`; refreshed read-plan root `scratch/ps1-perf-iterate/activity9-low-compact-fgp3-v174-readplans/20260508-184351-combined`. | Promoted low tide. ACTIVITY9 low improves `scene_vb 2333 -> 2323`, `loop_vb 2085 -> 2075`, `target_vb 2058 -> 2061`, `overrun_vb 27 -> 14`, `blocking_vb 29 -> 17`, `loop_reads 59 -> 47`, `loop_read_vb 289 -> 232`, and `due_misses 3 -> 1`; hidden refill stays `prefetch_overrun_vb=12`. Pack LBA stays `3961`, sectors stay `853`, and the PS-EXE bucket stays `215040`. ACTIVITY9 high stays on its accepted compact profile; VISITOR3 high/low, BUILDING2 high/low, BUILDING4 high/low, and FISHING1 high broad canaries stayed on their accepted profiles. | Keep as the new ACTIVITY9 low baseline. Public rollup moves from `+0.4404%` to `+0.4353%` over target and from `99.5782%` to `99.5831%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.3331%` / `100.3669%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.96` public over-target points and added about `12.48` public target-speed points. The refreshed read matrix now ranks ACTIVITY9 low safe standalone `12..28` and `343..359` ranges above the JOHNNY1 scheduler/guarded queue. |
| 2026-05-08 | `visitor3-upload-ready-safety-v175` | Log-only; no source change. | Retest the top VISITOR3 same-footprint upload-ready/x-band lane against the new ACTIVITY9 low baseline before implementing another raw append. The binary question was whether the byte-fitting budgeted subset now has a safe foreground-owned pixel source. | Ran `scripts/analyze-fg2-preprocess-plans.py` with `--selective-budget-from-zero-tail` on `generated/ps1/foreground/VISITOR3.FG2` and `generated/ps1/foreground/VIST3LOW.FG2`, writing JSON and frame CSV artifacts under `scratch/visitor3-preprocess-safety-v175/`. | Failed/no promotion. High tide's default selective model selects `117` frames, needs `3394200` bytes, and models `10602536` upload bytes saved; the budgeted subset selects `75` frames, uses `888880 / 891012` bytes, and models `6290232` saved bytes. Low tide's default model is the same `117` frames/`3394200` bytes/`10602536` saved, and its budgeted subset selects `74` frames, uses `853848 / 854114` bytes, and models `6166528` saved bytes. Both tides still report `0` selected draw-covered x-band bytes, `0` draw-covered rects, and `0` all-draw-covered selected frames. | Do not implement raw foreground-only same-footprint upload-ready append for VISITOR3. The apparent upload win depends on background/cleanup pixels that are dynamic at runtime; VISITOR3 still needs safe precomposed/background-owned payloads, compression plus ownership metadata, or generated scheduler ownership. The current public baseline remains `+0.4353%` over target / `99.5831%` target speed. |
| 2026-05-08 | `building2-high-group210-226-v176` | Log-only; source restored. | Rotate from VISITOR3/WALKSTUF1 blocked lanes to the next under-99 row with a current-window read candidate. The read matrix ranked BUILDING2 high `210..226` as append-start fireable, current-window-compatible, and medium validate-phase risk, but still scheduler-owned-only. The binary question was whether a local retained read group could activate a useful append despite the warning. | Temporarily appended `{210,226,0}` after the accepted BUILDING2 high `{60,72,0}` read group, then ran BUILDING2 high against the v174 broad baseline with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded symbol drift. Source was restored afterward. Artifact: `scratch/ps1-perf-iterate/building2-high-group210-226-v176-focused/20260508-192134-1765931/summary.json`. | Failed/no promotion. The run was exact-flat on every key metric: `scene_vb=1599`, `loop_vb=1349`, `target_vb=1316`, `overrun_vb=33`, `blocking_vb=48`, `prefetch_overrun_vb=12`, `loop_reads=61`, `loop_read_vb=259`, and `due_misses=7`. `group_hits=0`, so the added local table did not fire useful movement; pack LBA `6180` and the `215040` byte PS-EXE bucket stayed fixed. | Do not retry BUILDING2 high `210..226` as a hand-authored read table. The range remains evidence for generated scheduler/append ownership, but local source tables cannot move this cluster on the current baseline. Public baseline remains `+0.4353%` over target / `99.5831%` target speed. |
| 2026-05-08 | `johnny1-currentfit-groups-v177` | Log-only; source restored. | Test the top JOHNNY1 residual read-table lane after the compact-FGP3/v4 promotion left both tides just under 99%. The refreshed read matrix ranked balanced current-window-compatible groups `131..147`, `145..161`, and `138..154` as scheduler-or-guarded probes, so the binary question was whether any hand-authored retained-window group could turn the planner's one-read saving into runtime movement. | Temporarily wired JOHNNY1 to a single retained read group and tested each candidate separately against the current v174 broad baseline with zero regression allowance, `--require-improvement`, `95%` work identity, and bounded symbol drift. Artifacts: `scratch/ps1-perf-iterate/johnny1-group131-147-v177-focused/20260508-192846-1805911/summary.json`; `scratch/ps1-perf-iterate/johnny1-group145-161-v177-focused/20260508-193135-1822380/summary.json`; `scratch/ps1-perf-iterate/johnny1-group138-154-v177-focused/20260508-193446-1840872/summary.json`. | Failed/no promotion. All three probes were exact-flat on both tides: `scene_vb=2070`, `loop_vb=1974`, `target_vb=1945`, `overrun_vb=29`, `blocking_vb=26`, `prefetch_overrun_vb=26`, `loop_reads=7`, `loop_read_vb=56`, and `due_misses=0`. Each run reported `group_hits=0`, pack LBAs `13982/14201`, sectors `219`, and the `215040` byte PS-EXE bucket fixed. | Do not retry JOHNNY1 `131..147`, `145..161`, or `138..154` as hand-authored read tables. The planner rows remain useful scheduler evidence, but the existing append path never fires them on the current compact baseline; future JOHNNY1 work needs generated append ownership, a larger retained-window design, or a different pack/data-shape lane. Public baseline remains `+0.4353%` over target / `99.5831%` target speed. |
| 2026-05-08 | `visitor3-hot-atlas-v178-v179` | Log-only; source and packs restored. | Make a bigger VISITOR3-specific swing after scalar groups/setup caps were exhausted: duplicate selected late payloads into each pack's zero tail, point the late entries at that compact atlas, and preload the atlas as a persistent VISITOR3 setup-owned segment. The goal was to preserve the existing early setup window while making the terminal tight-cluster reads memory-resident without changing pack file sizes, pack LBAs, or the `215040` byte PS-EXE bucket. | First duplicated frames `139..144` into a 25-sector atlas (`50101` payload bytes) at high sector `325` / low sector `343`, added a VISITOR3-only persistent setup segment, and ran high/low against the v174 broad baseline. Then restored the packs and narrowed the atlas to frames `142..144`, a 7-sector / `12673` byte terminal cluster, before rerunning the same strict focused gate. Source and both packs were restored afterward. Artifacts: 25-sector atlas `scratch/ps1-perf-iterate/visitor3-hot-atlas-v178-focused/20260508-195314-1945924/summary.json`; 7-sector atlas `scratch/ps1-perf-iterate/visitor3-hot-atlas142-v179-focused/20260508-195555-1962147/summary.json`; pack rewrite summaries under `scratch/visitor3-hot-atlas-v178/`. | Failed/no promotion. The 25-sector atlas proved read removal but failed strict timing: high regressed `scene_vb 1422 -> 1458`, `loop_vb 1118 -> 1141`, `overrun_vb 90 -> 110`, and `blocking_vb 150 -> 153` while loop reads fell `27 -> 22`; low improved active loop/blocking (`1126 -> 1122`, `170 -> 154`, reads `31 -> 26`) but failed full-scene and hidden-refill gates (`scene_vb 1426 -> 1439`, `prefetch_overrun_vb 0 -> 2`). The 7-sector terminal atlas was still negative: high regressed to `1442`, `1132/1028`, `overrun_vb=104`, `blocking_vb=157`; low regressed to `1438`, `1129/1024`, `overrun_vb=105`, and `prefetch_overrun_vb=2`. | Close setup-owned VISITOR3 tail atlases under the current runtime. The useful signal is that targeted late payload residency can remove reads, but setup-time CD phase and persistent segment lifetime convert the gain into scene-time/hidden-refill debt. Next VISITOR3 work should either make the data intrinsically cheaper to present (safe precomposed/background-owned upload payloads, compression/ownership metadata) or give the scheduler first-class deadline ownership; do not retry another manual setup segment or tail atlas size. Public baseline remains `+0.4353%` over target / `99.5831%` target speed. |
| 2026-05-08 | `visitor3-pack-taildup-v180` | Log-only; packs restored. | Isolate whether the VISITOR3 hot-atlas failure came from the setup-segment/runtime path or from moving the terminal payloads to a later CD phase. The probe duplicated frames `139..144` into each pack's zero-tail space and repointed entries to that copy with no source or runtime changes. | Rewrote only the two VISITOR3 packs, preserving file sizes, LBAs, and the `215040` byte PS-EXE bucket. High duplicated `50101` payload bytes into a 25-sector tail cluster at sector `325`; low duplicated the same terminal cluster at sector `343`. Both packs were restored afterward from `scratch/visitor3-pack-taildup-v180/`. Artifact: `scratch/ps1-perf-iterate/visitor3-pack-taildup-v180-focused/20260508-200318-2004617/summary.json`. | Failed/no promotion. High regressed from the current `1118/1028` baseline to `1122/1027`, with `scene_vb 1422 -> 1426`, `overrun_vb 90 -> 95`, `blocking_vb 150 -> 154`, `prefetch_overrun_vb 0 -> 3`, `loop_reads=27`, and `due_misses=26`. Low regressed from `1126/1025` to `1130/1024`, with `scene_vb 1426 -> 1430`, `overrun_vb 101 -> 106`, `blocking_vb 170 -> 174`, `prefetch_overrun_vb 0 -> 3`, `loop_reads=31`, and `due_misses=29`. | Close layout-only VISITOR3 terminal tail duplication too. The no-code version did not remove loop reads and still added hidden refill debt, so moving the hot terminal payloads later is itself phase-negative. Do not retry VISITOR3 terminal tail repoints without a scheduler/precomposed-data change. Public baseline remains `+0.4353%` over target / `99.5831%` target speed. |
| 2026-05-08 | `visitor3-motion-x-v181` | `ps1: add visitor3 motion-copy foreground payloads` | Make the next VISITOR3 big swing after tail atlases and layout-only duplication proved the terminal CD phase was negative. The hypothesis was that frames `119..123` are mostly horizontal translations of already-composited state, so the pack can encode motion-copy rows plus small exposed-background cleanup and residual draw data instead of re-streaming full temporal residual payloads. | Added a compact FGP3 motion marker/runtime decoder, rewrote frames `119..123` in both `VISITOR3.FG2` and `VIST3LOW.FG2`, and preserved both `1555450` byte footprints. High active payload drops `661435 -> 586705`; low drops `698333 -> 623603`; each tide saves `74730` active bytes. Host visual-state verification passed all `145` frames for both tides. The first source shape crossed the PS-EXE bucket to `217088`, so motion helpers were shrunk with `optimize("Os"), noinline`, returning the build to the accepted `215040` byte bucket. Artifacts: transform root `scratch/visitor3-motion-x-v181/`; focused gate `scratch/ps1-perf-iterate/visitor3-motion-x-v181-focused/20260508-202531-2127593/summary.json`; broad no-regression gate `scratch/ps1-perf-iterate/visitor3-motion-x-v181-broad-norequire/20260508-203050-2158325/summary.json`; unrelated WALKSTUF1 high control `scratch/worktrees/v181-control/scratch/ps1-perf-iterate/v181-head-control-walkstuf1-high/20260508-204229-2226142/summary.json`. | Promoted. VISITOR3 high improves `scene_vb 1422 -> 1409`, `loop_vb 1118 -> 1105`, `target_vb 1028 -> 1031`, `overrun_vb 90 -> 74`, `blocking_vb 150 -> 129`, `loop_reads 27 -> 23`, `loop_read_vb 153 -> 132`, and `due_misses 26 -> 22`; low improves `scene_vb 1426 -> 1408`, `loop_vb 1126 -> 1108`, `target_vb 1025 -> 1028`, `overrun_vb 101 -> 80`, `blocking_vb 170 -> 143`, `loop_reads 31 -> 27`, `loop_read_vb 179 -> 152`, and `due_misses 29 -> 25`. Both keep `prefetch_overrun_vb=0`, pack LBAs stay `22472/23232`, sectors stay `760`, and the PS-EXE bucket stays `215040`. Matching broad controls stayed on baseline; the WALKSTUF1 high drift seen by one broad label mismatch reproduced on pre-change HEAD, so it is not caused by v181. | Keep as the new VISITOR3 baseline. Public rollup moves from `+0.4353%` to `+0.4064%` over target and from `99.5831%` to `99.6077%` target speed across `126` timing-bearing rows; raw signed rollup moves to `-0.3621%` / `100.3916%`. Since the compact full-matrix baseline was about `17.4%` over target / `87.1%` target speed, the headless methodology has removed about `16.99` public over-target points and added about `12.51` public target-speed points. VISITOR3 remains the white-whale row, but the next attempts should build on motion-copy expansion, residual dictionaries, generated motion opcodes, or precomposed strip ownership rather than tail-phase repoints. |
| 2026-05-08 | `visitor3-high-f115-motion-x-v182` | `ps1: add visitor3 high frame115 motion payload` | Extend the v181 motion-copy win without broadening the motion window blindly. The binary question was whether high-tide frame `115` could use the same state-hull motion-copy shape while preserving low-tide held slack and keeping hidden refill at zero. | Rewrote high-tide frame `115` only as motion-copy rows plus exposed cleanup/residual draw data, restored `VIST3LOW.FG2` to the v181 payload, and preserved both `1555450` byte footprints, LBAs `22472/23232`, and the `215040` byte PS-EXE bucket. The accepted frame saves `4334` high active payload bytes (`586705 -> 582371`); frame `115` uses `211` copy rows, `8012` copy pixels, `302` cleanup rows, `7102` cleanup pixels, and `3093` residual pixels. Artifacts: transform root `scratch/visitor3-motion-x-v182/`; broad gate `scratch/ps1-perf-iterate/visitor3-motion-x-v182-high-f115-broad/20260508-213318-2508409/summary.json`; preprocess detail `scratch/visitor3-motion-x-v182/visitor3-high-preprocess-v182.json`. | Promoted. VISITOR3 high improves `scene_vb 1409 -> 1408`, `loop_vb 1105 -> 1104`, `target_vb 1031 -> 1030`, `blocking_vb 129 -> 128`, and `loop_read_vb 132 -> 130`; overrun stays `74`, loop reads stay `23`, due misses stay `22`, and hidden refill stays `prefetch_overrun_vb=0`. Low remains exact v181 at `scene_vb=1408`, `loop_vb=1108`, `target_vb=1028`, `overrun_vb=80`, `blocking_vb=143`, `loop_reads=27`, `loop_read_vb=152`, and `due_misses=25`. The broad canary set passed: VISITOR3 high/low, WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high. Rejected probes: both-tide `115..118` improved high but regressed low and added prefetch overrun; high-only `116..118` added hidden overrun; high-only `118` regressed loop timing. | Keep as the current high VISITOR3 row. Rounded public rollup remains `+0.4064%` over target / `99.6077%` target speed because this is a key-row micro-win; raw signed rollup is now `-0.3620%` / `100.3915%`. Since the compact full-matrix baseline, the headless methodology has still removed about `16.99` public over-target points and added about `12.51` public target-speed points. Continue VISITOR3 with generated motion eligibility, residual dictionaries, custom compression, or precomposed ownership rather than wider hand-selected motion windows. |
| 2026-05-08 | `visitor3-low-motion-and-fastspan-v183` | Log-only; packs/source restored. | Probe two deeper VISITOR3 follow-ups after v182: first, whether low-tide precursor frames `114..118` could safely use motion-copy payloads, and second, whether the runtime motion-copy span path was losing enough CPU to justify a C fast path. | Low-tide motion probes rewrote frame ranges and then split to single-frame tests using `scratch/visitor3-motion-x-v182/try_motion.py` with `target-hull` copies. The split probes covered frames `114`, `115`, `116`, `117`, and `118`, plus a sparse-in-place `117` rewrite that preserved later payload offsets and the sound-table offset. The runtime probe temporarily added a same-tile-row fast path to `grMoveBgSpanX()` so motion spans did one row lookup and a directional copy instead of two `grMutableBgPixelAt()` lookups per pixel. All pack and source changes were restored after strict gates failed. Artifacts: low range `scratch/ps1-perf-iterate/visitor3-low-114-118-motion-v183-focused/20260508-222406-2789963/summary.json`; single-frame probes under `scratch/ps1-perf-iterate/visitor3-low-f11*-motion-v183-focused/`; sparse `117` `scratch/ps1-perf-iterate/visitor3-low-f117-sparse-motion-v183-focused/20260508-223352-2846196/summary.json`; fastspan `scratch/ps1-perf-iterate/visitor3-motion-fastspan-v183-focused/20260508-223747-2868177/summary.json`. | Failed/no promotion. The low-tide `114..118` rewrite saved `59543` active payload bytes on paper but regressed runtime to `loop_vb 1108 -> 1129`, `target_vb 1028 -> 1027`, `overrun_vb 80 -> 102`, and `prefetch_overrun_vb 0 -> 3`, despite reducing blocking `143 -> 129`. Every split frame failed: `118` and `114` each moved `1108 -> 1114`; `115` moved `1108 -> 1115`; `116` moved `1108 -> 1116` and added hidden refill; `117` was closest at `1108 -> 1110` with blocking `143 -> 141`. Sparse-in-place `117` still regressed to `1111`, proving later-offset compaction was not the root cause. The runtime fastspan source shape crossed the PS-EXE bucket `215040 -> 217088`, shifted pack LBAs, and produced no key improvement; the low-tide metrics stayed `1108/1028`, `blocking_vb=143`, while the high-labeled case selected the low pack under the shifted layout and failed identity. | Close low-tide precursor motion-copy expansion for frames `114..118` under the current opcode and close C-side row-copy fastspan unless it can be made code-size-neutral inside the existing `215040` byte PS-EXE bucket. The low motion family moves bytes out of CD but adds CPU/phase cost; the sparse test proves offset churn is not the issue. Next VISITOR3 attempts should be bigger data-shape changes: custom motion opcodes, precomposed/background-owned strips, per-frame dictionaries, or scheduler-owned deadline metadata rather than wider precursor windows or hot C growth. Public baseline remains `+0.4064%` over target / `99.6077%` target speed. |

| 2026-05-08 | `visitor3-zero-origin-readgroups-v184` | Log-only; packs/source restored. | After v183 proved wider low precursor motion and C-side fastspan growth were negative, probe pack-only and scheduler-adjacent VISITOR3 ideas that might produce a same-layout win without changing the accepted v182 baseline. The binary questions were whether terminal frames had zero-run trimming slack, whether compact entry origins could reduce u16 metadata, whether low precursor state-hull motion could be less CPU-negative than exact motion, and whether the refreshed terminal read clusters could fire profitably as retained `16`-sector groups. | Ran terminal zero-edge/interior trimming over high/low frames `139..144`, ran compact-origin rebasing over frames `100..144`, dry-ran low hull-mode motion on frames `114..118`, runtime-tested low hull frames `117`, `114`, and `115`, then temporarily wired VISITOR3 high/low terminal `16`-sector read groups in `src/foreground_pilot.c`. All source and pack changes were restored after strict gates failed. Artifacts: origin-rebase dry scan `scratch/visitor3-origin-rebase-v184/dry-run-100-144.json`; low hull gates `scratch/ps1-perf-iterate/visitor3-low-f117-hull-v184-focused/20260508-224559-2914339/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-f114-hull-v184-focused/20260508-224735-2923788/summary.json`, and `scratch/ps1-perf-iterate/visitor3-low-f115-hull-v184-focused/20260508-224901-2932191/summary.json`; two-group read gate `scratch/ps1-perf-iterate/visitor3-readgroups16-v184-focused/20260508-230138-3002179/summary.json`; single-terminal read gate `scratch/ps1-perf-iterate/visitor3-terminal-readgroup16-v184-focused/20260508-230407-3017071/summary.json`. | Failed/no promotion. Terminal zero trimming found `0` bytes. Compact-origin rebasing found only a `12`-byte high-tide frame `113` host saving, `0` low-tide saving, and no terminal-frame saving. Low hull-mode motion had a strong dry byte signal (`64781` bytes across frames `114..118`), but runtime stayed CPU/phase-negative: frame `117` was closest at `loop_vb 1108 -> 1110`, `overrun_vb 80 -> 82`, with blocking `143 -> 141`; frames `114` and `115` both regressed to `1114`. Hand-authored terminal read groups moved some read counters but failed cadence: high single `{277,293}` regressed `scene_vb 1408 -> 1412`, `loop_vb 1104 -> 1108`, and `overrun_vb 74 -> 78` while improving blocking `128 -> 125`, loop reads `23 -> 21`, and due misses `22 -> 21`; low single `{297,313}` regressed `loop_vb 1108 -> 1112`, `overrun_vb 80 -> 84`, and blocking `143 -> 144`. | Close these scalar v184 lanes. VISITOR3 terminal bytes are not sitting in simple zero/origin slack, low hull motion still trades CD bytes for visible CPU/phase debt, and hand terminal read groups prove scheduler ownership is the issue rather than raw range selection. Current baseline remains v182 at `+0.4064%` public average over target / `99.6077%` public target speed; the headless methodology total remains about `16.99` public over-target points removed and `12.51` public target-speed points added since the compact full-matrix baseline. |
| 2026-05-08 | `visitor3-motion-rowcopy-v185` | Log-only; source restored. | Test whether the current VISITOR3 motion-copy payloads are still CPU-bound enough that a code-size-neutral row pointer copy can improve timing without changing pack data. Host inspection proved every accepted motion span is unclipped and remains inside the left background tile, so a narrowed runtime path could legally avoid two `grMutableBgPixelAt()` calls per copied pixel. | First tested a generic same-row pointer-copy variant in `grMoveBgSpanX()`, then narrowed to a VISITOR3-current left-tile-only form after the generic build crossed the executable bucket. Source was restored after the focused gates failed. Artifacts: generic row-copy gate `scratch/ps1-perf-iterate/visitor3-motion-rowcopy-v185-focused/20260508-231354-3072005/summary.json`; narrowed left-tile gate `scratch/ps1-perf-iterate/visitor3-motion-leftrowcopy-v185-focused/20260508-231631-3088118/summary.json`. | Failed/no promotion. The generic row-copy variant was exact-flat but crossed the PS-EXE bucket `215040 -> 217088` and shifted VISITOR3 pack LBAs by one sector. The narrowed left-tile variant preserved layout (`215040`, LBAs `22472/23232`) but still had no key metric movement: high stayed `scene_vb=1408`, `loop_vb=1104`, `target_vb=1030`, `overrun_vb=74`, `blocking_vb=128`, `loop_reads=23`, and `due_misses=22`; low stayed `1408`, `1108/1028`, `overrun_vb=80`, `blocking_vb=143`, `loop_reads=27`, and `due_misses=25`. | Close simple motion-row CPU shaving. The accepted motion payloads are useful as a data-shape/CD reduction, but the remaining VISITOR3 gap is not moved by this C loop micro-optimization. Current baseline remains v182 at `+0.4064%` public average over target / `99.6077%` public target speed; next VISITOR3 work should target a custom payload format, precomposed/background-owned pixels, or generated frame-deadline scheduling. |
| 2026-05-08 | `visitor3-motion-copyv2-v186` | Log-only; source/packs restored. | Make a bigger VISITOR3-specific data-format swing from the accepted motion payloads. Host inspection showed every promoted motion-copy section is contiguous one-span rows, so a v2 motion opcode can omit per-row `relY` and `spanCount` metadata while preserving the same visual copy/cleanup/residual semantics. The binary question was whether reducing active payload bytes without changing file footprints or LBAs improves visible CD pressure enough to offset decoder/accounting changes. | Added a temporary opcode `2` decoder for contiguous one-span copy rows, rewrote accepted motion payloads in both VISITOR3 packs, and tested both compact repack and sparse-in-place forms. The transform saved `3906` high active bytes and `3485` low active bytes while preserving both `1555450` byte footprints; sparse-in-place also preserved later payload and sound-table offsets. Source and packs were restored after strict gates failed. Artifacts: dry/apply summaries under `scratch/visitor3-motion-copyv2-v186/`; compact gate `scratch/ps1-perf-iterate/visitor3-motion-copyv2-v186-focused/20260508-234518-3267229/summary.json`; sparse gate `scratch/ps1-perf-iterate/visitor3-motion-copyv2-sparse-v186-focused/20260508-234727-3280505/summary.json`. | Failed/no promotion. Compact repack kept VISITOR3 LBAs and the `215040` byte PS-EXE bucket fixed, but high regressed `loop_vb 1104 -> 1113`, `target_vb 1030 -> 1029`, `overrun_vb 74 -> 84`, `blocking_vb 128 -> 130`, and hidden refill `0 -> 3`; low regressed `1108 -> 1112`, `overrun_vb 80 -> 84`, and `blocking_vb 143 -> 146`. Sparse-in-place kept high exact-flat at `1104/1030`, but low regressed harder to `1119/1027`, `overrun_vb=92`, `blocking_vb=152`, and hidden refill `2`. | Close compact motion-copy metadata under the current scheduler. Smaller declared motion entries are not automatically better; they change due/refill accounting and cadence in a way that hurts low tide. Current baseline remains v182 at `+0.4064%` public average over target / `99.6077%` public target speed. The next VISITOR3 data-shape attempt should target safe precomposed/background-owned pixels or deadline scheduling, not another metadata-only shrink. |
| 2026-05-08 | `visitor3-narrow-upload-v187` | Log-only; source restored. | Test a larger VISITOR3 upload-path swing without touching pack data: use existing dirty-row X bounds to copy narrow dirty bands into a small aligned scratch buffer and `LoadImage` those compact bands instead of full-width tile rows when the modeled byte savings are large. The preprocess model still shows a strong VISITOR3 signal: budgeted high-tide x-bands model `75` frames at `967720` upload bytes versus `7575040` full-width bytes, and low-tide x-bands model `76` frames at `922704` bytes versus `7391360`. | Temporarily changed `grDrawBackground()` to compute per-band min/max X, align to 4 pixels, chunk rows through `gScrStreamRows`, and sync each scratch upload. No packs changed. Source was restored after the focused high/low gate failed. Artifacts: fresh x-band model CSV/JSON under `scratch/visitor3-precompose-v187-*`; focused gate `scratch/ps1-perf-iterate/visitor3-narrow-upload-v187-focused/20260508-235852-3344150/summary.json`. | Failed/no promotion. Both VISITOR3 high and low ran to the timeout without emitting `JCPERF2`, so no correctness or timing counters were promotable. The source also grew `grDrawBackground` by `816` bytes, shifted downstream hot symbols by `+816`, and crossed the PS-EXE bucket `215040 -> 217088`, making it structurally ineligible even before timing. | Close generic runtime narrow-upload heuristics. The x-band byte model is still valuable, but the hot-path scratch-copy implementation is too large and structurally unsafe. Retry this family only as pack-authored precomposed/background-owned upload payloads, a code-size-neutral deferred upload opcode, or generated ownership metadata that avoids expanding `grDrawBackground`. Current baseline remains v182 at `+0.4064%` public average over target / `99.6077%` public target speed. |
| 2026-05-09 | `visitor3-f124-sparse-motion-v188` | `ps1: add visitor3 frame124 sparse motion payload` | Make a bigger VISITOR3 data-shape swing after v187 proved generic runtime upload narrowing was too large. The candidate scan showed frame `124` in both tides could be represented as a target-hull motion payload of `3293` bytes instead of the current `17885` byte payload, saving `14592` active bytes per pack. The binary question was whether this late-yacht payload should compact/repack bytes or preserve every later offset and sound-table address as a sparse-in-place rewrite. | Rewrote frame `124` in both `VISITOR3.FG2` and `VIST3LOW.FG2` as state-hull motion-copy rows plus cleanup/residual draw data. The compact repack failed strict timing even with fixed LBAs: high regressed `1104 -> 1107` with hidden refill `0 -> 2`, and low regressed `1108 -> 1112` with hidden refill `0 -> 3`. Restored the packs and reapplied the same payload sparse-in-place, preserving later offsets, sound offsets, fixed LBAs `22472/23232`, and the `215040` byte PS-EXE bucket. Artifacts: scan `scratch/visitor3-motion-scan-v188/ranked-100-144.json`; transform summaries `scratch/visitor3-motion-f124-v188/apply-compact.json` and `scratch/visitor3-motion-f124-v188/apply-sparse.json`; focused sparse gate `scratch/ps1-perf-iterate/visitor3-f124-motion-sparse-v188-focused/20260509-001626-3442894/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-f124-motion-sparse-v188-broad/20260509-001852-3457696/summary.json`. | Promoted as work reduction, not a VBlank speed win. Sparse-in-place keeps VISITOR3 high flat at `scene_vb=1408`, `loop_vb=1104`, `target_vb=1030`, `overrun_vb=74`, `loop_reads=23`, `due_misses=22`, and `prefetch_overrun_vb=0`, while cutting active payload `582371 -> 567779`, `blocking_vb 128 -> 126`, and `loop_read_vb 130 -> 128`. Low stays flat at `1408`, `1108/1028`, `overrun_vb=80`, `loop_reads=27`, `due_misses=25`, and hidden refill `0`, while cutting active payload `623603 -> 609011`, `blocking_vb 143 -> 142`, and `loop_read_vb 152 -> 151`. The 9-case broad canary passed with WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high exact-flat against v182. | Keep as the current VISITOR3 row. Public rollup remains `+0.4064%` over target / `99.6077%` target speed and raw signed remains `-0.3620%` / `100.3915%`, because visible loop timing did not move. Since the compact full-matrix baseline, the headless methodology still totals about `16.99` public over-target points removed and `12.51` public target-speed points added, with additional same-speed CD pressure removed from the white-whale row. Continue VISITOR3 with offset-preserving data-shape changes first; compacting late yacht entries is proven phase-sensitive. |
| 2026-05-09 | `visitor3-f118-hull-sparse-v189` | `ps1: add visitor3 frame118 sparse hull payload` | Continue the offset-preserving VISITOR3 data-shape lane after v188 proved sparse-in-place rewrites can lower CD pressure without the compact-repack phase debt. The fresh candidate scan ranked frame `118` as the largest remaining single-frame hull-motion byte signal in both tides: `17568 -> 3667` bytes, saving `13901` active bytes per pack. The binary question was whether frame `118` hull motion could promote when applied sparse-in-place on top of the accepted frame `124` rewrite, despite earlier low precursor target-hull frames `114..118` failing when tested as compact/target-hull rewrites. | Rewrote frame `118` in both `VISITOR3.FG2` and `VIST3LOW.FG2` as sparse-in-place hull motion-copy rows plus cleanup/residual draw data, preserving later offsets, sound offsets, fixed LBAs `22472/23232`, both `1555450` byte footprints, and the `215040` byte PS-EXE bucket. The transform uses `297` copy rows, `27068` copy pixels, `4098` cleanup pixels, and `519` residual pixels per tide. Artifacts: scan `scratch/visitor3-motion-scan-v189/ranked-all-after-v188.json`; transform summary `scratch/visitor3-motion-f118-v189/apply-sparse-hull-both.json`; focused gate `scratch/ps1-perf-iterate/visitor3-f118-hull-sparse-v189-focused/20260509-003927-3579553/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-f118-hull-sparse-v189-broad/20260509-004218-3596385/summary.json`. | Promoted as a CD-pressure and small high-timing win. VISITOR3 high keeps `scene_vb=1408`, `loop_vb=1104`, `loop_reads=23`, due misses `22`, and hidden refill `0`, while target improves `1030 -> 1031`, overrun drops `74 -> 73`, active payload drops `567779 -> 553878`, `blocking_vb` drops `126 -> 120`, and `loop_read_vb` drops `128 -> 122`. Low stays flat at `scene_vb=1408`, `loop_vb=1108`, `target_vb=1028`, `overrun_vb=80`, `loop_reads=27`, due misses `25`, and hidden refill `0`, while active payload drops `609011 -> 595110`, `blocking_vb` drops `142 -> 139`, and `loop_read_vb` drops `151 -> 148`. The 9-case broad canary passed with WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high exact-flat against v188. | Keep as the current VISITOR3 row. Public rollup moves from `+0.4064%` to `+0.4056%` over target and from `99.6077%` to `99.6084%` target speed across `126` timing-bearing rows; raw signed moves to `-0.3629%` / `100.3922%`. Since the compact full-matrix baseline, the headless methodology has removed about `16.99` public over-target points and added about `12.51` public target-speed points, with further same-speed CD pressure removed from the white-whale row. Continue VISITOR3 with offset-preserving hull/motion payloads, custom compression, and scheduler-owned data rather than compacting late yacht entries. |
| 2026-05-09 | `visitor3-f117-hull-sparse-v190` | Log-only; packs restored. | Retest the largest remaining post-v189 single-frame sparse hull signal before leaving the offset-preserving motion lane. Frame `117` modeled as a hull motion-copy payload saves `12451` high active bytes (`16882 -> 4431`) and `13139` low active bytes (`17570 -> 4431`) while preserving later payload offsets, sound offsets, fixed LBAs, both `1555450` byte footprints, and the `215040` byte PS-EXE bucket. | Applied frame `117` sparse-in-place hull motion to both tides, then split the binary by restoring low and testing high-only against the v189 broad baseline. Artifacts: transform summary `scratch/visitor3-motion-f117-v190/apply-sparse-hull-both.json`; low/default focused gate `scratch/ps1-perf-iterate/visitor3-f117-hull-sparse-v190-focused/20260509-010408-3721483/summary.json`; high-only split gate `scratch/ps1-perf-iterate/visitor3-high-f117-hull-sparse-v190-focused/20260509-010544-3731174/summary.json`. | Failed/no promotion. The default low-tide run regressed badly to `scene_vb=1447`, `loop_vb=1129`, `target_vb=1026`, `overrun_vb=103`, with `blocking_vb=133`, `loop_reads=26`, due misses `25`, and `upload_vb=19` despite the active payload drop. The high-only split also failed: high moved `1104/1031 -> 1109/1029`, overrun `73 -> 80`, hidden refill `0 -> 2`, and blocking only `120 -> 119`; the restored low control in that dirty split showed the same phase-sensitive cadence debt, moving to `1121/1026`, overrun `95`, while blocking fell to `131`. | Close frame `117` sparse-hull under the current motion opcode. It removes CD bytes but expands dirty/upload cadence and visible phase debt; unlike frame `118`, the no-cleanup hull shape is not a safe work-reduction. The next VISITOR3 attempt should move away from another precursor hull frame and toward custom compression, upload ownership/precomposed strips, residual dictionaries, or scheduler-owned deadline metadata. Public baseline remains v189 at `+0.4056%` over target / `99.6084%` target speed. |
| 2026-05-09 | `visitor3-low-f117-public-sparse-v191` | Log-only; low pack restored. | Recheck the frame `117` sparse-hull idea with the actual public matrix low-tide VISITOR3 boot after discovering the earlier low-only control used a different/default scene state. The question was whether low tide alone could keep the v189 matrix cadence when high tide was untouched and the low pack preserved all later offsets. | Applied frame `117` sparse-in-place hull motion only to `VIST3LOW.FG2` from the v189 baseline, using the same `17570 -> 4431` dry byte signal, then ran `visitor3-low-canary` with `lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -154 54`. The first high/low focused gate was killed before `JCPERF2` on the unchanged high canary, so the low case was rerun in isolation against `scratch/ps1-perf-iterate/visitor3-f118-hull-sparse-v189-broad/20260509-004218-3596385/summary.json`. Artifacts: transform summary `scratch/visitor3-low-f117-public-v191/apply-low-sparse-hull.json`; interrupted high-first run `scratch/ps1-perf-iterate/visitor3-low-f117-public-sparse-v191-focused/20260509-011505-3784494/summary.json`; accepted-metrics low-only gate `scratch/ps1-perf-iterate/visitor3-low-f117-public-sparse-v191-lowonly/20260509-011752-3801179/summary.json`. | Failed/no promotion. The correct public low boot still regressed against v189: `scene_vb 1408 -> 1411`, `loop_vb 1108 -> 1111`, `target_vb=1028`, and `overrun_vb 80 -> 83`, with `blocking_vb=139`, `prefetch_overrun_vb=0`, `loop_reads=27`, and no key metric improved. The runner exited `137` only after complete `JCPERF2` metrics, so the parsed metrics are usable. The pack was restored. | Close low-only frame `117` sparse-hull as well. Correcting the matrix boot changes the failure size but not the decision: frame `117` buys bytes without buying visible cadence. Future VISITOR3 work should stop trying adjacent precursor hull frames under opcode `1` and instead spend effort on a different pixel/data ownership model, compressed residual dictionaries, precomposed/background-owned strips, or generated deadline scheduling. |
| 2026-05-09 | `visitor3-narrow-band-upload-v192` | Log-only; source restored. | Revisit the large VISITOR3 x-band upload model after v187 with a much smaller custom implementation. Current high/low matrix preprocess still models about `6.34 MB` of avoidable full-width upload if frames such as `133`, `132`, `131`, `140`, `139`, `122`, and `120` can upload only their dirty X spans. The binary question was whether a narrow helper limited to already-measured dirty bands could fit the PS-EXE bucket and produce valid `JCPERF2` timing. | Temporarily added `grUploadDirtyBandNarrow()` in `src/graphics_ps1.c` and called it from the dirty-band upload path only when the measured dirty X span was narrower than the full `288` pixel tile width. The helper copied strided tile RAM rows through `gScrStreamRows` before `LoadImage`, so it required no pack changes and no heap allocation. Ran the focused VISITOR3 high/low public matrix gate against the v189 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-narrow-band-upload-v192-focused/20260509-012600-3846774/summary.json`. | Failed/no promotion. Both VISITOR3 high and low timed out without `JCPERF2`, and the source shape was structurally ineligible: `jcreborn.elf` grew `958756 -> 961472`, `jcreborn.exe` crossed the PS-EXE bucket `215040 -> 217088`, and `grDrawBackground` grew `1300 -> 1404` with downstream hot symbols shifted. The source was restored and the worktree returned clean. | Close runtime scratch-copy narrow uploads for VISITOR3 unless a later code-size-neutral implementation avoids extra hot-path sync/copy cost. The upload model remains the biggest byte signal, but the implementation path must change: pack-authored precomposed/background-owned strips, a direct upload payload format, scene-state-specific generated data, or scheduler/data ownership can attack the same frames without expanding `grDrawBackground`. Public baseline remains v189 at `+0.4056%` over target / `99.6084%` target speed. |
| 2026-05-09 | `visitor3-high-f117-target-hull-v193` | `ps1: add visitor3 high frame117 target hull payload` | Retry frame `117` with a different ownership shape after v190/v191 proved no-cleanup sparse hull regressed both/low timing. The hypothesis was that preserving the original target cleanup geometry while still copying old background rows could keep dirty/upload cadence stable enough for high tide, and that low should be split out if it stayed phase-sensitive. | Applied a sparse-in-place target-hull frame `117` motion payload to both `VISITOR3.FG2` and `VIST3LOW.FG2`, preserving both `1555450` byte footprints, later offsets, sound offsets, LBAs `22472/23232`, and the `215040` byte PS-EXE bucket. The transform reduced the high entry `16882 -> 5733` bytes and the low entry `17570 -> 5733` bytes. The both-tide focused gate improved high but regressed low, so the low pack was restored to v189 and the high-only candidate was rerun through focused and 9-case broad gates. Artifacts: transform summary `scratch/visitor3-f117-target-hull-v193/apply-target-hull-both.json`; both-tide focused gate `scratch/ps1-perf-iterate/visitor3-f117-target-hull-v193-focused/20260509-013804-3914471/summary.json`; high-only focused gate `scratch/ps1-perf-iterate/visitor3-high-f117-target-hull-v193-focused/20260509-014013-3927354/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-high-f117-target-hull-v193-broad/20260509-014222-3940374/summary.json`; promoted filtered summary `scratch/visitor3-f117-target-hull-v193/summary-visitor3-promoted.json`. | Promoted high-only. VISITOR3 high improves `scene_vb 1408 -> 1405`, `loop_vb 1104 -> 1101`, `target_vb 1031 -> 1030`, `overrun_vb 73 -> 71`, `blocking_vb 120 -> 116`, `loop_reads 23 -> 22`, `loop_read_vb 122 -> 118`, and due misses `22 -> 21` while active payload drops `553878 -> 542729`, hidden refill remains `0`, and layout stays fixed. Low is restored to the v189 pack and remains exact-flat at `scene_vb=1408`, `loop_vb=1108`, `target_vb=1028`, `overrun_vb=80`, `blocking_vb=139`, `loop_reads=27`, `loop_read_vb=148`, and due misses `25`. The 9-case broad gate passed with WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high on accepted profiles. | Keep as the current VISITOR3 row. Public rollup moves to `+0.4041%` over target / `99.6097%` target speed across `126` timing-bearing rows; raw signed moves to `-0.3644%` / `100.3935%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.00` public over-target points and added about `12.51` public target-speed points. Target-hull cleanup preservation is viable for high frame `117`, but low remains closed under the current motion opcode; continue VISITOR3 with precomposed/background-owned strips, residual dictionaries, or scheduler/data ownership rather than another no-cleanup precursor hull rewrite. |
| 2026-05-09 | `visitor3-low-f114-target-hull-v194` | Log-only; low pack restored. | Test the largest remaining low-side target-hull byte signal after v193. Frame `114` target-hull modeled as a sparse-in-place payload saves `11664` active low bytes (`17570 -> 5906`) while preserving cleanup geometry, later offsets, sound offsets, LBA `23232`, the `1555450` byte footprint, and the `215040` byte PS-EXE bucket. | Rewrote only `VIST3LOW.FG2` frame `114` as target-hull motion-copy, leaving high as a v193 control, then ran the focused public VISITOR3 high/low gate against the v193 baseline. The transform used `164` copy rows, `3458` copy pixels, `302` cleanup rows, `7102` cleanup pixels, and `3483` residual pixels. Artifacts: transform summary `scratch/visitor3-low-f114-target-hull-v194/apply-low-target-hull.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-f114-target-hull-v194-focused/20260509-020820-4089445/summary.json`. | Failed/no promotion. High control stayed exact-flat at the v193 profile (`1405`, `1101/1030`, `blocking_vb=116`, `loop_reads=22`, due misses `21`). Low cut visible blocking `139 -> 137`, but regressed cadence: `scene_vb 1408 -> 1411`, `loop_vb 1108 -> 1111`, `overrun_vb 80 -> 83`, with `target_vb=1028`, `prefetch_overrun_vb=0`, `loop_reads=27`, and due misses `25`. The low pack was restored. | Close low frame `114` target-hull under the current motion opcode. It confirms that low precursor target-hull can buy CD/blocking but still loses the frame budget, so future low work should not spend more runs on adjacent precursor motion rewrites unless the ownership model changes. Current baseline remains v193 at `+0.4041%` public over target / `99.6097%` public target speed. |
| 2026-05-09 | `visitor3-high-f116-target-hull-v195` | Log-only; high pack restored. | Test whether the next-high high-tide target-hull signal can stack on the accepted v193 high frame `117` payload. Frame `116` saves `7800` active high bytes (`14226 -> 6426`) with sparse-in-place layout preservation and target cleanup geometry. | Rewrote only `VISITOR3.FG2` frame `116` as target-hull motion-copy, leaving low as a v193 control, then ran the focused public VISITOR3 high/low gate against the v193 baseline. The transform used `248` copy rows, `13554` copy pixels, `302` cleanup rows, `7388` cleanup pixels, and `2992` residual pixels. Artifacts: transform summary `scratch/visitor3-high-f116-target-hull-v195/apply-high-target-hull.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-f116-target-hull-v195-focused/20260509-021206-4111368/summary.json`. | Failed/no promotion. Low control stayed exact-flat at `1408`, `1108/1028`, `blocking_vb=139`, and due misses `25`. High regressed hard despite fixed layout: `scene_vb 1405 -> 1413`, `loop_vb 1101 -> 1109`, `target_vb 1030 -> 1028`, `overrun_vb 71 -> 81`, and hidden refill `0 -> 2`, with `blocking_vb=116`, `loop_reads=22`, and due misses `21`. The high pack was restored. | Close high frame `116` target-hull under the current motion opcode. The accepted frame `117` was a narrow local win; stacking an adjacent precursor target-hull payload reintroduces phase/refill debt. Current baseline remains v193 at `+0.4041%` public over target / `99.6097%` public target speed. |
| 2026-05-09 | `visitor3-compact-motion-dispatch-v196` | Log-only; source restored. | Test whether FGP3/v4 compact residual packs should dispatch the `0xffff` VISITOR3 motion marker through the existing compact motion-copy decoder. The current promoted VISITOR3 packs contain sparse motion-marker payloads inside version-4 packs, so the binary question was whether the intended opcode path was a hidden correctness/performance fix or whether its code/layout/cadence cost outweighed the semantic cleanup. | Temporarily added the same motion-marker dispatch used by the non-compact residual decoder to `grCompositePacked4CompactTemporalResidualToBackground()`, then ran the focused public VISITOR3 high/low gate against v193. Artifact: `scratch/ps1-perf-iterate/visitor3-compact-motion-dispatch-v196-focused/20260509-023210-28211/summary.json`. | Failed/no promotion. High nominally cut visible blocking `116 -> 115`, but regressed `target_vb 1030 -> 1028`, `overrun_vb 71 -> 73`, hidden refill `0 -> 7`, and loop reads `22 -> 24`; the small source change also crossed the PS-EXE bucket `215040 -> 217088` and moved VISITOR3 pack LBAs by one sector. Low regressed materially: `scene_vb 1408 -> 1423`, `loop_vb 1108 -> 1123`, `target_vb 1028 -> 1026`, `overrun_vb 80 -> 97`, `blocking_vb 139 -> 149`, and hidden refill `0 -> 3`. | Do not promote a naive compact-v4 motion-marker dispatch. The intended decoder route is not code-size-neutral and changes the accepted cadence/visual ownership; any future motion semantic cleanup needs a new compact opcode or precomposed/direct-strip path with fixed EXE bucket, fixed LBAs, and visual signoff. Current baseline remains v193 at `+0.4041%` public over target / `99.6097%` public target speed. |
| 2026-05-09 | `visitor3-drop-unused-motion-dispatch-v197` | `ps1: drop unused noncompact motion dispatch` | Create VISITOR3 code-layout headroom after v192 proved direct/narrow upload helpers can fail solely by crossing the PS-EXE bucket. A pack scan showed only `VISITOR3.FG2` and `VIST3LOW.FG2` version-4 compact packs contain the `0xffff` motion marker, while no noncompact FGP3/v3 pack uses it. The binary question was whether removing the now-dead noncompact marker dispatch was exact-flat across VISITOR3 and broad canaries. | Removed the marker dispatch from `grCompositePacked4TemporalResidualToBackground()`. The focused VISITOR3 gate was exact-flat but failed `--require-improvement`, so the candidate was rerun through the 9-case broad gate without requiring a timing win. Artifacts: focused gate `scratch/ps1-perf-iterate/visitor3-drop-unused-motion-dispatch-v197-focused/20260509-023736-59277/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-drop-unused-motion-dispatch-v197-broad/20260509-024124-81172/summary.json`. | Promoted as code-headroom/work reduction, not a VBlank speed win. VISITOR3 high remains `scene_vb=1405`, `1101/1030`, `overrun_vb=71`, `blocking_vb=116`, `loop_reads=22`, and `prefetch_overrun_vb=0`; low remains `1408`, `1108/1028`, `overrun_vb=80`, `blocking_vb=139`, `loop_reads=27`, and `prefetch_overrun_vb=0`. WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high also stayed exact-flat. Pack LBAs and the `215040` byte PS-EXE bucket stayed fixed, while hot symbols moved earlier (`grDrawBackground` and stream-read helpers by `-1360`). | Keep as the current source baseline for the next VISITOR3 direct/precomposed-data attempt. Public rollup remains `+0.4041%` over target / `99.6097%` target speed; the headless methodology total remains about `17.00` public over-target points removed and `12.51` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-09 | `visitor3-frame133-narrow-upload-v198` | Log-only; source restored. | Make the largest remaining VISITOR3 upload-byte swing concrete without changing pack data. Frame/source `179` (`frame_index=133`) at the public `island-pos -154 54` has four measured x-bands that model current full-width upload `315520` bytes versus `87168` bytes if uploaded narrowly. The binary question was whether a VISITOR3-only public-state helper could copy those four tile-RAM bands through the existing `gScrStreamRows` buffer after VSync and skip normal `grDrawBackground()` for that frame while preserving fixed LBAs and the `215040` byte PS-EXE bucket. | Temporarily added a VISITOR3 source-frame `179`/public-offset request flag in `foreground_pilot.c` and a four-band narrow present helper in `graphics_ps1.c`. The helper reduced measured upload bytes but still copied strided tile rows into scratch before each `LoadImage`, so it was a targeted runtime-scratch retry rather than a pack-authored/precomposed payload. Ran focused public VISITOR3 high/low against the v197 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-frame133-narrow-upload-v198-focused/20260509-030940-242308/summary.json`. | Failed/no promotion. Layout stayed eligible (`VISITOR3/VIST3LOW` LBAs `22472/23232`, PS-EXE bucket `215040`), and upload bytes dropped as modeled (`13675520 -> 13447168` high, `13555200 -> 13326848` low). The saved bytes did not translate into speed: high regressed `scene_vb 1405 -> 1412`, `loop_vb 1101 -> 1108`, `target_vb 1030 -> 1029`, `overrun_vb 71 -> 79`, and hidden refill `0 -> 3`; low regressed `1408 -> 1411`, `1108 -> 1111`, `overrun_vb 80 -> 83`, and `blocking_vb 139 -> 141`. Upload phase worsened (`upload_vb 4 -> 8` high, `4 -> 7` low) and upload rect pressure rose by two rects per tide. | Close runtime scratch-copy narrow present for frame `133`. This proves the byte model is real but the implementation must be contiguous/precomposed or pack-authored; copying strided tile RAM on the PS1 burns more VBlank than it saves. Keep frame `133` on the deck only as a precomposed/background-owned direct payload, sidecar strip, or zero-copy generated format that can `LoadImage` contiguous pixels without expanding `grDrawBackground`. Baseline remains v197 at `+0.4041%` public over target / `99.6097%` public target speed. |
| 2026-05-09 | `visitor3-frame133-prep-narrow-upload-v199` | Log-only; source restored. | Move the v198 frame `133` narrow-upload copy out of the present path: copy the same four public-state x-bands (`315520 -> 87168` modeled bytes) into an aligned contiguous staging buffer during compose/prepare, then let `grDrawBackground()` submit only four contiguous `LoadImage` calls. The binary question was whether pre-staging could keep the byte win while avoiding the VSync scratch-copy penalty. | Temporarily added an `87168` byte static staging buffer, a VISITOR3 source-frame `179` prepare hook, and a `grDrawBackground()` fast path that uploaded the staged four bands. The focused public VISITOR3 high/low gate ran against the v197 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-frame133-prep-narrow-upload-v199-focused/20260509-031915-296201/summary.json`. | Failed/no promotion. Both high and low ran the full `7200` frame headless window and DuckStation exited successfully, but neither case emitted `JCPERF2`; the scene did not reach the perf completion point. The build stayed in the `215040` byte PS-EXE bucket, but `jcreborn.elf` grew `952504 -> 956888`, `grDrawBackground` grew `1300 -> 1312`, and hot foreground symbols shifted by `+112`. No timing metrics were promotable. | Close runtime pre-staged/static-buffer frame `133` upload. Moving the strided tile copy out of present is still not a safe ownership model, and the large BSS/direct fast path breaks the accepted perf completion behavior. The remaining viable frame `133` lane is pack-authored or sidecar-authored contiguous pixels with no runtime tile-RAM copy, no large static staging allocation, fixed LBAs/PS-EXE bucket, and visual signoff. Baseline remains v197 at `+0.4041%` public over target / `99.6097%` public target speed. |
| 2026-05-09 | `visitor3-frame133-heap-prep-upload-v200` | Log-only; source restored. | Split the v199 failure by replacing the static `87168` byte staging buffer with a lazy heap allocation and by promoting dirty state after the narrow upload. The binary question was whether v199 failed because of large BSS/static layout, or because any runtime-staged frame `133` strip path breaks VISITOR3 cadence/completion. | Temporarily added the same source-frame `179` prepare hook and four-band direct upload fast path, but allocated the contiguous strip buffer lazily with `malloc(87168)` and called `grPromoteCurrDirtyToPrev()` after the fast upload. The focused public VISITOR3 high/low gate ran against the v197 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-frame133-heap-prep-upload-v200-focused/20260509-033121-364935/summary.json`. | Failed/no promotion. The build stayed in the `215040` byte PS-EXE bucket, but both public VISITOR3 tides again ran the full `7200` frame headless window with DuckStation success and no `JCPERF2`. The no-metrics failure reproduces v199 after removing the static BSS variable, so no timing counters were promotable. | Close runtime-staged frame `133` upload entirely, including heap-backed variants. The remaining credible upload lane must be truly pack-authored/sidecar-authored contiguous pixels or a new payload opcode that updates the RAM mirror and upload plan coherently without runtime tile-RAM strip copies. Baseline remains v197 at `+0.4041%` public over target / `99.6097%` public target speed. |
| 2026-05-09 | `visitor3-reanchor-f125-v201` | Log-only; packs restored. | Test a more aggressive late-yacht motion-copy shape after the normal frame `125` target-hull encoder failed only because source rows extend left of the current entry X. The experiment re-anchored frame `125` from `x=161,w=162` to `x=154,w=169`, making the sparse target-hull payload encodable while preserving the same file footprint, later offsets, sound offsets, and VISITOR3/VIST3LOW LBAs. | Rewrote frame `125` in both VISITOR3 packs as sparse-in-place target-hull motion-copy data with the wider entry anchor. The offline transform reduced each frame from `16828 -> 1799` bytes, saving `15029` active bytes per tide, using `302` copy rows, `32186` copied pixels, no cleanup rows, and `225` residual pixels. Artifacts: transform summary `scratch/visitor3-reanchor-f125-v201/apply-reanchor-f125-both.json`; focused public high/low gate `scratch/ps1-perf-iterate/visitor3-reanchor-f125-v201-focused/20260509-034740-455929/summary.json`. | Failed/no promotion. High tide stayed exact-flat at the accepted v197 profile (`scene_vb=1405`, `loop_vb=1101`, `target_vb=1030`, `overrun_vb=71`, `blocking_vb=116`, `loop_reads=22`, due misses `21`). Low tide regressed despite the large byte reduction: `scene_vb 1408 -> 1411`, `loop_vb 1108 -> 1111`, `overrun_vb 80 -> 83`, and `blocking_vb 139 -> 142`, with `target_vb=1028`, `prefetch_overrun_vb=0`, `loop_reads=27`, and due misses `25`. The packs were restored. | Close frame `125` re-anchored target-hull under the current motion-copy ownership model. The re-anchor trick is valid and useful for generating new candidates, but low tide shows that late-yacht byte cuts can still increase visible cadence debt. Future retries need an upload-aware or scheduler-owned variant, not another wider-anchor copy-only payload on the public low path. |
| 2026-05-09 | `visitor3-high-reanchor-f127-v202` | `ps1: add visitor3 high frame127 reanchor payload` | Split the re-anchor lane after v201 proved both-tide late-yacht rewrites can regress low. The fresh scan ranked frame `127` as the largest legal same-layout high candidate: re-anchor `x=209,w=163` to `x=154,w=218`, encode the old background copy rows sparse-in-place, and keep the same file footprint, sound offset, LBA, and PS-EXE bucket. | Rewrote only `VISITOR3.FG2` frame `127`/source `173` as a sparse-in-place target-hull motion-copy payload, leaving `VIST3LOW.FG2` as the v197 control. The transform reduced the high entry `16936 -> 1413` bytes, saving `15523` active bytes with `302` copy rows, `46682` copied pixels, no cleanup rows, and `36` residual pixels. Artifacts: transform summary `scratch/visitor3-high-reanchor-f127-v202/apply-high-reanchor-f127.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-reanchor-f127-v202-focused/20260509-035311-487443/summary.json`; broad no-regression gate `scratch/ps1-perf-iterate/visitor3-high-reanchor-f127-v202-broad-norequire/20260509-040448-557334/summary.json`. | Promoted as high-only work reduction/CD-pressure, not a public rollup-speed win. VISITOR3 high stays exact-flat at `scene_vb=1405`, `loop_vb=1101`, `target_vb=1030`, `overrun_vb=71`, `loop_reads=22`, due misses `21`, and `prefetch_overrun_vb=0`, while active payload drops `542729 -> 527206`, `blocking_vb 116 -> 114`, and `loop_read_vb 118 -> 116`. Low remains exact-flat at `1408`, `1108/1028`, `overrun_vb=80`, `blocking_vb=139`, `loop_reads=27`, and due misses `25`. The broad no-regression gate passed with WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high exact-flat. | Keep as the current VISITOR3 high row. Public rollup remains `+0.4041%` over target / `99.6097%` target speed because visible loop/overrun did not move; the headless methodology total remains about `17.00` public over-target points removed and `12.51` public target-speed points gained since the compact full-matrix baseline. Continue VISITOR3 with high-only/split re-anchor candidates, generated pack-side ownership, or custom compression; do not reapply wide re-anchor payloads to low without a scheduler/upload ownership change. |
| 2026-05-09 | `visitor3-low-reanchor-f127-v203` | Log-only; low pack restored. | Test whether the v202 high-only frame `127` re-anchor byte signal can safely apply to the public low-tide pack when isolated from high. The transform re-anchors low frame `127` from `x=209,w=163` to `x=154,w=218`, preserving the file footprint, later offsets, sound offset, LBA, and PS-EXE bucket. | Rewrote only `VIST3LOW.FG2` frame `127` as sparse-in-place target-hull motion-copy data, leaving high as the accepted v202 control. The offline transform reduced the low entry `16936 -> 1413` bytes, saving `15523` active bytes with `302` copy rows, `46682` copied pixels, no cleanup rows, and `36` residual pixels. Artifacts: transform summary `scratch/visitor3-low-reanchor-f127-v203/apply-low-reanchor-f127.json`; focused low gate `scratch/ps1-perf-iterate/visitor3-low-reanchor-f127-v203-low-focused/20260509-042806-691441/summary.json`. | Failed/no promotion. Low regressed despite the large active byte reduction: `scene_vb 1408 -> 1411`, `loop_vb 1108 -> 1111`, `overrun_vb 80 -> 83`, and `blocking_vb 139 -> 141`, with `target_vb=1028`, `prefetch_overrun_vb=0`, `loop_reads=27`, and due misses `25`. The focused gate also reported no key metric improved versus the v202 baseline. The low pack was restored. | Close low frame `127` re-anchored target-hull under current motion-copy ownership. Together with v201, this says low-tide late-yacht byte cuts are cadence-sensitive even when payload savings are large; low needs scheduler/upload ownership or a different compression shape, while high-only re-anchor candidates remain eligible if gated independently. |
| 2026-05-09 | `visitor3-low-segment281-305-v204` | `ps1: add visitor3 low persistent setup segment` | Take a scheduler/data-ownership swing at the largest current VISITOR3 low read-plan cluster instead of another late-yacht payload rewrite. The v202 low read plan ranked relative sectors `281..305` as a 24-sector setup-worthy cluster with multiple loop touches and enough potential visible-CD relief to justify testing a bounded reusable setup segment. | Added a VISITOR3-low-only persistent setup segment for relative sectors `281..305` in `foreground_pilot.c`, preserved it across frame/window reads with a reusable-segment flag, and taught `ps1-foreground-read-plan.py` to parse the policy from source. The focused low gate ran against v202, then a 9-case broad gate covered VISITOR3 high/low, WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high. Artifacts: focused low gate `scratch/ps1-perf-iterate/visitor3-low-segment281-305-v204-focused/20260509-043719-742709/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-low-segment281-305-v204-broad-norequire/20260509-044056-764102/summary.json`. | Promoted with an explicit setup tradeoff. VISITOR3 low improves active-loop timing `1108/1028 -> 1102/1032`, cuts overrun `80 -> 70`, blocking `139 -> 124`, loop reads `27 -> 23`, loop-read VBlanks `148 -> 131`, and due misses `25 -> 22`, with `prefetch_overrun_vb=0` and fixed LBA `23232`, sectors `760`, and `215040` byte PS-EXE bucket. Total low scene time rises `1408 -> 1415` because the segment is read before the active loop. VISITOR3 high and the broad canaries remained exact-flat. | Keep as the current VISITOR3 low row and new baseline. Public rollup moves to `+0.3962%` over target / `99.6166%` target speed; raw signed rollup moves to `-0.3723%` / `100.4004%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.00` public over-target points and added about `12.52` public target-speed points. Future VISITOR3 work should now treat setup segments as viable only when the active-loop payoff exceeds the setup debt, and continue pack-authored/precomposed/custom compression work for the remaining high/low gap. |
| 2026-05-09 | `visitor3-high-segment277-293-v205` | `ps1: add visitor3 high persistent setup segment` | Extend the setup-segment idea to VISITOR3 high, but use the read-plan's visible-cost class to avoid the hidden-refill debt that wider setup residency can create. The hypothesis was that a narrower high terminal segment could reduce same-loop CD pressure while keeping active timing and `prefetch_overrun_vb` flat. | Added a high-tide reusable setup segment path alongside the accepted low segment, taught `ps1-foreground-read-plan.py` to parse both high and low source policy, and tested three high segments against the v204 baseline. Wider candidates `261..285` and `269..285` were rejected despite active-loop CD wins because they introduced hidden refill. The promoted focused gate tested `277..293`, then a setup-buffer capacity guard was added and rerun through focused and 9-case broad gates. Artifacts: rejected `261..285` gate `scratch/ps1-perf-iterate/visitor3-high-segment261-285-v205-focused/20260509-054731-1131945/summary.json`; rejected `269..285` gate `scratch/ps1-perf-iterate/visitor3-high-segment269-285-v205-focused/20260509-054854-1140135/summary.json`; focused rerun `scratch/ps1-perf-iterate/visitor3-high-segment277-293-v205-focused-rerun/20260509-061501-1288138/summary.json`; broad rerun `scratch/ps1-perf-iterate/visitor3-high-segment277-293-v205-broad-norequire-rerun/20260509-061624-1296310/summary.json`. | Promoted as same-loop CD-pressure reduction, not a public rollup-speed win. VISITOR3 high keeps `loop_vb=1101`, `target_vb=1030`, and `overrun_vb=71` while cutting `blocking_vb 114 -> 108`, `loop_reads 22 -> 20`, `loop_read_vb 116 -> 108`, and due misses `21 -> 20`; `prefetch_overrun_vb` stays `0`, pack LBA stays `22472`, pack sectors stay `760`, and the PS-EXE stays `215040`. The tradeoff is setup time: high `scene_vb 1405 -> 1414`. Low remains flat at the v204 profile, and WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high controls stayed flat. | Keep as the current VISITOR3 high row. Public rollup remains `+0.3962%` over target / `99.6166%` target speed; raw signed rollup remains `-0.3723%` / `100.4004%`. Since the compact full-matrix baseline, the headless methodology still records about `17.00` public over-target points removed and `12.52` public target-speed points gained, with additional VISITOR3 high CD pressure removed. Treat wider setup segments as closed unless scheduler metadata can prove hidden refill stays flat. |
| 2026-05-09 | `visitor3-high-reanchor-f126-v206` | `ps1: add visitor3 high frame126 reanchor payload` | Continue the high-only re-anchor lane after v202 showed this family can safely reduce high CD pressure while low stays isolated. The fresh scan found frame `126`/source `172` can be re-anchored like the accepted frame `127` candidate: widen the entry from `x=185,w=162` to `x=154,w=193`, encode the old background copy rows sparse-in-place, and keep the same file footprint, sound offset, LBA, and PS-EXE bucket. | Rewrote only `VISITOR3.FG2` frame `126`/source `172` as a sparse-in-place target-hull motion-copy payload, leaving `VIST3LOW.FG2` unchanged on the v204 row. The transform reduced the high entry `16828 -> 1312` bytes, saving `15516` active bytes with `302` copy rows, `39434` copied pixels, no cleanup rows, and fixed `1555450` byte pack size. Artifacts: transform summary `scratch/visitor3-high-reanchor-f126-v206/apply-high-reanchor-f126.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-reanchor-f126-v206-focused/20260509-071711-1631901/summary.json`; broad no-regression gate `scratch/ps1-perf-iterate/visitor3-high-reanchor-f126-v206-broad-norequire/20260509-071828-1639276/summary.json`. | Promoted as high-only work reduction/CD-pressure, not a public rollup-speed win. VISITOR3 high stays exact-flat on visible timing at `scene_vb=1414`, `loop_vb=1101`, `target_vb=1030`, `overrun_vb=71`, `loop_reads=20`, due misses `20`, and `prefetch_overrun_vb=0`, while active payload drops `527206 -> 511690`, upload bytes drop `13675520 -> 13669760`, and both `blocking_vb` and `loop_read_vb` improve `108 -> 107`. Low remains exact-flat at `1415`, `1102/1032`, `overrun_vb=70`, `blocking_vb=124`, `loop_reads=23`, and due misses `22`. WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high controls stayed flat. | Keep as the current VISITOR3 high row. Public rollup remains `+0.3962%` over target / `99.6166%` target speed; raw signed rollup remains `-0.3723%` / `100.4004%`. Since the compact full-matrix baseline, the headless methodology still records about `17.00` public over-target points removed and `12.52` public target-speed points gained, with additional VISITOR3 high CD pressure removed. Continue high-only generated re-anchor/custom-compression candidates, and keep low isolated until scheduler/upload ownership changes. |
| 2026-05-09 | `visitor3-high-reanchor-f125-v207` | `ps1: add visitor3 high frame125 reanchor payload` | Revisit the v201 frame `125` re-anchor as a high-only split after the both-tide run failed only because low regressed. The hypothesis was that high could accept the wider frame `125` target-hull payload on top of v206 while low stayed isolated on v204. | Rewrote only `VISITOR3.FG2` frame `125`/source `171` as a sparse-in-place target-hull motion-copy payload, widening the entry from `x=161,w=162` to `x=154,w=169` while preserving the fixed file footprint, sound offset, pack LBA, and PS-EXE bucket. The transform reduced the high entry `16828 -> 1799` bytes, saving `15029` active bytes with `302` copy rows, `32186` copied pixels, no cleanup rows, and `225` residual pixels. Artifacts: transform summary `scratch/visitor3-high-reanchor-f125-v207/apply-high-reanchor-f125.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-reanchor-f125-v207-focused/20260509-075102-1822459/summary.json`; split broad partial `scratch/ps1-perf-iterate/visitor3-high-reanchor-f125-v207-broad-norequire/20260509-075210-1829320/`; tail rerun `scratch/ps1-perf-iterate/visitor3-high-reanchor-f125-v207-tail-rerun/20260509-075859-1871481/summary.json`. | Promoted as high-only work reduction/CD-pressure, not a public rollup-speed win. VISITOR3 high stays exact-flat on visible timing at `scene_vb=1414`, `loop_vb=1101`, `target_vb=1030`, `overrun_vb=71`, `loop_reads=20`, due misses `20`, and `prefetch_overrun_vb=0`, while active payload drops `511690 -> 496661` and both `blocking_vb` and `loop_read_vb` improve `107 -> 106`. Low remains exact-flat on the v204 row at `1415`, `1102/1032`, `overrun_vb=70`, `blocking_vb=124`, `loop_reads=23`, and due misses `22`. The first broad pass confirmed VISITOR3 low, WALKSTUF1 high/low, and BUILDING2 high/low flat before a harness/resource `137` killed JOHNNY1 before metrics; the tail rerun then passed JOHNNY1 high, ACTIVITY9 low, and FISHING1 high flat. | Keep as the current VISITOR3 high row. Public rollup remains `+0.3962%` over target / `99.6166%` target speed; raw signed rollup remains `-0.3723%` / `100.4004%`. Since the compact full-matrix baseline, the headless methodology still records about `17.00` public over-target points removed and `12.52` public target-speed points gained, with additional VISITOR3 high CD pressure removed. Continue high-only generated re-anchor/custom-compression candidates and keep low isolated until scheduler/upload ownership changes. |
| 2026-05-09 | `visitor3-high-hull-f116-v208` | Log-only; high pack restored. | Test the only large remaining dry-run high-side opcode-1 byte signal after v207. Frame `116` copy-only hull motion saves more than the already-rejected v195 target-hull form (`14226 -> 5124`, `9102` active bytes saved) and removes the `302` cleanup rows that likely made v195 CPU/phase-negative. The binary question was whether the no-cleanup hull shape could turn the frame `116` byte win into a safe high-only CD-pressure reduction. | Rewrote only `VISITOR3.FG2` frame `116` as sparse-in-place hull motion-copy data with `248` copy rows, `13554` copied pixels, no cleanup rows, `274` residual rows, and `2992` residual pixels. The file size, sound offset `585343`, pack LBA, and PS-EXE bucket stayed fixed. Artifacts: transform summary `scratch/visitor3-high-hull-f116-v208/apply-high-hull-f116.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-hull-f116-v208-focused/20260509-083315-2062735/summary.json`. | Failed/no promotion. The high canary regressed hard against v207 despite the byte cut: `scene_vb 1414 -> 1427`, `loop_vb 1101 -> 1114`, `target_vb 1030 -> 1029`, `overrun_vb 71 -> 85`, and `blocking_vb 106 -> 114`; `prefetch_overrun_vb` stayed `0`, loop reads stayed `20`, and due misses stayed `20`. The pack was restored from `scratch/visitor3-high-hull-f116-v208/VISITOR3.FG2.before-v208`. | Close frame `116` under both cleanup-preserving target-hull and no-cleanup hull opcode-1 shapes. It is a byte trap: copy/residual work and dirty cadence overpower the saved payload. Future VISITOR3 work should not spend more runtime gates on adjacent precursor hull motion unless the payload format changes ownership, compression, or scheduling semantics. |
| 2026-05-09 | `visitor3-high-hull-f114-v209` | Log-only; high pack restored. | Test the next-largest surviving high-side opcode-1 dry-run byte signal after frame `116` was closed. Frame `114` copy-only hull motion dropped the high entry `7171 -> 4604` bytes (`2567` active bytes saved) with a smaller copy footprint than frame `116`, so it was the last plausible same-family precursor candidate before switching to a different VISITOR3 strategy. | Rewrote only `VISITOR3.FG2` frame `114` as sparse-in-place hull motion-copy data with `164` copy rows, `3458` copied pixels, no cleanup rows, `211` residual rows, and `3483` residual pixels. The file size, sound offset `585343`, pack LBA, and PS-EXE bucket stayed fixed. Artifacts: transform summary `scratch/visitor3-high-hull-f114-v209/apply-high-hull-f114.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-hull-f114-v209-focused/20260509-090938-2275490/summary.json`. | Failed/no promotion. The high canary regressed against v207: `scene_vb 1414 -> 1418`, `loop_vb 1101 -> 1105`, `target_vb 1030 -> 1029`, and `overrun_vb 71 -> 76`; `blocking_vb` stayed `106`, `prefetch_overrun_vb` stayed `0`, loop reads stayed `20`, and due misses stayed `20`. The pack was restored from `scratch/visitor3-high-hull-f114-v209/VISITOR3.FG2.before-v209`. | Close frame `114` under no-cleanup hull opcode-1. Along with frames `116` and `117`, this shows precursor hull payloads still buy bytes at the cost of visible cadence. Continue VISITOR3 with a different ownership/compression/scheduler model rather than more adjacent hull rewrites. |
| 2026-05-09 | `visitor3-high-setup-prime288-v213` | `ps1: expand visitor3 high setup prime` | Switch from another packet rewrite to a scheduler/data-residency swing. The v207 high row still had a late uncovered read cluster after the accepted high segment and re-anchor stack. The hypothesis was that increasing only the high setup-prime cap from `232 KiB` to `288 KiB` could pull that cluster resident before the loop and trade setup time for visible active-loop relief, while low would need independent binary cap testing. | Raised `FG_VISITOR3_HIGH_SETUP_PRIME_WINDOW_BYTES` and the max resident setup-prime cap to `288 KiB`, kept the low cap binary-tested, and ran focused high plus low cap gates. Low `288 KiB` improved some active CD counters but added hidden refill `prefetch_overrun_vb 0 -> 6`; low `256 KiB` added hidden refill `0 -> 3`; low `240 KiB` regressed visible timing and also added hidden refill, so low was restored to the v204/v213-flat `208 KiB` cap. Artifacts: accepted high focused `scratch/ps1-perf-iterate/visitor3-setup-prime-288-v210-high/20260509-092529-2362759/summary.json`; rejected low caps `scratch/ps1-perf-iterate/visitor3-setup-prime-288-v210-low/20260509-092635-2369444/summary.json`, `scratch/ps1-perf-iterate/visitor3-setup-prime-high288-low256-v211-low/20260509-092743-2376258/summary.json`, and `scratch/ps1-perf-iterate/visitor3-setup-prime-high288-low240-v212-low/20260509-092848-2382794/summary.json`; low restored-flat proof `scratch/ps1-perf-iterate/visitor3-high288-low208-v213-low-flat/20260509-093004-2390398/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-high288-low208-v213-broad/20260509-093115-2397654/summary.json`. | Promoted with explicit setup debt. VISITOR3 high improves active-loop timing `1101/1030 -> 1099/1032`, cuts overrun `71 -> 67`, blocking `106 -> 96`, loop reads `20 -> 17`, loop-read VBlanks `106 -> 96`, and due misses `20 -> 17`, with `prefetch_overrun_vb=0`, fixed LBA `22472`, sectors `760`, and `215040` byte PS-EXE bucket. Total high scene time rises `1414 -> 1423` because the extra setup-prime work runs before the active loop. Low remains exact-flat at `1415`, `1102/1032`, overrun `70`, blocking `124`, loop reads `23`, loop-read VBlanks `131`, due misses `22`, and `prefetch_overrun_vb=0`. WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high controls stayed flat. | Keep as the new VISITOR3 high row and new baseline. Public rollup moves to `+0.3930%` over target / `99.6194%` target speed; raw signed rollup moves to `-0.3755%` / `100.4032%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.01` public over-target points and added about `12.52` public target-speed points. Future VISITOR3 work should treat high setup residency as viable but stop scalar low cap raises until scheduler metadata can prove no hidden refill; next large swings should be pack-authored/precomposed pixels, residual dictionaries, or deadline-owned scheduling. |
| 2026-05-09 | `visitor3-high-setup-prime320-v214` | `ps1: expand visitor3 high setup prime to 320k` | Continue the successful v213 setup-residency lane with a high-only binary cap step. The hypothesis was that the remaining VISITOR3 high active-loop reads would still benefit from more pre-loop residency, while low should remain on the proven `208 KiB` cap because prior low cap raises added hidden refill or visible cadence debt. | Raised `FG_VISITOR3_HIGH_SETUP_PRIME_WINDOW_BYTES` and the max resident setup-prime cap to `320 KiB`, kept `FG_VISITOR3_LOW_SETUP_PRIME_WINDOW_BYTES` at `208 KiB`, then ran focused high, focused low, and the 9-case broad gate. Artifacts: focused high `scratch/ps1-perf-iterate/visitor3-setup-prime-320-v214-high/20260509-095807-2551471/summary.json`; focused low `scratch/ps1-perf-iterate/visitor3-setup-prime-320-v214-low/20260509-095918-2558612/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-high320-low208-v214-broad/20260509-100028-2565654/summary.json`. | Promoted. VISITOR3 high improves active-loop timing `1099/1032 -> 1089/1035`, cuts overrun `67 -> 54`, blocking `96 -> 83`, loop reads `17 -> 15`, loop-read VBlanks `96 -> 83`, and due misses `17 -> 15`, with `prefetch_overrun_vb=0`, fixed LBA `22472`, sectors `760`, and the `215040` byte PS-EXE bucket. Low remains exact-flat at `1415`, `1102/1032`, overrun `70`, blocking `124`, loop reads `23`, loop-read VBlanks `131`, due misses `22`, and `prefetch_overrun_vb=0`. WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high controls stayed flat. | Keep as the new VISITOR3 high row and new baseline. Public rollup moves to `+0.3829%` over target / `99.6284%` target speed; raw signed rollup moves to `-0.3856%` / `100.4122%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.02` public over-target points and added about `12.53` public target-speed points. Future VISITOR3 work should prefer generated/deadline-aware setup residency, pack-authored/precomposed pixels, or residual dictionaries over another broad scalar cap unless the read-plan shows a bounded high-only payoff. |
| 2026-05-09 | `visitor3-low-dual-segment150-174-v215` | Log-only; source restored. | Test whether VISITOR3 low can retain a second setup-owned sector island without using the rejected scalar low setup-prime cap path. The read-plan after v214 showed the accepted low segment `281..305` plus prime `1..105`, with the next active cluster around `150..174`; the hypothesis was that making that cluster resident could save visible loop reads while avoiding the hidden-refill debt seen from larger low caps. | Temporarily added a second reusable VISITOR3-low setup segment for sectors `150..174`, extended runtime setup-segment lookup/copy to support two retained segments, and taught `ps1-foreground-read-plan.py` to report both segments. Ran the focused VISITOR3 low strict gate against the v214 broad baseline. Source was restored after the gate rejected. Artifact: `scratch/ps1-perf-iterate/visitor3-low-dual-segment150-174-v215-low/20260509-102554-2711188/summary.json`. | Failed/no promotion. The probe had a real visible-CD signal, cutting `loop_vb 1102 -> 1100`, overrun `70 -> 67`, blocking `124 -> 109`, loop reads `23 -> 20`, loop-read VBlanks `131 -> 109`, and due misses `22 -> 18`; however it regressed total scene time `1415 -> 1426` and, more importantly, introduced hidden refill debt `prefetch_overrun_vb 0 -> 3`, which violates the current strict promotion rule. Pack LBA `23232` and the `215040` byte PS-EXE bucket stayed fixed. | Do not promote a second hand-authored VISITOR3 low setup segment. The saved-read signal is real, but static dual residency perturbs hidden refill the same way scalar low cap raises did. Future low work should use deadline-aware/generated setup ownership, pack-authored payload reductions, or a custom compression path that removes read pressure without adding hidden refill. |
| 2026-05-09 | `visitor3-low-dual-segment150-174-slack4-v216` | `ps1: add visitor3 low guarded second setup segment` | Retry the v215 second-segment signal with explicit deadline ownership instead of static residency alone. The binary question was whether a VISITOR3-low-only `4` VBlank window-refill guard could keep hidden refill at zero while retaining enough of the `150..174` setup-segment read savings to promote. | Added a second reusable VISITOR3-low setup segment for sectors `150..174`, kept the existing low segment `281..305`, extended setup-segment lookup/copy and read-plan parsing for two segments, and raised only VISITOR3-low window-refill eligibility from `3` to `4` VBlanks. Ran a focused low strict gate and then the 9-case broad gate for VISITOR3 high/low, WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high. Artifacts: focused low `scratch/ps1-perf-iterate/visitor3-low-dual-segment150-174-slack4-v216-low/20260509-103215-2746966/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-low-dual-segment150-174-slack4-v216-broad/20260509-103329-2754585/summary.json`. | Promoted with explicit setup debt. VISITOR3 low improves active-loop timing `1102/1032 -> 1098/1034`, overrun `70 -> 64`, blocking `124 -> 112`, loop reads `23 -> 20`, loop-read VBlanks `131 -> 119`, and due misses `22 -> 19`; `prefetch_overrun_vb` stays `0`, pack LBA stays `23232`, sectors stay `760`, and the PS-EXE bucket stays `215040`. The tradeoff is pre-loop setup time: low `scene_vb 1415 -> 1424`. VISITOR3 high and all broad controls stayed flat against the v214 baseline. | Keep as the new VISITOR3 low row and baseline. Public rollup moves to `+0.3783%` over target / `99.6326%` target speed; raw signed rollup moves to `-0.3903%` / `100.4164%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.02` public over-target points and added about `12.53` public target-speed points. This closes unguarded dual residency but keeps deadline-owned segmented setup as viable for VISITOR3 when hidden refill stays flat. |
| 2026-05-09 | `visitor3-low-segment133-v217-v219` | Log-only; source restored. | Probe whether moving or widening the accepted VISITOR3 low second setup segment earlier could cover the frame `117..126` active read cluster and remove more of the last low-tide overrun without touching pack layout. Three binary variants were tested: v217 widened the second segment to `133..174`, v218 expressed the same `41`-sector range as `24+17` sectors to look for codegen neutrality, and v219 moved the existing `24`-sector segment to `133..157`. | v217/v218 focused gates both produced the large local win but grew `foregroundPilotPlay` by `16` bytes; v219 kept the segment size fixed and removed the obvious hot-symbol growth. Artifacts: v217 focused `scratch/ps1-perf-iterate/visitor3-low-segment133-174-v217-low/20260509-110818-2950599/summary.json`; v217 broad `scratch/ps1-perf-iterate/visitor3-low-segment133-174-v217-broad/20260509-110926-2957404/summary.json`; v218 focused `scratch/ps1-perf-iterate/visitor3-low-segment133-174-add17-v218-low/20260509-111943-3018745/summary.json`; v219 focused `scratch/ps1-perf-iterate/visitor3-low-segment133-157-v219-low/20260509-112117-3027866/summary.json`; v219 broad `scratch/ps1-perf-iterate/visitor3-low-segment133-157-v219-broad/20260509-112353-3042648/summary.json`. | Failed/no promotion. v217/v218 cut VISITOR3 low to `1088/1034`, overrun `54`, blocking `101`, loop reads `18`, and due misses `17`, but the broad gate rejected the hot-code shift: WALKSTUF1 high regressed `loop_vb 1495 -> 1499`, overrun `68 -> 84`, blocking `95 -> 105`, and prefetch `26 -> 42`; WALKSTUF1 low regressed `1489 -> 1498`, overrun `62 -> 80`, blocking `86 -> 99`, and prefetch `27 -> 40`; BUILDING2 low also moved hidden refill `1 -> 2`. v219's fixed-size segment still reproduced the broad failures despite only improving VISITOR3 low `1098 -> 1095`, overrun `64 -> 62`, with blocking flat at `112`. | Do not move the accepted low second segment earlier as a hand-authored runtime constant. The frame `117..126` cluster is a real local target, but every earlier segment shape tested either shifts hot code or perturbs global refill cadence enough to fail the broad canaries. Keep v216 as the baseline and only revisit this cluster through generated deadline metadata, pack-authored payload reduction, or a layout-pinned scheduler that proves WALKSTUF1 and BUILDING2 stay flat. |
| 2026-05-09 | `visitor3-low-hull-f117-v220` | Log-only; low pack restored. | Retest the closest low-tide precursor motion-copy miss on the current v216 baseline. The prior v183/v184 frame `117` probes were near misses before the accepted setup-prime and guarded low-segment changes; the binary question was whether a low-only sparse-in-place hull payload could now convert a large byte cut into a visible win without touching runtime code, pack size, LBA, or sound-table phase. | Rewrote only `VIST3LOW.FG2` frame `117`/source `163` as an opcode-1 hull motion payload, preserving the `1555450` byte pack footprint and sound offset `626575`. The replacement kept later offsets fixed and changed the frame payload `17570 -> 4431`, saving `13139` active bytes with `277` copy rows, `20148` copied pixels, no cleanup rows, and `2188` residual pixels. Artifacts: transform `scratch/visitor3-low-hull-f117-v220/apply-low-hull-f117.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-hull-f117-v220-focused/20260509-113836-3128409/summary.json`. | Failed/no promotion. Despite the pack-byte win and fixed layout, current v216 low regressed `scene_vb 1424 -> 1430`, `loop_vb 1098 -> 1104`, overrun `64 -> 70`, and blocking `112 -> 115`; `prefetch_overrun_vb` stayed `0`, loop reads stayed `20`, and due misses stayed `19`. The low pack was restored from `scratch/visitor3-low-hull-f117-v220/VIST3LOW.FG2.before-v220`. | Close low frame `117` under the no-cleanup hull opcode-1 shape on the current baseline. The byte savings are real but still CPU/phase-negative, confirming that remaining VISITOR3 low gains need a different format or scheduler ownership rather than more precursor hull payloads. |
| 2026-05-09 | `visitor3-low-segment175-199-v221` | Log-only; source restored. | Test a code-size-neutral segment swap rather than another added segment: keep the accepted low second segment `150..174`, but spend the first reusable low setup segment on the next read-plan cluster `175..199` instead of the v216 terminal segment `281..305`. The hypothesis was that the earlier active read cluster might buy more loop relief than the terminal resident island without adding runtime tables, code, or PS-EXE layout drift. | Temporarily changed `FG_VISITOR3_LOW_SETUP_SEGMENT_START` from `281` to `175`, leaving both segment byte counts and the second `150..174` segment unchanged. Ran the focused VISITOR3 low strict gate against the v216 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-segment175-199-plus150-174-v221-focused/20260509-114113-3143293/summary.json`. | Failed/no promotion. The swap removed the terminal residency and worsened the active profile: low moved `scene_vb 1424 -> 1425`, `loop_vb 1098 -> 1099`, `target_vb 1034 -> 1031`, overrun `64 -> 68`, blocking `112 -> 118`, loop reads `20 -> 21`, and due misses `19 -> 20`; `prefetch_overrun_vb` stayed `0`. | Do not replace the accepted terminal `281..305` low segment with `175..199`. The terminal segment is carrying useful deadline/slack value even though later read-plan clusters look attractive. Future low setup residency must be generated as an additional deadline-owned plan or paired with payload reduction, not by stealing the accepted terminal island. |
| 2026-05-09 | `visitor3-low-group133-149-v222` | Log-only; source restored. | Test the next VISITOR3 low scheduler-owned read cluster without adding another setup segment: register a guarded `133..149` grouped append that only fires from an observed append boundary and requires the existing VISITOR3-low `4` VBlank slack guard. The hypothesis was that this could collapse the frame `117..118` cluster at runtime, avoiding the v217-v219 scene-start setup and broad-cadence failures. | Temporarily added `kVisitor3LowReadGroups16[] = {133, 149, 4}` and selected it only for VISITOR3 low when the normal `16 KiB` stream window was active. Ran the focused VISITOR3 low strict gate against the v216 broad baseline, then restored the source. Artifact: `scratch/ps1-perf-iterate/visitor3-low-group133-149-v222-focused/20260509-114712-3176660/summary.json`. | Failed/no promotion. The run stayed exact-flat at the current v216 low profile: `scene_vb=1424`, `loop_vb=1098`, `target_vb=1034`, overrun `64`, blocking `112`, `prefetch_overrun_vb=0`, loop reads `20`, loop-read VBlanks `119`, and due misses `19`. The group never fired (`group_hits=0`), while `foregroundPilotPlay` grew `+48` bytes and shifted hot symbols by `+48` without changing the `215040` byte PS-EXE bucket or pack LBA `23232`. | Do not retry VISITOR3 low `133..149` as a hand-authored grouped append. Append-start fireability in the read-plan is not sufficient under the current one-staged-frame/window-prefetch state machine. The next VISITOR3 attempt needs either deeper scheduler state, a second retained window, or pack-side payload reduction rather than another source-table row. |
| 2026-05-09 | `visitor3-low-fresh-group133-149-v223` | Log-only; source restored. | Follow the v222 miss to its most direct scheduler variant: allow guarded read groups to extend fresh stream-window fills as well as append extensions, then register the same VISITOR3-low `133..149` group with the accepted `4` VBlank slack guard. The hypothesis was that v222 did not fire because the useful cluster starts a new window, not an append. | Temporarily added fresh-fill group expansion in `fgRuntimeFillWindowForEntry()` and selected `kVisitor3LowReadGroups16[] = {133, 149, 4}` only for VISITOR3 low. Ran the focused VISITOR3 low strict gate against the v216 broad baseline, then restored the source. Artifact: `scratch/ps1-perf-iterate/visitor3-low-fresh-group133-149-v223-focused/20260509-115354-3214558/summary.json`. | Failed/no promotion. Fresh-fill grouping also stayed exact-flat at v216: `scene_vb=1424`, `loop_vb=1098`, `target_vb=1034`, overrun `64`, blocking `112`, `prefetch_overrun_vb=0`, loop reads `20`, loop-read VBlanks `119`, and due misses `19`. The patch grew `fgRuntimeFillWindowForEntry` by `12` bytes and `foregroundPilotPlay` by `48` bytes, shifted hot symbols by `+156`, and did not change pack LBA `23232` or the `215040` byte PS-EXE bucket. | Close hand-authored `133..149` read-group scheduling in both append and fresh-fill forms. The cluster is still real, but the current contiguous one-window scheduler cannot convert it into a measured win without either deadline-owned residency, a second retained window/sector map, or pack-side payload reshaping. |
| 2026-05-09 | `visitor3-low-f123-original-v224` | Log-only; low pack restored. | Test whether part of the accepted v181 low `119..123` motion-copy cluster has become CPU-negative after the later setup-segment and setup-prime changes. The smallest binary rollback was frame `123` only: replace its compact motion payload with the original pre-v181 residual payload while preserving the fixed `1555450` byte pack footprint and leaving runtime source untouched. | Repacked only `VIST3LOW.FG2` frame `123`/source `169` from the v181-before payload. Frame `123` grew `2616 -> 17570` bytes, the sound table shifted `+362` bytes (`626575 -> 626937`), the pack LBA stayed `23232`, the PS-EXE bucket stayed `215040`, and the original low pack was restored after the focused gate. Artifacts: transform `scratch/visitor3-low-f123-original-v224/apply-low-f123-original.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-f123-original-v224-focused/20260509-115936-3246241/summary.json`. | Failed/no promotion. The rollback stayed exact-flat against v216: `scene_vb=1424`, `loop_vb=1098`, `target_vb=1034`, overrun `64`, blocking `112`, `prefetch_overrun_vb=0`, loop reads `20`, loop-read VBlanks `119`, and due misses `19`. | Do not spend more time on isolated low `119..123` rollback frames as the primary lane. At least frame `123` is not carrying measurable CPU debt under the current VBlank gate; the remaining VISITOR3 low gap still needs a larger data-shape format, retained-window/deadline scheduling, or upload/restore ownership change. |
| 2026-05-09 | `visitor3-low-window20k-v225-v226` | Log-only; source restored. | Test the direct implication from the v222/v223 misses: low frame `117` is larger than the default `16 KiB` stream window, so the `133..149` cluster may need a low-only `20 KiB` active stream window before any scheduler-owned cluster can become visible. The binary question was whether a bigger VISITOR3-low stream window could improve low without breaking setup-prime residency or broad canaries. | First ran a boot-parameter `prefetch-window 20480` probe, then tried the source-level version that keeps accepted VISITOR3 setup-prime behavior despite the non-default low stream window. Artifacts: rejected parameter-only probe `scratch/ps1-perf-iterate/visitor3-low-window20k-v225-focused-labeled/20260509-120330-3268879/summary.json`; source-level focused pass `scratch/ps1-perf-iterate/visitor3-low-window20k-setupprime-v226-focused/20260509-120521-3279846/summary.json`; broad rejection `scratch/ps1-perf-iterate/visitor3-low-window20k-setupprime-v226-broad/20260509-120630-3286805/summary.json`. | Failed/no promotion. The parameter-only path disabled setup-prime and regressed low to `1105/1035`, overrun `70`, blocking `139`, hidden refill `3`, and `42` loop reads. The source-level path gave a real VISITOR3-low local win (`1098/1034 -> 1093/1035`, overrun `64 -> 58`, blocking `112 -> 99`, due misses `19 -> 18`, hidden refill `0`), while high stayed exact-flat. The broad gate rejected the hot-code/cadence side effects: WALKSTUF1 high regressed overrun `68 -> 84`, blocking `95 -> 105`, and refill `26 -> 42`; WALKSTUF1 low regressed overrun `62 -> 80`, blocking `86 -> 99`, and refill `27 -> 40`; BUILDING2 low also moved hidden refill `1 -> 2`. | Do not promote scalar VISITOR3-low `20 KiB` stream windows, even with setup-prime preserved. The local signal confirms frame `117`/sectors `133..149` are still a meaningful target, but any runtime-source shape that grows `foregroundPilotPlay` and shifts global cadence must be layout-pinned, generated as data, or moved out of hot code before retrying. |
| 2026-05-09 | `visitor3-low-f125126-resident-v227` | `ps1: reanchor visitor3 low resident frames` | Convert the high-tide frame-125/frame-126 re-anchor win into a low-tide win without touching hot runtime code: repack only `VIST3LOW.FG2` so the re-anchored frame payloads land inside the already accepted low setup segment `150..174`. The binary question was whether data placement alone could remove one remaining low due read after v216 while keeping fixed pack size, LBA, sound offset, and PS-EXE bucket. | Repacked only the low pack. Frame `125` drops `16828 -> 1799` bytes, frame `126` drops `16828 -> 1312` bytes, total active payload drops by `30545` bytes, and both payloads fit within sectors `150..174`. Ran a focused low gate, a 9-case broad gate, and then a current-control with the original low pack restored for the unrelated WALKSTUF1/BUILDING2 failures. Artifacts: transform `scratch/visitor3-low-reanchor-f125126-resident-v227/apply-low-reanchor-f125126-resident.json`; focused low `scratch/ps1-perf-iterate/visitor3-low-f125126-resident-v227-focused/20260509-122159-3376401/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-low-f125126-resident-v227-broad/20260509-122307-3383321/summary.json`; current-control drift check `scratch/ps1-perf-iterate/visitor3-low-f125126-resident-v227-current-control/20260509-123359-3447654/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1424 -> 1421`, active loop `1098/1034 -> 1095/1035`, overrun `64 -> 60`, blocking `112 -> 108`, loop reads `20 -> 19`, loop-read VBlanks `119 -> 115`, and due misses `19 -> 18`; hidden refill stays `0`. VISITOR3 high stayed exact-flat. The broad gate reproduced the known WALKSTUF1 high/low and BUILDING2-low drift, but the original-pack control reproduced the exact same unrelated timings, proving the pack-only candidate did not cause those failures. | Keep as the new VISITOR3 low row and baseline. Public rollup moves to `+0.3751%` over target / `99.6354%` target speed; raw signed rollup moves to `-0.3934%` / `100.4192%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.03` public over-target points and added about `12.54` public target-speed points. This validates pack-only resident payload placement as the next VISITOR3 lane when runtime-source scheduler tweaks are layout-negative. |
| 2026-05-09 | `visitor3-low-f127-resident-v228` | Log-only; low pack restored. | Retry the previously rejected low frame-127 re-anchor after v227 changed the local data placement. Unlike v203, this probe copied the accepted high frame-127 target-hull payload into the already accepted low `150..174` resident setup segment immediately after frames `125/126`, testing whether the old low failure was placement/CD-phase rather than the frame shape itself. | Repacked only `VIST3LOW.FG2` frame `127`/source `173`: entry anchor `x=209,w=163 -> x=154,w=218`, data offset `372780 -> 342235`, payload `16936 -> 1413`, active bytes `564565 -> 549042`, with fixed pack size, LBA `23232`, sound offset, and no runtime-source change. Artifacts: transform `scratch/visitor3-low-reanchor-f127-resident-v228/apply-low-reanchor-f127-resident.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-f127-resident-v228-focused/20260509-125908-3588029/summary.json`. | Failed/no promotion. The candidate cut CD pressure but lost the visible timing gate: compared with v227, blocking improved `108 -> 104`, loop reads `19 -> 18`, loop-read VBlanks `115 -> 111`, and due misses `18 -> 17`, while `prefetch_overrun_vb` stayed `0`; however scene time regressed `1421 -> 1424`, loop timing regressed `1095/1035 -> 1098/1034`, and overrun regressed `60 -> 64`. | Do not promote low frame `127` as the next resident re-anchor. The read-pressure signal is real, but this shape spends more visible CPU/cadence than it saves even when placed inside the accepted setup segment. Future low frame-127 work needs a different residual/copy format, precomposed/upload ownership, or a scheduler that recovers the lost target/loop timing before trading for lower reads. |
| 2026-05-09 | `visitor3-motion-marker-skip-v229` | Log-only; source restored. | Probe the suspicious compact-motion runtime path: VISITOR3 packs contain `0xffff` motion markers and `graphics_ps1.c` defines a motion decoder, but the compact temporal residual entry point does not dispatch to it. The binary question was whether explicitly skipping motion-marker payloads would remove futile compact-residual parsing cost without changing behavior. | Temporarily added an early `cleanupRows == GR_FGP3_MOTION_MARKER` return in `grCompositePacked4CompactTemporalResidualToBackground()`, then ran focused VISITOR3 high/low against the v227 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-motion-marker-skip-v229-focused/20260509-130628-3629128/summary.json`. Source was restored after the gate failed. | Failed/no promotion. Both tides stayed exact-flat against v227: high remained `1420`, `1089/1035`, blocking `83`, due misses `15`; low remained `1421`, `1095/1035`, blocking `108`, due misses `18`; hidden refill stayed `0`. The source probe shifted tracked hot symbols by `+12` bytes without improving any key metric. | Close the marker-skip variant as a no-op. The remaining motion-marker question is not a simple skip; future work needs either a real compact-motion dispatch with visual proof or pack-side conversion back to normal residuals for frames where the marker path is not providing runtime value. |
| 2026-05-09 | `visitor3-low-empty-motion-v230-v233` | Log-only; low pack restored. | Test the extreme implication of v229 and the current marker behavior: if low motion-marker payloads are not visibly contributing through the compact residual entry point, can they be turned into holds or tiny resident no-op residuals to remove the tight frame-118 read without a runtime-source change? | Tried four pack-only variants against v227: v230 emptied low frames `118..126`; v231 emptied only resident frames `125/126`; v232 emptied only frame `118`; v233 kept frame `118` non-empty but repointed it to a two-byte no-op residual inside the accepted `150..174` setup segment. Artifacts: v230 transform `scratch/visitor3-low-empty-motion118-126-v230/apply-low-empty-motion118-126.json`, focused `scratch/ps1-perf-iterate/visitor3-low-empty-motion118-126-v230-focused/20260509-130950-3649210/summary.json`; v231 transform `scratch/visitor3-low-empty-motion125126-v231/apply-low-empty-motion125126.json`, focused `scratch/ps1-perf-iterate/visitor3-low-empty-motion125126-v231-focused/20260509-131140-3659644/summary.json`; v232 transform `scratch/visitor3-low-empty-motion118-v232/apply-low-empty-motion118.json`, focused `scratch/ps1-perf-iterate/visitor3-low-empty-motion118-v232-focused/20260509-131250-3666645/summary.json`; v233 transform `scratch/visitor3-low-f118-resident-noop-v233/apply-low-f118-resident-noop.json`, focused `scratch/ps1-perf-iterate/visitor3-low-f118-resident-noop-v233-focused/20260509-131439-3677009/summary.json`. | Failed/no promotion. The broad empty-hold sweep and the single-frame `118`/resident-noop variants all improved CD pressure but regressed the timing gate: v230 and v232/v233 moved low `1095/1035 -> 1096/1035`, overrun `60 -> 61`, blocking `108 -> 105`, loop reads `19 -> 18`, and due misses `18 -> 17`, with hidden refill still `0`. v231 was exact-flat and only changed hit accounting. | Do not promote no-op/empty motion holds. The frame-118 read is real, but the payload also preserves cadence/render scheduling in a way that the public loop needs; removing it buys CD at the cost of a VBlank. Future frame-118 work needs a different residual format that keeps the cadence benefit while shrinking or relocating bytes, not a semantic no-op. |
| 2026-05-09 | `visitor3-low-f118-resident-copy-v234` | `ps1: keep visitor3 low frame118 resident` | Convert the v230-v233 frame-118 CD-pressure signal into a semantic-preserving pack-only move. The failed no-op/empty variants proved frame `118` cannot be erased without cadence debt, so the binary question was whether copying the existing payload unchanged into the already accepted low `150..174` setup segment could remove the active-loop read while preserving render/cadence behavior. | Repacked only `VIST3LOW.FG2` by copying frame `118`'s existing `3667` byte payload from offset `290579` to offset `307200` inside the accepted resident setup segment. The entry geometry, payload bytes, pack footprint `1555450`, LBA `23232`, sound offset `626575`, and PS-EXE bucket stayed fixed. Artifacts: transform `scratch/visitor3-low-f118-resident-copy-v234/apply-low-f118-resident-copy.json`; focused low gate `scratch/ps1-perf-iterate/visitor3-low-f118-resident-copy-v234-focused/20260509-131645-3689098/summary.json`; broad/control gate `scratch/ps1-perf-iterate/visitor3-low-f118-resident-copy-v234-broad/20260509-131940-3705538/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1421 -> 1417`, active loop `1095/1035 -> 1091/1035`, overrun `60 -> 56`, blocking `108 -> 103`, loop reads `19 -> 18`, loop-read VBlanks `115 -> 110`, and due misses `18 -> 17`; hidden refill stays `0`. VISITOR3 high, WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high stayed exact-flat against v227 in the broad/control run; the script's nonzero exit was only `--require-improvement` rejecting flat controls. | Keep as the new VISITOR3 low row and baseline. Public rollup moves to `+0.3720%` over target / `99.6381%` target speed; raw signed rollup moves to `-0.3965%` / `100.4219%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.03` public over-target points and added about `12.54` public target-speed points. The next VISITOR3 low target is frame `127` or the `175..199` cluster, but only with a format/scheduler path that avoids the visible cadence loss seen in v228. |
| 2026-05-09 | `visitor3-low-f127-resident-copy-v237` | `ps1: keep visitor3 low frame127 resident` | Convert the remaining frame-127 CD-pressure signal without replaying the failed v228 re-anchor shape. The binary question was whether compacting the accepted low `150..174` resident segment and copying frame `127` unchanged into freed resident space could remove another active-loop read while preserving cadence/render semantics. | Repacked only `VIST3LOW.FG2`. Frames `125/126` moved earlier unchanged (`339124 -> 324532` and `340923 -> 326331`), and frame `127`'s existing `16936` byte payload moved unchanged from offset `372780` to `339124`, placing it in sectors `165..174`. Entry geometry, payload bytes, pack footprint `1555450`, LBA `23232`, sound offset `626575`, and PS-EXE bucket stayed fixed. Artifacts: transform `scratch/visitor3-low-f127-resident-copy-v237/apply-low-f127-resident-copy.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-f127-resident-copy-v237-focused/20260509-140328-3954717/summary.json`; broad/control gate `scratch/ps1-perf-iterate/visitor3-low-f127-resident-copy-v237-broad/20260509-140440-3961993/summary.json`; current-control drift check `scratch/ps1-perf-iterate/visitor3-low-f127-resident-copy-v237-current-control/20260509-141713-4034325/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1417 -> 1414`, active loop `1091/1035 -> 1088/1035`, overrun `56 -> 53`, blocking `103 -> 95`, loop reads `18 -> 17`, loop-read VBlanks `110 -> 102`, and due misses `17 -> 16`; hidden refill stays `0`. VISITOR3 high stayed flat. The broad run's WALKSTUF1-high timing differed from the older CSV baseline, but restoring the old `VIST3LOW.FG2` reproduced the same `1495/1427` current-control result, proving the drift is unrelated to this pack-only candidate. | Keep as the new VISITOR3 low row and baseline. Public rollup moves to `+0.3697%` over target / `99.6402%` target speed; raw signed rollup moves to `-0.3988%` / `100.4240%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.03` public over-target points and added about `12.54` public target-speed points. The next VISITOR3 target is high first by speed (`95.04%`) or another semantic-preserving low resident relocation only if it avoids the v228 cadence loss. |
| 2026-05-09 | `visitor3-high-f127-f130-resident-copy-v238` | `ps1: keep visitor3 high frames resident` | Convert the first uncovered high cluster after the `320 KiB` setup-prime cap into a semantic-preserving pack-only resident placement win. | Repacked only `VISITOR3.FG2`; compacted frames `117..130` unchanged into the existing high setup-prime resident window, placing frames `127..130` fully inside already paid setup residency. Entry geometry, payload bytes, pack footprint `1555450`, LBA `22472`, sound offset `585343`, and the `215040` byte PS-EXE bucket stayed fixed. Artifacts: transform `scratch/visitor3-high-f127-f130-resident-copy-v238/apply-high-f127-f130-resident-copy.json`; focused gate `scratch/ps1-perf-iterate/visitor3-high-f127-f130-resident-copy-v238-focused/20260509-144443-4186029/summary.json`; broad regression gate `scratch/ps1-perf-iterate/visitor3-high-f127-f130-resident-copy-v238-broad-regression/20260509-145507-53698/summary.json`. | Promoted. VISITOR3 high improves `scene_vb 1420 -> 1406`, active loop `1089/1035 -> 1075/1037`, overrun `54 -> 38`, blocking `83 -> 59`, loop reads `15 -> 11`, loop-read VBlanks `83 -> 59`, and due misses `15 -> 11`; hidden refill stays `0`. VISITOR3 low and all broad controls stayed flat. | Keep as the new VISITOR3 high row and baseline. Public rollup moves to `+0.3574%` over target / `99.6515%` target speed; raw signed rollup moves to `-0.4111%` / `100.4353%`. Methodology total is about `17.04` public over-target points removed plus `12.55` target-speed points gained. Next focus remains VISITOR3 low unless another semantic-preserving high relocation is obvious. |
| 2026-05-09 | `visitor3-low-f128-resident-evict124-v239` | Log-only; low pack restored. | Try the largest remaining semantic-preserving low resident relocation without runtime-source changes: evict small frame `124` from the accepted `150..174` resident segment, compact frames `125..127` earlier unchanged, and copy frame `128` unchanged into the freed resident tail. The binary question was whether trading one small frame-124 read could remove the larger frame-128 visible-loop read. | Repacked only `VIST3LOW.FG2` for the focused probe. Frame `124` moved `321239 -> 358400` (sectors `156..158 -> 175..176`), frames `125/126` moved to `321239/323038`, frame `127` moved `339124 -> 324350`, and frame `128` moved `389716 -> 341286` so it ended at sector `174` with `122` resident bytes spare. Pack size `1555450`, LBA `23232`, sound offset `626575`, and PS-EXE bucket stayed fixed. Artifacts: transform `scratch/visitor3-low-f128-resident-evict124-v239/apply-low-f128-resident-evict124.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-f128-resident-evict124-v239-focused/20260509-155524-388767/summary.json`. | Failed/no promotion. The read trade was negative: VISITOR3 low regressed from v238/v237 baseline `scene_vb 1414 -> 1423`, active loop `1088/1035 -> 1097/1034`, overrun `53 -> 63`, blocking `95 -> 108`, loop reads `17 -> 18`, and due misses `16 -> 17`; hidden refill stayed `0`. | Do not evict resident low frame `124` to buy frame `128` residency. Frame `124` is small but cadence/read placement is more valuable than the frame-128 relocation under the current single-window scheduler. Future VISITOR3 low work should avoid trading away the accepted `118..127` resident cluster and instead target a real data-shape reduction for frames `113..117` or `128..135`, or a scheduler/data format that can keep both frame `124` and frame `128` resident. |
| 2026-05-09 | `visitor3-low-dedupe-115117-v240` | Log-only; low pack restored. | Test a table-only duplicate-payload alias before building a new codec. Low frames `114`, `115`, and `117` contain identical `17570` byte payloads, so frames `115` and `117` were pointed at frame `114`'s earlier copy without changing geometry, payload bytes, pack footprint, sound offset, LBA, or runtime source. | Repacked only the entry table in `VIST3LOW.FG2`: frame `115` moved `237883 -> 220313` (sectors `116..124 -> 107..116`) and frame `117` moved `273009 -> 220313` (`133..141 -> 107..116`). The duplicate payload bytes remained in the fixed-size pack. Artifacts: transform `scratch/visitor3-low-dedupe-115117-v240/apply-low-dedupe-115117.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-dedupe-115117-v240-focused/20260509-155900-408779/summary.json`. | Failed/no promotion. Visible timing was exact-flat at `scene_vb=1414`, `loop_vb=1088`, `target_vb=1035`, and overrun `53`, but CD pressure regressed: blocking `95 -> 97`, loop reads stayed `17`, and due misses stayed `16`; hidden refill stayed `0`. | Do not promote duplicate-payload aliasing for low frames `115/117`. It can reveal scheduler candidates (`107..123` becomes group-compatible), but the direct alias shifts blocking the wrong way. Future low work should use real generated payload reduction or residency that does not perturb the accepted `114..117` read phase. |
| 2026-05-09 | `visitor3-low-dedupe-117-to-115-v241` | Log-only; low pack restored. | Narrow the duplicate-payload alias test after v240 showed aliasing both `115` and `117` to frame `114` perturbs the accepted read phase. The binary question was whether pointing only frame `117` at frame `115`'s identical payload could remove or reshape one read without touching frame `114` or the resident `118..127` cluster. | Repacked only the entry table in `VIST3LOW.FG2`: frame `117` moved `273009 -> 237883` (sectors `133..141 -> 116..124`) while frame `115` stayed at its own original payload. Geometry, payload bytes, pack footprint `1555450`, LBA `23232`, sound offset `626575`, and the PS-EXE bucket stayed fixed. Artifacts: transform `scratch/visitor3-low-dedupe-117-to-115-v241/apply-low-dedupe-117-to-115.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-dedupe-117-to-115-v241-focused/20260509-160353-435639/summary.json`. | Failed/no promotion. Timing was exact-flat at `scene_vb=1414`, `loop_vb=1088`, `target_vb=1035`, and overrun `53`, but visible CD pressure regressed `blocking_vb 95 -> 96`; loop reads stayed `17`, due misses stayed `16`, and hidden refill stayed `0`. | Do not promote table-only aliasing of frame `117` to frame `115`. The duplicate bytes are real, but the active-loop scheduler still prefers the accepted physical placement; future VISITOR3 low work needs an actual generated payload-size reduction or a new resident-cap/data-shape strategy rather than another direct table alias in the `114..117` cluster. |
| 2026-05-09 | `visitor3-low-motion-114117-v242` | Log-only; low pack restored. | Take a larger low-only data-shape swing at the `114..117` precursor cluster after table aliasing proved phase-negative. The hypothesis was that converting the four `~17 KiB` low payloads to sparse in-place hull motion-copy entries could reduce CD pressure enough to pay for the existing motion decoder, while preserving every later payload offset, sound offset, LBA, pack footprint, and setup segment. | Repacked only `VIST3LOW.FG2` for a focused probe. Frames `114..117` were rewritten as opcode-1 hull motion-copy payloads in place, keeping entry offsets fixed and leaving frame `118` unchanged after a dry-run showed it would not safely shrink. The four rewritten entries saved `50880` active bytes: frame `114` `17570 -> 4604`, frame `115` `17570 -> 5227`, frame `116` `17556 -> 5124`, and frame `117` `17570 -> 4431`. Artifacts: dry-run screen `scratch/visitor3-low-motion-114118-v242-dryrun-hull.json`; transform `scratch/visitor3-low-motion-114117-v242/apply-low-motion-114117.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-motion-114117-v242-focused/20260509-160813-460038/summary.json`. | Failed/no promotion. The transform did reduce visible CD pressure (`blocking_vb 95 -> 89`) with hidden refill still `0`, but the motion-copy render/cadence cost dominated: `scene_vb 1414 -> 1432`, active loop `1088/1035 -> 1106/1034`, and overrun `53 -> 72`; loop reads and due misses stayed `17` and `16`. | Do not promote the current hull-motion decoder for low frames `114..117`. The byte savings are real, but opcode-1 hull work is too expensive for this low-tide cluster; future precursor work needs a faster/precomposed opcode, direct upload-ready ownership, or a phase-aware residual layout rather than more current decoder hull rewrites. |
| 2026-05-09 | `visitor3-low-motion114117-group116132-v243` | Log-only; source and low pack restored. | Pair the failed v242 byte-shrinking hull payload with a generated read-plan follow-up. After v242 made frames `115` and `116` small enough to fit a `116..132` retained-window candidate, the binary question was whether a low-tide-only grouped append could save the extra read needed to offset the motion decoder cost. | Reapplied the v242 low `114..117` sparse in-place hull payloads, then temporarily added a low-tide VISITOR3 read group `{116,132}` through `src/foreground_pilot.c`. Pack footprint, LBA `23232`, sound offset `626575`, and PS-EXE bucket stayed fixed for the probe. Artifacts: pack transform `scratch/visitor3-low-motion114117-group116132-v243/apply-low-motion114117.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-motion114117-group116132-v243-focused/20260509-161039-473682/summary.json`. | Failed/no promotion. The focused result was effectively identical to v242: `scene_vb 1414 -> 1432`, active loop `1088/1035 -> 1106/1034`, overrun `53 -> 72`, blocking `95 -> 89`, hidden refill `0`, loop reads `17`, and due misses `16`. The hand group did not recover the lost cadence. | Do not retry this as another VISITOR3 low hand read-table combo. The candidate must be owned by generated scheduler/deadline metadata or a different payload format; the current source-table path adds risk without changing the v242 failure shape. |
| 2026-05-09 | `visitor3-low-f128-gap-align-v244` | Log-only; low pack restored. | Test a pure physical-placement alternative to v239 without evicting any accepted resident frame. Frame `128` still has to be read after the `150..174` setup segment, but the v237 layout leaves a zero gap after resident frame `127`; the hypothesis was that duplicating frame `128` at sector `175` could improve seek/phase versus the original sector `190` placement while preserving render semantics and the resident `118..127` cluster. | Repacked only the entry table and copied frame `128`'s existing `16992` byte payload unchanged from offset `389716` (sectors `190..199`) to offset `358400` (sectors `175..184`). Frame `124`, frames `118..127`, pack footprint `1555450`, LBA `23232`, sound offset `626575`, and PS-EXE bucket stayed fixed. Artifact: transform `scratch/visitor3-low-f128-gap-align-v244/apply-low-f128-gap-align.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-f128-gap-align-v244-focused/20260509-161318-488429/summary.json`. | Failed/no promotion. Earlier physical placement was negative unless it becomes setup-resident: `scene_vb 1414 -> 1424`, active loop `1088/1035 -> 1098/1035`, overrun `53 -> 63`, and blocking `95 -> 103`; loop reads and due misses stayed `17` and `16`, hidden refill stayed `0`. | Do not move low frame `128` to the immediate post-resident gap by itself. Frame `128` needs either real setup residency without displacing frame `124`, a smaller payload that changes read duration without cadence loss, or generated deadline scheduling; simple earlier sector placement worsens phase. |
| 2026-05-09 | `visitor3-low-f128-resident-evict125126-alias123-v245` | Log-only; low pack restored. | Test the strongest remaining physical-layout path for making frame `128` setup-resident without evicting frame `124`. The hypothesis was that aliasing duplicate resident frame `123` to frame `121`, sliding frame `124`, and trading tiny frames `125/126` out of the `150..174` setup segment would pay for the large frame `128` read while preserving the accepted `118..124` and `127` cadence. | Repacked only `VIST3LOW.FG2` for the focused probe: frame `123` pointed at frame `121`'s identical payload, frame `124` moved to offset `318623`, frame `127` moved to `321916`, frame `128` moved inside the setup segment at `338852`, and frames `125/126` moved outside the setup segment to `356352/358151`. Pack footprint `1555450`, LBA `23232`, sound offset `626575`, setup policy `segments=[[281,305],[150,174]]`, and the PS-EXE bucket stayed fixed. Artifact: focused gate `scratch/ps1-perf-iterate/visitor3-low-f128-resident-evict125126-alias123-v245-focused/20260509-161912-520876/summary.json`. | Failed/no promotion. Timing stayed flat (`scene_vb=1414`, active loop `1088/1035`, overrun `53`), but visible CD pressure regressed: `blocking_vb 95 -> 98` and `loop_read_vb 102 -> 105`; loop reads and due misses stayed `17` and `16`, hidden refill stayed `0`. | Do not evict low frames `125/126` to make frame `128` resident. The setup-resident frame `128` win is canceled by the new single-read pressure for `125/126`; frame `128` residency only looks viable if the segment grows, if a real payload reduction fits all of `124..128`, or if generated deadline scheduling can own the `174..186` spill without visible blocking debt. |
| 2026-05-09 | `visitor3-low-third-segment99-142-v246` | Log-only; source and tooling restored. | Probe a more aggressive VISITOR3-low-only residency path after layout-only frame `128` attempts failed. The accepted low trace still has five tight active reads for frames `113..117` at sectors `99..142`; the hypothesis was that a third reusable setup segment for `99..142` could remove that precursor cluster while leaving the accepted `150..174` and `281..305` segments intact. | Temporarily added a third reusable low setup segment, extended runtime segment lookup/copy/reset state, and taught the read-plan parser to recognize `SEGMENT3`. A first `--case-local-cd` run was intentionally discarded as invalid because it rebuilt only the case-local CD image; the full-rebuild gate then exercised the modified PS-EXE. Artifacts: invalid no-op run `scratch/ps1-perf-iterate/visitor3-low-third-segment99-142-v246-focused/20260509-162524-555296/summary.json`; valid full-rebuild run `scratch/ps1-perf-iterate/visitor3-low-third-segment99-142-v246-focused-rebuild/20260509-162716-565837/summary.json`. | Failed/no promotion. The extra segment pushed the runtime into no-prefetch memory shape: `prefetch.policy stage1_window -> none`, `prefetch` buffer `230562 -> 0`, `loop_reads 17 -> 143`, `due_misses 16 -> 143`, `blocking_vb 95 -> 531`, active loop `1088/1035 -> 1147/1031`, and overrun `53 -> 116`; setup grew `326 -> 350` VBlanks and `577012 -> 665076` bytes. | Do not add another independent VISITOR3 low segment buffer. The memory/policy interaction is worse than the read savings; future `113..117` work must either fit inside existing resident memory, use a smaller/precomposed payload format, or reuse an existing cache without disabling stage1 prefetch. Also avoid `--case-local-cd` for source-only probes because it can produce a misleading no-op read-plan/source mismatch. |
| 2026-05-09 | `visitor3-low-deferred-segment99-142-v247` | Log-only; source and tooling restored. | Retry the `99..142` precursor-cluster residency idea without the v246 memory-ordering failure. The hypothesis was that deferring the optional third segment until after clean-rect capture would keep the existing stage1/window buffers alive and either remove the `113..117` reads or degrade safely to the accepted baseline. | Temporarily added a separate optional `99..142` VISITOR3-low segment allocation/read after `fgBackdropSaveCleanBgRectsWithPressureFallback()`, left the accepted `150..174` and `281..305` setup segments unchanged, and taught the read-plan parser to report `SEGMENT3`. The full-rebuild focused gate exercised the modified PS-EXE. Artifact: `scratch/ps1-perf-iterate/visitor3-low-deferred-segment99-142-v247-focused/20260509-163251-596903/summary.json`. | Failed/no promotion. The deferred path avoided v246's prefetch collapse (`policy=stage1_window`, prefetch buffer `230562`, `loop_reads=17`, `blocking_vb=95`), but the optional allocation failed (`alloc_fail=1`, `alloc_fail_bytes=88064`) and the runtime fell back to exact-flat accepted behavior: `scene_vb=1414`, active loop `1088/1035`, overrun `53`, and hidden refill `0`. | Do not pursue another extra-buffer `99..142` segment. The safe degradation is useful evidence, but there is no free heap for this third resident island. Future `113..117` work needs pack-side payload reduction, repacking into already-owned setup memory, or a codec/precompose path that shrinks the bytes rather than adding another cache. |
| 2026-05-09 | `visitor3-low-noop114117-v248` | `ps1: compact visitor3 low precursor noops` | Convert the remaining low precursor byte-pressure signal after the v242/v247 failures without adding source code or another resident buffer. The binary question was whether frames `114..117`, whose compact-motion/no-op behavior had already shown no visual timing contribution through the current v4 compact residual path, could be reduced to two-byte compact no-op residuals while preserving cadence. | Repacked only `VIST3LOW.FG2` in place: frames `114..117` kept their original offsets but their data sizes became `2` bytes, saving `70258` active payload bytes while preserving the `1555450` byte pack footprint, LBA `23232`, sound offset `626575`, and the `215040` byte PS-EXE bucket. The source-level marker-skip plus motion-payload probe first passed VISITOR3 but failed broad controls due to hot-symbol phase movement, so the promoted shape is pack-only. Artifacts: transform `scratch/visitor3-low-noop114117-v248b/apply-low-noop114117.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-noop114117-v248b-focused/20260509-165134-705923/summary.json`; broad gate `scratch/ps1-perf-iterate/visitor3-low-noop114117-v248b-broad/20260509-165251-713811/summary.json`; rejected source guard `scratch/ps1-perf-iterate/visitor3-low-motion114117-marker-skip-v248-broad/20260509-164147-647512/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1414 -> 1412`, active loop `1088/1035 -> 1086/1035`, overrun `53 -> 51`, blocking `95 -> 93`, and loop-read VBlanks `102 -> 100`; loop reads stay `17`, due misses stay `16`, and hidden refill stays `0`. VISITOR3 high plus WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high all passed the broad no-regression gate. | Keep as the new VISITOR3 low row and baseline. Public rollup moves to `+0.3558%` over target / `99.6529%` target speed; raw signed rollup moves to `-0.4126%` / `100.4367%`. Methodology total is about `17.04` public over-target points removed plus `12.55` target-speed points gained. Next VISITOR3 low target is now the `128..138` tail/read-cluster or a visual-owned custom precompose path, not another extra resident segment. |
| 2026-05-09 | `visitor3-low-noop113-v249` | `ps1: compact visitor3 low frame113 noop` | Take the v248 pack-only no-op residual lane one frame earlier after the broad no-regression gate proved frame `114..117` could collapse without a source change. The binary question was whether the large cleanup-only entry `113` could also become a two-byte compact empty-cleanup marker without visible timing or hidden-refill debt. | Repacked only `VIST3LOW.FG2` in place: entry `113` keeps its offset `203179`, geometry, and cadence but its data size becomes `2`, saving `17132` active payload bytes (`17134 -> 2`) while preserving the `1555450` byte pack footprint, LBA `23232`, sound offset `626575`, and the `215040` byte PS-EXE bucket. Artifacts: transform summary `scratch/visitor3-low-noop113-v249/summary.json`; focused gate `scratch/ps1-perf-iterate/visitor3-low-noop113-v249-focused/20260509-175739-1072541/summary.json`; broad no-regression gate `scratch/ps1-perf-iterate/visitor3-low-noop113-v249-broad-norequire/20260509-180805-1135277/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1412 -> 1401`, active loop `1086/1035 -> 1075/1039`, overrun `51 -> 36`, blocking `93 -> 69`, loop reads `17 -> 16`, loop-read VBlanks `100 -> 83`, and due misses `16 -> 12`; hidden refill remains `0`. VISITOR3 high plus WALKSTUF1 high/low, BUILDING2 high/low, JOHNNY1 high, ACTIVITY9 low, and FISHING1 high all passed the broad no-regression gate. | Keep as the new VISITOR3 low row and baseline. Public rollup moves to `+0.3459%` over target / `99.6621%` target speed; raw signed rollup moves to `-0.4226%` / `100.4459%`. Methodology total is about `17.05` public over-target points removed plus `12.56` target-speed points gained. Next top under-99 row is now WALKSTUF1 high; VISITOR3 remains the custom data-shape/precompose lane rather than another scalar residency/no-op pass. |
| 2026-05-09 | `walkstuf1-high-primecap152160-v250` | Log-only; source restored. | Retest the high-tide WALKSTUF1 setup-prime ceiling after the v249 VISITOR3 baseline shifted the top outlier back to WALKSTUF1 high. The binary question was whether the earlier `144 KiB` high cap still represented the knee, or whether `152 KiB`/`160 KiB` could now remove another visible read without hidden refill debt. | Temporarily raised `FG_WALKSTUF1_HIGH_SETUP_PRIME_MAX_RESIDENT_BYTES` from the promoted `144 KiB` to `160 KiB`, ran a mislabeled local-signal probe, then reran canonical `walkstuf1-high-canary`; repeated the canonical gate at `152 KiB`. Source was restored after both canonical gates failed. Artifacts: local-signal `scratch/ps1-perf-iterate/walkstuf1-high-primecap160-v250-focused/20260509-183504-1288280/summary.json`; canonical `160 KiB` `scratch/ps1-perf-iterate/walkstuf1-high-primecap160-v250-focused-canonical/20260509-183621-1295958/summary.json`; canonical `152 KiB` `scratch/ps1-perf-iterate/walkstuf1-high-primecap152-v250-focused/20260509-183743-1304130/summary.json`. | Failed/no promotion. Both canonical caps produced the same trade: high improved `scene_vb 1783 -> 1779`, active loop `1495/1427 -> 1490/1424`, overrun `68 -> 66`, visible blocking `95 -> 87`, loop reads `70 -> 69`, and due misses `14 -> 13`, but hidden prefetch overrun regressed `26 -> 33`, exceeding the gate. | Keep the high setup-prime cap at `144 KiB`. Larger residency still converts visible blocking into hidden refill and target drift, so WALKSTUF1's remaining gap needs scheduler-owned/costed setup, selective payload reduction, or pack-side compression rather than another scalar cap raise. |
| 2026-05-09 | `walkstuf1-motion-x-target-hull-v251` | Log-only; source and packs restored. | Take a larger WALKSTUF1 data-shape swing after scalar prime caps stalled: reuse the existing compact motion-marker decoder for selected x-only target-hull frames, accepting a custom scene/format path if it could remove enough payload/read pressure to close the remaining WALKSTUF1 gap. | Repacked both `WALKSTUF1.FG2` and `WALK1LOW.FG2` with target-hull horizontal motion-marker payloads for frames `89`, `109`, `167`, and `201`, saving `8258` active bytes per tide while keeping each pack footprint `1535263` and sound offset `923863`. Temporarily wired `GR_FGP3_MOTION_MARKER` dispatch into `grCompositePacked4CompactTemporalResidualToBackground()` so version-4 compact packs would execute the marker decoder, then ran WALKSTUF1 high/low plus VISITOR3 high/low controls. Artifacts: transform `scratch/walkstuf1-motion-x-v250/apply-target-hull-89-109-167-201.json`; focused gate `scratch/ps1-perf-iterate/walkstuf1-motion-x-v250-focused/20260509-185345-1394511/summary.json`. | Failed/no promotion. WALKSTUF1 high showed a real visible signal (`scene_vb 1783 -> 1779`, active loop `1495/1427 -> 1491/1425`, overrun `68 -> 66`, blocking `95 -> 87`) but regressed hidden refill `26 -> 27`, loop reads `70 -> 72`, PS-EXE bucket `215040 -> 217088`, and pack LBA by one sector. WALKSTUF1 low traded the wrong way: active loop `1489/1427 -> 1488/1428` and overrun `62 -> 60`, but blocking regressed `86 -> 103`. VISITOR3 controls confirmed the shared compact dispatch is not safe: high regressed `1406 -> 1420` scene VBlanks and low regressed `1401 -> 1414`. | Do not enable the current compact motion-marker decoder globally and do not retry this all-four-frame WALKSTUF1 hull set. The byte savings are real, but the existing per-pixel move decoder is too expensive and its shared dispatch perturbs VISITOR3. Future WALKSTUF1 attempts should be pack-only/no-source where possible, or use a WALKSTUF1-only faster opcode/precomposed strip path that keeps the PS-EXE bucket fixed and avoids VISITOR3 marker semantics. |
| 2026-05-09 | `walkstuf1-setupseg287329-v252` | Log-only; source restored. | Try the strongest direct residency swing from the current read-candidate matrix: a persistent high-tide setup segment for sectors `287..303` and a low-tide setup segment for sectors `329..345`, both targeting the top current WALKSTUF1 uncovered read clusters. | Temporarily added WALKSTUF1 setup-segment handling through the existing reusable segment buffer and taught the read-plan model to account for the segments. A split high-only rerun reproduced the high shape but was killed before complete low metrics, so the canonical decision is the complete high+low+VISITOR3 focused gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-setupseg-v252-focused/20260509-190611-1465470/summary.json`. | Failed/no promotion. High-tide residency removed visible loop work but paid too much setup: `scene_vb 1783 -> 1787`, active loop `1495/1427 -> 1486/1428`, overrun `68 -> 58`, blocking `95 -> 92`, hidden refill `26 -> 24`, and loop reads `70 -> 64`. Low-tide residency was outright negative: `scene_vb 1780 -> 1798`, active loop `1489/1427 -> 1494/1424`, overrun `62 -> 70`, blocking `86 -> 92`, hidden refill `27 -> 30`, and loop reads `69 -> 65`. VISITOR3 controls stayed timing-flat. | Do not add WALKSTUF1 scene-start setup segments for these ranges. The high segment proves sectors `287..303` are valuable, but reading them during preprocessing violates the public scene metric; the low segment has the wrong phase entirely. Future WALKSTUF1 work should load any large segment from active-loop spare cycles or shrink/repack payloads so startup setup does not grow. |
| 2026-05-09 | `walkstuf1-high-runtime-seg287-v253` | Log-only; source restored. | Retry the high `287..303` residency win without scene-start preprocessing cost. The experiment allocated the same reusable segment metadata at setup but deferred the actual 16-sector read until WALKSTUF1 high had at least `8` held VBlanks, no urgent next-window read, and frame index before the target cluster. | Temporarily added a WALKSTUF1-high-only runtime segment prefetch hook in the held-frame scheduler. The hook used the existing retained setup-segment lookup/copy path once the lazy read completed, leaving low tide and VISITOR3 controls out of the custom path. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-runtime-seg287-v252-focused/20260509-191453-1517703/summary.json`. | Failed/no promotion. The deferred read avoided setup growth and improved high scene/loop modestly (`scene_vb 1783 -> 1779`, active loop `1495/1427 -> 1491/1424`, overrun `68 -> 67`, blocking `95 -> 93`, loop reads `70 -> 69`), but hidden refill regressed `26 -> 28` and due misses rose `14 -> 15`. Low tide and VISITOR3 controls stayed flat. | Do not retry `287..303` as one lazy 16-sector runtime read with an `8` VBlank threshold. The concept is valid but the chunk is too large for the available slack. Next candidates should try a smaller/later runtime segment (`201..213`, `206..222`, or `178..194`) or pack-side data reduction around the same cluster rather than a full 32 KiB opportunistic read. |
| 2026-05-09 | `walkstuf1-high-runtime-seg201-v254` | Log-only; source restored. | Test the smaller/lower-risk version of the lazy-runtime segment idea after v253 proved a 16-sector chunk was too expensive. This probe targeted high sectors `201..213`, a 12-sector cluster that read-plan marked as lower visible-risk and current-group-compatible, while leaving low tide and VISITOR3 out of the custom path. | Temporarily added a WALKSTUF1-high-only retained segment for `201..213`, allocated at setup but read only during active-loop held slack before frame `100` and with at least `8` held VBlanks. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-runtime-seg201-v254-focused/20260509-192238-1562579/summary.json`. | Failed/no promotion. The smaller lazy read improved visible blocking but still converted pressure into hidden refill: high `scene_vb 1783 -> 1780`, active loop `1495/1427 -> 1492/1422`, blocking `95 -> 86`, due misses `14 -> 13`, but overrun regressed `68 -> 70`, hidden refill regressed `26 -> 33`, and loop reads stayed `70`. Low tide and VISITOR3 controls stayed flat. | Stop trying one-shot lazy retained segments with the current scheduler. Both large and small variants improve visible blocking while worsening hidden refill/target cadence, so the remaining WALKSTUF1 gap needs pack-side placement or payload reduction, not another opportunistic sidecar read. A zero-noop scan also found no true visual-empty WALKSTUF1 compact residual entries, so the next pack swing should be physical placement or a purpose-built strip/precompose format rather than VISITOR3-style noops. |
| 2026-05-09 | `walkstuf1-high-f37-resident-evict33-v255` | Log-only; high pack restored. | Test a pack-only physical-placement swing after one-shot lazy segments failed: make the first uncovered high-tide frame `37` resident by moving it into a paid setup slot, while evicting a nearby resident frame to tail slack. The binary question was whether removing the earliest `72..77` read pressure could outweigh one new tail read without touching source code, low tide, or VISITOR3. | Temporarily rewrote only `WALKSTUF1.FG2`: entry `37` / source frame `47` moved from offset `148813` sectors `72..77` into entry `33`'s resident slot at offset `132628` sectors `64..69`, and entry `33` / source frame `43` moved to aligned tail slack at offset `925696` sectors `452..456`. The pack footprint, sound offset, high pack LBA, and PS-EXE bucket stayed fixed; `WALK1LOW.FG2` and source stayed untouched. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-f37-resident-evict33-v255-focused/20260509-193314-1623248/summary.json`. | Failed/no promotion. The tail-evicted resident frame cost more than the resident frame `37` win: high regressed `scene_vb 1783 -> 1797`, active loop `1495/1427 -> 1509/1422`, overrun `68 -> 87`, blocking `95 -> 111`, hidden refill `26 -> 40`, and loop reads `70 -> 71`. Low stayed flat at `1489/1427`, blocking `86`, hidden `27`; VISITOR3 high/low stayed flat at `1075/1037` and `1075/1039`. | Do not evict resident WALKSTUF1 frames to tail slack to buy earlier residency. Tail reads create a worse early seek/refill shape than the original nonresident frame, so future physical placement must avoid new tail dependencies: try same-region/similar-size swaps, offset-neutral compaction, payload reduction, or a purpose-built strip/precompose format instead of resident-slot eviction. |
| 2026-05-09 | `walkstuf1-compact-window-policy-v256` | Log-only; source restored. | Investigate a stale scheduler policy: WALKSTUF1 still had custom high/low stream-window sizes (`54 KiB` / `40 KiB`), but the policy was gated to `Indexed8TemporalResidual` even though the current packs are FGP3/v4 PAL4 compact residual. The binary question was whether activating that existing policy for the actual compact format would reduce the remaining read pressure without source growth. | Temporarily changed the WALKSTUF1 runtime window policy discriminator from `kFgPilotPackFormatIndexed8TemporalResidual` to `kFgPilotPackFormatPal4CompactTemporalResidual` and ran WALKSTUF1 high/low plus VISITOR3 high/low controls with a full source rebuild. Pack LBAs and the PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/walkstuf1-compact-window-policy-v256-focused/20260509-194129-1670217/summary.json`. | Failed/no promotion. The old wide-window policy reduced loop reads but overread too much per miss: high went `loop_reads 70 -> 18` but regressed `scene_vb 1783 -> 1839`, active loop `1495/1427 -> 1551/1439`, overrun `68 -> 112`, blocking `95 -> 121`, hidden refill `26 -> 54`, and due misses `14 -> 5`; low went `loop_reads 69 -> 27` but regressed `scene_vb 1780 -> 1829`, active loop `1489/1427 -> 1536/1436`, overrun `62 -> 100`, blocking `86 -> 105`, hidden refill `27 -> 57`, and due misses `12 -> 5`. VISITOR3 controls stayed flat. | Keep the stale format gate as-is until a narrower generated policy exists. This proves WALKSTUF1 cannot use coarse 40-54 KiB active windows under current timing: the fewer-read shape is real, but hidden refill and public cadence get much worse. Future scheduler work should test smaller compact-format windows, per-cluster append groups, or adaptive slack thresholds rather than reviving the old broad window wholesale. |
| 2026-05-09 | `walkstuf1-compact-window24-v257` | Log-only; source restored. | Narrow the v256 finding instead of abandoning compact-format windows outright. The candidate applied a `24 KiB` WALKSTUF1 compact-format stream window to both tides, aiming for a middle ground: fewer reads than the default `16 KiB`, but much less overread than the rejected `40..54 KiB` legacy policy. | Temporarily set the WALKSTUF1 runtime window policy to `24 KiB` high and low with `kFgPilotPackFormatPal4CompactTemporalResidual`, then ran WALKSTUF1 high/low plus VISITOR3 high/low controls with a full source rebuild. Pack LBAs and the PS-EXE bucket stayed fixed. Artifact: `scratch/ps1-perf-iterate/walkstuf1-compact-window24-v257-focused/20260509-194631-1700327/summary.json`. | Failed/no promotion. The narrower window still overread too much: high reads fell `70 -> 47`, but `scene_vb 1783 -> 1802`, active loop `1495/1427 -> 1514/1425`, overrun `68 -> 89`, blocking `95 -> 106`, hidden refill `26 -> 57`, and due misses `14 -> 8`; low reads fell `69 -> 46`, but `scene_vb 1780 -> 1802`, active loop `1489/1427 -> 1509/1426`, overrun `62 -> 83`, blocking `86 -> 97`, hidden refill `27 -> 56`, and due misses `12 -> 7`. VISITOR3 controls stayed flat. | Close global WALKSTUF1 compact-window activation above the default size for now. Even `24 KiB` converts visible read count reduction into hidden refill/cadence debt. The next scheduler lane should keep the default window and use targeted append groups or per-cluster rules, not a global larger window. |
| 2026-05-09 | `walkstuf1-readgroup201-v258` | Log-only; source restored. | Retry scheduler ownership at the accepted default `16 KiB` window after global larger windows failed. The candidate targeted only the lower-risk `201..213` append cluster that both tides marked fireable/current-group-compatible, with better slack than the tight `84..100`, `287..303`, and `329..345` clusters. | Temporarily added a WALKSTUF1 read-group table with one group `{201,213,0}` and selected it only for `walkstuf1` when the stream window stayed at `FG_PREFETCH_DEFAULT_WINDOW_BYTES`; VISITOR3 controls were included because the source insertion shifted hot symbols by `+20` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-readgroup201-v258-focused/20260509-195232-1735772/summary.json`. | Failed/no promotion. High showed the desired visible signal but missed strict hidden-refill: `scene_vb 1783 -> 1780`, active loop `1495/1427 -> 1492/1426`, overrun `68 -> 66`, blocking `95 -> 89`, loop reads `70 -> 69`, and due misses `14 -> 13`, but hidden refill regressed `26 -> 27`. Low improved timing but traded into visible/hidden CD pressure: `scene_vb 1780 -> 1777`, active loop `1489/1427 -> 1486/1427`, overrun `62 -> 59`, loop reads `69 -> 67`, but blocking regressed `86 -> 89`, hidden refill `27 -> 28`, and due misses `12 -> 13`. VISITOR3 metrics stayed flat. | Do not promote the all-tide zero-threshold `201..213` group. The high-tide shape is close enough to justify a high-only or stricter-min-slack follow-up, but low tide should not inherit this group. If the next variant still adds hidden refill, close `201..213` and move to a different cluster or a non-source pack/data-shape lane. |
| 2026-05-09 | `walkstuf1-high-readgroup201-slack6-v259` | Log-only; source restored. | Follow the v258 learning with the binary high-only/stricter-slack variant. Low tide should not inherit `201..213`, and high needed to keep the visible win while avoiding the one-VBlank hidden-refill regression. | Temporarily added a high-tide-only WALKSTUF1 read group `{201,213,6}`, selected only for `walkstuf1` high at the default `16 KiB` stream window. Low tide and VISITOR3 controls were included; pack LBAs and the PS-EXE bucket stayed fixed, though source insertion shifted hot symbols by `+48` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup201-slack6-v259-focused/20260509-195754-1768202/summary.json`. | Failed/no promotion. High was identical to v258 despite the threshold: `scene_vb 1783 -> 1780`, active loop `1495/1427 -> 1492/1426`, overrun `68 -> 66`, blocking `95 -> 89`, loop reads `70 -> 69`, and due misses `14 -> 13`, but hidden refill still regressed `26 -> 27`. Low tide stayed exact baseline at `1780`, `1489/1427`, blocking `86`, hidden `27`, reads `69`, and due `12`; VISITOR3 high/low stayed flat. | `minSlack=6` is not enough; the group still fires in the same high-tide phase. One more high-only threshold probe is justified only if it can skip the offending append. If a higher threshold either no-ops or still adds hidden refill, close `201..213` and move to a different cluster or pack-side lane. |
| 2026-05-09 | `walkstuf1-high-readgroup201-slack12-v260` | Log-only; source restored. | Close the threshold question from v259 by doubling the high-only gate to `12` held VBlanks before the `201..213` append group can fire. The binary question was whether a stricter threshold could keep the visible read win while skipping the phase that adds hidden refill. | Temporarily added a high-tide-only WALKSTUF1 read group `{201,213,12}`, selected only for `walkstuf1` high at the default `16 KiB` stream window. Low tide and VISITOR3 controls were included; pack LBAs and the PS-EXE bucket stayed fixed, though source insertion shifted hot symbols by `+48` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup201-slack12-v260-focused/20260509-200308-1799269/summary.json`. | Failed/no promotion. High repeated the same unsafe visible trade as v258/v259: `scene_vb 1783 -> 1780`, active loop `1495/1427 -> 1492/1426`, overrun `68 -> 66`, blocking `95 -> 89`, loop reads `70 -> 69`, and due misses `14 -> 13`, but hidden refill still regressed `26 -> 27`; strict layout also rejected the `+48` byte hot-symbol shift. Low tide stayed exact baseline at `1780`, `1489/1427`, blocking `86`, hidden `27`, reads `69`, and due `12`; VISITOR3 high/low stayed flat. | Close the `201..213` source read-group lane. Threshold `12` still fires in the hidden-regressing phase, while lower thresholds already matched the same result; further scalar min-slack variants are unlikely to promote. Future WALKSTUF1 scheduler work needs a different cluster, a source-size-neutral generated policy, or a pack/data-shape change rather than another hand `201..213` group. |
| 2026-05-09 | `walkstuf1-high-readgroup287-v261` | Log-only; source restored. | Move from the closed `201..213` lane to the highest-upside high-tide current-group-compatible cluster. The candidate targeted `287..303`, which the read-candidate matrix estimated at four saved reads, to see whether a larger group could produce a material WALKSTUF1 high win despite the tighter visible gaps. | Temporarily added a high-tide-only WALKSTUF1 read group `{287,303,0}`, selected only for `walkstuf1` high at the default `16 KiB` stream window. Low tide and VISITOR3 controls were included; pack LBAs and the PS-EXE bucket stayed fixed, though source insertion shifted hot symbols by `+48` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup287-v261-focused/20260509-200939-1837534/summary.json`. | Failed/no promotion. The group reduced high loop reads `70 -> 65`, but it did not move public timing and converted too much work into hidden refill: `scene_vb 1783 -> 1783`, active loop `1495/1427 -> 1495/1427`, overrun `68 -> 68`, blocking `95 -> 94`, due misses stayed `14`, and hidden refill regressed `26 -> 30`; strict layout also rejected the `+48` byte hot-symbol shift. Low tide and VISITOR3 high/low stayed timing-flat. | Close the direct `287..303` hand read-group lane. The saved-read count is real, but the chunk is too large or too tightly phased for the default-window append scheduler; future work around this region needs smaller phase-aware chunks, pack/data-shape reduction, or a generated policy that can account for hidden refill rather than another single hand group. |
| 2026-05-09 | `walkstuf1-high-readgroup329-v262` | Log-only; source restored. | Probe the next high-tide WALKSTUF1 current-window cluster after `287..303` failed. The `329..345` group had smaller estimated upside but appeared in both high and low matrices; this test kept it high-only to avoid all-tide contamination. | Temporarily added a high-tide-only WALKSTUF1 read group `{329,345,0}`, selected only for `walkstuf1` high at the default `16 KiB` stream window. Low tide and VISITOR3 controls were included; pack LBAs and the PS-EXE bucket stayed fixed, though source insertion shifted hot symbols by `+48` bytes. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup329-v262-focused/20260509-201538-1873290/summary.json`. | Failed/no promotion. This cluster shaped CD better than `287..303` but lost public timing: high regressed `scene_vb 1783 -> 1785`, active loop `1495/1427 -> 1497/1428`, and overrun `68 -> 69`, while blocking improved `95 -> 91`, hidden refill improved `26 -> 25`, loop reads dropped `70 -> 67`, and due misses improved `14 -> 13`; strict layout also rejected the `+48` byte hot-symbol shift. Low tide and VISITOR3 high/low stayed timing-flat. | Do not promote direct `329..345` hand grouping. It proves the cluster has useful CD shape, but the public cadence cost is higher than the CD win. Combined with v261 and the closed `201..213` lane, hand read-groups are now a weak path for WALKSTUF1; favor pack/data-shape or generated phase-aware scheduling before more source table probes. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-v263` | Log-only; high pack restored. | Try a no-source pack/data-shape swing after hand read-groups stalled. The candidate merged one-pixel gaps between adjacent cleanup spans in `WALKSTUF1.FG2`, reducing restore/dirty span count and payload metadata while keeping draw payloads byte-identical and padding the pack back to the same file size/LBA. | Built a high-pack-only transform with scratch tooling: merged `2,842` cleanup span gaps across `196` entries, reducing active payload `919,471 -> 913,053` bytes and cleanup spans `45,995 -> 43,153`, with `2,842` extra restored background pixels. The padded pack size stayed `1,535,263` bytes and LBA stayed fixed; low tide and VISITOR3 controls were included. Artifacts: transform summary `scratch/walkstuf1-cleanup-gap-merge-v263/summary.json`; focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-v263-focused/20260509-202658-1938114/summary.json`. | Failed/no promotion. The transform improved high scene and loop timing but worsened the public overrun target: `scene_vb 1783 -> 1781`, active loop `1495/1427 -> 1493/1420`, blocking `95 -> 88`, loop reads `70 -> 69`, and due misses `14 -> 12`, but overrun regressed `68 -> 73` and hidden refill regressed `26 -> 29`. Low tide and VISITOR3 high/low stayed timing-flat. | Do not promote blanket one-pixel cleanup-gap merging. The span-count and byte savings are real, but extra restores shorten the target/cadence enough to fail the public speed gate. Future cleanup-shape work must be selective by frame/phase or tied to visual-owned precompose metadata, not applied broadly across the pack. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-f145-155-v264` | Log-only; high pack restored. | Follow v263 with a narrower hot-cluster cleanup-shape swing. The candidate merged one-pixel cleanup gaps only for high-pack entries `145..155`, the sector `287..303` region that repeatedly shows the strongest WALKSTUF1 high read pressure, to preserve the public timing win while limiting global cadence drift. | Extended the scratch cleanup-gap transform with a frame-range filter and rewrote only `WALKSTUF1.FG2` entries `145..155`. The transform saved `836` active payload bytes, reduced cleanup spans `3,072 -> 2,729`, added `343` restored background pixels, kept the padded pack size at `1,535,263` bytes, and moved the sound table only inside the same fixed-size pack. Artifacts: transform summary `scratch/walkstuf1-cleanup-gap-merge-v264/summary.json`; focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-f145-155-v264-focused/20260509-203426-1982045/summary.json`. | Failed/no promotion. This is the best cleanup-shape signal so far: high improved `scene_vb 1783 -> 1777`, active loop `1495/1427 -> 1489/1423`, overrun `68 -> 66`, blocking `95 -> 86`, loop reads `70 -> 68`, and due misses `14 -> 13`, but strict gate rejected hidden refill `26 -> 31`. Low tide and VISITOR3 high/low stayed timing-flat. | Keep pursuing selective cleanup-shape, but do not promote the full `145..155` cluster. The public timing gain is real and bigger than the direct source read-groups, but the hidden-refill debt is too high. Next variants should bisect the cluster (`146..150`, `147..150`, or per-frame high-savings entries) and prefer the smallest slice that keeps blocking/read gains without increasing `prefetch.overrun_vb`. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-f147-150-v265` | Log-only; high pack restored. | Bisect the v264 hot-cluster win by keeping only entries `147..150`, the four-frame core behind the `290..302` candidate that the read-plan output ranked as fireable and high-upside. The goal was to retain the read-count/blocking reduction while cutting the v264 extra-restore footprint by more than half. | Reused the range-filtered cleanup-gap transform on high pack entries `147..150` only. The transform saved `406` active payload bytes, reduced cleanup spans `1,228 -> 1,074`, added `154` restored background pixels, and kept the padded pack size/LBA fixed. Artifacts: transform summary `scratch/walkstuf1-cleanup-gap-merge-v265/summary.json`; focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-f147-150-v265-focused/20260509-204029-2017627/summary.json`. | Failed/no promotion. This slice lost the v264 public win and still carried hidden debt: high moved only `scene_vb 1783 -> 1782` and active loop `1495 -> 1494`, while target collapsed `1427 -> 1423`, overrun regressed `68 -> 71`, blocking regressed `95 -> 97`, loop reads improved only `70 -> 69`, due misses stayed `14`, and hidden refill regressed `26 -> 31`. Low tide and VISITOR3 high/low stayed timing-flat. | Do not continue with the `147..150` core by itself. The broader `145..155` win depends on surrounding entries smoothing the stream shape; the isolated core creates cadence/restore debt without enough read relief. Next selective cleanup probes should test the full cluster minus the worst center (`145..146,151..155`) or left/right halves rather than only the central four frames. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-f145-146-151-155-v266` | Log-only; high pack restored. | Test the complementary slice after v265 showed the central `147..150` frames are unsafe alone. This candidate kept only the surrounding entries `145..146,151..155` from the v264 cluster to see whether edge smoothing produced the public timing gain without the center-core hidden-refill debt. | Reused the range-filtered cleanup-gap transform on high pack entries `145..146,151..155`. The transform saved `430` active payload bytes, reduced cleanup spans `1,844 -> 1,655`, added `189` restored background pixels, and kept the padded pack size/LBA fixed. Artifacts: transform summary `scratch/walkstuf1-cleanup-gap-merge-v266/summary.json`; focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-f145-146-151-155-v266-focused/20260509-204529-2047774/summary.json`. | Failed/no promotion. The surrounding slice recovered nearly all of v264's public timing but worsened hidden refill: high improved `scene_vb 1783 -> 1777`, active loop `1495/1427 -> 1489/1423`, overrun `68 -> 66`, blocking `95 -> 87`, loop reads `70 -> 68`, and due misses `14 -> 13`, but hidden refill regressed `26 -> 32`. Low tide and VISITOR3 high/low stayed timing-flat. | The edge slice proves the cleanup-shape win is not isolated to the center frames, but hidden refill is still the blocker. Next cleanup probes should move from contiguous frame selection to a per-frame score that limits extra restored pixels per saved byte, or combine small edge slices with a compensating hidden-prefetch policy rather than expanding this cluster further. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-inplace145-155-v267` | Log-only; high pack restored. | Split the v264/v266 failure mode by preserving every later payload offset instead of repacking the selected cleanup-gap savings. The binary question was whether the hidden-refill regression came from moving later payloads/sound data rather than from the cleanup shape itself. | Extended the scratch cleanup-gap transformer with `--preserve-offsets`, then rewrote high entries `145..155` in place. The transform kept pack size `1,535,263`, pack LBA, later payload offsets, and sound offset fixed while saving `836` active bytes, reducing cleanup spans `3,072 -> 2,729`, and adding `343` restored background pixels. Artifact: focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-inplace145-155-v267-focused/20260509-215538-2435832/summary.json`. | Failed/no promotion. Preserving offsets fixed the hidden-refill debt (`prefetch_overrun_vb 26 -> 24`) and kept the public timing win (`scene_vb 1783 -> 1780`, active loop `1495/1427 -> 1491/1425`, overrun `68 -> 66`), but visible blocking regressed `95 -> 97`, loop reads only moved `70 -> 69`, and due misses regressed `14 -> 15`. | Close the repack-offset hypothesis. The hidden-refill regression in v264/v266 was largely placement-induced, but the cleanup-gap geometry itself still increases visible blocking when offsets are preserved. Future cleanup work needs a different visual/restore ownership model, not another contiguous gap-merge slice. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-inplace-edge-v268` | Log-only; high pack restored. | Apply the same offset-preserving test to the v266 edge slice (`145..146,151..155`) to see whether removing the unsafe central frames would keep the loop win without the v267 blocking regression. | Reused the `--preserve-offsets` cleanup-gap transformer on high entries `145..146,151..155` only. The transform kept later offsets and sound offset fixed while saving `430` active bytes, reducing cleanup spans `1,844 -> 1,655`, and adding `189` restored background pixels. Artifact: focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-inplace-edge-v268-focused/20260509-215727-2446027/summary.json`. | Failed/no promotion. The edge-only in-place result reproduced the v267 timing shape: `scene_vb 1783 -> 1780`, active loop `1495/1427 -> 1491/1425`, overrun `68 -> 66`, and hidden refill `26 -> 24`, but blocking still regressed `95 -> 97`, loop reads improved only `70 -> 69`, and due misses regressed `14 -> 15`. | Close offset-preserving cleanup-gap slicing for WALKSTUF1 high. The smaller edge set does not isolate a promotable subset; the next WALKSTUF1 work should move to semantic-preserving placement, setup/scheduler ownership, or a custom pack format rather than more one-pixel cleanup-gap merges. |
| 2026-05-09 | `walkstuf1-high-setupseg283-295-v269` | Log-only; source restored. | Try a larger scheduler/data-ownership swing after cleanup shaping stalled: preload the hot high-tide sector `283..295` cluster as a reusable setup segment, like the VISITOR3 setup segments, so active-loop reads can be removed without consuming hidden slack. | Temporarily added a high-tide-only WALKSTUF1 setup segment at sectors `283..295` in `src/foreground_pilot.c`, rebuilt, and ran the focused high gate against a fresh current WALKSTUF1 high baseline (`1779`, `1490/1424`, overrun `66`, blocking `92`, hidden refill `31`, reads `69`). Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-setupseg283-295-v269-focused/20260509-220136-2470353/summary.json`. | Failed/no promotion. The setup segment did remove loop reads (`69 -> 67`) and lowered loop read VBlanks (`310 -> 303`) with hidden refill slightly better (`31 -> 30`), but setup became more expensive (`setup_vb 289 -> 297`, setup reads `13 -> 14`, setup bytes `413,236 -> 437,812`) and public timing regressed: `scene_vb 1779 -> 1789`, active loop `1490/1424 -> 1492/1426`, and blocking `92 -> 93`. | Do not promote setup-owned sector `283..295`. The read count is real, but moving that cluster to setup costs more than it saves and still worsens visible blocking. Future setup-segment attempts need a smaller/cheaper semantic-preserving resident copy or pack-side placement inside already paid setup coverage, not another new setup read. |
| 2026-05-09 | `walkstuf1-high-primecap148-v270` | Log-only; source restored. | Test the narrow cap knee between the accepted `144 KiB` high setup-prime cap and the previously rejected `152 KiB` cap. The idea was to pull entry `37`'s spill into setup coverage without paying for the larger rejected window. | Temporarily changed `FG_WALKSTUF1_HIGH_SETUP_PRIME_MAX_RESIDENT_BYTES` from `144 KiB` to `148 KiB` and ran the focused high gate against the fresh current baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-primecap148-v270-focused/20260509-220527-2492008/summary.json`. | Failed/no promotion. Setup coverage expanded from sectors `2..74` to `2..76`, and hidden refill improved strongly (`31 -> 21`) with loop reads `69 -> 68`, but visible blocking regressed `92 -> 100`, target fell `1424 -> 1422`, overrun regressed `66 -> 68`, due misses regressed `14 -> 16`, and scene time regressed `1779 -> 1780`. | Keep `144 KiB` as the high setup-prime cap. The extra entry-37 residency shifts pressure from hidden refill into visible blocking and target cadence, so WALKSTUF1 high needs pack/data-shape or phase-aware scheduling rather than any further setup-prime cap increase. |
| 2026-05-09 | `walkstuf1-high-cleanup-gap1-f145-155-v271-regate` | Log-only; high pack restored. | Re-test the prior best cleanup-shape public signal against the fresh current WALKSTUF1 high baseline after v267-v270 changed the measured baseline profile. The binary question was whether the newer `1779`, `1490/1424`, hidden-refill `31` baseline made the old `145..155` hot-cluster transform promotable. | Reapplied the non-offset-preserving one-pixel cleanup-gap transform to high entries `145..155` only, saving `836` active bytes (`919,471 -> 918,635`), reducing cleanup spans `3,072 -> 2,729`, adding `343` restored background pixels, and keeping pack size/LBA fixed. Artifact: transform summary `scratch/walkstuf1-cleanup-gap-merge-v271/summary.json`; focused gate `scratch/ps1-perf-iterate/walkstuf1-high-cleanup-gap1-f145-155-v271-regate/20260509-221333-2536975/summary.json`. | Failed/no promotion. Against the fresh baseline, the same transform is now a clear regression: `scene_vb 1779 -> 1784`, active loop `1490/1424 -> 1495/1420`, overrun `66 -> 75`, blocking `92 -> 97`, hidden refill `31 -> 37`, and loop reads `69 -> 71`; no key metric improved. The high pack was restored. | Close the cleanup-gap regate lane. The current baseline no longer tolerates the `145..155` geometry; more one-pixel gap merging is unlikely to promote without a different visual ownership model. Next WALKSTUF1 work should shift to generated phase-aware scheduling, source-size-neutral read policy, or custom pack/compression that reduces payload without adding restore pixels. |
| 2026-05-09 | `walkstuf1-high-primecap150-v272` | Log-only; source restored. | Test the remaining setup-prime cap knee after `148 KiB` improved hidden refill but worsened visible blocking. `150 KiB` is the next sector-rounded point that can cover the early entry-37 spill more completely while staying below the older rejected `152 KiB` cap. | Temporarily changed `FG_WALKSTUF1_HIGH_SETUP_PRIME_MAX_RESIDENT_BYTES` from `144 KiB` to `150 KiB` and ran the focused high gate against the fresh current baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-primecap150-v272-focused/20260509-221623-2553452/summary.json`. | Failed/no promotion. Setup coverage stayed at sectors `2..76`, matching the `148 KiB` effective sector coverage. Hidden refill improved `31 -> 21` and loop reads moved `69 -> 68`, but visible blocking regressed `92 -> 100`, target fell `1424 -> 1422`, overrun regressed `66 -> 68`, due misses regressed `14 -> 16`, and scene time regressed `1779 -> 1780`. | Close setup-prime cap increases for WALKSTUF1 high. The sector-rounded `148/150 KiB` shape proves the extra residency moves debt from hidden refill into visible blocking; keep `144 KiB` and pursue scheduler/pack-shape changes instead. |
| 2026-05-09 | `walkstuf1-high-prepare-before-window-v273` | Log-only; source restored. | Test whether WALKSTUF1 high's visible blocking comes from speculative window reads stealing exact prepare slots. The candidate gave high-tide WALKSTUF1 visual preparation priority over a pending window refill when a staged frame was exactly at the normal `4` VBlank prepare threshold. | Temporarily changed the held-loop scheduler in `foreground_pilot.c` so high-tide WALKSTUF1 prepares the staged next frame before calling `fgRuntimeTryPrefetchWindow()` in the staged/prepared branch, then ran the focused high gate against the fresh current baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-prepare-before-window-v273-focused/20260509-221848-2567419/summary.json`. | Failed/no promotion. The scheduler change confirmed CD coverage is more important than early visual prepare here: `scene_vb 1779 -> 1796`, active loop `1490/1424 -> 1507/1414`, overrun `66 -> 93`, blocking `92 -> 118`, hidden refill `31 -> 43`, loop reads `69 -> 74`, and due misses `14 -> 15`. | Close prepare-before-window for WALKSTUF1 high. The existing scheduler's CD reservation is protecting cadence; future scheduler work needs finer phase-aware read selection, not moving visual prepare ahead of window refill. |
| 2026-05-09 | `walkstuf1-high-motion-f109-v274-v276` | Log-only; source and high pack restored. | Make a larger semantic pack-format swing for the current largest outlier. Frame `109` had the cleanest WALKSTUF1 high target-hull motion-copy dry-run: one high-tide payload could shrink `4365 -> 1226` bytes (`3139` saved) with only `549` cleanup pixels and `89` residual pixels. The binary question was whether a scene-gated compact motion marker could convert that byte reduction into public timing without repeating the earlier global VISITOR3 marker-dispatch failure. | v274 rewrote only `WALKSTUF1.FG2` frame `109` as a sparse-in-place motion-copy payload and added a high-tide `walkstuf1` runtime gate for the compact motion decoder; v275 tried a faster row-pointer/memmove C helper for the motion rows; v276 restored the smaller helper and tried a non-sparse high-only repack so later payloads could move instead of leaving CD sectors unchanged. Artifacts: transform `scratch/walkstuf1-motion-x-v274/apply-high-f109.json`; sparse gate `scratch/ps1-perf-iterate/walkstuf1-high-motion-f109-v274-focused/20260509-222625-2609663/summary.json`; fast-copy gate `scratch/ps1-perf-iterate/walkstuf1-high-motion-f109-fastcopy-v275-focused/20260509-222854-2623925/summary.json`; layout-moving gate `scratch/ps1-perf-iterate/walkstuf1-high-motion-f109-repack-v276-focused/20260509-223140-2640107/summary.json`. | Failed/no promotion. v274 reduced active payload `919,471 -> 916,332` but left all key timing and CD counters exact-flat: `scene_vb=1779`, active loop `1490/1424`, overrun `66`, blocking `92`, hidden refill `31`, and loop reads `69`. v275 stayed timing-flat but crossed the PS-EXE bucket `217088 -> 219136` and shifted pack LBA `24745 -> 24746`. v276 improved hidden refill `31 -> 26` but regressed the public loop: `scene_vb 1779 -> 1784`, active loop `1490/1424 -> 1495/1427`, overrun `66 -> 68`, and loop reads `69 -> 73`. | Close single-frame WALKSTUF1 motion-copy under the current opcode/helper shape. Sparse-in-place does not change CD sectors enough to matter, fast row-copy costs too much code, and layout-moving motion shifts later reads into a worse cadence. The next white-whale work should either remove the runtime motion helper entirely with precomputed/precomposed pixels, use generated scheduler sidecars to own the changed cadence, or attack multiple frames with a cost model that includes both copied-pixel CPU and sector phase. |
| 2026-05-09 | `walkstuf1-high-group201-213-v277` | `ps1: group walkstuf1 high sector 201 read` | Promote the low-risk visible-cost WALKSTUF1 high read-plan candidate after cleanup-shape, setup-prime, and single-frame motion-copy lanes stalled. The candidate covers sectors `201..213` with a retained stream read group and should reduce active-loop CD pressure without setup-prime debt or pack layout movement. | Added a high-tide-only `walkstuf1` stream read group `{201, 213, 0}` in `foreground_pilot.c`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup201-213-v277-focused/20260509-224226-2699987/summary.json`; same-branch broad baseline: `scratch/ps1-perf-iterate/walkstuf1-readgroup201-213-v277-broad-baseline/20260509-224453-2714163/summary.json`; broad gate: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup201-213-v277-broad/20260509-225632-2784006/summary.json`. | Promoted. WALKSTUF1 high improves `scene_vb 1779 -> 1777`, active loop `1490/1424 -> 1488/1426`, overrun `66 -> 62`, hidden refill `31 -> 27`, loop reads `69 -> 68`, and loop-read VBlanks `310 -> 309`; blocking remains `92`, due misses remain `14`, pack LBA remains `24745`, pack sectors remain `750`, and the PS-EXE bucket remains `217088`. WALKSTUF1 low, VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed exact-flat in the same-branch broad run. | Keep as the new WALKSTUF1 high row and baseline. Public rollup moves to `+0.3426%` over target / `99.6651%` target speed; raw signed rollup moves to `-0.4259%` / `100.4489%`. Methodology total is about `17.06` public over-target points removed plus `12.57` target-speed points gained. Next top under-99 focus remains the WALKSTUF1 high/low cluster, with VISITOR3 still the custom data-shape lane. |
| 2026-05-09 | `walkstuf1-both-group201-213-v278` | Log-only; source restored. | Test the symmetric low-tide version of the v277 high read group after the current low read-plan ranked sectors `201..213` as fireable, zero-overread, and low-visible-risk. The binary question was whether the same retained stream read group could move WALKSTUF1 low without the setup-prime or cleanup-shape debt seen in earlier misses. | Temporarily made the `{201, 213, 0}` group apply to both WALKSTUF1 tides and ran the focused low gate against the v277 broad summary. Artifact: `scratch/ps1-perf-iterate/walkstuf1-both-readgroup201-213-v278-focused/20260509-232111-2927174/summary.json`. | Failed/no promotion. Low regressed despite lower read count: `scene_vb 1779 -> 1781`, active loop `1487/1424 -> 1489/1425`, overrun `63 -> 64`, blocking `95 -> 97`, hidden refill `25 -> 27`, while loop reads improved `67 -> 65` and due misses worsened to `17`. The group shifted visible cadence instead of buying timing. | Keep the v277 high-only read group. Do not apply `201..213` to low under the current retained stream policy; low needs generated phase ownership or a data-shape/preprocess change rather than symmetry with the high read table. |
| 2026-05-09 | `walkstuf1-high-group178-194-v279` | Log-only; source restored. | Try the next low-visible-risk/high-tide WALKSTUF1 retained-read candidate after v277. The read-plan still marked sectors `178..194` as append-start fireable, current-window-compatible, scheduler-owned, and capable of saving two active-loop reads, but prior direct-stage range tests showed this neighborhood could be phase-sensitive. | Temporarily added a second high-tide-only `walkstuf1` stream read group `{178, 194, 0}` beside the accepted `{201, 213, 0}` group, then ran the focused high gate against the v277 broad baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup178-194-v279-focused/20260509-232557-2954642/summary.json`. | Failed/no promotion. The candidate fired and reduced loop reads `68 -> 67`, but it shifted the active phase in the wrong direction: `scene_vb 1777 -> 1781`, active loop `1488/1426 -> 1492/1423`, overrun `62 -> 69`, blocking `92 -> 99`, hidden refill `27 -> 28`, and due misses `14 -> 16`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed, so this is a real cadence/read-phase failure rather than layout drift. | Do not promote or retry high `178..194` as a local retained stream table. This closes the read-table form of the same neighborhood that the direct-stage deny test already flagged as phase-negative; future WALKSTUF1 work should use generated deadline ownership, semantic-preserving resident placement inside already paid setup coverage, or a custom pack/data-shape change instead of more hand-authored groups in `178..194`. |
| 2026-05-09 | `walkstuf1-high-motion-f37-v280` | Log-only; source and high pack restored. | Attack the high-tide setup-prime edge directly: entry `37` started inside the accepted `144 KiB` setup coverage but spilled `2098` bytes past it. A custom motion-copy payload could shrink that entry by `2068` bytes, leaving only a `30` byte spill without raising the setup cap or moving the pack LBA. | Rewrote only `WALKSTUF1.FG2` frame `37` as a sparse-in-place compact motion marker using hull copy plus exact cleanup (`6885 -> 4817` bytes), and temporarily exposed the existing compact motion decoder through a high-tide-only `walkstuf1` marker dispatch in `foreground_pilot.c`. Pack size `1535263`, sound offset `923863`, and LBA stayed fixed. Transform artifact: `scratch/walkstuf1-motion-f37-v280/apply-high-f37.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-motion-f37-v280-focused/20260509-233513-3007493/summary.json`. | Failed/no promotion. The candidate reduced due misses `14 -> 13`, but runtime motion/cadence cost overwhelmed the CD placement win: `scene_vb 1777 -> 1790`, active loop `1488/1426 -> 1501/1422`, overrun `62 -> 79`, blocking `92 -> 101`, hidden refill `27 -> 38`, and loop reads stayed `68`. The source probe also shifted hot symbols by `+168` bytes though the PS-EXE bucket stayed `217088`. | Close WALKSTUF1 frame-37 runtime motion-copy under the current helper/dispatch shape. The setup-edge byte saving is real, but the runtime copy path is too expensive; retry this neighborhood only as precomputed/precomposed pixels, a no-runtime data-shape reduction, or generated scheduler ownership that can absorb the changed cadence. |
| 2026-05-09 | `walkstuf1-high-early-gap-v281-v284` | Log-only; high pack and source restored. | Explore no-runtime cleanup/data-shape surgery around the early high-tide prefix after frame `37` proved too expensive as runtime motion. The goal was to shrink enough bytes before entry `37` to use the already-paid `2..74` setup-prime coverage, without raising the setup cap or adding a new setup read. | Ran the cleanup-gap merger on high entries `0..37` from the v277 pack. Gap3 saved `2138` bytes, moved entry `37` fully into sectors `71..74`, and moved the sound offset `923863 -> 921725`; gap1 saved `1126` bytes with a smaller `923863 -> 922737` sound shift. Follow-ups tried the shifted high group `{200,212}` for the gap3 layout and a high-only `4` VBlank window-slack guard for gap1. Artifacts: gap3 focused `scratch/ps1-perf-iterate/walkstuf1-high-early-gap3-f0-37-v281-focused/20260509-233903-3030087/summary.json`; gap3 shifted group `scratch/ps1-perf-iterate/walkstuf1-high-gap3-group200-212-v282-focused/20260509-234039-3039792/summary.json`; gap1 focused `scratch/ps1-perf-iterate/walkstuf1-high-early-gap1-f0-37-v283-focused/20260509-234202-3048206/summary.json`; gap1 slack guard `scratch/ps1-perf-iterate/walkstuf1-high-gap1-slack4-v284-focused/20260509-234332-3057253/summary.json`. | Failed/no promotion, but gap1 is a useful signal. Gap3 was not acceptable: it improved blocking `92 -> 91` but regressed `scene_vb 1777 -> 1779`, active loop `1488/1426 -> 1490/1424`, overrun `62 -> 66`, hidden refill `27 -> 31`, and loop reads `68 -> 69`. Gap3 plus the shifted group worsened to `1780`, `1491/1422`, blocking `94`, hidden `30`. Gap1 nearly promoted: `scene_vb 1777 -> 1770`, active loop `1488/1426 -> 1481/1425`, overrun `62 -> 56`, blocking `92 -> 81`, but hidden refill regressed only `27 -> 28`. The slack-4 guard fixed hidden refill (`27 -> 16`) but starved the visible path (`blocking 132`, loop `1497`, due misses `25`). | Do not promote the current gap variants. Keep the gap1 result as the strongest current WALKSTUF1 clue: a no-runtime data-shape change can buy `-7` loop VBlanks and `-11` blocking VBlanks, but needs a one-VBlank hidden-refill fix that does not use a blunt slack guard. Next retries should use finer per-frame/refill ownership, a smaller gap1 subset plus compensating read placement, or a generated scheduler rule that only moves the specific hidden-overrun read rather than raising all high-tide refill slack. |

| 2026-05-10 | `walkstuf1-high-gap1-windowprefetch-slack4-v288` | `ps1: gate walkstuf1 high window prefetch` | Promote the strongest v283 cleanup-gap signal by fixing its only hidden-refill blocker without the blunt v284 slack-4 starvation. The binary question was whether the gap1 pack could keep its visible-loop win if WALKSTUF1 high skipped only speculative window prefetch attempts that had no more than the default 3 VBlanks of held slack. | Promoted the gap1 early-prefix high pack from `scratch/walkstuf1-early-gap-scan-v281/high-gap1.FG2`, kept the accepted high-tide read group `{201, 213, 0}`, and added a high-tide-only `walkstuf1` guard in `fgRuntimeTryPrefetchWindow()` that records a no-slack skip instead of running speculative window prefetch when `slackVBlanks <= FG_PREFETCH_WINDOW_MIN_SLACK_VBLANKS`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-gap1-windowprefetch-slack4-v288-focused/20260509-235622-3132109/summary.json`; broad gate: `scratch/ps1-perf-iterate/walkstuf1-gap1-windowprefetch-slack4-v288-broad/20260509-235742-3140302/summary.json`; promoted-row summary: `scratch/promoted-summaries/walkstuf1-high-gap1-windowprefetch-slack4-v288-summary.json`. | Promoted. WALKSTUF1 high improves `scene_vb 1777 -> 1766`, active loop `1488/1426 -> 1477/1431`, overrun `62 -> 46`, blocking `92 -> 90`, hidden refill `27 -> 19`, and loop-read VBlanks `309 -> 285`; loop reads rise `68 -> 69` and due misses rise `14 -> 17`, but both visible and hidden gates pass. Broad controls for WALKSTUF1 low, VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed flat. | Keep as the current WALKSTUF1 high baseline. Public rollup moves to `+0.3336%` over target / `99.6734%` target speed; raw signed rollup moves to `-0.4349%` / `100.4573%`. Methodology total is about `17.07` public over-target points removed plus `12.57` target-speed points gained. Next WALKSTUF1 work should mine smaller gap/data-shape variants and targeted generated refill reservation rather than more scalar slack or broad hand-authored read groups. |

| 2026-05-10 | `walkstuf1-low-gap1-v289` | `ps1: gap-compress walkstuf1 low prefix` | Apply the successful high-tide cleanup-gap data-shape idea to the current low-tide pack without adding runtime source. The binary question was whether merging one-pixel cleanup gaps in the early low prefix could reduce CD pressure while preserving hidden refill, pack LBA, and the accepted broad canaries. | Ran `scratch/walkstuf1-cleanup-gap-merge-v263/merge_cleanup_gaps.py` on `generated/ps1/foreground/WALK1LOW.FG2` with `--max-gap 1 --frames 0-37`, producing `scratch/walkstuf1-low-early-gap-scan-v289/low-gap1.FG2`. The transform changed `29` frames, saved `1126` active payload bytes (`919471 -> 918345`), collapsed `452` one-pixel cleanup gaps, and shifted the sound-event offset `923863 -> 922737` while keeping the padded pack size `1535263` bytes. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-gap1-v289-focused-match/20260510-003548-3358809/summary.json`; broad gate: `scratch/ps1-perf-iterate/walkstuf1-low-gap1-v289-broad/20260510-003714-3367301/summary.json`; promoted-row summary: `scratch/promoted-summaries/walkstuf1-low-gap1-v289-summary.json`. | Promoted. Against the current v288 broad baseline, WALKSTUF1 low improves `scene_vb 1779 -> 1770`, active loop `1487/1424 -> 1478/1428`, overrun `63 -> 50`, blocking `95 -> 75`, and due misses `15 -> 13`; `prefetch_overrun_vb` stays `25`, loop reads stay `69`, and the pack LBA stays `25495`. The durable public row moves from the previous matrix row `1489/1427` to `1478/1428` with public speed `96.6%`. WALKSTUF1 high, VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed flat in the broad gate. | Keep as the current WALKSTUF1 low baseline. Public rollup moves to `+0.3269%` over target / `99.6796%` target speed; raw signed rollup moves to `-0.4416%` / `100.4635%`. Methodology total is about `17.08` public over-target points removed plus `12.58` target-speed points gained. Next work should use the v289 read-plan output for WALKSTUF1 residual read grouping only if scheduler-owned, otherwise return to VISITOR3 custom data-shape and generated deadline metadata. |
| 2026-05-10 | `visitor3-high-f140-segment-copy-v291` | `ps1: move visitor3 high frame 140 into setup segment` | Convert the failed v290 scalar-prime lesson into a pack-only resident-placement swing: instead of expanding setup residency again, move the remaining high-tail frame that still causes visible CD pressure into the already paid high setup segment. | Rewrote only `generated/ps1/foreground/VISITOR3.FG2`: copied high frame `140` and tail frames `142..144` into the existing setup segment `277..293`, moved the sound table from `585343..585375` to `598121..598153`, and kept the pack size fixed at `1555450` bytes. Transform summary: `scratch/visitor3-high-f140-segment-copy-v291/apply-high-f140-segment-copy-v291.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-f140-segment-copy-v291-focused/20260510-012300-3621086/summary.json`; broad no-regression gate: `scratch/ps1-perf-iterate/visitor3-high-f140-segment-copy-v291-broad-norequire/20260510-013606-3696459/summary.json`; promoted high summary: `scratch/promoted-summaries/visitor3-high-f140-segment-copy-v291-summary.json`. | Promoted. VISITOR3 high improves `scene_vb 1407 -> 1406`, active loop `1075/1037 -> 1074/1038`, overrun `38 -> 36`, blocking `61 -> 58`, loop reads `11 -> 10`, loop-read VBlanks `61 -> 58`, and due misses `11 -> 10`; hidden refill stays `0`. Broad controls for WALKSTUF1 high/low, VISITOR3 low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed within the no-regression gate. The durable low row was also refreshed to the paired current-layout control `1079/1039`, blocking `70`, loop-read VBlanks `84`, and due misses `12`. | Keep as the current VISITOR3 high baseline. Public rollup with the honest current-layout low control is `+0.3284%` over target / `99.6783%` target speed; raw signed rollup is `-0.4401%` / `100.4621%`. Methodology total is about `17.07` public over-target points removed plus `12.58` target-speed points gained. Next focus is VISITOR3 low: scalar prime growth is closed, so chase low data-shape, precomposed/sidecar payloads, or generated scheduler metadata. |

| 2026-05-10 | `visitor3-low-alias-noop114117-v292` | `ps1: alias visitor3 low no-op payloads` | Attack VISITOR3 low's remaining precursor CD pressure without adding setup debt or changing cadence. Frames `114..117` were already semantic no-op residuals, but each still pointed at its original cold sector, while frame `113` had the same two-byte `0000` payload inside setup-prime coverage. | Rewrote only `generated/ps1/foreground/VIST3LOW.FG2`: entry table rows `114..117` now share frame `113`'s resident payload at offset `203179` / sector `99`; source frame, geometry, hold timing, pack size, sound table, and LBA all stay fixed. Transform summary: `scratch/visitor3-low-alias-noop114117-v292/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-low-alias-noop114117-v292-focused/20260510-023711-4035807/summary.json`; broad no-regression gate: `scratch/ps1-perf-iterate/visitor3-low-alias-noop114117-v292-broad-norequire/20260510-023830-4043787/summary.json`; promoted low summary: `scratch/promoted-summaries/visitor3-low-alias-noop114117-v292-summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1406 -> 1402`, active loop `1079/1039 -> 1075/1039`, overrun `40 -> 36`, blocking `70 -> 67`, loop reads `16 -> 12`, and loop-read VBlanks `84 -> 67`; due misses stay `12`, hidden refill stays `0`, and pack LBA stays `23233`. Broad controls for WALKSTUF1 high/low, VISITOR3 high, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed flat. | Keep as the current VISITOR3 low baseline. Public rollup moves to `+0.3253%` over target / `99.6811%` target speed; raw signed rollup moves to `-0.4432%` / `100.4649%`. Methodology total remains about `17.07` public over-target points removed plus `12.58` target-speed points gained. Remaining VISITOR3 work should keep favoring semantic-preserving pack/data-shape edits or generated scheduler metadata over scalar setup caps and raw hand-authored read groups. |

| 2026-05-10 | `walkstuf1-low-group208-220-v293` | Log-only; source restored. | Probe the current top under-99 row's best remaining low-visible-risk read-plan candidate. WALKSTUF1 low sectors `208..220` covered entries `110..113`, was append-start fireable, fit the current group window, estimated two saved reads, and had a long `3.17s` first gap, but the planner still marked it scheduler-only because the internal gap was tight. | Temporarily added a low-tide-only `walkstuf1` retained stream group `{208, 220, 0}` beside the accepted high-only `{201, 213, 0}` group, then ran the focused low gate against the v292 broad baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group208-220-v293-focused/20260510-031445-51875/summary.json`. Source was restored after the gate failed. | Failed/no promotion. The probe was exact-flat on every key metric: `scene_vb=1770`, active loop `1478/1428`, overrun `50`, blocking `75`, hidden refill `25`, loop reads `69`, and due misses `13`; pack LBA stayed `25495` and the PS-EXE bucket stayed `217088`. The harness failed `--require-improvement` with “no key metric improved vs baseline.” | Close raw retained-stream `208..220` for WALKSTUF1 low. The row remains top under-99, but local hand-authored read groups are not converting to timing; next WALKSTUF1 work should shift toward deeper pack data-shape changes, generated scheduler ownership, or semantic-preserving payload alias/compaction. |

| 2026-05-10 | `walkstuf1-low-windowprefetch-slack4-v294` | Log-only; source restored. | Test the high-tide v288 lesson symmetrically on low tide: skip speculative WALKSTUF1 window prefetch when held slack is only the default threshold, preserving the accepted high guard but allowing the low side to prove whether hidden refill is caused by the same no-slack speculative window attempts. | Temporarily removed the `!lowTide` guard from the WALKSTUF1 speculative-window skip in `fgRuntimeTryPrefetchWindow()`, then ran the focused low gate against the v292 broad baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-windowprefetch-slack4-v294-focused/20260510-031743-68471/summary.json`. Source was restored after the gate failed. | Failed/no promotion. Low stayed exact-flat: `scene_vb=1770`, active loop `1478/1428`, overrun `50`, blocking `75`, hidden refill `25`, loop reads `69`, due misses `13`, pack LBA `25495`, and PS-EXE bucket `217088`. The harness failed `--require-improvement`. | Close the symmetric low slack guard. The accepted high guard addresses a high-specific cadence issue; low needs a different lever, most likely pack/data-shape or generated deadline ownership rather than scalar speculative-window gating. |

| 2026-05-10 | `walkstuf1-low-gap1-tail-v295` | Log-only; pack restored. | Make a larger no-runtime data-shape swing after read groups and low slack gating stayed flat: apply the cleanup-gap merger with `max_gap=1` to WALKSTUF1 low frames `38..215`, leaving the already-promoted prefix `0..37` alone. The transform saved another `5292` active payload bytes, collapsed cleanup spans `39188 -> 36798`, added `2390` cleanup pixels, and moved the sound-event table `922737 -> 917445` while preserving the padded `1535263` byte footprint and LBA. | Generated `scratch/walkstuf1-low-gap-scan-v295/gap1-f38-215.FG2`, copied it over `generated/ps1/foreground/WALK1LOW.FG2`, and ran the focused low gate against the v292 broad baseline. Transform summary: `scratch/walkstuf1-low-gap1-tail-v295/summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-gap1-tail-v295-focused/20260510-032054-3396585/summary.json`. The original pack was restored after the gate failed. | Failed/no promotion. The byte saving shifted cadence the wrong way: low regressed `scene_vb 1770 -> 1791`, active loop `1478/1428 -> 1498/1422`, overrun `50 -> 76`, blocking `75 -> 95`, hidden refill `25 -> 37`, and loop reads only moved `69 -> 68`; due misses improved `13 -> 12` but not enough to offset the visible regressions. | Do not promote broad tail gap1 merging. The accepted low prefix gap1 remains valid, but later low frames need a narrower/cost-modeled subset or generated scheduler ownership; raw “save more bytes” compaction can harm read phase. |

| 2026-05-10 | `walkstuf1-low-gap1-f140-170-v296` | Log-only; pack restored. | Narrow the failed v295 tail compaction to the most byte-dense mid-tail subset before abandoning cleanup-gap surgery. The cleanup-gap merger with `max_gap=1` on WALKSTUF1 low frames `140..170` saved `1400` active payload bytes, collapsed cleanup spans while adding `603` cleanup pixels, and moved the sound-event table `922737 -> 921337`, with the padded pack size and LBA preserved. | Generated `scratch/walkstuf1-low-gap-subset-scan-v296/gap1-f140-170.FG2`, copied it over `generated/ps1/foreground/WALK1LOW.FG2`, and ran the focused low gate against the v292 broad baseline. Transform summary: `scratch/walkstuf1-low-gap1-f140-170-v296/summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-gap1-f140-170-v296-focused/20260510-032307-645992/summary.json`. The original pack was restored after the gate failed. | Failed/no promotion. Even the narrower mid-tail byte saving was phase-negative: low regressed `scene_vb 1770 -> 1777`, active loop `1478/1428 -> 1485/1427`, overrun `50 -> 58`, blocking `75 -> 87`, hidden refill `25 -> 30`, and loop reads stayed `69`; due misses stayed `13`. | Close mid-tail cleanup-gap compaction for WALKSTUF1 low. The accepted prefix remains the useful shape; later low cleanup-gap savings need explicit scheduler ownership or a different data representation, not more raw gap merging in frames `140..170`. |

| 2026-05-10 | `visitor3-high-f131-edge-copy-v297` | Log-only; high pack restored. | Test whether VISITOR3 high frame `131` could become mostly resident without a new codec. The existing high setup-prime window has `15,895` free bytes after frame `130`; frame `131` is `16,997` bytes, so the probe copied it unchanged to offset `313833`, leaving only a `1,102` byte spill beyond the already paid sector `1..161` setup coverage. | Rewrote only `generated/ps1/foreground/VISITOR3.FG2` entry `131` and payload bytes, with pack size, LBA, sound table, and runtime source unchanged. Transform summary: `scratch/visitor3-high-f131-edge-copy-v297/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-f131-edge-copy-v297-focused/20260510-033112-2896678/summary.json`. The original pack was restored after the gate failed. | Failed/no promotion. The partial resident copy did not reduce reads or blocking and shifted cadence backward: VISITOR3 high regressed `scene_vb 1406 -> 1407`, active loop `1074/1038 -> 1075/1038`, and overrun `36 -> 37`; blocking stayed `58`, loop reads stayed `10`, due misses stayed `10`, and hidden refill stayed `0`. | Do not promote partial frame-131 edge residency. Frame `131` needs to fit fully inside paid setup coverage or use a true no-runtime data-shape reduction; a small spill is enough to lose the visible timing gate without removing the read. |

| 2026-05-10 | `visitor3-high-f131-motion-scan-v298` | Host-side log-only; no pack/source delta. | Follow up v297 with the only plausible full-residency route: shrink high frame `131` by at least `1,102` bytes so it fits entirely in the existing setup-prime gap after frame `130`. Tested the existing VISITOR3 motion-copy generator on frame `131` before adding any runtime code or writing a pack. | Ran dry scans with `scratch/visitor3-motion-x-v182/try_motion.py` for frame `131`: exact copy mode, target-hull with exact cleanup, and target-hull with hull cleanup. The exact path failed because the current marker format would need a negative relative X (`-153`) that it cannot encode; both target-hull variants failed with “no target-hull candidate.” Partial output roots: `scratch/visitor3-high-f131-motion-scan-v298/`. | Failed/no promotion before emulator. No pack was written and no runtime gate was needed. | Close existing opcode-1 motion-copy for high frame `131`. A frame-131 shrink still looks valuable, but it needs a different encoder/opcode that can represent left-of-entry copy rows, or a non-motion data shape; the current target-hull/exact generator cannot produce a valid payload. |

| 2026-05-10 | `visitor3-high-f131-resident-alias121123-v299` | `ps1: keep visitor3 high frame131 resident` | Make a larger VISITOR3 high pack-only swing after the partial edge-copy and motion-scan misses. Frames `121` and `123` were byte-identical to frame `120`, so alias those entries to frame `120`, compact the resident setup-prime tail, and copy frame `131` unchanged fully inside paid setup-prime coverage. | Rewrote only `generated/ps1/foreground/VISITOR3.FG2`, preserving the `1555450` byte padded footprint, high LBA `22473`, sound table, and PS-EXE bucket. Transform summary: `scratch/visitor3-high-f131-resident-alias121123-v299/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-f131-resident-alias121123-v299-focused/20260510-033736-2567021/summary.json`; broad gate: `scratch/ps1-perf-iterate/visitor3-high-f131-resident-alias121123-v299-broad-norequire/20260510-033846-2573991/summary.json`; promoted row summary: `scratch/promoted-summaries/visitor3-high-f131-resident-alias121123-v299-summary.json`. | Promoted. Focused and 11-case broad no-regression gates improve VISITOR3 high `scene_vb 1406 -> 1402`, active timing `1074/1038 -> 1070/1039`, overrun `36 -> 31`, blocking `58 -> 49`, loop reads `10 -> 9`, loop-read VBlanks `58 -> 49`, and due misses `10 -> 9`; hidden refill stays `0`. VISITOR3 low plus WALKSTUF1 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed flat. Public rollup moves to `+0.3215%` over target / `99.6847%` target speed; raw signed rollup moves to `-0.4470%` / `100.4685%`. Methodology total is about `17.08` public over-target points removed and `12.58` target-speed points gained since the compact full-matrix baseline. | Keep. This proves full resident placement can still buy VISITOR3 active-loop VBlanks when the pack first removes duplicate resident payloads; partial residency and semantic rewrite attempts remain closed unless they fit entirely inside paid coverage or introduce a stronger generated format. |

| 2026-05-10 | `visitor3-low-f128-resident-seg27-v302` | `ps1: keep visitor3 low frame128 resident` | Make a larger VISITOR3 low pack/source swing after the v292 no-op alias and the v300/v301 WALKSTUF1 misses: keep the accepted low resident cluster, alias duplicate frame `123` to frame `121`, grow the second low setup segment from `24` to `27` sectors, and fit frame `128` fully inside paid setup coverage without evicting frames `125`/`126`. | Rewrote `generated/ps1/foreground/VIST3LOW.FG2` by compacting low resident-segment frames `118..128` into sectors `150..177`, pointing frame `123` at frame `121`'s identical payload, and moving frame `128` resident. `src/foreground_pilot.c` changes `FG_VISITOR3_LOW_SETUP_SEGMENT2_BYTES` from `24` to `27` CD sectors. The pack footprint stays `1555450` bytes, the low LBA/PS-EXE bucket stay fixed, and segment2 ends at `362496` with `821` spare bytes. Layout summary: `scratch/visitor3-low-f128-resident-seg27-v302/layout.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-low-f128-resident-seg27-v302-focused/20260510-042223-2818332/summary.json`; broad gate: `scratch/ps1-perf-iterate/visitor3-low-f128-resident-seg27-v302-broad-norequire/20260510-042342-2826400/summary.json`; promoted row summary: `scratch/promoted-summaries/visitor3-low-f128-resident-seg27-v302-summary.json`. | Promoted. Focused and broad no-regression gates improve VISITOR3 low `scene_vb 1402 -> 1399`, active loop `1075/1039 -> 1071/1039`, overrun `36 -> 32`, blocking `67 -> 63`, loop reads `12 -> 11`, loop-read VBlanks `67 -> 63`, and due misses `12 -> 11`; hidden refill stays `0`. VISITOR3 high plus WALKSTUF1 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed flat. Public rollup moves to `+0.3184%` over target / `99.6876%` target speed; raw signed rollup moves to `-0.4501%` / `100.4714%`. Methodology total is about `17.08` public over-target points removed and `12.59` target-speed points gained since the compact full-matrix baseline. | Keep as the current VISITOR3 low baseline. The low frame-128 setup-resident path works only when the segment grows and duplicate resident payloads pay for the move; earlier frame-128 evict/gap-align forms stay closed. Next VISITOR3 low edge is frame `129`/later large-frame residency or a true scene-owned data format with explicit scheduler ownership. |

| 2026-05-10 | `visitor3-high-prime448-v290` | Log-only; source restored. | Take the cheap high-only VISITOR3 setup-prime swing before building a larger pack-format sidecar: raise high-tide setup-prime residency from `320 KiB` to `448 KiB` so the first uncovered high-tide read cluster moves into pre-loop coverage, while leaving low tide isolated. | Temporarily changed `FG_VISITOR3_HIGH_SETUP_PRIME_WINDOW_BYTES` and `FG_VISITOR3_SETUP_PRIME_MAX_RESIDENT_BYTES` from `320 KiB` to `448 KiB`, rebuilt, and ran the focused high gate against the current VISITOR3 high baseline. The run expanded setup coverage from sectors `1..161` to `1..225` plus the existing `277..293` segment, with setup bytes rising `413,236 -> 626,164` and setup reads `13 -> 14`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-prime448-v290-focused/20260510-011516-3577925/summary.json`. | Failed/no promotion. Active loop did not improve: high stayed `1075/1037`, overrun `38`, loop reads `11`, loop-read VBlanks `61`, due misses `11`, and hidden refill `0`. The extra setup residency regressed scene time `1406 -> 1407`, visible blocking `59 -> 61`, shifted VISITOR3 high pack LBA `22472 -> 22473`, and crossed the PS-EXE sector bucket `215040 -> 217088`. | Keep VISITOR3 high setup-prime at `320 KiB`. Larger scalar prime caps are closed unless a future generated/layout-neutral form can avoid executable/LBA drift and prove an active-loop read-count win. The next VISITOR3 work should target pack-side data shape or generated scheduler metadata for the remaining `261..277` and `253..277` high clusters, not another cap increase. |

| 2026-05-10 | `walkstuf1-low-group290-314-v300` | Log-only; source restored. | Make a larger WALKSTUF1 low scheduler-owned swing after raw `208..220` stayed flat and scalar low setup-prime cap `168 KiB` was already closed. The current low read-plan ranked sectors `290..314` as the only high-signal 24-sector cluster: seven fully covered tight reads, six estimated saved reads, append-start fireable at sector `290`, and entries `147..156`, but it required larger retained group capacity. | Temporarily raised `FG_PREFETCH_GROUP_WINDOW_BYTES` from `16` to `24` sectors and added a low-tide-only `walkstuf1` retained group `{290, 314, 0}`, then ran the focused low gate against the v299 broad baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group290-314-v300-focused/20260510-041214-2761579/summary.json`. Source was restored after the gate failed. | Failed/no promotion. The modeled large group did not move timing: low stayed exact-flat at `scene_vb=1770`, active loop `1478/1428`, overrun `50`, blocking `75`, hidden refill `25`, loop reads `69`, and due misses `13`; pack LBA stayed `25495` and the PS-EXE bucket stayed `217088`. The temporary source branch shifted hot symbols by `+8` bytes, so even a flat result would not have been worth keeping. | Close larger retained-stream capacity and the `290..314` raw group for WALKSTUF1 low. The remaining tight mid-tail reads need true generated deadline ownership, pack/data-shape reduction, precomposed payloads, or semantic relocation; simply widening group capacity does not convert the read-plan signal into VBlank improvement. |

| 2026-05-10 | `walkstuf1-low-gap1-f38-60-v301` | Log-only; pack restored. | Test a very narrow no-runtime data-shape pass on the first post-setup low cluster instead of another mid-tail read group. The prebuilt cleanup-gap subset `f38..60` saved only `508` active payload bytes and shifted the sound table `922737 -> 922229` while keeping the padded `1535263` byte footprint and pack LBA fixed; the goal was to relieve sectors `78..99` without touching later cadence. | Copied `scratch/walkstuf1-low-gap-subset-scan-v296/gap1-f38-60.FG2` over `generated/ps1/foreground/WALK1LOW.FG2`, ran the focused low gate, then restored the original pack from `scratch/walkstuf1-low-gap1-f38-60-v301/WALK1LOW.FG2.before`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-gap1-f38-60-v301-focused/20260510-041459-2777412/summary.json`. | Failed/no promotion. The small early byte saving was phase-negative: low regressed `scene_vb 1770 -> 1782`, active loop `1478/1428 -> 1490/1424`, overrun `50 -> 66`, blocking `75 -> 97`, hidden refill `25 -> 31`, loop reads `69 -> 68`, and due misses `13 -> 15`. | Close early cluster cleanup-gap compaction for WALKSTUF1 low. For this scene, small payload shifts near the setup boundary can worsen cadence even when read count drops; future low work needs semantic-preserving relocation, precomposed/generated data, or explicit scheduler ownership rather than more cleanup-gap trimming in frames `38..60`. |

| 2026-05-10 | `walkstuf1-low-seg329-345-v303` | Log-only; source restored. | Try a larger scene-local residency swing on the current largest under-99 row. The fresh read plan ranked low-tide sectors `329..345` as a 16-sector cluster with four covered reads and three modeled saved reads, so the probe tested whether a reusable setup segment could move that late tight cluster out of the active loop without touching the pack. | Temporarily added `FG_WALKSTUF1_LOW_SETUP_SEGMENT_START` at sector `329`, `FG_WALKSTUF1_LOW_SETUP_SEGMENT_BYTES` at `16` CD sectors, and a low-tide-only branch in `fgRuntimePrimeSetupSegment()` that retained the segment in `gFgSetupSegmentBuffer`, then reran the matched WALKSTUF1 low gate against the v302 broad baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-seg329-345-v303-focused-matched/20260510-052020-3143846/summary.json`. Source was restored after the gate failed. | Failed/no promotion. The segment did convert CD pressure, but not public timing: active loop regressed `1478/1428 -> 1479/1432` and scene time regressed `1770 -> 1784`, while overrun improved `50 -> 47`, blocking improved `75 -> 67`, hidden refill improved `25 -> 16`, loop reads improved `69 -> 66`, and due misses improved `13 -> 12`. Setup reads rose `13 -> 14`, setup-read time rose `185 -> 198`, and the hot source branch grew `foregroundPilotPlay` by `180` bytes with downstream hot symbols shifted by `+180`. | Do not promote reusable setup-segment preloading for WALKSTUF1 low sectors `329..345`. The read savings are real, but the extra pre-loop setup read and hot-code growth consume the win under the strict timing gate. Future WALKSTUF1 low work should favor zero/new-runtime pack relocation, generated scheduler-owned append timing, or layout-pinned metadata rather than another retained setup segment. |

| 2026-05-10 | `walkstuf1-low-prefix-gap6-v304` | Log-only; pack restored. | Push the already-promoted low prefix cleanup-gap idea harder instead of adding source or setup reads. Gap2 through gap5 still left frame `39` spilling beyond the paid setup-prime sectors `2..78`, while gap6 saved enough prefix bytes to move frame `39` fully inside that resident coverage; the binary question was whether removing that active-loop read would beat the extra restore/cadence cost. | Generated `scratch/walkstuf1-low-prefix-gap6-v304/low-gap6-prefix.FG2` with `merge_cleanup_gaps.py --max-gap 6 --frames 0-37`, saving `2206` prefix payload bytes, changing `37` early entries, reducing cleanup spans `6355 -> 5576`, adding `3040` restored background pixels, and moving the sound table `922737 -> 920531` while preserving the padded `1535263` byte pack footprint and LBA. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-prefix-gap6-v304-focused/20260510-052456-3170523/summary.json`. The original pack was restored after the gate failed. | Failed/no promotion. The residency/read signal was real but overcompacted: low blocking improved `75 -> 70`, loop reads improved `69 -> 67`, and due misses improved `13 -> 11`, but public timing regressed `scene_vb 1770 -> 1774`, active loop `1478/1428 -> 1482/1426`, overrun `50 -> 56`, and hidden refill `25 -> 31`. Pack LBA and PS-EXE bucket stayed fixed. | Do not promote aggressive low-prefix gap6 compaction. Moving frame `39` resident is not enough when the cleanup geometry adds too much target/hidden-refill debt. Future low-prefix residency work needs semantic-preserving alias/relocation or a generated format that shrinks bytes without restoring thousands of extra pixels, not a larger cleanup-gap threshold. |

| 2026-05-10 | `walkstuf1-low-prefix-gap6-slackguard-v305` | `ps1: promote walkstuf1 low gap6 slack guard` | Revisit the failed gap6 prefix compaction as a paired binary instead of a standalone pack change. The gap6 pack proved it could move frame `39` fully into paid setup-prime coverage and remove reads, but it needed a low-tide cadence guard to avoid turning those byte savings into hidden refill debt. | Reused `scratch/walkstuf1-low-prefix-gap6-v304/low-gap6-prefix.FG2` as `generated/ps1/foreground/WALK1LOW.FG2`, then removed the high-tide-only condition from the WALKSTUF1 3-VBlank speculative window-prefetch skip in `fgRuntimeTryPrefetchWindow()`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-prefix-gap6-slackguard-v305-focused/20260510-053322-3217508/summary.json`; broad gate: `scratch/ps1-perf-iterate/walkstuf1-low-prefix-gap6-slackguard-v305-broad-norequire/20260510-053456-3226728/summary.json`; promoted-row summary: `scratch/promoted-summaries/walkstuf1-low-prefix-gap6-slackguard-v305-summary.json`. | Promoted. Focused and 11-case broad no-regression gates improve WALKSTUF1 low `scene_vb 1770 -> 1767`, active loop `1478/1428 -> 1475/1430`, overrun `50 -> 45`, blocking `75 -> 67`, hidden refill `25 -> 21`, loop reads `69 -> 66`, loop-read VBlanks `301 -> 282`, and due misses `13 -> 12`. WALKSTUF1 high, VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low stayed flat. Pack LBA stays `25495`, pack sectors stay `750`, and the `217088` byte PS-EXE bucket stays fixed while `fgRuntimeTryPrefetchWindow` shrinks by `16` bytes. | Keep as the current WALKSTUF1 low baseline. Public rollup moves to `+0.3156%` over target / `99.6902%` target speed; raw signed rollup moves to `-0.4529%` / `100.4740%`. Methodology total is about `17.08` public over-target points removed and `12.59` public target-speed points gained since the compact full-matrix baseline. Next WALKSTUF1 work should focus high first, then low generated scheduler-owned timing or semantic-preserving pack relocation rather than larger cleanup-gap thresholds alone. |

| 2026-05-11 | `walkstuf1-high-prefix-gap2-v306` | Log-only; pack restored. | Test whether the accepted high-tide gap1 prefix pack had one more safe cleanup-gap step available now that the v288 no-slack speculative-window guard and v305 low-side guard are in the release baseline. The binary question was whether a smaller incremental `max_gap=2` prefix compaction could reduce early CD pressure without the hidden-refill debt seen in wider gap scans. | Generated `scratch/walkstuf1-high-prefix-gap2-v306/high-gap2-prefix.FG2` from the current `generated/ps1/foreground/WALKSTUF1.FG2` with `merge_cleanup_gaps.py --max-gap 2 --frames 0-37`, changing `25` early entries, saving another `522` active prefix bytes (`918345 -> 917823`), reducing cleanup spans `6355 -> 6193`, adding `324` cleanup pixels, and preserving the padded `1535263` byte pack footprint and LBA. The first two harness attempts were structural `missing_jcperf2` runs while an unrelated visual DuckStation controller was active; after stopping that controller, the focused gate completed at `scratch/ps1-perf-iterate/walkstuf1-high-prefix-gap2-v306-focused-clean/20260511-162446-2126577/summary.json`. A restored-pack current-control pass at `scratch/ps1-perf-iterate/walkstuf1-high-current-control-v306/20260511-162639-2137915/summary.json` reconfirmed the public baseline. | Failed/no promotion. The incremental byte saving was strongly phase-negative: high regressed from the current-control baseline `scene_vb=1766`, active loop `1477/1431`, overrun `46`, blocking `90`, hidden refill `19`, loop-read VBlanks `285`, and due misses `17` to `scene_vb=1783`, active loop `1494/1418`, overrun `76`, blocking `98`, hidden refill `34`, loop-read VBlanks `326`, and due misses `15`. Loop reads stayed `69`, pack LBA stayed `24745`, pack sectors stayed `750`, and the PS-EXE bucket stayed `217088`. | Close higher-threshold cleanup-gap prefix compaction for WALKSTUF1 high. Gap1 remains valid only because the v288 guard repays its hidden-refill debt; even a small additional `522`-byte prefix shift changes target cadence and refill phase enough to lose `17` loop VBlanks and `30` overrun VBlanks. Future high work should avoid larger prefix gap thresholds and instead target semantic-preserving placement/aliasing, precomputed strip data, or generated scheduler ownership around the current read-plan clusters. |

| 2026-05-11 | `walkstuf1-high-readgroup329-current-v307` | Log-only; source restored. | Retest the old high-tide `329..345` retained read group on the current v288/v305 release baseline instead of the older pre-guard baseline. The fresh read plan ranked it as the best 16-sector WALKSTUF1 high group with three modeled saved reads and no partial overread, so the follow-up tested whether current no-slack window gating made the CD win promotable. | Temporarily added `{329, 345, 0}` beside the accepted high group `{201, 213, 0}` in `kWalkstuf1HighReadGroups12`, then ran the focused high gate at `scratch/ps1-perf-iterate/walkstuf1-high-readgroup329-current-v307-focused/20260511-163103-2162947/summary.json`. Because the no-threshold form improved CD but lost target cadence, two threshold probes followed: `minSlack=8` at `scratch/ps1-perf-iterate/walkstuf1-high-readgroup329-slack8-v307-focused/20260511-163234-2171917/summary.json` and `minSlack=4` at `scratch/ps1-perf-iterate/walkstuf1-high-readgroup329-slack4-v307-focused/20260511-163359-2180344/summary.json`. Source was restored after the threshold sweep. | Failed/no promotion. The zero-threshold group had a real CD signal: loop reads improved `69 -> 66`, blocking `90 -> 84`, and due misses `17 -> 15`, while scene and loop stayed `1766` and `1477`. It still regressed public cadence because target dropped `1431 -> 1428` and prefetch overrun rose `19 -> 22`, making over-target worse. `minSlack=8` and `minSlack=4` both no-opped exactly against the clean control: `1766`, `1477/1431`, overrun `46`, blocking `90`, prefetch overrun `19`, reads `69`, due `17`. | Close the direct `329..345` retained-read lane on the current baseline. The group only fires in the low-slack phase that trades CD savings for target/refill debt; thresholding skips it entirely. Future high work around this sector neighborhood needs generated deadline ownership that can reserve refill time, or a pack/data-shape change that removes the reads without asking the runtime to append them in that phase. |

| 2026-05-11 | `walkstuf1-high-readgroup178-current-v308` | Log-only; source restored. | Retest the lower-visible-risk `178..194` retained read group on the current high guard stack. The current read planner still marked it as scheduler-owned rather than high-risk: three source reads, no partial overlap, a `0.4514s` first gap, and two modeled saved reads, so it was the best remaining hand-table candidate after `329..345` closed. | Temporarily added `{178, 194, 0}` ahead of the accepted high group `{201, 213, 0}` in `kWalkstuf1HighReadGroups12`, then ran the focused high gate against the clean current control. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-readgroup178-current-v308-focused/20260511-163715-2199392/summary.json`. Source was restored after the gate failed. | Failed/no promotion. The retest repeated the old read-group shape: visible blocking improved only `90 -> 88`, while scene time regressed `1766 -> 1770`, active loop regressed `1477/1431 -> 1481/1426`, overrun worsened `46 -> 55`, and prefetch overrun worsened `19 -> 25`; loop reads improved `69 -> 65`, but due misses stayed `17`. Pack LBA and PS-EXE bucket stayed fixed. | Keep `178..194` closed as a direct retained-read group even on the current baseline. It removes reads, but the runtime spends the saved CD in the wrong phase and loses both target cadence and hidden refill. Any future work in this neighborhood needs generated deadline ownership or pack-side removal, not another scalar/min-slack table probe. |

| 2026-05-11 | `walkstuf1-high-prefix-gap2-preserve-v309` | Log-only; pack restored. | Isolate the v306 prefix-compaction failure mode by preserving every later payload offset. The hypothesis was that `max_gap=2` might still be useful as a pure span/payload shrink if it did not move sound offset, frame `38`, or the downstream CD phase. | Generated `scratch/walkstuf1-high-prefix-gap2-preserve-v309/high-gap2-prefix-preserve.FG2` with `merge_cleanup_gaps.py --max-gap 2 --frames 0-37 --preserve-offsets`, changing the same `25` early entries as v306, saving `522` bytes (`918345 -> 917823`), reducing cleanup spans `6355 -> 6193`, adding `324` cleanup pixels, and keeping the sound table at `922737`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-prefix-gap2-preserve-v309-focused/20260511-164057-2221058/summary.json`. The original pack was restored after the gate failed. | Failed/no promotion. As expected, preserving offsets eliminated the v306 cadence regression, but it also removed all measurable timing benefit: high stayed exact-flat at `scene_vb=1766`, active loop `1477/1431`, overrun `46`, blocking `90`, prefetch overrun `19`, loop reads `69`, loop-read VBlanks `285`, and due misses `17`. | Close offset-preserved higher-threshold prefix shrink for WALKSTUF1 high. The extra span compaction is safe but inert; the only path that moved timing was offset-shifting, and that path regressed badly. Future prefix work needs semantic placement or a new representation that removes reads without changing target cadence, not more cleanup-gap span merges. |

| 2026-05-11 | `walkstuf1-high-hot-gap-preserve-v310` | Log-only; pack restored. | Try a layout-stable active-loop cleanup pass instead of another setup-edge or retained-read-table tweak. The idea was to shrink the high-pressure WALKSTUF1 high visible cluster around entries `140..170` while preserving every payload offset, so CD placement, pack LBA, sound offset, and PS-EXE layout would stay fixed and only per-frame payload/span work would change. | Generated two offset-preserved packs from the current `generated/ps1/foreground/WALKSTUF1.FG2`: gap1 `scratch/walkstuf1-high-hot-gap-preserve-v310/gap1-f140-170.FG2` saved `1400` selected payload bytes, reduced cleanup spans `8095 -> 7492`, and added `603` cleanup pixels; gap2 `scratch/walkstuf1-high-hot-gap-preserve-v310/gap2-f140-170.FG2` saved `2012` selected payload bytes, reduced cleanup spans `8095 -> 7256`, and added `1075` cleanup pixels. Focused artifacts: gap2 `scratch/ps1-perf-iterate/walkstuf1-high-hot-gap2-preserve-v310/20260511-170228-2340356/summary.json`; gap1 `scratch/ps1-perf-iterate/walkstuf1-high-hot-gap1-preserve-v310/20260511-170342-2347464/summary.json`. The original pack was restored after both probes. | Failed/no promotion. Both thresholds converged to the same timing: scene and loop stayed `1766` and `1477`, but target dropped `1431 -> 1430`, so overrun worsened `46 -> 47`. CD submetrics moved in mixed directions: blocking improved `90 -> 85`, loop-read time improved `285 -> 282`, due misses improved `17 -> 16`, but hidden refill worsened `195 -> 197` and prefetch overrun worsened `19 -> 21`. Loop reads stayed `69`; pack LBA stayed `24745`; pack sectors stayed `750`. | Close active-loop cleanup-gap preserve passes for WALKSTUF1 high frames `140..170`. The CD-pressure signal is real but not enough to offset target/refill debt, and preserving offsets prevents any sector-layout win. Future work in this cluster needs a stronger data-shape change such as semantic aliasing, generated precomposed strips, or deadline-owned scheduling rather than span-gap compression. |

| 2026-05-11 | `walkstuf1-high-readgroup298-current-v311` | Log-only; source restored. | Try the top unclosed WALKSTUF1 high retained-read candidate after the prefix/preserve attempts closed. The planner ranked sectors `298..314` as a current-window-compatible 16-sector group with three modeled saved reads, so the probe tested whether direct grouping could turn that CD signal into public timing. | Temporarily added `{298, 314, 0}` ahead of the accepted `{201, 213, 0}` in `kWalkstuf1HighReadGroups12`, then swept threshold variants after the unthresholded result showed CD-positive/public-negative behavior. Artifacts: unthresholded `scratch/ps1-perf-iterate/walkstuf1-high-readgroup298-current-v311-focused/20260511-170819-2373586/summary.json`; `minSlack=4` `scratch/ps1-perf-iterate/walkstuf1-high-readgroup298-slack4-v311-focused/20260511-170936-2381592/summary.json`; `minSlack=3` `scratch/ps1-perf-iterate/walkstuf1-high-readgroup298-slack3-v311-focused/20260511-171106-2390686/summary.json`. Source was restored after the sweep. | Failed/no promotion. The unthresholded group improved CD pressure but spent it in the wrong phase: loop reads `69 -> 66`, blocking `90 -> 83`, loop-read VBlanks `285 -> 276`, and due misses `17 -> 13`, but scene regressed `1766 -> 1774`, active loop regressed `1477/1431 -> 1485/1426`, overrun worsened `46 -> 59`, and prefetch overrun worsened `19 -> 26`. Both `minSlack=4` and `minSlack=3` no-opped exactly against the baseline: `1766`, `1477/1431`, overrun `46`, blocking `90`, prefetch overrun `19`, reads `69`, due misses `17`. | Close direct retained-read grouping for high sectors `298..314`. Like `329..345`, it only fires when slack is too tight to preserve target/refill cadence; safe thresholds skip the opportunity entirely. Future work in the `286..322` cluster needs generated deadline ownership or a pack-side representation that removes reads without appending in the low-slack phase. |

| 2026-05-11 | `walkstuf1-high-setup-segment-v312` | Log-only; source restored. | Try to avoid the low-slack append failure by moving the first post-prime WALKSTUF1 high sector cluster into setup-time residency. The initial target was the planner's `80..92` 12-sector row, then the probe widened to cover the actual first uncovered frames before any active read could invalidate the transient segment. | Temporarily added a high-tide WALKSTUF1 setup segment through the existing transient `streamScratch` path. The first `80..92` form no-opped because the scratch buffer was too small and frames `38..39` would read before the segment was useful. The corrected `75..92` form enlarged scratch to `17` sectors and completed at `scratch/ps1-perf-iterate/walkstuf1-high-seg75-92-v312-focused/20260511-171712-2425864/summary.json`. A larger `74..98` 24-sector form failed structurally before `JCPERF2` at `scratch/ps1-perf-iterate/walkstuf1-high-seg74-98-v312-focused/20260511-171840-2436189/`. Source was restored after the sweep. | Failed/no promotion. The real `75..92` segment reduced active loop reads `69 -> 67` and prefetch overrun `19 -> 16`, but regressed public playback: scene `1766 -> 1778`, loop `1477 -> 1479`, overrun `46 -> 48`, blocking `90 -> 97`, hits `199 -> 197`, and due misses `17 -> 19`. The `74..98` extension was not structurally safe in the fixed headless window. | Close transient setup-segment residency for the first WALKSTUF1 high post-prime cluster. Moving early reads into setup changes the active CD cadence but does not improve the loop; it either exposes later blocking or becomes structurally unsafe. Future setup-residency work needs inter-scene preload or offset-stable generated resident placement, not a larger transient scratch segment. |

| 2026-05-11 | `walkstuf1-high-offscreen-clip-v313` | Log-only; pack/source restored. | Try a larger zero-runtime data-shape swing after direct read groups and setup residency closed. Many WALKSTUF1 high entries have bounding boxes outside the 640x480 display, so the probe clipped offscreen FGP3/v4 cleanup and/or draw spans while preserving every payload offset, pack size, pack LBA, and PS-EXE bucket. | Generated offset-preserved full clip, draw-only, cleanup-only, and active-cluster-only variants under `scratch/walkstuf1-high-draw-retained-v313/`. Focused artifacts: full clip `scratch/ps1-perf-iterate/walkstuf1-high-offscreen-clip-v313-focused/20260511-172921-2498419/summary.json`; draw-only `scratch/ps1-perf-iterate/walkstuf1-high-offscreen-draw-v313-focused/20260511-173207-2513943/summary.json`; cleanup-only `scratch/ps1-perf-iterate/walkstuf1-high-offscreen-cleanup-v313-focused/20260511-173318-2520828/summary.json`; active clusters `scratch/ps1-perf-iterate/walkstuf1-high-offscreen-active-v313-focused/20260511-173546-2534770/summary.json`; active clusters plus temporary WALKSTUF1 `5`-VBlank window slack floor `scratch/ps1-perf-iterate/walkstuf1-high-offscreen-active-slack5-v313-focused/20260511-173719-2543604/summary.json`. Pack and source were restored after the sweep. | Failed/no promotion. The full clip reduced active payload `918345 -> 814617`, removed `126750` offscreen draw pixels plus `75837` cleanup pixels, and improved CD pressure (`loop_reads 69 -> 62`, `blocking_vb 90 -> 83`, due misses `17 -> 15`), but regressed public timing to `scene_vb 1780`, loop `1478/1425`, overrun `53`, and prefetch overrun `22`. Draw-only regressed to `1784`, `1482/1423`, overrun `59`; cleanup-only regressed to `1784`, `1482/1427`, overrun `55`; active-cluster-only regressed to `1784`, `1482/1426`, overrun `56`; pairing active clusters with a `5`-VBlank slack floor cut prefetch overrun to `1` but regressed blocking to `151`, due misses to `32`, and loop to `1495/1428`. | Close naive offscreen clipping for WALKSTUF1 high. The pixel/work reduction is real, but trimming payload sizes changes setup/read cadence enough to move the saved work into visible or hidden refill debt. Revisit only with generated phase ownership, offset-stable resident placement, or a representation that preserves the current setup/target cadence while removing offscreen work. |
| 2026-05-11 | `walkstuf1-high-rg213-229-slack4-v316` | `ps1: extend walkstuf1 high read group` | After the stale-layout audit showed the current Mary5 wave-fix layout shifted later pack LBAs, retest the current WALKSTUF1 high row and promote the smallest source-side win that survives same-layout canaries. The candidate extends the accepted high-tide `{201,213}` retained stream group through sectors `213..229` and raises the WALKSTUF1 no-slack speculative-window guard by one VBlank only on high tide. | Added `{213,229,0}` to `kWalkstuf1HighReadGroups12`, made the WALKSTUF1 guard use `FG_PREFETCH_WINDOW_MIN_SLACK_VBLANKS + 1` only when `lowTide == 0`, rebuilt, and ran a focused confirmation at `scratch/ps1-perf-iterate/walkstuf1-high-rg213-229-slack4-v316-confirm/20260511-212551-3849427/summary.json`. The broad candidate canaries are in `scratch/ps1-perf-iterate/walkstuf1-high-rg213-229-slack4-v316-broad-norequire/20260511-205651-3681796/summary.json`; long canaries for ACTIVITY9 low and MARY3 high/low are in `scratch/ps1-perf-iterate/walkstuf1-high-rg213-229-slack4-v316-long-canaries/20260511-210800-3745968/summary.json`. | Promoted with an explicit hidden-refill tradeoff. Against the current-layout no-candidate high control, WALKSTUF1 high improves `scene_vb 1770 -> 1768`, active loop `1482/1429 -> 1480/1429`, public overrun `53 -> 51`, blocking `102 -> 85`, loop reads `70 -> 69`, and due misses `19 -> 16`; prefetch overrun rises `16 -> 26`, matching the accepted hidden-refill tradeoff pattern used elsewhere when public loop/blocking improves. WALKSTUF1 low, VISITOR3 high/low, BUILDING2 high/low, ACTIVITY9 low, FISHING1 high, JOHNNY1 high, and MARY3 high/low matched the no-candidate controls. | Keep as the current WALKSTUF1 high baseline. The CSV also refreshes measured same-layout canary rows whose stale pre-Mary5-fix LBAs understated current layout pressure, so the public rollup now reads `+0.3326%` over target / `99.6742%` target speed and raw signed reads `-0.4344%` / `100.4566%`. Methodology total is about `17.07` public over-target points removed and `12.57` public target-speed points gained since the compact full-matrix baseline. Next focus is WALKSTUF1 low and VISITOR3 low unless generated phase ownership opens a larger high-side lane. |
| 2026-05-11 | `walkstuf1-low-readgroup-v317` | Log-only; source restored. | Try one more low-tide scalar retained-read lane before moving to data-shape work. The v316 read plan ranked the late `305..321` cluster as a three-saved-read candidate and the `190..202` cluster as the safest current-window row, so the probes tested whether a guarded or unguarded low read table could convert either row without generated scheduler ownership. | Temporarily added a low-tide WALKSTUF1 read group table and routed it from `foregroundPilotPlay()`. Focused artifacts: guarded `305..321` with `minSlack=4` at `scratch/ps1-perf-iterate/walkstuf1-low-rg305-321-slack4-v317-focused/20260511-220625-4073420/summary.json`; unguarded `305..321` at `scratch/ps1-perf-iterate/walkstuf1-low-rg305-321-v317-focused/20260511-220738-4080444/summary.json`; unguarded `190..202` at `scratch/ps1-perf-iterate/walkstuf1-low-rg190-202-v317-focused/20260511-220914-4089795/summary.json`. Source was restored after the sweep. | Failed/no promotion. Both `305..321` variants were exact-flat against the current low baseline at `scene_vb=1775`, active loop `1483/1429`, blocking `74`, hidden refill `24`, loop reads `66`, loop-read VBlanks `290`, and due misses `12`; the safe threshold and unguarded forms did not produce a measurable grouped-read win. The lower-risk `190..202` row fired in a phase-negative way: active loop regressed to `1498/1422`, overrun worsened `54 -> 76`, blocking `74 -> 99`, hidden refill `24 -> 35`, due misses `12 -> 13`, and loop reads stayed `66`. | Close scalar low-tide WALKSTUF1 read groups for now. The low row needs generated phase ownership or a pack/data-shape change that removes reads without appending in the active low-slack phase; current-window table rows either no-op or damage target cadence. |
| 2026-05-11 | `walkstuf1-low-window-sweep-v317` | Log-only; no source changes. | Test whether the low-tide WALKSTUF1 stream-window size is too large for the active cadence. The binary swing was to shrink or slightly expand the stage-1 residual window around the current `40 KiB` policy using boot overrides, looking for a read-count/blocking knee before changing the source constant. | Ran one sequential focused sweep with `prefetch-window` overrides at `36 KiB`, `38 KiB`, `42 KiB`, and `44 KiB`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-window-sweep-v317/20260511-221242-4109620/summary.json`. | Failed/no promotion. Every override reduced loop reads, but all moved the saved CD work into worse public timing and blocking versus the current low baseline `scene_vb=1775`, active loop `1483/1429`, overrun `54`, blocking `74`, hidden refill `24`, reads `66`, due misses `12`. Results: `36 KiB` -> `1824`, `1530/1430`, overrun `100`, blocking `111`, reads `34`, due `7`; `38 KiB` -> `1815`, `1521/1438`, overrun `83`, blocking `105`, reads `30`, due `7`; `42 KiB` -> `1815`, `1521/1436`, overrun `85`, blocking `103`, reads `27`, due `6`; `44 KiB` -> `1823`, `1529/1437`, overrun `92`, blocking `101`, reads `22`, due `5`. | Keep the `40 KiB` low residual window. WALKSTUF1 low is not read-count limited in this simple window dimension; lowering reads starves target cadence and raises visible blocking. Next low work should be generated phase/deadline ownership, offset-stable pack/data-shape changes, or semantic placement that removes reads without changing the visible refill phase. |
| 2026-05-11 | `walkstuf1-low-seg329-345-seedwindow-v318` | Log-only; source restored and PS1 rebuilt. | Retry the prior near-miss low setup-residency lane with a stronger ownership model. The v303 `329..345` reusable setup segment improved blocking, hidden refill, reads, and due misses but missed promotion because active-loop cadence rose by one VBlank. This follow-up seeded the normal stream window from the resident segment on first hit so subsequent appends could continue from the owned cluster instead of using a stale window phase. | Temporarily added a low-tide WALKSTUF1 reusable setup segment for sectors `329..345`, then copied that segment into the normal stream-window buffer on first segment-backed entry copy. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-seg329-345-seedwindow-v318-focused/20260511-222038-4155372/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The seeded variant was worse than both current baseline and the earlier v303 side-segment result: current low baseline is `scene_vb=1775`, active loop `1483/1429`, overrun `54`, blocking `74`, hidden refill `24`, reads `66`, due `12`; v318 produced `scene_vb=1794`, active loop `1490/1430`, overrun `60`, blocking `88`, hidden refill `25`, reads `65`, and due `14`. The extra window seeding removed only one read while adding visible blocking and target/cadence debt. | Close setup-segment-plus-window-seed for WALKSTUF1 low `329..345`. The earlier side-segment signal was real but too small; seeding the window makes the phase worse. Future low work should move to pack-side semantic relocation/precomputed payloads or generated per-frame deadline ownership rather than more setup residency around this late cluster. |
| 2026-05-11 | `visitor3-low-rg198-222-slack4-v319` | Log-only; source restored and PS1 rebuilt. | Probe the only current VISITOR3 low read-plan candidate with a modeled saved read: a low-tide guarded group over sectors `198..222`, covering frames `129..130`, using the existing VISITOR3 low `4`-VBlank slack floor. The hope was to combine the first two uncovered terminal reads after the v302 resident-frame baseline without touching pack layout. | Temporarily added `kVisitor3LowReadGroups24[] = {{198, 222, 4}}` and routed it only for low-tide `visitor3` when the default stream window is active. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-rg198-222-slack4-v319-focused/20260511-222502-4180531/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The candidate was exact-flat against the current-layout VISITOR3 low baseline: `scene_vb=1408`, active loop `1079/1040`, overrun `39`, blocking `67`, hidden refill `0`, reads `11`, due misses `11`, and prefetch hits `133`. It added source/table churn without a work-volume or timing win. | Close hand-authored VISITOR3 low `198..222` grouping on the current baseline. The remaining low bottleneck is the one-read-per-large-frame terminal sequence; the next useful swing needs pack-side resident placement, payload aliasing/dictionary compression, or a generated scheduler form that can actually fire, not another scalar group table. |
| 2026-05-11 | `building2-high-rg17-33-v320` | Log-only; source restored and PS1 rebuilt. | Test the current BUILDING2 high top early retained-read candidate after WALKSTUF1/VISITOR3 scalar lanes closed. The read plan ranked sectors `17..33` as a 16-sector, three-saved-read row with a better first gap than many late BUILDING2 clusters. | Temporarily added `{17, 33, 4}` ahead of the accepted high `{60,72}` group, then tried the binary opposite `{17, 33, 0}` after the guarded form no-opped. Focused artifacts: guarded `scratch/ps1-perf-iterate/building2-high-rg17-33-slack4-v320-focused/20260511-222755-3558/summary.json`; unguarded `scratch/ps1-perf-iterate/building2-high-rg17-33-v320-focused/20260511-222928-12924/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probes. | Failed/no promotion. Both variants were exact-flat against the current BUILDING2 high baseline: `scene_vb=1603`, active loop `1352/1311`, overrun `41`, blocking `56`, hidden refill `20`, loop reads `62`, due misses `7`, and prefetch hits `326`. The group table did not convert the modeled early-row savings into measured work or timing. | Close hand-authored BUILDING2 high `17..33` grouping. BUILDING2 high likely needs generated phase ownership, setup/preload relocation, or pack-side data-shape work; scalar retained groups selected by the current read-count planner remain unreliable. |
| 2026-05-11 | `walkstuf1-low-hot-gap1-f146-154-v321` | Log-only; pack restored. | Try a pack-side data-shape slice on the current largest under-99 row after low read groups, window sizing, and setup-segment seeding closed. The target was the current WALKSTUF1 low tight `285..309` sector cluster, especially entries `146..154`, using a small cleanup-gap merge to reduce span metadata without adding runtime code. | Generated a moving-offset `max_gap=1` pack for frames `146..154`, saving `756` active bytes, reducing cleanup spans `2625 -> 2321`, adding `304` cleanup pixels, and moving the sound table `920531 -> 919775`; then tried the binary opposite with `--preserve-offsets`, keeping sound offset and all later payload offsets fixed. Focused artifacts: moving offsets `scratch/ps1-perf-iterate/walkstuf1-low-hot-gap1-f146-154-v321-focused/20260511-225243-142423/summary.json`; preserved offsets `scratch/ps1-perf-iterate/walkstuf1-low-hot-gap1-f146-154-preserve-v321-focused/20260511-225405-150358/summary.json`; transform summaries under `scratch/walkstuf1-low-hot-gap-v321/`. The accepted `WALK1LOW.FG2` pack was restored after the probes. | Failed/no promotion. The moving-offset variant improved active loop only `1483 -> 1482` and left target at `1429`, but regressed visible CD pressure badly: scene `1775 -> 1774`, blocking `74 -> 89`, loop reads `66 -> 68`, due misses `12 -> 16`, and hidden refill `24 -> 23`. The offset-preserved variant was exact-flat against baseline: `scene_vb=1775`, active loop `1483/1429`, overrun `54`, blocking `74`, hidden refill `24`, reads `66`, and due `12`. | Close cleanup-gap span merging for WALKSTUF1 low frames `146..154`. The byte/span reduction is too small to matter when offsets are fixed and phase-negative when offsets move. Future WALKSTUF1 low work needs semantic relocation, precomputed payloads, or generated deadline ownership, not more local cleanup-gap compaction around this cluster. |
| 2026-05-11 | `walkstuf1-low-rg285-297-v322` | Log-only; source restored and PS1 rebuilt. | Probe the remaining current-window-compatible WALKSTUF1 low retained-read row from the post-v321 plan. The candidate grouped sectors `285..297`, covering entries `146..149`, to see whether the planner's two-read saving could land without the previous `305..321` no-op and `190..202` phase regression. | Temporarily added a low-tide WALKSTUF1 read table with `{285,297,0}` and routed `walkstuf1` low through it while preserving the accepted high-tide groups. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg285-297-v322-focused/20260511-225839-176065/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The group fired and cut loop reads `66 -> 64`, but the saved CD work landed in the wrong phase: scene regressed `1775 -> 1782`, active loop regressed `1483/1429 -> 1490/1431`, blocking regressed `74 -> 78`, hidden refill regressed `24 -> 25`, and overrun worsened `54 -> 59`; due misses stayed `12`. | Close scalar retained grouping for WALKSTUF1 low sectors `285..297`. This confirms the late low cluster is not promotable through hand-authored stream groups: even when reads fall, target/cadence debt grows. Next low attempts should be generated deadline ownership, semantic relocation, or sprite/payload-specific compression that removes work without changing append timing. |
| 2026-05-11 | `walkstuf1-low-directstage9k-v323` | Log-only; source restored and PS1 rebuilt. | Test whether WALKSTUF1 low's borderline payload sizes were stuck on the wrong side of the tight-slack direct-stage cap. The current low max payload is only `8230` bytes, just above the global `8 KiB` cap, so the probe gave low-tide `walkstuf1` a local `9 KiB` cap to allow exact-payload staging instead of full-window refill when held slack equals the minimum. | Temporarily added a scene/tide-specific direct-stage cap helper and used it in the tight-slack direct-stage branch. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-directstage9k-v323-focused/20260511-230246-200391/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The local cap was effectively flat on public timing: `scene_vb=1775`, active loop `1483/1429`, blocking `74`, loop reads `66`, and due misses `12`; hidden refill worsened slightly `24 -> 25`. | Do not promote a WALKSTUF1-low direct-stage cap raise. Borderline exact-payload staging does not move the visible loop in this scheduler shape and risks hidden refill debt; future direct-stage work needs per-frame deadline ownership, not just a larger byte cap. |
| 2026-05-11 | `walkstuf1-low-rg285-309-v324` | Log-only; source restored and PS1 rebuilt. | Take the larger binary side of the late WALKSTUF1 low refill cluster after the smaller `285..297` group proved phase-negative. The read plan modeled `285..309` as a 24-sector group that could collapse six tight reads into one append if the coalesced read fit the active cadence. | Temporarily added a low-tide-only `{285,309,0}` read group while leaving the accepted high-tide groups unchanged. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg285-309-v324-focused/20260511-230606-219553/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The group reduced loop reads `66 -> 62` and due misses `12 -> 11`, but public playback regressed: scene `1775 -> 1781`, active loop `1483/1429 -> 1489/1431`, overrun `54 -> 58`, blocking `74 -> 78`, and hidden refill `24 -> 28`. | Close hand-authored late-cluster grouping for WALKSTUF1 low, including wider coalesced forms. The scheduler can remove reads, but not in a phase that improves the visible loop; this row now needs generated deadline ownership or semantic relocation that changes when bytes become resident, not a larger append. |
| 2026-05-11 | `visitor3-low-seg198-305-v325` | Log-only; source restored and PS1 rebuilt. | Try a deliberately oversized VISITOR3 low setup-resident span after scalar grouping could not touch the terminal sequence. The current low setup keeps frames `118..128` and `139..143` resident, while `129..138` remain one large read each; the probe expanded the primary low segment from `281..305` to `198..305` so frames `129..143` would all be resident. | Temporarily changed `FG_VISITOR3_LOW_SETUP_SEGMENT_START/BYTES` to `198..305`, built, and ran the focused low gate. Artifact: `scratch/ps1-perf-iterate/visitor3-low-seg198-305-v325-focused/20260511-231042-245706/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The resident coverage worked (`uncovered_entries=1`), but memory pressure disabled the stage1 prefetch path (`policy=none`) and destroyed active playback: loop `1079/1040 -> 1118/1034`, blocking `67 -> 474`, loop reads `11 -> 143`, and due misses `11 -> 143`. | Do not use a monolithic VISITOR3 low resident segment. The active-read idea is valid, but the allocation shape starves the normal prefetch buffers; future VISITOR3 low residency must add a much smaller third segment, trade existing segment bytes, or use pack relocation/aliasing inside the current resident budget. |
| 2026-05-11 | `visitor3-low-third-seg198-222-v326` | Log-only; source restored and PS1 rebuilt. | Test the smaller version of VISITOR3 low terminal residency after the monolithic segment failed: keep the accepted `150..177` and `281..305` segments, then add a third `198..222` resident segment for frames `129..130` only. This was meant to spend about `48 KiB` instead of the failed `166 KiB` expansion. | Temporarily added third setup-segment state to `TFgPilotRuntime`, allocated it contiguously with the existing VISITOR3 segments, and read `198..222` during setup only for low tide. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-seg3-198-222-v326-focused/20260511-231527-272816/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. Even the smaller third segment pushed runtime into the low-memory path and disabled stage1 prefetch (`policy=none`), regressing loop `1079/1040 -> 1124/1036`, blocking `67 -> 477`, loop reads `11 -> 143`, and due misses `11 -> 143`. | Close additive VISITOR3 low setup-segment residency. The heap budget cannot absorb another retained segment without losing the stage1 buffers; next VISITOR3 low work must relocate or alias terminal payloads inside the existing `150..177`/`281..305` resident budget, or compress one specific terminal frame without allocating another setup buffer. |
| 2026-05-11 | `visitor3-low-swap-f128-f129-v327` | `ps1: swap visitor3 low terminal residency` | Take the useful part of the failed additive-segment probes without spending heap: trade which terminal frame is resident inside the already paid low segment2 budget. Frame `128` was resident in segment2 while frame `129` was still a visible active read; the binary question was whether swapping those payload offsets would remove enough active-loop CD debt without changing pack size, LBA, or runtime source. | Rewrote only `generated/ps1/foreground/VIST3LOW.FG2`: copied frame `129` into frame `128`'s resident slot at offset `344683`, then pointed frame `128` back to its original cold payload at offset `389716`. Pack size stays `1555450`, low LBA stays `23371`, and sectors stay `760`. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-swap-f128-f129-v327-focused/20260511-233202-364290/summary.json`; broad canary: `scratch/ps1-perf-iterate/visitor3-low-swap-f128-f129-v327-broad-norequire/20260511-233441-379255/summary.json`; long Activity9/Mary3 canaries: `scratch/ps1-perf-iterate/visitor3-low-swap-f128-f129-v327-long-canaries/20260511-234703-450526/summary.json`; transform summary: `scratch/visitor3-low-swap-f128-f129-v327/summary.json`. | Promoted. VISITOR3 low improves `scene_vb 1408 -> 1401`, active loop `1079/1040 -> 1072/1040`, overrun `39 -> 32`, blocking `67 -> 64`, and loop-read VBlanks `67 -> 64`; loop reads stay `11`, due misses stay `11`, hidden refill stays `0`, and prefetch overrun stays `0`. VISITOR3 high plus WALKSTUF1 high/low, BUILDING2 high/low, FISHING1 high, and JOHNNY1 high stayed flat in the broad gate. The first broad wrapper failed only because ACTIVITY9 low and MARY3 high/low needed a longer frame cap; the long canary rerun passed all three at current matrix values. | Keep as the current VISITOR3 low baseline. Public rollup moves to `+0.3273%` over target / `99.6792%` target speed; raw signed rollup moves to `-0.4397%` / `100.4616%`. Methodology total is now about `17.07` public over-target points removed and `12.58` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-prefix-rg-v328` | Log-only; pack/source restored and PS1 rebuilt. | After VISITOR3 low v327, return to the largest remaining under-99 row and take more creative WALKSTUF1 low swings around already-paid setup coverage. The first idea was to extend the accepted prefix cleanup-gap compaction far enough to pull frame `39` fully inside the current `2..78` setup-prime window; the second was to try the unclosed early `78..94` retained-read row because it has a long first gap and modeled two-read saving. | Generated current-pack prefix variants with the scratch cleanup-gap merger: `gap12` over frames `0..39` fully moved frame `39` inside the setup window, while `gap8` tested the smaller side. Focused artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-gap12-f0-39-v328-focused/20260512-010318-869007/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-gap8-f0-39-v328-focused/20260512-010513-879999/summary.json`. Then temporarily added a low-tide-only read group `{78,94,0}` and routed WALKSTUF1 low through it; focused artifact `scratch/ps1-perf-iterate/walkstuf1-low-rg78-94-v328-focused/20260512-011006-908164/summary.json`. | Failed/no promotion. `gap12` regressed to `scene_vb=1776`, active loop `1484/1426`, blocking `93`, hidden refill `26`, and due misses `15`; `gap8` was worse at `1780`, `1488/1420`, blocking `88`, hidden `34`, and loop reads `67`. The `{78,94}` group did fire and reduced loop reads `66 -> 65`, but public playback regressed to `1784`, `1492/1429`, overrun `63`, blocking `102`, and due misses `16`. | Close wider current-prefix cleanup-gap compaction and the early `78..94` hand-authored low group. WALKSTUF1 low remains phase-bound: moving bytes into setup or coalescing early reads can remove read count, but it steals target cadence or turns into visible blocking. Next work should prefer generated deadline ownership, resident-slot aliasing that does not change early geometry, or a custom payload/precompute format rather than more cleanup-gap widening or scalar read groups. |
| 2026-05-12 | `walkstuf1-low-rg155-179-cap24-v329` | Log-only; source restored and PS1 rebuilt. | Test a larger retained append for WALKSTUF1 low after the 16-sector scalar groups stalled. The candidate `155..179` spans four visible reads with a safer first gap than the terminal `285+` clusters, but it requires a 24-sector group window rather than the normal 16-sector grouped append capacity. | First tried `{155,179,8}` with normal capacity; artifact `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-slack8-v329-focused/20260512-012446-989466/summary.json`. Then added a low-tide-only 24-sector group capacity and reran `{155,179,8}`; artifact `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-cap24-slack8-v329-focused/20260512-012745-1006433/summary.json`. Finally tested the firing thresholds `{155,179,0}`, `{155,179,4}`, and `{155,179,6}` with the 24-sector capacity; artifacts `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-cap24-slack0-v329-focused/20260512-012924-1016146/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-cap24-slack4-v329-focused/20260512-013102-1025788/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-cap24-slack6-v329-focused/20260512-013237-1035149/summary.json`. | Failed/no promotion. The normal-capacity and slack-8 variants were no-ops at the accepted baseline (`scene_vb=1775`, active loop `1483/1429`, blocking `74`, loop reads `66`). The firing thresholds did reduce loop reads `66 -> 64` and due misses `12 -> 11`, but public timing regressed to `scene_vb=1781`, active loop `1489/1427`, overrun `62`, and prefetch overrun `31`. | Close 24-sector retained append for `155..179`: it proves read-count savings are available, but the larger append steals more cadence than it saves. The next WALKSTUF1 low pass should target smaller low-risk windows such as the follow-on `179..195` candidate, or change scheduling ownership so coalesced reads happen before the visible loop pressure instead of during it. |
| 2026-05-12 | `walkstuf1-low-rg155-171-184-196-v330` | Log-only; source restored and PS1 rebuilt. | After closing the too-large 24-sector append, test baseline-fit WALKSTUF1 low groups that use the existing 16-sector capacity: `{155,171,0}` keeps the safe first gap from v329 but trims the append, and `{184,196,0}` tests a smaller two-read merge around the previously low-risk `190..202` neighborhood. | Temporarily routed low-tide WALKSTUF1 through one group at a time. Focused artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-rg155-171-v330-focused/20260512-013802-1065976/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-rg184-196-v330-focused/20260512-013942-1075979/summary.json`. | Failed/no promotion. `{155,171,0}` reduced loop reads `66 -> 64`, but regressed public timing to `scene_vb=1788`, active loop `1496/1428`, blocking `83`, and overrun `68`. `{184,196,0}` was worse: `scene_vb=1790`, active loop `1498/1423`, blocking `97`, prefetch overrun `33`, and due misses `13`, with no loop-read reduction. | Close these 16-sector hand-authored WALKSTUF1 low groups. The repeated pattern is now strong enough: active-loop read coalescing can reduce read count, but the visible scheduler phase and upload/restore pressure make the bigger synchronous append more expensive. Next work should stop adding scalar read groups for WALKSTUF1 low and instead attack visual work, setup-time/resident layout, or a scheduler change that moves the work out of the tight loop phase. |

| 2026-05-12 | `walkstuf1-low-prepare-before-window-v331` | `ps1: prepare walkstuf1 low before cd window` | After repeated scalar read groups reduced reads but regressed cadence, test a scheduler-side swing from the deep-trace signal: when WALKSTUF1 low already has the next frame staged and the held frame is exactly at the 4-VBlank prepare point, let visual prepare run before the speculative CD-window refill instead of blocking prepare behind CD ownership. | Added a low-tide-only `walkstuf1` branch in the held-frame scheduler that calls `fgRuntimePrepareStagedFrameForPresent()` when `fgRuntimeCanPrepareStagedFrame()` is true even if `fgRuntimeWindowPrefetchWouldRead()` is also true. Focused baseline gate: `scratch/ps1-perf-iterate/walkstuf1-low-prepare-before-window-v331-focused-baseline/20260512-020351-1214077/summary.json`; control canaries: `scratch/ps1-perf-iterate/walkstuf1-low-prepare-before-window-v331-controls-baseline/20260512-020528-1223518/summary.json`. The earlier broad run `scratch/ps1-perf-iterate/walkstuf1-low-prepare-before-window-v331-broad/20260512-015009-1136297/summary.json` passed the measured controls but hit the known VISITOR3 high frame-cap/missing-JCPERF2 artifact. | Promoted as a bounded public-pressure tradeoff. WALKSTUF1 low improves public overrun `54 -> 53`, blocking `74 -> 72`, prefetch overrun `24 -> 22`, and loop-read VBlanks `290 -> 288`; target moves `1429 -> 1431` while scene and active loop move `1775 -> 1776` and `1483 -> 1484`, so the gate used the explicit `1%` bounded-regression allowance. Loop reads rise `66 -> 67`; due misses stay `12`; pack LBA/sectors and the `217088` byte PS-EXE bucket stay fixed. WALKSTUF1 high, BUILDING2 high/low, VISITOR3 low, and FISHING1 high were exact-flat against the current matrix controls. | Keep as the current WALKSTUF1 low baseline. Public rollup moves to `+0.3267%` over target / `99.6798%` target speed; raw signed rollup moves to `-0.4403%` / `100.4621%`. Methodology total is still about `17.07` public over-target points removed and `12.58` public target-speed points gained since the compact full-matrix baseline, with this commit adding `0.0006` over-target points removed and `0.0006` target-speed points. Continue with under-99 rows: WALKSTUF1 high, BUILDING2 high/low, VISITOR3 high/low, then the remaining yellow-band outliers. |

| 2026-05-12 | `walkstuf1-both-prepare-before-window-v332` | Log-only; source restored and PS1 rebuilt. | Test the binary opposite of v331's low-only scheduler branch: if preparing staged visual work before speculative window refill helped low tide, maybe high tide could use the same priority inversion instead of only the high `213..229` read group and slack4 guard. | Temporarily removed the `islandState.lowTide` qualifier from the `walkstuf1` staged-prepare-before-window branch, rebuilt, and ran the high-tide focused gate against the v331 controls. Artifact: `scratch/ps1-perf-iterate/walkstuf1-both-prepare-before-window-v332-focused-high/20260512-023735-1403372/summary.json`. Source was restored to the accepted low-only branch and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. High tide regressed hard: `scene_vb 1768 -> 1786`, active loop `1480/1429 -> 1498/1425`, overrun `51 -> 73`, blocking `85 -> 103`, prefetch overrun `26 -> 32`, and due misses `16 -> 15`. It also produced no key metric improvement and only moved scheduler ownership into a worse visible phase. | Keep v331 low-only. Close high-tide prepare-before-window as a generalized branch; WALKSTUF1 high still needs generated phase ownership, precomputed visual work, or data-shape/layout changes rather than sharing the low scheduler inversion. |

| 2026-05-12 | `walkstuf1-high-tight-prepare-before-window-v333` | Log-only; source restored and PS1 rebuilt. | Narrow v332 to the most favorable high-tide version: only borrow the prepare-before-window priority when high tide is already inside the accepted no-window slack band (`<=4` VBlanks), so normal high window refill should not be starved. | Temporarily changed the branch to `islandState.lowTide || fgRuntimeHeldSlackBeforeWait() <= 4`, rebuilt, and reran the high-tide focused gate against the v331 controls. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-tight-prepare-before-window-v333-focused-high/20260512-024245-1432862/summary.json`. Source was restored to the accepted low-only branch and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The metrics matched the unrestricted high failure: `scene_vb 1768 -> 1786`, active loop `1480/1429 -> 1498/1425`, overrun `51 -> 73`, blocking `85 -> 103`, prefetch overrun `26 -> 32`, and no key metric improved. | Close high-tide prepare-before-window entirely. The useful v331 signal is low-tide-specific; high's remaining gap needs data/layout reduction or explicit generated phase ownership rather than more priority inversion at tight slack. |

| 2026-05-12 | `building2-high-window-slack4-v334` | Log-only; source restored and PS1 rebuilt. | Test the high-tide mirror of the accepted BUILDING2 low restore-window slack rule: raise BUILDING2 high's speculative window minimum from the default `3` VBlanks to `4`, targeting the current high row's `prefetch_overrun_vb=20` and `blocking_vb=56`. | Temporarily returned `FG_BUILDING2_LOW_WINDOW_MIN_SLACK_VBLANKS` for BUILDING2 high inside `fgRuntimeWindowMinSlackVBlanks()`, rebuilt, and ran the focused high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-window-slack4-v334-focused/20260512-024704-1457551/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The guard fixed hidden/window debt but starved visible playback: prefetch overrun improves `20 -> 4`, but scene regresses `1603 -> 1613`, active loop `1352/1311 -> 1362/1313`, overrun `41 -> 49`, blocking `56 -> 114`, loop reads `62 -> 67`, and due misses `7 -> 25`. | Close scalar BUILDING2 high slack guards. Like the previous low slack-5 reject, plain window starvation can remove hidden refill but moves too much CD work into the visible path; BUILDING2 high needs generated refill ownership or data-shape reduction, not a larger minimum slack alone. |

| 2026-05-12 | `building2-low-window-slack3-v335` | Log-only; source restored and PS1 rebuilt. | Test the opposite side of the accepted BUILDING2 low slack4 rule now that the current row has only `prefetch_overrun_vb=1` but still `blocking_vb=81`: drop low tide back to the default `3` VBlank window minimum and see if visible blocking improves enough to outweigh hidden refill. | Temporarily removed the BUILDING2 low branch from `fgRuntimeWindowMinSlackVBlanks()`, rebuilt, and ran the focused low gate. Artifact: `scratch/ps1-perf-iterate/building2-low-window-slack3-v335-focused/20260512-025031-1477419/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. It is a clean public tradeoff in the wrong direction: scene and loop stay flat at `1619` and `1349`, blocking improves `81 -> 53`, loop reads improve `55 -> 53`, and due misses improve `18 -> 8`, but target drops `1318 -> 1312`, overrun worsens `31 -> 37`, and prefetch overrun regresses `1 -> 11`. | Keep BUILDING2 low on slack4. The remaining low pressure needs a scheduler-owned refill placement or data-shape reduction that preserves target cadence; simple slack3/slack4 toggling is now closed. |

| 2026-05-12 | `visitor3-low-tail-segment25-v336` | Log-only; source restored and PS1 rebuilt. | Test the cheapest remaining VISITOR3 low residency edge after v327: frame `144` starts inside the `281..305` tail setup segment but spills by one CD sector, so grow only `FG_VISITOR3_LOW_SETUP_SEGMENT_BYTES` from `24` to `25` sectors and make the final cleanup frame fully resident. | Temporarily changed the low VISITOR3 tail segment to `25` sectors, rebuilt, and ran the focused low gate against the v327 broad baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-tail-segment25-v336-focused/20260512-030156-1541163/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The residency signal worked, dropping loop reads `11 -> 10` and blocking `64 -> 62`, but public timing lost more than it gained: scene `1401 -> 1408`, active loop `1072/1039 -> 1080/1039`, and overrun `32 -> 41`. | Close scalar one-sector tail expansion. VISITOR3 low cannot buy the last cleanup read by spending another setup sector; the next low work should trade bytes inside the existing `150..177`/`281..305` resident budget or change the frame data shape so the terminal cluster fits without extra setup read cost. |

| 2026-05-12 | `visitor3-low-tail-pack-only-v338` | `ps1: compact visitor3 low tail cleanup` | Retry the v336 residency signal without spending another setup sector or adding runtime code. The binary question was whether the last low-tide cleanup read could be removed by reshaping the pack tail inside the already paid `281..305` setup window. | Rewrote only `generated/ps1/foreground/VIST3LOW.FG2`. Frame `143` cleanup now uses one bounded compact span per active row, shrinking `3497 -> 2857` bytes and creating `640` bytes of earlier tail space. Frame `144` terminal cleanup now uses one compact full-width cleanup span per row, shrinking `3744 -> 1908` bytes and moving from offset `622831` to `622191`, so its exclusive end sector moves `306 -> 305`. Focused gate: `scratch/ps1-perf-iterate/visitor3-low-tail-pack-only-v338-focused/20260512-034631-1793388/summary.json`; controls: WALKSTUF1 high `scratch/ps1-perf-iterate/visitor3-low-tail-pack-only-v338-control-walkstuf1-high/20260512-034754-1801487/summary.json`, BUILDING2 high/low and FISHING1 high `scratch/ps1-perf-iterate/visitor3-low-tail-pack-only-v338-controls-rest/20260512-034918-1809535/summary.json`; transform summary: `scratch/visitor3-low-tail-pack-only-v338/summary.json`. | Promoted as CD-pressure/work reduction. VISITOR3 low public timing stays flat at `scene_vb=1401`, active loop `1072/1040`, and overrun `32`, while blocking improves `64 -> 58`, loop reads `11 -> 10`, loop-read VBlanks `64 -> 58`, and due misses `11 -> 10`; hidden refill and prefetch overrun stay `0`. Pack size/LBA/sectors stay `1555450`/`23371`/`760`, and no source code changes are needed. WALKSTUF1 high, BUILDING2 high/low, and FISHING1 high controls stayed exact-flat. VISITOR3 high still fails to emit `JCPERF2` under the current boot and is tracked as a separate measurement defect, not a regression from this low-pack-only change. | Keep as the current VISITOR3 low baseline. Public rollup remains `+0.3267%` over target / `99.6798%` target speed because active loop/target did not move; raw signed rollup remains `-0.4403%` / `100.4621%`. Methodology total remains about `17.07` public over-target points removed and `12.58` public target-speed points gained since the compact full-matrix baseline, with this commit removing same-speed VISITOR3 low CD pressure. |
| 2026-05-12 | `walkstuf1-high-rg178-194-v339` | Log-only; source restored and PS1 rebuilt. | Retest a current high-side low-risk read-plan group after v338. The binary question was whether the old `178..194` neighborhood had become safe after the accepted WALKSTUF1 high and VISITOR3 low baseline shifts. | Temporarily added `{178,194,0}` to `kWalkstuf1HighReadGroups12` and ran the focused high gate against the v316 high baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-v339-focused/20260512-041322-1944532/summary.json`. The first wrapper label did not match the baseline label, so the script did not enforce the baseline automatically; manual comparison to the v316 baseline is the gate result. | Failed/no promotion. The group reduced loop reads `69 -> 66`, but regressed scene `1768 -> 1774`, loop `1480 -> 1486`, target `1429 -> 1427`, overrun `51 -> 59`, blocking `85 -> 98`, and due misses `16 -> 17`; prefetch improved only `26 -> 23`. | Close this current high group. The read-count savings land in the wrong phase and are not worth the visible timing/blocking regression. |
| 2026-05-12 | `walkstuf1-high-rg344-360-v340` | `ps1: group walkstuf1 high sectors 344-360` | Take the next current-window-compatible high-side read-plan candidate after the `178..194` retest failed. The candidate `344..360` should merge two active-loop reads without touching low tide or the accepted early high groups. | Added `{344,360,0}` after `{201,213,0}` and `{213,229,0}` in `kWalkstuf1HighReadGroups12`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-rg344-360-v340-focused/20260512-041522-1956385/summary.json`; broad controls: `scratch/ps1-perf-iterate/walkstuf1-high-rg344-360-v340-controls/20260512-041718-1967510/summary.json`; WALKSTUF1 low control: `scratch/ps1-perf-iterate/walkstuf1-high-rg344-360-v340-control-walkstuf1-low/20260512-042124-1992222/summary.json`. | Promoted. WALKSTUF1 high keeps scene/loop flat at `1768/1480`, moves target `1429 -> 1432`, overrun `51 -> 48`, blocking `85 -> 83`, loop reads `69 -> 67`, loop-read VBlanks `301 -> 292`, and due misses `16`; hidden refill stays `26`. WALKSTUF1 low, BUILDING2 high/low, VISITOR3 low, and FISHING1 high controls stayed exact-flat. | Keep as the current WALKSTUF1 high baseline. Public rollup moves to `+0.3250%` over target / `99.6814%` target speed; raw signed rollup moves to `-0.4420%` / `100.4637%`. Methodology total is about `17.07` public over-target points removed and `12.58` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-rg371-387-v341` | Log-only; source restored and PS1 rebuilt. | Probe the only unclosed low-side current-window candidate with a lower visible-risk score after v340. The `371..387` range covers three late reads with no partial overlap and short, not tight, gaps, so it was the best remaining scalar table candidate before switching back to generated scheduler/data-shape work. | Temporarily added a low-tide WALKSTUF1 read table with `{371,387,0}` and routed low tide through it while preserving the accepted high groups. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg371-387-v341-focused/20260512-044915-2146995/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the probe. | Failed/no promotion. The group fired and reduced loop reads `67 -> 65`, but public playback regressed: scene `1776 -> 1780`, active loop `1484 -> 1488`, overrun `53 -> 56`, blocking `72 -> 75`, and hidden refill `22 -> 26`; due misses stayed `12`. | Close `371..387` as a scalar low table. This repeats the WALKSTUF1 low pattern: raw table grouping can cut transactions, but the coalesced append lands in a phase that worsens visible and hidden pressure. Next low work should use generated deadline ownership or pack/data-shape changes, not more hand-authored low read groups. |
| 2026-05-12 | `walkstuf1-low-f188-resident-slot39-v342` | Log-only; pack restored and PS1 rebuilt. | Try a VISITOR3-style resident-slot swap instead of another low read group. Frame `188` (`source_frame=305`, old sectors `383..387`) is part of the late `371..387` tight cluster; frame `39` (`source_frame=49`) starts in the already paid setup-prime tail at sector `75` with enough room to hold frame `188` fully before sector `78`. The binary question was whether moving one late tight read into setup and evicting one early long-gap frame to padded tail space would improve active-loop cadence without changing pack bytes or LBA. | Rewrote only the temporary `WALK1LOW.FG2`: copied frame `188` to frame `39`'s old offset `153867` (`75..78`), moved frame `39` to zero tail offset `920628` (`449..454`), and kept pack size `1535263` plus LBA `25633`. Transform summary: `scratch/walkstuf1-low-f188-resident-slot39-v342/summary.txt`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-f188-resident-slot39-v342-focused/20260512-050428-2232961/summary.json`. The pack was restored from `scratch/walkstuf1-low-f188-resident-slot39-v342/WALK1LOW.before-v342.FG2` and PS1 rebuilt after the gate failed. | Failed/no promotion. The swap regressed low hard: scene `1776 -> 1800`, active loop `1484 -> 1508`, target `1431 -> 1429`, overrun `53 -> 79`, blocking `72 -> 119`, hidden refill `22 -> 29`, loop reads `67 -> 68`, and due misses `12 -> 17`. | Close setup-tail resident swaps that evict early WALKSTUF1 low frames into padded tail space. The late read removal is outweighed by the new far-tail/reverse seek pattern (`443..459` showed up as a top candidate after the probe), so future pack-side work must preserve early-frame locality or create new setup-resident bytes by shrinking/aliasing payloads rather than tail-evicting them. |
| 2026-05-12 | `walkstuf1-low-setup-segment-sweep-v343-v345` | Log-only; source restored and PS1 rebuilt. | Test whether retained setup-side segment ownership could cover WALKSTUF1 low's late hot sectors without the tail-eviction damage from v342. The sweep tried `371..387` with the normal setup order, the same range primed before the contiguous setup window so the final setup read ended near the prefix, and a smaller `190..202` segment near the previously modeled lower-risk neighborhood. | Temporarily added a low-tide WALKSTUF1 setup-segment branch in `fgRuntimePrimeSetupSegment()` and rebuilt each variant. Focused artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-seg371-387-v343-focused/20260512-051206-2276124/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-seg371-387-before-window-v344-focused/20260512-051634-2301644/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-low-seg190-202-v345-focused/20260512-051836-2313604/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline afterward. | Failed/no promotion. The original-order `371..387` segment had the only useful signal, improving loop/overrun `1484 -> 1480` and `53 -> 50`, but it still regressed blocking `72 -> 76`, hidden refill `22 -> 24`, setup cost, and due misses `12 -> 13`. The before-window ordering reduced reads `67 -> 66` but regressed loop/overrun/blocking to `1485`, `56`, and `80`. The smaller `190..202` segment had no key win: loop `1486`, overrun `57`, blocking `79`, hidden refill `24`, and due misses `13`. | Close these hand-owned WALKSTUF1 low setup segments. Extra setup reads and CD-head phase shifts outweigh the active-loop coverage, so the next low work should avoid new setup reads and instead use pack-side byte shrink/aliasing that preserves early locality or generated deadline scheduling that owns exact read phase. |
| 2026-05-12 | `walkstuf1-high-rg156-164-v346` | Log-only; source restored and PS1 rebuilt. | Test the highest-ranked current-compatible WALKSTUF1 high read-plan row after v340. The `156..164` group had the best visible-cost score among unclosed high-side groups, saved one modeled read, and had a long first visible gap, so it was the next binary high-side scalar probe before moving to BUILDING2 or pack-shape work. | Temporarily added `{156,164,0}` before the accepted high groups `{201,213}`, `{213,229}`, and `{344,360}` in `kWalkstuf1HighReadGroups12`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg156-164-v346-focused/20260512-053045-2381845/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt to the accepted baseline after the gate failed. | Failed/no promotion. The added group regressed high from the v340 baseline: scene `1768 -> 1774`, active loop `1480 -> 1486`, target `1432 -> 1427`, overrun `48 -> 59`, blocking `83 -> 102`, and due misses `16 -> 17`; hidden refill improved only `26 -> 23`, and loop reads stayed `67`. | Close `156..164` as another high-side scalar read group. Even a visually lower-risk retained group perturbs high cadence more than it helps, so remaining WALKSTUF1 high work should move toward generated deadline ownership or pack-side payload removal instead of more hand-authored group rows. |
| 2026-05-12 | `building2-high-rg249-257-v347` | `ps1: group building2 high sectors 249-257` | After WALKSTUF1 scalar lanes regressed cadence, test the cleanest non-WALKSTUF1 under-99 row with a visible current-window grouping signal. The BUILDING2 high `249..257` cluster should remove one read and a few CD VBlanks without touching low tide or pack layout. | Added `{249,257,0}` after existing `{60,72}` in `kBuilding2HighReadGroups12`. Focused gate: `scratch/ps1-perf-iterate/building2-high-rg249-257-v347-focused/20260512-053606-2412312/summary.json`; selected controls: `scratch/ps1-perf-iterate/building2-high-rg249-257-v347-controls/20260512-053754-2422756/summary.json`, `scratch/ps1-perf-iterate/building2-high-rg249-257-v347-control-walkstuf1-high/20260512-054054-2441218/summary.json`, and `scratch/ps1-perf-iterate/building2-high-rg249-257-v347-control-walkstuf1-low/20260512-054204-2448444/summary.json`. | Promoted as same-speed CD-pressure/read-time reduction. BUILDING2 high scene/loop/target/overrun stay flat at `1603`, `1352/1311`, and `41`; blocking improves `56 -> 55`, hidden refill `20 -> 19`, loop reads `62 -> 61`, loop-read VBlanks `266 -> 262`, and due misses stay `7`. Pack LBA/sectors and PS-EXE bucket are fixed. BUILDING2 low, VISITOR3 low, FISHING1 high, and WALKSTUF1 high/low controls stayed exact-flat. | Keep as the current BUILDING2 high baseline. Public rollup remains `+0.3250%` over target / `99.6814%` target speed; raw signed remains `-0.4420%` / `100.4637%`; methodology totals remain about `17.07` public over-target points removed and `12.58` target-speed points gained, with same-speed CD pressure removed from BUILDING2 high. |
| 2026-05-12 | `building2-high-rg226-238-v348` | Log-only; source restored and PS1 rebuilt. | Try the highest-ranked current BUILDING2 high follow-up after v347. The `226..238` row was a 12-sector current-read-group-compatible candidate with two modeled read saves, but tight visible gaps made it a cadence risk. | Temporarily inserted `{226,238,0}` between the accepted `{60,72}` and `{249,257}` groups in `kBuilding2HighReadGroups12`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg226-238-v348-focused/20260512-055421-2516954/summary.json`. Source was restored to v347 and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Public timing, visible blocking, hidden refill, and due misses were exact-flat against v347 (`1603`, `1352/1311`, overrun `41`, blocking `55`, hidden refill `19`, due `7`). The group reduced loop reads `61 -> 59`, but loop-read VBlanks worsened `262 -> 263`, total read VBlanks worsened `402 -> 403`, and hidden read time rose `207 -> 208`. | Close `226..238` as a hand-authored BUILDING2 high group. The next high path should not count transaction reduction alone as a win; it needs read-time/blocking relief, generated deadline ownership, or a pack-side shape change that reduces bytes without moving hidden cost backward. |
| 2026-05-12 | `building2-low-rg67-73-v349` | Log-only; source restored and PS1 rebuilt. | Try the safest-looking current BUILDING2 low read-plan row: `67..73` had a six-second first visible gap and one modeled read save near setup-prime tail coverage. Because the unguarded result had mixed pressure, also test the binary guarded form with a `4`-VBlank minimum slack. | Temporarily added `{67,73,0}` to `kBuilding2LowReadGroups12`, then changed it to `{67,73,4}` for the guarded follow-up. Focused artifacts: unguarded `scratch/ps1-perf-iterate/building2-low-rg67-73-v349-focused/20260512-060032-2552332/summary.json`; guarded `scratch/ps1-perf-iterate/building2-low-rg67-73-slack4-v349-focused/20260512-060232-2564311/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Unguarded improved visible blocking `81 -> 76` and loop reads `55 -> 54`, but regressed scene `1619 -> 1621`, loop `1349 -> 1351`, target `1318 -> 1316`, overrun `31 -> 35`, and hidden refill `1 -> 3`. The guarded variant was exact-flat at `1619`, `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, loop reads `55`, and due `18`. | Close `67..73` as a local BUILDING2 low table. The unguarded variant moves work into hidden/target debt, and the guarded variant simply suppresses the useful append. Continue low-side work with generated placement or pack-side byte/cleanup reduction instead of setup-tail scalar grouping. |
| 2026-05-12 | `skip-zero-restore-mark-v350` | Log-only; source restored and PS1 rebuilt. | Test a tiny hot-path instrumentation/work-shape cleanup before spending runtime on a new VISITOR3 pack format: skip `ps1PerfMarkRestore(0)` calls in FGP3 residual/direct restore paths while preserving all nonzero restore accounting. | Temporarily guarded five `ps1PerfMarkRestore(restoredBytes)` call sites in `src/graphics_ps1.c` with `restoredBytes != 0`. First VISITOR3 low run exited `137` without `JCPERF2` and is kept only as structural noise: `scratch/ps1-perf-iterate/skip-zero-restore-mark-v350-visitor3-low/20260512-061019-2608214/visitor3-low-canary/headless-regtest.log`. Rerun artifact: `scratch/ps1-perf-iterate/skip-zero-restore-mark-v350-visitor3-low-rerun/20260512-061213-2620278/summary.json`. Source was restored afterward. | Failed/no promotion. The completed rerun was exact-flat against VISITOR3 low v338: scene `1401`, loop `1072/1040`, overrun `32`, blocking `58`, hidden refill `0`, and due misses `10`; no key metric improved. The source also shifted hot symbols (`foregroundPilotPlay +8`, graphics helpers +40), so it is not useful as a headroom pass. | Close zero-restore perf-marker pruning. VISITOR3 needs generated pack-side/direct-upload ownership or scheduler metadata, not measurement-marker trimming. |
| 2026-05-12 | `walkstuf1-high-rmc-second-v351` | Log-only; pack restored and PS1 rebuilt. | Try a pack-only second restore-minus-current pass on the largest remaining row after scalar read groups failed. The existing transformer reported a tiny restore-byte reduction (`725714 -> 725550`) at the cost of a small active-payload increase (`918345 -> 918551`). | Temporarily replaced `generated/ps1/foreground/WALKSTUF1.FG2` with `scratch/fgp3-existing-transform-scan-v351/WALKSTUF1.FG2.rmc` after backing up the accepted pack to `scratch/walkstuf1-high-rmc-second-v351/WALKSTUF1.before-v351.FG2`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rmc-second-v351-focused/20260512-061733-2650926/summary.json`. The harness did not match the baseline label, so the result was manually compared to the v340 high baseline. | Failed/no promotion. The probe regressed high hard: scene `1768 -> 1784`, active loop `1480 -> 1496`, target `1432 -> 1416`, overrun `48 -> 80`, blocking `83 -> 120`, hidden refill `26 -> 35`, loop reads `67 -> 74`, and due misses `16 -> 19`. | Close second-pass restore-minus-current for WALKSTUF1 high. The small restore-byte reduction is overwhelmed by payload/read/cadence damage; future WALKSTUF1 work needs generated deadline ownership or a shape change that removes bytes without increasing active payload. |
| 2026-05-12 | `visitor3-motion-aware-existing-transform-scan-v352a` | Host-side log-only; no pack/source change. | Before writing a new VISITOR3 codec, check whether the existing restore-minus-current or draw-tail transforms can still affect normal FGP3/v4 entries if motion-marker and intentional no-op payloads are skipped byte-identically. | Ran a scratch motion-aware wrapper over `scripts/compact-fgp3-restore-minus-current.py` and `scripts/compact-fgp3-trim-draw-tails.py` for `VISITOR3.FG2` and `VIST3LOW.FG2`, leaving `0xffff` motion-marker entries untouched and measuring per-entry deltas rather than promoting a rewritten pack. Scratch outputs were written under `scratch/visitor3-motion-aware-existing-transform-v352/`. | Failed/no promotion before emulator. VISITOR3 high had zero positive normal-entry deltas for both transforms. VISITOR3 low had no draw-tail trim and only one restore-minus-current candidate, frame `143`, which grew `2857 -> 2861` bytes. The remaining parser failures are already-known intentional no-op/motion-hold payloads (`119`, `120`, and terminal no-op rows), not missed compactable residuals. | Close the existing-transform VISITOR3 scan. The next VISITOR3 work needs a new generated data shape, precomposed strip/sidecar, or deadline-owned scheduler metadata; the old cleanup/draw-tail transformers have no remaining positive payload to mine. |
| 2026-05-12 | `building2-high-rg83-95-v352` | Log-only; source restored and PS1 rebuilt. | Test the only current BUILDING2 high planner row classified as lower-visible-risk after the tight `226..238` lane failed. The `83..95` group was append-start fireable, fit the existing grouped-read capacity, and modeled one saved read with medium gaps. | Temporarily inserted `{83,95,0}` between the accepted high groups, rebuilt, and ran the focused gate. Then tried the binary guarded form `{83,95,4}` before closing the row. Focused artifacts: unguarded `scratch/ps1-perf-iterate/building2-high-rg83-95-v352-focused/20260512-063524-2750153/summary.json`; guarded `scratch/ps1-perf-iterate/building2-high-rg83-95-slack4-v352-focused/20260512-063656-2759460/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Both variants produced the same result against v347: scene `1603 -> 1602` and loop `1352 -> 1351` moved down by one, but target fell `1311 -> 1309`, overrun regressed `41 -> 42`, blocking regressed `55 -> 56`, hidden refill regressed `19 -> 20`, and loop reads only improved `61 -> 60`; due misses stayed `7`. | Close BUILDING2 high `83..95` as another hand-authored group. It proves the planner's lower-risk row still steals target/refill cadence; the remaining BUILDING2 high work should move to generated deadline ownership or pack-side byte reduction rather than more source table rows. |
| 2026-05-12 | `building6-readgroups16-v353` | Log-only; source restored and PS1 rebuilt. | Test whether BUILDING6 can get a small same-window CD win without repeating the failed `48 KiB` scalar-window lane. The current v165 read plan ranked shared `181..197` and `269..285` 16-sector clusters as current-group-compatible and modeled one saved read each. | Temporarily added a BUILDING6 read-group table with `{181,197,0}` and `{269,285,0}` while keeping the normal `24 KiB` read window. Focused artifact: `scratch/ps1-perf-iterate/building6-readgroups16-v353-focused/20260512-070511-2916263/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The groups never fired (`group_hits=0`) and `loop_reads` stayed `42` on both tides, so the apparent loop movement was just phase noise plus source-size damage. High moved `2482/2457 -> 2479/2453`, but overrun regressed `25 -> 26`, blocking `25 -> 26`, and hidden refill `27 -> 29`. Low moved `2485/2456 -> 2482/2456`, but hidden refill regressed `29 -> 30`. The source table also crossed the PS-EXE bucket `215040 -> 217088` and shifted BUILDING6 pack LBAs by one sector. | Close standalone BUILDING6 hand-authored read groups for direct-stage clusters. The active reads are intercepted by the exact-slack direct-stage path, so a table row can add hot code without saving a transaction. Remaining BUILDING6 work needs generated direct-stage ownership, a shrinking data-shape encoder, or a same-bucket code-neutral scheduler change. |
| 2026-05-12 | `johnny6-compact-fgp3-v354` | `ps1: compact johnny6 foreground packs` | Clear a yellow-band row pair without source code by applying the proven compact-FGP3 metadata pipeline to JOHNNY6's current high/low packs. Both tides still had the older clean-pressure prefetch-relief rows at `2832/2800`; the binary question was whether compact cleanup metadata plus restore-minus-current could remove enough read pressure while preserving the `402498` byte footprint, pack LBAs, and PS-EXE bucket. | Ran residual metadata compaction, PAL4 draw-metadata compaction, restore-minus-current, and draw-tail trim on `JOHNNY6.FG2` and `JOHN6LOW.FG2`, padding each candidate back to the original `402498` bytes. Same-commit control artifact: `scratch/ps1-perf-iterate/johnny6-current-v354-control/20260512-072329-3019584/summary.json`; confirm gate: `scratch/ps1-perf-iterate/johnny6-compact-fgp3-v354-confirm/20260512-072910-3052754/summary.json`. | Promoted. Both candidate packs keep the same size and fixed layout (`pack_lba=15304/15501`, `pack_sectors=197`, PS-EXE bucket `217088`). High improves `scene_vb 2941 -> 2939`, active loop `2832/2800 -> 2829/2802`, overrun `32 -> 27`, blocking/refill `29 -> 24`, and loop reads `12 -> 7`. Low improves `2941 -> 2939`, `2832/2800 -> 2830/2802`, overrun `32 -> 28`, blocking/refill `29 -> 25`, and loop reads `12 -> 7`. | Keep as the current JOHNNY6 baseline. Public rollup moves to `+0.3224%` over target / `99.6839%` target speed; raw signed rollup moves to `-0.4446%` / `100.4662%`. Green rows rise to `113 / 126`, yellow rows fall to `13 / 126`, and methodology total is about `17.08` public over-target points removed plus `12.58` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-current-rg-sweep-v355-v358` | Log-only; source restored and PS1 rebuilt. | After regenerating the current WALKSTUF1 read plan from the v354 baseline, test the remaining low-side lower-risk scalar retained-read rows before leaving this family. The sweep covered the combined groups `83..91`, `155..163`, and `237..245`, then each group alone, to distinguish a multi-cluster phase win from isolated append debt. | Temporarily added a low-tide WALKSTUF1 read-group table and routed low tide through the candidates while preserving accepted high-tide groups. Focused artifacts: combined `scratch/ps1-perf-iterate/walkstuf1-low-rg83-91-155-163-237-245-v355-focused/20260512-080625-3264614/summary.json`; `155..163` `scratch/ps1-perf-iterate/walkstuf1-low-rg155-163-v356-focused/20260512-080754-3273445/summary.json`; `83..91` `scratch/ps1-perf-iterate/walkstuf1-low-rg83-91-v357-focused/20260512-080922-3282129/summary.json`; `237..245` `scratch/ps1-perf-iterate/walkstuf1-low-rg237-245-v358-focused/20260512-081052-3291015/summary.json`. | Failed/no promotion. The combined table was the only visible-speed hint, improving scene/loop/overrun `1776/1484/53 -> 1774/1482/51`, but it failed the pressure gate with blocking `72 -> 76`, prefetch overrun `22 -> 24`, loop reads `67 -> 68`, and due misses `12 -> 13`. The isolated rows were worse: `155..163` regressed to `1783`, `1491/1423`, overrun `68`, blocking `101`, hidden `26`, due `17`; `83..91` regressed to `1784`, `1492/1429`, overrun `63`, blocking `86`, hidden `25`, due `13`; `237..245` regressed to `1784`, `1492/1428`, overrun `64`, blocking `80`, hidden `27`. | Close the current low-side hand-authored retained-read table family. The residual groups either reduce loop time while worsening blocking/refill/due pressure or regress outright; WALKSTUF1 low needs generated deadline ownership, code-neutral pack metadata, or pack-side byte/geometry removal rather than more scalar read groups. |
| 2026-05-12 | `building2-dup-alias-v359-v361` | Log-only; packs restored and PS1 rebuilt. | Try a pack-only duplicate-payload alias for BUILDING2 high/low. Both packs contain `67,719` bytes of exact duplicate FGP3/v4 residual payloads; the binary question was whether entry-table aliases could remove those reads without changing file size, pack LBA, PS-EXE bucket, or runtime code. | Took a fresh current-layout baseline at `scratch/ps1-perf-iterate/building2-dup-alias-v359-baseline/20260512-082613-3378245/summary.json`. Then tested three table-only variants: all duplicate aliases (`62` aliases per tide, `67,719` logical bytes), window-distance-limited aliases (`44` aliases, `64,040` bytes), and adjacent-only aliasing (`1` alias, `6,656` bytes). Artifacts: `scratch/ps1-perf-iterate/building2-dup-alias-v359-focused/20260512-082908-3395324/summary.json`, `scratch/ps1-perf-iterate/building2-dup-alias-window16-v360-focused/20260512-083256-3417671/summary.json`, and `scratch/ps1-perf-iterate/building2-dup-alias-adjacent-v361-focused/20260512-083552-3435754/summary.json`; transform summaries are under `scratch/building2-dup-alias-v359/`, `scratch/building2-dup-alias-window16-v360/`, and `scratch/building2-dup-alias-adjacent-v361/`. | Failed/no promotion. Full aliasing introduced mixed/reverse seek debt and regressed high `1352/1311 -> 1454/1305`, blocking `55 -> 195`, due `7 -> 28`; low regressed `1349/1318 -> 1457/1314`, blocking `81 -> 228`, due `18 -> 44`. The 16 KiB-limited form was smaller but still regressed high to `1368/1307`, blocking `80`, hidden `27`, due `11`, and low to `1369/1318`, blocking `101`, hidden `2`, due `21`. The adjacent-only form was exact-flat on all key metrics and failed `--require-improvement`. | Close naive duplicate-payload aliasing for BUILDING2. Duplicate bytes are real, but pointing entries backward creates CD-head/window churn unless the scheduler has explicit alias ownership; adjacent aliases are too small to move timing. Future duplicate work needs forward-order repacking with generated copy/no-op semantics or a runtime-supported alias cache, not raw table backreferences. |
| 2026-05-12 | `building2-low-setupseg67-73-v362` | Log-only; source restored and PS1 rebuilt. | Test whether BUILDING2 low can buy out its first uncovered active-loop cluster by spending setup time instead of runtime read-group ownership. The current low setup-prime covers sectors `3..67`, while the top low candidate is `67..73`; the probe adds that six-sector range as a reusable low-only setup segment. | Temporarily added `FG_BUILDING2_LOW_SETUP_SEGMENT_START/BYTES` and a low-tide `building2` branch in `fgRuntimePrimeSetupSegment()`, allocating `12 KiB` in the existing setup-segment buffer. Focused artifact: `scratch/ps1-perf-iterate/building2-low-setupseg67-73-v362-focused/20260512-084348-3480725/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. High stayed exact-flat as a control, but low regressed despite one fewer loop read: `scene_vb 1619 -> 1630`, active loop `1349/1318 -> 1354/1314`, overrun `31 -> 40`, blocking `81 -> 132`, due misses `18 -> 29`, and prefetch overrun stayed `1`. | Close additive BUILDING2 low setup segments at the prime edge. Moving the first cluster to setup changes phase and due ownership in the wrong direction; this row needs generated deadline placement or pack-side byte reduction that keeps target cadence intact. |
| 2026-05-12 | `johnny1-window-slack4-v363` | Log-only; source restored and PS1 rebuilt. | Test whether JOHNNY1 high/low can trade a little more held slack for less hidden refill and move the last sub-99% black-backdrop rows into green. Both current tides are due-miss-free and sit at fresh same-layout `1973/1945`, with blocking/refill `25/25` and only `7` loop reads. | Temporarily added a JOHNNY1-specific `4` VBlank window minimum in `fgRuntimeWindowMinSlackVBlanks()`. Fresh baselines: high `scratch/ps1-perf-iterate/johnny1-window-slack4-v363-high-baseline/20260512-090028-3577351/summary.json`; low `scratch/ps1-perf-iterate/johnny1-window-slack4-v363-low-baseline/20260512-085903-3568971/summary.json`. Focused artifacts: high `scratch/ps1-perf-iterate/johnny1-window-slack4-v363-high-focused/20260512-090214-3588017/summary.json`; low `scratch/ps1-perf-iterate/johnny1-window-slack4-v363-low-focused/20260512-090339-3596611/summary.json`. | Failed/no promotion. Both tides produced the same phase tradeoff: loop stayed flat at `1973`, target improved `1945 -> 1946`, overrun improved `28 -> 27`, and hidden refill improved `25 -> 24`, but visible blocking regressed `25 -> 28`, loop reads rose `7 -> 10`, prefetch hits dropped `111 -> 110`, and one due miss appeared. | Close JOHNNY1 scalar window-slack tuning. The current 3-VBlank knee is already the safe point; stricter slack only defers window work into visible reads. Future JOHNNY1 work should use generated append ownership, a larger retained-window design, or another pack/data-shape reduction rather than local slack constants. |
| 2026-05-12 | `building6-window-slack4-v364` | `ps1: tighten building6 window refill slack` | Retest the slack idea on BUILDING6 after JOHNNY1 proved it was scene-sensitive. Fresh BUILDING6 current rows still had high `2479/2453`, low `2482/2456`, and high hidden refill, so the binary question was whether one more held VBlank would prevent refill overrun without starving visible reads. | Added a BUILDING6-specific `4` VBlank return in `fgRuntimeWindowMinSlackVBlanks()`. Fresh baselines: high `scratch/ps1-perf-iterate/building6-current-v364-high-baseline/20260512-091009-3634721/summary.json`; low `scratch/ps1-perf-iterate/building6-current-v364-low-baseline/20260512-091137-3643383/summary.json`. Focused gates: high `scratch/ps1-perf-iterate/building6-window-slack4-v364-high-focused/20260512-091354-3656745/summary.json`; low `scratch/ps1-perf-iterate/building6-window-slack4-v364-low-focused/20260512-091523-3665609/summary.json`. Canaries passed for BUILDING2 high/low, WALKSTUF1 high/low, FISHING1 high, and ACTIVITY9 low. | Promoted. High improves `scene_vb 2744 -> 2736`, active loop `2479/2453 -> 2471/2456`, overrun `26 -> 15`, blocking `26 -> 20`, and hidden refill `29 -> 16`. Low improves `2748 -> 2740`, `2482/2456 -> 2474/2455`, overrun `26 -> 19`, blocking `28 -> 24`, and hidden refill `30 -> 20`. Both tides keep `loop_reads=42` and accept one due miss. | Keep as the current BUILDING6 baseline. BUILDING6 high/low both move into green, raising the matrix to `115 / 126` green rows and `11 / 126` yellow rows. Public rollup moves to `+0.3159%` over target / `99.6903%` target speed; raw signed rollup moves to `-0.4511%` / `100.4726%`. Methodology total is about `17.08` public over-target points removed plus `12.59` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-seg291-307-v365` | Log-only; source restored and PS1 rebuilt. | Try the current read-plan's largest unclosed WALKSTUF1 low signal without adding another active-loop read table. The range `291..307` modeled three saved reads, but its visible gaps were tight; the binary question was whether setup-owned reusable residency could remove the active-loop cluster without the grouped-append phase damage seen in prior low probes. | Temporarily added `FG_WALKSTUF1_LOW_SETUP_SEGMENT_START/BYTES` for sectors `291..307` and a low-tide `walkstuf1` branch in `fgRuntimePrimeSetupSegment()`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-seg291-307-v365-focused/20260512-095034-3869248/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The setup segment did save some active-loop work: loop `1484 -> 1479`, overrun `53 -> 50`, hidden refill `22 -> 21`, loop reads `67 -> 65`, and loop-read VBlanks `288 -> 278`. It failed the gate because scene time regressed `1776 -> 1783`, target fell `1431 -> 1429`, visible blocking regressed `72 -> 92`, and due misses rose `12 -> 16`. | Close additive setup-owned `291..307` for WALKSTUF1 low. The saved-read signal is real but lands in the wrong setup/CD phase; future low work should shrink or relocate payloads without adding setup reads, or use generated deadline ownership rather than a fixed setup segment. |
| 2026-05-12 | `walkstuf1-low-rmc-second-v366` | Log-only; pack restored. | Try the existing pack-side restore-minus-current transform on WALKSTUF1 low before investing in a new data shape. The transform had a weak host-side signal: restore bytes fell by about `2 KB`, but active payload grew by about `1.2 KB`. | Generated `scratch/walkstuf1-low-transform-scan-v366/WALK1LOW.rmc.FG2` with `compact-fgp3-restore-minus-current.py --pad-to-input-size`, backed up the accepted pack to `scratch/walkstuf1-low-transform-scan-v366/WALK1LOW.before-v366.FG2`, temporarily replaced `generated/ps1/foreground/WALK1LOW.FG2`, and ran the focused low gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rmc-second-v366-focused/20260512-095705-3905755/summary.json`. The original pack was restored byte-for-byte afterward. | Failed/no promotion. The tiny restore-byte win was overwhelmed by payload/read cadence damage: scene `1776 -> 1785`, active loop `1484/1431 -> 1493/1426`, overrun `53 -> 67`, blocking `72 -> 93`, hidden refill `22 -> 24`, loop reads `67 -> 69`, and due misses `12 -> 14`. | Close second-pass restore-minus-current for WALKSTUF1 low. The old transform family is exhausted for both tides; future work needs a new phase-preserving pack shape, selective metadata, or generated scheduler ownership. |
| 2026-05-12 | `walkstuf1-low-primecap176-v367` | Log-only; source restored and PS1 rebuilt. | Retest whether the current low-tide contiguous setup-prime ceiling was still the blocker after the v331 staged-prepare and v364 baseline shifts. The old `168 KiB` cap probe was exact-flat on a much older baseline, so this tried a larger `176 KiB` current-baseline cap before writing more complex residency code. | Temporarily changed `FG_WALKSTUF1_LOW_SETUP_PRIME_MAX_RESIDENT_BYTES` from `160 KiB` to `176 KiB`, rebuilt, and ran the focused low gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-primecap176-v367-focused/20260512-100513-3951989/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The probe was exact-flat: scene `1776`, active loop `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, loop reads `67`, loop-read VBlanks `288`, and due misses `12`. The emitted read plan still shows `prime=[2, 78]`, meaning the larger byte cap does not buy additional useful contiguous coverage for the accepted low pack. | Close scalar low setup-prime cap growth above `160 KiB` on the current baseline. The remaining WALKSTUF1 low work needs noncontiguous generated residency, pack-side shrink/relocation, or scheduler-owned timing; a larger contiguous cap is just inert under this policy. |
| 2026-05-12 | `walkstuf1-high-rg360-376-v368` | Log-only; source restored and PS1 rebuilt. | Test the next high-tide current read-plan row after the accepted `{344,360}` promotion. The `360..376` group looked like a low-visible-risk follow-up that might cut another late append without touching low tide. | Temporarily appended `{360,376,0}` to `kWalkstuf1HighReadGroups12`, rebuilt, and ran a focused high gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg360-376-v368-focused/20260512-101635-4016837/summary.json`. The perf wrapper printed PASS because the case label did not match the baseline label; the row was manually compared to `scratch/ps1-perf-iterate/walkstuf1-high-rg344-360-v340-focused/20260512-041522-1956385/summary.json`. | Failed/no promotion. The group reduced loop reads `67 -> 65` and loop-read VBlanks `292 -> 289`, but public playback regressed: scene `1768 -> 1774`, active loop `1480 -> 1486`, target `1432 -> 1431`, overrun `48 -> 55`, blocking `83 -> 87`, and hidden refill `26 -> 28`; due misses stayed `16`. | Close direct high read-group `360..376`. The follow-up read saving is real but costs more cadence than it removes; future WALKSTUF1 high work should move to generated scheduler ownership, setup-safe noncontiguous residency, or pack-side byte/geometry reduction. |
| 2026-05-12 | `building2-high-rg17-33-v369` | Log-only; source restored and PS1 rebuilt. | Try the strongest unclosed BUILDING2 high early current-read candidate after the later-cluster hand groups had either regressed or stayed inert. The `17..33` span modeled three saved reads and was not part of the already closed `226..238` / `83..95` families. | Temporarily inserted `{17,33,0}` before the accepted BUILDING2 high groups, rebuilt, and ran a focused high gate against the v347 baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg17-33-v369-focused/20260512-102204-4048790/summary.json`. | Failed/no promotion. The group was exact-flat against v347 on every key metric: scene `1603`, active loop `1352/1311`, overrun `41`, blocking `55`, hidden refill `19`, loop reads `61`, loop-read VBlanks `262`, and due misses `7`. The only visible effect was a `+32` byte hot-symbol address shift. | Close BUILDING2 high `17..33` as an inert scalar group. The remaining high row needs generated scheduler ownership or pack-side byte/data-shape work; more direct table rows are not producing timing movement. |
| 2026-05-12 | `building2-high-rg17-33-directdeny-v370` | Log-only; source restored and PS1 rebuilt. | Check whether the v369 exact-flat result was caused by the direct-stage shortcut consuming the early cluster before grouped append could own it. This variant kept `{17,33,0}` and added a BUILDING2-high-only direct-stage deny when the next entry aligned to sector `17`. | Temporarily added the direct-stage guard in `fgRuntimeTryStageNextFrame()`, rebuilt, and reran the same focused high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-rg17-33-directdeny-v370-focused/20260512-102744-4080793/summary.json`. | Failed/no promotion. The result remained exact-flat against v347: scene `1603`, active loop `1352/1311`, overrun `41`, blocking `55`, hidden refill `19`, loop reads `61`, loop-read VBlanks `262`, and due misses `7`. The source probe only added code shape debt (`foregroundPilotPlay +64`). | Close this direct-stage-ownership variant. BUILDING2 high needs generated scheduler metadata or pack-side data-shape reduction; hand-coded exact-sector direct-stage exceptions are not moving the row. |
| 2026-05-12 | `visitor5-low-rg9-25-v372` | Log-only; source restored and PS1 rebuilt. | After rerunning current VISITOR5 low as v371 and confirming the public matrix row is current, test the safest visible-cost read-plan row before leaving this under-99 tail. The `9..25` group modeled one saved read with a long first gap and lower risk than the terminal tight cluster. | Temporarily added a `kVisitor5LowReadGroups12` table with `{9,25,0}` and selected it only for low-tide `visitor5`, then gated against the v371 current rerun. Artifact: `scratch/ps1-perf-iterate/visitor5-low-rg9-25-v372-focused/20260512-103519-4124809/summary.json`. | Failed/no promotion. The row stayed exact-flat: scene `1371`, active loop `1112/1090`, overrun `22`, blocking `12`, hidden refill `12`, loop reads `25`, loop-read VBlanks unchanged, and due misses `0`. The source only shifted hot symbols and grew `foregroundPilotPlay` by `48` bytes. | Close VISITOR5 low direct read group `9..25`; future work needs generated ownership or a pack/data-shape change, not a local scalar table. |
| 2026-05-12 | `johnny1-rg131-147-v373` | Log-only; source restored and PS1 rebuilt. | Test a symmetric JOHNNY1 grouped-window row after the slack-4 probe showed scalar window timing was already at the safe knee. Both tides' read plans ranked `131..147` as the top low-risk 16-sector candidate. | Temporarily added a shared `kJohnny1ReadGroups12` table with `{131,147,0}` and selected it for `johnny1`, then ran high tide against the fresh v363 high baseline. Artifact: `scratch/ps1-perf-iterate/johnny1-rg131-147-v373-high-focused/20260512-103943-4149723/summary.json`. | Failed/no promotion. High stayed exact-flat: scene `2069`, active loop `1973/1945`, overrun `28`, blocking/refill `25`, loop reads `7`, loop-read VBlanks unchanged, and due misses `0`; source grew `foregroundPilotPlay` by `32` bytes. Low was skipped because the shared high gate gave no key movement. | Close JOHNNY1 direct grouped-window rows until generated ownership or a pack/data-shape reduction changes the timing model. |
| 2026-05-12 | `walkstuf1-trim-copy-unparseable-v374` | Log-only tooling; no PS1 gate. | After scalar read tables and second-pass restore-minus-current transforms closed, test whether the generic FGP3/v4 draw-tail trim family was only blocked by the two WALKSTUF1 payload entries that the parser cannot decode. | Added an opt-in `--copy-unparseable` mode to `scripts/compact-fgp3-trim-draw-tails.py` so unparsable entries are copied byte-identically while parseable entries are still trimmed. Host scans wrote `scratch/walkstuf1-trim-skip-v374/WALKSTUF1.trim.FG2` and `scratch/walkstuf1-trim-skip-v374/WALK1LOW.trim.FG2`. | Failed/no promotion before emulator. Both current WALKSTUF1 packs copied entries `54` and `137` as unparsable and trimmed `0` bytes from all parseable entries: high active payload stayed `918345 -> 918345`, low stayed `916139 -> 916139`, `trimmed_entries=0`, `trimmed_pixels=0`, and `trimmed_bytes=0`. | Close generic draw-tail trimming for current WALKSTUF1 high/low. The parser tolerance stays useful for future mixed-format scans, but this family has no current byte signal; next WALKSTUF1 work should use a new pack shape, generated ownership metadata, or setup-local physical duplication rather than tail trimming. |
| 2026-05-12 | `walkstuf1-low-tailclone-v375` | Log-only; pack/source restored and PS1 rebuilt. | Test the smallest physical-duplication version of the resident-copy idea after strict in-prime cloning proved impossible: current WALK1LOW sectors `2..78` contain only `296` unused bytes before `data_offset`, so no late multi-KB payload fits inside already paid prime slack. | Temporarily copied low entries `148..149` (`9855` bytes, old sectors `291..297`) into zero-tail sector `450`, repointed only those two entry offsets, and added a low-tide reusable setup segment for `450..455`. Transform summary: `scratch/walkstuf1-low-tailclone-v375/summary.txt`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-tailclone-v375-focused/20260512-105621-50177/summary.json`. | Failed/no promotion. Manual comparison to the accepted v331 low baseline rejects the wrapper's label-mismatch pass: scene regressed `1776 -> 1794`, active loop/target regressed `1484/1431 -> 1494/1427`, overrun `53 -> 67`, blocking `72 -> 93`, hidden refill `22 -> 29`, loop reads `67 -> 69`, and due misses `12 -> 13`. | Close zero-tail physical clone plus setup segment for this low cluster. The tail copy preserves early-frame locality but still moves setup/CD phase in the wrong direction; future resident-copy work needs true in-prime slack created by a prior shrink, or generated scheduler ownership that can preload without leaving the CD head at the tail. |
| 2026-05-12 | `visitor3-high-motion-dispatch-v376` | Log-only; source restored and PS1 rebuilt. | Test a suspicious dead-code path before deeper VISITOR3 pack work: `graphics_ps1.c` has a compact motion-X decoder and current VISITOR3 packs contain `0xffff` marker payloads, but the compact temporal-residual dispatcher did not call that decoder. | Temporarily dispatched `cleanupRows == 0xffff` to `grCompositePacked4CompactMotionXToBackground()`, rebuilt, and ran VISITOR3 high against the current v338 high control. Artifact: `scratch/ps1-perf-iterate/visitor3-high-motion-dispatch-v376-focused/20260512-110503-98676/summary.json`. | Failed/no promotion. The extra dispatch crossed runtime/layout pressure and effectively disabled prefetch: high regressed from about `1071/1040`, overrun `31`, blocking `52`, reads `9`, due `9` to `scene_vb=1498`, active loop `1161/1031`, overrun `130`, blocking `505`, `policy=none`, loop reads `143`, and due misses `143`. | Do not enable the generic motion marker dispatch in this code shape. Existing marker payloads are part of the accepted pack/layout contract; using the dormant runtime path is much too expensive unless a future decoder is code-size-neutral and validated with memory/prefetch headroom first. |
| 2026-05-12 | `remove-dead-motion-decoder-v377` | `ps1: remove dormant compact motion decoder` | After v376 proved the generic motion-marker runtime path is phase-dangerous, remove the unused compact motion-X decoder and its helper code rather than leaving a tempting dead path in the graphics TU. This is a cleanup/headroom promotion, not a VBlank-speed promotion. | Deleted `GR_FGP3_MOTION_MARKER`, `GR_FGP3_MOTION_X_OPCODE`, `grMutableBgPixelAt()`, `grMoveBgSpanX()`, and `grCompositePacked4CompactMotionXToBackground()` from `src/graphics_ps1.c`. The PS1 build stayed in the `217088` byte sector bucket. Validation artifacts: VISITOR3 low `scratch/ps1-perf-iterate/remove-dead-motion-decoder-v377-visitor3-low/20260512-111340-148163/summary.json`; sequential WALKSTUF1 low `scratch/ps1-perf-iterate/remove-dead-motion-decoder-v377-walkstuf1-low-seq/20260512-111858-179277/summary.json`; sequential WALKSTUF1 high `scratch/ps1-perf-iterate/remove-dead-motion-decoder-v377-walkstuf1-high-seq/20260512-112013-186541/summary.json`; BUILDING2 high `scratch/ps1-perf-iterate/remove-dead-motion-decoder-v377-building2-high/20260512-112200-196961/summary.json`. | Promoted as exact-flat cleanup. VISITOR3 low stayed `1401`, `1072/1040`, overrun `32`, blocking `58`, reads/due `10/10`; WALKSTUF1 low stayed `1776`, `1484/1431`, overrun `53`, blocking `72`; WALKSTUF1 high stayed `1768`, `1480/1432`, overrun `48`, blocking `83`; BUILDING2 high stayed `1603`, `1352/1311`, overrun `41`, blocking `55`. Pack LBAs and the PS-EXE sector bucket stayed fixed on all completed gates. | Keep as a source hygiene baseline. This removes the accidental-runtime-dispatch trap exposed by v376, but it does not change the public battle card, methodology speed total, website rollup, or CSV scene matrix. Continue speed work on WALKSTUF1, BUILDING2, VISITOR3, VISITOR5, and JOHNNY1 under-99 rows. |
| 2026-05-12 | `walkstuf1-low-drawclip-holefill-v378` | Log-only; pack restored and PS1 rebuilt. | Try a zero-runtime, low-tide-only WALKSTUF1 pack swing after scalar setup/read lanes closed. The first pass clipped offscreen PAL4 draw spans while preserving every entry size/offset; follow-ups used the ignored clipped tail of already setup-prime frames as in-prime clones for later payloads, testing whether true paid-residency hole fill could remove late read pressure without adding setup reads. | Generated `scratch/walkstuf1-low-drawclip-v378/WALK1LOW.drawclip-preserve.FG2`, trimming `179604` offscreen draw pixels and `23012` spans across `91` frames without changing pack size. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-drawclip-preserve-v378-focused/20260512-114555-330619/summary.json`. Then generated a three-source clone variant (`37:57`, `35:190`, `33:215`) and a minimal single-clone variant (`35:190`), with focused artifacts `scratch/ps1-perf-iterate/walkstuf1-low-holefill-combo-v378-focused/20260512-114833-345697/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-holefill-35-190-v378-focused/20260512-115023-356253/summary.json`. The original `WALK1LOW.FG2` was restored byte-for-byte and the PS1 build was restored afterward. | Failed/no promotion. Broad size-preserving draw clipping regressed low from `1776`, `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, reads `67`, due `12` to `1780`, `1488/1426`, overrun `62`, blocking `86`, hidden refill `30`, reads `71`, due `14`. The three-clone hole fill was worse (`1791`, `1499/1421`, overrun `78`, blocking `113`, hidden `37`, reads `73`, due `16`). The minimal single clone still regressed (`1780`, `1488/1427`, overrun `61`, blocking `91`, hidden `23`, reads `68`, due `14`). | Close naive WALKSTUF1 low offscreen draw clipping and setup-prime tail hole fill. Even size-preserving CPU removal and true in-prime payload cloning shift frame/CD phase enough to create visible blocking or due misses; remaining low work needs generated phase ownership, a frame-split/sector-boundary planner, or a custom data format that preserves scheduler cadence while changing the work itself. |
| 2026-05-12 | `building2-high-rg226-242-v379` | `ps1: group building2 high sectors 226-242` | Revisit the current BUILDING2 high middle cluster after the narrower `226..238` row proved transaction-only. The binary question was whether the full `226..242` span crosses the read-time/blocking threshold when placed between accepted `60..72` and `249..257`. | Added `{226,242,0}` between `{60,72,0}` and `{249,257,0}` in `kBuilding2HighReadGroups12`. The PS1 build stayed in the `217088` byte sector bucket. Focused gate: `scratch/ps1-perf-iterate/building2-high-rg226-242-v379-focused/20260512-115949-410103/summary.json`; combined BUILDING2 low / VISITOR3 low controls completed flat under `scratch/ps1-perf-iterate/building2-high-rg226-242-v379-controls-a/20260512-120148-421332/`; FISHING1 high was rerun separately at `scratch/ps1-perf-iterate/building2-high-rg226-242-v379-control-fishing1/20260512-120404-435566/summary.json`; WALKSTUF1 high/low controls are `scratch/ps1-perf-iterate/building2-high-rg226-242-v379-control-walkstuf1-high/20260512-120512-442222/summary.json` and `scratch/ps1-perf-iterate/building2-high-rg226-242-v379-control-walkstuf1-low/20260512-120620-449241/summary.json`. | Promoted. BUILDING2 high improves scene `1603 -> 1602`, active loop `1352/1311 -> 1351/1313`, overrun `41 -> 38`, blocking `55 -> 50`, hidden refill `19 -> 17`, and loop-read VBlanks `262 -> 254`; loop reads stay `61`, due misses stay `7`, pack LBA/sectors stay `6181/637`. BUILDING2 low stayed `1349/1318`, VISITOR3 low stayed `1072/1040`, FISHING1 high stayed `1068/1072`, and WALKSTUF1 high/low stayed `1480/1432` and `1484/1431`. | Keep as the current BUILDING2 high baseline. Public rollup moves to `+0.3141%` over target / `99.6920%` target speed; raw signed rollup moves to `-0.4529%`. Methodology total is about `17.08` public over-target points removed plus `12.59` target-speed points gained since the compact full-matrix baseline, with this commit adding `0.0019` public over-target points removed and `0.0017` public target-speed points over the prior baseline. |
| 2026-05-12 | `walkstuf1-low-rg285-309-v380` | Log-only; source restored and PS1 rebuilt. | Re-test the full late WALKSTUF1 low cluster after v379 proved that expanding a previously failed/narrow read group can cross a read-time threshold. The binary question was whether the newer v331/v379 baseline made the old `285..309` late cluster promotable as a low-tide-only retained-read group. | Temporarily added a low-tide `kWalkstuf1LowReadGroups12` table with `{285,309,0}` and selected it only for `walkstuf1` low tide. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg285-309-v380-focused/20260512-123157-592346/summary.json`. The wrapper printed PASS only because the case label did not match the baseline; the row was manually compared to `scratch/ps1-perf-iterate/walkstuf1-low-prepare-before-window-v331-focused-baseline/20260512-020351-1214077/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The group fired and reduced loop reads `67 -> 63` plus due misses `12 -> 11`, but it regressed every public timing gate: scene `1776 -> 1782`, active loop/target `1484/1431 -> 1490/1433`, overrun `53 -> 57`, blocking `72 -> 76`, hidden refill `22 -> 26`, and loop-read VBlanks `288 -> 304`. | Close the full-cluster `285..309` retest on the current baseline. Larger retained groups can save reads, but this WALKSTUF1 low cluster still steals cadence from the visible loop; next low attempts need generated deadline ownership, sector-boundary frame planning, or a custom format that preserves phase rather than another plain retained table. |
| 2026-05-12 | `walkstuf1-low-rg190-202-v381` | Log-only; source restored and PS1 rebuilt. | Test the lowest visible-risk fireable WALKSTUF1 low retained-read candidate that was not already closed as a direct hand table. The `190..202` range had only one modeled read saved, but it was the planner's safest current-fit low row and had previously been rejected only as a setup segment, not as a retained-read group. | Temporarily added a low-tide `kWalkstuf1LowReadGroups12` table with `{190,202,0}` and routed only `walkstuf1` low tide through it. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg190-202-v381-focused/20260512-124323-657657/summary.json`. The focused gate used the matching `walkstuf1-low-canary` label against the accepted v331 baseline. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The group shifted the row into much worse CD pressure with no key win: scene `1776 -> 1784`, active loop/target `1484/1431 -> 1492/1423`, overrun `53 -> 69`, blocking `72 -> 90`, hidden refill `22 -> 34`, loop reads `67 -> 70`, loop-read VBlanks `288 -> 313`, and due misses `12 -> 13`. | Close direct retained grouping for `190..202`. WALKSTUF1 low's remaining fireable hand-table rows are either already closed or now clearly cadence-negative; move low work toward generated scheduler ownership or pack/frame-geometry changes that preserve phase. |
| 2026-05-12 | `walkstuf1-high-window53-55-v382` | Log-only; no source changed. | Recheck the old WALKSTUF1 high stream-window knee after the current binary/layout baseline moved. The older `53 KiB` boot override once looked promising but failed as a source default, so the current run tested both sides of the accepted `54 KiB` high window as parameter-only probes before touching code. | Ran high-tide boot overrides against the current v379 control baseline: `53 KiB` (`prefetch-window 54272`) at `scratch/ps1-perf-iterate/walkstuf1-high-window53-v382-param/20260512-125118-703753/summary.json` and `55 KiB` (`prefetch-window 56320`) at `scratch/ps1-perf-iterate/walkstuf1-high-window55-v382-param/20260512-125249-713341/summary.json`. Both runs used the matching `walkstuf1-high-rg213-229-slack4-v316-confirm` baseline label. | Failed/no promotion. `53 KiB` cut loop reads `67 -> 22` and due misses `16 -> 6`, but regressed scene `1768 -> 1820`, active loop/target `1480/1432 -> 1531/1434`, overrun `48 -> 97`, blocking `83 -> 115`, and hidden refill `26 -> 46`. `55 KiB` was worse on timing: scene `1828`, active loop/target `1539/1439`, overrun `100`, blocking `108`, hidden refill `49`, reads `19`, and due `5`. | Keep the accepted `54 KiB` high window. The current layout turns adjacent window sizes into lower-read but higher-latency schedules; future high work should target generated deadline ownership or a pack/data-shape change, not scalar window retunes around this knee. |
| 2026-05-12 | `walkstuf1-high-rg422-434-v383` | `ps1: group walkstuf1 high sectors 422-434` | After the adjacent-window retunes failed, test the smallest remaining high-tide tail read group with low visible-risk instead of another scalar scheduler knob. The goal was lower CD work on the current v379/v340 baseline without touching WALKSTUF1 low. | Appended `{422,434,0}` to `kWalkstuf1HighReadGroups12` after the accepted `{201,213}`, `{213,229}`, and `{344,360}` groups. The PS1 build stayed in the `217088` byte sector bucket. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-rg422-434-v383-focused/20260512-125740-741146/summary.json`. Sequential controls stayed flat: WALKSTUF1 low `scratch/ps1-perf-iterate/walkstuf1-high-rg422-434-v383-control-low/20260512-125856-748673/summary.json`, FISHING1 high `scratch/ps1-perf-iterate/walkstuf1-high-rg422-434-v383-control-fishing1/20260512-130015-756631/summary.json`, BUILDING2 high `scratch/ps1-perf-iterate/walkstuf1-high-rg422-434-v383-control-building2-high/20260512-130115-763409/summary.json`, and VISITOR3 low `scratch/ps1-perf-iterate/walkstuf1-high-rg422-434-v383-control-visitor3-low/20260512-130238-771727/summary.json`. | Promoted as same-speed CD-work reduction. WALKSTUF1 high timing stays exact-flat at scene `1768`, active loop `1480/1432`, overrun `48`, blocking `83`, hidden refill `26`, and due misses `16`, while loop reads improve `67 -> 66` and loop-read VBlanks improve `292 -> 286`. Pack LBA/sectors stay `24883/750`. | Keep as the current WALKSTUF1 high baseline. Public rollup remains `+0.3141%` over target / `99.6920%` target speed, raw signed remains `-0.4529%`, and methodology totals stay about `17.08` public over-target points removed plus `12.59` target-speed points gained. The new baseline has lower active-loop CD pressure but no VBlank-speed movement. |
| 2026-05-12 | `walkstuf1-high-rg444-456-v384` | `ps1: group walkstuf1 high sectors 444-456` | Continue the same tail-cluster lane after v383 lowered CD work with flat cadence. The read plan's next clean low-visible-risk row was `444..456`: append-start fireable, no overread, and one modeled saved read. | Appended `{444,456,0}` to `kWalkstuf1HighReadGroups12` after `{422,434}`. The PS1 build stayed in the `217088` byte sector bucket. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-rg444-456-v384-focused/20260512-131433-840569/summary.json`. Sequential controls stayed flat: WALKSTUF1 low `scratch/ps1-perf-iterate/walkstuf1-high-rg444-456-v384-control-low/20260512-131617-850672/summary.json`, FISHING1 high `scratch/ps1-perf-iterate/walkstuf1-high-rg444-456-v384-control-fishing1/20260512-131726-857345/summary.json`, BUILDING2 high `scratch/ps1-perf-iterate/walkstuf1-high-rg444-456-v384-control-building2-high/20260512-131825-863155/summary.json`, and VISITOR3 low `scratch/ps1-perf-iterate/walkstuf1-high-rg444-456-v384-control-visitor3-low/20260512-131932-869593/summary.json`. | Promoted as same-speed CD-work reduction. WALKSTUF1 high timing stays exact-flat at scene `1768`, active loop `1480/1432`, overrun `48`, blocking `83`, hidden refill `26`, and due misses `16`, while loop reads improve `66 -> 65`, loop-read VBlanks improve `286 -> 284`, hidden read VBlanks improve `203 -> 201`, and total read VBlanks improve `467 -> 465`. Pack LBA/sectors stay `24883/750`. | Keep as the current WALKSTUF1 high baseline. Public rollup remains `+0.3141%` over target / `99.6920%` target speed, raw signed remains `-0.4529%`, and methodology totals stay about `17.08` public over-target points removed plus `12.59` target-speed points gained. The new baseline removes another active-loop CD transaction but does not count as a VBlank-speed win. |
| 2026-05-12 | `walkstuf1-high-rg268-280-v385` | Log-only; source restored and PS1 rebuilt. | Try the next clean fireable current-window-compatible high-tide read-plan row after the accepted v384 tail group. The `268..280` candidate had no partial overlap or overread, fit the current grouped-read window, and modeled one saved read with low visible risk. | Temporarily inserted `{268,280,0}` between `{213,229}` and `{344,360}` in `kWalkstuf1HighReadGroups12`, then ran the focused high gate against the v384 summary. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg268-280-v385-focused/20260512-132816-919239/summary.json`. The wrapper printed PASS because the case label did not match the baseline row; the result was manually compared to v384. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The candidate did not reduce CD work and regressed public timing: scene `1768 -> 1772`, active loop/target `1480/1432 -> 1484/1424`, overrun `48 -> 60`, blocking `83 -> 98`, hidden refill `26 -> 27`, loop reads `65 -> 66`, loop-read VBlanks `284 -> 291`, and due misses `16 -> 17`. | Close direct retained grouping for `268..280`. Even the low-visible-risk mid-pack hand table steals phase on the current v384 baseline; remaining WALKSTUF1 high groups should move toward generated deadline ownership or pack-side byte/geometry removal instead of another local table. |
| 2026-05-12 | `building2-low-rg210-222-v386` | Log-only; source restored and PS1 rebuilt. | Move from exhausted WALKSTUF1 direct tables to the next under-99 CD-pressure row with a medium-risk planner candidate. BUILDING2 low `210..222` was fireable, fit the current grouped-read window, and modeled two saved reads without partial overlap; after the unguarded probe failed, the binary `minSlack=4` variant was tested before closing the row. | Temporarily inserted `{210,222,0}` into `kBuilding2LowReadGroups12`, then changed it to `{210,222,4}` for the guarded follow-up. The first run died before `JCPERF2`, so it was repeated for a valid gate. Valid artifacts: unguarded `scratch/ps1-perf-iterate/building2-low-rg210-222-v386-confirm/20260512-133603-964357/summary.json`; guarded `scratch/ps1-perf-iterate/building2-low-rg210-222-slack4-v386-focused/20260512-133738-973706/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Both valid forms produced the same regression against the current low baseline: scene `1619 -> 1621`, active loop/target `1349/1318 -> 1351/1317`, overrun `31 -> 34`, blocking stayed `81`, hidden refill worsened `1 -> 3`, reads improved `55 -> 53`, and due misses improved `18 -> 17`. The saved reads are real but convert into worse public cadence. | Close BUILDING2 low direct group `210..222`. Like `67..73`, this row needs generated refill ownership or pack-side byte/cleanup reduction; hand tables are now moving reads without lowering visible-loop debt. |
| 2026-05-12 | `building4-low-window-v387` | Log-only; source restored and PS1 rebuilt. | After BUILDING2 low's hand-table miss, test BUILDING4 low's best current fresh-fill/read-shape lane instead of another stale baseline. The fresh current baseline is `scratch/ps1-perf-iterate/building4-low-current-v387-baseline/20260512-135436-1071076/summary.json` at scene `3131`, active loop `2856/2816`, overrun `40`, blocking `44`, prefetch overrun `37`, and loop reads `30`. | First tried a low-tide-only retained append group for `178..202` with a 48 KiB retained capacity. The stale-baseline run is `scratch/ps1-perf-iterate/building4-low-rg178-202-v387-focused/20260512-135219-1058012/summary.json`; the fresh-baseline rerun is `scratch/ps1-perf-iterate/building4-low-rg178-202-v387-focused-rerun/20260512-135648-1084041/summary.json`. The corrected append group stayed exact-flat, proving the candidate is fresh-fill/window-owned rather than append-owned. Then tested scene-local stream windows at `48 KiB` and `40 KiB`: `scratch/ps1-perf-iterate/building4-low-window48-v387-focused/20260512-135930-1100249/summary.json` and `scratch/ps1-perf-iterate/building4-low-window40-v387-focused/20260512-140115-1110449/summary.json`. | Failed/no promotion. The append-group form had no key movement. The `48 KiB` fresh-fill window cut loop reads `30 -> 18` but regressed scene `3131 -> 3176`, active loop/target `2856/2816 -> 2897/2814`, overrun `40 -> 83`, blocking `44 -> 85`, and prefetch overrun `37 -> 87`. The `40 KiB` midpoint also failed: loop reads `30 -> 23`, but scene `3131 -> 3158`, active loop `2856 -> 2881`, overrun `40 -> 65`, blocking `44 -> 68`, and prefetch overrun `37 -> 71`. | Close BUILDING4 low append group `178..202` and scalar stream-window growth above `32 KiB`. The read-count signal is real, but larger fresh fills land in visible CD/refill debt; remaining BUILDING4 low work needs generated deadline ownership, pack-side byte/cleanup reduction, or selective preprocessing rather than larger raw windows. |
| 2026-05-12 | `building2-low-prepare-before-window-v388` | Log-only; source restored. | Test whether BUILDING2 low has the same visible-prepare ownership problem that v331 fixed for WALKSTUF1 low. The fresh same-source baseline is `scratch/ps1-perf-iterate/building2-low-current-v388-baseline/20260512-141651-1198625/summary.json` at scene `1619`, active loop `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, loop reads `55`, and due misses `18`. | Temporarily extended the staged-frame scheduler branch so low-tide `building2`, like accepted low-tide `walkstuf1`, prepares a staged visual frame before a speculative CD-window refill when the held frame is at the normal prepare slot. Focused artifact: `scratch/ps1-perf-iterate/building2-low-prepare-before-window-v388-focused/20260512-141806-1205867/summary.json`. The wrapper printed PASS because the probe label did not match the baseline label; the metrics were manually compared to the fresh v388 baseline. | Failed/no promotion. The scheduler branch was exact-flat: scene `1619`, active loop `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, loop reads `55`, loop-read VBlanks unchanged, and due misses `18`. No CD or visible timing counter moved. | Close BUILDING2 low prepare-before-window as an inert local scheduler variant. The remaining low row needs generated refill ownership, pack-side byte/cleanup reduction, or selective preprocessing; priority inversion alone does not change the accepted BUILDING2 low cadence. |
| 2026-05-12 | `scene-policy-cache-v389` | Log-only; source restored. | Try candidate-deck runtime-shape item 21: cache hot scene-policy flags at `foregroundPilotRuntimeStart()` and replace repeated `fgSceneEquals(gFgRuntime.sceneName, ...)` checks in the held-frame scheduler, window-slack policy, VISITOR3 fallthrough guard, and WALKSTUF1 prepare-before-window branch. The goal was to remove hot-loop string compares without changing scene data or CD policy. | Temporarily added a `scenePolicyFlags` byte to `TFgPilotRuntime`, populated it from the scene name at start, and used bit tests in the hot scheduler/prefetch helpers. The PS1 build stayed in the `217088` byte sector bucket. Artifacts: BUILDING2 low `scratch/ps1-perf-iterate/scene-policy-cache-v389-building2-low/20260512-142215-1230276/summary.json`, VISITOR3 high `scratch/ps1-perf-iterate/scene-policy-cache-v389-visitor3-high/20260512-142356-1239942/summary.json`, VISITOR3 low `scratch/ps1-perf-iterate/scene-policy-cache-v389-visitor3-low/20260512-142610-1252572/summary.json`, and WALKSTUF1 low `scratch/ps1-perf-iterate/scene-policy-cache-v389-walkstuf1-low/20260512-142724-1259867/summary.json`. | Failed/no promotion. BUILDING2 low, VISITOR3 low, and WALKSTUF1 low were exact-flat on all key metrics. VISITOR3 high failed to emit `JCPERF2` under the shifted layout, matching the known fragile high-row completion path but providing no promotable timing data. The source also shifted hot foreground symbols and did not produce code-size or work-volume savings. | Close cached scene-policy flags as a standalone runtime-shape optimization. If generated scheduler metadata later needs policy bits, emit them as part of that generated table; do not carry this manual cache for speed alone. |
| 2026-05-12 | `holiday-stamp-guard-v390` | Log-only; source restored. | Test a tiny global no-holiday fast path: all current perf boots use `holiday 0`, but `fgRuntimeComposeEntryToBackground()` still called `fgBackdropStampHoliday()` every composed frame and let the helper return immediately. The binary question was whether skipping the call at the hot compose site would save measurable VBlanks without changing holiday-on behavior. | Temporarily guarded the compose-site stamp call with `if (islandState.holiday > 0)`. The PS1 build stayed in the `217088` byte sector bucket. BUILDING2 low gate: `scratch/ps1-perf-iterate/holiday-stamp-guard-v390-building2-low/20260512-143120-1282222/summary.json`. WALKSTUF1 low follow-up was killed before `JCPERF2`: `scratch/ps1-perf-iterate/holiday-stamp-guard-v390-walkstuf1-low/20260512-143226-1288766/walkstuf1-low-canary/perf-summary.json`. | Failed/no promotion. BUILDING2 low stayed exact-flat at scene `1619`, active loop `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, reads `55`, and due `18`, while hot foreground symbols shifted by `+40` bytes. The WALKSTUF1 low run produced no timing metrics, so it cannot justify carrying the source change. | Close the standalone holiday-stamp guard as an optimization. It is semantically reasonable but too small to move the current headless counters and adds layout drift; retry only if bundled into a larger holiday/backdrop cleanup with stable symbol padding. |
| 2026-05-12 | `johnny1-readgroups16-v391` | Log-only; source restored. | Refresh JOHNNY1 high/low on the current baseline and retest the two best current-fit late retained-read candidates as a combined scene-specific table. The read planner still ranked `131..147` and `145..161` as scheduler-owned 16-sector candidates, each modeling one saved read, so this probe checked whether combining them could cross a timing threshold that the older individual probes missed. | Fresh paired baseline: `scratch/ps1-perf-iterate/johnny1-current-v391-baseline/20260512-143800-1320715/summary.json`, with both tides at scene `2069`, active loop `1973/1945`, overrun `28`, blocking/refill `25`, loop reads `7`, and due misses `0`. Temporarily added `kJohnny1ReadGroups16[] = {{131,147,0},{145,161,0}}` and routed `johnny1` through it for both tides. Focused gate: `scratch/ps1-perf-iterate/johnny1-readgroups16-v391-focused/20260512-144141-1342001/summary.json`. Source was restored after the gate failed. | Failed/no promotion. Both tides stayed exact-flat on every key counter: scene `2069`, active loop `1973/1945`, overrun `28`, blocking `25`, hidden refill `25`, loop reads `7`, and due misses `0`. The source branch grew `foregroundPilotPlay` by `36` bytes and shifted hot symbols by `-40`, so the no-op timing result is not worth carrying. | Keep JOHNNY1 direct retained-read groups closed. The remaining `25` blocking/refill VBlanks are not exposed through plain append-group ownership; future work needs generated deadline ownership, a larger retained-window design with proven phase, or a pack/data-shape change that removes the reads before the runtime scheduler sees them. |
| 2026-05-12 | `johnny1-zero-tail-compact-v392` | Log-only; packs restored. | Test a JOHNNY1 data-shape cleanup discovered while investigating the black-backdrop residual path: both accepted FGP3/v4 JOHNNY1 packs still carried `129450` bytes of trailing zero padding. The probe used the existing safe FGP3 compactor to shrink both files and asked whether the lower footprint or shifted low-tide LBA would move any timing counter. | Ran `./scripts/compact-fgp3-packs.py generated/ps1/foreground/JOHNNY1.FG2 generated/ps1/foreground/JOHN1LOW.FG2`, shrinking each pack `448370 -> 318920` bytes (`258900` bytes total saved), then rebuilt PS1 and gated against the fresh v391 baseline. Focused gate: `scratch/ps1-perf-iterate/johnny1-zero-tail-compact-v392-focused/20260512-144417-1358143/summary.json`. The packs were restored to the padded `448370` byte size after the gate failed. | Failed/no promotion. High stayed exact-flat with fixed LBA. Low also stayed exact-flat on timing (`2069`, `1973/1945`, overrun `28`, blocking/refill `25`, reads `7`, due `0`) but shifted pack LBA `14202 -> 14139`, which violates the strict layout-identity gate without producing a speed or work-volume win. | Do not promote JOHNNY1 zero-tail compaction as a timing optimization. It is a valid footprint cleanup candidate for a future whole-disc compaction release, but not for the current under-99 speed baseline because it changes layout and leaves all public counters unchanged. |
| 2026-05-12 | `johnny1-black-clean-direct-clear-v393` | Log-only; source restored. | Test a scene-specific black-backdrop cleanup shortcut for JOHNNY1. In black clean-background mode the cleanup pixels are known zero, so the probe tried direct framebuffer black `TILE` clears for cleanup spans instead of marking those spans dirty and uploading full dirty-row bands later. | Temporarily added a `grClearFramebufferBlackRect()` helper in `graphics_ps1.c` and called it from the black-mode branch of `grRestoreCleanBgSpanFromRects()`, falling back to `grMarkRectDirty()` only if the primitive buffer was full. Build passed. High focused artifact: `scratch/ps1-perf-iterate/johnny1-black-clean-direct-clear-v393-focused/20260512-145326-1410258/johnny1-high-current-v391/perf-summary.json`. Low rerun artifact after the first low wrapper died before `JCPERF2`: `scratch/ps1-perf-iterate/johnny1-black-clean-direct-clear-v393-low-rerun/20260512-145611-1427005/summary.json`. | Failed/no promotion. High locally improved active loop/target `1973/1945 -> 1972/1946`, overrun `28 -> 26`, and blocking/refill `25 -> 23`, but it grew the PS-EXE bucket `217088 -> 219136` and shifted `JOHNNY1.FG2` LBA `13983 -> 13984`. Low did not improve: scene regressed `2069 -> 2070`, active loop stayed `1973/1945`, blocking/refill stayed `25`, the PS-EXE bucket stayed grown, and `JOHN1LOW.FG2` LBA shifted `14202 -> 14203`. | Close direct framebuffer black-clear as a JOHNNY1 speed lane. The high-side signal proves black cleanup/upload work is still a lever, but the source helper's code-size/layout cost and low-tide scene regression make it unsafe. Retry only as a code-size-neutral primitive path, pack-authored black cleanup elision, or generated upload/restore plan that preserves the `217088` byte EXE bucket and both pack LBAs. |
| 2026-05-12 | `walkstuf1-low-rg297-313-v394` | Log-only; source restored. | Return to the largest remaining under-99 row and test the highest-upside current-window-compatible WALKSTUF1 low hand table. The `297..313` cluster modeled three saved reads, but the read plan marked it high-risk because the internal visible gaps are about `0.083s`. | Temporarily added `kWalkstuf1LowReadGroups16[] = {{297,313,0}}` and routed low-tide WALKSTUF1 through it while preserving the accepted high groups. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-rg297-313-v394-focused/20260512-150340-1469637/summary.json`. Source was restored after the gate failed. | Failed/no promotion. The group fired and reduced loop reads `67 -> 66` plus due misses `12 -> 11`, but public cadence regressed: scene `1776 -> 1778`, active loop/target `1484/1431 -> 1486/1430`, overrun `53 -> 56`, blocking `72 -> 73`, and hidden refill `22 -> 25`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed, so this is a true phase failure rather than layout drift. | Close direct retained grouping for `297..313`. Even the high-upside low cluster converts saved reads into worse visible cadence; WALKSTUF1 low needs generated deadline ownership, sector-boundary pack shaping, or a custom data/precompose path rather than more source-table append groups. |
| 2026-05-12 | `walkstuf1-high-rg238-246-v395` | Log-only; source restored. | Rotate to WALKSTUF1 high, the second-largest remaining under-99 row, and test the cleanest unclosed current-window-compatible direct table. The `238..246` candidate modeled one saved read with a short but not zero first/internal gap and no LBA or capacity change. | Temporarily inserted `{238,246,0}` between the accepted high groups `{213,229}` and `{344,360}`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-rg238-246-v395-focused/20260512-150938-1504301/summary.json`. Source was restored after the gate failed. | Failed/no promotion. The row landed in a worse schedule and did not reduce reads: scene `1768 -> 1778`, active loop/target `1480/1432 -> 1490/1420`, overrun `48 -> 70`, blocking `83 -> 111`, hidden refill `26 -> 32`, loop reads `65 -> 68`, and due misses `16 -> 19`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Close high-tide direct grouping for `238..246`. This confirms the remaining mid-pack WALKSTUF1 high rows are not safely exposed through hand-authored append tables; future high work should use generated deadline ownership, sector-boundary pack shaping, or a custom data/precompose path rather than more source-table groups. |
| 2026-05-12 | `walkstuf1-low-window36-44-v396` | Log-only; no source changed. | Recheck the low-tide WALKSTUF1 stream-window knee after the current v331/v384 baseline, because window size is a larger lever than another one-read table and the older 36/44/48 KiB probes predated several pack/scheduler changes. | Ran parameter-only low-tide boot overrides against the accepted v331 low baseline. The `44 KiB` probe used `prefetch-window 45056` and produced a complete failed summary at `scratch/ps1-perf-iterate/walkstuf1-low-window44-v396-param/20260512-151626-1542697/summary.json`. The opposite `36 KiB` probe used `prefetch-window 36864` and was killed before `JCPERF2`; its incomplete per-case artifact is `scratch/ps1-perf-iterate/walkstuf1-low-window36-v396-param/20260512-151738-1549625/walkstuf1-low-canary/perf-summary.json`. | Failed/no promotion. `44 KiB` reduced loop reads sharply (`67 -> 22`) but regressed timing and pressure: scene `1776 -> 1827`, active loop/target `1484/1431 -> 1533/1436`, overrun `53 -> 97`, blocking `72 -> 106`, hidden refill `22 -> 52`, and due misses `12 -> 5`. The `36 KiB` run did not reach metrics before the wrapper killed it while still issuing repeated CD reads, so it is not a candidate. | Keep the accepted `40 KiB` low window. Current WALKSTUF1 low scalar window retunes are closed: larger windows reduce transactions but collapse cadence, and smaller windows are structurally too slow/unmeasured under the current harness. Future low work needs generated deadline ownership or pack/data-shape changes, not adjacent window constants. |
| 2026-05-12 | `visitor3-low-segment-swing-v397-v398` | Log-only; source restored and PS1 rebuilt. | Return to the last VISITOR3 low gap and test whether the current large late cluster can be made resident without changing pack bytes. The plan tried the maximum contiguous swing first, then a same-footprint segment swap, then the more surgical binary opposite: keep the accepted `281..305` tail and `150..177` segment while adding a bounded `206..230` third segment. | Temporary source probes changed VISITOR3 low setup residency only. The fat `206..305` preload artifact is `scratch/ps1-perf-iterate/visitor3-low-fat-seg206-305-v397-focused/20260512-152338-1584220/summary.json`; the same-footprint `206..230` swap artifact is `scratch/ps1-perf-iterate/visitor3-low-seg206-230-v397-focused/20260512-152508-1593062/summary.json`; the additive third-segment hard-failure artifact is `scratch/ps1-perf-iterate/visitor3-low-seg206-230-plus-tail-v398-focused/20260512-152953-1620195/visitor3-low-canary/perf-summary.json`. Source was restored to accepted `281..305` plus `150..177`, and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. Full `206..305` residency hit the memory cliff and disabled effective prefetch: policy `stage1_window -> none`, scene `1401 -> 1481`, active loop/target `1072/1040 -> 1119/1034`, overrun `32 -> 85`, blocking `58 -> 475`, loop reads/due misses `10 -> 143`. The same-footprint `206..230` swap preserved policy but regressed: scene `1401 -> 1402`, active loop/target `1072/1040 -> 1073/1038`, overrun `32 -> 35`, blocking `58 -> 67`, reads/due `10 -> 12`. The additive third segment was killed before `JCPERF2` after the large setup read sequence, so it is a hard structural failure rather than a speed candidate. | Keep the accepted v338 tail-pack-only VISITOR3 low baseline. Do not add low setup residency or move the accepted tail segment; the heap budget and CD-head phase are already at the edge. Any remaining VISITOR3 low work must compress/alias one specific payload inside the existing resident budget or use generated deadline ownership without another retained setup buffer. |
| 2026-05-12 | `walkstuf1-low-rg297-321-v399` | Log-only; source restored and PS1 rebuilt. | Make a larger WALKSTUF1 low grouped-append swing than the rejected 16-sector `297..313` table. The current read plan ranked `297..321` as a high-upside 24-sector cluster that could save up to five reads, so the probe tested the unguarded group and the binary guarded variants before closing the lane. | Temporarily added low-tide `walkstuf1` read group `{297,321}` and routed low through the grouped append table. Focused artifacts: unguarded `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-v399-focused/20260512-153638-1659395/summary.json`, slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-slack8-v399-focused/20260512-153806-1667996/summary.json`, and slack6 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-slack6-v399-focused/20260512-153928-1676090/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Unguarded proved the read-saving signal but regressed cadence: loop reads `67 -> 63`, while scene `1776 -> 1782`, active loop `1484 -> 1490`, overrun `53 -> 59`, blocking `72 -> 87`, hidden refill `22 -> 23`, and due misses `12 -> 13`. Slack8 and slack6 both prevented useful firing and stayed exact-flat on all key counters with no work-volume win. | Close the large direct grouped-append path for WALKSTUF1 low. The thresholded forms are either phase-negative or inert; the row needs generated deadline ownership that can schedule exact reads earlier, or pack/data-shape work that removes payload bytes before the runtime grouping layer. |
| 2026-05-12 | `building2-readgroup-v400` | Log-only; source restored and PS1 rebuilt. | Rotate to BUILDING2 after closing the current WALKSTUF1/VISITOR3 scalar lanes. Fresh read plans exposed high-tide `17..25` as a medium-risk standalone group and low-tide `250..258` as a long-gap standalone group, so both were tested as small layout-stable source probes. | Generated current read plans at `scratch/building2-high-current-read-plan-v400.json` and `scratch/building2-low-current-read-plan-v400.json`. Temporarily added high `{17,25,0}` for `scratch/ps1-perf-iterate/building2-high-rg17-25-v400-focused/20260512-154356-1702723/summary.json`, then restored high and temporarily added low `{250,258,0}` for `scratch/ps1-perf-iterate/building2-low-rg250-258-v400-focused/20260512-154528-1711801/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. High saved one loop read and improved hidden refill `17 -> 16`, but regressed scene `1602 -> 1603`, active loop/target `1351/1313 -> 1352/1312`, overrun `38 -> 40`, and blocking `50 -> 51`. Low also saved one read but regressed hard: scene `1619 -> 1625`, active loop/target `1349/1318 -> 1355/1317`, overrun `31 -> 38`, blocking `81 -> 88`, and hidden refill `1 -> 2`. Pack LBAs and the PS-EXE bucket stayed fixed. | Close these two standalone BUILDING2 table probes. Current BUILDING2 direct read groups remain phase-sensitive; further work should use generated refill ownership or pack-side byte/cleanup reduction rather than adding more small hand tables. |
| 2026-05-12 | `visitor5-high-rg99-111-v401` | Log-only; source restored and PS1 rebuilt; CSV refreshed. | After VISITOR5 high was still under 99%, refresh its current-layout baseline and test the read plan's top current-window-compatible 12-sector row, `99..111`, which modeled one saved read with a short but nonzero internal gap. | First added a high-tide-only `kVisitor5HighReadGroups12` table with `{99,111,0}` and routed only high-tide `visitor5` through it. Focused artifact: `scratch/ps1-perf-iterate/visitor5-high-rg99-111-v401-focused/20260512-155119-1745169/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt. Because the older v158 summary predated the current `217088` byte EXE bucket and shifted LBA, the accepted branch was rerun at `scratch/ps1-perf-iterate/visitor5-high-current-v401-baseline/20260512-155359-1761359/summary.json`, and `performance-scene-matrix.csv` was refreshed to that current-layout row. | Failed/no promotion. Against the current accepted row (`1365`, `1107/1090`, overrun `17`, blocking/refill `16`, reads `19`, due `0`), the `99..111` table regressed to scene `1370`, active loop/target `1112/1091`, overrun `21`, blocking/refill `18`, and reads `18`. The saved-read signal is real but costs visible cadence. The baseline refresh moves VISITOR5 high from stale `1104/1092`, blocking `11`, LBA `24082` to current `1107/1090`, blocking `16`, LBA `24221`, so the public rollup corrects to `+0.3178%` over target / `99.6884%` target speed; green rows remain `115/126`. | Close direct retained grouping for VISITOR5 high `99..111`. The next VISITOR5 high attempts should use generated deadline ownership or a pack/data-shape reduction; plain hand tables can reduce read count while worsening timing. Treat the v401 current-layout row as the baseline for any future VISITOR5 high work. |
| 2026-05-12 | `walkstuf1-low-directstage-late-v402` | Log-only; source restored and PS1 rebuilt. | Investigate why the late WALKSTUF1 low `297..321` cluster keeps showing a real read-saving signal but regresses when grouped. The current code uses a direct-stage shortcut for small entries at minimum window slack; the probe tested both binary ownership choices for that late sector band: deny direct-stage so the full window owns it, then force direct-stage so exact reads own it. | First temporarily denied the direct-stage shortcut for low-tide WALKSTUF1 entries whose payload starts in sectors `297..321`, forcing `fgRuntimeFillWindowForEntry()` to own that band. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-directstage-late-window-v402-focused/20260512-161059-1857828/summary.json`. Then changed the condition the opposite way so those same entries take direct-stage ownership whenever slack is eligible. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-directstage-late-direct-v402-focused/20260512-161230-1866843/summary.json`. Source was restored to the accepted direct-stage rule and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. Full-window ownership saved reads (`67 -> 65`) but regressed target `1431 -> 1429`, blocking `72 -> 76`, hidden refill `22 -> 27`, and prefetch overrun `22 -> 27`. Forced direct-stage ownership kept scene/loop flat and reads at `67`, but still regressed target `1431 -> 1429`, blocking `72 -> 73`, hidden refill `22 -> 24`, and prefetch overrun `22 -> 24`. Both variants kept correctness clean but failed the strict pressure gate. | Close hand-coded direct-stage/window ownership toggles for WALKSTUF1 low sectors `297..321`. The row is not missing a simple branch choice: it needs a generated reservation map with exact phase ownership, sector-boundary pack changes that remove reads before the scheduler sees them, or a custom data representation. Public rollup and methodology totals are unchanged at `+0.3178%` over target / `99.6884%` target speed and about `17.08` public over-target points removed plus `12.59` target-speed points gained. |
| 2026-05-12 | `walkstuf1-low-align145-159-v403` | Log-only; pack restored and PS1 rebuilt. | Try a no-runtime-code sector-boundary swing for the largest under-99 row after direct-stage/window toggles failed. The hypothesis was that the late exact-read cluster loses time by rereading overlapping sectors, so sector-aligning the hot entries inside the existing padded file might reduce direct-stage overlap without changing pack size or LBA. | Backed up `generated/ps1/foreground/WALK1LOW.FG2`, rebuilt the pack in frame order, inserted `18908` bytes of zero padding to sector-align entries `145..159`, moved the sound table from offset `920531` to `939439`, and kept the total file size fixed at `1535263` bytes. Transform output: `scratch/walkstuf1-low-align-hot-v403/WALK1LOW.align145-159.FG2`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-align145-159-v403-focused/20260512-161628-1890470/summary.json`. The original pack was restored byte-for-byte and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The layout saved one loop read (`67 -> 66`) but regressed the row badly: scene `1776 -> 1780`, active loop/target `1484/1431 -> 1488/1427`, overrun `53 -> 61`, blocking `72 -> 102`, and due misses `12 -> 18`; prefetch overrun stayed `22`. Pack LBA and PS-EXE bucket stayed fixed, so the regression is a true CD-head/phase cost from the padding layout. | Close brute sector-align padding for WALKSTUF1 low entries `145..159`. The useful pack-side direction is not “add gaps to avoid overlap”; it must remove bytes, preserve CD-head locality, or emit generated ownership/reservation metadata. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-low-directstage-mid-window-v404` | Log-only; source restored and PS1 rebuilt. | Test whether the smaller `285..297` WALKSTUF1 low medium-risk cluster behaves differently from the already closed late `297..321` ownership lane. The read plan modeled two saved reads and a less extreme first-gap profile, so this tried denying the direct-stage shortcut only for payloads starting in sectors `285..297`. | Temporarily added a low-tide WALKSTUF1 sector guard around the direct-stage shortcut in `fgRuntimeTryStageNextFrame()`, forcing matching entries through `fgRuntimeFillWindowForEntry()`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-directstage-mid-window-v404-focused/20260512-162119-1917867/summary.json`. Source was restored to the accepted direct-stage condition and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The branch saved reads (`67 -> 65`) but regressed every speed gate: scene `1776 -> 1784`, active loop/target `1484/1431 -> 1492/1430`, overrun `53 -> 62`, blocking `72 -> 81`, and prefetch overrun `22 -> 28`; due misses stayed `12`. | Close manual direct-stage-deny for the `285..297` mid cluster. Together with v402, this proves WALKSTUF1 low needs generated phase ownership or data removal, not hand-authored direct-stage/window branches. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-high-rg178-194-v405-v406` | Log-only; source restored and PS1 rebuilt. | Test the last apparently unclosed lower-risk WALKSTUF1 high retained-read candidate from the accepted v384 plan. The `178..194` row modeled two saved reads in the plan and sat outside the already closed `238..246`, `268..280`, `298..314`, `329..345`, and `360..376` hand-table failures, so the probe tried the direct binary pair: unguarded first, then `minSlack=4`. | Temporarily inserted `{178,194,0}` before the accepted WALKSTUF1 high groups, built, and ran `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-v405-focused/20260512-162746-1955726/summary.json`. Then changed the row to `{178,194,4}`, rebuilt, and ran `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-slack4-v406-focused/20260512-162904-1963508/summary.json`. Source was restored to the accepted v384 group table and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. Both variants produced the same regression against v384: loop reads improved `65 -> 64` and hidden refill improved `26 -> 23`, but scene regressed `1768 -> 1774`, active loop/target `1480/1432 -> 1486/1427`, overrun `48 -> 59`, blocking `83 -> 98`, and due misses `16 -> 17`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed, so this is a true phase/cadence failure. | Close WALKSTUF1 high direct grouping for `178..194`. The remaining high-side mid-pack hand tables are now mostly closed or phase-negative; continue with generated deadline ownership, sector-boundary pack shaping that removes rather than pads bytes, or custom payload/precompose work. Public rollup and methodology totals are unchanged at `+0.3178%` over target / `99.6884%` target speed and about `17.08` public over-target points removed plus `12.59` target-speed points gained. |
| 2026-05-12 | `perf-baseline-label-gate-v407` | `perf: fail improvement gates without baseline match` | Fix the harness loophole exposed by multiple recent probes where the wrapper could print PASS when a focused run used a label that did not exist in the supplied baseline summary. With `--require-improvement`, no baseline match means the gate cannot prove either improvement or no-regression, so it must be a hard failure. | Updated `scripts/ps1-perf-iterate.sh` so a missing matching baseline case remains a warning for exploratory comparisons, but becomes a gate failure when `--require-improvement` is active. Validation: `bash -n scripts/ps1-perf-iterate.sh`. | Promoted as a methodology/validation hardening change. Runtime code, pack files, CSV timing rows, and public rollup are unchanged, but future promotion-style gates cannot silently skip the baseline comparison because of a label mismatch. | Keep. This directly supports the headless optimization loop: failed experiments become unambiguous log-only rows, and accepted improvements must compare against the intended baseline case. |
| 2026-05-12 | `visitor3-low-directstage-f117-v408` | Log-only; source restored and PS1 rebuilt. | Test a tightly scoped VISITOR3 low scheduler-ownership idea from the white-whale backlog: deny the exact-min-slack direct-stage shortcut only for source frame `117`, hoping the existing retained window could own the precursor payload without adding setup residency or moving pack bytes. | Temporarily guarded the direct-stage branch in `fgRuntimeTryStageNextFrame()` with a low-tide `visitor3` / `sourceFrame == 117` exclusion, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused low gate against the accepted v338 baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-directstage-f117-v408-focused/20260512-163639-2006977/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The row stayed exact-flat against v338: scene `1401`, active loop/target `1072/1040`, overrun `32`, blocking `58`, hidden refill `0`, loop reads `10`, and due misses `10`. The source branch grew `foregroundPilotPlay` by `68` bytes and shifted hot symbols by `+32` or more, so the no-op is not worth carrying. | Close VISITOR3 low frame-117 direct-stage denial as a hand-coded branch. The remaining VISITOR3 gap is not this single shortcut; continue with trace/heatmap-guided payload compression, generated ownership metadata, or a data-shape change inside the existing resident budget. Public rollup is unchanged. |
| 2026-05-12 | `compact-compositor-o2-v409` | Log-only; source restored and PS1 rebuilt. | Test an unclosed compiler-codegen lane for the largest under-99 row: remove the function-scoped `optimize("Os")` from `grCompositePacked4CompactTemporalResidualToBackground()` so the active compact FGP3/v4 compositor uses default `-O2`. | Temporarily removed the attribute, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused `walkstuf1-low-canary` gate against the accepted v331/vcurrent baseline. Artifact: `scratch/ps1-perf-iterate/compact-compositor-o2-v409-walkstuf1-low/20260512-165435-2108227/summary.json`. Source was restored to scoped `-Os` and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. Layout stayed fixed, but WALKSTUF1 low regressed from scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, prefetch overrun `22`, loop reads `67`, due `12` to scene `1778`, active loop/target `1486/1428`, overrun `58`, blocking `81`, prefetch overrun `27`, loop reads `69`, due `13`. The source/codegen shift also grew `foregroundPilotPlay` by `8` bytes and moved hot symbols. | Keep the compact FGP3/v4 compositor at scoped `-Os`. Default `-O2` is cadence-negative even without a pack LBA or EXE-bucket change; retry compiler shaping only as a source-split/layout-pinned rewrite or alongside a generated data-shape change that materially reduces compositor work. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-low-windowprefetch-slack-v410-v411` | Log-only; source restored and PS1 rebuilt. | Test whether the accepted high-tide speculative window-prefetch guard should apply more strongly to low tide now that low remains the largest under-99 row. The binary pair raised the low-tide WALKSTUF1 skip threshold from the accepted `3` VBlanks to `4`, then to `5`, while leaving high's accepted `4` VBlank guard unchanged. | Temporarily changed the low branch in `fgRuntimeTryPrefetchWindow()`, rebuilt, and ran focused `walkstuf1-low-canary` gates against the accepted v331/vcurrent baseline. Artifacts: `4` VBlanks at `scratch/ps1-perf-iterate/walkstuf1-low-windowprefetch-slack4-v410-focused/20260512-165820-2129924/summary.json`; `5` VBlanks at `scratch/ps1-perf-iterate/walkstuf1-low-windowprefetch-slack5-v411-focused/20260512-165949-2138651/summary.json`. Source was restored to the accepted low `3` VBlank guard and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. Both variants stayed exact-flat: scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, prefetch overrun `22`, loop reads `67`, and due misses `12`. The `4` VBlank variant shrank `fgRuntimeTryPrefetchWindow()` by `20` bytes but did not improve a timing/work counter and still shifted hot symbols; the `5` VBlank variant was likewise timing-flat with source/code-shape churn only. | Close scalar low-tide speculative-window slack guards above the accepted `3` VBlank rule. The remaining WALKSTUF1 low gap is not a simple wait-threshold problem; it needs generated deadline ownership or pack/data-shape work that changes the read/work schedule before this guard sees it. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-low-rg78-90-v412` | Log-only; source restored and PS1 rebuilt. | Test the smaller early low-tide group left by the fresh read plan after the broader `78..94` row had failed. The narrowed `78..90` span modeled one saved read with a long first gap and less overread, so it was the last plausible scalar early-cluster table before moving back to generated ownership/data-shape work. | Temporarily added a low-tide `kWalkstuf1LowReadGroups12` table with `{78,90,0}` and routed low-tide WALKSTUF1 through grouped appends. The PS1 build stayed in the `217088` byte bucket. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg78-90-v412-focused/20260512-170514-2170485/summary.json`. Source was restored and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The narrower group was still phase-negative: low regressed from scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, prefetch overrun `22`, reads/due `67/12` to scene `1780`, active loop/target `1488/1428`, overrun `60`, blocking `79`, prefetch overrun `27`, reads/due `69/11`. It reduced due misses by one but increased reads, blocking, and public timing. | Close early WALKSTUF1 low hand-authored read groups. The accepted row needs generated deadline ownership, sector-boundary data movement, or a custom representation; narrowing the `78..94` range does not fix the cadence failure. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `building2-low-rg243-259-v413` | Log-only; source restored and PS1 rebuilt. | Rotate from the closed WALKSTUF1 scalar lanes to BUILDING2 low and test the strongest non-closed current read-plan row that was still compatible with the existing grouped-append capacity. The `243..259` span modeled two saved reads across reads `103..105`, had medium visible-risk scoring, and was not one of the already closed low groups. | Generated `scratch/building2-low-current-read-plan-v413.json`, temporarily inserted `{243,259,0}` ahead of the accepted low groups `{318,330}` and `{365,381}`, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused BUILDING2 low gate against the current matrix baseline. Artifact: `scratch/ps1-perf-iterate/building2-low-rg243-259-v413-focused/20260512-171318-2216534/summary.json`. Source was restored and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The group was phase-negative and increased read count: low regressed from scene `1619`, active loop/target `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, loop reads/due `55/18` to scene `1625`, active loop/target `1355/1320`, overrun `35`, blocking `79`, hidden refill `1`, loop reads/due `57/17`. | Close BUILDING2 low `243..259` as a standalone hand-authored table. Even a planner-marked scheduler-owned candidate can move reads into worse public cadence; the next low work should generate explicit refill reservations or remove bytes/cleanup work before the grouped-append layer. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `building2-low-rg243-259-slack4-v414` | Log-only; source restored and PS1 rebuilt. | Test the binary scheduler-owned form of the rejected `243..259` BUILDING2 low tail cluster. The hypothesis was that the unguarded table regressed because it could enlarge due-time window fills at `slack=0`; adding `minSlack=4` should restrict the group to eligible prefetch slots only. | Temporarily changed the same low-tide table entry to `{243,259,4}`, rebuilt in the same `217088` byte PS-EXE bucket, and reran the focused BUILDING2 low gate. Artifact: `scratch/ps1-perf-iterate/building2-low-rg243-259-slack4-v414-focused/20260512-171747-2242317/summary.json`. Source was restored and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The guard removed the regression but also removed the useful movement: scene `1619`, active loop/target `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, loop reads `55`, and due misses `18` all stayed exact-flat against baseline. The source still shifted foreground hot symbols, so there is no promotable work or timing win. | Close `243..259` in both standalone and simple min-slack guarded forms. BUILDING2 low needs a real refill-reservation map or pack-side byte/cleanup reduction; a local table guard only chooses between phase-negative and inert. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `visitor3-low-third-segment-190-v415` | Log-only; source restored and PS1 rebuilt. | Take a smaller VISITOR3 low setup-residency swing than the closed `206..230`/`206..305` attempts. The current read plan showed an isolated `190..199` read with the only long preceding gap, so the probe asked whether a tiny third retained segment could remove one due miss without paying the larger segment's heap/setup cost. | Temporarily added a VISITOR3-low-only third reusable setup segment for sectors `190..199`, extended the setup-segment lookup/reset plumbing, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused low gate against the accepted v338 baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-third-segment-190-v415-focused/20260512-173039-2314861/summary.json`. Source was restored and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. The tiny segment did reduce CD pressure (`blocking_vb 58 -> 54`, `loop_reads 10 -> 9`, `due_misses 10 -> 9`), but the added setup/runtime shape regressed the strict public gates: scene `1401 -> 1407`, active loop/target `1072/1040 -> 1073/1040`, and overrun `32 -> 33`. | Close VISITOR3 low `190..199` as another retained setup segment. Even the smallest isolated read cannot pay back its setup debt under the current v338 budget; remaining VISITOR3 low work should compress/alias inside existing resident bytes or use generated deadline ownership rather than another setup buffer. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-low-rg273-297-v416` | Log-only; source restored and PS1 rebuilt. | Test the largest remaining plausible WALKSTUF1 low retained-read swing after the adjacent late clusters failed. The accepted-baseline read plan ranked `273..297` as a 24-sector medium-risk candidate with four modeled saved reads, distinct from the already rejected `285..309` and `297..321` tight-cluster probes. | Temporarily added a low-tide-only `walkstuf1` retained read group `{273,297,0}` and routed low through the same grouped append path while leaving high's accepted groups unchanged. The PS1 build stayed in the `217088` byte bucket. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg273-297-v416-focused/20260512-173721-2353915/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The modeled read saving landed but cadence got worse: loop reads fell `67 -> 64`, while scene regressed `1776 -> 1782`, active loop/target `1484/1431 -> 1490/1431`, overrun `53 -> 59`, blocking `72 -> 80`, and prefetch overrun `22 -> 26`. Pack LBA and the PS-EXE bucket stayed fixed, so this is a true phase failure. | Close WALKSTUF1 low `273..297` as a retained table. The last large local low group still turns saved reads into worse public timing; remaining WALKSTUF1 low work should move to generated deadline ownership, a real byte-removing pack transform, or a custom representation rather than another hand-authored append table. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-low-gap6-f38-39-v417` | Log-only; generated pack restored. | Test a narrower pack-side version of the previously failed prefix-gap widening path. Instead of widening the whole active prefix, only frames `38` and `39` used the existing `gap6` cleanup-span merge so the sound table could move earlier by `444` bytes while preserving the `1535263` byte pack footprint and pack LBA. | Temporarily replaced `generated/ps1/foreground/WALK1LOW.FG2` with `scratch/walkstuf1-low-v328-gap-extend/low-gap6-f38-39.FG2` and ran the focused low gate against the accepted v331 baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-gap6-f38-39-v417-focused/20260512-174303-2386091/summary.json`. The tracked pack was restored afterward. | Failed/no promotion. The narrow pack shift still moved cadence the wrong way: scene `1776 -> 1780`, active loop/target `1484/1431 -> 1488/1429`, overrun `53 -> 59`, blocking `72 -> 78`, prefetch overrun `22 -> 29`, and due misses `12 -> 11`. Loop reads stayed `67`, so the pack relocation did not produce useful CD work reduction. | Close frame-38/39-only cleanup-gap widening for WALKSTUF1 low. Even the smallest boundary-frame prefix shift disrupts timing more than it helps; remaining low work needs generated deadline ownership, a byte-removing transform with fixed phase, or a custom representation that changes upload/compose work without moving CD cadence. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-low-narrow-upload-v418` | Log-only; source restored and PS1 rebuilt. | Make a render-side swing instead of another CD table: for low-tide WALKSTUF1 only, try uploading tight dirty X-bands through a 64 KiB scratch buffer when the saved full-row DMA bytes exceed `8 KiB`. The hypothesis was that lower upload work could free enough held-frame slack for the existing scheduler. | Temporarily added `grSetNarrowDirtyUploadMode()`, a scene-gated scratch-copy path in `grDrawBackground()`, and enabled it only for low-tide `walkstuf1`. Build passed but grew the PS-EXE bucket `217088 -> 219136`, so the exploratory focused gate was run with `--allow-layout-change`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-narrow-upload-v418-focused/20260512-175112-2433151/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt back to `217088` afterward. | Failed/no promotion. The scene did not reach `JCPERF2` before the harness timeout (`missing_jcperf2`); logs advanced very slowly after the normal completion window. The implementation also shifted `WALK1LOW.FG2` layout through the larger EXE bucket, so there is no promotable speed signal. | Close scratch-copy narrow dirty upload as a runtime path. If upload-byte reduction is revisited, it must be pack-authored/upload-ready or code-size-neutral; copying bands into scratch during playback is too slow and too large for the current frame cadence. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `building2-high-rg210-222-v419` | Log-only; source restored and PS1 rebuilt. | Retest the BUILDING2 high middle cluster with a narrower non-overlapping group after the accepted `226..242` promotion. The older `210..226` probe no-opped before the current baseline, while the refreshed read plan ranked `210..222` as a current-window-compatible two-read-saving row immediately before the accepted group. | Temporarily inserted `{210,222,0}` between accepted high groups `{60,72}` and `{226,242}`. The first focused run died before `JCPERF2`; the rerun produced valid metrics at `scratch/ps1-perf-iterate/building2-high-rg210-222-v419-rerun/20260512-175916-2479670/summary.json`. The source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The added group reduced loop reads `61 -> 59`, but public cadence and CD pressure regressed: scene `1602 -> 1607`, active loop/target `1351/1313 -> 1356/1311`, overrun `38 -> 45`, blocking `50 -> 57`, and prefetch overrun `17 -> 23`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Close BUILDING2 high `210..222` as a hand-authored retained table. The current middle cluster is phase-negative when forced locally; remaining high-side work needs generated refill ownership or pack-side byte reduction rather than another scalar append table. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `building2-high-rg222-238-v420` | Log-only; source restored and PS1 rebuilt. | Test whether shifting the accepted middle group earlier, from `226..242` to `222..238`, could keep the v379 cadence gain while covering more of the current pre-cluster. This was a replacement probe rather than an additive table, so it asked whether a nearby phase point was better than the accepted span. | Temporarily replaced `{226,242,0}` with `{222,238,0}` between accepted high groups `{60,72}` and `{249,257}`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg222-238-v420-focused/20260512-180417-2508498/summary.json`. The source was restored to `{226,242,0}` and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The replacement lost the accepted v379 timing: scene `1602 -> 1603`, active loop/target `1351/1313 -> 1352/1311`, overrun `38 -> 41`, blocking `50 -> 55`, hidden refill `17 -> 19`, with loop reads/due misses still `61/7`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Keep `226..242` as the current BUILDING2 high middle group. Close the adjacent replacement `222..238`; the middle cluster is phase-sensitive and should not get more hand-table nudges without generated deadline ownership or a pack-side byte reduction that preserves the accepted phase. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `visitor3-low-rg206-230-v421-v423` | Log-only; source restored and PS1 rebuilt. | Test a generated-ownership approximation for the remaining VISITOR3 low tight cluster without adding setup residency. The first pair used a low-tide-only `206..230` grouped append with a 24-sector stream capacity, first guarded at `minSlack=4` and then unguarded; after both failed to move metrics, the final binary variant targeted the inferred append-extension edge with `218..230` under the normal 16-sector capacity. | Temporary source variants added a VISITOR3-low-only grouped-read table and, for v421/v422, a 24-sector buffer capacity. Artifacts: guarded `206..230` at `scratch/ps1-perf-iterate/visitor3-low-rg206-230-cap24-v421-focused/20260512-181743-2584380/summary.json`; unguarded `206..230` rerun at `scratch/ps1-perf-iterate/visitor3-low-rg206-230-cap24-v422-rerun/20260512-182002-2599138/summary.json`; append-edge `218..230` at `scratch/ps1-perf-iterate/visitor3-low-rg218-230-v423-focused/20260512-182140-2608856/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. All valid variants were exact-flat against v338: scene `1401`, active loop/target `1072/1040`, overrun `32`, blocking `58`, hidden refill `0`, reads/due `10/10`, and pack LBA/PS-EXE bucket fixed. The first unguarded v422 attempt died before `JCPERF`, but the rerun completed and matched the flat result. The source branch only shifted hot symbols and grew `foregroundPilotPlay` by `56..92` bytes depending on capacity. | Close local grouped-append ownership for VISITOR3 low `206..230` and append-edge `218..230`. The remaining cluster is not exposed through the current window/group table path; future VISITOR3 low work must compress or alias one payload into existing resident bytes, introduce generated deadline metadata deeper than the hand table, or use a pack-authored custom representation. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `walkstuf1-shared-dual-tail-v428` | `ps1: share walkstuf1 low tail read group` | Promote the last low-side CD-work-only WALKSTUF1 tail signal without a low-only branch expanding the hot path. The low pack wants sector `443..455`, while high already kept `444..456`; the binary question was whether one shared dual-tail table could lower low loop reads while preserving high timing/read count and other under-99 controls. | Added `{443,455,0}` beside the accepted `{444,456,0}` in the shared WALKSTUF1 table, routed the table for both tides, and added a cold PS1 layout pad after the compact fgpilot return to avoid downstream hot-text drift. Focused low artifact: `scratch/ps1-perf-iterate/walkstuf1-shared-dual-tail-pad-v428-focused-low/20260512-184938-2772418/summary.json`; high control: `scratch/ps1-perf-iterate/walkstuf1-shared-dual-tail-pad-v428-control-high/20260512-185043-2778742/summary.json`; VISITOR3 low control: `scratch/ps1-perf-iterate/walkstuf1-shared-dual-tail-pad-v428-control-visitor3-low/20260512-184808-2763550/summary.json`; BUILDING2 clean-current/candidate controls: `scratch/ps1-perf-iterate/current-clean-control-building2-high-v428-baseline-check/20260512-184518-2746980/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-shared-dual-tail-pad-v428-control-building2-current/20260512-184701-2756987/summary.json`. | Promoted as same-speed CD-work reduction. WALKSTUF1 low stays exact-flat on public timing at scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, refill `22`, and due `12`, while loop reads improve `67 -> 66` and loop-read time `288 -> 287`. WALKSTUF1 high stays exact-flat at `1768`, `1480/1432`, reads `65`; VISITOR3 low stays flat. BUILDING2 high is unchanged relative clean-current at `1603`, `1352/1311`, overrun `41`, blocking `55`, refill `19`, reads/due `61/7`; the older v379 `1351/1313` artifact no longer reproduces on current HEAD. | Keep v428 as the current WALKSTUF1 low CD-work baseline, not a VBlank speed win. The refreshed public rollup is `+0.3196%` over target / `99.6867%` target speed, raw signed `-0.4474%` / `100.4690%`, with about `17.08` public over-target points removed and `+12.59` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-rg285-297-v429` | Log-only; source restored and PS1 rebuilt. | Test the biggest current-compatible WALKSTUF1 low table candidate after the v428 tail promotion. The current read plan ranked `285..297` as a fireable 12-sector row with two modeled saved reads, and the shared-table form should be inert for high because high's matching fire point is shifted to `287..299`. | Temporarily inserted `{285,297,0}` into the shared WALKSTUF1 read-group table between `{213,229}` and `{344,360}`, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused low gate against the v428 low baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg285-297-v429-focused/20260512-190557-2866030/summary.json`. Source was restored to v428 and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The group saved the modeled reads (`66 -> 64`) but regressed public cadence and CD pressure: scene `1776 -> 1784`, active loop/target `1484/1431 -> 1492/1430`, overrun `53 -> 62`, blocking `72 -> 81`, hidden refill `22 -> 28`, while due misses stayed `12`. Pack LBA and the PS-EXE bucket stayed fixed, so this is a true phase failure. | Close WALKSTUF1 low `285..297` as a shared direct table. Together with the rejected `273..297`, `285..309`, `297..313`, and direct-stage `285..297` lanes, this confirms the mid hot cluster needs generated deadline ownership, byte-removing pack transforms, or a custom representation rather than another retained-read row. Public rollup and methodology totals remain at v428. |
| 2026-05-12 | `walkstuf1-low-rg291-297-v430` | Log-only; source restored and PS1 rebuilt. | Narrow the failed v429 mid-cluster to the smallest fireable row. The read plan ranked `291..297` as a 6-sector row with one modeled saved read and a later first gap, so it tested whether the small group could get the same kind of same-speed CD-work win as the accepted v428 tail. | Temporarily inserted `{291,297,0}` into the shared WALKSTUF1 table after `{213,229}`, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused low gate against v428. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg291-297-v430-focused/20260512-191005-2890124/summary.json`. Source was restored to v428 and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The candidate was exact-flat on all public metrics and work counters: scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, reads `66`, and due misses `12`. With `--require-improvement`, this correctly failed as no key metric improved. | Close `291..297` as an inert shared direct table. The mid-cluster’s smallest row does not expose useful work through the current grouped-append path; future attempts should target generated deadline ownership or byte/data-shape changes instead of more narrow table rows. |
| 2026-05-12 | `walkstuf1-low-rg303-309-v431` | Log-only; source restored and PS1 rebuilt. | Test the later half of the WALKSTUF1 low hot mid-cluster after v429 regressed and v430 no-opped. The `303..309` row is another current-compatible fire point with one modeled saved read; the goal was to confirm whether the later subcluster had a safe same-speed CD-work signal before abandoning narrow direct rows. | Temporarily inserted `{303,309,0}` into the shared WALKSTUF1 read-group table after `{213,229}`, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused low gate against v428. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg303-309-v431-focused/20260512-191244-2905868/summary.json`. Source was restored to v428 and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The candidate was exact-flat on all public metrics and work counters: scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, reads `66`, and due misses `12`. | Close `303..309` as another inert shared direct table. With `285..297` regressing and `291..297` / `303..309` no-oping, the mid-cluster direct-table lane is exhausted until generated scheduler ownership or pack/data-shape work changes the phase model. |
| 2026-05-12 | `building2-high-pad-sweep-v432-v433` | Log-only; source restored and PS1 rebuilt. | Test whether the BUILDING2 high v379 cadence loss on current HEAD was recoverable by a tiny hot-text phase adjustment around the accepted three-NOP layout pad. This was a binary pad sweep: add one NOP, then remove one NOP. | Temporarily changed the cold fgpilot layout pad from three NOPs to four for `scratch/ps1-perf-iterate/building2-high-pad4-v432-focused/20260512-191547-2923887/summary.json`, then to two for `scratch/ps1-perf-iterate/building2-high-pad2-v433-focused/20260512-191711-2933261/summary.json`. Both variants rebuilt in the same `217088` byte PS-EXE bucket and used the v428 BUILDING2 current-control summary as baseline. Source was restored to the three-NOP baseline and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Both pad directions were exact-flat on BUILDING2 high: scene `1603`, active loop/target `1352/1311`, overrun `41`, blocking `55`, hidden refill `19`, reads/due `61/7`. Pad4 shifted tracked hot symbols by `+4` bytes; pad2 shifted them by `-4` bytes. Neither recovered the older v379 `1351/1313` artifact. | Close ±1 NOP layout-pad phase recovery for BUILDING2 high. The current row needs generated refill ownership or pack/data-shape changes, not another one-word text-phase nudge. Public rollup and methodology totals remain at v428. |
| 2026-05-12 | `building2-high-rg23-29-v434` | Log-only; source restored and PS1 rebuilt. | Try a narrower early BUILDING2 high read-group probe after the broader v400 `17..25` row saved a read but regressed cadence. The current read plan exposed `23..29` as a fireable 6-sector row with one modeled saved read. | Temporarily inserted `{23,29,0}` before the accepted BUILDING2 high groups, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused high gate against the v428 current-control baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg23-29-v434-focused/20260512-192119-2957409/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The candidate was exact-flat against the current control: scene `1603`, active loop/target `1352/1311`, overrun `41`, blocking `55`, hidden refill `19`, loop reads `61`, and due misses `7`. It also shifted tracked hot symbols by `+4` bytes, so there is no promotable work-volume or timing signal. | Close BUILDING2 high `23..29` as an early direct table. The early cluster remains unavailable through scalar grouped appends; use generated ownership or pack/data-shape reduction instead. |
| 2026-05-12 | `walkstuf1-high-rg80-92-v435` | Log-only; source restored and PS1 rebuilt. | Test an unclosed high-upside WALKSTUF1 high early-cluster row after low direct rows and BUILDING2 pad/table probes stalled. The current high read plan ranked `80..92` as fireable with two modeled saved reads, making this a larger high-side swing rather than a low-risk table scrap. | Temporarily inserted `{80,92,0}` at the front of the shared WALKSTUF1 read-group table, rebuilt in the same `217088` byte PS-EXE bucket, and ran the focused high gate against the v428 high baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg80-92-v435-focused/20260512-193439-3031981/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The row regressed hard: high moved from scene `1768`, active loop/target `1480/1432`, overrun `48`, blocking `83`, hidden refill `26`, reads/due `65/16` to scene `1788`, `1500/1425`, overrun `75`, blocking `105`, hidden refill `26`, reads/due `66/15`. Pack LBA and PS-EXE bucket stayed fixed, so this is a real cadence/phase failure. | Close WALKSTUF1 high `80..92` as a direct retained table. The early high cluster converts modeled saved-read opportunity into worse public cadence; future high work needs generated deadline ownership or pack/data-shape changes, not early scalar groups. |
| 2026-05-12 | `building2-high-rg206-230-cap24-v441` | `ps1: widen building2 high grouped read capacity` | Make a larger BUILDING2 high swing after the narrower hand-table lanes either no-opped or regressed. The first binary half tested `{206,230}` under the existing 16-sector grouped capacity; the second half raised grouped-read capacity to 24 sectors and kept the row. | Added `{206,230,0}` between the accepted `{60,72,0}` and `{226,242,0}` high groups. The 16-sector probe stayed exact-flat at `scratch/ps1-perf-iterate/building2-high-rg206-230-v440-focused/20260512-195126-3132235/summary.json`; then `FG_PREFETCH_GROUP_WINDOW_BYTES` was raised to 24 sectors and the focused pass completed at `scratch/ps1-perf-iterate/building2-high-rg206-230-cap24-v441-focused/20260512-195257-3141634/summary.json`. Canaries are `scratch/ps1-perf-iterate/building2-high-rg206-230-cap24-v441-canaries/20260512-195509-3154355/summary.json`. | Promoted. BUILDING2 high improves scene `1603 -> 1602`, active loop/target `1352/1311 -> 1351/1311`, overrun `41 -> 40`, blocking `55 -> 54`, hidden refill `19 -> 18`, loop reads `61 -> 58`, and loop-read VBlanks `262 -> 257`; due misses stay `7`, pack LBA/sectors stay `6181/637`, and the `217088` byte PS-EXE bucket stays fixed. BUILDING2 low, WALKSTUF1 high/low, and VISITOR3 low canaries stayed exact-flat. | Keep as the current BUILDING2 high baseline. Public rollup moves to `+0.3190%` over target / `99.6873%` target speed, raw signed rollup moves to `-0.4480%` / `100.4696%`, and the methodology total is about `17.08` public over-target points removed plus `12.59` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-preserve-gap-v442` | Log-only; pack restored and PS1 rebuilt. | Test the phase-fixed version of the WALKSTUF1 low cleanup-gap idea after shifted pack compaction repeatedly changed CD cadence. Instead of moving later payload offsets or the sound table, the transform shrank selected entry sizes in place and zero-filled the reclaimed bytes, preserving pack size, pack LBA, every later payload offset, and the sound-event offset. | Generated `scratch/walkstuf1-low-preserve-gap-v442/low-gap1-f38-215-preserve.FG2` with `merge_cleanup_gaps.py --max-gap 1 --frames 38-215 --preserve-offsets`, cutting `5292` encoded bytes and collapsing cleanup spans `39188 -> 36798` while leaving `sound_events_offset=920531`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-preserve-gap1-f38-215-v442-focused/20260512-201532-3271622/summary.json`. The binary smaller variant only touched max-payload frame `210`, cutting `176` bytes; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-preserve-gap1-f210-v442-focused/20260512-201653-3279854/summary.json`. The generated pack was restored byte-for-byte and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. The broad phase-fixed compaction kept scene/loop flat at `1776/1484`, but target fell `1431 -> 1430`, overrun rose `53 -> 54`, blocking regressed `72 -> 74`, and hidden refill regressed `22 -> 25`; reads/due stayed `66/12`. The frame-210-only binary variant was exact-flat on every key timing/work counter at `1776`, `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, reads/due `66/12`, so it failed `--require-improvement`. | Close preserve-offset cleanup-gap compaction for WALKSTUF1 low. Keeping CD phase fixed avoids the large shifted-pack regressions, but broad span merging still adds cleanup pressure and the safe narrow form is inert. Remaining low work needs generated deadline/refill ownership, a true semantic-preserving byte removal that changes sector reads, or a custom representation that reduces render/upload work without increasing cleanup pixels. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `building2-low-rg67-91-cap24-v443-v444` | Log-only; source restored and PS1 rebuilt. | Revisit the BUILDING2 low early cluster now that v441 raised grouped-read capacity to 24 sectors. The prior `67..73` row was too small and phase-negative; this test asked whether the whole current-plan `67..91` span could turn the long first gap into a large read-count win, then tried the guarded binary form. | Temporarily inserted `{67,91,0}` before the accepted BUILDING2 low groups `{318,330}` and `{365,381}` and ran the focused low gate. Artifact: `scratch/ps1-perf-iterate/building2-low-rg67-91-cap24-v443-focused/20260512-202402-3321108/summary.json`. Then changed the same row to `{67,91,4}`; the first guarded run died before `JCPERF2`, and the clean rerun completed at `scratch/ps1-perf-iterate/building2-low-rg67-91-cap24-slack4-v444b-focused/20260512-202558-3333864/summary.json`. Source was restored and `build-ps1/jcreborn.exe` rebuilt afterward. | Failed/no promotion. Unguarded fired and cut loop reads `55 -> 51`, but regressed scene `1619 -> 1630`, active loop/target `1349/1318 -> 1360/1315`, overrun `31 -> 45`, blocking `81 -> 110`, hidden refill `1 -> 4`, and due misses `18 -> 22`. The `minSlack=4` binary form was exact-flat at scene `1619`, active loop/target `1349/1318`, overrun `31`, blocking `81`, hidden refill `1`, loop reads `55`, and due misses `18`. | Close BUILDING2 low `67..91` under both unguarded and simple slack-guarded 24-sector forms. Larger grouped capacity does expose the modeled read-count win, but the phase cost is too high; the guarded form only disables the useful movement. The low early cluster needs generated refill/deadline ownership or a pack-side byte reduction before retrying. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `building2-low-rg238-250-v445` | `ps1: add building2 low mid grouped read` | Continue BUILDING2 low after the early cluster proved phase-negative. The read plan still exposed a mid retained group that could cover current loop reads without hitting the failed first-gap cluster, so the binary question was whether `238..250` could convert the modeled read win into public cadence under the already-promoted 24-sector grouped-read capacity. | Inserted `{238,250,0}` before the accepted low groups `{318,330,0}` and `{365,381,0}`. Focused artifact: `scratch/ps1-perf-iterate/building2-low-rg238-250-v445-focused/20260512-202859-3351410/summary.json`. Canary artifact: `scratch/ps1-perf-iterate/building2-low-rg238-250-v445-canaries/20260512-203028-3360202/summary.json`. High control: `scratch/ps1-perf-iterate/building2-low-rg238-250-v445-high-control/20260512-203428-3384397/summary.json`. | Promoted. BUILDING2 low keeps scene/loop flat at `1619/1349`, improves target `1318 -> 1319`, overrun `31 -> 30`, blocking `81 -> 80`, loop reads `55 -> 53`, loop-read VBlanks `230 -> 227`, and due misses `18 -> 17`; hidden refill stays `1`, pack LBA/sectors stay `6818/637`, and the `217088` byte PS-EXE bucket stays fixed. BUILDING2 high stays flat at `1602`, `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, reads/due `58/7`; WALKSTUF1 high/low and VISITOR3 low canaries stayed flat. | Keep as the current BUILDING2 low baseline. Public rollup moves to `+0.3184%` over target / `99.6878%` target speed, raw signed rollup moves to `-0.4486%` / `100.4702%`, and the methodology total remains about `17.08` public over-target points removed plus `12.59` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-12 | `walkstuf1-low-prepare-threshold-v446-v447` | Log-only; source restored and PS1 rebuilt. | Try a scheduler-shape variant instead of another exhausted WALKSTUF1 low read table. The accepted staged-prepare fallback only prepares the staged frame at exactly `4` VBlanks of held slack; the binary question was whether low-tide WALKSTUF1 should prepare one VBlank earlier (`5`) or later (`3`) to move visual work out of the due path without changing packs. | Temporarily changed `fgRuntimeCanPrepareStagedFrame()` for low-tide `walkstuf1` only. The `5`-VBlank probe is `scratch/ps1-perf-iterate/walkstuf1-low-prepare5-v446-focused/20260512-204557-3450514/summary.json`; the `3`-VBlank probe is `scratch/ps1-perf-iterate/walkstuf1-low-prepare3-v447-focused/20260512-204718-3458656/summary.json`. Source was restored to the accepted `4`-VBlank threshold afterward. | Failed/no promotion. Both variants kept pack LBA and PS-EXE bucket fixed but regressed public cadence and CD pressure. The `5`-VBlank form moved low from `1776`, `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, reads/due `66/12` to scene `1782`, `1490/1428`, overrun `62`, blocking `84`, hidden refill `24`, reads/due `66/14`. The `3`-VBlank form was worse on target/refill: `1782`, `1490/1423`, overrun `67`, blocking `84`, hidden refill `31`, reads/due `67/12`. Both failed `--require-improvement`. | Close scalar prepare-threshold tuning for WALKSTUF1 low. The current accepted `4`-VBlank prepare point remains least bad; remaining work needs generated deadline/refill ownership, sector-read-changing byte removal, or a custom data/precompose representation rather than another single slack constant. Public rollup and methodology totals are unchanged from v445. |
| 2026-05-12 | `runtime-scene-policy-flags-v448` | Log-only; source restored. | Test the candidate-deck runtime-shape idea of caching repeated scene-policy checks into a runtime flag byte. The hypothesis was that replacing hot-loop `fgSceneEquals(gFgRuntime.sceneName, ...)` branches for WALKSTUF1, VISITOR3, BUILDING2, MARY3, and BUILDING6 could reduce CPU/layout noise in the current under-99 rows without changing pack data. | Temporarily added `scenePolicyFlags` to `TFgPilotRuntime`, set it at scene start, and used it in the window slack and WALKSTUF1/visitor scheduler checks. WALKSTUF1 low focused artifact: `scratch/ps1-perf-iterate/runtime-scene-policy-flags-v448-focused/20260512-205215-3487768/summary.json`. WALKSTUF1 high control artifact: `scratch/ps1-perf-iterate/runtime-scene-policy-flags-v448-high-control/20260512-205328-3495358/summary.json`. Source was restored after both probes. | Failed/no promotion. WALKSTUF1 low stayed exact-flat at scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, reads/due `66/12`, so it failed `--require-improvement`. WALKSTUF1 high emitted the accepted metrics manually despite a baseline-label mismatch: scene `1768`, `1480/1432`, overrun `48`, blocking `83`, hidden refill `26`, reads/due `65/16`. The patch shifted hot symbols and changed `foregroundPilotPlay` size by `+16`, but produced no row speed signal. | Close cached scene-policy flags as a standalone optimization. It may be cleaner architecture after a larger scheduler refactor, but it is not worth carrying for perf because the current counters are flat and symbol drift is real. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `visitor3-low-window-slack-v449-v450` | Log-only; source restored and PS1 rebuilt. | Test whether VISITOR3 low's remaining due-miss/blocking cluster could be exposed by relaxing the low-tide dual-segment window-refill guard. Since the accepted row has zero hidden refill but still `58` blocking VBlanks and `10` due misses, the binary sweep tried `3` VBlanks, then the more aggressive `2`, against the accepted `4`-VBlank baseline. | Temporarily changed `FG_VISITOR3_LOW_DUAL_SEGMENT_MIN_SLACK_VBLANKS` to `3`, rebuilt, and ran `scratch/ps1-perf-iterate/visitor3-low-window-slack3-v449-focused/20260512-210105-3539781/summary.json`; then changed it to `2`, rebuilt, and ran `scratch/ps1-perf-iterate/visitor3-low-window-slack2-v450-focused/20260512-210221-3547273/summary.json`. Source was restored to `4` and `build-ps1/jcreborn.exe` was rebuilt afterward. | Failed/no promotion. Both variants stayed exact-flat against v338: scene `1401`, active loop/target `1072/1040`, overrun `32`, blocking `58`, hidden refill `0`, loop reads `10`, and due misses `10`. The only effect was source/code-shape churn: tracked hot symbols shifted and `foregroundPilotPlay` grew by `8` bytes. | Close scalar VISITOR3 low window-slack relaxation. The remaining cluster is not blocked by this guard; future VISITOR3 low work should compress or alias payloads inside existing resident bytes, add generated deadline metadata deeper than the scalar threshold, or use a custom pack-authored representation. Public rollup and methodology totals are unchanged. |
| 2026-05-12 | `visitor5-low-compact-rg23-47-v451` | `ps1: compact visitor5 low foreground pack` | Reopen VISITOR5 low after the direct scalar read tables failed but the compact-FGP3 data-shape signal remained real. The larger swing was to promote the padded compact FGP3/v4 restore-minus-current pack for low tide and pair it with a compact-only `23..47` retained read, scoped away from high tide and FGP2 fallback. | Replaced `generated/ps1/foreground/VIST5LOW.FG2` with the size-preserving compact candidate from `scratch/ps1-perf-iterate/visitor5-noautoprime-v158-assets/VIST5LOW.final.FG2`, then added a low-tide compact-FGP3-only read group `{23,47,0}`. Focused artifact: `scratch/ps1-perf-iterate/visitor5-low-compact-rg23-47-v451-focused/20260512-211311-3610217/summary.json`. Broad canaries: `scratch/ps1-perf-iterate/visitor5-low-compact-rg23-47-v451-canaries/20260512-211434-3618498/summary.json`; BUILDING2 high control: `scratch/ps1-perf-iterate/visitor5-low-compact-rg23-47-v451-building2-high-control/20260512-211850-3644117/summary.json`; VISITOR5 high control: `scratch/ps1-perf-iterate/visitor5-low-compact-rg23-47-v451-visitor5-high-control/20260512-211957-3650896/summary.json`; FISHING1 high control: `scratch/ps1-perf-iterate/visitor5-low-compact-rg23-47-v451-fishing1-control/20260512-212057-3657089/summary.json`. | Promoted. VISITOR5 low improves scene `1371 -> 1363`, active loop/target `1112/1090 -> 1104/1092`, overrun `22 -> 12`, blocking `12 -> 11`, hidden refill `12 -> 11`, loop reads `25 -> 19`, and loop-read VBlanks `123 -> 91`; due misses stay `0`, pack LBA/sectors stay `24394/173`, and the `217088` byte PS-EXE bucket stays fixed. BUILDING2 high/low, WALKSTUF1 high/low, VISITOR3 low, VISITOR5 high, and FISHING1 high controls stayed on accepted profiles. | Keep as the current VISITOR5 low baseline. Public rollup moves to `+0.3111%` over target / `99.6949%` target speed, raw signed rollup moves to `-0.4559%` / `100.4772%`, and the methodology total is about `17.09` public over-target points removed plus `12.59` target-speed points gained since the compact full-matrix baseline. Next targets are WALKSTUF1 low/high, VISITOR3 low/high, BUILDING2 high/low, VISITOR5 high, JOHNNY1 high/low, BUILDING4 low, and the remaining under-99 tail. |
| 2026-05-12 | `visitor3-low-frame129-delta-v452` | `ps1: add visitor3 low frame129 delta` | Make a larger VISITOR3 low white-whale swing after scalar slack/read-table probes no-opped: compress one hot frame inside already-paid resident bytes instead of moving CD phase. The promotable binary narrowed the earlier multi-frame delta experiment to frame `129` only and kept Freeplay diagnostics default-off to preserve executable bucket headroom. | Authored `scripts/compact-fgp3-delta-previous.py`, moved full frame `128` into the accepted resident slot, and stored frame `129` as a 609-byte custom D4 delta against frame `128` in the freed payload gap. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-frame129-delta-v452-focused/20260512-220759-3925391/summary.json`; WALKSTUF1 high control: `scratch/ps1-perf-iterate/visitor3-low-frame129-delta-v452-walkstuf1-high/20260512-220902-3931852/summary.json`; broad canaries: `scratch/ps1-perf-iterate/visitor3-low-frame129-delta-v452-broad/20260512-221023-3939828/summary.json`; pack summary: `scratch/visitor3-delta-v452/low-frame129-only-summary.json`. | Promoted. VISITOR3 low improves scene `1401 -> 1397`, active loop/target `1072/1040 -> 1068/1041`, overrun `32 -> 27`, blocking `58 -> 51`, loop reads `10 -> 9`, loop-read VBlanks `58 -> 51`, and due misses `10 -> 9`; hidden refill stays `0`, pack LBA/sectors stay `23371/760`, pack bytes stay `1555450`, and the PS-EXE bucket stays `217088`. VISITOR3 high, BUILDING2 high/low, WALKSTUF1 high/low, VISITOR5 high/low, and FISHING1 high canaries stayed on accepted profiles. | Keep as the current VISITOR3 low baseline. Public rollup moves to `+0.3072%` over target / `99.6985%` target speed, raw signed rollup moves to `-0.4598%` / `100.4809%`, and the methodology total is about `17.09` public over-target points removed plus `12.60` target-speed points gained since the compact full-matrix baseline. Next targets are WALKSTUF1 low/high, BUILDING2 high, VISITOR3 high/low, BUILDING2 low, VISITOR5 high, JOHNNY1 high/low, BUILDING4 low, and the remaining under-99 tail. |
| 2026-05-12 | `walkstuf1-low-frame181-delta-v453` | Log-only; pack/source restored and PS1 rebuilt. | Test whether the newly promoted D4 previous-frame delta mechanism generalizes to the top remaining under-99 row. The adjacent-frame scan found WALKSTUF1 low frame `181` could shrink from `4708` bytes to `1484` bytes against frame `180`, reducing that entry's span from four sectors to three without moving pack LBA. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` with a frame `181` D4 delta and added a low-tide `walkstuf1` / frame `181` runtime gate. The first alternate-buffer implementation built in the `217088` byte PS-EXE bucket at `LOAD 0x34346/0x5ca90` and ran `scratch/ps1-perf-iterate/walkstuf1-low-frame181-delta-v453-focused/20260512-223433-4078355/summary.json`. The binary stream-scratch implementation built smaller at `LOAD 0x34316/0x5ca60` and ran `scratch/ps1-perf-iterate/walkstuf1-low-frame181-delta-v453-streamscratch-focused/20260512-223725-4094703/summary.json`. Pack summary: `scratch/walkstuf1-low-frame181-delta-v453/summary.json`. | Failed/no promotion. Both implementations produced the same metrics: scene `1776 -> 1773`, active loop/target `1484/1431 -> 1481/1433`, overrun `53 -> 48`, and prefetch overrun `22 -> 18`, but visible blocking regressed `72 -> 76` and due misses regressed `12 -> 14`. The strict gate rejected the candidate on `cd.blocking_vb`; pack LBA and the `217088` byte PS-EXE bucket stayed fixed, so this is a true phase/cadence tradeoff rather than layout drift. | Close WALKSTUF1 low frame `181` as a standalone D4 delta. The data-shape can improve public loop speed but shifts visible CD ownership the wrong way; any retry needs a paired scheduler/deadline ownership change that keeps blocking at or below `72`, not another decoder buffer variant. Public rollup and methodology totals remain at v452. |
| 2026-05-12 | `building2-low-delta-v454` | `ps1: add building2 low frame deltas` | Continue the custom D4 data-shape lane on BUILDING2 low after hand-authored early groups proved phase-negative. The binary split tested high and low frame `71`/`77` previous-frame deltas independently; high regressed cadence, while low had a large visible blocking win without changing pack LBA or the PS-EXE bucket. | Encoded low frames `71` and `77` as previous-frame D4 deltas against frames `70` and `76`, shrinking frame `71` from `6656 -> 13` bytes and frame `77` from `8828 -> 909` bytes. The generic previous-frame delta decoder is gated to VISITOR3 low frame `129` and BUILDING2 low frames `71`/`77`; high pack bytes were restored after the high-side miss. Focused low artifact: `scratch/ps1-perf-iterate/building2-low-delta-v454-focused/20260512-224520-4141451/summary.json`; high exact-flat control: `scratch/ps1-perf-iterate/building2-low-delta-v454-high-control/20260512-224626-4147967/summary.json`; FISHING1 high control: `scratch/ps1-perf-iterate/building2-low-delta-v454-fishing1-control/20260512-225612-11633/summary.json`; WALKSTUF1 high control: `scratch/ps1-perf-iterate/building2-low-delta-v454-walkstuf1-high-control/20260512-225712-17803/summary.json`; broad canary partial completed accepted profiles before FISHING1 was rerun standalone. | Promoted. BUILDING2 low keeps scene/loop flat at `1619/1349`, improves target `1319 -> 1320`, overrun `30 -> 29`, blocking `80 -> 70`, prefetch overrun `1 -> 0`, loop reads `53 -> 52`, loop-read VBlanks `227 -> 221`, and due misses `17 -> 15`; pack LBA/sectors stay `6818/637` and the PS-EXE bucket stays `217088`. BUILDING2 high stays exact-flat at `1602`, `1351/1311`, overrun `40`, blocking `54`, refill `18`, reads/due `58/7`; FISHING1 high and WALKSTUF1 high also stayed exact-flat, and completed broad canaries stayed on accepted profiles. | Keep as the current BUILDING2 low baseline. Public rollup moves to `+0.3066%` over target / `99.6991%` target speed, raw signed rollup moves to `-0.4604%` / `100.4815%`, and the methodology total remains about `17.09` public over-target points removed plus `12.60` target-speed points gained since the compact full-matrix baseline. Next targets are WALKSTUF1 low/high, BUILDING2 high, VISITOR3 high/low, VISITOR5 high, JOHNNY1 high/low, BUILDING4 low, and the remaining under-99 tail. |
| 2026-05-12 | `walkstuf1-low-frame100-delta-v455` | Log-only; pack/source restored and PS1 rebuilt. | Test the more phase-preserving version of the closed WALKSTUF1 low `190..202` hand-group lane. Frame `100` sits inside that neighborhood and could shrink in-place from `4683` bytes to a 586-byte D4 previous-frame delta against frame `99`, reducing its payload span from three sectors to one without changing pack LBA. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` with only frame `100` encoded as a D4 delta and added a low-tide `walkstuf1` / frame `100` runtime gate. The build stayed in the `217088` byte PS-EXE bucket at `LOAD 0x34396/0x5cae0`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame100-delta-v455-focused/20260512-231248-106491/summary.json`; pack summary: `scratch/walkstuf1-low-frame100-delta-v455/summary.json`. | Failed/no promotion. The candidate was exact-flat against the current low baseline: scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, prefetch overrun `22`, loop reads `66`, loop-read VBlanks `287`, and due misses `12`. Pack LBA and PS-EXE bucket stayed fixed, but no key metric moved. | Close frame `100` as a standalone D4 delta. The byte shrink is real but not exposed to the current read cadence; WALKSTUF1 low still needs either a different sector-boundary byte removal, generated deadline ownership, or custom render/upload data rather than another exact-flat single-frame delta in this range. Public rollup and methodology totals remain at v454. |
| 2026-05-12 | `walkstuf1-low-frame187-delta-v456` | Log-only; pack/source restored and PS1 rebuilt. | Test a later tail D4 candidate after frame `100` proved exact-flat. Frame `187` sits in the same late pressure family as the rejected `371..387` setup-segment/read-group lane and could shrink from `4764` bytes to a 731-byte previous-frame delta against frame `186`, reducing its modeled span from roughly four sectors to one without moving pack LBA. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` with only frame `187` encoded as a D4 delta and added a low-tide `walkstuf1` / frame `187` runtime gate. The build stayed in the `217088` byte PS-EXE bucket at `LOAD 0x34396/0x5cae0`. The first focused run died before `JCPERF2` with `regtest_exit=137` / missing metrics, so it was not used for acceptance; the clean rerun artifact is `scratch/ps1-perf-iterate/walkstuf1-low-frame187-delta-v456-rerun/20260512-231721-133791/summary.json`. Pack summary: `scratch/walkstuf1-low-frame187-delta-v456/summary.json`. | Failed/no promotion. The clean rerun was exact-flat against the accepted low baseline: scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, prefetch overrun `22`, loop reads `66`, loop-read VBlanks `287`, and due misses `12`. Pack LBA and PS-EXE bucket stayed fixed, but no key metric moved. | Close frame `187` as a standalone D4 delta. Together with frames `181` and `100`, this shows WALKSTUF1 low byte shrinkage alone is not enough unless it lands on a scheduler-visible sector/deadline boundary; future work should pair custom data shapes with generated deadline/refill ownership or target a different representation that reduces visible render/upload work. Public rollup and methodology totals remain at v454. |
| 2026-05-12 | `walkstuf1-high-frame100-delta-v457` | Log-only; pack/source restored, current-control CSV refreshed, and PS1 rebuilt. | Test whether the same two-sector frame `100` D4 shrink that was exact-flat in low tide could affect the second-worst under-99 high-tide row, which still has heavier visible blocking and hidden refill pressure. | Temporarily rewrote `generated/ps1/foreground/WALKSTUF1.FG2` with frame `100` encoded as a D4 delta against frame `99`, shrinking `4683 -> 586` bytes, and added a high-tide-only `walkstuf1` / frame `100` runtime gate. The candidate built in the `217088` byte PS-EXE bucket at `LOAD 0x343a6/0x5caf0`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame100-delta-v457-focused/20260512-233124-213065/summary.json`; restored current-control artifact: `scratch/ps1-perf-iterate/walkstuf1-high-current-v458-baseline/20260512-233640-242509/summary.json`; pack summary: `scratch/walkstuf1-high-frame100-delta-v457/summary.json`. | Failed/no promotion after current-baseline review. The harness reported a pass versus the stale v428 matrix row (`1480/1432`, blocking `83`, refill `26`), but the candidate counters are identical to the restored current control: scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking `81`, prefetch overrun `23`, loop reads `65`, loop-read VBlanks `282`, and due misses `16`. Pack LBA stayed fixed and the apparent win came from comparing against an older row, not from the D4 candidate. | Close WALKSTUF1 high frame `100` as a standalone D4 delta and mark the stale-baseline hazard explicitly. The CSV refresh records the current WALKSTUF1 high row as `walkstuf1-high-current-v458-refresh`, moving public rollup to `+0.3033%` over target / `99.7023%` target speed and raw signed rollup to `-0.4637%` / `100.4846%`. Future high D4 work should use a fresh same-label current control or pair byte shrinkage with generated scheduler ownership before treating a stale-row pass as promotable. |
| 2026-05-13 | `building2-high-segment11-27-v459` | Log-only; source restored and PS1 rebuilt. | Test a scheduler/data-ownership version of the rejected BUILDING2 high setup-prime lane. Instead of a contiguous high setup-prime window, retain only the read-plan's early `11..27` sector cluster as a reusable setup segment, aiming to remove several active-loop reads without moving pack layout. | Temporarily added a BUILDING2 high reusable setup segment at sectors `11..27` using the existing setup-segment buffer path. The build stayed in the `217088` byte PS-EXE bucket but grew hot text to `LOAD 0x34426/0x5cb70`. The first focused run died before metrics with missing `JCPERF2`; the clean rerun completed at `scratch/ps1-perf-iterate/building2-high-segment11-27-v459-rerun/20260513-000541-404934/summary.json`. The missing-metrics artifact is `scratch/ps1-perf-iterate/building2-high-segment11-27-v459-focused/20260513-000221-386015/building2-high-canary/perf-summary.json`. | Failed/no promotion. The setup segment reduced loop reads `58 -> 55`, but active timing and CD pressure regressed: scene `1602 -> 1619`, loop/target `1351/1311 -> 1359/1310`, overrun `40 -> 49`, blocking `54 -> 62`, prefetch overrun `18 -> 26`, and loop-read VBlanks `257 -> 250`. The hot-code growth also shifted downstream foreground/graphics symbols by `+192` bytes. | Close BUILDING2 high early reusable setup segment `11..27` as a hand-authored source lane. The read-count signal is real but the extra setup/code cadence costs more than it saves; future BUILDING2 high work needs generated ownership metadata, pack-side byte reductions, or selective/preprocessed render data rather than another local setup-segment constant. Public rollup remains at the v458 CSV refresh. |
| 2026-05-13 | `visitor3-high-frame137-delta-v460` | `ps1: add visitor3 high frame137 delta` | Continue the successful D4 data-shape lane on VISITOR3 high after BUILDING2 high setup residency proved phase-negative. The adjacent-frame scan found frame `137` could shrink from `16828` bytes to a 503-byte D4 previous-frame delta against frame `136`, reducing the modeled active read at file sectors `244..253` without moving pack layout. | Rewrote only `generated/ps1/foreground/VISITOR3.FG2` frame `137` as a D4 delta and gated the previous-frame decoder to VISITOR3 high frame `137` while preserving the existing VISITOR3 low frame `129` gate. Focused control: `scratch/ps1-perf-iterate/visitor3-high-current-v460-baseline/20260513-002012-486603/summary.json`; accepted focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame137-delta-v460-rerun/20260513-002812-532362/summary.json`; first label-mismatch proof: `scratch/ps1-perf-iterate/visitor3-high-frame137-delta-v460-focused/20260513-002650-524346/summary.json`; canary per-case artifacts before the host-killed VISITOR5 low aggregate are under `scratch/ps1-perf-iterate/visitor3-high-frame137-delta-v460-canaries/20260513-003039-546335/`; VISITOR5 low rerun: `scratch/ps1-perf-iterate/visitor3-high-frame137-delta-v460-visitor5low-rerun/20260513-003745-588040/summary.json`; FISHING1 high rerun: `scratch/ps1-perf-iterate/visitor3-high-frame137-delta-v460-fishing1-rerun/20260513-003938-598710/summary.json`; pack summary: `scratch/visitor3-high-frame137-delta-v460/summary.json`. | Promoted as a CD-pressure win. VISITOR3 high keeps scene and active timing flat at `1402` and `1071/1040`, with overrun `31`, hidden refill `0`, loop reads `9`, and due misses `9`, while visible blocking improves `52 -> 51`, loop-read VBlanks `52 -> 51`, total CD sectors `390 -> 388`, and overread bytes `21791 -> 18139`. Pack LBA/sectors stay `22611/760`, pack bytes stay `1555450`, and the PS-EXE bucket stays `217088` despite a small hot-symbol shift. VISITOR3 low, WALKSTUF1 high/low, BUILDING2 high/low, VISITOR5 high/low, and FISHING1 high canaries stayed on accepted profiles. | Keep as the current VISITOR3 high CD-pressure baseline. Public rollup remains `+0.3033%` over target / `99.7023%` target speed because loop/target did not move; raw signed rollup remains `-0.4637%` / `100.4846%`, and the methodology total remains about `17.09` public over-target points removed plus `12.60` target-speed points gained since the compact full-matrix baseline. Next VISITOR3 work should chase loop-speed movement via larger data-shape or generated deadline ownership, not count this as a green-row step. |
| 2026-05-13 | `walkstuf1-low-frame92-delta-v461` | Log-only; pack/source restored and PS1 rebuilt. | Test the next large unclosed WALKSTUF1 low D4 candidate after frames `181`, `100`, and `187` failed or no-opped. Frame `92` has a much smaller decoder command count and sits in the earlier `177..183` read cluster, so it looked more likely to reduce visible CD without the late-tail due-miss penalty. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` with frame `92` encoded as a D4 previous-frame delta against frame `91`, shrinking `6035 -> 2056` bytes, and added a low-tide `walkstuf1` / frame `92` runtime gate. The build stayed in the `217088` byte PS-EXE bucket but moved hot text to `LOAD 0x343b6/0x5cb00`. Fresh control: `scratch/ps1-perf-iterate/walkstuf1-low-current-v461-baseline/20260513-010603-745622/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame92-delta-v461-focused/20260513-010942-766323/summary.json`; pack summary: `scratch/walkstuf1-low-frame92-delta-v461/summary.json`. | Failed/no promotion. The candidate regressed every key timing/CD metric: scene `1776 -> 1790`, active loop/target `1484/1431 -> 1498/1421`, overrun `53 -> 77`, blocking `72 -> 114`, prefetch overrun `22 -> 35`, loop reads `66 -> 70`, loop-read VBlanks `287 -> 330`, and due misses `12 -> 17`. Pack LBA stayed fixed, so this is not a pack-placement/LBA artifact; the D4 data shape changed the visible scheduling path badly. | Close WALKSTUF1 low frame `92` as a standalone D4 delta. The larger lesson is stronger than v453/v455/v456: even earlier two-sector byte reductions can destabilize WALKSTUF1 low unless the scheduler owns the changed deadlines. Public rollup and methodology totals remain at v460. |
| 2026-05-13 | `visitor3-high-frame132-delta-slack4-v462` | `ps1: add visitor3 high frame132 delta guard` | Make the next VISITOR3 high white-whale swing from a fresh adjacent-frame D4 scan. Frame `132` shrinks from `16997` bytes to a 768-byte D4 delta against frame `131`; the raw frame132 data shape removed an active read burst but exposed a strict hidden-refill edge. | Rewrote `generated/ps1/foreground/VISITOR3.FG2` from the pre-v460 pack with D4 deltas for frames `132` and `137`, gated the previous-frame decoder to VISITOR3 high frames `132`/`137`, and raised VISITOR3 high window refill minimum slack to `4` VBlanks. Fresh control: `scratch/ps1-perf-iterate/visitor3-high-current-v462-baseline/20260513-014417-958871/summary.json`; failed combined frame129+132 proof: `scratch/ps1-perf-iterate/visitor3-high-frame129-132-delta-v462-focused2/20260513-014737-979343/summary.json`; failed frame132-only raw proof: `scratch/ps1-perf-iterate/visitor3-high-frame132-delta-v462-focused/20260513-014929-990335/summary.json`; failed frame129-only no-op proof: `scratch/ps1-perf-iterate/visitor3-high-frame129-delta-v462-focused/20260513-015116-1000753/summary.json`; accepted focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame132-minslack4-v462-focused/20260513-015832-1042963/summary.json`; pack summary: `scratch/visitor3-high-frame129-132-delta-v462/frame132-slack4-summary.json`. | Promoted as a strict CD-pressure win. The raw frame132-only candidate improved active loop `1071 -> 1068`, overrun `31 -> 29`, blocking `51 -> 46`, and due misses `9 -> 8`, but failed the hard gate because hidden refill became `1`. The accepted slack4 version keeps scene and active timing flat at `1402` and `1071/1040`, overrun `31`, loop reads `9`, due misses `9`, and hidden refill `0`, while reducing blocking `51 -> 50`, loop-read VBlanks `51 -> 50`, delivered sectors `388 -> 387`, and overread bytes `18139 -> 16704`. VISITOR3 low, BUILDING2 high/low, and WALKSTUF1 high/low canaries stayed on accepted profiles. | Keep as the current VISITOR3 high CD-pressure baseline. Public rollup remains `+0.3033%` over target / `99.7023%` target speed because loop/target did not move; raw signed rollup remains `-0.4637%` / `100.4846%`, and the methodology total remains about `17.09` public over-target points removed plus `12.60` public target-speed points gained since the compact full-matrix baseline. The bigger frame132 speed win is real but requires generated deadline ownership or a safer prefetch slot before it can be promoted. |
| 2026-05-13 | `building2-high-d4-split-v463` | Log-only; pack/source restored and PS1 rebuilt. | Split the only high-tide BUILDING2 D4 candidates after the earlier paired high attempt failed. The current scan shows frame `77` can shrink `8828 -> 909` bytes and frame `71` can shrink `6656 -> 13` bytes; testing them separately answers whether one half can avoid the paired phase regression. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with frame `77` only, then restored and repeated with frame `71` only. The runtime D4 gate was temporarily changed from low-tide-only BUILDING2 frames `71`/`77` to high frame `77`, then high frame `71`. Baseline/control: `scratch/ps1-perf-iterate/building2-high-canary-v462/20260513-020104-1057908/summary.json`; frame77 first killed-before-metrics artifact: `scratch/ps1-perf-iterate/building2-high-frame77-delta-v463-focused/20260513-022005-1165769/`; frame77 rerun: `scratch/ps1-perf-iterate/building2-high-frame77-delta-v463-rerun/20260513-022056-1171704/summary.json`; frame71 focused artifact: `scratch/ps1-perf-iterate/building2-high-frame71-delta-v463-focused/20260513-022306-1184224/summary.json`; pack summaries: `scratch/building2-high-frame77-delta-v463/summary.json` and `scratch/building2-high-frame71-delta-v463/summary.json`. | Failed/no promotion. Frame `77` drops reads `58 -> 57`, but regresses target `1311 -> 1310`, overrun `40 -> 41`, blocking `54 -> 56`, and hidden refill `18 -> 19`; frame `71` is exact-flat at `1602`, `1351/1311`, overrun `40`, blocking `54`, refill `18`, reads/due `58/7`. | Close BUILDING2 high frames `71` and `77` as standalone D4 deltas. Public rollup and methodology totals remain at v462. Future BUILDING2 high work needs generated deadline/refill ownership or a different pack shape; hand tables and standalone D4 are closed for these clusters. |
| 2026-05-13 | `visitor3-high-frame132-setupseg1-v464` | `ps1: preload visitor3 high frame132 delta` | Promote the stronger frame132 speed signal without reintroducing the hidden refill that blocked the raw v462 candidate. The current VISITOR3 high pack already has frame `132` as a 768-byte D4 delta entirely inside file sector `203`, outside the high setup-resident ranges, so the binary question was whether a single retained setup sector could own that deadline cleanly. | Added `FG_VISITOR3_HIGH_SETUP_SEGMENT2_START/BYTES` for one sector at `203` and routed high-tide VISITOR3 through the existing reusable second setup-segment buffer. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame132-setupseg1-v464-focused/20260513-023302-1240332/summary.json`; low canary: `scratch/ps1-perf-iterate/visitor3-high-frame132-setupseg1-v464-canaries/20260513-023504-1251967/summary.json`. | Promoted as a real active-loop win. VISITOR3 high keeps scene timing flat at `1402` and improves active loop/target `1071/1040 -> 1067/1039`, overrun `31 -> 28`, blocking `50 -> 45`, loop reads `9 -> 8`, loop-read VBlanks `50 -> 45`, and due misses `9 -> 8`; hidden refill and prefetch overrun stay `0`. Setup reads rise to `15`, but the extra setup debt is paid before the loop and the active payoff is positive. VISITOR3 low stayed exact-flat at `1397`, `1068/1041`, blocking `51`, reads/due `9/9`. | Keep as the current VISITOR3 high baseline. Public rollup moves to `+0.3010%` over target / `99.7044%` target speed; raw signed rollup moves to `-0.4660%` / `100.4868%`. Methodology total is about `17.10` public over-target points removed and `12.60` public target-speed points gained since the compact full-matrix baseline, with this commit adding about `0.0023` over-target points removed and `0.0021` target-speed points. Next targets: WALKSTUF1 low, BUILDING2 high, WALKSTUF1 high, VISITOR3 high/low, then BUILDING2 low and the remaining yellow rows. |
| 2026-05-13 | `walkstuf1-low-frame189-delta-v465` | Log-only; pack/source restored and PS1 rebuilt. | Start the next WALKSTUF1 low pack/data-shape lane after direct read groups and setup segments repeatedly proved phase-negative. The adjacent-frame D4 scan ranked frame `189` as the cleanest late low-tide byte shrink: `5382 -> 1444` bytes, collapsing its payload from sectors `386..388` to sector `386` without moving pack LBA. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` frame `189` as a D4 previous-frame delta against frame `188` and added a low-tide `walkstuf1` / frame `189` runtime gate. Transform summary: `scratch/walkstuf1-low-frame189-delta-v465/summary.json`. The first focused run died before `JCPERF2` sections and was not used for acceptance: `scratch/ps1-perf-iterate/walkstuf1-low-frame189-delta-v465-focused/20260513-025410-1359535/`. The clean rerun completed at `scratch/ps1-perf-iterate/walkstuf1-low-frame189-delta-v465-rerun/20260513-025440-1363232/summary.json`. | Failed/no promotion. The byte shrink reduced loop reads `66 -> 65`, but public timing and pressure regressed: scene `1776 -> 1779`, active loop/target `1484/1431 -> 1487/1429`, overrun `53 -> 58`, blocking `72 -> 80`, hidden refill `22 -> 23`, and due misses `12 -> 13`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed, so this is a real deadline/phase failure rather than layout drift. | Close frame `189` as a standalone WALKSTUF1 low D4 delta. The lesson matches frame `92` but from the late cluster: even a two-sector byte collapse can worsen deadline ownership unless generated scheduling or a more structural pack representation moves the work earlier. Public rollup and methodology totals remain at v464. |
| 2026-05-13 | `walkstuf1-low-setup-hole-d4-v466` | Log-only; pack/source restored and PS1 rebuilt. | Make a larger setup-prime hole-fill swing after standalone low D4 probes showed byte savings but no safe deadline ownership. Frame `28` was rewritten as a D4 delta (`6764 -> 3098`) to open a `3666` byte gap inside the already-paid setup-prime range, then late frames `181`, `187`, and `189` were rewritten as D4 deltas and relocated into that gap so a persistent 2-sector sidecar could copy the range from the setup-prime buffer instead of issuing new setup CD reads. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2`, added low-tide WALKSTUF1 D4 gates for frames `28`, `181`, `187`, and `189`, and added a WALKSTUF1 low setup-hole segment copied from the existing setup window. Transform summary: `scratch/walkstuf1-low-setup-hole-d4-v466/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setup-hole-d4-v466-focused/20260513-030820-1440751/summary.json`. | Failed/no promotion. The probe preserved pack LBA and the `217088` byte PS-EXE bucket, saved one loop read (`66 -> 65`), reduced hidden refill `22 -> 19`, and reduced hidden CD VBlanks `215 -> 201`, but it regressed the acceptance counters: active loop stayed `1484`, target fell `1431 -> 1429`, overrun rose `53 -> 55`, visible blocking rose `72 -> 82`, and due misses rose `12 -> 15`. The source shape also moved hot foreground symbols, including `foregroundPilotPlay` size `+464`, so the saved read was more than erased by due-path ownership and code-shape cost. | Close setup-prime D4 hole-fill sidecars in this form. The useful signal is that persistent setup-owned late bytes can lower hidden refill, but WALKSTUF1 low needs a generated sidecar that does not add hot-loop code or D4 decode debt, or a custom representation that changes visual work without moving deadline ownership. Public rollup and methodology totals remain at v464. |
| 2026-05-13 | `building2-high-rg202-226-v467` | Log-only; source restored and PS1 rebuilt. | Test the largest fresh BUILDING2 high current read-plan signal after the `11..27` setup segment and standalone D4 lanes closed. The plan showed the tight middle cluster `202..226` could theoretically cover six reads and align into the accepted `226..242` follow-up, so the binary question was whether replacing the accepted `206..230` row with `202..226` could capture the cluster without additional code or pack changes. | Temporarily changed the BUILDING2 high read-group table from `{206, 230, 0}` to `{202, 226, 0}` while keeping `{60,72}`, `{226,242}`, and `{249,257}` unchanged. The first focused run was host-killed before `JCPERF2` and is non-signal: `scratch/ps1-perf-iterate/building2-high-rg202-226-v467-focused/20260513-031704-1490140/`. The clean rerun emitted complete metrics and then hit the known post-success wrapper `137`: `scratch/ps1-perf-iterate/building2-high-rg202-226-v467-rerun/20260513-031728-1493566/summary.json`. | Failed/no promotion. The group shift was exact-flat against v441 on every acceptance counter: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, loop reads `58`, and due misses `7`. It also moved hot foreground symbols and changed `foregroundPilotPlay` size by `+156`, so there is no work-reduction or code-shape reason to carry it. | Keep the accepted `206..230` row. Close BUILDING2 high `202..226` as a hand-authored grouped-read replacement; the current middle cluster still needs generated deadline ownership or pack-side data reduction rather than another table nudge. Public rollup and methodology totals are unchanged. |
| 2026-05-13 | `building2-high-frame91-delta-v468` | Log-only; pack/source restored and PS1 rebuilt. | Test the only remaining BUILDING2 high adjacent-frame D4 candidate with a real sector-boundary effect after frames `71` and `77` closed. Frame `91` shrinks only `8867 -> 8766` bytes, but that is enough to move its payload end from sector `150` to `149`, so the binary question was whether a minimal pack-side byte reduction could shave blocking without upsetting cadence. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` frame `91` as a D4 delta against frame `90` and gated high-tide BUILDING2 frame `91` through the previous-frame delta decoder. Transform summary: `scratch/building2-high-frame91-delta-v468/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-frame91-delta-v468-focused/20260513-032311-1526380/summary.json`. | Failed/no promotion. The sector-boundary shrink improved visible blocking by one VBlank (`54 -> 53`), but the D4 decode/cadence cost regressed scene `1602 -> 1631`, active loop/target `1351/1311 -> 1380/1310`, and overrun `40 -> 70`; hidden refill stayed `18`, reads stayed `58`, and due misses stayed `7`. Hot foreground symbols shifted and `foregroundPilotPlay` grew by `156` bytes. | Close BUILDING2 high micro D4 sector-boundary deltas. The remaining adjacent-frame savings are too small to pay for runtime decode/code-shape cost; BUILDING2 high needs generated deadline ownership or a data format that reduces render/CD work without adding hot decode overhead. Public rollup and methodology totals are unchanged. |
| 2026-05-13 | `visitor3-high-frame129-current-v469` | Log-only; pack/source restored and PS1 rebuilt. | Retest the large VISITOR3 high frame `129` D4 candidate against the current v464 baseline after frame `132` gained setup-segment ownership. The earlier v462 frame129-only probe was flat before the setup-segment change; the binary question was whether the current deadline ownership would let the `17025 -> 609` byte / eight-sector shrink become visible. | Temporarily rewrote `generated/ps1/foreground/VISITOR3.FG2` frame `129` as a D4 delta against frame `128` while preserving the accepted frame `132` and `137` deltas, then gated high-tide VISITOR3 frame `129` through the previous-frame delta decoder. Transform summary: `scratch/visitor3-high-frame129-current-v469/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame129-current-v469-focused/20260513-033037-1569248/summary.json`. | Failed/no promotion. The run hit a baseline-label mismatch, but the emitted counters were exact-flat against the accepted v464 row: scene `1402`, active loop/target `1067/1039`, overrun `28`, blocking `45`, hidden refill `0`, loop reads `8`, and due misses `8`. There is no key metric or work-volume improvement to promote. | Close VISITOR3 high frame `129` as a standalone current-layout D4 delta. The remaining high-tide VISITOR3 D4 wins need either setup ownership for a frame that actually moves loop timing or a broader generated deadline plan; frame `129` alone remains inert. Public rollup and methodology totals are unchanged. |
| 2026-05-13 | `visitor3-low-frame132-delta-v470` | `ps1: add visitor3 low frame132 delta` | Continue the VISITOR3 low custom D4 lane after frame `129` became the current low baseline. Frame `132` shrinks from `16997` bytes to a 768-byte previous-frame D4 delta against frame `131`, collapsing another hot payload to one sector without moving pack LBA. | Rewrote `generated/ps1/foreground/VIST3LOW.FG2` frame `132` as a D4 delta while preserving the accepted frame `129` delta, then gated VISITOR3 low frames `129` and `132` through the previous-frame delta decoder. Transform summary: `scratch/visitor3-low-frame132-delta-v470/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-frame132-delta-v470-focused/20260513-033442-1593006/summary.json`. Aggregate canaries under `scratch/ps1-perf-iterate/visitor3-low-frame132-delta-v470-canaries/20260513-033604-1600971/` completed VISITOR3 high, BUILDING2 high/low, and WALKSTUF1 high on accepted profiles before the host killed the final WALKSTUF1 low wrapper; standalone WALKSTUF1 low rerun: `scratch/ps1-perf-iterate/visitor3-low-frame132-delta-v470-walkstuf1-low-rerun/20260513-034012-1626312/summary.json`. | Promoted as a CD-pressure win. VISITOR3 low keeps scene and active timing flat at `1397` and `1068/1041`, with overrun `27`, hidden refill `0`, loop reads `9`, and due misses `9`, while visible blocking improves `51 -> 50` and loop-read VBlanks improve `51 -> 50`. Pack LBA/sectors stay `23371/760`, pack bytes stay `1555450`, and the PS-EXE bucket stays `217088`; canaries stayed on accepted profiles. | Keep as the current VISITOR3 low baseline. Public rollup remains `+0.3010%` over target / `99.7044%` target speed because loop/target did not move; raw signed rollup remains `-0.4660%` / `100.4868%`, and the methodology total remains about `17.10` public over-target points removed plus `12.60` target-speed points gained since the compact full-matrix baseline. This commit does not add speed points, but it removes one more visible blocking/read VBlank from VISITOR3 low. |
| 2026-05-13 | `walkstuf1-low-frame59-delta-v471` | Log-only; pack/source restored and PS1 rebuilt. | Test an untried earlier WALKSTUF1 low D4 candidate after the late-tail and setup-hole D4 lanes failed. Frame `59` shrinks from `4200` bytes to a 1452-byte previous-frame D4 delta against frame `58`, collapsing its modeled payload from sectors `109..111` to about one sector without moving pack LBA. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` frame `59` as a D4 delta and gated low-tide WALKSTUF1 frame `59` through the previous-frame delta decoder. Transform summary: `scratch/walkstuf1-low-frame59-delta-v471/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame59-delta-v471-focused/20260513-035435-1708169/summary.json`. Source and pack were restored from `scratch/walkstuf1-low-frame59-delta-v471/WALK1LOW.before-v471.FG2`, and PS1 was rebuilt afterward. | Failed/no promotion. The candidate was exact-flat on all acceptance counters: scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, loop reads `66`, loop-read VBlanks `287`, and due misses `12`. It also shifted/got larger hot foreground symbols, including `foregroundPilotPlay +160`, so there is no work-volume reason to promote. | Close frame `59` as a standalone WALKSTUF1 low D4 delta. The D4 lesson now covers early, middle, and late low-tide sectors: byte collapses alone are inert or phase-negative unless scheduler ownership changes too. Public rollup and methodology totals remain unchanged at v470. |
| 2026-05-13 | `walkstuf1-low-rg78-90-v472` | Log-only; source restored and PS1 rebuilt. | Test a different WALKSTUF1 low lane after standalone D4 closed: the current read plan ranked `78..90` as a post-setup-prime group with a long first gap and one expected saved read. Unlike the late `285..309` and `190..202` misses, this starts at the first uncovered post-prime read and answers whether that safer phase boundary can carry a plain retained table. | Temporarily inserted `{78,90,0}` at the front of the shared WALKSTUF1 retained-read table. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg78-90-v472-focused/20260513-035757-1727765/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. Due misses improved `12 -> 11`, but visible cadence and CD pressure regressed: scene `1776 -> 1780`, active loop/target `1484/1431 -> 1488/1428`, overrun `53 -> 60`, blocking `72 -> 79`, hidden refill `22 -> 27`, loop reads `66 -> 68`, and loop-read VBlanks `287 -> 293`. The PS-EXE bucket stayed `217088`, but hot foreground symbols shifted and `foregroundPilotPlay` grew by `160` bytes. | Close WALKSTUF1 low `78..90` as a standalone retained group. Even the long first post-prime gap converts saved due work into hidden/visible cadence debt; remaining WALKSTUF1 low read groups need generated scheduler ownership rather than another plain table. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `policy-cache-v473` | Log-only; source restored and PS1 rebuilt. | Test a runtime-shape swing instead of another pack/table nudge: cache scene/tide policy bits and the window-slack threshold at scene start, then replace hot-loop `fgSceneEquals()` policy checks in previous-frame D4, direct-stage, visitor fallthrough, WALKSTUF1 window gating, and the prepare-before-window branch. | Temporarily added `scenePolicyFlags` and `windowMinSlackVBlanks` to `TFgPilotRuntime` and routed the affected hot policy checks through those cached values. Focused artifacts: WALKSTUF1 low `scratch/ps1-perf-iterate/walkstuf1-low-policy-cache-v473-focused/20260513-040619-1776152/summary.json`; BUILDING2 high cross-check `scratch/ps1-perf-iterate/policy-cache-v473-building2-high-focused/20260513-040740-1784058/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. WALKSTUF1 low stayed exact-flat at scene `1776`, active loop/target `1484/1431`, overrun `53`, blocking `72`, hidden refill `22`, loop reads `66`, loop-read VBlanks `287`, and due misses `12`. BUILDING2 high also stayed exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, reads/due `58/7`. The PS-EXE bucket stayed `217088`, but BUILDING2 high moved/got larger hot symbols including `fgRuntimeLoadSceneFrame +284` and `foregroundPilotPlay +168`. | Close naive scene-policy caching as a speed lane. It can shrink some local functions, but the code-shape movement is scene-dependent and produced no measurable timing/CD win on the top two affected outliers. Retry only as a broader cold-policy refactor with a hot-symbol budget and a same-layout proof, not as a one-off cache field. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-rg78-91-v474` | `ps1: add walkstuf1 low rg78-91` | Revisit the first post-prime WALKSTUF1 low retained-read lane after v472 showed the due-miss signal but failed as `78..90`. CD comparison showed the narrowed row left an overlapping follow-up read beginning at sector `89`, so the binary boundary fix was to extend the retained row through sector `91` and let the shared table own the full cluster. | Added `{78,91,0}` to the shared WALKSTUF1 retained-read table ahead of the existing `201..213`, `213..229`, `344..360`, `422..434`, `443..455`, and `444..456` rows. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg78-91-v474-focused/20260513-041155-1808725/summary.json`; WALKSTUF1 high canary: `scratch/ps1-perf-iterate/walkstuf1-low-rg78-91-v474-high-canary/20260513-041311-1816101/summary.json`; BUILDING2/VISITOR3 layout canaries: `scratch/ps1-perf-iterate/walkstuf1-low-rg78-91-v474-layout-canaries/20260513-041423-1823964/summary.json`. | Promoted. WALKSTUF1 low improves scene `1776 -> 1770`, active loop/target `1484/1431 -> 1478/1431`, overrun `53 -> 47`, blocking `72 -> 64`, hidden refill `22 -> 20`, loop reads `66 -> 64`, loop-read VBlanks `287 -> 286`, and due misses `12 -> 11`. Pack LBA/sectors stay `25633/750`, pack bytes stay `1535263`, and the PS-EXE bucket stays `217088`. WALKSTUF1 high, BUILDING2 high/low, and VISITOR3 high/low canaries stayed on accepted profiles. | Keep as the current WALKSTUF1 low baseline. Public rollup moves to `+0.2977%` over target / `99.7075%` target speed; raw signed rollup moves to `-0.4693%` / `100.4899%`. Methodology total is about `17.10` public over-target points removed and `12.61` public target-speed points gained since the compact full-matrix baseline, with this commit adding about `0.0033` public over-target points removed and `0.0031` public target-speed points. Next targets: BUILDING2 high, WALKSTUF1 high, VISITOR3 high/low, then BUILDING2 low and the remaining yellow rows. |
| 2026-05-13 | `walkstuf1-low-rg78-98-v475` | Log-only; source restored and PS1 rebuilt. | Test whether the v474 boundary insight can extend one more uncovered early read. The accepted `78..91` group owns the first post-prime cluster; the next observed read is `91..98`, so replacing it with `78..98` still fits the 24-sector grouped window and asks whether the same 3.21s post-prime gap can safely pay for both reads. | Temporarily changed the shared WALKSTUF1 retained-read table from `{78,91,0}` to `{78,98,0}` and ran focused WALKSTUF1 low against the v474 baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg78-98-v475-focused/20260513-043439-1939676/summary.json`. Source was restored to `{78,91,0}` and the PS1 executable was rebuilt afterward. | Failed/no promotion. The extension regressed hard: scene `1770 -> 1783`, active loop/target `1478/1431 -> 1491/1424`, overrun `47 -> 67`, blocking `64 -> 101`, hidden refill `20 -> 26`, loop reads `64 -> 65`, loop-read VBlanks `286 -> 303`, and due misses `11 -> 15`. Pack LBA stayed `25633`, sectors stayed `750`, and the PS-EXE bucket stayed `217088`, so this is a real phase/deadline failure rather than layout drift. | Keep the v474 `78..91` boundary as the accepted endpoint. With older `78..94` already closed and `78..98` worse, do not extend the first post-prime table row past sector `91` as a hand-authored group. Remaining WALKSTUF1 low work should move to generated deadline ownership, a cold sidecar that does not add hot-loop code, or a custom data shape that changes work without pulling more early sectors into this visible phase. Public rollup and methodology totals remain unchanged from v474. |
| 2026-05-13 | `visitor3-low-frame128-132-resident-v476` | Log-only; pack/source restored. | Make a larger VISITOR3 low white-whale swing by compressing frame `128` as D4 and moving frame `132` into a low setup-resident segment. The goal was to convert the existing frame132 D4 byte win into active-loop ownership while testing whether frame128 could open enough setup-owned space. | Temporarily rewrote `generated/ps1/foreground/VIST3LOW.FG2`, gated VISITOR3 low frame `128` through the previous-frame D4 decoder, and tried setup staging guards around the moved frame132 payload. Candidate files are under `scratch/visitor3-low-frame128-132-resident-v476/`. Focused low proof showed the intended timing shape, but the BUILDING2 low canary failed before complete `JCPERF2`; after restoring source and pack, BUILDING2 low passed exact-flat at `scratch/ps1-perf-iterate/baseline-building2-low-runner-check-after-v476/20260513-050241-2109676/summary.json`. | Failed/no promotion. The focused low candidate improved scene `1397 -> 1394`, active loop `1068 -> 1065`, overrun `27 -> 24`, blocking `50 -> 45`, reads `9 -> 8`, and due misses `9 -> 8`, but the source/pack shape was not canary-stable. The restored control proved the canary issue was tied to the candidate state, not the BUILDING2 low baseline. | Close frame128-D4 plus source-owned setup staging in this form. The useful signal was not the new D4 frame128 path; it was that frame132 can be owned earlier. Prefer a source-neutral pack-only relocation if existing setup-prime slack can hold frame132. Public rollup and methodology totals remain unchanged from v474. |
| 2026-05-13 | `visitor3-low-frame132-primegap-v477` | `ps1: relocate visitor3 low frame132 into prime gap` | Convert the v476 speed signal into a source-neutral pack-only change. VIST3LOW's existing setup-prime range has an unused gap at offset `203181` inside sector `99`, large enough for the already-accepted 768-byte frame132 D4 payload, so the binary question was whether moving only that payload would own the deadline without changing code, pack footprint, LBA, or sectors. | Repacked `generated/ps1/foreground/VIST3LOW.FG2` from the current baseline with frame `132` moved to offset `203181`. Transform summary: `scratch/visitor3-low-frame132-primegap-v477/summary.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-low-frame132-primegap-v477-focused/20260513-050511-2124475/summary.json`; BUILDING2 low canary: `scratch/ps1-perf-iterate/visitor3-low-frame132-primegap-v477-building2-low-canary-rerun/20260513-051014-2155245/summary.json`. | Promoted. VISITOR3 low improves scene `1397 -> 1394`, active loop/target `1068/1041 -> 1065/1041`, overrun `27 -> 24`, blocking `50 -> 45`, loop-read VBlanks `50 -> 45`, loop reads `9 -> 8`, and due misses `9 -> 8`; hidden refill and prefetch overrun stay `0`. VIST3LOW remains `1555450` bytes at LBA/sectors `23371/760`, and the PS-EXE bucket stays `217088`. BUILDING2 low stayed exact-flat at `1619`, `1349/1320`, overrun `29`, blocking `70`, refill `0`, reads/due `52/15`. | Keep as the current VISITOR3 low baseline. Public rollup moves to `+0.2954%` over target / `99.7097%` target speed; raw signed rollup moves to `-0.4716%` / `100.4920%`. Methodology total remains about `17.10` public over-target points removed and `12.61` public target-speed points gained since the compact full-matrix baseline, with this commit adding about `0.0023` public over-target points removed and `0.0022` public target-speed points. Next targets: WALKSTUF1 low, BUILDING2 high, WALKSTUF1 high, VISITOR3 high, then BUILDING2 low and the remaining yellow rows. |
| 2026-05-13 | `walkstuf1-low-rg273-285-slack8-v497` | Log-only; source restored and PS1 rebuilt. | Test a scheduler-owned version of the current read plan's `273..285` low-tide candidate. The planner ranked the row as low visible risk with one expected saved read, but raw WALKSTUF1 low read groups have repeatedly been phase-negative, so the candidate used a low-only table and gated `{273,285}` behind an 8-VBlank minimum slack. | Temporarily split WALKSTUF1 read groups so low tide preserved the accepted rows plus `{273,285,8}` while high retained the existing shared table. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg273-285-slack8-v497-focused/20260513-080801-3201763/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The guard reduced loop reads `64 -> 63`, but visible pressure regressed: blocking `64 -> 66` and hidden refill `20 -> 21`, while scene, active loop/target, overrun, and due misses stayed `1770`, `1478/1431`, `47`, and `11`. Pack LBA and the PS-EXE bucket stayed fixed. | Close `273..285` as a guarded low-only table under the current scheduler. The saved read is real, but it still lands in a worse visible phase; future WALKSTUF1 low work needs generated per-frame/deadline ownership or a data shape that removes visual/CD work without adding hot-loop table/code shape. Public rollup and methodology totals remain at v0.8.8. |
| 2026-05-13 | `building2-high-rg60-76-v498` | Log-only; source restored and PS1 rebuilt. | Test the only current BUILDING2 high low-visible-risk extension of an accepted row. The read plan ranked `60..76` as a balanced 16-sector form that keeps the accepted `60..72` fire point, adds one modeled saved read, and has medium first/internal gaps rather than the tight middle-cluster profile. | Temporarily extended the high-tide BUILDING2 retained-read row from `{60,72,0}` to `{60,76,0}` while leaving `{206,230}`, `{226,242}`, and `{249,257}` unchanged. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg60-76-v498-focused/20260513-081558-3248330/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The extension reduced loop reads `58 -> 57`, but moved the cost into visible cadence: scene `1602 -> 1608`, active loop/target `1351/1311 -> 1357/1309`, overrun `40 -> 48`, blocking `54 -> 60`, hidden refill `18 -> 26`, and due misses stayed `7`. Pack LBA and PS-EXE bucket stayed fixed. | Keep the accepted `60..72` boundary. Close `60..76` as a hand-authored extension; BUILDING2 high read reductions after v441 need generated deadline ownership or pack-side work reduction, not a wider early retained row. Public rollup and methodology totals remain at v0.8.8. |
| 2026-05-13 | `building2-high-rg312-336-v499` | Log-only; source restored and PS1 rebuilt. | Test the largest unclosed low-visible-risk BUILDING2 high retained-read row after the accepted-row extension failed. The current read plan ranked `312..336` as a 24-sector exact append-start candidate with two modeled saved loop reads and low overlap risk, away from the failed early and middle clusters. | Temporarily appended `{312,336,0}` to `kBuilding2HighReadGroups12` after the accepted `{249,257}` row, preserving the 24-sector grouped-read capacity. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg312-336-v499-focused/20260513-082313-3289865/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The row produced complete metrics but no key improvement: scene stayed `1602`, active loop/target stayed `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, loop reads `58`, loop-read VBlanks `257`, due misses `7`, and `group_hits=0`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close `312..336` as a standalone retained-read table. The planner's modeled tail read savings are not exposed by the current runtime append state, so BUILDING2 high should move to generated deadline ownership, pack-side work reduction, or a generated append-start plan rather than more scalar late rows. Public rollup and methodology totals remain at v0.8.8. |
| 2026-05-13 | `building2-high-rg53-77-v500` | Log-only; source restored and PS1 rebuilt. | Test whether the failed early `60..76` extension could be made phase-safe by starting earlier at the previous modeled slack gap. The `53..77` row covers the accepted `60..72` region plus the next read and was modeled as a two-read 24-sector candidate. | Temporarily inserted `{53,77,0}` before the accepted BUILDING2 high rows while leaving `{60,72}`, `{206,230}`, `{226,242}`, and `{249,257}` in place. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg53-77-v500-focused/20260513-082700-3312109/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The row completed with exact-flat counters: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, loop reads `58`, loop-read VBlanks `257`, and due misses `7`. Pack LBA/sectors and PS-EXE bucket stayed fixed, but no key metric improved. | Close `53..77` as an early-wide scalar retained row. Together with v498 and v499, this leaves BUILDING2 high needing generated deadline/append-start ownership or pack-side byte/work reduction rather than more plain table entries. Public rollup and methodology totals remain at v0.8.8. |
| 2026-05-13 | `visitor3-high-frame137-sector203-v501` | `ps1: relocate visitor3 high frame137 setup` | Make the next VISITOR3 high white-whale swing source-neutral: v464 already pays one setup sector at sector `203` for frame132's 768-byte D4 payload, and the accepted frame137 D4 payload is only 503 bytes. The binary question was whether moving frame137 into the same sector before frame132 would own one more deadline without changing source, pack footprint, LBA/sectors, or the PS-EXE bucket. | Repacked `generated/ps1/foreground/VISITOR3.FG2` with frame `137` moved from offset `501526` / sectors `244..246` into offset `415744` / sector `203`, before frame132 at offset `416540`. Pack size stays `1555450`; transform summary: `scratch/visitor3-high-frame137-sector203-v501/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame137-sector203-v501-focused/20260513-083037-3333155/summary.json`; canaries: VISITOR3 low `scratch/ps1-perf-iterate/visitor3-low-canary-after-v501/20260513-083340-3351598/summary.json`, BUILDING2 high `scratch/ps1-perf-iterate/building2-high-canary-after-v501/20260513-083449-3358542/summary.json`, BUILDING2 low `scratch/ps1-perf-iterate/building2-low-canary-after-v501/20260513-083548-3364689/summary.json`, WALKSTUF1 low `scratch/ps1-perf-iterate/walkstuf1-low-canary-after-v501/20260513-083648-3370770/summary.json`, and WALKSTUF1 high `scratch/ps1-perf-iterate/walkstuf1-high-canary-after-v501/20260513-083748-3358541/summary.json`. | Promoted. VISITOR3 high improves scene `1402 -> 1400`, active loop/target `1067/1039 -> 1065/1039`, overrun `28 -> 26`, blocking `45 -> 41`, loop-read VBlanks `45 -> 41`, loop reads `8 -> 7`, and due misses `8 -> 7`; hidden refill and prefetch overrun stay `0`. Pack LBA/sectors stay `22611/760`, pack bytes stay `1555450`, and the PS-EXE bucket stays `217088`. VISITOR3 low, BUILDING2 high/low, and WALKSTUF1 high/low canaries stayed exact-flat on accepted profiles. | Keep as the current VISITOR3 high baseline. Public rollup moves to `+0.2852%` over target / `99.7198%` target speed; raw signed rollup moves to `-0.4820%` / `100.5021%`. Methodology total remains about `17.11` public over-target points removed and `12.62` public target-speed points gained since the compact full-matrix baseline, with this commit adding about `0.0015` public over-target points removed and `0.0015` public target-speed points. Next targets: WALKSTUF1 low, BUILDING2 high, WALKSTUF1 high, VISITOR3 high/low, then BUILDING2 low and the remaining yellow rows. |
| 2026-05-13 | `walkstuf1-low-mid-rg-v502` | Log-only; source restored and PS1 rebuilt. | Probe the next WALKSTUF1 low retained-read family after the v501 baseline. The current read-plan showed several low-only append-start candidates beyond the accepted first post-prime `78..91` row, so the test split the WALKSTUF1 table by tide and tried mid-row candidates without disturbing high tide. | Temporarily added a low-only WALKSTUF1 read-group table and tested `{196,208}`, `{190,202}`, `{209,225}`, and `{225,241}` against the accepted v474 low baseline. Focused artifacts: `{196,208}` `scratch/ps1-perf-iterate/walkstuf1-low-rg196-208-v502-focused/20260513-085527-3476053/summary.json`; `{190,202}` `scratch/ps1-perf-iterate/walkstuf1-low-rg190-202-v502-focused/20260513-085657-3484757/summary.json`; `{209,225}` `scratch/ps1-perf-iterate/walkstuf1-low-rg209-225-v502-focused/20260513-085813-3492594/summary.json`; `{225,241}` `scratch/ps1-perf-iterate/walkstuf1-low-rg225-241-v502-focused/20260513-085935-3500984/summary.json`. | Failed/no promotion. `{196,208}` was exact-flat at `1770`, `1478/1431`, overrun `47`, blocking `64`, refill `20`, reads/due `64/11`; `{190,202}` regressed to `1782`, `1490/1428`, overrun `62`, blocking `96`, refill `27`, due `15`; `{209,225}` saved one loop read (`64 -> 63`) but regressed to `1782`, `1490/1431`, overrun `59`, blocking `93`, refill `30`, due `14`; `{225,241}` was exact-flat with added hot code shape. | Close this mid-row low-only scalar table family. The useful signal is negative: even modeled low-risk append starts either do not fire in a promotable way or convert saved reads into visible/refill debt. Future WALKSTUF1 low work should move to generated deadline ownership, source-neutral pack relocation, or upload/restore work reduction rather than more hand-authored retained rows. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `building2-high-rg298-322-v503` | Log-only; source restored and PS1 rebuilt. | Test the next BUILDING2 high retained-read row after the early, middle, and late scalar groups closed. The current read-plan ranked `298..322` as a medium-risk append-start candidate with two modeled saved reads and less visible-gap pressure than the tight middle cluster, so this was the last plausible direct table before moving BUILDING2 high toward generated ownership or pack-side work removal. | Temporarily appended `{298,322,0}` to `kBuilding2HighReadGroups12` after the accepted `{249,257}` row, preserving the current 24-sector grouped-read capacity. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg298-322-v503-focused/20260513-090500-3532417/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The row produced complete metrics but no key improvement: scene stayed `1602`, active loop/target stayed `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, loop reads `58`, and due misses `7`. Pack LBA and the PS-EXE bucket stayed fixed; the source edit only shifted hot foreground symbols, including `foregroundPilotPlay +164`, with no timing/work payoff. | Close `298..322` as a standalone retained-read table. BUILDING2 high scalar grouped reads are now saturated around the current append state; further progress should use generated deadline ownership, explicit append-start metadata, or pack-side upload/cleanup reduction rather than another hand-authored row. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `walkstuf1-low-rg285-297-v504` | Log-only; source restored and PS1 rebuilt. | Return to the largest remaining under-99 row and retest the high-upside low-tide `285..297` cluster under the current v501/v502 baseline, but isolate it from high tide. The row modeled two saved reads in the current read plan, while the adjacent pack-only probes showed no cheap cleanup transform: no-op compaction zeroed `0` entries, draw-tail trimming saved `0` bytes, and restore-minus-current grew active payload (`916139 -> 917347`). | Temporarily split the WALKSTUF1 retained-read table by tide so high retained the accepted shared rows, while low added `{285,297,0}` after `{213,229}`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg285-297-v504-focused/20260513-091020-3564157/summary.json`; pack probe scratch outputs are under `scratch/walkstuf1-pack-probes-v504/`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The row saved one loop read (`64 -> 63`) but regressed all acceptance counters: scene `1770 -> 1778`, active loop/target `1478/1431 -> 1486/1431`, overrun `47 -> 55`, blocking `64 -> 75`, hidden refill `20 -> 26`, and due misses stayed `11`. Pack LBA and the PS-EXE bucket stayed fixed; the source split also shifted hot symbols and grew `foregroundPilotPlay` by `36` bytes. | Close `285..297` again on the current baseline, including the low-only split form. The remaining WALKSTUF1 low gap is not a missing scalar row or a cheap existing cleanup transform; it needs generated deadline ownership, a code-neutral sidecar, or a custom upload/data representation that removes visual work without pulling the tight cluster into visible CD/refill debt. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `building2-low-rg314-338-v505` | Log-only; source restored and PS1 rebuilt. | Test whether the current BUILDING2 low accepted middle row should be widened and shifted earlier. The refreshed v501 read plan ranked `314..338` as a 24-sector low-visible-risk replacement around the accepted `318..330` row, with two modeled saved reads and a long first gap. | Temporarily replaced `{318,330,0}` in `kBuilding2LowReadGroups12` with `{314,338,0}`, preserving the accepted `{238,250}` and `{365,381}` rows. Focused artifact: `scratch/ps1-perf-iterate/building2-low-rg314-338-v505-focused/20260513-091533-3594529/summary.json`. Source was restored to `{318,330,0}` and PS1 was rebuilt afterward. | Failed/no promotion. The replacement reduced CD pressure (`loop_reads 52 -> 50`, `loop_read_vb 221 -> 217`, `blocking_vb 70 -> 66`, due misses `15 -> 13`) with hidden refill still `0`, but it regressed strict public timing: scene `1619 -> 1623`, active loop/target `1349/1320 -> 1353/1321`, and overrun `29 -> 32`. Pack LBA/sectors and the `217088` byte PS-EXE bucket stayed fixed. | Keep the accepted `{318,330}` middle row. Close `314..338` as a hand-authored replacement: the read saving is real, but the wider phase point costs more public cadence than it removes. Future BUILDING2 low work should use generated refill/deadline ownership or pack-side work reduction before replacing accepted rows. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `walkstuf1-shared-rg201-225-v506` | Log-only; source restored and PS1 rebuilt. | Test whether the accepted adjacent WALKSTUF1 high read groups `{201,213}` and `{213,229}` should be merged into a single 24-sector `{201,225}` row. The read-plan output showed a current-fit `201..225` shape with modeled savings, and this replacement reduced table shape instead of adding another scalar row. | Temporarily replaced the two adjacent rows in `kWalkstuf1HighReadGroups12` with `{201,225,0}` and ran the focused high gate first because the shared table affects both tides. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-shared-rg201-225-v506-high-focused/20260513-092732-3662200/summary.json`. Source was restored to the accepted split rows and PS1 was rebuilt afterward. | Failed/no promotion. The merge regressed every acceptance counter on WALKSTUF1 high: scene `1764 -> 1782`, active loop/target `1476/1434 -> 1494/1427`, overrun `42 -> 67`, blocking `81 -> 108`, refill stayed `23`, loop reads stayed `65`, and due misses worsened `16 -> 18`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Keep `{201,213}` and `{213,229}` split. The lost `225..229` coverage and changed phase are worse than the simpler 24-sector table shape; future WALKSTUF1 high work should use generated deadline ownership or pack-side work reduction rather than merging accepted adjacent rows. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `building2-high-frame71-alias-v507` | Log-only; high pack restored and PS1 rebuilt. | Try a zero-code BUILDING2 high duplicate-payload alias instead of another read-group. Frame `71` was byte-identical to frame `70` but pointed at a second physical payload in sectors `75..78`; aliasing it to frame `70`'s sectors `72..75` tested whether the existing stream window could reuse the prior payload without D4 decode cost, source growth, file-size change, LBA movement, or PS-EXE movement. | Temporarily rewrote only `generated/ps1/foreground/BUILDING2.FG2` entry `71` to reuse entry `70`'s offset and size. Transform summary: `scratch/building2-high-frame71-alias-v507/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-frame71-alias-v507-focused/20260513-093712-3718326/summary.json`. The original high pack was restored from `scratch/building2-high-frame71-alias-v507/BUILDING2.before.FG2` and PS1 was rebuilt afterward. | Failed/no promotion. The alias was exact-flat on all acceptance counters: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, loop reads `58`, loop-read VBlanks `257`, and due misses `7`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Close adjacent duplicate aliasing for BUILDING2 high frame `71`. The window was already effectively covering this shape, so duplicate-table aliases are not a speed win unless they move a later due-path read with proven current-window residency. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `building2-high-setupseg202-226-v508` | Log-only; source restored and PS1 rebuilt. | Test the segmented/prepared-coverage version of the BUILDING2 high middle cluster after direct retained rows closed. The read plan still ranked `202..226` as a large saved-read cluster, so this probe moved that 24-sector range into a reusable setup segment to see whether active-loop ownership could avoid the visible/refill phase failures from hand read groups. | Temporarily added a high-tide-only BUILDING2 setup segment for sectors `202..226` in `src/foreground_pilot.c`, backed by the reusable setup segment buffer. Focused artifact: `scratch/ps1-perf-iterate/building2-high-setupseg202-226-v508-focused/20260513-094206-3746500/summary.json`. Source was restored and PS1 was rebuilt afterward. | Failed/no promotion. The segment reduced loop reads `58 -> 55`, but setup/source phase debt regressed every acceptance counter: scene `1602 -> 1619`, active loop/target `1351/1311 -> 1358/1308`, overrun `40 -> 50`, blocking `54 -> 61`, hidden refill `18 -> 25`, and due misses stayed `7`. The PS-EXE bucket stayed fixed but hot foreground symbols shifted by `+172` bytes. | Close setup-owned `202..226` in this local source form. BUILDING2 high's middle cluster is not solved by moving the same sector range to scene setup; it needs code-neutral generated ownership or pack-side visual-work reduction that does not shift hot foreground code. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `walkstuf1-low-primehole-f78-f80-v509` | Log-only; low pack restored and PS1 rebuilt. | Test the source-neutral version of the WALKSTUF1 low setup-prime hole idea. The accepted low pack has a `296`-byte unused prefix before `data_offset=4392`, already inside the setup-prime-covered sector range, so the probe asked whether tiny early hot payloads could move into that already-paid residency without adding source code, changing file size, moving pack LBA, or crossing a PS-EXE bucket. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` so frame `78` / source `137` moved from offset `316963` sector `154` to offset `4096` sector `2` (`53` bytes), and frame `80` / source `141` moved from offset `322008` sector `157` to offset `4149` sector `2` (`194` bytes). The transform used `247` of the `296` prefix bytes and left the file footprint fixed at `1535263`. Transform summary: `scratch/walkstuf1-low-primehole-f78-f80-v509/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-primehole-f78-f80-v509-focused/20260513-095603-3824409/summary.json`. The original low pack was restored from `scratch/walkstuf1-low-primehole-f78-f80-v509/WALK1LOW.before.FG2` and PS1 was rebuilt afterward. | Failed/no promotion. The focused run produced no `JCPERF2` result and logged repeated invalid byte reads around `0x4209FC4D..0x4209FC79` at PC `0x80034D20`, followed by a clean-retry prefetch drop for `walkstuf1`. No key speed metric was accepted, and the run is treated as a structural failure rather than a perf comparison. | Close prefix setup-prime hole payload relocation for FGP3 packs. Payloads must not be moved before `data_offset`, even if the sector is setup-prime-covered; that prefix behaves as metadata/setup territory, not safe payload storage. Future WALKSTUF1 low work should use generated/code-neutral residency, a valid in-data hole, or a custom data/upload representation. Public rollup and methodology totals remain at v501. |
| 2026-05-13 | `visitor3-low-frame137-primegap-v510` | `ps1: relocate visitor3 low frame137 prime gap` | Continue the VISITOR3 low white-whale pack-only lane after v477 proved the setup-prime in-data gap can own a hot D4 payload without source debt. Frame `137` was the standout: the full payload was `16828` bytes, but a previous-frame D4 delta against decoded frame `136` is only `503` bytes and fits in the remaining setup-prime gap after frame132. | Repacked `generated/ps1/foreground/VIST3LOW.FG2` with frame `137` moved from offset `542758` / sectors `265..273` to offset `203949` / sector `99`, preserving the `1555450` byte pack footprint, foreground LBA/sectors `23371/760`, and PS-EXE bucket `217088`. Runtime previous-frame D4 dispatch now includes low-tide frame `137`. Transform summary: `scratch/visitor3-low-frame137-primegap-v510/summary.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-low-frame137-primegap-v510-focused/20260513-101023-3905601/summary.json`; broad canaries: `scratch/ps1-perf-iterate/visitor3-low-frame137-primegap-v510-canaries/20260513-101234-3918391/summary.json`. | Promoted. VISITOR3 low improves scene `1394 -> 1391`, active loop/target `1065/1041 -> 1062/1040`, overrun `24 -> 22`, blocking `45 -> 42`, loop-read VBlanks `45 -> 42`, loop reads `8 -> 7`, and due misses `8 -> 7`; hidden refill and prefetch overrun stay `0`. VISITOR3 high, BUILDING2 high/low, and WALKSTUF1 high/low stayed exact-flat on accepted profiles. | Keep as the current VISITOR3 low baseline. Public rollup moves to `+0.2837%` over target / `99.7212%` target speed; raw signed rollup moves to `-0.4835%` / `100.5035%`. Methodology total is about `17.12` public over-target points removed and `12.62` public target-speed points gained since the compact full-matrix baseline, with this commit adding about `0.0015` public over-target points removed and `0.0014` public target-speed points. Next targets: WALKSTUF1 low, BUILDING2 high, WALKSTUF1 high, VISITOR3 high, BUILDING2 low, VISITOR3 low, and the remaining yellow rows. |
| 2026-05-13 | `walkstuf1-low-prime-reorder-146-154-v511` | Log-only; low pack restored and PS1 rebuilt. | Try a larger code-neutral WALKSTUF1 low swing after scalar read groups and prefix-hole relocation closed. Instead of adding source or moving payloads before `data_offset`, physically reorder the valid payload data so the tight frame `146..154` cluster, originally sectors `286..308`, lives inside the existing setup-prime bytes while preserving the file size, sound offset, LBA/sectors, and PS-EXE bucket. | Temporarily rewrote only `generated/ps1/foreground/WALK1LOW.FG2` payload order to keep startup frames `0..29` first, then place frames `146..154` at offsets `113484..157501` / sectors `55..76`, fully covered by the existing setup-prime read. Frame `30` became the first displaced payload at sector `76`. Transform summary: `scratch/walkstuf1-low-prime-reorder-146-154-v511/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-prime-reorder-146-154-v511-focused/20260513-103646-4056782/summary.json`. The original low pack was restored from `scratch/walkstuf1-low-prime-reorder-146-154-v511/WALK1LOW.before.FG2` and PS1 was rebuilt afterward. | Failed/no promotion. The focused gate proved the structural layout was readable but phase-negative: scene `1770 -> 1795`, active loop/target `1478/1431 -> 1503/1424`, overrun `47 -> 79`, blocking `64 -> 120`, loop reads `64 -> 71`, and due misses `11 -> 19`; hidden refill nudged `20 -> 19` but not enough to offset the displaced early-ramp CD debt. | Close full-prefix physical reordering of the `146..154` tight cluster in this form. Existing setup-prime bytes are not free if they displace the early payload ramp; the lost startup/read-window continuity costs more than the cluster residency saves. Future WALKSTUF1 low work should use generated deadline ownership, smaller same-sector swaps that do not displace early entries, or a custom data representation that shrinks/aliases payloads before changing physical order. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `walkstuf1-low-swap26-36-148-157-v512` | Log-only; low pack restored and PS1 rebuilt. | Retest the physical-reorder idea with a near-equal setup-prime/hot-range swap instead of the broad v511 prefix shuffle. The setup range frames `26..36` and hot range frames `148..157` differ by only `3` bytes, so the binary question was whether moving the tight cluster into sectors `47..71` while shifting the intervening payload region by only `-3` bytes would avoid the early-ramp debt. | Temporarily rewrote only `generated/ps1/foreground/WALK1LOW.FG2`: frames `148..157` moved from sectors `291..315` to offsets `97236..145685` / sectors `47..71`, fully setup-prime resident; frames `26..36` moved to offsets `597103..645555` / sectors `291..315`; entries `37..147` shifted by `-3` bytes. Transform summary: `scratch/walkstuf1-low-swap26-36-148-157-v512/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-swap26-36-148-157-v512-focused/20260513-104205-4087027/summary.json`. The original low pack was restored from `scratch/walkstuf1-low-swap26-36-148-157-v512/WALK1LOW.before.FG2` and PS1 was rebuilt afterward. | Failed/no promotion. The near-equal swap still regressed the strict gate: scene `1770 -> 1798`, active loop/target `1478/1431 -> 1506/1424`, overrun `47 -> 82`, blocking `64 -> 131`, hidden refill `20 -> 24`, loop reads `64 -> 70`, and due misses `11 -> 22`. | Close early-ramp physical swaps as a WALKSTUF1 low setup-prime strategy. Even a 3-byte middle shift and exact file/LBA/EXE identity cannot compensate for moving frames `26..36` out of startup residency. Future physical layout work must avoid displacing resident startup entries entirely, or first shrink/alias payloads to create real in-window slack. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `walkstuf1-low-frame158-d4-v513` | Log-only; low pack/source restored and PS1 rebuilt. | Test the largest unclosed WALKSTUF1 low D4 byte signal in the tight cluster after physical swaps failed. Offline sizing showed frame `158` could shrink from `5232` bytes to a `3181`-byte previous-frame D4 delta against frame `157`, saving `2051` bytes and about one sector without moving payload offsets. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` frame `158` as a previous-frame D4 payload and gated low-tide WALKSTUF1 frame `158` through `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-low-frame158-d4-v513/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame158-d4-v513-focused/20260513-104555-4109196/summary.json`. The original low pack was restored from `scratch/walkstuf1-low-frame158-d4-v513/WALK1LOW.before.FG2`, the source predicate was removed, and PS1 was rebuilt afterward. | Failed/no promotion. The one-sector byte saving did not survive runtime phase/decode cost: scene `1770 -> 1778`, active loop/target `1478/1431 -> 1486/1429`, overrun `47 -> 57`, blocking `64 -> 80`, hidden refill `20 -> 21`, loop reads stayed `64`, and due misses worsened `11 -> 14`. | Close standalone WALKSTUF1 low frame158 D4. Together with the earlier frame `59`, `92`, `181`, `187`, and `189` D4 failures, byte-only previous-frame deltas are not enough for WALKSTUF1 low unless they are paired with generated deadline ownership or a code-neutral resident-slot plan. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `building2-high-frame71-77-d4-v514` | Log-only; high pack/source restored and PS1 rebuilt. | Test whether the accepted BUILDING2 low frame `71`/`77` D4 payloads generalize to high tide. The high pack still stored full payloads for both frames, and offline D4 sizing matched the low win: frame `71` shrinks `6656 -> 13` bytes and frame `77` shrinks `8828 -> 909` bytes. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` frames `71` and `77` as previous-frame D4 payloads, and widened the BUILDING2 previous-frame D4 predicate from low-tide-only to both tides. Transform summary: `scratch/building2-high-frame71-77-d4-v514/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-frame71-77-d4-v514-focused/20260513-105009-4133439/summary.json`. The original high pack was restored from `scratch/building2-high-frame71-77-d4-v514/BUILDING2.before.FG2`, the predicate was restored to low-tide-only, and PS1 was rebuilt afterward. | Failed/no promotion. The byte win saved one loop read (`58 -> 57`) but did not improve speed and regressed strict counters: scene stayed `1602`, active loop stayed `1351`, target `1311 -> 1310`, overrun `40 -> 41`, blocking `54 -> 56`, hidden refill `18 -> 19`, and due misses stayed `7`. | Close high-tide BUILDING2 frame71/frame77 D4. The low-tide promotion remains valid, but high-tide pays more decode/source phase than it saves. Future BUILDING2 high work should avoid hot predicate growth and use generated deadline ownership, existing-code-neutral pack placement, or visual-work reduction. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `building2-high-duplicate-payload-cache-v515-v521` | Log-only; source/layout restored and PS1 rebuilt. | Make a larger BUILDING2 high custom-algorithm swing by caching repeated high-tide payload bytes for frames `19..25`, `28..33`, and `35..41` in scene-local RAM. The target was to eliminate reverse CD seeks/FGP3 duplicate payload reads without changing pack contents, then test whether fixed-bucket code-shape and controlled LBA/EXE phase could recover the earlier speed signal. | Tried compact switch and table helpers, fixed-bucket legacy-perf pruning, a cold foreground phase pad, a debug-wait size cut, an explicit one-sector `BUILDING2.FG2` LBA pad, and finally the oversized PS-EXE reproduction with layout changes allowed. Key artifacts: speed-but-layout-fail first pass `scratch/ps1-perf-iterate/building2-high-dup-cache-v515-focused/20260513-110210-7541/summary.json`; fixed-bucket pass `scratch/ps1-perf-iterate/building2-high-dup-cache-v515-fixedbucket-focused/20260513-111142-63820/summary.json`; pad132 `scratch/ps1-perf-iterate/building2-high-dup-cache-v515-pad132-focused/20260513-111343-75847/summary.json`; table helper `scratch/ps1-perf-iterate/building2-high-dup-cache-v517-table-focused/20260513-112318-131482/summary.json`; explicit LBA shift `scratch/ps1-perf-iterate/building2-high-lba6182-switch-cache-v519-focused/20260513-112744-157428/summary.json`; oversized reproduction `scratch/ps1-perf-iterate/building2-high-dup-cache-v520-oversize-repro-focused/20260513-113016-172267/summary.json`; pad136 sweep `scratch/ps1-perf-iterate/building2-high-dup-cache-v521-oversize-pad136-focused/20260513-113226-185091/summary.json`. | Failed/no promotion. The cache reliably saved two loop reads (`58 -> 56`), but fixed-bucket variants regressed strict timing (`loop_vb 1351`, target `1308`, overrun `43`, blocking `58`, prefetch overrun `19`). The oversized layout recovered a real loop win (`1602 -> 1599`, `1351/1311 -> 1348/1309`, overrun `40 -> 39`, prefetch overrun `18 -> 17`) but still regressed blocking (`54 -> 56`) and required PS-EXE bucket growth `217088 -> 219136` plus `BUILDING2.FG2` LBA `6181 -> 6182`. | Close this duplicate-payload cache as a local runtime/cache strategy. The useful signal is that duplicate reads are real but not sufficient: fixed layout turns them into worse visible refill pressure, and the only loop win depends on an unaccepted oversized EXE/CD phase with blocking regression. Future BUILDING2 high work should target generated deadline ownership, custom pack-side visual-work reduction, or a data representation that removes both reads and visible blocking without adding hot-loop cache code. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `walkstuf1-low-resident-hole-f158-v522` | Log-only; low pack/source restored and PS1 rebuilt. | Try the no-early-eviction form of the WALKSTUF1 low setup-prime hole idea. Frame `28` can shrink from `6764` to `3098` bytes as a previous-frame D4 delta, creating a valid in-data setup-prime hole without moving the startup ramp. The probe asked whether moving frame `158`'s D4 payload plus tiny full frames `137` and `78` into that hole could repay the D4 decode cost and remove tight-cluster due reads. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2`: frame `28` D4 created a `3666` byte hole, frame `158` D4 (`5232 -> 3181`) moved to offset `108583`, frame `137` moved after it at `111764`, and frame `78` moved after that at `112082`. Low-tide WALKSTUF1 frames `28` and `158` were gated through `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-low-resident-hole-f158-v522/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-resident-hole-f158-v522-focused/20260513-114357-250361/summary.json`. The original low pack was restored from `scratch/walkstuf1-low-resident-hole-f158-v522/WALK1LOW.before.FG2`, the source predicate was removed, and PS1 was rebuilt afterward. | Failed/no promotion. The layout was structurally readable and kept pack LBA plus the `217088` byte PS-EXE bucket fixed, but every strict counter regressed: scene `1770 -> 1792`, active loop/target `1478/1431 -> 1500/1427`, overrun `47 -> 73`, blocking `64 -> 104`, hidden refill `20 -> 28`, loop reads `64 -> 69`, and due misses `11 -> 15`. | Close frame28-created resident-hole relocation for frame158. This confirms that manufacturing setup-prime slack with an early D4 decode is not code-neutral enough for WALKSTUF1 low; future hole work must use exact alias/no-decode slack, a lower-CPU generated representation, or explicit generated deadline ownership. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `walkstuf1-low-frame100-d4-tailcompact-v523` | Log-only; low pack/source restored and PS1 rebuilt. | Test a same-order compaction variant after v522 proved backward resident-hole moves are unsafe. Frame `100` was the prior exact-flat standalone D4 candidate (`4683 -> 586` bytes); this probe used that `4097` byte saving to compact the later payload stream earlier without changing total pack bytes, sound-table offset, LBA/sectors, or the PS-EXE bucket. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2`: frame `100` became a previous-frame D4 payload at the same offset, all payloads after frame `100` and before the fixed sound table shifted earlier by `4097` bytes, and low-tide WALKSTUF1 frame `100` was gated through `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-low-frame100-d4-tailcompact-v523/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame100-d4-tailcompact-v523-focused/20260513-114922-281388/summary.json`. The original low pack was restored from `scratch/walkstuf1-low-frame100-d4-tailcompact-v523/WALK1LOW.before.FG2`, the source predicate was removed, and PS1 was rebuilt afterward. | Failed/no promotion. The same-order shift avoided reverse reads and kept loop reads flat at `64`, but it still regressed strict timing: scene `1770 -> 1784`, active loop/target `1478/1431 -> 1492/1426`, overrun `47 -> 66`, blocking `64 -> 99`, hidden refill `20 -> 29`, and due misses `11 -> 15`. | Close same-order D4 tail compaction under the current hand-authored read-group sectors. The byte saving shifts accepted phase boundaries more than it helps; retry only with generated retained-read/deadline metadata that is recomputed for the compacted offsets. Public rollup and methodology totals remain at v510. |
| 2026-05-13 | `visitor5-low-rg30-46-v526` | `ps1: retarget visitor5 low retained read` | Finish one of the remaining yellow rows by reusing the accepted VISITOR5 high `30..46` retained-read shape on low tide. The refreshed read-candidate sheet ranked `30..54` and `9..33` as two-read balanced candidates and `30..46` as a smaller candidate that exactly matched the new high-tide winner. | Changed the VISITOR5 low retained-read group from `{23,47}` to `{30,46}`. Binary alternatives v524 `{30,54}` and v525 `{9,33}` were exact-flat at `1104/1092`, blocking/refill `11`, and loop reads `19`; only v526 improved. Focused artifact: `scratch/ps1-perf-iterate/visitor5-low-rg30-46-v526-focused/20260513-120256-360007/summary.json`; broad canaries: `scratch/ps1-perf-iterate/visitor5-low-rg30-46-v526-canaries/20260513-120419-368727/summary.json`; high control: `scratch/ps1-perf-iterate/visitor5-low-rg30-46-v526-visitor5-high-control/20260513-121018-403699/summary.json`. | Promoted. VISITOR5 low improves scene `1363 -> 1361`, active loop/target `1104/1092 -> 1102/1097`, overrun `12 -> 5`, blocking/refill `11 -> 5`, loop reads `19 -> 18`, and due misses stay `0`; pack LBA/sectors stay `24394/173`, and the PS-EXE bucket stays `217088`. VISITOR5 high stays exact-flat at `1101/1096`, overrun `5`, blocking/refill `5`, reads/due `18/0`. VISITOR3 high/low, BUILDING2 high/low, and WALKSTUF1 high/low stayed exact-flat on accepted profiles. | Keep as the current VISITOR5 low baseline and close v524/v525 as inert alternatives. Public rollup moves to `+0.2786%` over target / `99.7262%` target speed; raw signed rollup moves to `-0.4886%` / `100.5086%`. Methodology total is about `17.12` public over-target points removed and `12.63` target-speed points gained since the compact full-matrix baseline. Bands are now `117` green and `9` yellow. Next targets: WALKSTUF1 low, BUILDING2 high, WALKSTUF1 high, VISITOR3 high, BUILDING2 low, VISITOR3 low, JOHNNY1 low/high, and BUILDING4 low. |
| 2026-05-13 | `walkstuf1-low-rg297-309-v527-v530` | Log-only; source restored and PS1 rebuilt. | Test the top remaining WALKSTUF1 low scheduler-owned read candidate after VISITOR5 low went green. The read-candidate sheet ranked `297..309` as a two-read saved range but flagged it `scheduler-owned-only`, so the binary sweep tried held-slack guards before allowing raw append behavior. | Temporarily split WALKSTUF1 read groups into high and low tables and added low-only `{297,309}` with `minSlack` values `5`, `4`, `3`, then `0`. Artifacts: slack5 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-309-slack5-v527-focused/20260513-122629-493934/summary.json`; slack4 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-309-slack4-v528-focused/20260513-122747-501761/summary.json`; slack3 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-309-slack3-v529-focused/20260513-122859-509035/summary.json`; unguarded `scratch/ps1-perf-iterate/walkstuf1-low-rg297-309-v530-focused/20260513-123014-516528/summary.json`. | Failed/no promotion. Slack `5`, `4`, and `3` were exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking `64`, hidden refill `20`, reads/due `64/11`, while adding source/table shape. Unguarded fired and saved one loop read (`64 -> 63`) but regressed every strict counter: scene `1770 -> 1778`, active loop/target `1478/1431 -> 1486/1430`, overrun `47 -> 56`, blocking `64 -> 74`, and hidden refill `20 -> 26`; due misses stayed `11`. | Close WALKSTUF1 low `297..309` under scalar guarded and unguarded retained-read ownership. The guard thresholds do not fire, and the raw form converts the saved read into visible CD debt. Future WALKSTUF1 low work needs generated scheduler ownership that can move the read earlier without hot-table source shape, or a pack/data representation that removes visual work before this tight cluster. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-early-rg-v531-v532` | Log-only; source restored. | Test the refreshed BUILDING2 high early-cluster read-plan rows before moving the row out of scalar retained-read work. The current matrix ranked `11..35` as a 24-sector, four-read-saved candidate with a long first gap, and the focused run exposed `3..27` as the actual first-window variant. | Temporarily inserted high-tide BUILDING2 rows `{11,35,0}` and then `{3,27,0}` ahead of the accepted `{60,72}`, `{206,230}`, `{226,242}`, and `{249,257}` table. Artifacts: `11..35` at `scratch/ps1-perf-iterate/building2-high-rg11-35-v531-focused/20260513-123650-553984/summary.json`; `3..27` at `scratch/ps1-perf-iterate/building2-high-rg3-27-v532-focused/20260513-123858-566442/summary.json`. | Failed/no promotion. Both variants were exact-flat against the current v526 BUILDING2 high row: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill `18`, loop reads `58`, loop-read VBlanks `257`, due misses `7`, and `group_hits=0`. Pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close BUILDING2 high early-cluster scalar retained rows in this hand-authored form. The read-plan model still shows early payload density, but the current runtime append state does not expose it through simple table entries. Further BUILDING2 high work should use generated append/deadline ownership, pack-side visual-work reduction, or a data representation that removes the reads before the scheduler sees them. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `johnny1-setup-prime128-v533` | Log-only; source restored and PS1 rebuilt. | Try a scene-local `128 KiB` setup-prime residency for JOHNNY1 high/low after direct read groups and slack tuning closed. JOHNNY1 has only seven active reads left but still about `25` blocking/refill VBlanks, so the goal was to remove late CD pressure without moving pack data or crossing the PS-EXE bucket. | Temporarily added `{"johnny1", 128UL * 1024UL, 128UL * 1024UL}` to `kFgRuntimeSetupPrimePolicies`, measured a fresh paired baseline, rebuilt, and gated both tides. Baseline artifact: `scratch/ps1-perf-iterate/johnny1-setup-prime-v533-baseline/20260513-124809-618684/summary.json`; focused artifact: `scratch/ps1-perf-iterate/johnny1-setup-prime128-v533-focused/20260513-125032-632808/summary.json`. The policy was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. Both tides regressed despite fixed pack LBAs and fixed `217088` byte PS-EXE bucket. High moved scene `2069 -> 2097`, active loop/target `1973/1945 -> 1975/1944`, overrun `28 -> 31`, blocking/refill `25 -> 28`, reads stayed `7`, and due misses stayed `0`; low moved scene `2069 -> 2096` with the same active/read regression. | Close raw contiguous JOHNNY1 setup-prime residency. The startup residency changes CD/held-frame phase without removing active reads; future JOHNNY1 work should target code-size-neutral black cleanup/upload elision, generated append ownership, or pack-authored cleanup suppression. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-low-rg305-311-v534` | Log-only; source restored and PS1 rebuilt. | Test the last high-ranked unclosed WALKSTUF1 low tight-cluster scalar row before moving pressure to larger high-tide rows. The refreshed matrix ranked `305..311` as a one-read-saved current-fit row, narrower than the already-closed `305..321` v317 lane. | Temporarily added shared WALKSTUF1 retained-read row `{305,311,0}` to the existing table so it could be tested without new low-tide routing code. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg305-311-v534-focused/20260513-125752-675763/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate was exact-flat and never produced a grouped-read hit: scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking `64`, hidden refill `20`, loop reads `64`, due misses `11`, and `group_hits=0`; pack LBA stayed `25633` and the PS-EXE bucket stayed `217088`. | Close WALKSTUF1 low `305..311` as another scalar retained-read lane. The refreshed matrix still sees the payload density, but the current runtime phase does not expose the row through hand-authored groups; remaining WALKSTUF1 low work needs generated scheduler ownership or data-shape changes before this cluster should be retried. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg298-322-v535` | Log-only; source restored and PS1 rebuilt. | Try the largest remaining current-fit WALKSTUF1 high retained-read row. The refreshed matrix ranked `298..322` as a 24-sector cluster with up to five modeled saved reads, making it a bigger high-tide swing than the low scraps. | Temporarily added shared WALKSTUF1 retained-read row `{298,322,0}` after the accepted `{213,229}` row and gated high tide against the v526 canary baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg298-322-v535-focused/20260513-130040-692414/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row changed CD phase but regressed strict timing: scene `1764 -> 1770`, active loop/target `1476/1434 -> 1482/1429`, overrun `42 -> 53`, blocking `81 -> 89`, hidden refill/prefetch overrun `23 -> 21`, loop reads `65 -> 63`, and due misses `16 -> 15`; pack LBA stayed `24883` and the PS-EXE bucket stayed `217088`. | Close WALKSTUF1 high `298..322` as a scalar retained-read row. The saved-read signal is real but the larger clustered read pushes visible cadence and target timing the wrong way; retry only with generated scheduler ownership or a data representation that removes the reads without consuming tight active slack. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg274-298-v536` | Log-only; source restored and PS1 rebuilt. | Try the earlier high-tide tight cluster after `{298,322}` proved phase-negative. The `274..298` row had slightly lower modeled upside but a better first-gap profile in the matrix, so it tested whether the earlier boundary could safely convert reads. | Temporarily added shared WALKSTUF1 retained-read row `{274,298,0}` after `{213,229}` and gated high tide against the v526 canary baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg274-298-v536-focused/20260513-130310-707210/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. It saved more loop reads but amplified the timing loss: scene `1764 -> 1776`, active loop/target `1476/1434 -> 1488/1427`, overrun `42 -> 61`, blocking `81 -> 102`, hidden refill/prefetch overrun `23 -> 30`, loop reads `65 -> 60`, and due misses `16 -> 17`; pack LBA stayed `24883` and the PS-EXE bucket stayed `217088`. | Close the large WALKSTUF1 high scalar clustered-read family in this phase. Both big rows convert read-count wins into worse visible blocking and target cadence; remaining WALKSTUF1 high work should switch to generated scheduler ownership, byte/layout reductions, or custom data representation rather than more hand-authored clustered appends. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg287-311-v537` | Log-only; source restored and PS1 rebuilt. | Test the shifted middle high-tide cluster after `{298,322}` and `{274,298}` both regressed. The `287..311` row still modeled five saved reads but had a different boundary and was the highest-ranked remaining open row after refreshing the matrix. | Temporarily added shared WALKSTUF1 retained-read row `{287,311,0}` and gated high tide against the v526 canary baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg287-311-v537-focused/20260513-130600-723917/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. This was less damaging than the other wide rows but still failed every strict guard: scene `1764 -> 1768`, active loop/target `1476/1434 -> 1480/1432`, overrun `42 -> 48`, blocking `81 -> 84`, hidden refill/prefetch overrun `23 -> 24`, loop reads `65 -> 63`, and due misses `16 -> 15`; pack LBA stayed `24883` and the PS-EXE bucket stayed `217088`. | Close shifted WALKSTUF1 high `287..311` as a scalar retained-read row. It confirms the high cluster's read-count win is real but not promotable through plain table appends; only generated scheduler ownership or data-shape reduction is likely to make this cluster useful. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg292-316-v538` | Log-only; source restored and PS1 rebuilt. | Test the last wide shifted WALKSTUF1 high scalar row before switching away from this family. The `292..316` row still modeled five saved reads but overlapped the prior failures differently enough to close explicitly. | Temporarily added shared WALKSTUF1 retained-read row `{292,316,0}` and gated high tide against the v526 canary baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg292-316-v538-focused/20260513-130820-737878/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat: scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking `81`, hidden refill/prefetch overrun `23`, loop reads `65`, due misses `16`, pack LBA `24883`, and PS-EXE bucket `217088`. | Close WALKSTUF1 high `292..316` as inert in the current runtime phase. With `298..322`, `274..298`, `287..311`, and `292..316` all closed, the high cluster should not get more hand-authored scalar rows until generated scheduler ownership or data-shape work changes the phase model. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-rg17-41-v539` | Log-only; source restored and PS1 rebuilt. | Move to BUILDING2 high after the WALKSTUF1 scalar cluster closed. The `17..41` early row extends the previously closed narrower rows and modeled four saved reads, so it tested whether a wider early boundary could finally fire. | Temporarily inserted `{17,41,0}` before the accepted BUILDING2 high rows `{60,72}`, `{206,230}`, `{226,242}`, and `{249,257}`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg17-41-v539-focused/20260513-131102-753868/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate was exact-flat against v526: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill/prefetch overrun `18`, loop reads `58`, due misses `7`, pack LBA `6181`, and PS-EXE bucket `217088`. | Close BUILDING2 high `17..41` as another inert early scalar retained row. The read-plan density is real, but the current append state does not expose this range through simple table entries; use generated ownership, pack-side reduction, or a different cluster before retrying early BUILDING2 high. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-rg90-114-v540` | Log-only; source restored and PS1 rebuilt. | Test BUILDING2 high's separate mid-early `90..114` cluster after the expanded early row no-opped. The matrix modeled four saved reads in a different timing region, so this was the next largest high-side scalar probe. | Temporarily inserted `{90,114,0}` after the accepted `{60,72}` row and before the accepted mid rows. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg90-114-v540-focused/20260513-131316-767219/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill/prefetch overrun `18`, loop reads `58`, due misses `7`, pack LBA `6181`, and PS-EXE bucket `217088`. | Close BUILDING2 high `90..114` as inert in the current scalar table phase. Remaining high-side scalar rows are mostly overlapping/subset regions around accepted groups, so BUILDING2 high likely needs generated ownership or pack-side visual-work reduction rather than another plain row. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg210-234-v541-v544` | Log-only; source restored and PS1 rebuilt. | Test the largest open BUILDING2 low scalar cluster after high-side no-ops. The raw `210..234` row modeled five saved reads and overlapped the known phase-negative `210..222` lane, so the sweep tried unguarded, `minSlack=4`, `minSlack=8`, and `minSlack=6` to find a safe cutoff. | Temporarily inserted `{210,234,minSlack}` before the accepted low rows. Artifacts: unguarded `scratch/ps1-perf-iterate/building2-low-rg210-234-v541-focused/20260513-131533-780693/summary.json`; slack4 `scratch/ps1-perf-iterate/building2-low-rg210-234-slack4-v542-focused/20260513-131650-788648/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg210-234-slack8-v543-focused/20260513-131806-796438/summary.json`; slack6 `scratch/ps1-perf-iterate/building2-low-rg210-234-slack6-v544-focused/20260513-131922-804187/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Unguarded, slack4, and slack6 all produced the same unsafe mixed signal: loop reads `52 -> 47`, blocking `70 -> 67`, due misses `15 -> 14`, but target `1320 -> 1317`, overrun `29 -> 32`, and hidden refill/prefetch overrun `0 -> 6`. Slack8 disabled the row and was exact-flat at `1349/1320`, overrun `29`, blocking `70`, refill `0`, reads/due `52/15`. | Close BUILDING2 low `210..234` under scalar guarded forms. The read/blocking win is real, but the visible-loop target/refill cost is not promotable; this cluster needs generated deadline ownership or pack-side reduction rather than a simple slack threshold. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg214-238-v545-v547` | Log-only; source restored and PS1 rebuilt. | Shift the failed BUILDING2 low `210..234` window later to see whether the same five-read cluster could keep its blocking win with less early refill debt. The binary sweep tested raw, `minSlack=8`, and `minSlack=12`. | Temporarily inserted `{214,238,minSlack}` before the accepted low rows. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg214-238-v545-focused/20260513-132220-821680/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg214-238-slack8-v546-focused/20260513-132337-829568/summary.json`; slack12 `scratch/ps1-perf-iterate/building2-low-rg214-238-slack12-v547-focused/20260513-132454-837497/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Raw and slack8 matched: loop reads `52 -> 47`, blocking `70 -> 67`, due misses `15 -> 14`, but target `1320 -> 1318`, overrun `29 -> 31`, and hidden refill/prefetch overrun `0 -> 6`. Slack12 disabled the row and was exact-flat at `1349/1320`, overrun `29`, blocking `70`, refill `0`, reads/due `52/15`. | Close BUILDING2 low `214..238` under scalar guarded forms. Moving the window later reduces the target/overrun damage versus `210..234` but still cannot clear the strict refill guard; this row needs generated ownership or pack-side reduction before retry. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg218-234-v548-v551` | Log-only; source restored and PS1 rebuilt. | Narrow the prior BUILDING2 low mixed-signal cluster to the later `218..234` range. The raw row modeled three saved reads and was expected to avoid the worst early refill debt from `210..234` and `214..238`, so the sweep tested unguarded plus `minSlack=8`, `9`, and `10`. | Temporarily inserted `{218,234,minSlack}` before the accepted low rows. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg218-234-v548-focused/20260513-133013-869019/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg218-234-slack8-v550-focused/20260513-133332-888906/summary.json`; slack9 `scratch/ps1-perf-iterate/building2-low-rg218-234-slack9-v551-focused/20260513-133443-896287/summary.json`; slack10 `scratch/ps1-perf-iterate/building2-low-rg218-234-slack10-v549-focused/20260513-133214-880969/summary.json`. Source was restored afterward. | Failed/no promotion. Raw and slack8 produced the best scalar signal in this family so far: scene `1619 -> 1617`, active loop `1349 -> 1347`, blocking `70 -> 63`, loop reads `52 -> 49`, and due misses `15 -> 14`, while target also moved `1320 -> 1318` and hidden refill/prefetch overrun regressed `0 -> 2`, failing the strict zero-regression gate. Slack9 and slack10 disabled the row and were exact-flat at `1349/1320`, overrun `29`, blocking `70`, refill `0`, reads/due `52/15`. | Close BUILDING2 low `218..234` as a plain scalar row. This is a useful clue: the cluster can buy `7` visible blocking VBlanks with only `2` refill VBlanks of debt, but there is no scalar slack threshold between "unsafe full fire" and "off." Retry only with generated deadline ownership, a custom split row that avoids the refill owner, or pack-side byte reduction that makes the raw fire cost zero. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg306-322-v552` | Log-only; source restored and PS1 rebuilt. | Test the smaller 16-sector WALKSTUF1 high suffix row after the wider high cluster closed. The `306..322` row still modeled three saved reads and overlapped the least-late part of the failed `298..322` window, so it was the highest-ranked remaining scalar candidate. | Temporarily inserted `{306,322,0}` into the shared WALKSTUF1 retained-read table after the accepted `{213,229}` row. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg306-322-v552-focused/20260513-133737-913294/summary.json`. Source was restored afterward. | Failed/no promotion. The row was exact-flat against the v526 canary baseline: scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking `81`, hidden refill/prefetch overrun `23`, loop reads `65`, loop-read VBlanks `282`, due misses `16`, pack LBA `24883`, and PS-EXE bucket `217088`. | Close WALKSTUF1 high `306..322` as inert in the current scalar phase. The model still sees payload density, but the runtime append state does not expose a hand-authored grouped-read hit; continue only with generated ownership/data-shape work or close the remaining tiny suffix rows quickly. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg298-310-v553` | Log-only; source restored and PS1 rebuilt. | Test the smaller WALKSTUF1 high `298..310` row after the 16-sector suffix no-opped. This 12-sector cluster modeled two saved reads and was the top remaining matrix row, but it overlapped the failed wide `298..322` family closely enough to require an explicit closure. | Temporarily inserted `{298,310,0}` into the shared WALKSTUF1 retained-read table after the accepted `{213,229}` row. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg298-310-v553-focused/20260513-133958-927302/summary.json`. Source was restored afterward. | Failed/no promotion. The row was phase-negative: scene `1764 -> 1776`, active loop/target `1476/1434 -> 1488/1426`, overrun `42 -> 62`, blocking `81 -> 96`, hidden refill/prefetch overrun `23 -> 25`, and loop reads `65 -> 66`; due misses stayed `16`, pack LBA stayed `24883`, and the PS-EXE bucket stayed `217088`. | Close WALKSTUF1 high `298..310` as another scalar retained-read loss. The narrower row is worse than the wider exact-flat suffix and confirms this late high-tide cluster needs generated ownership or pack-side data reduction, not more plain append-table entries. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg292-298-v554` | Log-only; source restored and PS1 rebuilt. | Close the highest-ranked remaining one-read WALKSTUF1 high row after the larger overlapping rows failed. The `292..298` row had the least modeled visible-cost penalty among the tiny suffix candidates but only one saved-read upside. | Temporarily inserted `{292,298,0}` into the shared WALKSTUF1 retained-read table after the accepted `{213,229}` row. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg292-298-v554-focused/20260513-134257-944875/summary.json`. Source was restored afterward. | Failed/no promotion. The row was exact-flat: scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking `81`, hidden refill/prefetch overrun `23`, loop reads `65`, due misses `16`, pack LBA `24883`, and PS-EXE bucket `217088`. | Close WALKSTUF1 high `292..298` as inert. With this exact-flat result plus the wider phase-negative rows, remaining tiny suffix rows are unlikely to produce a scalar win, but they can be closed quickly before switching to generated/data-shape work. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-high-rg306-312-v555` | Log-only; source restored and PS1 rebuilt. | Close the one-read `306..312` suffix after `306..322` no-opped. The smaller row has the same tight visible gap but fewer sectors, so it tested whether the exact-flat wide suffix hid a narrower fireable boundary. | Temporarily inserted `{306,312,0}` into the shared WALKSTUF1 retained-read table after the accepted `{213,229}` row. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg306-312-v555-focused/20260513-134511-958246/summary.json`. Source was restored afterward. | Failed/no promotion. The row was exact-flat: scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking `81`, hidden refill/prefetch overrun `23`, loop reads `65`, due misses `16`, pack LBA `24883`, and PS-EXE bucket `217088`. | Close WALKSTUF1 high `306..312` as inert. The current append phase does not expose this boundary through scalar retained reads; remaining suffix work should end with `304..310` or move to generated/data-shape ownership. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-rg206-222-v556` | Log-only; source restored and PS1 rebuilt. | Test whether shortening the accepted BUILDING2 high `{206,230}` row to `{206,222}` could reduce overread/refill debt while retaining most of the modeled read win. This was an override-style experiment: the shorter row was inserted before the accepted row with the same start sector. | Temporarily inserted `{206,222,0}` before the accepted `{206,230}` in `kBuilding2HighReadGroups12`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg206-222-v556-focused/20260513-134725-971604/summary.json`. Source was restored afterward. | Failed/no promotion. The shorter override regressed cadence: scene `1602 -> 1607`, active loop `1351 -> 1356`, overrun `40 -> 45`, blocking `54 -> 56`, hidden refill/prefetch overrun `18 -> 22`, loop reads `58 -> 55`, and due misses stayed `7`; pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close BUILDING2 high `206..222` as an unsafe replacement for the accepted `206..230` row. The accepted overread is carrying useful cadence; trimming it saves reads but raises visible/refill debt. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-rg185-197-v557` | Log-only; source restored and PS1 rebuilt. | Test the separate BUILDING2 high `185..197` mid cluster after the accepted-row trim failed. This row modeled two saved reads and does not share the accepted `{206,230}` start sector, so it tested a true additive hand-authored retained read. | Temporarily inserted `{185,197,0}` before the accepted mid/high rows. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg185-197-v557-focused/20260513-134951-986116/summary.json`. Source was restored afterward. | Failed/no promotion. The row regressed visible cadence despite saving reads: scene `1602 -> 1607`, active loop/target `1351/1311 -> 1356/1312`, overrun `40 -> 44`, blocking `54 -> 58`, hidden refill/prefetch overrun `18 -> 23`, loop reads `58 -> 56`, and due misses stayed `7`; pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close BUILDING2 high `185..197` as phase-negative. It confirms the high-side mid cluster cannot be added as a plain scalar group; any retry needs generated deadline ownership or pack-side visual-work reduction. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-rg206-218-v558` | Log-only; source restored and PS1 rebuilt. | Test the more aggressive accepted-row trim after `206..222` regressed. The goal was to determine whether the cost came from the `222..230` tail or from replacing the accepted `{206,230}` cadence at all. | Temporarily inserted `{206,218,0}` before the accepted `{206,230}` in `kBuilding2HighReadGroups12`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg206-218-v558-focused/20260513-135201-999076/summary.json`. Source was restored afterward. | Failed/no promotion. The result matched the unsafe trim family: scene `1602 -> 1607`, active loop/target `1351/1311 -> 1356/1312`, overrun `40 -> 44`, blocking `54 -> 58`, hidden refill/prefetch overrun `18 -> 23`, loop reads `58 -> 56`, and due misses stayed `7`; pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close BUILDING2 high `206..218` as unsafe. Together with `206..222`, this shows the accepted `{206,230}` row's extra coverage is required for cadence even if the read-plan matrix ranks shorter trims. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-high-rg214-226-v559` | Log-only; source restored and PS1 rebuilt. | Close the last BUILDING2 high scalar overlap row. Unlike the `206`-start trims, `214..226` has a distinct start sector and overlaps the accepted mid cluster from inside, so it tested whether a shorter internal append could help without replacing `{206,230}`. | Temporarily inserted `{214,226,0}` between accepted `{206,230}` and `{226,242}`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg214-226-v559-focused/20260513-135413-1012308/summary.json`. Source was restored afterward. | Failed/no promotion. The row regressed every strict speed gate: scene `1602 -> 1607`, active loop/target `1351/1311 -> 1356/1310`, overrun `40 -> 46`, blocking `54 -> 59`, hidden refill/prefetch overrun `18 -> 24`, loop reads `58 -> 56`, and due misses stayed `7`; pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close BUILDING2 high `214..226` as phase-negative. The remaining BUILDING2 high scalar matrix rows are exhausted; future high-side work should be generated ownership or pack-side reduction only. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg214-230-v560-v562` | Log-only; source restored and PS1 rebuilt. | Narrow the mixed-signal BUILDING2 low `214..238` cluster to `214..230` to see whether dropping the tail could keep the visible blocking win while removing refill debt. The sweep tested raw, `minSlack=9`, and `minSlack=8`. | Temporarily inserted `{214,230,minSlack}` before the accepted low rows. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg214-230-v560-focused/20260513-135628-1026146/summary.json`; slack9 `scratch/ps1-perf-iterate/building2-low-rg214-230-slack9-v561-focused/20260513-135741-1033763/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg214-230-slack8-v562-focused/20260513-135854-1041315/summary.json`. Source was restored afterward. | Failed/no promotion. Raw and slack8 produced a real but unsafe win: scene `1619 -> 1617`, active loop `1349 -> 1347`, blocking `70 -> 65`, loop reads `52 -> 49`, and due misses `15 -> 14`, while target moved `1320 -> 1318` and hidden refill/prefetch overrun regressed `0 -> 3`. Slack9 disabled the row and was exact-flat at `1349/1320`, overrun `29`, blocking `70`, refill `0`, reads/due `52/15`. | Close BUILDING2 low `214..230` under scalar guarded forms. Like `218..234`, it proves the cluster has useful blocking upside, but scalar slack control is binary: unsafe full fire or no-op. Future retry should be generated deadline ownership, split-row ownership, or pack-side byte reduction. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg222-234-v563` | Log-only; source restored and PS1 rebuilt. | Test the later 12-sector BUILDING2 low row after the wider/narrower mixed-signal rows failed on refill. The goal was to see if shifting later would preserve zero refill while exposing a smaller read win. | Temporarily inserted `{222,234,0}` before the accepted low rows. Focused artifact: `scratch/ps1-perf-iterate/building2-low-rg222-234-v563-focused/20260513-140114-1055974/summary.json`. Source was restored afterward. | Failed/no promotion. The row kept scene/loop/target and refill flat at `1619`, `1349/1320`, overrun `29`, refill `0`, but regressed visible blocking `70 -> 72` while loop reads improved `52 -> 50`; due misses stayed `15`, pack LBA stayed `6818`, and the PS-EXE bucket stayed `217088`. | Close BUILDING2 low `222..234` as phase-negative. Later clustering avoids refill debt but shifts visible CD blocking the wrong way, so it is not a scalar path to 99%. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg218-230-v564-v566` | Log-only; source restored and PS1 rebuilt. | Test the final BUILDING2 low scalar row after `222..234` regressed blocking and `214..230` missed on refill. The row is centered on the best part of the cluster and had a chance to trade less refill for a still-material blocking win. | Temporarily inserted `{218,230,minSlack}` before the accepted low rows. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg218-230-v564-focused/20260513-140330-1069446/summary.json`; slack9 `scratch/ps1-perf-iterate/building2-low-rg218-230-slack9-v565-focused/20260513-140445-1077195/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg218-230-slack8-v566-focused/20260513-140600-1084866/summary.json`. Source was restored afterward. | Failed/no promotion, but it is the best scalar near-miss in the BUILDING2 low family. Raw and slack8 improved scene `1619 -> 1614`, active loop/target `1349/1320 -> 1344/1317`, overrun `29 -> 27`, blocking `70 -> 61`, loop reads `52 -> 50`, and due misses `15 -> 14`, but hidden refill/prefetch overrun regressed `0 -> 1`. Slack9 disabled the row and was exact-flat. | Close BUILDING2 low `218..230` under scalar guarded forms. This row is only one refill VBlank away from promotion, so future work should target generated ownership or pack-side byte reduction specifically to absorb that last refill VBlank. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `johnny1-rg123-139-v568` | Log-only; source restored and PS1 rebuilt. | Resolve the stale matrix's JOHNNY1 `123..139` row after a fresh current baseline showed no observed append start for that range. The direct runtime probe still tested both tides explicitly so the stale candidate could be closed rather than hand-waved. | Captured a fresh current baseline at `scratch/ps1-perf-iterate/johnny1-current-v567-baseline/20260513-140824-1099679/summary.json`, then temporarily added `kJohnny1ReadGroups16[] = {{123,139,0}}` and routed `johnny1` through it for both tides. Focused artifact: `scratch/ps1-perf-iterate/johnny1-rg123-139-v568-focused/20260513-141101-1114957/summary.json`. Source was restored afterward. | Failed/no promotion. Both tides were exact-flat against the fresh baseline: scene `2069`, active loop/target `1973/1945`, overrun `28`, blocking/refill `25`, loop reads `7`, and due misses `0`; pack LBAs stayed `13983/14202`, and the PS-EXE bucket stayed `217088`. The only change was source shape: `foregroundPilotPlay` grew by `24` bytes and hot symbols shifted by `+24`. | Close JOHNNY1 `123..139` as a stale/no-observed-append scalar row. JOHNNY1 direct retained-read groups remain closed; future work needs code-size-neutral black cleanup/upload elision, generated ownership, or pack-authored cleanup suppression. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `visitor3-high-rg211-235-v569` | Log-only; source restored and PS1 rebuilt. | Test the stale Visitor3 high `211..235` scalar row after the matrix moved past BUILDING2 and JOHNNY1. The fresh current read plan suggested the row was no longer a saved-read candidate, but the runtime table probe closed it explicitly. | Temporarily added a high-tide-only `kVisitor3HighReadGroups24[] = {{211,235,0}}` and routed `visitor3` through it. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-rg211-235-v569-focused/20260513-141452-1137573/summary.json`. Source was restored afterward. | Failed/no promotion. The row was exact-flat against the v526 canary baseline: scene `1400`, active loop/target `1065/1039`, overrun `26`, blocking `41`, hidden refill `0`, loop reads `7`, and due misses `7`; pack LBA stayed `22611` and the PS-EXE bucket stayed `217088`. The only effect was source shape: `foregroundPilotPlay` grew by `48` bytes and hot symbols shifted by `+48`. | Close Visitor3 high `211..235` as stale/no-saved-read under the current pack. Visitor3 high remaining work should be pack/data-shape or generated deadline ownership, not a hand-authored retained row. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `visitor3-high-rg228-252-v570` | Log-only; source restored and PS1 rebuilt. | Close the final open scalar row in the stale read-candidate matrix. `visitor3` high `228..252` was still listed as one saved read and scheduler-owned-only, but the fresh runtime read plan from the focused probe classified the visible-cost row as no-saved-read under the current pack. | Temporarily added a high-tide-only `kVisitor3HighReadGroups24[] = {{228,252,0}}` and routed `visitor3` through it. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-rg228-252-v570-focused/20260513-141908-1163330/summary.json`. Source was restored afterward, and the PS1 executable was rebuilt cleanly. | Failed/no promotion. The probe was exact-flat against the v526 canary baseline: scene `1400`, active loop/target `1065/1039`, overrun `26`, blocking `41`, hidden refill `0`, loop reads `7`, and due misses `7`; pack LBA stayed `22611` and the PS-EXE bucket stayed `217088`. The only effect was code shape: `foregroundPilotPlay` grew by `40` bytes and hot foreground symbols shifted by `+40`. | Close Visitor3 high `228..252` as stale/no-saved-read under the current pack. The hand-authored retained-read scalar queue is now exhausted; remaining under-99 work should move to generated deadline ownership, pack/data-shape reduction, code-size-neutral cleanup/upload elision, or scene-specific compression. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `building2-low-rg250-262-v571-v574` | Log-only; source restored and PS1 rebuilt. | After v570 exhausted the stale scalar matrix, regenerated fresh read plans for the nine under-99 rows under `scratch/current-under99-readplans-v570-20260513`. The new matrix exposed BUILDING2 low `250..262` as the top balanced scheduler-or-guarded row, adjacent to the accepted `{238,250}` low-tide retained read, so it was the next binary scalar/guarded probe. | Temporarily inserted `{250,262,minSlack}` after `{238,250}` in `kBuilding2LowReadGroups12`. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg250-262-v571-focused/20260513-142651-1206942/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg250-262-slack8-v572-focused/20260513-142817-1215446/summary.json`; slack7 `scratch/ps1-perf-iterate/building2-low-rg250-262-slack7-v573-focused/20260513-142936-1223501/summary.json`; slack6 `scratch/ps1-perf-iterate/building2-low-rg250-262-slack6-v574-focused/20260513-143124-1234260/summary.json`. | Failed/no promotion. Raw cut visible blocking `70 -> 60`, loop reads `52 -> 51`, and due misses `15 -> 13` with hidden refill still `0`, but it regressed scene `1619 -> 1625`, active loop/target `1349/1320 -> 1355/1321`, and overrun `29 -> 34`. Slack6, slack7, and slack8 all disabled the row and stayed exact-flat at `1619`, `1349/1320`, overrun `29`, blocking `70`, refill `0`, reads/due `52/15`. | Close BUILDING2 low `250..262` as a scalar/slack row. The raw result proves the cluster can remove visible CD blocking, but the active-loop cadence loss means it needs generated deadline ownership or a pack-side byte/phase change before promotion. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `johnny1-rg131-155-138-162-v575-v578` | Log-only; source restored and PS1 rebuilt. | Close the four fresh JOHNNY1 scheduler-or-guarded rows from the v570 under-99 read-plan refresh. These were the only remaining guarded rows after BUILDING2 low `250..262` closed, and both tides shared the same `131..155` / `138..162` 24-sector shapes. | Temporarily routed JOHNNY1 through a single 24-sector retained-read row, first low `131..155`, then low `138..162`, then high `138..162`, then high `131..155`. Artifacts: `scratch/ps1-perf-iterate/johnny1-low-rg131-155-v575-focused/20260513-143523-1258108/summary.json`; `scratch/ps1-perf-iterate/johnny1-low-rg138-162-v576-focused/20260513-143642-1266332/summary.json`; `scratch/ps1-perf-iterate/johnny1-high-rg138-162-v577-focused/20260513-143815-1275797/summary.json`; `scratch/ps1-perf-iterate/johnny1-high-rg131-155-v578-focused/20260513-143937-1284204/summary.json`. | Failed/no promotion. Every tested row was exact-flat against the fresh v567 JOHNNY1 baseline: scene `2069`, active loop/target `1973/1945`, overrun `28`, blocking/refill `25`, loop reads `7`, and due misses `0`; pack LBAs stayed `14202` low and `13983` high, and the PS-EXE bucket stayed `217088`. The only effect was code shape: `foregroundPilotPlay` grew by `40` bytes and hot foreground symbols shifted by `+40`. | Close the fresh JOHNNY1 guarded scalar rows. JOHNNY1 is not CD-due bound in this path; future work should target pack-authored black cleanup suppression, upload/restore byte reduction, or code-neutral dirty-row metadata. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-low-rg237-261-v579` | Log-only; source restored and PS1 rebuilt. | Test the top fresh scheduler-owned row for the largest remaining outlier, WALKSTUF1 low. The row modeled four saved reads, but the matrix marked it `unsafe:tight-visible-gap` / scheduler-owned-only, so the probe used a low-tide-only table split to verify whether a plain retained row could ever be accepted. | Temporarily split WALKSTUF1 read-group routing by tide and added low-only `{237,261,0}` after the accepted `{213,229}` row while keeping the accepted high table unchanged. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg237-261-v579-focused/20260513-144325-1306397/summary.json`. Source was restored afterward. | Failed/no promotion. The row reduced due misses `11 -> 9`, loop reads `64 -> 60`, and visible blocking `64 -> 63`, but it regressed scene `1770 -> 1778`, active loop `1478 -> 1486`, overrun `47 -> 55`, and hidden refill/prefetch overrun `20 -> 30`. Pack LBA stayed `25633`, sectors stayed `750`, and the PS-EXE bucket stayed `217088`; `foregroundPilotPlay` grew by `36` bytes during the temporary table split. | Close WALKSTUF1 low `237..261` as a plain retained row. This is exactly the scheduler-owned-only failure mode: saved reads exist, but their ownership is paid in the visible loop/refill phase. Future WALKSTUF1 low work must be generated deadline ownership or pack-side byte/phase reduction, not another direct table. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-low-scheduler-guard-v580-v585` | Log-only; source restored and PS1 rebuilt. | After the raw `237..261` row proved that saved reads can be paid back as visible/refill debt, test whether low-tide split rows with `minSlack` guards can isolate scheduler-owned prefetch slots from visible due-load expansion. This batch covered the top fresh `242..266` row, the known fireable `285..309` cluster under two thresholds, the adjacent `308..332` cluster, and the lower-risk `141..165` row under raw and guarded forms. | Temporarily split WALKSTUF1 routing by tide and tested `{242,266,4}`, `{285,309,4}`, `{285,309,5}`, `{308,332,4}`, `{141,165,0}`, and `{141,165,4}`. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-rg242-266-slack4-v580-focused/20260513-145034-1347921/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg285-309-slack4-v581-focused/20260513-145206-1357289/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg285-309-slack5-v582-focused/20260513-145329-1366728/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg308-332-slack4-v583-focused/20260513-145455-1375697/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg141-165-v584-focused/20260513-145619-1384456/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg141-165-slack4-v585-focused/20260513-145740-1392949/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `{242,266,4}`, `{285,309,5}`, and `{308,332,4}` were exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/due `64/11`. `{285,309,4}` saved reads/due `64/11 -> 60/10`, but regressed scene `1770 -> 1776`, active loop/target `1478/1431 -> 1484/1433`, overrun `47 -> 51`, blocking `64 -> 70`, and refill `20 -> 25`. `{141,165,0}` and `{141,165,4}` both saved reads/due `64/11 -> 62/10` and improved blocking `64 -> 63`, but regressed scene `1770 -> 1776`, active loop/target `1478/1431 -> 1484/1432`, overrun `47 -> 52`, and refill `20 -> 25`. | Close WALKSTUF1 low `242..266`, `285..309`, `308..332`, and `141..165` under scalar/scheduler-threshold ownership. The guarded split shows a binary cliff: thresholds that fire still steal visible/refill cadence, while the next threshold no-ops. The next WALKSTUF1 low work should leave hand-authored read-group thresholds and move to generated frame-deadline ownership, pack-side byte reduction before these clusters, or upload/restore work removal. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-low-rg305-148-364-v586-v590` | Log-only; source restored and PS1 rebuilt. | Continue the fresh WALKSTUF1 low scalar closure after the first scheduler-guard batch moved `305..329`, `148..172`, and `364..388` to the top of the active matrix. The goal was to separate inert append starts from rows that still have useful but unsafe CD-work movement. | Temporarily split WALKSTUF1 routing by tide and tested low-only `{305,329,0}`, `{148,172,0}`, `{148,172,4}`, `{364,388,0}`, and `{364,388,4}`. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-rg305-329-v586-focused/20260513-150202-1418546/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg148-172-v587-focused/20260513-150333-1427955/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg148-172-slack4-v588-focused/20260513-150453-1436372/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg364-388-v589-focused/20260513-150614-1444804/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg364-388-slack4-v590-focused/20260513-150737-1453517/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `{305,329,0}` was exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/due `64/11`. `{148,172,0}` and `{148,172,4}` both regressed hard: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1429`, overrun `47 -> 61`, blocking `64 -> 88`, refill `20 -> 32`, and due misses `11 -> 13` while loop reads only improved `64 -> 62`. `{364,388,0}` and `{364,388,4}` were closer but still negative: scene `1770 -> 1773`, active loop/target `1478/1431 -> 1481/1433`, overrun `47 -> 48`, blocking `64 -> 68`, refill `20 -> 24`, and reads `64 -> 61`. | Close WALKSTUF1 low `305..329`, `148..172`, and `364..388` under scalar guarded forms. `305..329` is inert, `148..172` is visibly phase-destructive, and `364..388` saves reads but still lands four refill/blocking VBlanks short of promotion. Remaining WALKSTUF1 low table rows should only be used for queue cleanup; the bigger path is generated ownership or reducing per-frame visual/upload work before the CD cluster. Public rollup and methodology totals remain at v526. |
| 2026-05-13 | `walkstuf1-shared-rg427-443-v591-v598` | `ps1: add walkstuf1 shared rg427-443` | Continue the WALKSTUF1 low tail scalar closure, then salvage the one row that produced safe same-speed CD-work. The batch tested remaining fresh tail/mid candidates before moving to generated ownership. | First used a temporary low-tide table split for `{303,319,0}`, `{155,171,0}`, `{91,107,0}`, `{405,421,0}`, `{405,421,4}`, `{427,443,0}`, and `{378,394,0}`. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-rg303-319-v591-focused/20260513-151058-1473463/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg155-171-v592-focused/20260513-151221-1482078/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg91-107-v593-focused/20260513-151348-1491115/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg405-421-v594-focused/20260513-151513-1499944/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg405-421-slack4-v595-focused/20260513-151639-1508792/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg427-443-v596-focused/20260513-151801-1517379/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-low-rg378-394-v597-focused/20260513-151923-1525902/summary.json`. Then promoted `{427,443,0}` as a shared table row and validated focused low at `scratch/ps1-perf-iterate/walkstuf1-shared-rg427-443-v598-focused/20260513-152315-1548645/summary.json`, high at `scratch/ps1-perf-iterate/walkstuf1-shared-rg427-443-v598-high-canary-match/20260513-152547-1564205/summary.json`, and VISITOR3/BUILDING2 canaries at `scratch/ps1-perf-iterate/walkstuf1-shared-rg427-443-v598-layout-canaries/20260513-152658-1571526/summary.json`. | Promoted as same-speed CD-work reduction, not a VBlank speed win. WALKSTUF1 low timing stays exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, and due misses `11`, while loop reads improve `64 -> 62` and loop-read VBlanks `286 -> 281`. WALKSTUF1 high stays exact-flat at `1764`, `1476/1434`, overrun `42`, blocking/refill `81/23`, reads/due `65/16`. VISITOR3 high/low and BUILDING2 high/low stayed exact-flat with fixed pack LBAs and the `217088` byte PS-EXE bucket. The other rows are rejected: `303..319` was inert, `155..171` and `91..107` were visibly destructive, `405..421` improved scene/loop by one but regressed blocking/refill by one, and `378..394` regressed scene/loop plus blocking/refill. | Keep shared `427..443` in the WALKSTUF1 table as the new CD-work baseline. Close WALKSTUF1 low `303..319`, `155..171`, `91..107`, `405..421`, and `378..394` under scalar/guarded forms. Public rollup remains `+0.2786%` over target / `99.7262%` target speed, raw signed remains `-0.4886%` / `100.5086%`, and methodology speed totals remain about `17.12` public over-target points removed and `12.63` target-speed points gained; the baseline now carries two fewer WALKSTUF1 low loop reads for future generated ownership work. |
| 2026-05-13 | `walkstuf1-high-scalar-closure-v599-v608` | Log-only; source restored and PS1 rebuilt. | Close the remaining high-ranked WALKSTUF1 high scalar retained-read rows after v598 lowered low-side CD work without moving public timing. The batch intentionally tested both high-upside scheduler-owned clusters and one lower-risk candidate before leaving this optimization lane. | Temporarily added one shared WALKSTUF1 high/low read-group row at a time after the accepted `{213,229}` family, then restored the accepted table with `{427,443}` still present. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-shared-rg295-319-v599-focused/20260513-154002-1648628/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg84-108-v600-focused/20260513-154138-1658514/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg238-262-v601-focused/20260513-154304-1667511/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg238-262-slack4-v602-focused/20260513-154430-1676477/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg238-262-slack5-v603-focused/20260513-154553-1685191/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg306-330-v604-focused/20260513-154825-1700123/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg164-188-v605-focused/20260513-155000-1709719/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg360-384-v606-focused/20260513-155111-1717074/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg156-180-v607-focused/20260513-155222-1724518/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-shared-rg268-280-v608-focused/20260513-155336-1732139/summary.json`. | Failed/no promotion. `{295,319,0}`, `{238,262,5}`, and `{306,330,0}` were exact-flat at the accepted high timing `1764`, `1476/1434`, overrun `42`, blocking/refill `81/23`, reads `65`. `{84,108,0}` regressed to `1788`, `1500/1425`, overrun `75`, blocking/refill `103/26` despite reads `64`. `{238,262,0}` and `{238,262,4}` improved blocking/reads to `78` / `62` but regressed scene/loop to `1768/1480` and refill to `27`. `{164,188,0}` was destructive at `1784`, `1496/1419`, overrun `77`, blocking/refill `122/30`, reads `67`. `{360,384,0}` saved reads `65 -> 62` but regressed to `1774`, `1486/1432`, overrun `54`, blocking/refill `84/32`. `{156,180,0}` regressed to `1784`, `1496/1426`, overrun `70`, blocking/refill `108/33`. The lower-risk `{268,280,0}` still regressed to `1772`, `1484/1424`, overrun `60`, blocking/refill `98/27`. Pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Close these WALKSTUF1 high scalar rows. The useful signal is consistent: rows can save loop reads or blocking in isolation, but the current hand-authored table phase pays that back as visible-loop, target, or refill debt. Next WALKSTUF1 high work should be generated frame-deadline ownership, pack-side byte/phase reduction before the CD cluster, or upload/restore work removal, not another shared scalar append. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `walkstuf1-low-prepare-window-sweep-v609-v612` | Log-only; source restored and PS1 rebuilt. | Test whether WALKSTUF1 low could keep the accepted prepare-before-window behavior and then immediately reclaim CD window work from any remaining held slack. This was the direct scheduler-owner version of the scalar-row lesson: do visual prepare first, then let only leftover slack own CD. | Temporarily added a W1-low-only post-prepare `fgRuntimeTryPrefetchWindow` attempt after `fgRuntimePrepareStagedFrameForPresent`. Artifacts: conservative slack6 `scratch/ps1-perf-iterate/walkstuf1-low-prepare-then-window-v609-focused/20260513-160019-1772425/summary.json`; any-slack `scratch/ps1-perf-iterate/walkstuf1-low-prepare-then-window-anyslack-v610-focused/20260513-160132-1779970/summary.json`; slack2 `scratch/ps1-perf-iterate/walkstuf1-low-prepare-then-window-slack2-v611-focused/20260513-160248-1787833/summary.json`; slack4 `scratch/ps1-perf-iterate/walkstuf1-low-prepare-then-window-slack4-v612-focused/20260513-160409-1796205/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Slack6 did not fire and stayed exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, and reads `62`. Any-slack, slack2, and slack4 all fired but regressed to the same shape: scene `1776`, active loop/target `1484/1428`, overrun `56`, blocking/refill `81/23`, reads `66`, and due misses `14`. The PS-EXE bucket stayed `217088`, but the temporary scheduler branch grew `foregroundPilotPlay` by `140..156` bytes. | Close post-prepare speculative window refill as binary no-op/regress. The remaining WALKSTUF1 low gap is not solved by stealing slack after visual prepare; it needs a generated owner that knows exact frame/deadline windows, or pack-side byte/upload work reduction that lowers the cost before scheduling. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `building2-low-gapcompact-v613-v615` | Log-only; pack restored and PS1 rebuilt. | Test a no-runtime physical compaction for BUILDING2 low by removing the two large interior holes left behind by accepted tiny D4 payloads before the known `218..230` near-miss cluster. The goal was to shift the hot cluster earlier by one or more sectors without adding decode or source-code cost. | Generated three temporary `BUIL2LOW.FG2` variants from the accepted pack: v613 compacted both gaps after entries `71` and `77` (`6643 + 7919` bytes shifted out), v614 compacted only the later `77 -> 78` gap, and v615 compacted only the earlier `71 -> 72` gap. Generation summary: `scratch/building2-low-gapcompact-v613/summary.json`; focused artifacts: `scratch/ps1-perf-iterate/building2-low-gapcompact-v613-focused/20260513-161136-1839816/summary.json`; `scratch/ps1-perf-iterate/building2-low-gap77compact-v614-focused/20260513-161301-1848684/summary.json`; `scratch/ps1-perf-iterate/building2-low-gap71compact-v615-focused/20260513-161410-1856185/summary.json`. | Failed/no promotion. Compacting both gaps regressed scene/loop `1619/1349 -> 1627/1357`, overrun `29 -> 39`, blocking `70 -> 73`, refill `0 -> 2`, reads `52 -> 55`, and due misses `15 -> 17`. Compacting only the later gap regressed to `1625/1355`, overrun `39`, blocking/refill `87/3`, reads `58`, due `19`. Compacting only the earlier gap was less bad but still failed: `1623/1353`, overrun `36`, blocking/refill `76/2`, reads `55`, due `16`. Pack LBA, file size, and PS-EXE bucket stayed fixed. | Close naive D4-hole physical compaction for BUILDING2 low. The holes are phase-bearing padding, not free bytes; removing them shifts the active cadence into worse visible/refill slots. The `218..230` one-refill near-miss still needs a targeted byte reduction inside or immediately before that cluster, generated deadline ownership, or upload/restore work reduction, not broad interior gap removal. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `walkstuf1-origin-rebase-v616` | Log-only; dry-run zero-savings scan. | Test a no-runtime compact-origin rebase against both accepted WALKSTUF1 packs after scalar rows and post-prepare refill proved phase-negative. The goal was to find any current/residual origin offset that could be rebased in place, shrinking payload bytes before the tight W1 clusters without changing runtime code, file size, LBA, or read-table cadence. | Ran `scratch/visitor3-origin-rebase-v184/rebase_compact_origin.py --dry-run --frames 0-215` over `generated/ps1/foreground/WALK1LOW.FG2` and `generated/ps1/foreground/WALKSTUF1.FG2`. Summary: `scratch/walkstuf1-origin-rebase-v616/dryrun-summary.json`. | Failed/no promotion. Both packs reported `total_saved=0`, `applied_saved=0`, and `frames_rewritten=0`; every sampled payload already matched the input under the tested origin-rebase transform. No pack was modified and no PS1 timing gate was warranted. | Close compact-origin rebasing for WALKSTUF1 under the current FGP3/v4 data. The remaining W1 gap needs a transform below full-payload origin rebasing, such as row-span/header canonicalization, generated frame-deadline ownership, or upload/restore work reduction. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `walkstuf1-draw-gap-preserve-v617-v619` | Log-only; packs restored and PS1 rebuilt. | Test a no-runtime W1 row/span canonicalization pass below full-payload origin rebasing. The transform replayed known palette-index foreground state, merged only draw-span gaps whose pixels were already known foreground indices, preserved entry offsets/file size/LBA, and zero-filled reclaimed bytes. | Added scratch transformer `scratch/walkstuf1-draw-gap-merge-v617/merge_known_draw_gaps.py`. Low max-gap4 merged `53246` gaps, cut spans `138813 -> 85567`, and saved `93614` payload bytes; low gap1 merged `40224` gaps, cut spans to `98589`, and saved `77112` bytes; high gap1 merged `40307` gaps, cut spans to `98506`, and saved `77287` bytes. Focused artifacts: low gap4 `scratch/ps1-perf-iterate/walkstuf1-low-drawgap-preserve-v617-focused/20260513-163230-1963596/summary.json`; low gap1 `scratch/ps1-perf-iterate/walkstuf1-low-drawgap1-preserve-v618-focused/20260513-163358-1972728/summary.json`; high gap1 `scratch/ps1-perf-iterate/walkstuf1-high-drawgap1-preserve-v619-focused/20260513-163512-1980616/summary.json`. | Failed/no promotion. Low gap4 cut reads `62 -> 59` but regressed scene/loop/target to `1772`, `1480/1426`, overrun `54`, blocking/refill `75/29`, while payload/spans became `822525/86042`. Low gap1 stayed scene/loop flat at `1770/1478` but regressed target/refill to `1424`, overrun `54`, blocking/refill `77/27`, due `12`, payload/spans `839027/99064`. High gap1 regressed to `1774`, `1486/1424`, overrun `62`, blocking/refill `90/38`, reads `74`, payload/spans `841058/98981`. | Close preserve-offset W1 draw-gap coalescing. The byte/span savings are real, but the added draw pixels and dirtier target/refill cadence dominate. Future W1 pack work should reduce spans without adding pixels, use semantic/precomposed strip or row-template dictionaries, introduce generated frame-deadline ownership, or remove upload/restore work. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `walkstuf1-low-window-resweep-v620` | Log-only; no source/pack change. | Recheck the W1 low stream-window knee after the accepted shared `427..443` retained-read row changed loop-read shape. This was a cheap binary sweep of the already-proven split-window technique before investing in a generated owner or new pack format. | Ran explicit low-tide windows `36`, `38`, `42`, `44`, and `48` KiB with `--skip-build`, all against the restored v598 pack/source state. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-window-resweep-v620/20260513-164510-2038806/summary.json`. | Failed/no promotion. Every explicit window was worse than the current `40 KiB` default (`1770`, `1478/1431`, overrun `47`, blocking/refill `64/20`, reads `62`, due `11`). Results: 36 KiB `1525/1434`, overrun `91`, blocking/refill `96/52`, reads `29`; 38 KiB `1526/1438`, overrun `88`, blocking/refill `109/44`, reads `31`; 42 KiB `1521/1436`, overrun `85`, blocking/refill `103/41`, reads `27`; 44 KiB `1533/1436`, overrun `97`, blocking/refill `106/52`, reads `22`; 48 KiB `1527/1436`, overrun `91`, blocking/refill `96/55`, reads `24`. | Keep W1 low at the current scene-local `40 KiB` window. Read-count reduction alone is not a promotion signal for this row; windows outside the knee steal visible/refill cadence. Future W1 low work needs generated frame-deadline ownership or data/upload reduction that lowers work without paying it back as blocking/refill. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `building2-low-split218-224-224-230-v622` | Log-only; source restored and PS1 rebuilt. | Retry the strongest BUILDING2 low scalar near-miss with split ownership instead of the single `218..230` append. The prior raw/slack8 row improved scene `1619 -> 1614`, loop `1349 -> 1344`, overrun `29 -> 27`, blocking `70 -> 61`, reads `52 -> 50`, and due misses `15 -> 14`, but failed only on refill `0 -> 1`; the split tested whether `218..224` plus `224..230` could keep the visible win while avoiding that one refill VBlank. | Temporarily inserted `{218,224,0}` and `{224,230,0}` ahead of the accepted BUILDING2 low rows, rebuilt, and ran the strict focused `building2-low-canary` gate against the current v454 baseline. Artifact: `scratch/ps1-perf-iterate/building2-low-split218-224-224-230-v622-focused/20260513-165951-2125707/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The split did keep refill flat at `0`, but the smaller groups lost the useful scalar trade: scene `1619 -> 1623`, active loop/target `1349/1320 -> 1353/1319`, overrun `29 -> 34`, blocking `70 -> 74`, loop reads `52 -> 51`, and due misses stayed `15`. Pack LBA/sectors and the `217088` byte PS-EXE bucket stayed fixed. | Close the simple `218..224 + 224..230` split as phase-negative. The single-row `218..230` near-miss remains the better signal, but it now needs pack-side byte reduction, upload/restore work reduction, or true generated deadline ownership that preserves the raw row's visible blocking win without paying the refill VBlank. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `walkstuf1-low-xband-upload-v623` | Log-only; source restored and PS1 rebuilt. | Make one last generic runtime upload swing after W1 detail showed high upload byte volume and the preprocess matrix still modeled large x-band savings. Unlike the earlier scene-gated v418 helper, this probe reused the existing per-row dirty X metadata in the shared `grDrawBackground()` band path, staged only bands narrower than `256` pixels through a static `32 KiB` buffer, and fell back to the full-width path for broad bands. | Temporarily added a staged x-band helper in `graphics_ps1.c`, rebuilt, and ran the focused WALKSTUF1 low gate against the accepted v598 baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-xband-upload-v623-focused/20260513-170533-2159822/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The run timed out without `JCPERF2`, so no correctness or timing counters were promotable. The implementation was structurally ineligible before timing: PS-EXE bucket grew `217088 -> 219136`, `jcreborn.elf` grew to `966388`, and downstream hot symbols shifted by about `+1116` bytes even though `grDrawBackground` itself shrank under the helper split. | Close runtime scratch/staged dirty-X uploads globally, not just the earlier W1/Visitor3 scene-gated variants. The x-band byte model can still guide pack-authored/precomposed work, but strided tile-RAM copy during playback is too slow and too code-size-heavy. Future upload work must be pre-contiguous pack data, direct upload-ready payloads, or code-size-neutral metadata that avoids expanding the hot graphics path. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `building2-low-rg218-230-plus250-262-v624` | Log-only; source restored and PS1 rebuilt. | Combine the best BUILDING2 low near-miss with the downstream candidate exposed after that near-miss fires. The raw/slack8 `218..230` row was one refill VBlank short of promotion, and the post-raw read plan exposed `250..262` as a lower-visible-risk follow-up; the binary question was whether the extra downstream grouping would absorb refill/deadline pressure or simply deepen cadence debt. | Temporarily inserted `{218,230,8}` plus `{250,262,0}` ahead of the accepted low rows, rebuilt, and ran the strict focused `building2-low-canary` gate against the current v454 baseline. Artifact: `scratch/ps1-perf-iterate/building2-low-rg218-230-plus250-262-v624-focused/20260513-171152-2197356/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The combination proved the read/blocking win is real, but not public-safe: blocking improved `70 -> 56`, loop reads `52 -> 49`, and due misses `15 -> 12`, while scene regressed `1619 -> 1625`, active loop/target regressed `1349/1320 -> 1355/1319`, overrun regressed `29 -> 36`, hidden refill regressed `0 -> 1`, and hidden CD time rose `151 -> 163`. Pack LBA/sectors and the `217088` byte PS-EXE bucket stayed fixed. | Close scalar follow-up grouping around `218..230` plus `250..262`. Extra grouped rows can lower visible blocking, but the active loop pays it back as target/refill debt. BUILDING2 low should now move to pack-side byte/work reduction or generated deadline ownership with explicit refill budgets. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `policy-cache-v625` | Log-only; source restored and PS1 rebuilt. | Retest the runtime policy-cache idea against the current v598 WALKSTUF1 low baseline after the hand-authored scalar queue was exhausted. This version cached scene-policy flags plus the window minimum slack at scene start and replaced hot `fgSceneEquals()` checks in previous-frame D4, direct-stage, VISITOR3 fallthrough, WALKSTUF1 window gating, and low-tide prepare-before-window. | Temporarily added `scenePolicyFlags` and `windowMinSlackVBlanks` to `TFgPilotRuntime`, rebuilt in the fixed `217088` byte PS-EXE bucket, and ran the strict focused `walkstuf1-low-canary` gate against `scratch/ps1-perf-iterate/walkstuf1-shared-rg427-443-v598-focused/20260513-152315-1548645/summary.json`. Artifact: `scratch/ps1-perf-iterate/policy-cache-v625-w1-low/20260513-173243-2317845/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. WALKSTUF1 low stayed flat on public timing at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking `64`, loop reads `62`, and due misses `11`, but hidden refill/prefetch overrun regressed `20 -> 21`. Hot symbols moved substantially and `foregroundPilotPlay` grew by `80` bytes even though the PS-EXE bucket stayed fixed. | Close policy caching again on the current baseline. The cached checks are not code-shape-neutral and can worsen refill without a speed/work win; any future policy bits should arrive only as generated scheduler metadata with a hot-symbol budget, not as a standalone runtime-shape probe. Public rollup and methodology totals remain at v598. |
| 2026-05-13 | `building2-low-rg218-229-slack8-v626` | `ps1: add building2 low rg218-229` | Salvage the strongest BUILDING2 low scalar near-miss. The wider `218..230` slack8 row already proved the visible win (`1614/1344`, blocking `61`, reads `50`) but missed promotion by one hidden refill VBlank; the binary follow-up was whether trimming the tail sector to `218..229` would keep the useful timing while avoiding the refill debt. | Added `{218,229,8}` before the accepted low `238..250`, `318..330`, and `365..381` rows, rebuilt, and ran the strict focused gate plus controls. Artifacts: focused `scratch/ps1-perf-iterate/building2-low-rg218-229-slack8-v626-focused/20260513-174244-2376448/summary.json`; VISITOR3 high/low and BUILDING2 high/low controls `scratch/ps1-perf-iterate/building2-low-rg218-229-slack8-v626-canaries-pass/20260513-175047-2425621/summary.json`; WALKSTUF1 low control `scratch/ps1-perf-iterate/building2-low-rg218-229-slack8-v626-w1-low-control/20260513-175448-2450223/summary.json`; WALKSTUF1 high control `scratch/ps1-perf-iterate/building2-low-rg218-229-slack8-v626-w1-high-control/20260513-175557-2457410/summary.json`. | Promoted. BUILDING2 low improves scene/loop `1619/1349 -> 1614/1344`, target `1320 -> 1318`, overrun `29 -> 26`, blocking `70 -> 61`, prefetch overrun stays `0`, loop reads `52 -> 50`, loop-read time `221 -> 218`, and due misses `15 -> 14`; pack LBA/sectors stay `6818/637` and the PS-EXE bucket stays `217088`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high/low controls stayed exact-flat. | Keep `218..229` as the new BUILDING2 low baseline and close the wider `218..230` one-refill form as superseded unless a future pack/source shape changes the refill budget. Public rollup moves to `+0.2767%` over target / `99.7279%` target speed; raw signed rollup moves to `-0.4904%` / `100.5103%`. This adds about `0.0018` public over-target points removed and `0.0017` target-speed points gained; methodology totals are about `17.12` / `12.63`. |
| 2026-05-13 | `walkstuf1-low-prepare-cooldown-v627` | Log-only; source restored and PS1 rebuilt. | Test the generated-ownership cooldown idea from the W1 low queue: after the accepted prepare-before-window branch chooses visual prepare over CD ownership, suppress the same-loop opportunistic window refill so the next loop can own CD only if slack is still safe. This checked whether W1 low was paying hidden/refill debt from immediate post-prepare refill. | Temporarily added a `suppressWindowAfterPrepare` guard in the held-frame scheduler, rebuilt in the fixed `217088` byte PS-EXE bucket, and ran the strict focused `walkstuf1-low-canary` gate against the accepted v598 baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-prepare-cooldown-v627-focused/20260513-182331-2615023/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. W1 low was exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking `64`, hidden refill `20`, loop reads `62`, loop-read time `281`, and due misses `11`; the gate failed `--require-improvement`. The temporary branch also grew `foregroundPilotPlay` by `72` bytes and shifted hot graphics/perf symbols by `+72`, so it is structurally worse than the accepted baseline. | Close same-loop post-prepare cooldown as inert/code-shape-negative. The accepted W1 low scheduler path is not leaving an easy refill win in that slot; next W1 work should be generated deadline ownership with explicit hit counts or pack-side visual/upload byte reduction that lowers work without growing the hot scheduler. Public rollup and methodology totals remain at v626. |
| 2026-05-13 | `walkstuf1-low-pack-detail-v628` | Log-only; no source/pack change. | Before leaving W1 low, check whether an already available pack-side cleanup transform could produce a zero-runtime byte win, and capture one detail pass to verify the bottleneck mix. This targeted the current biggest under-99 row without adding hot scheduler branches. | Ran out-of-place transforms under `scratch/walkstuf1-low-host-transforms-v628/`: trim-draw-tails, zero-noop-entries, PAL4 draw metadata, and restore-minus-current. Then captured a perf-detail sample at `scratch/ps1-perf-iterate/walkstuf1-low-detail-v628/20260513-182647-2634557/summary.json` with log `scratch/ps1-perf-iterate/walkstuf1-low-detail-v628/20260513-182647-2634557/walkstuf1-low-detail/headless-regtest.log`. | Failed/no promotion. Trim-draw-tails and zero-noop entries saved `0` bytes, PAL4 draw metadata rejected the pack as not compact PAL4, and restore-minus-current grew active payload `916139 -> 917347` despite reducing restore bytes. The detail run is instrumentation-heavy and slower (`1488/1428`), but its counters show the real mix: blocking/refill `100/29`, scheduler wait `669`, compose crossings `64`, upload VBlanks only `9`, and full-width upload bytes are high but not the cheapest timing lever. | Close the existing generic W1-low pack cleanup tools for this baseline. The next W1-low attempt needs either generated deadline ownership with explicit hit counts or a new custom pack representation that reduces spans/bytes without adding pixels, payload growth, or hot decode branches. Public rollup and methodology totals remain at v626. |
| 2026-05-13 | `visitor3-high-tail-pack-v629` | `ps1: repack visitor3 high tail` | Use the same pack-only technique that worked for VISITOR3 low v338 on the remaining high-side tail read. Frames `143` and `144` in high were byte-identical to the old low payloads before v338, so the binary question was whether the proven compact cleanup payloads could free enough tail space to move frame `141` fully inside the already paid high `277..293` setup segment without changing source, pack footprint, LBA/sectors, or the PS-EXE bucket. | Repacked only `generated/ps1/foreground/VISITOR3.FG2`. Frame `141` moves from offset `563699` / sector `275` to offset `567296` / sector `277`, frames `140`, `142`, compacted `143`, compacted `144`, and the 32-byte sound-event table then fit contiguously inside `277..293` with `790` bytes slack. Transform summary: `scratch/visitor3-high-tail-pack-v629/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-tail-pack-v629-focused/20260513-184157-2722587/summary.json`. | Promoted. VISITOR3 high improves scene `1400 -> 1398`, active loop/target `1065/1039 -> 1063/1040`, overrun `26 -> 23`, blocking `41 -> 35`, loop-read VBlanks `41 -> 35`, loop reads `7 -> 6`, and due misses `7 -> 6`; hidden refill and prefetch overrun stay `0`. Pack bytes/LBA/sectors stay `1555450`/`22611`/`760`, and the PS-EXE bucket stays `217088`. | Keep v629 as the new VISITOR3 high baseline. Public rollup moves to `+0.2744%` over target / `99.7301%` target speed; raw signed rollup moves to `-0.4927%` / `100.5125%`. This adds about `0.0023` public over-target points removed and `0.0022` target-speed points gained; methodology totals are about `17.13` public over-target points removed and `12.63` target-speed points gained since the compact full-matrix baseline. Next targets: WALKSTUF1 low, BUILDING2 high, WALKSTUF1 high, VISITOR3 low, BUILDING2 low, and JOHNNY1 high/low. |
| 2026-05-13 | `walkstuf1-low-rg91-98-v630-v632` | Log-only; source restored and PS1 rebuilt. | Test the next W1-low queue item after the VISITOR3 high promotion: keep the accepted `78..91` row, then add a separate `91..98` grouped append only when enough slack is available. This was the safe two-phase form of the earlier rejected `78..98` replacement. | Temporarily added `{91,98,minSlack}` after `{78,91,0}` in the WALKSTUF1 read table and tested slack `6`, `5`, and `4` against the accepted v598 baseline. Artifacts: slack6 `scratch/ps1-perf-iterate/walkstuf1-low-rg91-98-slack6-v630-focused/20260513-185837-2818891/summary.json`; slack5 `scratch/ps1-perf-iterate/walkstuf1-low-rg91-98-slack5-v631-focused/20260513-190005-2827999/summary.json`; slack4 `scratch/ps1-perf-iterate/walkstuf1-low-rg91-98-slack4-v632-focused/20260513-190126-2836506/summary.json`. Slack3 is structurally unreachable because W1 low rejects window prefetch at `<=3` slack. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. All three feasible gates were exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads `62`, loop-read VBlanks `281`, due misses `11`, and `group_hits=0`; the strict gates failed `--require-improvement`. Pack LBA stayed `25633` and the PS-EXE bucket stayed `217088`. | Close the two-phase `91..98` scalar row for this baseline. The accepted `78..91` row already captures the safe post-prime opportunity; the next W1-low work should move to generated deadline ownership for the `285..321` cluster or a custom pack representation that reduces bytes/spans before scheduling. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-rg297-321-v633-v634` | Log-only; source restored and PS1 rebuilt. | Test the highest remaining W1-low late-cluster row from the current read plan, `297..321`, as a large grouped append with slack gating. This was a bigger swing than `91..98`: if the append start lined up, it could cover five tight-loop reads without adding runtime branches. | Temporarily added `{297,321,minSlack}` after the accepted `213..229` row and tested slack `5` and `4` against the accepted v598 baseline. Artifacts: slack5 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-slack5-v633-focused/20260513-190617-2865155/summary.json`; slack4 `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-slack4-v634-focused/20260513-190738-2873753/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both gates stayed exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads `62`, loop-read VBlanks `281`, due misses `11`, and `group_hits=0`; lowering below slack4 cannot help because W1 low rejects window prefetch at `<=3` slack. Pack LBA stayed `25633` and the PS-EXE bucket stayed `217088`. | Close plain `297..321` scalar ownership. The planner's sector range is real, but the runtime append start never reaches it; future W1-low generated ownership must be based on observed append starts or a pack reorder that changes the window boundary, not another same-shape read-table row. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building2-high-drawgap1-v635` | Log-only; pack restored and PS1 rebuilt. | Try a no-runtime BUILDING2 high pack-shape reduction after scalar rows exhausted. The preserve-offset draw-gap merge replays known foreground pixels and merges only one-pixel draw gaps, aiming to shrink payload/span work without changing pack size, LBA, or hot code. | Ran the generic draw-gap transformer on `generated/ps1/foreground/BUILDING2.FG2`. Host gap1 changed `231` frames, saved `54093` payload bytes, merged `29058` gaps, and cut spans `104351 -> 75293`; gap2 saved `65879` bytes but added more draw pixels, so it was not gated after gap1 failed. Applied gap1 and ran focused high timing at `scratch/ps1-perf-iterate/building2-high-drawgap1-v635-focused/20260513-191150-2898755/summary.json`; transform summary `scratch/building2-draw-gap-v635/BUILDING2.applied-gap1.summary.json`. The original pack was restored from `scratch/building2-draw-gap-v635/BUILDING2.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The byte/span savings were real but phase-negative: BUILDING2 high regressed scene `1602 -> 1607`, active loop/target `1351/1311 -> 1356/1312`, overrun `40 -> 44`, blocking `54 -> 57`, hidden refill/prefetch overrun `18 -> 22`, with loop reads still `58` and due misses still `7`. Pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close preserve-offset draw-gap merging for BUILDING2 high under the current codec. Like W1, adding draw pixels to reduce span headers trades bytes for visible/refill cadence. Future BUILDING2 high pack work must reduce bytes/spans without increasing pixels, move payload boundaries without changing decode work, or use precomposed/upload-ready data. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building2-high-frame91-gap1-v636` | Log-only; pack restored and PS1 rebuilt. | Test the targeted version of the v635 pack-shape miss instead of the broad 231-frame transform. Frame `91` was the documented no-decode sector-boundary candidate: applying the preserve-offset gap1 merge only to that frame shrinks its payload without source changes, file-size changes, LBA movement, or PS-EXE bucket movement. | Generated a fresh full gap1 candidate, then copied only frame `91` back into `generated/ps1/foreground/BUILDING2.FG2`. The host transform shrank frame `91` from `8867 -> 7594` bytes, saving `1273` bytes, merging `683` one-pixel gaps, and moving the frame end from sector `151 -> 150` while keeping the pack at `1303332` bytes. Transform summary: `scratch/building2-frame91-gap1-v636/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-frame91-gap1-v636-focused/20260513-193045-3007872/summary.json`. The original pack was restored from `scratch/building2-frame91-gap1-v636/BUILDING2.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The one-frame sector shave was exact-flat against the accepted BUILDING2 high baseline: scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking `54`, hidden refill/prefetch overrun `18`, loop reads `58`, and due misses `7`. Pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`; the strict gate failed `--require-improvement`. | Close frame91 preserve-offset gap1 as inert. A single-frame no-runtime sector-boundary shave still does not change observed CD/window cadence, and it adds draw pixels. Future BUILDING2 high pack work needs byte/span reduction without extra pixels, a payload placement change that changes an observed read boundary, or generated deadline ownership. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `visitor3-low-d4-gap-scan-v637` | Log-only; low pack/source restored and PS1 rebuilt. | Continue the VISITOR3 low paid-gap branch after v477/v510 moved tiny D4 payloads into already paid setup-prime bytes. The host pass checked whether any remaining due frames could fit the `204452..215040` setup-prime gap, then tested the only plausible same-offset D4 sector shave before closing the lane. | Host D4 sizing found frames `130`, `133`, and `138` were not smaller as previous-frame D4 payloads. Frames `131`, `134`, `135`, and `136` shrank by about `2.8 KiB`, but their encoded sizes stayed around `14.1..14.2 KiB`, too large for the remaining `10588` paid prime-gap bytes. The focused PS1 probe rewrote frame `131` at its original offset (`16997 -> 14212`, `2785` bytes saved, `961` commands) and temporarily added low-tide frame `131` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/visitor3-low-frame131-d4-v637/summary.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-low-frame131-d4-v637-focused/20260513-193840-3054036/summary.json`. | Failed/no promotion. The same-offset frame131 D4 probe regressed VISITOR3 low from scene `1391 -> 1400`, active loop `1062 -> 1071`, overrun `22 -> 30`, blocking `42 -> 49`, loop reads `7 -> 8`, and due misses `7 -> 8` while pack LBA and the `217088` byte PS-EXE bucket stayed fixed. | Close simple VISITOR3 low previous-frame D4 gap relocation for the remaining due frames. The current paid gaps can only absorb sub-`10.6 KiB` payloads, and the larger same-offset D4 shape pays more decode/cadence than it saves. Future VISITOR3 low work needs a different custom compression family, generated deadline ownership, or new paid residency; do not retry ordinary previous-frame D4 for frames `130`, `131`, `133`, `134`, `135`, `136`, or `138`. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-row-dict-scan-v638` | Log-only; host-only no pack/source change. | Test the next WALKSTUF1 low pack-shape idea before adding any runtime decoder: a scene-local dictionary for repeated cleanup row records and draw row metadata across the hot `146..158` cluster. The goal was to prove a no-pixel, no-scheduler byte win before paying code-size or decode overhead. | Parsed `generated/ps1/foreground/WALK1LOW.FG2` FGP3/v4 payloads for frames `146..158`. Cleanup row records totaled `1000` rows / `10218` bytes but had `980` unique rows; a dictionary plus one-byte references would grow to `11134` bytes. Draw row metadata totaled `995` rows / `22460` bytes but had `978` unique rows; a dictionary plus one-byte references would grow to `23235` bytes. The top duplicate cleanup savings was only `84` bytes and top duplicate draw-metadata savings only `220` bytes before references. | Failed/no promotion. The host byte report proves the proposed row dictionary cannot save a sector in the current hot cluster and would grow the data even before a runtime decoder, so no PS1 gate was warranted. | Close simple row-record/draw-metadata dictionarying for WALKSTUF1 low frames `146..158`. Future W1 low pack work needs semantic row-template compression that can group near-duplicates, generated deadline ownership, or upload/restore work reduction; exact row dictionaries are not a viable byte-shape win on this baseline. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-rg297-321-raw-v639` | Log-only; source restored and PS1 rebuilt. | Complete the binary sweep for the late W1 low `297..321` cluster. v633/v634 only tested slack-gated forms, and the current read plan still marked the raw row as fireable, current-window-compatible, and modeled as a five-read saver, so this probe tested the unguarded table row before moving to generated ownership. | Temporarily inserted `{297,321,0}` after the accepted `{213,229}` row in `kWalkstuf1HighReadGroups12`, rebuilt in the fixed `217088` byte PS-EXE bucket, and ran the strict focused low-tide gate against the accepted v598 baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-raw-v639-focused/20260513-194508-3091888/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The raw row fired and reduced loop reads `62 -> 58`, but it moved the cost into visible/refill cadence: scene `1770 -> 1776`, active loop `1478 -> 1484`, overrun `47 -> 53`, blocking `64 -> 79`, hidden refill/prefetch overrun `20 -> 21`, and due misses `11 -> 12`. Pack LBA stayed `25633` and the PS-EXE bucket stayed fixed. | Close raw scalar ownership for W1 low `297..321`. The modeled saved reads are real, but not timing-safe under the hand-authored grouped-append path. Future work on this cluster needs generated deadline ownership that can choose the gap without stealing visual prepare, or a pack-side byte/work reduction before scheduling. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building2-high-rg185-197-slack8-v640` | Log-only; source restored and PS1 rebuilt. | Revisit the separate BUILDING2 high `185..197` cluster with a slack guard after the raw v557 row proved read savings but visible/refill regression. The binary question was whether requiring eight held VBlanks would keep the two-read win while avoiding the unsafe phase. | Temporarily inserted `{185,197,8}` before the accepted `{206,230}` row in `kBuilding2HighReadGroups12`, rebuilt in the fixed `217088` byte PS-EXE bucket, and ran the strict focused high-tide gate against the accepted v441 baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg185-197-slack8-v640-focused/20260513-194741-3107394/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The guarded row still fired and reproduced the unsafe trade: loop reads `58 -> 56`, but scene `1602 -> 1607`, active loop `1351 -> 1356`, overrun `40 -> 44`, blocking `54 -> 58`, and hidden refill/prefetch overrun `18 -> 23`. Pack LBA stayed `6181` and the PS-EXE bucket stayed fixed. | Close slack-gated `185..197` for BUILDING2 high. This cluster needs generated deadline ownership or pack-side work reduction; hand table rows, even guarded, convert read savings into visible/refill debt. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `visitor3-high-rg211-235-current-v641` | Log-only; source restored and PS1 rebuilt. | Retest the VISITOR3 high `211..235` grouped-append row against the current v629 tail-repack baseline. The stale v569/v570 rows were exact-flat before v629; the refreshed read plan again showed `211..235` as a current-compatible one-read candidate, so this checked whether the pack-side tail win reopened the scalar lane. | Temporarily added a high-only `kVisitor3HighReadGroups24[] = {{211,235,0}}` route for compact VISITOR3 high. The first run used the wrong case label and was rejected by `--require-improvement`; the valid matching-label rerun is `scratch/ps1-perf-iterate/visitor3-high-rg211-235-current-v641-match/20260513-195215-3134927/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The current-pack row stayed exact-flat against v629: scene `1398`, active loop/target `1063/1040`, overrun `23`, blocking `35`, loop reads `6`, and due misses `6`. The only effect was source/code shape: `foregroundPilotPlay` grew by `48` bytes and hot graphics/perf symbols shifted by `+48`. | Close VISITOR3 high `211..235` again on the post-v629 baseline. The pack tail win did not make this scalar row productive; remaining high work needs custom compression, payload placement inside existing paid segments, or generated deadline ownership with a proven non-flat work metric. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `johnny1-pack-cleanup-proof-v642` | Log-only; host-only no pack/source change. | Test whether JOHNNY1's remaining under-99 gap has a pack-authored cleanup-suppression win before retrying code-heavy black-clear paths. Since JOHNNY1 has `due_misses=0`, the probe looked for redundant same-frame cleanup, duplicate payloads, or no-op held-frame cleanup that could be removed without a runtime primitive. | Parsed the current `generated/ps1/foreground/JOHNNY1.FG2` FGP3/v4 pack. It has `112` frames, one zero-size frame, no duplicate payload groups, and no repeated offset runs. Cleanup exists almost entirely in frame `2`: `204436` cleanup pixels in `2828` cleanup bytes, with only `216` draw pixels in the same frame and `0` same-frame cleanup/draw overlap. Total pack cleanup/draw pixels are `204436/519908`, with `0` same-frame redundant cleanup pixels. | Failed/no promotion. The host proof found no pack-only cleanup spans that can be omitted safely. The remaining signal is a runtime primitive problem: frame `2` restores a large black/background area, matching the prior v393 direct-black-clear speed signal, but pack surgery alone cannot suppress it without stale pixels. | Close pack-only JOHNNY1 cleanup suppression/no-op aliasing for the current pack. Future JOHNNY1 work should either reclaim code headroom for a smaller black-clear primitive, generate code-neutral dirty-upload metadata, or use a new cleanup opcode with exact visual proof and fixed PS-EXE bucket. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `johnny1-cleanbgspan-o2-v643` | Log-only; source restored and PS1 rebuilt. | Try the smallest codegen-only version of the JOHNNY1 black-cleanup signal after v642 proved pack-only cleanup suppression is unavailable. The probe put only `grRestoreCleanBgSpanFromRects()` under function-scoped `optimize("O2")`, looking for a fixed-bucket restore-loop win without reintroducing the oversized v393 direct-black-clear helper. | Temporarily added `__attribute__((optimize("O2")))` to `grRestoreCleanBgSpanFromRects()`. The PS-EXE stayed in the fixed `217088` byte bucket, `grRestoreCleanBgSpanFromRects` stayed `1068` bytes, `grDrawBackground` stayed `1300`, and `foregroundPilotPlay` stayed `11964`. Focused paired gate artifact: `scratch/ps1-perf-iterate/johnny1-cleanbgspan-o2-v643-focused/20260513-200016-3182361/summary.json`. The source attribute was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. Both JOHNNY1 tides were exact-flat against the current paired baseline: scene `2069`, active loop/target `1973/1945`, overrun `28`, blocking/refill `25`, loop reads `7`, and due misses `0`. The strict gate failed `--require-improvement` on both labels. | Close function-scoped O2 on the clean-background span restore as an inert compiler-shape probe. The black-cleanup signal still needs a real code-size-neutral primitive, generated dirty-upload/restore metadata, or reclaimed cold-code headroom before retrying runtime clear work. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-deadline-rg297-321-v644` | Log-only; source restored and PS1 rebuilt. | Test a narrow generated-deadline approximation for the worst remaining row. Prior scalar `297..321` proved real read savings only when it expanded on the due path; this probe added `{297,321,4}` and let WALKSTUF1 low choose CD-window ownership before visual prepare only when the retained stream window had reached sector `297`. | Temporarily added `{297,321,4}` to the WALKSTUF1 read groups and a low-tide sector-297 scheduler branch ahead of the accepted visual prepare-before-window policy. The build stayed in the `217088` byte PS-EXE bucket, but `foregroundPilotPlay` grew `11964 -> 12052` bytes and downstream hot graphics/perf symbols shifted by `+88`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-deadline-rg297-321-v644-focused/20260513-201150-3250147/summary.json`. | Failed/no promotion. The branch moved the row deeper into the visible/refill cliff: scene `1770 -> 1799`, active loop/target `1478/1431 -> 1489/1430`, overrun `47 -> 59`, blocking `64 -> 94`, hidden refill/prefetch overrun `20 -> 25`, loop reads `62 -> 64`, and due misses `11 -> 16`. | Close sector-297 CD-before-prepare ownership for WALKSTUF1 low. The learned constraint is stronger: this cluster cannot be rescued by simply giving CD priority at the deadline; it needs pack-side byte/work reduction, a lower-CPU representation, or generated ownership that prefetches earlier without evicting prepared visual work. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building2-low-rg250-262-after-v626-v645` | Log-only; source restored and PS1 rebuilt. | Retest the downstream BUILDING2 low `250..262` candidate on the current v626 baseline. The earlier v571-v574 row was rejected before the accepted `218..229` slack8 promotion; this checked whether that new upstream phase made the adjacent follow-up useful. | Temporarily inserted `{250,262,0}` after the accepted `{238,250}` row while keeping `{218,229,8}`, `{318,330}`, and `{365,381}` intact. The build stayed in the `217088` byte PS-EXE bucket and grew `foregroundPilotPlay` only `+4` bytes. Focused artifact: `scratch/ps1-perf-iterate/building2-low-rg250-262-after-v626-v645-focused/20260513-201653-3280002/summary.json`. | Failed/no promotion. The row was worse on the v626 baseline: scene `1614 -> 1649`, active loop/target `1344/1318 -> 1358/1317`, overrun `26 -> 41`, blocking `61 -> 64`, hidden refill/prefetch overrun `0 -> 4`, loop reads `50 -> 48`, and due misses `14 -> 13`. | Close `250..262` even after the v626 upstream row. The saved reads/due misses are real but the cadence/refill cost is larger than before; remaining BUILDING2 low work should target pack-side work/byte reduction or true generated deadline metadata, not adjacent scalar follow-up rows. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building4-d4-frames33-37-v646` | Log-only; packs/source restored and PS1 rebuilt. | Test the biggest no-runtime-looking BUILDING4 pack byte win before building a larger static-mask pipeline. A host scan found previous-frame D4 candidates for frames `33` and `37`: frame `33` shrank `6656 -> 13` bytes and frame `37` shrank `8828 -> 909` bytes, saving about seven sectors across both packs while preserving file size. | Rewrote frames `33` and `37` in both `BUILDING4.FG2` and `BUIL4LOW.FG2`, then temporarily allowed BUILDING4 to use the existing previous-frame D4 alternate load buffer for those two frames. Transform summaries: `scratch/building4-d4-v646/BUILDING4.d4-summary.json` and `scratch/building4-d4-v646/BUIL4LOW.d4-summary.json`. Focused high/low artifact: `scratch/ps1-perf-iterate/building4-d4-frames33-37-v646-focused/20260513-202622-3335649/summary.json`. | Failed/no promotion. The byte savings did not reduce loop reads and the D4 decode/cadence cost regressed both tides. High moved `2844/2816 -> 2848/2818`, overrun `28 -> 30`, blocking `37 -> 38`, while refill improved `30 -> 26`. Low regressed `2855/2815 -> 2868/2817`, overrun `40 -> 51`, blocking `46 -> 52`, and refill `38 -> 47`. Packs and source were restored; the clean rebuild returned to the accepted `217088` byte PS-EXE bucket with `foregroundPilotPlay=11964`. | Close ordinary previous-frame D4 for BUILDING4 frames `33` and `37`. Even large sector-size payload wins are not enough when they do not change observed read count and add decode work in a tight scene. Remaining BUILDING4 low work should use same-commit render/CD classification, static-mask/precomposed upload bands, or cleanup/upload suppression that measurably reduces work counters before timing. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building2-high-forward-alias70-v647` | Log-only; pack restored and PS1 rebuilt. | Test the forward-only duplicate-payload placement variant from the post-v646 queue. The earlier v507 alias pointed frame `71` backward to frame `70` and stayed flat; this probe inverted that shape so frame `70` points forward to frame `71`'s byte-identical `6656` byte payload at sector `75`, avoiding decoder code and preserving pack file size, LBA, and old payload bytes as inert padding. | Patched only the `BUILDING2.FG2` frame table entry for frame `70`; no source changed. Host summary: `scratch/building2-high-forward-alias-v647/summary.json`. Focused high-tide artifact: `scratch/ps1-perf-iterate/building2-high-forward-alias70-v647-focused/20260513-203909-3409896/summary.json`. The original pack was restored from `scratch/building2-high-forward-alias-v647/BUILDING2.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The forward alias kept loop reads and due misses flat at `58` and `7`, but shifted the CD/visual phase negative: scene `1602 -> 1605`, active loop/target `1351/1311 -> 1354/1309`, overrun `40 -> 45`, blocking `54 -> 57`, and hidden refill/prefetch overrun `18 -> 21`. Pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close adjacent duplicate forward aliasing for BUILDING2 high frame `70/71`. Both backward and forward table-only aliases are now proven insufficient: one is inert, the other regresses cadence. Future duplicate-payload work needs a scheduler-costed physical copy/reorder planner, not single-entry aliasing. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-offscreen-drawclip-v648` | Log-only; pack restored and PS1 rebuilt. | Test the pack-authored offscreen draw clipping lane on the largest remaining row. Host scan showed `WALK1LOW.FG2` was still carrying `126750` PAL4 draw pixels outside the 640x480 viewport, similar in scale to the earlier VISITOR3 high offscreen clip win but without changing source code, entry offsets, entry sizes, pack bytes, or pack LBA. | Rewrote only the logical draw streams in `WALK1LOW.FG2`, preserving every payload slot and zero-filling clipped tails. The transform changed `60` frames, removed `126750` offscreen draw pixels, dropped `12767` spans and `343` draw rows, and reduced logical draw bytes by `94836`. Host summary: `scratch/walkstuf1-low-offscreen-drawclip-v648/summary.json`. Focused low-tide artifact: `scratch/ps1-perf-iterate/walkstuf1-low-offscreen-drawclip-v648-focused/20260513-204420-3440299/summary.json`. The original pack was restored from `scratch/walkstuf1-low-offscreen-drawclip-v648/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The work reduction was real but phase-negative for the W1 low scheduler: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1427`, overrun `47 -> 63`, blocking `64 -> 88`, hidden refill/prefetch overrun `20 -> 27`, loop reads `62 -> 64`, and due misses `11 -> 13`. Pack LBA stayed `25633` and the PS-EXE bucket stayed `217088`. | Close broad offscreen draw clipping for WALKSTUF1 low under the current scheduler. Removing hidden draw work gives the runtime different CD opportunities and makes the tight row worse. Future W1 low draw-work reduction needs scheduler coupling, a frame-subset gate with proof that reads do not increase, or static/precomposed data that lowers upload/restore without opening unsafe refill slots. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-offscreen-guard4-5-v649-v650` | Log-only; pack/source restored and PS1 rebuilt. | Try to rescue the v648 offscreen draw-work reduction by preventing the newly opened slack from being spent on unsafe W1 low window refills. This paired the same clipped `WALK1LOW.FG2` pack with two scalar low-tide guard settings: skip speculative window prefetch at `<=4` held VBlanks, then at `<=5`. | Reapplied the v648 clipped pack, changed the low-tide WALKSTUF1 branch in `fgRuntimeTryPrefetchWindow()` to `FG_PREFETCH_WINDOW_MIN_SLACK_VBLANKS + 1`, built, and gated; then changed it to `+2`, rebuilt, and gated. Artifacts: guard4 `scratch/ps1-perf-iterate/walkstuf1-low-offscreen-guard4-v649-focused/20260513-204725-3458765/summary.json`; guard5 `scratch/ps1-perf-iterate/walkstuf1-low-offscreen-guard5-v650-focused/20260513-204849-3467432/summary.json`. The original pack and source were restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both guard settings reproduced the same clipped-pack failure: scene `1782`, active loop/target `1490/1427`, overrun `63`, blocking `88`, hidden refill/prefetch overrun `27`, loop reads `64`, and due misses `13`. Guard4 kept `foregroundPilotPlay` size flat and shrank `fgRuntimeTryPrefetchWindow`; guard5 restored the original hot size, but neither changed the bad scheduling path. | Close scalar slack-guard rescue for W1 low offscreen clipping. The unsafe slots are not controlled by the simple window-prefetch threshold; any future clipped/visual-work variant needs generated ownership based on observed scheduler events, not a broader hand threshold. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `walkstuf1-low-f0-offscreen-v651` | Log-only; pack restored and PS1 rebuilt. | Narrow the failed W1 low offscreen lane to the single largest and most suspicious frame. Frame `0` is the current max elapsed frame and carried `9234` offscreen PAL4 draw pixels, so this tested whether a one-frame clip could reduce work without opening the broad transform's refill cliff. | Rewrote only frame `0` in `WALK1LOW.FG2`, preserving its payload offset and `6411` byte entry size while removing `9234` offscreen pixels, dropping `142` spans, and reducing logical bytes by `5310`. Host summary: `scratch/walkstuf1-low-f0-offscreen-v651/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-f0-offscreen-v651-focused/20260513-205152-3485651/summary.json`. The original pack was restored from `scratch/walkstuf1-low-f0-offscreen-v651/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The one-frame clip still reproduced the same phase cliff: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1427`, overrun `47 -> 63`, blocking `64 -> 89`, hidden refill/prefetch overrun `20 -> 28`, loop reads `62 -> 64`, and due misses `11 -> 13`. | Close frame-0-only offscreen clipping for W1 low. The max-elapsed draw work is entangled with initial CD/window phase; reducing it in isolation makes the active loop worse. Future W1 low visual-work work needs a generated phase model, not frame-local clipping. Public rollup and methodology totals remain at v629. |
| 2026-05-13 | `building4-low-offscreen-drawclip-v652` | `ps1: clip building4 low offscreen draw spans` | After the W1 offscreen attempts proved broad clipping can be phase-negative, test the same pack-only transform on BUILDING4 low, whose remaining debt is small and whose preprocess matrix still showed high upload/restore pressure. Acceptance required a same-commit baseline, fixed pack LBA, fixed PS-EXE bucket, and strict speed/CD improvement. | Rewrote only logical PAL4 draw streams in `BUIL4LOW.FG2`, preserving every entry offset/size, file byte count, and LBA by zero-filling clipped tails. The transform changed `30` frames, removed `65111` offscreen draw pixels, dropped `11871` spans and `471` draw rows, and reduced logical draw bytes by `61047`. Host summary: `scratch/building4-low-offscreen-drawclip-v652/summary.json`; same-commit baseline: `scratch/ps1-perf-iterate/building4-low-current-v652-baseline/20260513-205600-3510572/summary.json`; focused acceptance: `scratch/ps1-perf-iterate/building4-low-offscreen-drawclip-v652-samecommit/20260513-210327-3554857/summary.json`. | Promoted. BUILDING4 low improves scene `3131 -> 3128`, active loop/target `2856/2816 -> 2853/2816`, overrun `40 -> 37`, blocking `44 -> 40`, prefetch overrun `37 -> 34`, and loop-read VBlanks `223 -> 215`; loop reads and due misses stay `30` and `1`. Pack bytes/sectors stay `1714154`/`837`, pack LBA stays `9118`, and the PS-EXE bucket stays `217088`. | Keep v652 as the new BUILDING4 low baseline. Public rollup moves to `+0.2736%` over target / `99.7310%` target speed; raw signed rollup moves to `-0.4936%` / `100.5133%`. This adds about `0.0008` public over-target points removed and `0.0009` public target-speed points gained; methodology totals are about `17.13` public over-target points removed and `12.63` target-speed points gained since the compact full-matrix baseline. BUILDING4 low remains yellow at `98.70%`, but the remaining gap is now `37` VBlanks. |
| 2026-05-13 | `walkstuf1-low-late-offscreen-v653` | `ps1: clip walkstuf1 low late offscreen draw spans` | After broad W1 low offscreen clipping and frame-0-only clipping regressed scheduler phase, narrow the lane to the late offscreen tail frames `202..210` so the initial/max-elapsed phase stays untouched. Acceptance treats this as work-volume only: no timing/CD regression, fixed layout, and measurable frame/upload reduction. | Rewrote only `WALK1LOW.FG2` logical PAL4 draw streams for frames `202..210`, preserving every entry offset/size and file size by zero-filling clipped tails. Transform summary: `scratch/walkstuf1-low-late-offscreen-v653/summary.json`; baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v653-baseline/20260513-211628-3631092/summary.json`; strict require-improvement artifact: `scratch/ps1-perf-iterate/walkstuf1-low-late-offscreen-v653-focused/20260513-211813-3641762/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-late-offscreen-v653-workreduction/20260513-211936-3650582/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: draw pixels `824527 -> 785455`, spans `139288 -> 135025`, rows `17611 -> 17298`, dirty rows `27900 -> 27821`, upload rects `667 -> 666`, upload bytes `17856000 -> 17805440`. | Keep v653 as the W1 low visual-work baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`. Next W1 low speed work must target the `285..321` read cluster with generated ownership or pack/data-shape changes; the frame `202..210` late-tail clip is closed as safe work-volume cleanup. |
| 2026-05-13 | `walkstuf1-high-late-offscreen-v654` | `ps1: clip walkstuf1 high late offscreen draw spans` | Apply the safe part of the W1 low late-tail lesson to high tide before returning to generated-deadline work. The high pack carried a larger late-tail offscreen signal in frames `194..210`, and the candidate could be evaluated with a same-commit baseline without touching source or layout. | Rewrote only `WALKSTUF1.FG2` logical PAL4 draw streams for frames `194..210`, preserving every entry offset/size and file size by zero-filling clipped tails. Transform summary: `scratch/walkstuf1-high-late-offscreen-v654/summary.json`; same-commit baseline: `scratch/ps1-perf-iterate/walkstuf1-high-current-v654-baseline/20260513-212859-3706012/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-late-offscreen-v654-focused/20260513-213014-3714154/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-late-offscreen-v654-workreduction/20260513-213134-3722627/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `65/282`, and due misses `16`; pack LBA/sectors stay `24883/750`, and the PS-EXE bucket stays `217088`. Work drops: draw pixels `824527 -> 776856`, spans `139288 -> 134136`, draw rows `17611 -> 17296`, and logical draw bytes drop by `36334`. | Keep v654 as the W1 high visual-work baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`. The result confirms late-tail offscreen clipping is safe for W1 work-volume cleanup but does not solve the remaining W1 speed gap; next speed work still needs generated deadline ownership or a custom data/upload representation. |
| 2026-05-13 | `building2-high-offscreen-drawclip-v655` | Log-only; pack restored and PS1 rebuilt. | Test whether the successful BUILDING4 low offscreen draw-span lane transfers to the second-largest under-99 row. A host scan of `BUILDING2.FG2` found `42` frames with offscreen PAL4 draw work, totaling `132298` pixels, `27787` spans, `1769` draw rows, and `134358` logical draw bytes outside the viewport. | First rewrote all offscreen draw streams in `BUILDING2.FG2`, preserving every entry offset/size and file size by zero-filling clipped tails. Host summary: `scratch/building2-high-offscreen-drawclip-v655/summary.json`; same-commit baseline: `scratch/ps1-perf-iterate/building2-high-current-v655-baseline/20260513-213726-3757285/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-offscreen-drawclip-v655-focused/20260513-213841-3765462/summary.json`. Then restored the pack and tested the hot `72..92` split, which removed `101870` pixels, `22113` spans, `1334` rows, and `105110` logical draw bytes; split summary: `scratch/building2-high-offscreen-drawclip-v655/summary-hot72-92.json`; split focused artifact: `scratch/ps1-perf-iterate/building2-high-offscreen-hot72-92-v655-focused/20260513-214008-3774873/summary.json`. | Failed/no promotion. Both broad and hot-split clips reproduced the same regression against the same-commit baseline: scene `1602 -> 1605`, active loop/target `1351/1311 -> 1354/1309`, overrun `40 -> 45`, blocking/refill `54/18 -> 57/21`, while loop reads/due stayed `58/7`. Pack LBA stayed `6181` and the PS-EXE bucket stayed `217088`. | Close BUILDING2 high offscreen clipping as a speed lane. The work reduction is real but phase-negative in the same way as broad W1 low clipping; unlike BUILDING4 low, this row needs generated deadline ownership, sector-boundary byte removal, or precomposed upload/restore work that does not shorten visual work into worse CD/refill cadence. Public rollup and methodology totals remain at v654. |
| 2026-05-13 | `walkstuf1-low-tailcompact-v656` | Log-only; pack restored and PS1 rebuilt. | Test whether physically compacting the already-clipped W1 low late-tail payloads would convert the safe v653 work-volume win into a CD timing win. The supported tail compactor reduced active payload `916139 -> 886102` while preserving the `1535263` byte pack footprint and fixed LBA/sectors. | Applied `compact-fgp3-trim-draw-tails.py --copy-unparseable --pad-to-input-size` to `WALK1LOW.FG2` and gated focused low tide. Transform note: `scratch/walkstuf1-low-tailcompact-v656/summary.txt`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-tailcompact-v656-focused/20260513-214710-3817129/summary.json`. The original pack was restored from `scratch/walkstuf1-low-tailcompact-v656/WALK1LOW.before.FG2`. | Failed/no promotion. The physical compaction saved one loop read (`62 -> 61`) but was phase-negative: scene `1770 -> 1771`, active loop/target `1478/1431 -> 1479/1430`, overrun `47 -> 49`, blocking/refill `64/20 -> 66/22`, loop-read time `281 -> 276`, and due misses stayed `11`. Pack LBA/sectors stayed `25633/750`, and the PS-EXE bucket stayed `217088`. | Close W1 low physical tail compaction as a direct speed lane. The byte saving is real, but shortening the low tail changes the window cadence enough to lose target-relative timing; future W1 low work needs generated deadline ownership or a data shape that does not open worse refill slots. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-high-tailcompact-v657` | `ps1: compact walkstuf1 high late-tail payloads` | Apply the same physical compaction to the W1 high side, where the v654 clipped tails left more removable zero padding and the high read cadence had already tolerated same-speed CD-work reductions. Acceptance required exact-flat key timing against the v654 accepted baseline plus material CD/work reduction with fixed pack LBA and PS-EXE bucket. | Applied `compact-fgp3-trim-draw-tails.py --copy-unparseable --pad-to-input-size` to `WALKSTUF1.FG2`. Transform note: `scratch/walkstuf1-high-tailcompact-v657/summary.txt`; first work gate: `scratch/ps1-perf-iterate/walkstuf1-high-tailcompact-v657-workreduction/20260513-215007-3835890/summary.json`; final v654-baseline gate: `scratch/ps1-perf-iterate/walkstuf1-high-tailcompact-v657-v654base/20260513-215233-3850859/summary.json`. | Promoted as same-speed CD/work reduction. Timing stays exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, and due misses `16`. Active payload drops `918345 -> 882007`, total CD sectors `605 -> 586`, total reads `78 -> 76`, loop reads/read VBlanks `65/282 -> 63/275`, and scratch bytes `1239040 -> 1200128`; pack bytes/LBA/sectors stay `1535263/24883/750`, and the PS-EXE bucket stays `217088`. | Keep v657 as the W1 high work baseline but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-tail210compact-v658` | Log-only; pack restored and PS1 rebuilt. | After full W1 low late-tail physical compaction regressed, isolate the safest suffix byte saving by compacting only frame `210`. The goal was to see whether a tiny active-payload cut could become a same-speed CD/work promotion without changing the tight low-tide scheduler phase. | Repacked only frame `210` from the accepted v653 low pack, preserving pack file size, LBA, and unselected frame payloads. Host summary: `scratch/walkstuf1-low-tail210compact-v658/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-tail210compact-v658-focused/20260513-220100-3901279/summary.json`. The original pack was restored from `scratch/walkstuf1-low-tail210compact-v658/WALK1LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate saved only `3225` active payload bytes and all key timing/CD metrics stayed exact-flat: scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads `62`, and due misses `11`. No accepted key work metric improved enough to justify a same-speed promotion. | Close frame-210-only physical compaction as inert. It proves the suffix byte cut can be harmless, but it is too small to move observed reads, read VBlanks, or visible speed. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-tail209210compact-v659` | Log-only; pack restored and PS1 rebuilt. | Broaden the inert frame-210 suffix compaction by one frame to test whether frames `209..210` could cross a CD-read boundary while staying smaller than the failed full `202..210` compaction. This was the last physical-tail subset before closing the W1 low direct-compaction lane. | Repacked only frames `209` and `210` from the accepted v653 low pack, preserving pack file size, LBA, and unselected frame payloads. Host summary: `scratch/walkstuf1-low-tail209210compact-v659/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-tail209210compact-v659-focused/20260513-220354-3918765/summary.json`. The original pack was restored from `scratch/walkstuf1-low-tail210compact-v658/WALK1LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The subset saved `6929` active payload bytes but regressed scheduler phase: scene `1770 -> 1772`, active loop/target `1478/1431 -> 1480/1430`, overrun `47 -> 50`, blocking/refill `64/20 -> 67/23`, loop reads stayed `62`, and due misses stayed `11`. | Close selective W1 low physical tail compaction after the full, one-frame, and two-frame probes. The threshold is unfavorable: frame `210` is harmless but inert, while adding frame `209` reintroduces the same phase-negative behavior as the full compaction. Future W1 low speed work should switch to generated deadline ownership, row-template/data-shape compression that preserves scheduler phase, or upload/restore suppression rather than more direct late-tail shortening. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `building2-low-offscreen-drawclip-v660` | `ps1: clip building2 low offscreen draw spans` | After BUILDING2 high offscreen clipping proved phase-negative, test the same in-place draw-span transform on low tide, whose v626 baseline has zero hidden refill and a different retained-read cadence. Acceptance treated this as a same-speed work-volume lane: fixed pack footprint/LBA/PS-EXE bucket, exact-flat timing/CD, and a material measured work-counter drop. | Rewrote only logical draw streams in `BUIL2LOW.FG2`, preserving every entry offset/size and the `1303332` byte pack footprint by zero-filling clipped tails. The host transform changed `40` frames, removed `120179` offscreen draw pixels, dropped `25136` spans and `1537` draw rows, and reduced logical draw bytes by `121686`. Host summary: `scratch/building2-low-offscreen-drawclip-v660/summary.json`; strict speed gate: `scratch/ps1-perf-iterate/building2-low-offscreen-drawclip-v660-focused/20260513-221137-3964879/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-low-offscreen-drawclip-v660-workreduction/20260513-221318-3975261/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1614`, active loop/target `1344/1318`, overrun `26`, blocking/refill `61/0`, loop reads/read VBlanks `50/218`, and due misses `14`; pack LBA/sectors stay `6818/637`, and the PS-EXE bucket stays `217088`. Work drops: runtime frame rows `17912 -> 16375`; host logical draw pixels/spans/rows drop by `120179`/`25136`/`1537`. | Keep v660 as the BUILDING2 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Remaining BUILDING2 low speed work must preserve the v626/v660 zero-refill cadence while changing generated ownership, upload/restore work, or a sector boundary. |
| 2026-05-13 | `walkstuf1-low-rg285-297-v661` | Log-only; source restored and PS1 rebuilt. | Test the read-plan's biggest W1 low retained-window swing after direct tail compaction closed: split WALKSTUF1 read groups by tide and add a low-only `{285,297}` group while preserving the accepted shared groups for high tide. | Added the low-only runtime group in `src/foreground_pilot.c`, built successfully with the PS-EXE bucket still `217088`, and gated against the accepted v653 W1 low baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg285-297-v661-focused/20260513-222243-4031089/summary.json`. The source was restored to the shared-table baseline and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate saved one loop read (`62 -> 61`) but regressed visible timing and refill: scene `1770 -> 1778`, active loop/target `1478/1431 -> 1486/1431`, overrun `47 -> 55`, blocking `64 -> 75`, prefetch overrun `20 -> 26`, and due misses stayed `11`. | Close `{285,297}` as too aggressive despite the modeled two-read upside. The post-run plan now ranks `{190,202}` and `{273,285}` as lower-visible-risk scheduler-owned candidates; test those next before returning to generated payload/data-shape work. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-rg190-202-v662` | Log-only; source restored and PS1 rebuilt. | Test the lower-visible-risk W1 low retained-window group suggested after v661. The `{190,202}` candidate had a wider internal modeled gap than `{285,297}` and could be isolated with the same low-only read-group table split. | Added `{190,202}` to a low-only WALKSTUF1 group table while preserving the accepted shared groups and high-tide table, built successfully with the PS-EXE bucket still `217088`, and gated against the accepted v653 W1 low baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg190-202-v662-focused/20260513-222541-4049629/summary.json`. The source was restored to the shared-table baseline and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. It did not save a loop read (`62 -> 62`) and regressed hard: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1428`, overrun `47 -> 62`, blocking `64 -> 96`, prefetch overrun `20 -> 27`, and due misses `11 -> 15`. | Close `{190,202}`. The attempted overlap with existing `{201,213}` did not create useful residency and instead pushed the loop into a worse due/refill phase. Next test should use the post-run `{209,225}` low-risk candidate or switch to non-read-group data-shape work. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-rg209-225-v663` | Log-only; source restored and PS1 rebuilt. | Test the post-v662 read-plan's wider-gap `{209,225}` candidate before closing the hand-authored W1 low read-group lane. This candidate had modeled two-read upside with a much larger first gap than the failed `{190,202}` and `{285,297}` groups. | Added `{209,225}` to a low-only WALKSTUF1 group table while preserving the accepted shared groups and high-tide table, built successfully with the PS-EXE bucket still `217088`, and gated against the accepted v653 W1 low baseline. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg209-225-v663-focused/20260513-222810-4065207/summary.json`. The source was restored to the shared-table baseline and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. It saved one loop read (`62 -> 61`) but regressed cadence: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1431`, overrun `47 -> 59`, blocking `64 -> 93`, prefetch overrun `20 -> 30`, and due misses `11 -> 14`. | Close the low-only read-group retry lane after `{285,297}`, `{190,202}`, and `{209,225}`. Every candidate either failed to remove a read or removed one by opening worse visible/refill debt. Future W1 low work should pivot to pack/data-shape or upload/restore suppression that preserves current scheduler phase. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `building2-high-late-offscreen-v664` | `ps1: clip building2 high late offscreen spans` | Salvage the safe part of the failed B2-high offscreen lane. Broad and hot `72..92` clipping regressed phase, so this candidate clipped only late frames `168..177`, where the draw work is offscreen but no longer controls the early CD/refill cadence. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2`, preserving every entry offset/size and the `1303332` byte pack footprint by zero-filling clipped tails. Host summary: `scratch/building2-high-late-offscreen-v664/summary.json`; strict speed gate: `scratch/ps1-perf-iterate/building2-high-late-offscreen-v664-focused/20260513-223241-4092850/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-late-offscreen-v664-workreduction/20260513-223348-4100046/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due misses `7`; pack LBA/sectors stay `6181/637`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4375`/`607`/`45`, and runtime frame rows/spans/pixels drop `18144/110717/468636 -> 18099/110110/464261`. | Keep v664 as the BUILDING2 high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Remaining BUILDING2 high speed work needs generated deadline ownership, sector-boundary byte removal, or precomposed upload/restore work. |
| 2026-05-13 | `walkstuf1-low-mid-offscreen-v665` | `ps1: clip walkstuf1 low mid offscreen spans` | Extend the safe-subset offscreen lesson to the worst remaining row without touching the known-bad early frame `0` or the broad W1 low clip path. The candidate clips only the isolated mid offscreen cluster `133..139` on top of the accepted v653 late-tail clip. | Rewrote only logical draw streams in `generated/ps1/foreground/WALK1LOW.FG2`, preserving every entry offset/size and the `1535263` byte pack footprint by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-mid-offscreen-v665/summary.json`; strict speed gate: `scratch/ps1-perf-iterate/walkstuf1-low-mid-offscreen-v665-focused/20260513-224515-4167677/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-mid-offscreen-v665-workreduction/20260513-224626-4175178/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4898`/`251`/`6`, and runtime frame rows/spans/pixels drop `17298/135025/785455 -> 17292/134774/780557`. | Keep v665 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Next W1 low speed work still needs generated deadline ownership, source-neutral relocation, or upload/restore suppression. |
| 2026-05-13 | `walkstuf1-low-left-offscreen-v666` | `ps1: clip walkstuf1 low left offscreen spans` | Continue the isolated-subset W1 low offscreen lane with the larger left-side cluster `43..57`, still avoiding the known-bad frame `0` and broad all-frame clipping. | Rewrote only logical draw streams in `generated/ps1/foreground/WALK1LOW.FG2`, preserving every entry offset/size and the `1535263` byte pack footprint by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-left-offscreen-v666/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-left-offscreen-v666-focused/20260513-225322-22670/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `43186`/`4137`/`454`, and runtime frame rows/spans/pixels drop `17292/134774/780557 -> 16838/130637/737371`. | Keep v666 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. This proves isolated offscreen clusters can be safely stripped, while early frame `0` and broad clipping remain closed as phase-negative. |
| 2026-05-13 | `walkstuf1-low-left2-offscreen-v667` | Log-only; pack restored and PS1 rebuilt. | Test whether the adjacent remaining offscreen band `58..74` could extend the safe v666 left-side cleanup. The host signal was large, but this band sits closer to the W1 low CD/refill cliff. | Rewrote only frames `58..74` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v666, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-left2-offscreen-v667/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-left2-offscreen-v667-focused/20260513-225819-52423/summary.json`. The pack was restored from `scratch/walkstuf1-low-left2-offscreen-v667/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Despite removing `48096` pixels, `8216` spans, and `417` rows host-side, the timing regressed into the known broad-clip phase cliff: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1427`, overrun `47 -> 63`, blocking `64 -> 97`, refill `20 -> 28`, loop reads `62 -> 63`, and due misses `11 -> 15`. | Close W1 low frames `58..74` offscreen clipping under the current scheduler. The safe window is discontinuous: `43..57`, `133..139`, and `202..210` are safe work-volume strips, but `58..74`, frame `0`, and broad clipping are phase-negative. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-left0-offscreen-v668` | `ps1: clip walkstuf1 low pre-left offscreen spans` | After v667 proved the post-v666 adjacent band `58..74` is unsafe, test the independent pre-left cluster `30..41` before the accepted `43..57` band. | Rewrote only frames `30..41` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v666, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-left0-offscreen-v668/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-left0-offscreen-v668-focused/20260513-230051-69352/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `25047`/`2687`/`158`, and runtime frame rows/spans/pixels drop `16838/130637/737371 -> 16680/127950/712324`. | Keep v668 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-pretail-offscreen-v669` | `ps1: clip walkstuf1 low pre-tail offscreen spans` | Continue the discontinuous safe-subset W1 low offscreen lane by testing the independent pre-tail cluster `194..201`, adjacent to the already accepted `202..210` tail clip but outside the failed `58..74` phase cliff. | Rewrote only frames `194..201` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v668, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-pretail-offscreen-v669/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-pretail-offscreen-v669-focused/20260513-230802-112406/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `8599`/`889`/`2`, and runtime frame rows/spans/pixels drop `16680/127950/712324 -> 16678/127061/703725`. | Keep v669 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-midright-offscreen-v670` | Log-only; pack restored and PS1 rebuilt. | Test the largest remaining post-v669 offscreen cluster outside the known-bad frame `0` and `58..74` lanes by clipping frames `85..101` as one broad mid-right subset. | Rewrote only frames `85..101` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v669, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-offscreen-v670/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-midright-offscreen-v670-focused/20260513-231624-162159/summary.json`. The pack was restored from `scratch/walkstuf1-low-midright-offscreen-v670/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate removed `2966` draw pixels and `980` spans, but the strict gate caught visible CD blocking regression: scene/loop stayed `1770/1478`, target moved `1431 -> 1432`, overrun improved `47 -> 46`, but blocking regressed `64 -> 65`; refill stayed `20`, reads stayed `62`, and due misses stayed `11`. | Close the broad `85..101` clip as a unit under the current scheduler. The signal is close enough to split into smaller windows, but do not promote the full mid-right band. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-midright-a-offscreen-v671` | Log-only; pack restored and PS1 rebuilt. | Bisect the v670 broad mid-right miss by testing only frames `85..92`, the larger lower half of the `85..101` offscreen cluster. | Rewrote only frames `85..92` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v669, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-a-offscreen-v671/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-midright-a-offscreen-v671-focused/20260513-231855-178134/summary.json`. The pack was restored from `scratch/walkstuf1-low-midright-a-offscreen-v671/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate removed `1702` draw pixels and `595` spans, but reproduced the v670 blocking regression: scene/loop stayed `1770/1478`, target moved `1431 -> 1432`, overrun improved `47 -> 46`, blocking regressed `64 -> 65`, refill stayed `20`, reads stayed `62`, and due misses stayed `11`. | Close frames `85..92` as a direct clip under the current scheduler. The remaining split should test `93..101`; if that also regresses, the mid-right cluster needs a different data-shape approach. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-midright-b-offscreen-v672` | `ps1: clip walkstuf1 low upper midright spans` | Finish the v670/v671 binary split by testing only frames `93..101`, after the full `85..101` band and the lower `85..92` split both regressed visible CD blocking. | Rewrote only frames `93..101` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v669, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-b-offscreen-v672/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-midright-b-offscreen-v672-focused/20260513-232104-191822/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `1264`/`385`/`0`, and runtime frame rows/spans/pixels drop `16678/127061/703725 -> 16678/126676/702461`. | Keep v672 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-preleft2-offscreen-v673` | `ps1: clip walkstuf1 low pre-left edge spans` | Test the largest remaining non-closed W1 low offscreen cluster after v672: frames `27..28`, immediately before the accepted v668 `30..41` pre-left slice. | Rewrote only frames `27..28` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v672, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-preleft2-offscreen-v673/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-preleft2-offscreen-v673-focused/20260513-232526-218276/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `1668`/`122`/`0`, and runtime frame rows/spans/pixels drop `16678/126676/702461 -> 16678/126554/700793`. | Keep v673 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-postleft-offscreen-v674` | `ps1: clip walkstuf1 low post-left spans` | Test the next largest W1 low offscreen cluster after v673: frames `75..77`, directly after the rejected `58..74` phase cliff but small enough to isolate. | Rewrote only frames `75..77` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v673, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-postleft-offscreen-v674/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-postleft-offscreen-v674-focused/20260513-232928-242735/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `1879`/`628`/`0`, and runtime frame rows/spans/pixels drop `16678/126554/700793 -> 16678/125926/698914`. | Keep v674 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-left2b-offscreen-v675` | `ps1: clip walkstuf1 low late-left2 spans` | Reopen the old rejected `58..74` left2 band as a split after `75..77` proved exact-flat. Test only frames `66..74`, leaving the worst early `58..65` section untouched. | Rewrote only frames `66..74` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v674, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-left2b-offscreen-v675/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-left2b-offscreen-v675-focused/20260513-234453-332081/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `15524`/`3364`/`11`, and runtime frame rows/spans/pixels drop `16678/125926/698914 -> 16667/122562/683390`. | Keep v675 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. This proves `58..74` was phase-negative because of its earlier frames, not because every frame in that band is unsafe. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-13 | `walkstuf1-low-left2c-offscreen-v676` | Log-only; pack restored and PS1 rebuilt. | Split the remaining early-left2 `58..65` band after v675 proved the adjacent `66..74` section safe. Test the later, less destructive `62..65` subset before touching the near-empty `58..61` frames. | Rewrote only frames `62..65` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v675, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-left2c-offscreen-v676/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-left2c-offscreen-v676-focused/20260513-235357-385575/summary.json`. The pack was restored from `scratch/walkstuf1-low-left2c-offscreen-v676/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate removed `15288` draw pixels, `2277` spans, and `103` rows, but reproduced the old left2 phase cliff: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1427`, overrun `47 -> 63`, blocking `64 -> 97`, refill `20 -> 28`, loop reads `62 -> 63`, and due misses `11 -> 15`; pack LBA/sectors and the PS-EXE bucket stayed fixed. | Do not promote `62..65` as a direct offscreen clip. Any retry in the remaining `58..65` band needs a narrower one/two-frame split or a non-shrinking data-shape/hole-fill approach that avoids the cadence cliff. Public rollup and methodology totals remain unchanged. |
| 2026-05-13 | `walkstuf1-low-left2d-offscreen-v677` | Log-only; pack restored and PS1 rebuilt. | Bisect the v676 failure toward the proven-safe `66..74` boundary by testing only frames `64..65`. | Rewrote frames `64..65` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v675, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-left2d-offscreen-v677/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-left2d-offscreen-v677-focused/20260513-235720-406550/summary.json`. The pack was restored from `scratch/walkstuf1-low-left2d-offscreen-v677/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate removed `6795` draw pixels, `1054` spans, and `15` rows, but still reproduced the full left2 cliff: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1427`, overrun `47 -> 63`, blocking `64 -> 97`, refill `20 -> 28`, loop reads `62 -> 63`, and due misses `11 -> 15`; pack LBA/sectors and the PS-EXE bucket stayed fixed. | Do not promote `64..65` as a direct offscreen clip. Since even this small adjacent split flips the same phase, the next binary test should isolate frame `65` only before closing the direct boundary entirely. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-left2e-offscreen-v678` | `ps1: clip walkstuf1 low frame65 spans` | Run the single-frame boundary test after `62..65` and `64..65` both failed with the same phase cliff. Frame `65` is adjacent to the safe `66..74` slice but leaves frame `64` untouched. | Rewrote only frame `65` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v675, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-left2e-offscreen-v678/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-left2e-offscreen-v678-focused/20260514-000011-424791/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `3439`/`527`/`4`, and runtime frame rows/spans/pixels drop `16667/122562/683390 -> 16663/122035/679951`. | Keep v678 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Frame `65` is safe; frame `64` remains the current direct-clip boundary trigger. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-left2f-offscreen-v679` | Log-only; pack restored and PS1 rebuilt. | Confirm the v678 boundary by testing frame `64` alone on top of the accepted frame `65` clip. | Rewrote only frame `64` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v678, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-left2f-offscreen-v679/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-left2f-offscreen-v679-focused/20260514-000624-461941/summary.json`. The pack was restored from `scratch/walkstuf1-low-left2f-offscreen-v679/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The single frame removed `3356` draw pixels, `527` spans, and `11` rows, but still reproduced the left2 cliff: scene `1770 -> 1782`, active loop/target `1478/1431 -> 1490/1427`, overrun `47 -> 63`, blocking `64 -> 97`, refill `20 -> 28`, loop reads `62 -> 63`, and due misses `11 -> 15`; pack LBA/sectors and the PS-EXE bucket stayed fixed. | Close frame `64` as a direct offscreen clip. The direct boundary is now proven: frame `65` is safe, frame `64` is not. Remaining `58..63` should not be direct-clipped until a non-shrinking/hole-fill or scheduler-safe approach is available. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-postleft-singletons-v680` | `ps1: clip walkstuf1 low post-left singletons` | Move out of the closed `58..64` phase cliff and test the isolated post-left singleton candidates `79`, `81`, and `83` as one small bundle. | Rewrote frames `79`, `81`, and `83` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v678, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-postleft-singletons-v680/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-postleft-singletons-v680-focused/20260514-000915-479813/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `1538`/`540`/`0`, and runtime frame rows/spans/pixels drop `16663/122035/679951 -> 16663/121495/678413`. | Keep v680 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-midright-aa-offscreen-v681` | Log-only; pack restored and PS1 rebuilt. | Reopen the rejected `85..92` mid-right range as a split after v680 moved out of the left2 cliff. Test the larger lower half `85..88` first. | Rewrote frames `85..88` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v680, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-aa-offscreen-v681/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-midright-aa-offscreen-v681-focused/20260514-001427-511034/summary.json`. The pack was restored from `scratch/walkstuf1-low-midright-aa-offscreen-v681/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate removed `1133` draw pixels and `388` spans, but reproduced the mid-right blocking regression: scene/loop stayed `1770/1478`, target moved `1431 -> 1432`, overrun improved `47 -> 46`, blocking regressed `64 -> 65`, refill stayed `20`, loop reads stayed `62`, and due misses stayed `11`; pack LBA/sectors and the PS-EXE bucket stayed fixed. | Do not promote `85..88` as a direct bundle. Split again before closing individual frames; this is a small cadence/blocking cliff, not the severe `58..64` phase cliff. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-midright-ab-offscreen-v682` | Log-only; pack restored and PS1 rebuilt. | Split the v681 `85..88` miss by testing the larger lower pair `85..86`. | Rewrote frames `85..86` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v680, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-ab-offscreen-v682/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-midright-ab-offscreen-v682-focused/20260514-001706-527736/summary.json`. The pack was restored from `scratch/walkstuf1-low-midright-ab-offscreen-v682/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The pair removed `707` draw pixels and `234` spans, but reproduced the same small mid-right blocking regression: scene/loop stayed `1770/1478`, target moved `1431 -> 1432`, overrun improved `47 -> 46`, blocking regressed `64 -> 65`, refill stayed `20`, loop reads stayed `62`, and due misses stayed `11`; pack LBA/sectors and the PS-EXE bucket stayed fixed. | Do not promote `85..86` as a direct pair. Isolate frame `86` or `85` next before closing the lower mid-right frames. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-midright-ac-offscreen-v683` | Log-only; pack restored and PS1 rebuilt. | Isolate frame `86` after the `85..86` pair repeated the mid-right blocking regression. | Rewrote only frame `86` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v680, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-midright-ac-offscreen-v683/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-midright-ac-offscreen-v683-focused/20260514-001951-545044/summary.json`. The pack was restored from `scratch/walkstuf1-low-midright-ac-offscreen-v683/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The single frame removed `319` draw pixels and `105` spans, but still regressed blocking: scene/loop stayed `1770/1478`, target moved `1431 -> 1432`, overrun improved `47 -> 46`, blocking regressed `64 -> 65`, refill stayed `20`, loop reads stayed `62`, and due misses stayed `11`; pack LBA/sectors and the PS-EXE bucket stayed fixed. | Close frame `86` as a direct clip. Frame `85` is likely not worth an isolated direct clip unless all safer W1-low candidates are exhausted; test `87..88` or smaller non-risk candidates next. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-midright-ad-offscreen-v684` | `ps1: clip walkstuf1 low midright-ad spans` | Test the upper half `87..88` after `85..88`, `85..86`, and frame `86` all reproduced the small mid-right blocking cliff. | Rewrote frames `87..88` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v680, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-ad-offscreen-v684/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-midright-ad-offscreen-v684-focused/20260514-002232-561898/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `426`/`154`/`0`, and runtime frame rows/spans/pixels drop `16663/121495/678413 -> 16663/121341/677987`. | Keep v684 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The mid-right lower trigger is at or before frame `86`; `87..88` is safe. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-midright-ae-offscreen-v685` | `ps1: clip walkstuf1 low midright-ae spans` | Continue the safe upper-midright split by testing frames `89..92` after `87..88` passed and frame `86` was closed. | Rewrote frames `89..92` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v684, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-midright-ae-offscreen-v685/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-midright-ae-offscreen-v685-focused/20260514-002818-596528/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `569`/`207`/`0`, and runtime frame rows/spans/pixels drop `16663/121341/677987 -> 16663/121134/677418`. | Keep v685 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The safe mid-right range is now `87..92`; frame `86` remains closed. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-midright-af-offscreen-v686` | `ps1: clip walkstuf1 low frame85 spans` | Isolate frame `85` after frame `86` failed but the upper mid-right `87..92` range passed. | Rewrote only frame `85` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v685, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-midright-af-offscreen-v686/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-midright-af-offscreen-v686-focused/20260514-003341-628648/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `388`/`129`/`0`, and runtime frame rows/spans/pixels drop `16663/121134/677418 -> 16663/121005/677030`. | Keep v686 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The mid-right result is now precise: frame `86` is toxic, while `85` and `87..92` are safe. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame1-offscreen-v687` | `ps1: clip walkstuf1 low frame1 spans` | Test frame `1` after frame `0` was already closed and the mid-right split was fully mapped; this was the largest remaining non-closed singleton before moving to smaller work-volume scraps. | Rewrote only frame `1` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v686, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame1-offscreen-v687/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame1-offscreen-v687-focused/20260514-003837-658365/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `269`/`118`/`0`, and runtime frame rows/spans/pixels drop `16663/121005/677030 -> 16663/120887/676761`. | Keep v687 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Frame `0` and frame `86` remain closed for direct clipping; frame `1` is safe. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-postmid106-112-offscreen-v688` | `ps1: clip walkstuf1 low post-mid spans` | Test the next largest remaining non-closed W1-low direct-clip range after frame `1`: frames `106..112`, outside the toxic frame `0`, `58..64`, and `86` neighborhoods. | Rewrote frames `106..112` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v687, preserving pack size/LBA by zero-filling clipped tails. Host summary: `scratch/walkstuf1-low-postmid106-112-offscreen-v688/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-postmid106-112-offscreen-v688-focused/20260514-004920-722046/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `218`/`104`/`0`, and runtime frame rows/spans/pixels drop `16663/120887/676761 -> 16663/120783/676543`. | Keep v688 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame3-offscreen-v689` | `ps1: clip walkstuf1 low frame3 spans` | Test frame `3`, the largest remaining non-closed singleton after v688; frame `0` stays closed, while frame `1` and the `106..112` post-mid range are now safe. | Rewrote only frame `3` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v688, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame3-offscreen-v689/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame3-offscreen-v689-focused/20260514-005552-761011/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `93`/`45`/`0`, and runtime frame rows/spans/pixels drop `16663/120783/676543 -> 16663/120738/676450`. | Keep v689 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame140-offscreen-v690` | `ps1: clip walkstuf1 low frame140 spans` | Test frame `140`, the last meaningful non-closed direct W1-low offscreen singleton in the current queue before moving to scheduler/data-shape work or other under-99 rows. | Rewrote only frame `140` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v689, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame140-offscreen-v690/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame140-offscreen-v690-focused/20260514-010124-795396/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `50`/`28`/`0`, and runtime frame rows/spans/pixels drop `16663/120738/676450 -> 16663/120710/676400`. | Keep v690 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. W1-low direct offscreen clipping now has no large non-closed candidates left; next work should use generated scheduler/data-shape changes or move to the next under-99 rows. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame61-offscreen-v691` | `ps1: clip walkstuf1 low frame61 spans` | Reopen the remaining `58..63` left2 direct-clip remainder as a singleton probe after broad `58..74`, `62..65`, `64..65`, and frame `64` failed. Frame `61` was the largest remaining singleton by tail bytes. | Rewrote only frame `61` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v690, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame61-offscreen-v691/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame61-offscreen-v691-focused/20260514-011044-851699/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4495`/`684`/`79`, and runtime frame rows/spans/pixels drop `16663/120710/676400 -> 16584/120026/671905`. | Keep v691 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The `58..63` region is not uniformly toxic; continue with singleton/binary probes there before falling back to generated scheduler/data-shape work. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame60-offscreen-v692` | `ps1: clip walkstuf1 low frame60 spans` | Continue the reopened `58..63` singleton map after frame `61` passed. Frame `60` was the next largest non-closed singleton by tail bytes. | Rewrote only frame `60` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v691, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame60-offscreen-v692/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame60-offscreen-v692-focused/20260514-011817-897176/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4431`/`662`/`80`, and runtime frame rows/spans/pixels drop `16584/120026/671905 -> 16504/119364/667474`. | Keep v692 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The direct singleton safe island now includes frames `60..61`; continue `62`, `59`, `58`, or `63` individually before trying adjacent pairs. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame62-offscreen-v693` | `ps1: clip walkstuf1 low frame62 spans` | Continue the reopened `58..63` singleton map after frames `61` and `60` passed. Frame `62` was the next largest non-closed singleton by tail bytes. | Rewrote only frame `62` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v692, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame62-offscreen-v693/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame62-offscreen-v693-focused/20260514-012351-930734/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4510`/`645`/`58`, and runtime frame rows/spans/pixels drop `16504/119364/667474 -> 16446/118719/662964`. | Keep v693 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The direct singleton safe island now includes frames `60..62`; continue `59`, `58`, or `63` individually before retesting adjacent pairs. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame59-offscreen-v694` | `ps1: clip walkstuf1 low frame59 spans` | Continue the reopened `58..63` singleton map after frames `60..62` passed. Frame `59` was the next largest non-closed singleton by tail bytes. | Rewrote only frame `59` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v693, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame59-offscreen-v694/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame59-offscreen-v694-focused/20260514-013100-973790/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4191`/`633`/`72`, and runtime frame rows/spans/pixels drop `16446/118719/662964 -> 16374/118086/658773`. | Keep v694 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The direct singleton safe island now includes frames `59..62`; continue `58` or `63` individually before retesting adjacent pairs. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame58-offscreen-v695` | `ps1: clip walkstuf1 low frame58 spans` | Continue the reopened `58..63` singleton map after frames `59..62` passed. Frame `58` was the next largest non-closed singleton by tail bytes. | Rewrote only frame `58` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v694, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame58-offscreen-v695/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame58-offscreen-v695-focused/20260514-013617-1005818/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4167`/`596`/`72`, and runtime frame rows/spans/pixels drop `16374/118086/658773 -> 16302/117490/654606`. | Keep v695 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The direct singleton safe island now includes frames `58..62`; continue `63` individually before retesting adjacent pairs. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-frame63-offscreen-v696` | `ps1: clip walkstuf1 low frame63 spans` | Finish the reopened `58..63` singleton map after frames `58..62` all passed. Frame `63` is adjacent to the known direct-clip cliff at frame `64`, so it needed an isolated gate. | Rewrote only frame `63` in `generated/ps1/foreground/WALK1LOW.FG2` on top of v695, preserving pack size/LBA by zero-filling the clipped tail. Host summary: `scratch/walkstuf1-low-frame63-offscreen-v696/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame63-offscreen-v696-focused/20260514-014107-1035355/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `62/281`, and due misses `11`; pack LBA/sectors stay `25633/750`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `3983`/`578`/`30`, and runtime frame rows/spans/pixels drop `16302/117490/654606 -> 16272/116912/650623`. | Keep v696 as the WALKSTUF1 low work-volume baseline, but do not count it as a VBlank speed win. The direct singleton safe island now includes frames `58..63`; frame `64` remains the boundary trigger and should stay closed unless a different algorithm avoids the phase cliff. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. |
| 2026-05-14 | `walkstuf1-low-tailcompact-v697` | log-only after `ba4702221` | Retry the W1-low physical draw-tail compaction after the v696 singleton island, mirroring the accepted high-tide v657 compaction but keeping the file padded to preserve filesystem layout. | Repacked `generated/ps1/foreground/WALK1LOW.FG2` with `scripts/compact-fgp3-trim-draw-tails.py --copy-unparseable --pad-to-input-size`, then gated focused W1 low against v696. The transform preserved file size `1535263`, kept pack LBA and PS-EXE bucket stable, and reduced active payload `916139 -> 773574` with `88` compacted entries and `142565` payload bytes removed. Source pack was restored after the gate failed. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-tailcompact-v697-focused/20260514-014715-1072449/summary.json`. | Rejected. The smaller payload reduced loop reads/read VBlanks `62/281 -> 56/256`, but changed read cadence in the wrong direction: scene/loop regressed `1770/1478 -> 1776/1484`, target fell `1431 -> 1428`, overrun regressed `47 -> 56`, blocking `64 -> 73`, and refill `20 -> 29`; due misses stayed `11`. | Do not promote whole-pack W1-low physical compaction. The rejected v656/v659/v697 pattern is consistent: lower read count is not enough when the new sector cadence steals visible slack. Future compaction retries must be subset-scoped or scheduler-owned, not one full-pack physical rewrite. |
| 2026-05-14 | `building2-high-tail94-104-offscreen-v698` | `ps1: clip building2 high post-hot spans` | Continue the safe-subset salvage after v664 proved late frames `168..177` but broad and hot `72..92` clipping regressed BUILDING2 high phase. The remaining post-hot tail `94..104` was not part of the v655 hot split and could be tested as an in-place work-volume clip. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2` for frames `94..104`, preserving every entry offset/size and the `1303332` byte pack footprint by zero-filling clipped tails. Host summary: `scratch/building2-high-tail94-104-offscreen-v698/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-tail94-104-offscreen-v698-workreduction/20260514-015329-1110483/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due misses `7`; pack LBA/sectors stay `6181/637`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4848`/`1152`/`7`, and runtime frame rows/spans/pixels drop `18099/110110/464261 -> 18092/108958/459413`. | Keep v698 as the BUILDING2 high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` over target / `99.7310%` target speed and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Remaining BUILDING2 high speed work still needs generated deadline ownership, sector-boundary byte removal, or precomposed upload/restore work. |
| 2026-05-14 | `building2-high-early67-71-offscreen-v699` | log-only after `ef81432b2` | Test the untried pre-hot offscreen draw subset after v698 promoted the safe post-hot tail. Frames `67,69..71` were outside the failed v655 `72..92` hot split and removed enough work to justify a binary gate. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2` for frames `67,69..71`, preserving entry offsets/sizes and pack footprint by zero-filling clipped tails. Host summary: `scratch/building2-high-early67-71-offscreen-v699/summary.json`; focused gate: `scratch/ps1-perf-iterate/building2-high-early67-71-offscreen-v699-workreduction/20260514-020234-1165129/summary.json`. The v698 pack was restored afterward and `./scripts/build-ps1.sh` passed. | Rejected. The work reduction was large (`21205` pixels, `3915` spans, `383` rows), but it reproduced the bad early BUILDING2 high phase: scene/loop regressed `1602/1351 -> 1605/1354`, target drifted `1311 -> 1309`, overrun `40 -> 45`, blocking `54 -> 57`, and refill `18 -> 21`; loop reads/due stayed `58/7`. | Close frames `67,69..71` for direct offscreen clipping. Safe B2-high offscreen work currently consists of v664 `168..177` and v698 `94..104`; further B2-high speed work needs generated scheduler ownership, sector-boundary byte movement, or precomposed upload/restore data rather than another early visible-phase clip. |
| 2026-05-14 | `building2-high-frame92-offscreen-v700` | `ps1: clip building2 high frame92 spans` | Continue B2-high hot-block salvage from the safe side after v698; test frame `92` as the boundary singleton adjacent to the failed v655 `72..92` hot split. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2` for frame `92`, preserving entry offsets/sizes and pack footprint by zero-filling clipped tail. Host summary: `scratch/building2-high-frame92-offscreen-v700/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame92-offscreen-v700-workreduction/20260514-020602-1186813/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due misses `7`; pack LBA/sectors stay `6181/637`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `2195`/`577`/`3`, and runtime frame rows/spans/pixels drop `18092/108958/459413 -> 18089/108381/457218`. | Keep v700 as the BUILDING2 high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue hot-block salvage by testing frame `91` next before broader hot ranges. |
| 2026-05-14 | `building2-high-frame91-offscreen-v701` | `ps1: clip building2 high frame91 spans` | Continue B2-high hot-block salvage after frame `92` passed; test adjacent frame `91` as the next boundary singleton from the safe side of the failed v655 `72..92` hot split. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2` for frame `91`, preserving entry offsets/sizes and pack footprint by zero-filling clipped tail. Host summary: `scratch/building2-high-frame91-offscreen-v701/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame91-offscreen-v701-workreduction/20260514-021548-1245094/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due misses `7`; pack LBA/sectors stay `6181/637`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `3007`/`804`/`20`, and runtime frame rows/spans/pixels drop `18089/108381/457218 -> 18069/107577/454211`. | Keep v701 as the BUILDING2 high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue with frame `90` only as a singleton; do not retry the broad/hot block without scheduler ownership. |
| 2026-05-14 | `building2-high-frame90-offscreen-v702` | `ps1: clip building2 high frame90 spans` | Continue B2-high hot-block salvage after frames `92` and `91` passed; test adjacent frame `90` as the next boundary singleton from the safe side of the failed v655 `72..92` hot split. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2` for frame `90`, preserving entry offsets/sizes and pack footprint by zero-filling clipped tail. Host summary: `scratch/building2-high-frame90-offscreen-v702/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame90-offscreen-v702-workreduction/20260514-022205-1282719/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due misses `7`; pack LBA/sectors stay `6181/637`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `3749`/`921`/`20`, and runtime frame rows/spans/pixels drop `18069/107577/454211 -> 18049/106656/450462`. | Keep v702 as the BUILDING2 high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue with frame `89` only as a singleton; stop if it reproduces the v655 hot-block phase shift. |
| 2026-05-14 | `building2-high-frame89-offscreen-v703` | `ps1: clip building2 high frame89 spans` | Continue B2-high hot-block salvage after frames `92`, `91`, and `90` passed; test adjacent frame `89` as the next boundary singleton from the safe side of the failed v655 `72..92` hot split. | Rewrote only logical draw streams in `generated/ps1/foreground/BUILDING2.FG2` for frame `89`, preserving entry offsets/sizes and pack footprint by zero-filling clipped tail. Host summary: `scratch/building2-high-frame89-offscreen-v703/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame89-offscreen-v703-workreduction/20260514-022739-1316524/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1602`, active loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due misses `7`; pack LBA/sectors stay `6181/637`, and the PS-EXE bucket stays `217088`. Work drops: host logical offscreen pixels/spans/rows drop by `4216`/`1011`/`19`, and runtime frame rows/spans/pixels drop `18049/106656/450462 -> 18030/105645/446246`. | Keep v703 as the BUILDING2 high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue with frame `88` only as a singleton; stop if it reproduces the v655 hot-block phase shift. |
| 2026-05-14 | `building2-high-frame88-offscreen-v704` | log-only after `0be52ec65` | Resolve the next B2-high hot-boundary singleton after frames `89..92` all promoted as exact-flat work-volume clips. Frame `88` was the next binary question before leaving the direct boundary lane. | Ran the same preserve-offset offscreen clipper on frame `88` only. Host summary: `scratch/building2-high-frame88-offscreen-v704/summary.json`. It changed `0` frames and trimmed `0` draw bytes/pixels/spans/rows, so no PS1 timing gate was run; the pack was restored from `scratch/building2-high-frame88-offscreen-v704/BUILDING2.before.FG2` and byte-compared. | Rejected/no-op. There is no removable offscreen draw work in frame `88` under the current transform. The current v703 runtime baseline remains `18030/105645/446246` at exact-flat timing/CD `1602`, `1351/1311`, overrun `40`, blocking/refill `54/18`, and reads/due `58/7`. | Close the direct B2-high hot-boundary singleton lane below frame `89`. Remaining B2-high speed work should move to generated deadline ownership, upload/restore data, or another representation rather than more direct offscreen clipping. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-rg17-33-v822` | Log-only; source restored and PS1 rebuilt. | Pivot from exhausted W1-low hand groups to the next worst row, BUILDING2 high. The current read plan ranked `17..33` as a medium-risk 16-sector high-side candidate with modeled three-read savings, distinct from the already rejected `11..27` probe. | Temporarily inserted `{17, 33, 0}` into `kBuilding2HighReadGroups12` and ran the focused `building2-high-canary` gate against the v703 matched-label baseline. Failed artifact: `scratch/ps1-perf-iterate/building2-high-rg17-33-v822-probe/20260514-174749-2580112/summary.json`. The source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The saved reads were real (`58 -> 55`) and blocking improved `54 -> 53`, but visible cadence regressed: scene/loop `1602/1351 -> 1607/1356`, overrun `40 -> 45`, refill stayed `18`, and due stayed `7`; the temporary table also moved hot symbols by `+208` bytes. | Close raw B2-high `17..33`. Hand-authored high-side read groups keep converting read-count savings into loop/overrun debt; B2-high needs generated deadline ownership, pack-side shape work that preserves cadence, or upload/restore reduction rather than more scalar retained groups. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-tail194-210compact-v705` | `ps1: compact walkstuf1 low late-tail payloads` | Retry the successful high-tide late-tail physical compaction pattern, but only on the low-tide `194..210` tail subset instead of the rejected full-pack v697 rewrite. This tests whether smaller same-order payload displacement can reduce CD work without stealing visible slack. | Extended `scripts/compact-fgp3-trim-draw-tails.py` with `--frames`, then repacked only entries `194..210` in `generated/ps1/foreground/WALK1LOW.FG2` with `--copy-unparseable --pad-to-input-size --frames 194:210`. The transform preserves file size `1535263`, pack LBA/sectors, and the PS-EXE bucket, while compacting `17` entries and trimming active payload `916139 -> 879801` (`36338` bytes). Focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-tail194-210compact-v705-focused/20260514-023846-1383025/summary.json`. | Promoted as same-speed CD-work reduction. Scene/loop/target stay exact-flat at `1770/1478/1431`, overrun `47`, blocking/refill `64/20`, and due misses `11`; loop reads/read VBlanks improve `62/281 -> 60/273`, and runtime draw rows/spans/pixels stay `16272/116912/650623`. | Keep v705 as the new WALKSTUF1 low baseline. It does not move public target speed, so rollup remains `+0.2736%` / `99.7310%` and raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. The full-pack compaction failure is now narrowed: late-tail-only physical compaction is safe, while early/mid payload displacement remains closed without scheduler ownership. |
| 2026-05-14 | `walkstuf1-low-left43-57compact-v706` | log-only after `c4f60af86` | Test the largest remaining compactable W1-low safe-direct cluster after v705: entries `43..57`, which had `32855` bytes of trailing payload to remove. This was the high-upside binary answer to whether early-left physical displacement is safe after late-tail-only compaction passed. | Repacked only entries `43..57` in `generated/ps1/foreground/WALK1LOW.FG2` with `--copy-unparseable --pad-to-input-size --frames 43:57`; one unparseable entry (`54`) was copied unchanged. The transform preserved file size, LBA/sectors, and the PS-EXE bucket, and trimmed active payload `879801 -> 846946`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-left43-57compact-v706-focused/20260514-024632-1429637/summary.json`. The pack was restored from `scratch/walkstuf1-low-left43-57compact-v706/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. The byte reduction changed cadence in the wrong direction: scene/loop regressed `1770/1478 -> 1778/1486`, target fell `1431 -> 1430`, overrun regressed `47 -> 56`, blocking `64 -> 72`, refill overrun `20 -> 25`, and loop reads worsened `60 -> 61`; due misses improved `11 -> 10` but did not offset the visible timing loss. | Close physical compaction of the early-left `43..57` cluster under the current scheduler. The safe v705 form is late-tail-only; larger early/mid displacement still needs generated deadline ownership or a representation that does not shift hot read cadence. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-left66-77compact-v707` | log-only after `670ef5383` | Continue the binary split after v706 by trying the later safe-direct left subset `66..77`, which had `18110` removable trailing bytes and starts after the known frame-64 direct-clip boundary. | Repacked only entries `66..77` in `generated/ps1/foreground/WALK1LOW.FG2` with `--copy-unparseable --pad-to-input-size --frames 66:77`. The transform preserved file size, LBA/sectors, and the PS-EXE bucket, and trimmed active payload `879801 -> 861691`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-left66-77compact-v707-focused/20260514-024911-1446391/summary.json`. The pack was restored from `scratch/walkstuf1-low-left66-77compact-v707/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. The later displacement regressed even more than v706: scene/loop `1770/1478 -> 1784/1492`, target `1431 -> 1425`, overrun `47 -> 67`, blocking `64 -> 92`, refill overrun `20 -> 29`, and due misses `11 -> 14`; loop reads stayed `60`. | Close broad physical compaction of the left-side `66..77` subset under the current scheduler. The remaining safe physical compaction evidence is late-tail-only v705; left-side byte removal needs generated ownership or no-shift encoding rather than same-order payload displacement. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-mid133-140compact-v708` | log-only after `9e54d676d` | Test whether a tiny mid-pack physical compaction can be safe after the larger left-side shifts failed. Entries `133..140` had only `3239` removable bytes and sit near current read-pressure sectors, so this was a small-risk binary check. | Repacked entries `133..140` in `generated/ps1/foreground/WALK1LOW.FG2` with `--copy-unparseable --pad-to-input-size --frames 133:140`; unparseable entry `137` was copied unchanged. The transform preserved file size, LBA/sectors, and the PS-EXE bucket, and trimmed active payload `879801 -> 876562`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-mid133-140compact-v708-focused/20260514-025152-1463454/summary.json`. The pack was restored from `scratch/walkstuf1-low-mid133-140compact-v708/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. It made the active loop one VBlank shorter but failed the no-regression gate: target fell `1431 -> 1428`, overrun regressed `47 -> 49`, blocking worsened `64 -> 79`, due misses worsened `11 -> 14`, and refill overrun only improved `20 -> 18`. | Close small mid-pack physical displacement under the current scheduler. The pattern across v706-v708 says non-tail same-order compaction changes CD cadence in ways the current scheduler cannot absorb, even when byte savings are small. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-safecompact-v709` | log-only after `900d2a461` | Test whether physically compacting already accepted B2-high safe offscreen subsets could convert same-speed work-volume cleanup into a sector/cadence win. The candidate used only frames that had promoted as direct clips: `89..104` and `168..177`. | Repacked `generated/ps1/foreground/BUILDING2.FG2` with `--copy-unparseable --pad-to-input-size --frames 89:104,168:177`, preserving file size `1303332`, pack LBA/sectors, and the PS-EXE bucket while trimming active payload `674798 -> 651508` across `25` entries (`23290` bytes). Focused gate: `scratch/ps1-perf-iterate/building2-high-safecompact-v709-focused/20260514-025512-1484334/summary.json`. The pack was restored from `scratch/building2-high-safecompact-v709/BUILDING2.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. The byte reduction shifted cadence negative: scene/loop `1602/1351 -> 1609/1358`, target `1311 -> 1309`, overrun `40 -> 49`, blocking `54 -> 60`, refill overrun `18 -> 26`, and loop reads worsened `58 -> 59`; due misses stayed `7`. | Close combined physical compaction of the accepted B2-high safe subsets. If this lane is reopened, split post-hot `89..104` from late `168..177` or require generated scheduler ownership; do not promote the combined same-order payload displacement. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-low-fullcompact-v710` | log-only after `b113dfbe6` | Test the high-upside BUILDING2 low physical compaction lane after v660 proved low tide can safely accept direct offscreen work-volume cleanup. Unlike high tide, the low baseline has zero hidden refill, so the binary question was whether the large remaining tail trim would preserve that cadence or fail structurally. | Repacked `generated/ps1/foreground/BUIL2LOW.FG2` with `--copy-unparseable --pad-to-input-size`, preserving file size `1303332`, pack LBA/sectors, and the PS-EXE bucket while trimming active payload `660236 -> 538534` across `41` entries (`121702` bytes); entries `71` and `77` were copied as unparseable. Focused gate path: `scratch/ps1-perf-iterate/building2-low-fullcompact-v710-focused/20260514-030115-1521526/`. The pack was restored from `scratch/building2-low-fullcompact-v710/BUIL2LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected as structural failure. The run emitted no `JCPERF2` sections and the gate failed `missing_jcperf2`; the headless log shows `JCWALK: walkClean buffer alloc failed` before termination. | Close whole-pack BUILDING2 low physical tail compaction. Future B2-low pack-side byte work must be subset-scoped or memory-budgeted first; do not retry a broad same-order payload rewrite against the current allocator/cleanup shape. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-low-hot80-86compact-v711` | log-only after `9982afb7f` | Narrow the v710 whole-pack failure to the largest localized BUILDING2 low trim cluster, entries `80..86`, to test whether a meaningful sector-boundary cut could avoid the structural allocator failure and preserve the v660 zero-refill cadence. | Repacked only entries `80..86` in `generated/ps1/foreground/BUIL2LOW.FG2` with `--copy-unparseable --pad-to-input-size --frames 80:86`, preserving file size `1303332`, pack LBA/sectors, and the PS-EXE bucket while trimming active payload `660236 -> 618622` (`41614` bytes). Focused gate: `scratch/ps1-perf-iterate/building2-low-hot80-86compact-v711-focused/20260514-030500-1545767/summary.json`. The pack was restored from `scratch/building2-low-hot80-86compact-v711/BUIL2LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. The subset avoided the v710 structural failure and reduced blocking `61 -> 59`, but strict timing regressed: scene/loop `1614/1344 -> 1617/1349`, overrun `26 -> 31`; target `1318`, refill `0`, loop reads `50`, and due misses improved `14 -> 13`. | Close hot `80..86` same-order physical compaction for BUILDING2 low. Byte movement can reduce visible blocking but loses more active-loop timing; future B2-low compaction must be scheduler-owned or split to preserve loop timing, not just blocking. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-island58-63compact-v712` | log-only after `47a3ecd7b` | Test the W1-low direct-clip safe island `58..63` as a physical compaction subset after v705 proved late-tail-only compaction safe and v706/v707 rejected adjacent left-side displacement. The island was safe for preserve-offset draw clipping, so this checked whether its remaining tail bytes could move without stealing visible slack. | Repacked only entries `58..63` in `generated/ps1/foreground/WALK1LOW.FG2` with `--copy-unparseable --pad-to-input-size --frames 58:63`, preserving file size `1535263`, pack LBA/sectors, and the PS-EXE bucket while trimming active payload `879801 -> 857162` (`22639` bytes). Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-island58-63compact-v712-focused/20260514-030747-1563615/summary.json`. The pack was restored from `scratch/walkstuf1-low-island58-63compact-v712/WALK1LOW.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. It reproduced the non-tail compaction cliff: scene/loop `1770/1478 -> 1784/1492`, target `1431 -> 1429`, overrun `47 -> 63`, blocking `64 -> 97`, refill `20 -> 31`, and due misses `11 -> 15`; loop reads stayed `60`. | Close W1-low physical compaction for the `58..63` safe direct-clip island. Preserve-offset clipping can be safe there, but same-order payload displacement is not; only late-tail physical compaction remains accepted without generated scheduler ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-posthot89-104compact-v713` | log-only after `89b372e44` | Split the rejected v709 B2-high combined physical compaction and test only the post-hot accepted offscreen-clip subset `89..104`, leaving the late `168..177` subset untouched. | Repacked entries `89..104` in `generated/ps1/foreground/BUILDING2.FG2` with `--copy-unparseable --pad-to-input-size --frames 89:104`, preserving file size `1303332`, pack LBA/sectors, and the PS-EXE bucket while trimming active payload `674798 -> 655182` (`19616` bytes). Focused gate: `scratch/ps1-perf-iterate/building2-high-posthot89-104compact-v713-focused/20260514-031417-1603243/summary.json`. The pack was restored from `scratch/building2-high-posthot89-104compact-v713/BUILDING2.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. Scene/loop/blocking stayed flat at `1602/1351/54`, but target fell `1311 -> 1310`, overrun regressed `40 -> 41`, refill overrun worsened `18 -> 24`, and loop reads worsened `58 -> 59`; due stayed `7`. | Close post-hot `89..104` physical compaction for B2 high. The v709 regression is mostly reproduced without the late tail; only the late-only split remains worth resolving, and it should be treated as same-speed work-reduction at best. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-late168-177compact-v714` | log-only after `5ee0fa168` | Resolve the second half of the v709 split by testing only the late accepted offscreen-clip subset `168..177`, with post-hot `89..104` left untouched. | Repacked entries `168..177` in `generated/ps1/foreground/BUILDING2.FG2` with `--copy-unparseable --pad-to-input-size --frames 168:177`, preserving file size `1303332`, pack LBA/sectors, and the PS-EXE bucket while trimming active payload `674798 -> 671124` (`3674` bytes). Focused gate: `scratch/ps1-perf-iterate/building2-high-late168-177compact-v714-focused/20260514-031654-1620035/summary.json`. The pack was restored from `scratch/building2-high-late168-177compact-v714/BUILDING2.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected. The tiny byte move was heavily phase-negative: scene/loop `1602/1351 -> 1617/1366`, target `1311 -> 1305`, overrun `40 -> 61`, blocking `54 -> 67`, and refill `18 -> 30`; loop reads stayed `58` and due stayed `7`. | Close late `168..177` physical compaction for B2 high. Together with v709 and v713, this closes same-order physical compaction of the accepted offscreen-clip subsets; B2-high speed now needs scheduler-owned metadata, no-shift encoding, or a different upload/restore representation. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-high-offscreen-drawclip-v715` | Log-only; pack restored and PS1 rebuilt. | Test whether the accepted BUILDING4 low offscreen draw-span transform can be mirrored onto high tide. A host scan found the same direct offscreen work shape in `BUILDING4.FG2`: `30` changed frames, `65111` offscreen draw pixels, `11871` spans, `471` draw rows, and `61047` logical draw bytes removable while preserving every entry offset/size and the file footprint. | Applied the preserve-offset PAL4 draw clip to `generated/ps1/foreground/BUILDING4.FG2`. Host summary: `scratch/building4-high-offscreen-drawclip-v715/summary.json`; fresh same-commit baseline: `scratch/ps1-perf-iterate/building4-high-current-v715-baseline/20260514-032525-1671640/summary.json`; matched-label focused gate: `scratch/ps1-perf-iterate/building4-high-offscreen-drawclip-v715-matched/20260514-032924-1697227/summary.json`. The pack was restored from `scratch/building4-high-offscreen-drawclip-v715/BUILDING4.before.FG2`, byte-compared, and `./scripts/build-ps1.sh` passed afterward. | Rejected/no promotion. Against the fresh current-code baseline the candidate is safe but only a one-VBlank target-relative nudge: scene/loop stay `3118/2847`, target improves `2815 -> 2816`, overrun `32 -> 31`, blocking/refill stay `37/33`, reads/due stay `49/1`, and upload bytes drop `20705280 -> 20700800`. That fresh row is worse than the published matrix row `2844/2816`, so promoting this would be a stale-row refresh plus still-yellow result rather than a net battlecard gain. | Close direct BUILDING4 high offscreen draw clipping as a promotion lane for now. It is safe, but too small to beat the documented baseline or move the row green; future BUILDING4 high work should target scheduler-owned `31..47` or `264..280` clusters, precomposed/static upload bands, or a broader current-row refresh before any pack-only high-tide promotion. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame133-offscreen-v716` | `ps1: clip walkstuf1 low frame133 spans` | Continue the isolated W1 low offscreen singleton lane after v696 and v705: broad clipping and physical non-tail compaction are closed, but host rescans still showed frame `133` had a sizeable direct offscreen draw tail adjacent to already accepted mid-frame clips. | Rewrote only logical PAL4 draw streams for frame `133` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame133-offscreen-v716/summary.json`; fresh baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v716-baseline/20260514-033610-1738569/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame133-offscreen-v716-workreduction/20260514-033727-1747145/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `2937` draw pixels, `188` spans, and `4` draw rows; runtime frame rows/spans/pixels drop `16272/116912/650623 -> 16268/116724/647686`. | Keep v716 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue singleton-only clipping around the remaining W1 low mid/early frames; do not retry broad clipping, frame `0`, frame `64`, or physical non-tail compaction without scheduler ownership. |
| 2026-05-14 | `walkstuf1-low-frame132-offscreen-v717` | `ps1: clip walkstuf1 low frame132 spans` | Continue the accepted singleton-only W1 low lane after v716 by clipping the adjacent frame `132` direct offscreen draw tail. The first run exposed a baseline-label mismatch, so the promotion was gated only after creating a same-label v716 baseline. | Rewrote only logical PAL4 draw streams for frame `132` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame132-offscreen-v717/summary.json`; matched v716 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v717-matched-baseline/20260514-034640-1803608/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame132-offscreen-v717-matched/20260514-034748-1811230/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `2131` draw pixels, `142` spans, and `1` draw row; runtime frame rows/spans/pixels drop `16268/116724/647686 -> 16267/116582/645555`. | Keep v717 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue with narrow singleton gates only; avoid broad W1 low clipping, frame `0`, frame `64`, frame `86`, and physical non-tail compaction without scheduler ownership. |
| 2026-05-14 | `walkstuf1-low-frame5-offscreen-v718` | `ps1: clip walkstuf1 low frame5 spans` | Take the next largest known W1 low singleton after v717, excluding already closed frame `0`, frame `64`, and frame `86`. Frame `5` has a large offscreen draw tail but no row-count change, making it a narrow work-volume candidate rather than a speed candidate. | Rewrote only logical PAL4 draw streams for frame `5` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame5-offscreen-v718/summary.json`; clean v717 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v718-clean-baseline/20260514-035347-1848399/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame5-offscreen-v718-clean-matched/20260514-035453-1855985/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `1864` draw pixels and `352` spans; runtime frame rows/spans/pixels drop `16267/116582/645555 -> 16267/116230/643691`. | Keep v718 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue with singleton gates by remaining work size, then shift back to scheduler/CD ownership once no sizeable safe offscreen singletons remain. |
| 2026-05-14 | `walkstuf1-low-frame141-offscreen-v719` | `ps1: clip walkstuf1 low frame141 spans` | Continue singleton clipping by work size after v718. Frame `141` is adjacent to accepted frame `140` and the newer mid-frame clips, making it a narrow preserve-offset candidate with no expected CD or layout movement. | Rewrote only logical PAL4 draw streams for frame `141` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame141-offscreen-v719/summary.json`; clean v718 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v719-clean-baseline/20260514-035914-1882691/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame141-offscreen-v719-clean-matched/20260514-040034-1891648/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `1695` draw pixels and `358` spans; runtime frame rows/spans/pixels drop `16267/116230/643691 -> 16267/115872/641996`. | Keep v719 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Continue with frame `131`, frame `19`, frame `6`, and other remaining singleton-sized work before moving back to CD scheduler ownership. |
| 2026-05-14 | `walkstuf1-low-frame131-offscreen-v720` | `ps1: clip walkstuf1 low frame131 spans` | Continue the descending W1 low singleton pass after v719. Frame `131` sits next to the accepted frame `132` clip and still had a sizeable direct offscreen tail. | Rewrote only logical PAL4 draw streams for frame `131` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame131-offscreen-v720/summary.json`; clean v719 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v720-clean-baseline/20260514-040435-1916317/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame131-offscreen-v720-clean-matched/20260514-040553-1924973/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `1673` draw pixels, `103` spans, and `1` draw row; runtime frame rows/spans/pixels drop `16267/115872/641996 -> 16266/115769/640323`. | Keep v720 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Next singleton choices by current work size are frame `19`, frame `6`, frame `142`, and frame `130`; maintain exact baseline gates. |
| 2026-05-14 | `walkstuf1-low-frame19-offscreen-v721` | `ps1: clip walkstuf1 low frame19 spans` | Continue the W1 low singleton pass after v720. Frame `19` is an early-frame candidate, but unlike the closed frame `0` path it preserves all timing/CD/layout counters when clipped in isolation. | Rewrote only logical PAL4 draw streams for frame `19` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame19-offscreen-v721/summary.json`; clean v720 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v721-clean-baseline/20260514-040941-1948369/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame19-offscreen-v721-clean-matched/20260514-041055-1956654/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `1320` draw pixels, `70` spans, and `9` draw rows; runtime frame rows/spans/pixels drop `16266/115769/640323 -> 16257/115699/639003`. | Keep v721 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Next singleton choices by work size are frame `6`, frame `142`, frame `130`, and frame `145`; maintain exact no-regression gates. |
| 2026-05-14 | `walkstuf1-low-frame6-offscreen-v722` | `ps1: clip walkstuf1 low frame6 spans` | Continue the W1 low singleton pass after v721. Frame `6` is another early-frame candidate adjacent to accepted frame `5`, with no expected row-count change. | Rewrote only logical PAL4 draw streams for frame `6` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame6-offscreen-v722/summary.json`; clean v721 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v722-clean-baseline/20260514-041500-1981638/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame6-offscreen-v722-clean-matched/20260514-041618-1990364/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `1317` draw pixels and `307` spans; runtime frame rows/spans/pixels drop `16257/115699/639003 -> 16257/115392/637686`. | Keep v722 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Next singleton choices by work size are frame `142`, frame `130`, frame `145`, and frame `129`; maintain exact no-regression gates. |
| 2026-05-14 | `walkstuf1-low-frame142-offscreen-v723` | `ps1: clip walkstuf1 low frame142 spans` | Continue the W1 low singleton pass after v722. Frame `142` is the next largest known preserve-offset offscreen tail and sits beside the accepted `140`/`141` clips. | Rewrote only logical PAL4 draw streams for frame `142` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame142-offscreen-v723/summary.json`; clean v722 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v723-clean-baseline/20260514-042032-2016204/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame142-offscreen-v723-clean-matched/20260514-042147-2024660/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`. Host logical work drops by `1097` draw pixels and `294` spans; runtime frame rows/spans/pixels drop `16257/115392/637686 -> 16257/115098/636589`. | Keep v723 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Next singleton choices by work size are frame `130`, frame `145`, and frame `129`; maintain exact no-regression gates. |
| 2026-05-14 | `walkstuf1-low-frame130-offscreen-v724` | `ps1: clip walkstuf1 low frame130 spans` | Continue the W1 low singleton pass after v723. Frame `130` is the next largest known preserve-offset offscreen tail and sits beside the accepted `131..133` cuts. | Rewrote only logical PAL4 draw streams for frame `130` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame130-offscreen-v724/summary.json`; clean v723 baseline taken at current HEAD: `scratch/ps1-perf-iterate/walkstuf1-low-current-v724-clean-baseline/20260514-043051-2080573/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame130-offscreen-v724-clean-matched/20260514-043206-2088949/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`. Host logical work drops by `1015` draw pixels and `57` spans; runtime frame rows/spans/pixels drop `16257/115098/636589 -> 16257/115041/635574`. | Keep v724 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Next singleton choices by work size are frame `145` and frame `129`; maintain exact no-regression gates, then switch back to speed-moving CD/scheduler ownership. |
| 2026-05-14 | `walkstuf1-low-frame145-offscreen-v725` | `ps1: clip walkstuf1 low frame145 spans` | Continue the W1 low singleton pass after v724. Frame `145` is the next largest preserve-offset offscreen tail in the late-mid cluster. | Rewrote only logical PAL4 draw streams for frame `145` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame145-offscreen-v725/summary.json`; clean v724 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v725-clean-baseline/20260514-043652-2117870/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame145-offscreen-v725-clean-matched/20260514-043809-2126427/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`. Host logical work drops by `916` draw pixels and `187` spans; runtime frame rows/spans/pixels drop `16257/115041/635574 -> 16257/114854/634658`. | Keep v725 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. Next singleton choice by work size is frame `129`; after that, shift back to speed-moving CD/scheduler ownership or another under-99 row. |
| 2026-05-14 | `walkstuf1-low-frame129-offscreen-v726` | `ps1: clip walkstuf1 low frame129 spans` | Finish the current W1 low singleton pass after v725. Frame `129` is the last sizeable known preserve-offset offscreen tail from the active list. | Rewrote only logical PAL4 draw streams for frame `129` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte footprint, every entry offset/size, pack LBA/sectors, and the PS-EXE bucket. Host summary: `scratch/walkstuf1-low-frame129-offscreen-v726/summary.json`; clean v725 baseline: `scratch/ps1-perf-iterate/walkstuf1-low-current-v726-clean-baseline/20260514-044335-2159163/summary.json`; accepted no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame129-offscreen-v726-clean-matched/20260514-044456-2168184/summary.json`. | Promoted as same-speed work reduction. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`. Host logical work drops by `782` draw pixels and `56` spans; runtime frame rows/spans/pixels drop `16257/114854/634658 -> 16257/114798/633876`. | Keep v726 as the current W1 low work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2736%` / `99.7310%`; raw signed remains `-0.4936%` / `100.5133%`; methodology totals remain about `17.13` / `12.63`. The current W1-low singleton worklist is now exhausted; shift to speed-moving CD/scheduler ownership or the next under-99 outlier. |
| 2026-05-14 | `visitor3-high-staged-d4-current-v727` | Log-only; source restored and PS1 rebuilt. | Before retrying the rejected frame133 chained-D4 pack shape, test whether the runtime can safely decode any staged previous-frame D4 payload before `fgRuntimeSetStagedFrame()` on the current accepted VISITOR3 high pack. | Temporarily added a broad staged-D4 decode hook in `src/foreground_pilot.c` and ran the current VISITOR3 high case without any pack change. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-staged-d4-current-v727/20260514-045510-2229866/summary.json`; log: `scratch/ps1-perf-iterate/visitor3-high-staged-d4-current-v727/20260514-045510-2229866/visitor3-high-staged-d4-current-v727/headless-regtest.log`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The source-only safety probe reached the `JCRECT` marker but timed out without `JCPERF2`, leaving no valid speed or correctness counters. The accepted v629/v726 baseline was restored cleanly, so public rollup and methodology totals remain unchanged. | Close broad staged-D4 decoding for existing previous-frame deltas. Any future VISITOR3 high frame133 retry must be frame-specific and generated-owned, with an offline staged/prepared ownership validator proving previous-frame lifetime, decode budget, and complete sound/frame correctness before PS1 timing. |
| 2026-05-14 | `walkstuf1-low-rg371-399-v728-v729` | Log-only; source restored and PS1 rebuilt. | After the singleton offscreen list was exhausted, test the current W1-low read-plan's lower-risk tail candidates before leaving the worst under-99 row. The plan listed `371..387` as a two-read-save row and the follow-up plan after that failure listed adjacent `387..399` as a one-read-save row. | Temporarily split the WALKSTUF1 retained-read table by tide and added low-only `{371,387,0}`, then replaced it with `{387,399,0}`. Focused artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-rg371-387-v728-focused/20260514-050354-2282968/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-rg387-399-v729-focused/20260514-050520-2292282/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `371..387` fired and cut reads `60 -> 58`, but regressed scene/loop `1770/1478 -> 1772/1480`, overrun `47 -> 48`, blocking `64 -> 67`, and refill `20 -> 23`. `387..399` was the smaller split and still regressed scene/loop `1770/1478 -> 1771/1479`, overrun `47 -> 48`, blocking `64 -> 66`, and refill `20 -> 22` while only cutting reads `60 -> 59`. | Close W1-low direct retained rows over `371..399` on the current baseline. This repeats the W1-low scalar pattern: saved reads exist, but hand-authored append ownership steals visible/refill cadence. Future W1-low work needs generated frame-deadline ownership, pack-side byte/phase reduction, or upload/restore elimination before another read-row gate. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `under99-current-refresh-v730` | Docs/CSV baseline refresh; no code or pack change. | Before opening another risky under-99 lane, rerun the current under-99 set from a clean tree to catch stale rows. The sweep covered WALKSTUF1 low/high, BUILDING2 high/low, VISITOR3 high/low, and JOHNNY1 high/low. | Ran `./scripts/ps1-perf-iterate.sh` with the eight under-99 cases and `--skip-build`. Artifact: `scratch/ps1-perf-iterate/under99-current-refresh-v730/20260514-051331-2341777/summary.json`. | Promoted as a measurement-baseline correction. Seven rows matched the matrix exactly; JOHNNY1 low refreshed from stale `1974/1945`, overrun/blocking/refill `29/26/26`, LBA `14201`, and loop-read time `56` to current `1973/1945`, overrun/blocking/refill `28/25/25`, LBA `14202`, and loop-read time `58`, matching JOHNNY1 high. | Keep as the new JOHNNY1 low baseline. This is not a runtime-code win, but it prevents optimizing against a stale row and moves the public rollup from `+0.2736%` / `99.7310%` to `+0.2732%` / `99.7314%`; raw signed moves to `-0.4940%` / `100.5137%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.13` public over-target points and added about `12.63` public target-speed points. Continue with WALKSTUF1 low first, then BUILDING2 high and WALKSTUF1 high. |
| 2026-05-14 | `walkstuf1-high-frame55-offscreen-v731` | `ps1: clip walkstuf1 high frame55 spans` | Continue the W1-high generated-pack cleanup after v657 left timing flat but reduced read work. The next safe singleton was frame `55`, which is fully offscreen at the current high-tide island offset and can be removed without changing frame offsets, pack bytes, pack LBA/sectors, or the PS-EXE bucket. | Reused the offset-preserving draw-span clipper from the W1-low lane, clipping only frame `55` in `generated/ps1/foreground/WALKSTUF1.FG2` and zero-filling the reclaimed tail inside the existing entry. Transform summary: `scratch/walkstuf1-high-frame55-offscreen-v731/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame55-offscreen-v731-focused/20260514-053922-2499165/summary.json`. | Promoted as same-speed work-volume reduction. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/275`, and due misses `16`; pack LBA/sectors stay `24883/750`, active payload stays `882007`, and the PS-EXE bucket stays `217088`. The logical frame clip trims `4712` draw bytes, `8736` pixels, `97` spans, and `61` rows, dropping runtime rows/spans/pixels `17296/134136/776856 -> 17235/134039/768120`. | Keep v731 as the new W1-high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2732%` over target / `99.7314%` target speed and raw signed remains `-0.4940%` / `100.5137%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Continue the W1-high singleton pass with the next safe offscreen candidates before pivoting back to B2 high or W1 low speed-bearing scheduler/data-shape work. |
| 2026-05-14 | `walkstuf1-high-frame138-offscreen-v732` | `ps1: clip walkstuf1 high frame138 spans` | Continue the W1-high singleton offscreen pass after frame55 proved the lane still accepts exact-flat pack-only work. Frame `138` had the next non-risky remaining clip candidate once frame0 stayed parked as a known risky boundary. | Clipped only frame `138` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving entry offsets/file size/LBA and zero-filling the tail inside the existing entry. Transform summary: `scratch/walkstuf1-high-frame138-offscreen-v732/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame138-offscreen-v732-focused/20260514-055138-2572494/summary.json`. | Promoted as same-speed work-volume reduction. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/275`, and due misses `16`; pack LBA/sectors stay `24883/750`, active payload stays `882007`, and the PS-EXE bucket stays `217088`. The logical frame clip trims `912` draw bytes, `1805` pixels, and `4` spans, dropping runtime rows/spans/pixels `17235/134039/768120 -> 17235/134035/766315`. | Keep v732 as the new W1-high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2732%` over target / `99.7314%` target speed and raw signed remains `-0.4940%` / `100.5137%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Next W1-high singleton candidates are smaller, so pivot pressure back to speed-bearing W1-low/B2-high scheduler or data-shape work if the next two exact-flat clips stay timing-inert. |
| 2026-05-14 | `walkstuf1-high-frame51-offscreen-v733` | `ps1: clip walkstuf1 high frame51 spans` | Use the largest remaining W1-high current singleton after W1-low's remaining scan candidates proved no-op on the v726 pack and B2-high early offscreen was already closed as phase-negative. Frame `51` still had a real isolated offscreen trim in the current v732 pack. | Clipped only frame `51` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving entry offsets/file size/LBA and zero-filling the tail inside the existing entry. Transform summary: `scratch/walkstuf1-high-frame51-offscreen-v733/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame51-offscreen-v733-focused/20260514-055847-2615464/summary.json`. | Promoted as same-speed work-volume reduction. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/275`, and due misses `16`; pack LBA/sectors stay `24883/750`, active payload stays `882007`, and the PS-EXE bucket stays `217088`. The logical frame clip trims `4816` draw bytes, `5954` pixels, `639` spans, and `94` rows, dropping runtime rows/spans/pixels `17235/134035/766315 -> 17141/133396/760361`. | Keep v733 as the new W1-high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2732%` over target / `99.7314%` target speed and raw signed remains `-0.4940%` / `100.5137%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. The lane is useful for risk reduction but still not moving timing, so the next true speed push should be W1-low/B2-high generated ownership, pack-side byte movement, or selective upload/restore preprocessing. |
| 2026-05-14 | `walkstuf1-high-frame49-offscreen-v734` | `ps1: clip walkstuf1 high frame49 spans` | Take one final high-value W1-high direct singleton before pivoting away from this timing-inert lane. Frame `49` was the next biggest current clip after frame51 and still reduced whole-frame offscreen draw work without layout movement. | Clipped only frame `49` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving entry offsets/file size/LBA and zero-filling the tail inside the existing entry. Transform summary: `scratch/walkstuf1-high-frame49-offscreen-v734/summary.json`; focused no-regression gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame49-offscreen-v734-focused/20260514-060517-2655404/summary.json`. | Promoted as same-speed work-volume reduction. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/275`, and due misses `16`; pack LBA/sectors stay `24883/750`, active payload stays `882007`, and the PS-EXE bucket stays `217088`. The logical frame clip trims `4497` draw bytes, `5323` pixels, `623` spans, and `89` rows, dropping runtime rows/spans/pixels `17141/133396/760361 -> 17052/132773/755038`. | Keep v734 as the new W1-high work-volume baseline, but do not count it as a VBlank speed win. Public rollup remains `+0.2732%` over target / `99.7314%` target speed and raw signed remains `-0.4940%` / `100.5137%`; methodology totals remain about `17.13` public over-target points removed and `12.63` target-speed points gained. Direct W1-high singleton clipping has now produced four exact-flat promotions in a row, so the next optimization should pivot to W1-low/B2-high generated ownership, sector/pack relocation, or selective upload/restore preprocessing. |
| 2026-05-14 | `walkstuf1-high-prepare-before-window-v735` | Log-only; source restored and PS1 rebuilt. | Test whether the W1-low prepare-before-window scheduler rule also helps W1-high, since both tides show `prep_blocked_cd=6` and large visible blocking. | Temporarily removed the low-tide guard from the WALKSTUF1 prepare-first scheduler branch in `src/foreground_pilot.c`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-prepare-before-window-v735-focused/20260514-061343-2706456/summary.json`. The source was restored to the v734 condition and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. W1-high regressed scene/loop `1764/1476 -> 1784/1496`, target `1434 -> 1425`, overrun `42 -> 71`, blocking `81 -> 103`, refill `23 -> 32`, loop reads `63 -> 65`, and due misses `16 -> 15`. | Close the simple W1-high prepare-first toggle. Unlike W1-low, high tide still needs CD-window ownership at that slack point; future W1-high work should use generated deadline ownership or pack/sector byte movement instead of sharing the low-tide scheduler shortcut. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-prepare-before-window-v736` | Log-only; source restored and PS1 rebuilt. | Test whether BUILDING2 high can use the accepted WALKSTUF1 low prepare-before-window scheduler shape to prioritize visual prepare over a speculative retained-window refill at the `prep_blocked_cd` slack point. | Temporarily extended the prepare-first branch in `src/foreground_pilot.c` to high-tide `building2`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-prepare-before-window-v736-focused/20260514-062149-2756023/summary.json`. The source was restored to the v735 condition and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. BUILDING2 high regressed scene/loop `1602/1351 -> 1611/1360`, target `1311 -> 1308`, overrun `40 -> 52`, blocking `54 -> 64`, refill `18 -> 29`, while loop reads and due misses stayed `58/7`. The PS-EXE bucket stayed fixed at `217088`, but `foregroundPilotPlay` grew by `48` bytes during the probe. | Close simple BUILDING2 high prepare-first ownership. Like W1-high, this row still needs the CD window at that deadline; future BUILDING2 high work should use generated frame-deadline metadata, pack-side sector relocation, or preprocessed upload/restore work rather than stealing the scheduler slot from the window refill. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-low-prepare-before-window-v737` | Log-only; source restored and PS1 rebuilt. | Test the low-tide half of the BUILDING2 prepare-first idea separately because low had zero refill overrun and a different due-miss profile than the rejected high-tide v736 probe. | Temporarily extended the low-tide prepare-first branch in `src/foreground_pilot.c` to include `building2` beside the accepted `walkstuf1` exception. Focused artifact: `scratch/ps1-perf-iterate/building2-low-prepare-before-window-v737-focused/20260514-062435-2773667/summary.json`. The source was restored to the v736 condition and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. BUILDING2 low regressed scene/loop `1614/1344 -> 1619/1349`, target `1318 -> 1313`, overrun `26 -> 36`, blocking `61 -> 124`, refill `0 -> 2`, and due misses `14 -> 27`; loop reads moved `50 -> 52`. The PS-EXE bucket stayed fixed but `foregroundPilotPlay` grew by `32` bytes during the probe. | Close BUILDING2 low prepare-first ownership. Both BUILDING2 tides now prove the retained window is more important than early visual prepare at this slack point; future BUILDING2 work should avoid simple scheduler-slot stealing and focus on generated deadline metadata, sector-visible pack relocation, or preprocessed render/upload reductions. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-window34-v738` | Log-only; source restored and PS1 rebuilt. | Test the smallest untried BUILDING4 low retained-window midpoint after the old `40 KiB`/`48 KiB` growth probes failed. The current row only needed roughly nine loop VBlanks to reach 99%, so a `34 KiB` window was the binary check between the accepted `32 KiB` baseline and the rejected larger shapes. | Temporarily changed `FG_BUILDING4_LOW_WINDOW_BYTES` from `32 KiB` to `34 KiB`, rebuilt, and ran the focused BUILDING4 low gate against `scratch/ps1-perf-iterate/building4-low-offscreen-drawclip-v652-samecommit/20260513-210327-3554857/summary.json` with strict no-regression and `--require-improvement`. Focused artifact: `scratch/ps1-perf-iterate/building4-low-window34-v738-focused/20260514-063056-2812675/summary.json`. The source was restored to `32 KiB` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate reduced loop reads `30 -> 29`, but the larger fresh fill landed in visible/refill debt: scene/loop `3128/2853 -> 3139/2863`, target `2816 -> 2814`, overrun `37 -> 49`, blocking `40 -> 52`, and prefetch overrun `34 -> 53`; due misses improved `1 -> 0`, but not enough to offset the timing regression. The PS-EXE bucket and pack layout stayed fixed. | Close BUILDING4 low scalar window growth above `32 KiB` on the current v652 baseline. Since `34 KiB`, `40 KiB`, and `48 KiB` all regress, remaining BUILDING4 low work needs pack-side byte/cleanup reduction, generated deadline ownership, or selective preprocessing rather than a retained-window-size knob. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-low-trimtails-v739` | `ps1: trim building2 low draw-tail metadata` | Retry BUILDING2 low physical tail trimming after the broader v710 failure, but promote only after a clean focused gate and cross-row canary sweep prove the current pack/runtime state is structurally safe. The candidate preserves the `1303332` byte file size, pack LBA/sectors, and PS-EXE bucket while removing dead draw-tail payload bytes inside the existing low-tide pack footprint. | Repacked `generated/ps1/foreground/BUIL2LOW.FG2` with `scripts/compact-fgp3-trim-draw-tails.py`, preserving file size `1303332` and trimming active payload `660236 -> 538534` across `41` entries (`121702` bytes); entries `71` and `77` remain copied as unparseable previous-delta payloads. Focused artifact: `scratch/ps1-perf-iterate/building2-low-trimtails-v739-focused/20260514-063433-2835496/summary.json`. Canary artifact: `scratch/ps1-perf-iterate/building2-low-trimtails-v739-canaries/20260514-063615-2846269/summary.json` covering BUILDING2 low/high, WALKSTUF1 low/high, and VISITOR3 low/high. | Promoted. BUILDING2 low improves scene/loop/target `1614/1344/1318 -> 1603/1339/1317`, overrun `26 -> 22`, blocking `61 -> 53`, loop reads/read VBlanks `50/218 -> 37/150`, and due misses `14 -> 12`; refill stays `0`, pack LBA/sectors stay `6818/637`, and the PS-EXE bucket stays fixed. The canaries stayed exact-flat: BUILDING2 high `1351/1311`, WALKSTUF1 low `1478/1431`, WALKSTUF1 high `1476/1434`, VISITOR3 low `1062/1040`, and VISITOR3 high `1063/1040`. | Keep v739 as the new BUILDING2 low baseline and supersede the old v660 same-speed low work baseline for timing work. Public rollup moves from `+0.2732%` over target / `99.7314%` target speed to `+0.2708%` / `99.7337%`; raw signed moves from `-0.4940%` / `100.5137%` to `-0.4963%` / `100.5160%`. Since the compact full-matrix baseline, the headless methodology has removed about `17.13` public over-target points and added about `12.63` public target-speed points. Continue with WALKSTUF1 low first, then BUILDING2 high and WALKSTUF1 high. |
| 2026-05-14 | `building2-low-rg96-120-v740` | Log-only; source restored and PS1 rebuilt. | After v739 changed BUILDING2 low's physical pack/read shape, test the new top read-plan candidate `96..120`. The plan estimated five saved reads and current-window fit, but marked the row scheduler-owned/high-risk because the first/internal gaps were tight. | Temporarily inserted raw low-tide read group `{96,120,0}` before the accepted BUILDING2 low groups and rebuilt. Focused artifact: `scratch/ps1-perf-iterate/building2-low-rg96-120-v740-focused/20260514-065951-2989916/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row fired and reduced loop reads `37 -> 32`, but it stole visible/refill cadence: scene/loop `1603/1339 -> 1608/1344`, target `1317 -> 1314`, overrun `22 -> 30`, blocking `53 -> 89`, refill `0 -> 4`, and due misses `12 -> 19`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close raw BUILDING2 low `96..120` on the v739 baseline. The saved reads are real, but this must be scheduler-owned or earlier-deadline generated metadata before it can be useful. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-tail129-145compact-v741` | Log-only; pack restored and PS1 rebuilt. | Test whether the W1-low late-mid zero-tail work from the accepted v665/v716..v726 preserve-offset clips can safely become physical payload compaction. This is later than the rejected early-left v706/v707 physical compactions and trims a smaller, already-proven logical subset. | Repacked only selected entries `129..145` in `generated/ps1/foreground/WALK1LOW.FG2` with `scripts/compact-fgp3-trim-draw-tails.py --frames 129:145 --pad-to-input-size --copy-unparseable`, preserving the `1535263` byte file size and pack LBA/sectors while cutting active payload `879801 -> 867007` across `14` entries (`12794` bytes); entry `137` was copied as unparseable. Host summary: `scratch/walkstuf1-low-tail129-145compact-v741/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-tail129-145compact-v741-focused/20260514-070933-3049955/summary.json`. The pack was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The byte cut reduced loop reads `60 -> 58`, but moved the row into visible/refill debt: scene/loop `1770/1478 -> 1779/1487`, target `1431 -> 1427`, overrun `47 -> 60`, blocking `64 -> 84`, refill `20 -> 27`, and due misses `11 -> 13`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close W1-low physical compaction over `129..145` on the v726 baseline. Like early-left physical displacement, this subset proves saved reads alone are not enough when entry offsets shift into a tighter visible cadence. Future W1-low byte movement needs smaller singleton/boundary probes with explicit read-plan proof, generated deadline ownership, or an encoding that reduces payload without shifting downstream schedule. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-rg155-179-v742` | Log-only; source restored and PS1 rebuilt. | Test the restored W1-low read-plan's best non-closed low-risk direct row after v741 failed. The plan ranked `155..179` as a scheduler-owned candidate with three saved reads, better visible slack than the rejected `371..399` tail rows, and current-window fit. | Temporarily split WALKSTUF1 read groups by tide and added low-only `{155,179,0}` while leaving high tide on the existing shared table. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-v742-focused/20260514-071222-3067864/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row fired and reduced loop reads `60 -> 57`, but still stole visible/refill cadence: scene/loop `1770/1478 -> 1776/1484`, overrun `47 -> 53`, blocking `64 -> 70`, refill `20 -> 27`, and due misses stayed `11`. The PS-EXE bucket stayed fixed, but the temporary source split moved hot symbols by `36` bytes. | Close raw W1-low `155..179` on the v726 baseline. The direct retained-row pattern remains consistent: saved reads are real, but hand-authored ownership without generated deadlines increases visible/refill debt. Future W1-low retained rows should be generated-owned or paired with a phase-preserving pack/layout change before another strict gate. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-rg11-27-v743` | Log-only; source restored and PS1 rebuilt. | Test the B2-high read-plan's large early retained row after physical compaction and prepare-first ownership lanes closed. Candidate `11..27` had a long first visible gap and three saved reads, but still carried tight internal cadence risk. | Temporarily added `{11,27,0}` to `kBuilding2HighReadGroups12` before the accepted high-tide read groups and rebuilt. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg11-27-v743-focused/20260514-071525-3087242/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row fired and reduced loop reads `58 -> 55`, but target and refill cadence regressed: scene/loop `1602/1351 -> 1605/1354`, target `1311 -> 1305`, overrun `40 -> 49`, blocking `54 -> 61`, refill `18 -> 25`, and due misses stayed `7`. The PS-EXE bucket stayed fixed, with a temporary `+4` byte `foregroundPilotPlay` size change and `+4` hot-symbol address drift. | Close raw B2-high `11..27` on the v703 baseline. Saved reads are again real, but scalar hand-ownership is not enough for this row; future B2-high read work needs generated deadlines or data-shape/sector changes that do not lower target cadence. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-high-frame55-physcompact-v744` | Log-only; pack restored and PS1 rebuilt. | Test whether W1-high's accepted frame55 preserve-offset offscreen clip can safely become physical payload compaction. This is the smallest high-value physical check before trying the other zero-tail high-tide frames. | Repacked only frame `55` in `generated/ps1/foreground/WALKSTUF1.FG2` with `scripts/compact-fgp3-trim-draw-tails.py --frames 55 --pad-to-input-size --copy-unparseable`, preserving the `1535263` byte file size and pack LBA/sectors while cutting active payload `882007 -> 877293` (`4714` bytes); the selected entry shrank from `4716` bytes to `2`. Host summary: `scratch/walkstuf1-high-frame55-physcompact-v744/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame55-physcompact-v744-focused/20260514-071802-3104034/summary.json`. The pack was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The byte move was phase-negative: scene/loop `1764/1476 -> 1770/1482`, target `1434 -> 1424`, overrun `42 -> 58`, blocking `81 -> 94`, refill `23 -> 25`, and loop reads worsened `63 -> 64`; due misses stayed `16`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close W1-high frame55 physical compaction on the v734 baseline. Preserve-offset logical clipping is safe here, but downstream offset movement is not. Do not batch the high zero-tail frames without generated scheduler proof; future W1-high speed should prefer no-shift work reduction, generated retained-read ownership, or upload/restore preprocessing. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame291-physcompact-v745` | Log-only; pack restored and PS1 rebuilt. | Test whether the accepted BUILDING4-low offscreen draw-clip work can be converted into a smaller late physical payload move without the broad-window/refill regressions seen in scalar window growth. Frame `291` was the largest remaining trim candidate and sits late enough to be a targeted binary probe. | Repacked only frame `291` in `generated/ps1/foreground/BUIL4LOW.FG2` with `scripts/compact-fgp3-trim-draw-tails.py --frames 291 --pad-to-input-size --copy-unparseable`, preserving the `1714154` byte file size and pack LBA/sectors while cutting active payload `855284 -> 849109` (`6175` bytes); the selected entry shrank from `8585` bytes to `2410`. Host summary: `scratch/building4-low-frame291-physcompact-v745/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building4-low-frame291-physcompact-v745-focused/20260514-072136-3126470/summary.json`. The pack was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The byte move was narrowly phase-negative: scene/loop `3128/2853 -> 3129/2854`, overrun `37 -> 38`, blocking `40 -> 41`, refill `34 -> 35`, while target stayed `2816`, reads stayed `30`, and due misses stayed `1`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close BUILDING4-low frame291 physical compaction on the v652 baseline. The result is close, but still fails the strict new-baseline rule; future BUILDING4-low payload movement should try no-shift encodings or smaller/static upload-preprocess work rather than downstream offset movement. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame291-inplace-v746` | `ps1: shrink building4 low frame291 payload in place` | Retry the v745 byte-reduction signal without moving downstream entry offsets. The physical compaction failed by one VBlank because it shifted later payload phase; this probe shrinks only the frame `291` table size in-place, zero-fills the old tail, and preserves every following offset. | Rewrote only the frame `291` entry size in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/building4-low-frame291-inplace-v746/summary.json`; speed-only focused artifact: `scratch/ps1-perf-iterate/building4-low-frame291-inplace-v746-focused/20260514-074851-3284418/summary.json`; work-reduction promotion gate: `scratch/ps1-perf-iterate/building4-low-frame291-inplace-v746-workreduction/20260514-075311-3310458/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, active loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due misses `1`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `855284 -> 849109` (`6175` bytes) and the selected entry shrinks `8585 -> 2410` without downstream offset movement. | Keep v746 as the new BUILDING4 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; since the compact full-matrix baseline, the headless methodology has removed about `17.13` public over-target points and added about `12.63` public target-speed points. Next BUILDING4-low speed work should target sector-changing no-shift shrinks, generated deadline ownership, or selective static/upload preprocessing. |
| 2026-05-14 | `walkstuf1-low-frame51-inplace-v747` | `ps1: shrink walkstuf1 low frame51 payload in place` | Apply the no-shift entry-size lane to the top WALKSTUF1 low under-99 row after v746 proved downstream offset movement was the risky part. Frame `51` ranked as the largest low-tide no-shift shrink with modeled entry sector coverage `4 -> 1`. | Rewrote only frame `51` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame51-inplace-v747/summary.json`; matched-label promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame51-inplace-v747-workreduction/20260514-080307-3371073/summary.json`; the earlier mismatched-label speed-only run is `scratch/ps1-perf-iterate/walkstuf1-low-frame51-inplace-v747-focused/20260514-080148-3362248/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `879801 -> 874983` (`4818` bytes), and the selected entry shrinks `5588 -> 770` with entry-sector coverage `4 -> 1`. | Keep v747 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue W1-low with no-shift sector-changing shrinks before returning to generated deadline ownership. |
| 2026-05-14 | `walkstuf1-low-frame55-inplace-v748` | Log-only; pack restored and PS1 rebuilt. | Test the next largest W1-low no-shift entry-size candidate after v747. Frame `55` had modeled entry-sector coverage `4 -> 1` and shrank to a two-byte draw payload, but it sits on a more cadence-sensitive boundary than frame `51`. | Rewrote only frame `55` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size, pack LBA/sectors `25633/750`, and the PS-EXE bucket while cutting active payload by `4714` bytes (`4716 -> 2` for the entry). Transform summary: `scratch/walkstuf1-low-frame55-inplace-v748/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame55-inplace-v748-focused/20260514-080934-3410874/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame55-inplace-v748/WALK1LOW.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The shrink saved one loop read (`60 -> 59`), but it pulled visible/refill cadence into debt: scene/loop `1770/1478 -> 1782/1490`, target `1431 -> 1426`, overrun `47 -> 64`, blocking `64 -> 98`, refill `20 -> 27`, and due misses `11 -> 16`. | Close W1-low frame55 no-shift shrink on the v747 baseline. The lane is not uniformly safe: frame51 is exact-flat, but frame55 proves some no-shift sector reductions still perturb active-loop staging. Continue with lower-risk no-shift frames only if their baseline read-plan gaps are not tight, otherwise return to generated deadline ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame49-inplace-v749` | `ps1: shrink walkstuf1 low frame49 payload in place` | Continue the W1-low no-shift entry-size lane after frame51 promoted and frame55 proved cadence-sensitive. Frame `49` sits before the safe frame51 boundary and ranked as the next largest remaining shrink with modeled entry sectors `3 -> 1`. | Rewrote only frame `49` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame49-inplace-v749/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame49-inplace-v749-focused/20260514-081211-3427764/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `874983 -> 870484` (`4499` bytes), and the selected entry shrinks `5269 -> 770` with entry-sector coverage `3 -> 1`. | Keep v749 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue W1-low with lower-risk no-shift sector shrinks, skipping the rejected frame55 boundary. |
| 2026-05-14 | `walkstuf1-low-frame47-inplace-v750` | `ps1: shrink walkstuf1 low frame47 payload in place` | Continue the W1-low no-shift entry-size lane after frames `51` and `49` promoted exact-flat and frame `55` proved cadence-sensitive. Frame `47` is the next large pre-boundary shrink and preserves downstream offsets while reducing entry-sector coverage `5 -> 3`. | Rewrote only frame `47` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame47-inplace-v750/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame47-inplace-v750-focused/20260514-082123-3484009/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `870484 -> 866243` (`4241` bytes), and the selected entry shrinks `7399 -> 3158` with entry-sector coverage `5 -> 3`. | Keep v750 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue W1-low no-shift probes only where the baseline read-plan gaps are not tight; otherwise switch back to generated deadline ownership or the next under-99 row. |
| 2026-05-14 | `walkstuf1-low-frame61-inplace-v751` | `ps1: shrink walkstuf1 low frame61 payload in place` | Continue the W1-low no-shift entry-size lane after v750. Frame `61` is the next largest sector-saving candidate after the rejected frame55 boundary and reuses the already-safe singleton area from the offscreen work-volume lane. | Rewrote only frame `61` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame61-inplace-v751/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame61-inplace-v751-focused/20260514-082755-3524464/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/274`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `866243 -> 862205` (`4038` bytes), and the selected entry shrinks `4590 -> 552` with entry-sector coverage `3 -> 1`. | Keep v751 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with adjacent safe no-shift candidates if they keep timing flat, but switch to generated deadline ownership when read cadence remains stuck. |
| 2026-05-14 | `walkstuf1-low-frame60-inplace-v752` | Log-only; pack restored and PS1 rebuilt. | Test the next largest W1-low no-shift entry-size candidate after v751. Frame `60` looked mechanically similar to promoted frame `61`, with modeled entry-sector coverage `3 -> 1`, but it sits on a tighter active-loop cadence boundary. | Rewrote only frame `60` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size, pack LBA/sectors `25633/750`, and the PS-EXE bucket while cutting active payload by `3956` bytes (`4432 -> 476` for the entry). Transform summary: `scratch/walkstuf1-low-frame60-inplace-v752/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame60-inplace-v752-focused/20260514-083348-3560773/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame60-inplace-v752/WALK1LOW.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The shrink perturbed active staging: scene/loop `1770/1478 -> 1780/1488`, target `1431 -> 1428`, overrun `47 -> 60`, blocking `64 -> 82`, refill `20 -> 30`, loop reads `60 -> 61`, and due misses `11 -> 12`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close W1-low frame60 no-shift shrink on the v751 baseline. The safe no-shift island is discontinuous: frames `51`, `49`, `47`, and `61` are exact-flat, but frames `55` and `60` are cadence-sensitive. Continue only with isolated candidates that are not on the same tight boundary, or return to generated deadline ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame62-inplace-v753` | `ps1: shrink walkstuf1 low frame62 payload in place` | Test the adjacent frame after v752 proved frame60 is cadence-sensitive. Frame `62` is still in the known safe offscreen singleton area and reduces entry-sector coverage `4 -> 2` without moving downstream offsets. | Rewrote only frame `62` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame62-inplace-v753/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame62-inplace-v753-focused/20260514-083627-3577880/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `862205 -> 858349` (`3856` bytes), and the selected entry shrinks `4599 -> 743` with entry-sector coverage `4 -> 2`. | Keep v753 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Frame60 stays closed; continue isolated no-shift probes only while strict gates remain flat. |
| 2026-05-14 | `walkstuf1-low-frame59-inplace-v754` | Log-only; pack restored and PS1 rebuilt. | Test the next largest W1-low no-shift entry-size candidate after v753. Frame `59` promised entry-sector coverage `3 -> 1`, but it sits on the same volatile boundary family as the rejected frame60 probe. | Rewrote only frame `59` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size, pack LBA/sectors `25633/750`, and the PS-EXE bucket while cutting active payload by `3744` bytes (`4200 -> 456` for the entry). Transform summary: `scratch/walkstuf1-low-frame59-inplace-v754/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame59-inplace-v754-focused/20260514-084434-3627519/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame59-inplace-v754/WALK1LOW.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The shrink badly perturbed active staging: scene/loop `1770/1478 -> 1786/1494`, target `1431 -> 1424`, overrun `47 -> 70`, blocking `64 -> 102`, refill `20 -> 31`, loop reads `60 -> 64`, and due misses `11 -> 14`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close W1-low frame59 no-shift shrink on the v753 baseline. Frames `59` and `60` both prove the post-safe-island boundary can increase reads and visible debt even without moving offsets. Prefer frame58 only as an explicit binary probe; otherwise move to earlier pre-boundary frames or generated deadline ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame58-inplace-v755` | `ps1: shrink walkstuf1 low frame58 payload in place` | Explicitly probe the remaining boundary frame after frame59/frame60 failed and frame61/frame62 passed. Frame `58` is still the largest remaining sector-saving candidate and reduces entry-sector coverage `3 -> 1` without moving downstream offsets. | Rewrote only frame `58` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame58-inplace-v755/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame58-inplace-v755-focused/20260514-084715-3644859/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `858349 -> 854703` (`3646` bytes), and the selected entry shrinks `3942 -> 296` with entry-sector coverage `3 -> 1`. | Keep v755 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. The frame58/61/62 pass plus frame59/60 fail means the boundary is discontinuous; continue with frame45 or frame37 next rather than assuming adjacency. |
| 2026-05-14 | `walkstuf1-low-frame45-inplace-v756` | `ps1: shrink walkstuf1 low frame45 payload in place` | Skip the closed cadence-sensitive frame55/59/60 probes and continue the earlier isolated no-shift lane. Frame `45` is the next largest unclosed sector-saving candidate and reduces entry-sector coverage `3 -> 1` while preserving every downstream offset. | Rewrote only frame `45` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame45-inplace-v756/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame45-inplace-v756-focused/20260514-085632-3701832/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `854703 -> 851084` (`3619` bytes), and the selected entry shrinks `5276 -> 1657` with entry-sector coverage `3 -> 1`. | Keep v756 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame37 or frame63 next only under exact-flat gating; the row still needs a read/scheduler conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame37-inplace-v757` | `ps1: shrink walkstuf1 low frame37 payload in place` | Continue the earlier isolated no-shift lane after frame45 passed exact-flat. Frame `37` is the next largest unclosed sector-saving candidate, sits away from the rejected 59/60 boundary, and reduces entry-sector coverage `4 -> 2` without moving downstream offsets. | Rewrote only frame `37` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame37-inplace-v757/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame37-inplace-v757-focused/20260514-090316-3743452/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `851084 -> 847643` (`3441` bytes), and the selected entry shrinks `6677 -> 3236` with entry-sector coverage `4 -> 2`. | Keep v757 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame63 or frame35 next only under exact-flat gating, but the row still needs a read/scheduler conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame63-inplace-v758` | Log-only; pack restored and PS1 rebuilt. | Test the next largest W1-low no-shift entry-size candidate after v757. Frame `63` promised entry-sector coverage `3 -> 1`, but it sits on the volatile post-safe-island boundary family near rejected frames `59` and `60`. | Rewrote only frame `63` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size, pack LBA/sectors `25633/750`, and the PS-EXE bucket while cutting active payload by `3399` bytes (`4643 -> 1244` for the entry). Transform summary: `scratch/walkstuf1-low-frame63-inplace-v758/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame63-inplace-v758-focused/20260514-091004-3785276/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame63-inplace-v758/WALK1LOW.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The shrink perturbed active staging: scene/loop `1770/1478 -> 1780/1488`, target `1431 -> 1429`, overrun `47 -> 59`, blocking `64 -> 82`, refill `20 -> 30`, loop reads `60 -> 61`, and due misses `11 -> 12`. Pack LBA/sectors and PS-EXE bucket stayed fixed. | Close W1-low frame63 no-shift shrink on the v757 baseline. The post-58 boundary remains unsafe for direct entry-size shrinking even when the earlier frame37/45 lane is exact-flat. Continue with frame35 or earlier isolated candidates; do not retry adjacent 59/60/63 without a scheduler/read-plan change. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame35-inplace-v759` | `ps1: shrink walkstuf1 low frame35 payload in place` | Continue the earlier isolated no-shift lane after frame63 proved the post-58 boundary remains cadence-sensitive. Frame `35` is the next largest unclosed sector-saving candidate, sits before the exact-flat frame37/45 area, and reduces entry-sector coverage `4 -> 2` without moving downstream offsets. | Rewrote only frame `35` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame35-inplace-v759/summary.json`; promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame35-inplace-v759-focused/20260514-091313-3805611/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/273`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `847643 -> 844668` (`2975` bytes), and the selected entry shrinks `6538 -> 3563` with entry-sector coverage `4 -> 2`. | Keep v759 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame65, frame43, or a scheduler/read-plan conversion after the release. |
| 2026-05-14 | `walkstuf1-low-frame65-inplace-v760-probe` | log-only after `00f7f3b41` | Continue the W1-low no-shift lane after v759 by testing frame `65`, the largest remaining isolated candidate with modeled entry-sector coverage `3 -> 1`. | Rewrote only frame `65` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the file footprint and offsets while cutting the entry from `4630 -> 1666` bytes. Transform summary: `scratch/walkstuf1-low-frame65-inplace-v760/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame65-inplace-v760-focused/20260514-095014-4022236/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame65-inplace-v760/WALK1LOW.before.FG2` before continuing. | Not promoted. The run exposed a current-code timing regression rather than a valid pack win/loss: W1 low measured `1813/1513/1417`, overrun `96`, blocking/refill `149/52`, loop reads `60`, and due misses `18`, with pack LBA `25634` and PS-EXE bucket `219136` instead of the published v759 `1770/1478/1431` row. | Close the frame65 result as confounded until the current runtime baseline is restored. Do not promote the pack shrink from this artifact; retest frame65 only against the v760 fast-poll baseline if the no-shift lane is reopened. |
| 2026-05-14 | `walkstuf1-low-cd-fastpoll-v760` | `ps1: restore bounded fast CD polling` | The post-release merge preserved the long-soak CD hang fallback but changed normal foreground reads to pay a VBlank before every non-blocking sync poll. A fresh clean-pack W1-low baseline confirmed the current-code regression at `1813/1513/1417`, blocking/refill `149/52`, and PS-EXE bucket `219136`. | Added `ps1CdReadSyncBounded()` so ordinary reads first use the old VBlank-free fast poll and only fall back to a bounded 240-VBlank wait if a read stays pending. Removed temporary `JCSU`, `JCRECT2`, and `JCSW` probe logging that had also pushed layout. Focused promotion gate: `scratch/ps1-perf-iterate/walkstuf1-low-cd-fastpoll-v760-focused/20260514-095803-4069969/summary.json`; matrix row updated at `2026-05-14T09:58:03`. | Promoted as a current-baseline recovery and runtime speed fix. W1 low returns to scene/loop/target `1770/1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, due misses `11`, pack LBA `25633`, and PS-EXE bucket `217088`. Against the true slow current baseline this recovers `43` scene VBlanks, `35` active-loop VBlanks, `85` blocking VBlanks, and `32` refill-overrun VBlanks. | Public rollup returns to the documented `+0.2708%` over target / `99.7337%` target speed and raw signed `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained since the compact matrix baseline. v760 is the new W1-low baseline for any further no-shift or scheduler/read-plan probes. |
| 2026-05-14 | `walkstuf1-low-frame65-inplace-v761` | Log-only; pack restored and PS1 rebuilt. | Retest frame `65` against the now-valid v760 fast-poll baseline after the earlier v760 probe was confounded by the post-release CD sync regression. Frame `65` still ranked as the largest remaining W1-low no-shift entry-size candidate, shrinking `4630 -> 1666` with modeled entry-sector coverage `3 -> 1`. | Copied the saved candidate from `scratch/walkstuf1-low-frame65-inplace-v760/WALK1LOW.candidate.FG2` into `generated/ps1/foreground/WALK1LOW.FG2` and gated against `scratch/ps1-perf-iterate/walkstuf1-low-cd-fastpoll-v760-focused/20260514-095803-4069969/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame65-inplace-v761-focused/20260514-100846-4136606/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame65-inplace-v760/WALK1LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The no-shift shrink perturbed active staging even on the corrected v760 runtime: scene/loop `1770/1478 -> 1780/1488`, target `1431 -> 1428`, overrun `47 -> 60`, blocking `64 -> 83`, refill `20 -> 31`, loop reads `60 -> 61`, and due misses `11 -> 12`; pack LBA and PS-EXE bucket stayed fixed. | Close W1-low frame65 no-shift shrinking under the v760 baseline. The safe no-shift lane remains frames `51`, `49`, `47`, `61`, `62`, `58`, `45`, `37`, and `35`; next candidates should move to frame43/frame41-style earlier isolated probes or generated scheduler/read-plan ownership rather than the frame65 boundary. |
| 2026-05-14 | `walkstuf1-low-frame43-inplace-v762` | `ps1: shrink walkstuf1 low frame43 payload in place` | Continue the earlier isolated W1-low no-shift lane after frame65 closed. Frame `43` was the largest remaining unclosed candidate away from the rejected post-58/post-65 boundary and reduced entry-sector coverage `3 -> 2` without moving downstream offsets. | Rewrote only frame `43` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame43-inplace-v762/summary.json`; speed-gate artifact with `--require-improvement`: `scratch/ps1-perf-iterate/walkstuf1-low-frame43-inplace-v762-focused/20260514-101227-4160114/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame43-inplace-v762-workreduction/20260514-101342-4168422/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `844668 -> 841324` (`3344` bytes), and the selected entry shrinks `5555 -> 2211` with entry-sector coverage `3 -> 2`. | Keep v762 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame41/frame39/56/57 only under exact-flat gating, but the row still needs scheduler/read-plan conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame41-inplace-v763` | `ps1: shrink walkstuf1 low frame41 payload in place` | Continue the earlier isolated W1-low no-shift lane on top of v762. Frame `41` ranked as the next large unclosed candidate away from the rejected post-58/post-65 boundary and reduces entry-sector coverage `3 -> 2` without moving downstream offsets. | Rewrote only frame `41` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame41-inplace-v763/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame41-inplace-v763-workreduction/20260514-101952-13308/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `841324 -> 838090` (`3234` bytes), and the selected entry shrinks `5526 -> 2292` with entry-sector coverage `3 -> 2`. | Keep v763 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame39/56/57 only under exact-flat gating, but the row still needs scheduler/read-plan conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame39-inplace-v764` | Log-only; pack restored and PS1 rebuilt. | Try the next largest earlier isolated W1-low no-shift candidate after v763. Frame `39` had `3111` removable bytes and modeled entry-sector coverage `4 -> 3`, so it was the highest-upside unclosed probe before the known post-58/post-65 cadence-sensitive area. | Rewrote only frame `39` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file footprint, pack LBA/sectors, and the PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame39-inplace-v764/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame39-inplace-v764-workreduction/20260514-103014-77187/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame39-inplace-v764/WALK1LOW.before.FG2` and byte-compared back to HEAD before continuing. | Failed/no promotion. The no-shift shrink regressed scene/loop/target `1770/1478/1431 -> 1787/1495/1424`, overrun `47 -> 71`, blocking `64 -> 99`, refill `20 -> 33`, loop reads `60 -> 64`, and due misses `11 -> 14`; pack LBA and PS-EXE bucket stayed fixed. | Close W1-low frame39 no-shift shrinking under the v763 baseline. The accepted no-shift lane is now frames `51`, `49`, `47`, `61`, `62`, `58`, `45`, `37`, `35`, `43`, and `41`; continue with frame56/frame57-style probes only under exact-flat gating or switch to generated scheduler/read-plan ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame56-inplace-v765` | Log-only; pack restored and PS1 rebuilt. | Try the largest remaining unclosed sector-saving W1-low no-shift candidate after frame39 closed. Frame `56` had `3101` removable bytes and modeled entry-sector coverage `2 -> 1`, so this checked whether a smaller post-left singleton could still be safe despite adjacent frame55/59/60/63/65 closures. | Rewrote only frame `56` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file footprint, pack LBA/sectors, and the PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame56-inplace-v765/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame56-inplace-v765-workreduction/20260514-103251-94185/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame56-inplace-v765/WALK1LOW.before.FG2` and byte-compared back to the v763 pack before continuing. | Failed/no promotion. The no-shift shrink regressed scene/loop/target `1770/1478/1431 -> 1776/1484/1432`, overrun `47 -> 52`, blocking `64 -> 72`, loop reads `60 -> 61`, and due misses `11 -> 12`; refill stayed flat at `20`, and pack LBA/PS-EXE bucket stayed fixed. | Close W1-low frame56 no-shift shrinking under the v763 baseline. This confirms the post-55 area remains cadence-sensitive even for a one-sector singleton; try frame57 once for symmetry, then prefer earlier safe islands or generated scheduler/read-plan ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame57-inplace-v766` | `ps1: shrink walkstuf1 low frame57 payload in place` | Try the adjacent singleton after frame56 failed, because frame57 has nearly identical byte upside but a separate read cadence. Frame `57` had `3089` removable bytes and modeled entry-sector coverage `2 -> 1`, making it the next largest unclosed sector-saving no-shift candidate. | Rewrote only frame `57` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame57-inplace-v766/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame57-inplace-v766-workreduction/20260514-103515-109771/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `838090 -> 835001` (`3089` bytes), and the selected entry shrinks `3409 -> 320` with entry-sector coverage `2 -> 1`. | Keep v766 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame33/66/67 only under exact-flat gating, but the row still needs scheduler/read-plan conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame33-inplace-v767` | `ps1: shrink walkstuf1 low frame33 payload in place` | Continue the earlier isolated W1-low no-shift lane after frame57 promoted. Frame `33` was the next largest unclosed sector-saving candidate before the post-55 cadence-sensitive region, with `2626` removable bytes and modeled entry-sector coverage `4 -> 3`. | Rewrote only frame `33` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame33-inplace-v767/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame33-inplace-v767-workreduction/20260514-104250-156840/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `835001 -> 832375` (`2626` bytes), and the selected entry shrinks `6760 -> 4134` with entry-sector coverage `4 -> 3`. | Keep v767 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame66/67/68 only under exact-flat gating, but the row still needs scheduler/read-plan conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame66-inplace-v768` | Log-only; pack restored and PS1 rebuilt. | Test the largest remaining post-65 no-shift candidate after v767. Frame `66` had `2579` removable bytes and modeled entry-sector coverage `3 -> 2`, but sits in the same cadence-sensitive region that already rejected frame65. | Rewrote only frame `66` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file footprint, pack LBA/sectors, and the PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame66-inplace-v768/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame66-inplace-v768-workreduction/20260514-105016-202836/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame66-inplace-v768/WALK1LOW.before.FG2` and byte-compared back to HEAD before continuing. | Failed/no promotion. The no-shift shrink regressed scene/loop/target `1770/1478/1431 -> 1780/1488/1429`, overrun `47 -> 59`, blocking `64 -> 82`, refill `20 -> 30`, loop reads `60 -> 61`, and due misses `11 -> 12`; pack LBA and PS-EXE bucket stayed fixed. | Close W1-low frame66 no-shift shrinking under the v767 baseline. The post-65 boundary remains unsafe for singleton entry-size shrinkage; continue with frame67/68 only if we want direct symmetry checks, otherwise shift to earlier frame32/133-style probes or generated scheduler/read-plan ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame67-inplace-v769` | `ps1: shrink walkstuf1 low frame67 payload in place` | Run the direct symmetry check after frame66 failed, because frame67 is adjacent but has its own read cadence. Frame `67` had `2463` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `67` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame67-inplace-v769/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame67-inplace-v769-workreduction/20260514-105245-219100/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, active loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, and due misses `11`; pack LBA/sectors and PS-EXE bucket stay fixed. Active payload drops `832375 -> 829912` (`2463` bytes), and the selected entry shrinks `4756 -> 2293` with entry-sector coverage `3 -> 2`. | Keep v769 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2708%` over target / `99.7337%` target speed and raw signed remains `-0.4963%` / `100.5160%`; methodology totals remain about `17.13` public over-target points removed and `12.63` public target-speed points gained. Continue with frame68/69/32 only under exact-flat gating, but the row still needs scheduler/read-plan conversion to move above 99%. |
| 2026-05-14 | `walkstuf1-low-frame68-inplace-v770` | `ps1: shrink walkstuf1 low frame68 payload in place` | Continue direct post-65 symmetry checks after frame67 passed and frame66 failed. Frame `68` had `2186` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `68` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame68-inplace-v770/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame68-inplace-v770-workreduction/20260514-110926-321456/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `829912 -> 827726` (`2186` bytes), and the selected entry shrinks `4633 -> 2447` with entry-sector coverage `3 -> 2`. | Keep v770 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame69/32/133 only under exact-flat gating. |
| 2026-05-14 | `walkstuf1-low-frame69-inplace-v771` | `ps1: shrink walkstuf1 low frame69 payload in place` | Continue direct post-65 symmetry checks after frames `67` and `68` passed and frame `66` failed. Frame `69` had `2030` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `69` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame69-inplace-v771/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame69-inplace-v771-workreduction/20260514-115022-558481/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `827726 -> 825696` (`2030` bytes), and the selected entry shrinks `4779 -> 2749` with entry-sector coverage `3 -> 2`. | Keep v771 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame32/133-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame32-inplace-v772` | `ps1: shrink walkstuf1 low frame32 payload in place` | Continue the earlier isolated W1-low no-shift lane after the post-65 frame67/68/69 run passed. Frame `32` was the next-largest remaining candidate, with `1981` removable bytes and modeled entry-sector coverage `5 -> 4`. | Rewrote only frame `32` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame32-inplace-v772/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame32-inplace-v772-workreduction/20260514-115748-600413/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `825696 -> 823715` (`1981` bytes), and the selected entry shrinks `8210 -> 6229` with entry-sector coverage `5 -> 4`. | Keep v772 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame133-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame133-inplace-v773` | `ps1: shrink walkstuf1 low frame133 payload in place` | Continue the late-mid no-shift lane after frame32 passed. Frame `133` was the next-largest remaining candidate, with `1942` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `133` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame133-inplace-v773/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame133-inplace-v773-workreduction/20260514-120442-639343/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `823715 -> 821773` (`1942` bytes), and the selected entry shrinks `4324 -> 2382` with entry-sector coverage `3 -> 2`. | Keep v773 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame5-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame5-inplace-v774` | `ps1: shrink walkstuf1 low frame5 payload in place` | Continue the early no-shift lane after frame133 passed. Frame `5` was the next-largest remaining candidate, with `1765` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `5` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame5-inplace-v774/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame5-inplace-v774-workreduction/20260514-121115-676249/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `821773 -> 820008` (`1765` bytes), and the selected entry shrinks `4219 -> 2454` with entry-sector coverage `3 -> 2`. | Keep v774 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame141-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame141-inplace-v775` | `ps1: shrink walkstuf1 low frame141 payload in place` | Continue the late-mid no-shift lane after frame5 passed. Frame `141` was the next-largest remaining candidate, with `1693` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `141` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame141-inplace-v775/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame141-inplace-v775-workreduction/20260514-122938-777910/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `820008 -> 818315` (`1693` bytes), and the selected entry shrinks `4267 -> 2574` with entry-sector coverage `3 -> 2`. | Keep v775 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame70-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame70-inplace-v776` | `ps1: shrink walkstuf1 low frame70 payload in place` | Continue the post-65 no-shift symmetry lane after frame141 passed. Frame `70` was the next-largest remaining candidate, with `1660` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `70` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame70-inplace-v776/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame70-inplace-v776-workreduction/20260514-123955-835952/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `818315 -> 816655` (`1660` bytes), and the selected entry shrinks `4874 -> 3214` with entry-sector coverage `3 -> 2`. | Keep v776 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame30-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame30-inplace-v777` | `ps1: shrink walkstuf1 low frame30 payload in place` | Continue the early no-shift lane after frame70 passed. Frame `30` was the next-largest remaining candidate, with `1642` removable bytes and modeled entry-sector coverage `4 -> 3`. | Rewrote only frame `30` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame30-inplace-v777/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame30-inplace-v777-workreduction/20260514-124737-879499/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `816655 -> 815013` (`1642` bytes), and the selected entry shrinks `6876 -> 5234` with entry-sector coverage `4 -> 3`. | Keep v777 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame132-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame132-inplace-v778` | Log-only; pack restored and PS1 rebuilt. | Test the next largest no-shift candidate after v777. Frame `132` had `1435` removable bytes and modeled entry-sector coverage `3 -> 2`, but it sits in a cadence-sensitive mid cluster. | Temporarily rewrote only frame `132` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size, pack LBA/sectors, and PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame132-inplace-v778/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame132-inplace-v778-workreduction/20260514-125544-925243/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame132-inplace-v778/WALK1LOW.before.FG2` before continuing. | Failed/no promotion. Scene/loop/target stayed exact-flat at `1770/1478/1431`, but CD counters regressed: blocking `64 -> 65` and prefetch overrun `20 -> 21`; loop reads/read VBlanks stayed `60/272`, and due stayed `11`. | Close frame132 no-shift shrinking under the v777 baseline. Public rollup and methodology totals remain unchanged. Continue with frame6/frame71/frame72-style probes only under exact-flat gating, but prioritize generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame6-inplace-v779` | `ps1: shrink walkstuf1 low frame6 payload in place` | Continue the early singleton no-shift lane after frame132 failed and was restored. Frame `6` was the next-largest remaining candidate, with `1385` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `6` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame6-inplace-v779/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame6-inplace-v779-workreduction/20260514-125828-941137/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `815013 -> 813628` (`1385` bytes), and the selected entry shrinks `4348 -> 2963` with entry-sector coverage `3 -> 2`. | Keep v779 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame71-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame71-inplace-v780` | `ps1: shrink walkstuf1 low frame71 payload in place` | Continue the post-65 symmetry lane after frame6 passed and frame132 was closed. Frame `71` was the next-largest remaining candidate, with `1375` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `71` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame71-inplace-v780/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame71-inplace-v780-workreduction/20260514-131105-1011005/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `813628 -> 812253` (`1375` bytes), and the selected entry shrinks `4726 -> 3351` with entry-sector coverage `3 -> 2`. | Keep v780 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame72-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame72-inplace-v781` | `ps1: shrink walkstuf1 low frame72 payload in place` | Continue the post-65 symmetry lane after frame71 passed exact-flat. Frame `72` was the next-largest remaining candidate, with `1263` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `72` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame72-inplace-v781/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame72-inplace-v781-workreduction/20260514-132148-1071406/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `812253 -> 810990` (`1263` bytes), and the selected entry shrinks `4795 -> 3532` with entry-sector coverage `3 -> 2`. | Keep v781 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame142-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame142-inplace-v782` | `ps1: shrink walkstuf1 low frame142 payload in place` | Continue the late-mid no-shift lane after frame72 passed exact-flat. Frame `142` was the next-largest remaining candidate, with `1245` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `142` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame142-inplace-v782/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame142-inplace-v782-workreduction/20260514-132927-1114146/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `810990 -> 809745` (`1245` bytes), and the selected entry shrinks `4247 -> 3002` with entry-sector coverage `3 -> 2`. | Keep v782 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame130-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame73-inplace-v783` | `ps1: shrink walkstuf1 low frame73 payload in place` | Continue the post-65 symmetry lane after frame142 passed exact-flat. Frame `73` was the next-largest unclosed candidate after closed frame39/55/56/59/60/63/65/66/132 paths, with `1132` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `73` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame73-inplace-v783/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame73-inplace-v783-workreduction/20260514-135100-1231755/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `809745 -> 808613` (`1132` bytes), and the selected entry shrinks `4755 -> 3623` with entry-sector coverage `3 -> 2`. | Keep v783 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame131/frame74-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame131-inplace-v784` | `ps1: shrink walkstuf1 low frame131 payload in place` | Continue the late-mid no-shift lane after frame73 passed exact-flat. Frame `131` was the next-largest unclosed candidate, with `1107` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `131` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame131-inplace-v784/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame131-inplace-v784-workreduction/20260514-140114-1288759/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `808613 -> 807506` (`1107` bytes), and the selected entry shrinks `4411 -> 3304` with entry-sector coverage `3 -> 2`. | Keep v784 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame74/frame19-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame74-inplace-v785` | `ps1: shrink walkstuf1 low frame74 payload in place` | Continue the post-65 no-shift lane after frame131 passed exact-flat. Frame `74` was the next-largest unclosed candidate, with `1011` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `74` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame74-inplace-v785/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame74-inplace-v785-workreduction/20260514-140919-1334075/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `807506 -> 806495` (`1011` bytes), and the selected entry shrinks `4850 -> 3839` with entry-sector coverage `3 -> 2`. | Keep v785 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame19/frame28-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame19-inplace-v786` | `ps1: shrink walkstuf1 low frame19 payload in place` | Continue the early no-shift lane after frame74 passed exact-flat. Frame `19` was the next-largest unclosed candidate, with `980` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `19` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame19-inplace-v786/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame19-inplace-v786-workreduction/20260514-141651-1376409/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `806495 -> 805515` (`980` bytes), and the selected entry shrinks `5059 -> 4079` with entry-sector coverage `3 -> 2`. | Keep v786 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame28/frame138-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame28-inplace-v787` | `ps1: shrink walkstuf1 low frame28 payload in place` | Retest frame `28` with the narrow no-shift tail-size shrink, not the old resident-hole/D4 relocation that was closed by v522. Frame `28` had `923` removable bytes and modeled entry-sector coverage `4 -> 3`. | Rewrote only frame `28` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame28-inplace-v787/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame28-inplace-v787-workreduction/20260514-143028-1451481/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `805515 -> 804592` (`923` bytes), and the selected entry shrinks `6764 -> 5841` with entry-sector coverage `4 -> 3`. | Keep v787 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame138/frame145-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame138-inplace-v788` | `ps1: shrink walkstuf1 low frame138 payload in place` | Continue the late-mid no-shift lane after frame28 passed exact-flat. Frame `138` was the next-largest unclosed candidate, with `912` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `138` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame138-inplace-v788/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame138-inplace-v788-workreduction/20260514-143813-1494779/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `804592 -> 803680` (`912` bytes), and the selected entry shrinks `4716 -> 3804` with entry-sector coverage `3 -> 2`. | Keep v788 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame145/frame75-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame145-inplace-v789` | `ps1: shrink walkstuf1 low frame145 payload in place` | Continue the no-shift sector-cut lane after frame138 passed exact-flat. Frame `145` was the next-largest unclosed candidate, with `895` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `145` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame145-inplace-v789/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame145-inplace-v789-workreduction/20260514-144747-1548125/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `803680 -> 802785` (`895` bytes), and the selected entry shrinks `4990 -> 4095` with entry-sector coverage `3 -> 2`. | Keep v789 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame75/frame76-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame75-inplace-v790` | `ps1: shrink walkstuf1 low frame75 payload in place` | Continue the no-shift sector-cut lane after frame145 passed exact-flat. Frame `75` was the next-largest unclosed candidate, with `882` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `75` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame75-inplace-v790/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame75-inplace-v790-workreduction/20260514-145524-1590690/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `802785 -> 801903` (`882` bytes), and the selected entry shrinks `4853 -> 3971` with entry-sector coverage `3 -> 2`. | Keep v790 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame76/frame77-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame76-inplace-v791` | `ps1: shrink walkstuf1 low frame76 payload in place` | Continue the no-shift sector-cut lane after frame75 passed exact-flat. Frame `76` was the next-largest unclosed candidate, with `800` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `76` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame76-inplace-v791/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame76-inplace-v791-workreduction/20260514-150237-1630977/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `801903 -> 801103` (`800` bytes), and the selected entry shrinks `4822 -> 4022` with entry-sector coverage `3 -> 2`. | Keep v791 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame77/frame130-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `postrelease-heap-pin-rollback-v793` | `ps1: restore lazy cd and fg stream buffers` | The origin/main merge brought in a heap-fragmentation experiment that pinned a `256 KB` CD sector pool plus boot-time `192 KB` FG frame and `64 KB` scratch buffers. Immediately after the `v0.8.10-ps1` release, W1-low no longer produced valid baseline metrics: the restored v791 image emitted `JCSKIP clean-rect-alloc-failed` after one entry (`loop_vb=0`, `target_vb=2`). Disabling only the boot-time FG stream pin restored rendering but still regressed W1-low timing (`1478 -> 1482`, blocking `64 -> 83`), so the static CD pool was also part of the timing regression. | Removed the static CD sector pool path, restored `ps1_streamRead*` sector buffers to per-read `malloc/free`, kept FG frame/scratch lazy at boot, and removed the unused prefetch pre-prime branch. Inconclusive/failing artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-frame77-inplace-v792-workreduction/20260514-151829-1722776/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-v791-postrelease-baseline-check/20260514-151958-1731631/summary.json`; partial rollback artifact: `scratch/ps1-perf-iterate/walkstuf1-low-lazy-streams-postrelease-fix/20260514-152141-1741701/summary.json`; accepted rollback artifact: `scratch/ps1-perf-iterate/walkstuf1-low-restore-lazy-cd-buffers-v793/20260514-152341-1753206/summary.json`. | Promoted as a regression fix. W1-low returns exact-flat to the v791 baseline: scene/loop/target `1770/1478/1431`, overrun `47`, blocking/refill `64/20`, loop reads/read VBlanks `60/272`, due `11`, and pack LBA/PS-EXE bucket unchanged. The initial frame77 v792 no-shift probe is not a valid candidate result because it ran against the post-release heap-pin regression and should be retried only from the restored lazy-buffer baseline. | Keep lazy CD/FG stream allocation as the release baseline until a fragmentation fix proves both long-soak stability and no focused W1-low timing regression. Heap-fragmentation work should move toward scene-bound arena reset or measured per-scene teardown, not permanent boot/static reservations that starve clean-rect allocation. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame77-inplace-v794` | `ps1: shrink walkstuf1 low frame77 payload in place` | Retry frame `77` after the v793 lazy-buffer rollback restored the release baseline. The earlier v792 attempt was invalid because it ran against the bad post-release heap-pin state. Frame `77` was the next-largest remaining no-shift candidate, with `729` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `77` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame77-inplace-v794/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame77-inplace-v794-workreduction/20260514-152935-1787930/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `801103 -> 800374` (`729` bytes), and the selected entry shrinks `4762 -> 4033` with entry-sector coverage `3 -> 2`. | Keep v794 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame130/frame83-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame130-inplace-v795` | `ps1: shrink walkstuf1 low frame130 payload in place` | Continue the late-mid no-shift sector-cut lane after frame77 passed exact-flat. Frame `130` was the next-largest unclosed candidate, with `680` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `130` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame130-inplace-v795/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame130-inplace-v795-workreduction/20260514-153635-1826834/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `800374 -> 799694` (`680` bytes), and the selected entry shrinks `4593 -> 3913` with entry-sector coverage `3 -> 2`. | Keep v795 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue with frame83/frame135-style no-shift probes only under exact-flat gating, then shift to generated scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame83-inplace-v796` | Log-only; pack restored and PS1 rebuilt. | Test the next-largest remaining W1-low no-shift sector-cut candidate after v795. Frame `83` looked mechanically attractive because it would trim `634` bytes and reduce modeled entry-sector coverage `3 -> 2` without moving downstream offsets. | Rewrote only frame `83` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame83-inplace-v796/summary.json`; failed focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame83-inplace-v796-workreduction/20260514-154917-1898181/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame83-inplace-v796/WALK1LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The local sector cut perturbed visible/refill cadence: scene/loop/target regressed `1770/1478/1431 -> 1776/1484/1428`, overrun `47 -> 56`, blocking `64 -> 81`, refill `20 -> 28`, loop reads improved `60 -> 59`, and due misses regressed `11 -> 13`. | Close W1-low frame83 no-shift shrink on the v795 baseline. Continue with frame135 or another sector-changing candidate only under exact-flat gating; if late-mid sector cuts keep increasing visible debt, move to scheduler/read-plan ownership for actual speed conversion. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame135-inplace-v797` | `ps1: shrink walkstuf1 low frame135 payload in place` | Continue the late-mid no-shift lane after frame83 failed and frame135 remained the next sector-changing candidate. Frame `135` had `565` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `135` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame135-inplace-v797/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame135-inplace-v797-workreduction/20260514-155142-1912118/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `799694 -> 799129` (`565` bytes), and the selected entry shrinks `4415 -> 3850` with entry-sector coverage `3 -> 2`. | Keep v797 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue only with candidates that pass exact-flat gating; the frame83 miss suggests late-mid sector cuts need scheduler/read-plan ownership before they convert to speed. |
| 2026-05-14 | `walkstuf1-low-frame1-inplace-v798` | `ps1: shrink walkstuf1 low frame1 payload in place` | Continue the no-shift lane after frame135 passed exact-flat. Frame `1` was a remaining sector-changing candidate, with `417` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `1` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame1-inplace-v798/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame1-inplace-v798-workreduction/20260514-155643-1940198/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `799129 -> 798712` (`417` bytes), and the selected entry shrinks `4195 -> 3778` with entry-sector coverage `3 -> 2`. | Keep v798 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue only with candidates that pass exact-flat gating, then shift to scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame87-inplace-v799` | Log-only; pack restored and PS1 rebuilt. | Test the next remaining W1-low no-shift sector-cut candidate after v798. Frame `87` looked mechanically safe because it would trim `292` bytes and reduce modeled entry-sector coverage `3 -> 2` without moving downstream offsets. | Rewrote only frame `87` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame87-inplace-v799/summary.json`; failed focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame87-inplace-v799-workreduction/20260514-160349-1980148/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame87-inplace-v799/WALK1LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Loop time stayed flat and target moved favorably, but visible CD blocking regressed: scene/loop/target changed `1770/1478/1431 -> 1770/1478/1432`, overrun `47 -> 46`, blocking `64 -> 65`, refill stayed `20`, loop reads/read VBlanks stayed `60/272`, and due stayed `11`. | Close W1-low frame87 no-shift shrink on the v798 baseline. Do not trade a same-loop target movement for worse blocking in this lane; continue with frame88/frame90/frame3 or scheduler/read-plan ownership under exact-flat gating. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame88-inplace-v800` | `ps1: shrink walkstuf1 low frame88 payload in place` | Continue the no-shift sector-cut lane after frame87 failed visible-blocking identity and frame88 remained a clean sector-saving candidate. Frame `88` had `277` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `88` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame88-inplace-v800/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame88-inplace-v800-workreduction/20260514-160617-1994502/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `798712 -> 798435` (`277` bytes), and the selected entry shrinks `4347 -> 4070` with entry-sector coverage `3 -> 2`. | Keep v800 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. Continue only with candidates that pass exact-flat gating, then shift to scheduler/read-plan ownership for actual speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame90-inplace-v801` | `ps1: shrink walkstuf1 low frame90 payload in place` | Continue the no-shift sector-cut lane after frame88 passed exact-flat. Frame `90` was the remaining small sector-saving candidate, with `203` removable bytes and modeled entry-sector coverage `2 -> 1`. | Rewrote only frame `90` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame90-inplace-v801/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame90-inplace-v801-workreduction/20260514-161022-2017565/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `798435 -> 798232` (`203` bytes), and the selected entry shrinks `2241 -> 2038` with entry-sector coverage `2 -> 1`. | Keep v801 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. The remaining no-shift sector cuts are now marginal; continue only exact-flat candidates or pivot to scheduler/read-plan ownership for speed conversion. |
| 2026-05-14 | `walkstuf1-low-frame3-inplace-v802` | `ps1: shrink walkstuf1 low frame3 payload in place` | Close the remaining obvious W1-low no-shift sector-saving candidate after frame90 passed exact-flat. Frame `3` had `153` removable bytes and modeled entry-sector coverage `3 -> 2`. | Rewrote only frame `3` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and PS-EXE bucket `217088`. Transform summary: `scratch/walkstuf1-low-frame3-inplace-v802/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame3-inplace-v802-workreduction/20260514-161419-2039939/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1770`, loop/target `1478/1431`, overrun `47`, blocking/refill `64/20`, reads/read VBlanks `60/272`, and due `11`. Active payload drops `798232 -> 798079` (`153` bytes), and the selected entry shrinks `4225 -> 4072` with entry-sector coverage `3 -> 2`. | Keep v802 as the new W1-low work-volume baseline. Rollup/methodology totals unchanged. The remaining single-frame tail trims are non-sector or closed failures; pivot toward scheduler/read-plan ownership or a broader generated pack-shape change for real speed conversion. |
| 2026-05-14 | `building2-high-frame89-inplace-v803` | Log-only; pack restored and PS1 rebuilt. | After W1-low no-shift sector candidates were exhausted, scan the remaining yellow packs for the same in-place entry-size shrink opportunity. BUILDING2 high frame `89` was the largest candidate, trimming `4510` bytes and modeled entry-sector coverage `5 -> 3`. | Rewrote only frame `89` in `generated/ps1/foreground/BUILDING2.FG2`, preserving file size `1303332`, pack LBA/sectors `6181/637`, and PS-EXE bucket `217088`. Transform summary: `scratch/building2-high-frame89-inplace-v803/summary.json`; invalid exploratory run with a mismatched baseline label: `scratch/ps1-perf-iterate/building2-high-frame89-inplace-v803-workreduction/20260514-161926-2068702/summary.json`; valid matched-label failed gate: `scratch/ps1-perf-iterate/building2-high-frame89-inplace-v803-workreduction-matchlabel/20260514-162043-2076350/summary.json`. The pack was restored from `scratch/building2-high-frame89-inplace-v803/BUILDING2.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The matched-label gate saved one loop read but regressed the actual bottlenecks: scene/loop/target moved `1602/1351/1311 -> 1603/1352/1308`, overrun `40 -> 44`, blocking `54 -> 57`, refill `18 -> 22`, loop reads `58 -> 57`, and due stayed `7`. | Close BUILDING2 high frame89 no-shift entry-size shrink on the v703 baseline. The tail shrink changes read timing but steals cadence; future B2-high work should use generated ownership around the accepted `206..230` shape or a pack transform that proves timing before changing entry sizes. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame34-inplace-v804` | `ps1: shrink building4 low frame34 payload in place` | Pivot the no-shift sector-cut scan to BUILDING4 low after B2-high frame89 failed. BUILDING4 low had already tolerated a same-speed frame291 shrink, and frame `34` was the largest remaining candidate, trimming `5116` bytes with modeled entry-sector coverage `5 -> 2`. | Rewrote only frame `34` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame34-inplace-v804/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame34-inplace-v804-workreduction/20260514-162319-2091321/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `849109 -> 843993` (`5116` bytes), and the selected entry shrinks `8455 -> 3339` with entry-sector coverage `5 -> 2`. | Keep v804 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue B4-low sector-saving frames only under exact-flat gating, then look for static-mask/upload work if timing remains yellow. |
| 2026-05-14 | `building4-low-frame31-inplace-v805` | `ps1: shrink building4 low frame31 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame34 passed exact-flat. Frame `31` was the next-largest candidate, trimming `5064` bytes with modeled entry-sector coverage `4 -> 1`. | Rewrote only frame `31` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame31-inplace-v805/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame31-inplace-v805-workreduction/20260514-162810-2122606/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `843993 -> 838929` (`5064` bytes), and the selected entry shrinks `6672 -> 1608` with entry-sector coverage `4 -> 1`. | Keep v805 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue large B4-low sector-saving frames under exact-flat gating. |
| 2026-05-14 | `building4-low-frame290-inplace-v806` | `ps1: shrink building4 low frame290 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame31 passed exact-flat. Frame `290` was the next-largest candidate, trimming `4911` bytes with modeled entry-sector coverage `5 -> 2`. | Rewrote only frame `290` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame290-inplace-v806/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame290-inplace-v806-workreduction/20260514-163458-2160872/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `838929 -> 834018` (`4911` bytes), and the selected entry shrinks `8508 -> 3597` with entry-sector coverage `5 -> 2`. | Keep v806 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue large B4-low sector-saving frames under exact-flat gating, then pivot to timing-bearing scheduler/upload work if payload shrinks stop moving the yellow row. |
| 2026-05-14 | `building4-low-frame32-inplace-v807` | `ps1: shrink building4 low frame32 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame290 passed exact-flat. Frame `32` was the next-largest candidate, trimming `4896` bytes with modeled entry-sector coverage `4 -> 1`. | Rewrote only frame `32` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame32-inplace-v807/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame32-inplace-v807-workreduction/20260514-164029-2192613/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `834018 -> 829122` (`4896` bytes), and the selected entry shrinks `6656 -> 1760` with entry-sector coverage `4 -> 1`. | Keep v807 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue large B4-low sector-saving frames under exact-flat gating while the row remains yellow. |
| 2026-05-14 | `building4-low-frame35-inplace-v808` | `ps1: shrink building4 low frame35 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame32 passed exact-flat. Frame `35` was the next-largest candidate, trimming `4574` bytes with modeled entry-sector coverage `5 -> 3`. | Rewrote only frame `35` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame35-inplace-v808/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame35-inplace-v808-workreduction/20260514-164506-2218620/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `829122 -> 824548` (`4574` bytes), and the selected entry shrinks `8770 -> 4196` with entry-sector coverage `5 -> 3`. | Keep v808 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue exact-flat B4-low sector-saving frames while they remain clean; speed conversion still needs timing-bearing read/scheduler/upload work. |
| 2026-05-14 | `building4-low-frame30-inplace-v809` | Log-only; pack restored and PS1 rebuilt. | Continue the BUILDING4 low no-shift sector-cut lane after frame35 passed exact-flat. Frame `30` was the next-largest candidate, trimming `4501` bytes with modeled entry-sector coverage `3 -> 1`. | Rewrote only frame `30` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame30-inplace-v809/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/building4-low-frame30-inplace-v809-workreduction/20260514-164959-2246029/building4-low-canary/perf-summary.json`. The pack was restored from `scratch/building4-low-frame30-inplace-v809/BUIL4LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The run exited `137` before emitting `JCPERF2`; the parsed gate failed with `missing_jcperf2`, so no timing/CD metrics were valid for promotion. The log shows boot selection reached `fgpilot building4 lowtide 1` before the later timeout/kill. | Close frame30 as unsafe on the v808 baseline. The extreme `4650 -> 149` shrink likely removes timing/stream structure needed for this early scene phase; continue with less aggressive exact-flat candidates and skip frame30 unless a decoder/stream invariant checker explains the missing metrics. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame33-inplace-v810` | Log-only; pack restored and PS1 rebuilt. | Continue the BUILDING4 low no-shift sector-cut lane after frame30 failed structurally. Frame `33` offered a less extreme shrink, trimming `4412` bytes with modeled entry-sector coverage `4 -> 2`. | Rewrote only frame `33` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame33-inplace-v810/summary.json`; failed no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame33-inplace-v810-workreduction/20260514-165211-2259698/summary.json`. The pack was restored from `scratch/building4-low-frame33-inplace-v810/BUIL4LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The focused gate emitted valid metrics but regressed timing/CD: scene/loop `3128/2853 -> 3130/2855`, overrun `37 -> 39`, blocking `40 -> 45`, refill `34 -> 39`, while loop reads stayed `30` and due stayed `1`. | Close frame33 on the v808 baseline. This confirms not every sector-saving no-shift shrink is phase-neutral; prefer the remaining later frames or candidates with lower early-phase cadence risk, and keep exact-flat gating strict. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame36-inplace-v811` | `ps1: shrink building4 low frame36 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame33 failed phase cadence. Frame `36` was the next-largest remaining candidate, trimming `4001` bytes with modeled entry-sector coverage `5 -> 3`. | Rewrote only frame `36` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame36-inplace-v811/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame36-inplace-v811-workreduction/20260514-165504-2276195/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `824548 -> 820547` (`4001` bytes), and the selected entry shrinks `8823 -> 4822` with entry-sector coverage `5 -> 3`. | Keep v811 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue with later-frame B4-low candidates next, since frame30/33 showed the early cluster can be phase-sensitive. |
| 2026-05-14 | `building4-low-frame289-inplace-v812` | Log-only; pack restored and PS1 rebuilt. | Continue the BUILDING4 low no-shift sector-cut lane with a later-frame candidate after frame36 passed exact-flat. Frame `289` trimmed `3738` bytes with modeled entry-sector coverage `5 -> 3`. | Rewrote only frame `289` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame289-inplace-v812/summary.json`; failed no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame289-inplace-v812-workreduction/20260514-165940-2302013/summary.json`. The pack was restored from `scratch/building4-low-frame289-inplace-v812/BUIL4LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Valid metrics regressed scene/loop `3128/2853 -> 3133/2858`, overrun `37 -> 41`, blocking `40 -> 46`, and due `1 -> 2`; refill improved `34 -> 31`, but visible timing/blocking failed the strict gate. | Close frame289 on the v811 baseline. Later-frame payload shrinking is not automatically safe either; continue with smaller/lower-risk candidates or pivot to timing-bearing scheduler/upload work once sector-saving trims stop passing exact-flat. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame37-inplace-v813` | `ps1: shrink building4 low frame37 payload in place` | Try the next smaller early-cluster candidate after frame36 passed exact-flat and frame289 failed. Frame `37` trims `2889` bytes with modeled entry-sector coverage `5 -> 3`. | Rewrote only frame `37` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame37-inplace-v813/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame37-inplace-v813-workreduction/20260514-170235-2319539/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `820547 -> 817658` (`2889` bytes), and the selected entry shrinks `8828 -> 5939` with entry-sector coverage `5 -> 3`. | Keep v813 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Smaller adjacent trims can still be phase-neutral; continue exact-flat candidates, but treat large frame30/33/289-style shifts as closed unless a new timing model justifies retry. |
| 2026-05-14 | `building4-low-frame288-inplace-v814` | `ps1: shrink building4 low frame288 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame37 passed and frame289 failed. Frame `288` trims `2815` bytes with modeled entry-sector coverage `5 -> 3`. | Rewrote only frame `288` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame288-inplace-v814/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame288-inplace-v814-workreduction/20260514-170804-2350171/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `817658 -> 814843` (`2815` bytes), and the selected entry shrinks `8681 -> 5866` with entry-sector coverage `5 -> 3`. | Keep v814 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Continue exact-flat candidates individually; adjacent frames can diverge, so do not infer frame289 safety from frame288. |
| 2026-05-14 | `building4-low-frame287-inplace-v815` | `ps1: shrink building4 low frame287 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame288 passed exact-flat. Frame `287` trims `1933` bytes with modeled entry-sector coverage `5 -> 4`. | Rewrote only frame `287` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame287-inplace-v815/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame287-inplace-v815-workreduction/20260514-171253-2378014/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `814843 -> 812910` (`1933` bytes), and the selected entry shrinks `8576 -> 6643` with entry-sector coverage `5 -> 4`. | Keep v815 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. The remaining same-speed B4-low tail trims are now small; continue exact-flat candidates or pivot to timing-bearing scheduler/upload work. |
| 2026-05-14 | `building4-low-frame39-inplace-v816` | `ps1: shrink building4 low frame39 payload in place` | Continue the BUILDING4 low no-shift sector-cut lane after frame287 passed exact-flat. Frame `39` trims `1933` bytes with modeled entry-sector coverage `5 -> 4`. | Rewrote only frame `39` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size `1714154`, pack LBA/sectors `9118/837`, and PS-EXE bucket `217088`. Transform summary: `scratch/building4-low-frame39-inplace-v816/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame39-inplace-v816-workreduction/20260514-171736-2404449/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `812910 -> 810977` (`1933` bytes), and the selected entry shrinks `8833 -> 6900` with entry-sector coverage `5 -> 4`. | Keep v816 as the new BUILDING4 low work-volume baseline. Rollup/methodology totals unchanged. Remaining B4-low no-shift trims are small; prefer timing-bearing scheduler/upload experiments after closing the last obvious exact-flat entries. |
| 2026-05-14 | `walkstuf1-low-rg394-410-v817` | `ps1: add walkstuf1 low retained read group` | Pivot from same-speed payload shaving to timing-bearing W1-low CD ownership. The current read-plan artifact had only one unclosed compatible candidate: low-tide sectors `394..410`, saving two reads with no overread but marked scheduler-owned because the visible gaps are short. | Added a low-tide-only WALKSTUF1 read-group table that preserves the existing shared/high groups and adds `{394, 410, 0}` only for low tide. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-group394-410-v817-probe/20260514-172546-2451666/summary.json`; pack layout remains fixed at LBA/sectors `25633/750`, and the PS-EXE bucket stays `217088`. | Promoted as a VBlank/CD win. Scene/loop/target improve `1770/1478/1431 -> 1769/1477/1431`, overrun `47 -> 46`, loop reads/read VBlanks `60/272 -> 58/266`, hidden read VBlanks `208 -> 202`, and skipped-no-slack drops `83 -> 81`; blocking/refill/due stay `64/20/11`, and correctness stays clean. | New rollup is `+0.2702%` public over target / `99.7342%` public target speed, raw signed `-0.4969%` / `100.5165%`, with bands unchanged at `117` green and `9` yellow. Methodology total is about `17.13` public over-target points removed and `+12.63` public target-speed points since the compact full-matrix baseline. Keep W1-low as top focus at `96.886%`, then retest the new `410..422` candidate or move to B2-high/W1-high scheduler-owned rows. |
| 2026-05-14 | `walkstuf1-low-rg410-422-v818` | Log-only; code reverted and PS1 rebuilt. | Retest the next W1-low scheduler-owned candidate after v817. The read-plan ranked low-tide sectors `410..422` as a 12-sector compatible group with `saved=1`, no overread, and low visible risk, so it was worth a binary focused gate. | Temporarily added `{410, 422, 0}` to the low-tide-only WALKSTUF1 read-group table on top of v817. Failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group410-422-v818-probe/20260514-173654-2515096/summary.json`. The code change was reverted, leaving v817 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The probe did save one active-loop read (`58 -> 57`) but gave the win back as visible timing debt: scene/loop/target regressed `1769/1477/1431 -> 1770/1478/1431` and overrun `46 -> 47`; blocking/refill/due stayed `64/20/11`. The strict `--require-improvement` gate failed on timing regression and no key metric improvement. | Close `410..422` as phase-negative on the v817 baseline. The remaining safe-looking W1-low read-plan candidates are now `190..202` and `273..285`; test them individually, or pivot to B2-high/W1-high if W1-low direct read groups keep trading saved reads for loop debt. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-rg190-202-v819` | Log-only; code reverted and PS1 rebuilt. | Try the next higher-scoring W1-low read-plan candidate after `410..422` failed. Sectors `190..202` were marked compatible with `saved=1`, no overread, and low visible risk, but they touch the region immediately before the existing `201..213` retained group. | Temporarily added `{190, 202, 0}` to the low-tide-only WALKSTUF1 read-group table on top of v817. Failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group190-202-v819-probe/20260514-173956-2532845/summary.json`. The code change was reverted, leaving v817 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The overlap/phase interaction was strongly negative: scene/loop/target regressed `1769/1477/1431 -> 1778/1486/1429`, overrun `46 -> 57`, blocking `64 -> 81`, refill `20 -> 29`, and due `11 -> 12`, while loop reads stayed at `58`. | Close `190..202` on the v817 baseline. Avoid candidates adjacent to existing retained groups unless the scheduler can account for overlap/phase side effects; test the non-overlapping `273..285` candidate next or pivot to B2-high/W1-high if W1-low read groups keep failing strict gates. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-rg273-285-v820` | Log-only; code reverted and PS1 rebuilt. | Test the remaining non-overlapping low-visible-risk W1-low read-plan candidate after `410..422` and `190..202` failed. Sectors `273..285` were a 12-sector group with `saved=1`, no overread, and no adjacency to the existing retained groups. | Temporarily added `{273, 285, 0}` to the low-tide-only WALKSTUF1 read-group table on top of v817. Failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group273-285-v820-probe/20260514-174225-2547407/summary.json`. The code change was reverted, leaving v817 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The focused run was exact-flat against v817: scene/loop/target stayed `1769/1477/1431`, overrun `46`, blocking/refill `64/20`, reads/read time `58/266`, and due `11`; the strict gate failed because no key metric improved. | Close `273..285` as a no-op on the v817 baseline. The cheap W1-low low-risk direct read groups are exhausted; next W1-low work needs higher-risk `285..297`/`297..309` validation, generated scheduler ownership, or a pivot to B2-high/W1-high timing-bearing rows. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-rg285-297-v821` | Log-only; code reverted and PS1 rebuilt. | Take one higher-upside W1-low validation shot after the low-risk direct groups closed. Sectors `285..297` had a modeled two-read save and medium visible risk, but the read-plan marked it scheduler-only/high-risk because of the tight internal cadence. | Temporarily added `{285, 297, 0}` to the low-tide-only WALKSTUF1 read-group table on top of v817. Failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group285-297-v821-probe/20260514-174450-2561751/summary.json`. The code change was reverted, leaving v817 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The run was exact-flat against v817: scene/loop/target stayed `1769/1477/1431`, overrun `46`, blocking/refill `64/20`, reads/read time `58/266`, and due `11`; the strict gate failed because no key metric improved. | Close hand-authored W1-low direct read-group probes for now. Further W1-low progress likely needs generated scheduler ownership, pack/upload work reduction, or deeper phase modeling; pivot to B2-high or W1-high for the next biggest under-99 timing-bearing opportunities. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `visitor3-low-terminal-pack-v824` | Log-only; low pack restored. | Test whether VISITOR3 low could avoid the closed D4/decode lane by moving existing terminal payloads byte-for-byte into the large in-file gap after frame `129`. The candidate kept `VIST3LOW.FG2` size, LBA/sectors, frame payload bytes, source code, and the PS-EXE bucket fixed while changing only frame table offsets. | Built a scratch applier under `scratch/visitor3-low-terminal-pack-v824/` and tested three binary splits against the fresh low baseline `scratch/ps1-perf-iterate/current-visitor3-refresh-v824/20260514-180214-2662503/summary.json`: `130` only (`scratch/ps1-perf-iterate/visitor3-low-terminal-pack-v824-130-focused/20260514-181229-2721628/summary.json`), `130,131` (`scratch/ps1-perf-iterate/visitor3-low-terminal-pack-v824-130-131-focused/20260514-181007-2707688/summary.json`), and `130,131,133,134` (`scratch/ps1-perf-iterate/visitor3-low-terminal-pack-v824-130-131-133-134-focused/20260514-181118-2714692/summary.json`). The wider all-terminal variant is recorded at `scratch/ps1-perf-iterate/visitor3-low-terminal-pack-v824-focused/20260514-180828-2697360/summary.json`. The pack was restored from `scratch/visitor3-low-terminal-pack-v824/VIST3LOW.before.FG2` and byte-compared afterward. | Failed/no promotion. `130,131` was exact-flat at scene/loop/target `1391/1062/1040`, overrun `22`, blocking `42`, reads/due `7/7`, so it failed `--require-improvement`. `130` alone regressed to scene/loop `1395/1066`, overrun `26`, blocking `45`. `130,131,133,134` regressed to `1394/1065/1041`, overrun `24`, blocking `44`. The all-terminal move stayed scene/loop flat but regressed blocking `42 -> 46` and loop reads `7 -> 8`. | Close raw terminal payload relocation for VISITOR3 low. Moving the large terminal frames earlier changes visible cadence more than it helps, even with no decode cost and fixed layout. Future VISITOR3 low work should use generated scheduler ownership, a custom compression family with offline timing proof, or newly paid residency; do not retry byte-for-byte terminal compaction after frame `129` on this baseline. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame40-inplace-v825` | Log-only; pack restored and PS1 rebuilt. | Continue the BUILDING4 low no-shift sector-cut lane after v816 by testing the largest remaining open sector-changing trim. Frame `40` trims `889` bytes and modeled entry-sector coverage `5 -> 4` while preserving every payload offset, the `1714154` byte file size, pack LBA/sectors `9118/837`, and the PS-EXE bucket. | Rewrote only frame `40` in `generated/ps1/foreground/BUIL4LOW.FG2`, changing the entry size `8788 -> 7899` and zeroing the old tail. Transform summary: `scratch/building4-low-frame40-inplace-v825/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/building4-low-frame40-inplace-v825-workreduction/20260514-182034-2767288/building4-low-canary/perf-summary.json`. The pack was restored from `scratch/building4-low-frame40-inplace-v825/BUIL4LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The run reached `fgpilot building4 lowtide 1` and progressed through frame `297/428`, but exited `137` before emitting `JCPERF2`; the parsed gate failed with `missing_jcperf2`, so no timing/CD/correctness metrics were valid for promotion. | Close frame40 as unsafe/inconclusive on the v816/v817-era baseline. The remaining B4-low no-shift sector-changing lane is down to frame286, but green conversion should prioritize generated static-mask/upload ownership because most no-shift trims are now either exact-flat or phase-sensitive. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building4-low-frame286-inplace-v827` | `ps1: shrink building4 low frame286 payload in place` | Test the last open sector-changing BUILDING4 low no-shift trim after the no-change v826 refresh proved the current code/build still completed the row cleanly. Frame `286` trims `751` bytes and modeled entry-sector coverage `5 -> 4` while preserving every payload offset, the `1714154` byte file size, pack LBA/sectors `9118/837`, and the PS-EXE bucket. | Rewrote only frame `286` in `generated/ps1/foreground/BUIL4LOW.FG2`, changing the entry size `8562 -> 7811` and zeroing the old tail. Transform summary: `scratch/building4-low-frame286-inplace-v827/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame286-inplace-v827-workreduction/20260514-182608-2800133/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`. Active payload drops `810977 -> 810226` (`751` bytes), and the selected entry shrinks `8562 -> 7811` with entry-sector coverage `5 -> 4`. | Keep v827 as the new BUILDING4 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. The obvious B4-low no-shift sector-changing trims are now exhausted; move to generated static-mask/upload ownership for green conversion. |
| 2026-05-15 | `building4-low-frame292-inplace-v893` | `ps1: shrink building4 low frame292 payload in place` | Test the next largest unclosed BUILDING4-low preserve-offset trim after v827. Frame `292` is a fixed-sector late-frame shrink, reducing `3556 -> 3122` bytes with modeled entry-sector coverage `3 -> 3`, so the expected acceptance path was exact-flat timing plus smaller active payload. | Rewrote only frame `292` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `810226 -> 809792`. Transform summary: `scratch/building4-low-frame292-inplace-v893/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building4-low-frame292-inplace-v893-focused-rerun/20260515-040947-1970870/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame292-inplace-v893-workreduction/20260515-041121-1980121/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat on the v827 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v893 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-frame426-inplace-v895` | `ps1: shrink building4 low frame426 payload in place` | Continue the fixed-sector BUILDING4-low preserve-offset payload lane after v893. Entry `426` / source frame `621` was the largest remaining fixed-sector candidate, shrinking `616 -> 188` bytes while keeping sector coverage `1 -> 1`, so the acceptance path was exact-flat timing plus smaller active payload. | Rewrote only entry `426` / source frame `621` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `809792 -> 809364`. Transform summary: `scratch/building4-low-frame426-inplace-v895/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building4-low-frame426-inplace-v895-focused/20260515-042342-2051734/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame426-inplace-v895-workreduction/20260515-042514-2060794/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay exact-flat on the v893 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v895 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-14 | `walkstuf1-low-rg297-309-v828` | Log-only; source restored and PS1 rebuilt. | After v817 and the closed `410..422`, `190..202`, `273..285`, and `285..297` probes, take one higher-upside W1-low validation shot from the current read plan. Sectors `297..309` were a high-risk 12-sector grouped append candidate with modeled two-read savings in the tight late cluster. | Temporarily inserted `{297, 309, 0}` into the low-tide-only WALKSTUF1 read-group table on top of v817. Failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-group297-309-v828-probe/20260514-183807-2868107/summary.json`. The source row was removed again, leaving v817 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The probe saved one loop read (`58 -> 57`) but regressed the actual bottlenecks: scene/loop/target moved `1769/1477/1431 -> 1776/1484/1430`, overrun `46 -> 54`, blocking `64 -> 74`, refill `20 -> 26`, and due stayed `11`. | Close `297..309` as phase-negative under the v817 baseline. Hand-authored W1-low tight-cluster read groups are exhausted; the next W1-low path should be generated scheduler/deadline ownership, pack/upload work reduction, or a different data shape with a host timing proof before touching `foregroundPilotPlay`. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-trimtails-v829` | Log-only; pack restored and PS1 rebuilt. | Test whether the current v703 BUILDING2 high pack can get the low-tide v739 tail-trim win without changing file size, LBA, sectors, source code, or the PS-EXE bucket. The scratch transform trimmed trailing transparent draw-span tails in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte footprint while reducing active payload `674798 -> 651506` across `26` entries. | Generated `scratch/b2high-trimtails-v829.FG2` with `scripts/compact-fgp3-trim-draw-tails.py --copy-unparseable --pad-to-input-size`, installed it temporarily, and ran the matched high-tide focused gate against `scratch/ps1-perf-iterate/building2-high-frame89-offscreen-v703-workreduction/20260514-022739-1316524/summary.json`. Failed artifact: `scratch/ps1-perf-iterate/building2-high-trimtails-v829-probe/20260514-184325-2898304/summary.json`. The pack was restored from `scratch/b2high-trimtails-v829.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The transform reduced payload but repeated the high-tide phase regression: scene/loop/target moved `1602/1351/1311 -> 1609/1358/1309`, overrun `40 -> 49`, blocking `54 -> 60`, refill `18 -> 26`, loop reads `58 -> 59`, and due stayed `7`. | Close B2-high whole-pack draw-tail trimming on the v703 baseline. Low tide benefits from this data shape, but high tide uses the removed bytes as cadence padding; future high work needs generated deadline ownership or a transform that preserves the accepted read/refill phase before reducing entry sizes. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-high-frame47-offscreen-v830` | `ps1: clip walkstuf1 high frame47 spans` | Continue the W1-high singleton work-volume lane after B2-high tail trimming failed and the v731-v734 W1-high clips stayed timing-flat. Frame `47` was the largest remaining current singleton after frames `51` and `49`; the candidate had to be regenerated by splicing only frame47's payload into the current v734 pack so accepted frame51/frame49 edits were preserved. | Rewrote only logical PAL4 draw streams for frame `47` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame47-offscreen-v830/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame47-offscreen-v830-focused/20260514-184821-2927591/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame47-offscreen-v830-workreduction/20260514-185006-2937884/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads `63`, and due `16`; loop-read VBlanks move `275 -> 276` without affecting active timing. Host logical work drops by `4241` draw bytes, `5251` pixels, `600` spans, and `13` rows; runtime rows/spans/pixels drop `17052/132773/755038 -> 17039/132173/749787`. | Keep v830 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. W1-high still needs generated deadline/read ownership or upload/restore work to move above 99%. |
| 2026-05-14 | `walkstuf1-high-frame45-offscreen-v831` | `ps1: clip walkstuf1 high frame45 spans` | Continue the W1-high singleton work-volume lane after v830 stayed exact-flat. Frame `45` was the next largest remaining current singleton by logical draw-byte/pixel removal, and it preserves the same fixed pack footprint. | Rewrote only logical PAL4 draw streams for frame `45` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame45-offscreen-v831/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame45-offscreen-v831-focused/20260514-190257-3010972/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame45-offscreen-v831-workreduction/20260514-190411-3018354/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `3619` draw bytes, `4574` pixels, `524` spans, and `28` rows; runtime rows/spans/pixels drop `17039/132173/749787 -> 17011/131649/745213`. | Keep v831 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Continue only exact-flat W1-high singletons, or pivot to generated deadline/upload work for speed conversion. |
| 2026-05-14 | `walkstuf1-high-frame43-offscreen-v832` | `ps1: clip walkstuf1 high frame43 spans` | Continue the W1-high singleton work-volume lane after v831 stayed exact-flat. Frame `43` was the next largest current singleton by logical draw-byte/pixel removal, and it preserves the same fixed pack footprint. | Rewrote only logical PAL4 draw streams for frame `43` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame43-offscreen-v832/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame43-offscreen-v832-focused/20260514-191400-3073714/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame43-offscreen-v832-workreduction/20260514-191508-3080581/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `3344` draw bytes, `4149` pixels, `513` spans, and `26` rows; runtime rows/spans/pixels drop `17011/131649/745213 -> 16985/131136/741064`. | Keep v832 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Direct W1-high singleton clipping is still timing-inert, so green conversion needs generated deadline/read ownership or upload/restore work. |
| 2026-05-14 | `walkstuf1-high-frame56-offscreen-v833` | `ps1: clip walkstuf1 high frame56 spans` | Continue the W1-high singleton work-volume lane after v832 stayed exact-flat. Frame `56` was the next largest current singleton and clips to an empty draw stream at the current high placement. | Rewrote only logical PAL4 draw streams for frame `56` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame56-offscreen-v833/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame56-offscreen-v833-focused/20260514-192335-3128945/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame56-offscreen-v833-workreduction/20260514-192441-3135670/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `3099` draw bytes, `3606` pixels, `488` spans, and `61` rows; runtime rows/spans/pixels drop `16985/131136/741064 -> 16924/130648/737458`. | Keep v833 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Direct W1-high singleton clipping is still timing-inert, so the next speed-bearing work should be generated deadline/read ownership or upload/restore work. |
| 2026-05-14 | `walkstuf1-high-frame57-offscreen-v834` | `ps1: clip walkstuf1 high frame57 spans` | Continue the W1-high singleton work-volume lane after v833 stayed exact-flat. Frame `57` is the paired fully offscreen singleton after frame `56`, so it was tested with a retry after the first wrapper kill emitted no `JCPERF2`. | Rewrote only logical PAL4 draw streams for frame `57` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame57-offscreen-v834/summary.json`; transient missing-`JCPERF2` artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame57-offscreen-v834-focused/20260514-193021-3167856/walkstuf1-high/perf-summary.json`; accepted no-regression retry: `scratch/ps1-perf-iterate/walkstuf1-high-frame57-offscreen-v834-retry/20260514-193101-3172948/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `3087` draw bytes, `3507` pixels, `501` spans, and `61` rows; runtime rows/spans/pixels drop `16924/130648/737458 -> 16863/130147/733951`. | Keep v834 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Direct W1-high singleton clipping is still timing-inert, so the next speed-bearing work should be generated deadline/read ownership or upload/restore work. |
| 2026-05-14 | `walkstuf1-high-frame136-offscreen-v835` | `ps1: clip walkstuf1 high frame136 spans` | Continue the W1-high singleton work-volume lane after v834 stayed exact-flat. Frame `136` was the next safe singleton by current host draw-byte removal and preserves the fixed pack footprint. | Rewrote only logical PAL4 draw streams for frame `136` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame136-offscreen-v835/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame136-offscreen-v835-focused/20260514-195355-3299458/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame136-offscreen-v835-workreduction/20260514-195503-3306400/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `1166` draw bytes, `1850` pixels, `89` spans, and `2` rows; runtime rows/spans/pixels drop `16863/130147/733951 -> 16861/130058/732101`. | Keep v835 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Direct W1-high singleton clipping is still timing-inert, so speed conversion still needs generated deadline/read ownership or upload/restore work. |
| 2026-05-14 | `walkstuf1-high-frame135-offscreen-v836` | `ps1: clip walkstuf1 high frame135 spans` | Continue the W1-high singleton work-volume lane after v835 stayed exact-flat. Frame `135` was the next remaining safe singleton and keeps the fixed pack footprint. | Rewrote only logical PAL4 draw streams for frame `135` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame135-offscreen-v836/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame135-offscreen-v836-focused/20260514-200302-3351479/summary.json`; transient killed no-summary artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame135-offscreen-v836-workreduction/20260514-200411-3358391/walkstuf1-high/headless-regtest.log`; accepted retry: `scratch/ps1-perf-iterate/walkstuf1-high-frame135-offscreen-v836-retry/20260514-200531-3367192/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `565` draw bytes, `871` pixels, `41` spans, and `2` rows; runtime rows/spans/pixels drop `16861/130058/732101 -> 16859/130017/731230`. | Keep v836 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. W1-high direct singleton work is nearly exhausted; generated deadline/read ownership or upload/restore preprocessing is still required for speed conversion. |
| 2026-05-14 | `walkstuf1-high-frame139-offscreen-v837` | `ps1: clip walkstuf1 high frame139 spans` | Finish the current W1-high singleton work-volume lane after v836 stayed exact-flat. Frame `139` was the last meaningful safe singleton in the current list, with span-only draw work to remove while keeping the fixed pack footprint. | Rewrote only logical PAL4 draw streams for frame `139` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, entry offsets/sizes, pack LBA/sectors `24883/750`, active payload `882007`, and the `217088` byte PS-EXE bucket. Host summary: `scratch/walkstuf1-high-frame139-offscreen-v837/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame139-offscreen-v837-focused/20260514-203248-3518155/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame139-offscreen-v837-workreduction/20260514-203354-3524856/summary.json`. | Promoted as same-speed work reduction, not a VBlank win. Timing/CD stay flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Host logical work drops by `343` draw bytes, `214` pixels, and `98` spans; runtime rows/spans/pixels drop `16859/130017/731230 -> 16859/129919/731016`. | Keep v837 as the new WALKSTUF1 high work-volume baseline and close the current direct singleton lane. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Next W1-high/W1-low work should be generated deadline/read ownership or upload/restore preprocessing rather than more closed scalar rows. |
| 2026-05-14 | `building2-high-rg319-335-v838` | Log-only; source restored and PS1 rebuilt. | Test the current BUILDING2 high read-plan's append-start-fireable late row after the W1-high singleton lane closed. `{319,335}` was distinct from the older stale `{320,336}` no-op and modeled one saved read with a low visible-risk classifier. | Temporarily added `{319,335,0}` to `kBuilding2HighReadGroups12` after the accepted high-tide rows. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg319-335-v838-focused/20260514-204411-3583647/summary.json`. The source row was removed again, leaving v703/v837 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row fired and saved one loop read (`58 -> 57`) but regressed the actual bottlenecks: scene/loop `1602/1351 -> 1605/1354`, overrun `40 -> 43`, blocking `54 -> 57`, refill stayed `18`, and due misses stayed `7`; the temporary source row also shifted tracked hot symbols by `+208` bytes. | Close BUILDING2 high `{319,335}` as another hand-authored cadence miss on the current baseline. The late row needs generated deadline ownership or pack/data-shape reduction before retry; public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-high-frame51-inplace-v839` | `ps1: shrink walkstuf1 high frame51 payload in place` | Start a preserve-offset W1-high payload-size lane after the direct singleton draw-span lane closed. The binary question was whether shrinking the largest already-clipped high entry in place, without moving downstream offsets, could reduce pack work while preserving the accepted v837 timing row. | Added `--preserve-offsets` and `--summary` support to `scripts/compact-fgp3-trim-draw-tails.py`, then rewrote only entry `51` / source frame `61` in `generated/ps1/foreground/WALKSTUF1.FG2`. The transform preserves the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking entry `51` from `5588 -> 770` bytes and active payload `882007 -> 877189`. Transform summary: `scratch/walkstuf1-high-frame51-inplace-v839/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame51-inplace-v839-focused/20260514-205027-3620146/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame51-inplace-v839-workreduction/20260514-205134-3626929/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Runtime rows/spans/pixels stay `16859/129919/731016`, while current active frame payload drops to `877189`. | Keep v839 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Continue W1-high preserve-offset payload trims only under exact-flat gates; speed conversion still needs generated deadline/read ownership or upload/restore work. |
| 2026-05-14 | `walkstuf1-high-frame55-inplace-v840` | Log-only; pack restored and PS1 rebuilt. | Test the largest remaining W1-high preserve-offset shrink after v839. Entry `55` / source frame `65` modeled a large in-place tail removal (`4716 -> 2` bytes, sector coverage `4 -> 1`) without moving payload offsets, so it was the next binary candidate in the same lane. | Generated `scratch/walkstuf1-high-frame55-inplace-v840/WALKSTUF1.candidate.FG2` from the current v839 pack with `--preserve-offsets --frames 55`, cutting active payload `877189 -> 872475` in the candidate. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame55-inplace-v840-focused/20260514-210006-3675557/summary.json`. The pack was restored from `scratch/walkstuf1-high-frame55-inplace-v840/WALKSTUF1.before.FG2`, leaving v839 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The shrink changed visible cadence enough to regress scene/loop/target `1764/1476/1434 -> 1780/1492/1422`, overrun `42 -> 70`, blocking `81 -> 113`, refill `23 -> 27`, and due misses `16 -> 18`; loop reads stayed `63`. | Close frame55 preserve-offset shrinking on the current W1-high baseline. This proves not every size-only tail trim is timing-neutral; continue with smaller isolated trims only under strict exact-flat gates, or prefer generated deadline/read ownership for actual speed conversion. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-high-frame49-inplace-v841` | `ps1: shrink walkstuf1 high frame49 payload in place` | Continue the W1-high preserve-offset payload-size lane after v840 proved the zero-payload frame55 trim was cadence-sensitive. Frame `49` matched the successful non-empty v839 shape more closely: it trims to a 770-byte payload while preserving all offsets. | Rewrote only entry `49` / source frame `59` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking entry `49` from `5269 -> 770` bytes and active payload `877189 -> 872690`. Transform summary: `scratch/walkstuf1-high-frame49-inplace-v841/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame49-inplace-v841-focused/20260514-210513-3706389/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame49-inplace-v841-workreduction/20260514-210622-3713317/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Runtime rows/spans/pixels stay `16859/129919/731016`, while current active frame payload drops to `872690`. | Keep v841 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Continue with non-empty preserve-offset candidates before trying zero-payload trims again. |
| 2026-05-14 | `walkstuf1-high-frame47-inplace-v842` | `ps1: shrink walkstuf1 high frame47 payload in place` | Continue the W1-high non-empty preserve-offset payload-size lane after v839 and v841 proved exact-flat and v840 closed the zero-payload frame55 shape as cadence-sensitive. Frame `47` / source frame `57` was the next large non-empty shrink candidate. | Rewrote only entry `47` / source frame `57` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking entry `47` from `7399 -> 3158` bytes and active payload `872690 -> 868449`. Transform summary: `scratch/walkstuf1-high-frame47-inplace-v842/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame47-inplace-v842-focused/20260514-211334-3754787/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame47-inplace-v842-workreduction/20260514-211439-3761479/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Runtime rows/spans/pixels stay `16859/129919/731016`, while current active frame payload drops to `868449`. | Keep v842 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Continue only exact-flat preserve-offset trims; speed conversion still needs generated deadline/read ownership or upload/restore work. |
| 2026-05-14 | `walkstuf1-high-frame45-inplace-v843` | `ps1: shrink walkstuf1 high frame45 payload in place` | Continue the W1-high non-empty preserve-offset payload-size lane after v842 stayed exact-flat. Frame `45` / source frame `55` was the next largest non-empty shrink candidate and keeps the fixed footprint shape that v839/v841/v842 accepted. | Rewrote only entry `45` / source frame `55` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking entry `45` from `5276 -> 1657` bytes and active payload `868449 -> 864830`. Transform summary: `scratch/walkstuf1-high-frame45-inplace-v843/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame45-inplace-v843-focused/20260514-212619-3828063/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame45-inplace-v843-workreduction/20260514-212726-3834957/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Runtime rows/spans/pixels stay `16859/129919/731016`, while current active frame payload drops to `864830`. | Keep v843 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Continue only exact-flat preserve-offset trims, but pivot to W1-low/B2-high speed-bearing work if the remaining candidates stop reducing meaningful payload. |
| 2026-05-14 | `walkstuf1-high-frame43-inplace-v844` | `ps1: shrink walkstuf1 high frame43 payload in place` | Continue the W1-high non-empty preserve-offset payload-size lane after v843 stayed exact-flat. Frame `43` / source frame `53` was the next non-empty shrink that still removed multiple sectors of tail payload without moving offsets. | Rewrote only entry `43` / source frame `53` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking entry `43` from `5555 -> 2211` bytes and active payload `864830 -> 861486`. Transform summary: `scratch/walkstuf1-high-frame43-inplace-v844/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame43-inplace-v844-focused/20260514-213249-3865580/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame43-inplace-v844-workreduction/20260514-213356-3872395/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1764`, active loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`. Runtime rows/spans/pixels stay `16859/129919/731016`, while current active frame payload drops to `861486`. | Keep v844 as the new WALKSTUF1 high work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Remaining W1-high size candidates are smaller or previously cadence-sensitive, so start pivoting toward W1-low/B2-high speed-bearing work after one more exact-flat check at most. |
| 2026-05-14 | `walkstuf1-high-frame56-inplace-v845` | Log-only; pack restored and PS1 rebuilt. | Test one final near-empty W1-high preserve-offset shrink after the non-empty v839/v841/v842/v843/v844 lane stayed exact-flat. Frame `56` / source frame `67` modeled a `3425 -> 324` byte shrink, but it was closer to the failed frame55 cadence class and needed a strict gate before promotion. | Generated `scratch/walkstuf1-high-frame56-inplace-v845/WALKSTUF1.candidate.FG2` from the v844 pack with `--preserve-offsets --frames 56`, cutting candidate active payload `861486 -> 858385`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame56-inplace-v845-focused/20260514-213907-3902612/summary.json`. The pack was restored from `scratch/walkstuf1-high-frame56-inplace-v845/WALKSTUF1.before.FG2`, leaving v844 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The near-empty shrink regressed scene/loop/target `1764/1476/1434 -> 1778/1490/1422`, overrun `42 -> 68`, blocking `81 -> 114`, refill `23 -> 28`, and due misses `16 -> 20`; loop reads stayed `63`. | Close frame56 preserve-offset shrinking on the current W1-high baseline. Combined with the frame55 failure, near-empty W1-high payload trims are cadence-sensitive and should not be retried without a different scheduler/read-ownership change. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame53-inplace-v846` | `ps1: shrink walkstuf1 low frame53 payload in place` | Test the best remaining W1-low preserve-offset shrink after the v817 low retained-read row and the W1-high near-empty frame56 miss. Frame `53` / source frame `63` was a non-empty sector-reducing candidate that preserved all payload offsets and avoided the closed near-empty cadence class. | Rewrote only entry `53` / source frame `63` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking entry `53` from `3893 -> 2463` bytes and active payload `798079 -> 796649`. Transform summary: `scratch/walkstuf1-low-frame53-inplace-v846/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame53-inplace-v846-focused/20260514-214245-3923850/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame53-inplace-v846-workreduction/20260514-214352-3930667/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1769`, active loop/target `1477/1431`, overrun `46`, blocking/refill `64/20`, loop reads/read VBlanks `58/266`, and due `11`. Runtime rows/spans/pixels stay `16257/114798/633876`, while current active frame payload drops to `796649`. | Keep v846 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Continue W1-low/B2-high speed-bearing work through generated deadline/read ownership or pack/data-shape changes rather than retrying closed scalar rows. |
| 2026-05-14 | `walkstuf1-low-frame136-inplace-v847` | `ps1: shrink walkstuf1 low frame136 payload in place` | Continue the W1-low preserve-offset payload-size lane after v846 stayed exact-flat. Frame `136` / source frame `244` was the next meaningful untried non-empty shrink candidate, trimming one sector without moving offsets and avoiding the known near-empty cadence failures. | Rewrote only entry `136` / source frame `244` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking entry `136` from `3762 -> 2596` bytes and active payload `796649 -> 795483`. Transform summary: `scratch/walkstuf1-low-frame136-inplace-v847/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame136-inplace-v847-focused/20260514-215413-3989738/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame136-inplace-v847-workreduction/20260514-215520-3996493/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1769`, active loop/target `1477/1431`, overrun `46`, blocking/refill `64/20`, loop reads/read VBlanks `58/266`, and due `11`. Runtime rows/spans/pixels stay `16257/114798/633876`, while current active frame payload drops to `795483`. | Keep v847 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Remaining W1-low scalar trims are smaller or previously closed, so the next biggest timing attempt should move to generated deadline/read ownership or BUILDING2 high residual work. |
| 2026-05-14 | `walkstuf1-low-rg410-422-v848` | Log-only; source restored and PS1 rebuilt. | Retest low-tide sectors `410..422` against the new v847 work-volume baseline because the read plan still ranked it as a low-visible-risk direct retained-read candidate after the frame136 shrink. | Temporarily inserted `{410, 422, 0}` after `{394, 410, 0}` in the low-tide-only WALKSTUF1 read-group table. The build stayed in the `217088` byte PS-EXE bucket. Failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg410-422-v848-focused/20260514-220217-4036585/summary.json`. The source row was removed again, leaving v847 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The probe saved one loop read and one loop-read VBlank (`58/266 -> 57/265`) but regressed the speed-bearing metrics: scene/loop/target moved `1769/1477/1431 -> 1770/1478/1431` and overrun `46 -> 47`; blocking/refill stayed `64/20`, due stayed `11`, and pack LBA stayed `25633`. | Close `410..422` as phase-negative under both v817 and v847. The remaining W1-low retained windows (`190..202`, `273..285`, `285..297`, `297..313`, `305..321`) need generated ownership or pack/data-shape work rather than another scalar table row. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame79-inplace-v849` | `ps1: shrink walkstuf1 low frame79 payload in place` | Continue the W1-low preserve-offset payload lane after v847 and the failed v848 read-group retest. Entry `79` / source frame `140` was the largest remaining unclosed sector-reducing candidate in the current no-shift trim scan. | Rewrote only entry `79` / source frame `140` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking entry `79` from `4992 -> 4264` bytes and active payload `795483 -> 794755`. Transform summary: `scratch/walkstuf1-low-frame79-inplace-v849/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame79-inplace-v849-focused/20260514-220838-4072951/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame79-inplace-v849-workreduction/20260514-220945-4079718/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1769`, active loop/target `1477/1431`, overrun `46`, blocking/refill `64/20`, loop reads/read VBlanks `58/266`, and due `11`. Runtime rows/spans/pixels stay `16257/114798/633876`, while current active frame payload drops to `794755`. | Keep v849 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Remaining W1-low no-shift trims are smaller or clustered near closed cadence-sensitive rows, so the next timing attempt should prefer generated deadline/read ownership or BUILDING2 high residual work. |
| 2026-05-14 | `walkstuf1-low-frame96-inplace-v850` | Log-only; pack restored and PS1 rebuilt. | Test the next unclosed W1-low sector-changing no-shift trim after v849. Entry `96` / source frame `176` was a small `158` byte shrink, but still changed modeled sector coverage `4 -> 3`, so it needed one strict gate before shifting to non-sector candidates. | Temporarily rewrote only entry `96` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking `4649 -> 4491` bytes and active payload `794755 -> 794597`. Transform summary: `scratch/walkstuf1-low-frame96-inplace-v850/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame96-inplace-v850-focused/20260514-221817-4128721/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame96-inplace-v850/WALK1LOW.before.FG2`, leaving v849 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The sector-boundary shrink regressed scene/loop/target `1769/1477/1431 -> 1775/1483/1431`, overrun `46 -> 52`, and blocking/refill `64/20 -> 71/25`, while loop reads and due stayed `58/11`; the strict gate failed timing regression plus no key metric improvement. | Close frame96 no-shift shrinking under the v849 baseline. Remaining W1-low trims should be non-sector, exact-flat-only work-volume checks or, for speed, generated deadline/read ownership rather than late-cluster sector-boundary edits. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `building2-high-seg185-197-v851` | Log-only; source restored and PS1 rebuilt. | Test the generated-ownership queue's separate BUILDING2 high `185..197` cluster as reusable setup-owned residency rather than another active-loop retained-read row. The previous raw and slack-gated table variants saved reads but regressed cadence, so this checked whether moving the read into setup could keep the read win without stealing the active-loop refill slot. | Temporarily added a high-tide-only BUILDING2 setup segment for sectors `185..197`, backed by the reusable setup segment buffer, while preserving pack LBA/sectors `6181/637` and the `217088` byte PS-EXE bucket. Focused artifact: `scratch/ps1-perf-iterate/building2-high-seg185-197-v851-focused/20260514-222505-4168786/summary.json`. The source change was removed again, leaving v703/v849 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The setup segment saved active reads (`58 -> 55`) but still regressed the speed-bearing metrics: scene/loop/target `1602/1351/1311 -> 1613/1357/1309`, overrun `40 -> 48`, blocking `54 -> 63`, refill `18 -> 26`, and due misses `7 -> 8`. The temporary source also grew `foregroundPilotPlay` by `380` bytes. | Close BUILDING2 high `185..197` as both a scalar retained row and a reusable setup segment under the current baseline. The cluster is phase-negative unless a generated planner can prove earlier non-visible ownership with fixed hot code and no refill/blocking rise. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame81-inplace-v852` | `ps1: shrink walkstuf1 low frame81 payload in place` | Continue the W1-low preserve-offset payload lane after v849 and the failed sector-boundary v850 probe. Entry `81` / source frame `143` was the largest remaining open no-sector trim, so it tested whether smaller fixed-sector payload work stays cadence-neutral. | Rewrote only entry `81` / source frame `143` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking entry `81` from `4919 -> 4259` bytes and active payload `794755 -> 794095`. Transform summary: `scratch/walkstuf1-low-frame81-inplace-v852/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame81-inplace-v852-focused/20260514-222850-4190863/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame81-inplace-v852-workreduction/20260514-223007-4797/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1769`, active loop/target `1477/1431`, overrun `46`, blocking/refill `64/20`, loop reads/read VBlanks `58/266`, and due `11`. Runtime rows/spans/pixels stay `16257/114798/633876`, while current active frame payload drops to `794095`. | Keep v852 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Remaining W1-low no-sector trims are smaller, so speed conversion still needs generated deadline/read ownership or a pack/data-shape change. |
| 2026-05-14 | `walkstuf1-low-frame129-inplace-v853` | `ps1: shrink walkstuf1 low frame129 payload in place` | Continue the W1-low preserve-offset no-sector payload lane after v852 stayed exact-flat. Entry `129` / source frame `237` was the next largest open no-sector trim, so this tested whether another fixed-sector payload cut remains cadence-neutral on the new v852 baseline. | Rewrote only entry `129` / source frame `237` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking entry `129` from `5093 -> 4535` bytes and active payload `794095 -> 793537`. Transform summary: `scratch/walkstuf1-low-frame129-inplace-v853/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame129-inplace-v853-focused/20260514-224024-63773/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame129-inplace-v853-workreduction/20260514-224135-70955/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1769`, active loop/target `1477/1431`, overrun `46`, blocking/refill `64/20`, loop reads/read VBlanks `58/266`, and due `11`. Runtime rows/spans/pixels stay `16257/114798/633876`, while current active frame payload drops to `793537`. | Keep v853 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Remaining W1-low no-sector trims are now smaller; speed conversion still needs generated deadline/read ownership or a pack/data-shape change. |
| 2026-05-14 | `walkstuf1-low-frame85-inplace-v854` | Log-only; pack restored and PS1 rebuilt. | Test the next largest remaining W1-low preserve-offset no-sector trim after v853. Entry `85` / source frame `148` looked like a 503-byte fixed-sector work-volume cut, but it sits in the cadence-sensitive active read cluster, so it needed a strict gate before any work-reduction promotion. | Temporarily rewrote only entry `85` / source frame `148` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking `4854 -> 4351` bytes and active payload `793537 -> 793034`. Transform summary: `scratch/walkstuf1-low-frame85-inplace-v854/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame85-inplace-v854-focused/20260514-224737-105909/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame85-inplace-v854/WALK1LOW.before.FG2`, leaving v853 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The fixed-sector shrink still changed cadence: scene/loop/target regressed `1769/1477/1431 -> 1773/1481/1428`, overrun `46 -> 53`, blocking/refill `64/20 -> 86/28`, loop reads `58 -> 61`, and due `11 -> 13`. | Close frame85 preserve-offset shrinking on the v853 baseline. The no-sector lane is not automatically safe in this active cluster; remaining W1-low trims must keep strict timing gates, and speed conversion still needs generated deadline/read ownership or a pack/data-shape change. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame139-inplace-v855` | `ps1: shrink walkstuf1 low frame139 payload in place` | Continue the W1-low preserve-offset no-sector payload lane after v854 closed frame85 as cadence-sensitive. Entry `139` / source frame `247` was the next meaningful no-sector shrink that avoided the failed active read-cluster frame85 shape. | Rewrote only entry `139` / source frame `247` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking entry `139` from `4011 -> 3668` bytes and active payload `793537 -> 793194`. Transform summary: `scratch/walkstuf1-low-frame139-inplace-v855/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame139-inplace-v855-focused/20260514-225028-122831/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame139-inplace-v855-workreduction/20260514-225136-129684/summary.json`. | Promoted as same-speed payload work reduction, not a VBlank win. Timing/CD stay exact-flat at scene `1769`, active loop/target `1477/1431`, overrun `46`, blocking/refill `64/20`, loop reads/read VBlanks `58/266`, and due `11`. Runtime rows/spans/pixels stay `16257/114798/633876`, while current active frame payload drops to `793194`. | Keep v855 as the new WALKSTUF1 low work-volume baseline. Public rollup remains `+0.2702%` over target / `99.7342%` target speed and raw signed remains `-0.4969%` / `100.5165%`; methodology totals remain about `17.13` public over-target points removed and `+12.63` target-speed points gained. Remaining W1-low no-sector trims are smaller; speed conversion still needs generated deadline/read ownership or a pack/data-shape change. |
| 2026-05-14 | `walkstuf1-low-frame55-inplace-v856` | Log-only; pack restored and PS1 rebuilt. | Test the largest remaining W1-low preserve-offset shrink after v855. Entry `55` / source frame `65` modeled a large near-empty tail removal (`4716 -> 2` bytes, sector coverage `4 -> 1`) without moving offsets, so it was the biggest remaining binary payload candidate despite the known near-empty cadence risk. | Temporarily rewrote only entry `55` / source frame `65` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `793194 -> 788480`. Transform summary: `scratch/walkstuf1-low-frame55-inplace-v856/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame55-inplace-v856-focused/20260514-225952-176794/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame55-inplace-v856/WALK1LOW.before.FG2`, leaving v855 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The near-empty shrink regressed scene/loop/target `1769/1477/1431 -> 1775/1483/1430`, overrun `46 -> 53`, blocking/refill `64/20 -> 73/21`, and due `11 -> 12`; loop reads stayed `58`. | Close frame55 preserve-offset shrinking on the current W1-low baseline. It confirms the near-empty row class is cadence-sensitive on low tide too; remaining W1-low payload trims should be non-empty/fixed-sector exact-flat probes or generated deadline/read ownership for speed conversion. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame60-inplace-v857` | Log-only; pack restored and PS1 rebuilt. | Continue the biggest-first W1-low preserve-offset scan after v856 closed frame55. Entry `60` / source frame `81` was the next largest candidate (`4432 -> 476` bytes, sector coverage `3 -> 1`), still in the near-empty sector-collapse class but with a nonzero residual payload. | Temporarily rewrote only entry `60` / source frame `81` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `793194 -> 789238`. Transform summary: `scratch/walkstuf1-low-frame60-inplace-v857/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame60-inplace-v857-focused/20260514-230323-197400/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame60-inplace-v857/WALK1LOW.before.FG2`, leaving v855 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The sector-collapse shrink regressed scene/loop/target `1769/1477/1431 -> 1773/1481/1427`, overrun `46 -> 54`, blocking/refill `64/20 -> 85/25`, loop reads `58 -> 60`, and due `11 -> 13`. | Close frame60 preserve-offset shrinking on the current W1-low baseline. The second large sector-collapse failure confirms this family is not safe as size-only surgery; continue only with fixed-sector/non-empty exact-flat probes or switch to generated deadline/read ownership and other under-99 timing rows. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame39-inplace-v858` | Log-only; pack restored and PS1 rebuilt. | Test a different high-byte W1-low preserve-offset shape after the near-empty sector collapses failed. Entry `39` / source frame `49` kept a large non-empty residual and only changed modeled sector coverage `4 -> 3`, so it checked whether a one-sector early shrink could stay cadence-neutral. | Temporarily rewrote only entry `39` / source frame `49` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size `1535263`, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking `7835 -> 4724` bytes and active payload `793194 -> 790083`. Transform summary: `scratch/walkstuf1-low-frame39-inplace-v858/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame39-inplace-v858-focused/20260514-230643-216860/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame39-inplace-v858/WALK1LOW.before.FG2`, leaving v855 as the runtime baseline, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The early one-sector shrink regressed scene/loop/target `1769/1477/1431 -> 1783/1491/1427`, overrun `46 -> 64`, blocking/refill `64/20 -> 79/30`, and loop reads `58 -> 64`; due improved `11 -> 10` but not enough to offset the timing/CD regression. | Close frame39 preserve-offset shrinking on the current W1-low baseline. Large early sector-reducing trims are also phase-sensitive; remaining W1-low pack probes should prefer fixed-sector exact-flat work reductions or a generated scheduler/data-shape change. Public rollup and methodology totals remain unchanged. |
| 2026-05-14 | `walkstuf1-low-frame87-inplace-v859` | `ps1: shrink walkstuf1 low frame87 payload in place` | Switch to fixed-sector W1-low probes after v856-v858 closed the large sector-reducing shapes. Entry `87` / source frame `156` was the largest remaining fixed-sector shrink (`4358 -> 4066`, sector coverage `3 -> 3`), so it tested whether the smaller no-sector lane could convert to a timing win on top of v855. | Rewrote only entry `87` / source frame `156` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `793194 -> 792902`. Transform summary: `scratch/walkstuf1-low-frame87-inplace-v859/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame87-inplace-v859-focused/20260514-230917-232908/summary.json`; confirmation artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame87-inplace-v859-confirm/20260514-231031-240397/summary.json`. | Promoted as a small deterministic speed win. Both gates reproduced scene `1769`, loop `1477`, target `1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; compared with v855, target improves `1431 -> 1432`, overrun improves `46 -> 45`, and read time improves `266 -> 259`, with blocking rising only within the gate allowance `64 -> 65`. | Keep v859 as the new WALKSTUF1 low baseline at `96.953%` target speed. Public rollup improves to `+0.2697%` over target / `99.7347%` target speed, raw signed improves to `-0.4975%` / `100.5171%`, with bands unchanged at `117` green and `9` yellow. Methodology total is about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. Continue W1-low fixed-sector probes only if they stay exact-flat or improve target/overrun; otherwise pivot to B2-high/W1-high scheduler/data-shape work. |
| 2026-05-14 | `walkstuf1-low-frame89-inplace-v860` | `ps1: shrink walkstuf1 low frame89 payload in place` | Continue the fixed-sector W1-low lane after v859 produced a speed-bearing target/overrun win. Entry `89` / source frame `162` was the next largest open fixed-sector candidate after the already closed frame85 row, shrinking `3792 -> 3547` bytes with sector coverage `3 -> 3`. | Rewrote only entry `89` / source frame `162` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `792902 -> 792657`. Transform summary: `scratch/walkstuf1-low-frame89-inplace-v860/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame89-inplace-v860-focused/20260514-232031-297577/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame89-inplace-v860-workreduction/20260514-232142-304757/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v859 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v860 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-14 | `walkstuf1-low-frame98-inplace-v861` | `ps1: shrink walkstuf1 low frame98 payload in place` | Continue the fixed-sector W1-low lane after v860 stayed cadence-neutral. Entry `98` / source frame `183` was the next largest open fixed-sector candidate, shrinking `4985 -> 4779` bytes with sector coverage `3 -> 3`. | Rewrote only entry `98` / source frame `183` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `792657 -> 792451`. Transform summary: `scratch/walkstuf1-low-frame98-inplace-v861/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame98-inplace-v861-focused/20260514-232947-350562/summary.json`; accepted no-regression work gate after one wrapper-killed non-metric run: `scratch/ps1-perf-iterate/walkstuf1-low-frame98-inplace-v861-workreduction-rerun/20260514-233117-360907/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v860 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v861 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-14 | `walkstuf1-low-frame27-inplace-v862` | `ps1: shrink walkstuf1 low frame27 payload in place` | Continue the fixed-sector W1-low lane after v861 stayed cadence-neutral. Entry `27` / source frame `34` was the next largest open fixed-sector candidate, shrinking `7015 -> 6830` bytes with sector coverage `4 -> 4`. | Rewrote only entry `27` / source frame `34` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `792451 -> 792266`. Transform summary: `scratch/walkstuf1-low-frame27-inplace-v862/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame27-inplace-v862-focused/20260514-234019-411614/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame27-inplace-v862-workreduction/20260514-234126-418363/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v861 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v862 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-14 | `walkstuf1-low-frame101-inplace-v863` | `ps1: shrink walkstuf1 low frame101 payload in place` | Continue the fixed-sector W1-low lane after v862 stayed cadence-neutral. Entry `101` / source frame `192` was the next largest open fixed-sector candidate, shrinking `4457 -> 4289` bytes with sector coverage `3 -> 3`. | Rewrote only entry `101` / source frame `192` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `792266 -> 792098`. Transform summary: `scratch/walkstuf1-low-frame101-inplace-v863/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame101-inplace-v863-focused/20260514-234630-448173/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame101-inplace-v863-workreduction/20260514-234739-455087/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v862 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v863 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame93-inplace-v864` | `ps1: shrink walkstuf1 low frame93 payload in place` | Continue the fixed-sector W1-low lane after v863 stayed cadence-neutral. Entry `93` / source frame `170` was the next largest open fixed-sector candidate after the closed frame85/sector-changing rows, shrinking `5661 -> 5494` bytes with sector coverage `4 -> 4`. | Rewrote only entry `93` / source frame `170` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `792098 -> 791931`. Transform summary: `scratch/walkstuf1-low-frame93-inplace-v864/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame93-inplace-v864-focused/20260515-000007-526123/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame93-inplace-v864-workreduction/20260515-000116-533096/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v863 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v864 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame94-inplace-v865` | `ps1: shrink walkstuf1 low frame94 payload in place` | Continue the fixed-sector W1-low lane after v864 stayed cadence-neutral. Entry `94` / source frame `171` was the next largest open fixed-sector candidate, shrinking `5544 -> 5377` bytes with sector coverage `4 -> 4`. | Rewrote only entry `94` / source frame `171` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `791931 -> 791764`. Transform summary: `scratch/walkstuf1-low-frame94-inplace-v865/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame94-inplace-v865-focused/20260515-001217-595733/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame94-inplace-v865-workreduction/20260515-001325-602537/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v864 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v865 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame97-inplace-v866` | `ps1: shrink walkstuf1 low frame97 payload in place` | Continue the fixed-sector W1-low lane after v865 stayed cadence-neutral. Entry `97` / source frame `179` was the next largest open fixed-sector candidate, shrinking `4825 -> 4658` bytes with sector coverage `3 -> 3`. | Rewrote only entry `97` / source frame `179` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `791764 -> 791597`. Transform summary: `scratch/walkstuf1-low-frame97-inplace-v866/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame97-inplace-v866-focused/20260515-001849-633936/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame97-inplace-v866-workreduction/20260515-001955-640695/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v865 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v866 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame99-inplace-v867` | `ps1: shrink walkstuf1 low frame99 payload in place` | Continue the fixed-sector W1-low lane after v866 stayed cadence-neutral. Entry `99` / source frame `186` was the next largest open fixed-sector candidate, shrinking `4766 -> 4600` bytes with sector coverage `4 -> 4`. | Rewrote only entry `99` / source frame `186` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `791597 -> 791431`. Transform summary: `scratch/walkstuf1-low-frame99-inplace-v867/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame99-inplace-v867-focused/20260515-002507-670231/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame99-inplace-v867-workreduction/20260515-002619-677572/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v866 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v867 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame100-inplace-v868` | `ps1: shrink walkstuf1 low frame100 payload in place` | Continue the fixed-sector W1-low lane after v867 stayed cadence-neutral. Entry `100` / source frame `189` was the next largest open fixed-sector candidate, shrinking `4683 -> 4517` bytes with sector coverage `3 -> 3`. | Rewrote only entry `100` / source frame `189` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `791431 -> 791265`. Transform summary: `scratch/walkstuf1-low-frame100-inplace-v868/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame100-inplace-v868-focused/20260515-003434-724785/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame100-inplace-v868-workreduction/20260515-003541-731636/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v867 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v868 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame134-inplace-v869` | `ps1: shrink walkstuf1 low frame134 payload in place` | Continue the fixed-sector W1-low lane after v868 stayed cadence-neutral. Entry `134` / source frame `242` was the next largest open fixed-sector candidate, shrinking `4397 -> 4235` bytes with sector coverage `3 -> 3`. | Rewrote only entry `134` / source frame `242` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `791265 -> 791103`. Transform summary: `scratch/walkstuf1-low-frame134-inplace-v869/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame134-inplace-v869-focused/20260515-004522-786629/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame134-inplace-v869-workreduction/20260515-004631-793587/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v868 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v869 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame91-inplace-v870` | `ps1: shrink walkstuf1 low frame91 payload in place` | Continue the fixed-sector W1-low lane after v869 stayed cadence-neutral. Entry `91` / source frame `166` was the next largest open fixed-sector candidate, shrinking `5545 -> 5387` bytes with sector coverage `3 -> 3`. | Rewrote only entry `91` / source frame `166` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `791103 -> 790945`. Transform summary: `scratch/walkstuf1-low-frame91-inplace-v870/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame91-inplace-v870-focused/20260515-005210-826371/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame91-inplace-v870-workreduction/20260515-005316-833131/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v869 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v870 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame92-inplace-v871` | `ps1: shrink walkstuf1 low frame92 payload in place` | Continue the fixed-sector W1-low lane after v870 stayed cadence-neutral. Entry `92` / source frame `168` was the next largest open fixed-sector candidate, shrinking `6035 -> 5877` bytes with sector coverage `4 -> 4`. | Rewrote only entry `92` / source frame `168` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790945 -> 790787`. Transform summary: `scratch/walkstuf1-low-frame92-inplace-v871/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame92-inplace-v871-focused/20260515-005853-865003/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame92-inplace-v871-workreduction/20260515-005959-871637/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v870 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v871 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame95-inplace-v872` | `ps1: shrink walkstuf1 low frame95 payload in place` | Continue the fixed-sector W1-low lane after v871 stayed cadence-neutral. Entry `95` / source frame `173` was the next largest open fixed-sector candidate, shrinking `4878 -> 4720` bytes with sector coverage `3 -> 3`. | Rewrote only entry `95` / source frame `173` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790787 -> 790629`. Transform summary: `scratch/walkstuf1-low-frame95-inplace-v872/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame95-inplace-v872-focused/20260515-010531-903907/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame95-inplace-v872-workreduction/20260515-010639-910766/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v871 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v872 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame140-inplace-v873` | `ps1: shrink walkstuf1 low frame140 payload in place` | Continue the fixed-sector W1-low lane after v872 stayed cadence-neutral. Entry `140` / source frame `248` was the next largest open fixed-sector candidate, shrinking `4296 -> 4205` bytes with sector coverage `3 -> 3`. | Rewrote only entry `140` / source frame `248` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790629 -> 790538`. Transform summary: `scratch/walkstuf1-low-frame140-inplace-v873/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame140-inplace-v873-focused/20260515-011423-954822/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame140-inplace-v873-workreduction/20260515-011531-961671/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v872 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v873 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame108-inplace-v874` | `ps1: shrink walkstuf1 low frame108 payload in place` | Continue the fixed-sector W1-low lane after v873 stayed cadence-neutral. Entry `108` / source frame `216` was the next largest open fixed-sector candidate, shrinking `4271 -> 4187` bytes with sector coverage `3 -> 3`. | Rewrote only entry `108` / source frame `216` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790538 -> 790454`. Transform summary: `scratch/walkstuf1-low-frame108-inplace-v874/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame108-inplace-v874-focused/20260515-012118-994708/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame108-inplace-v874-workreduction/20260515-012228-1001791/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v873 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v874 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame109-inplace-v875` | `ps1: shrink walkstuf1 low frame109 payload in place` | Continue the fixed-sector W1-low lane after v874 stayed cadence-neutral. Entry `109` / source frame `217` was the next largest open fixed-sector candidate, shrinking `4365 -> 4292` bytes with sector coverage `3 -> 3`. | Rewrote only entry `109` / source frame `217` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790454 -> 790381`. Transform summary: `scratch/walkstuf1-low-frame109-inplace-v875/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame109-inplace-v875-focused/20260515-012952-1044598/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame109-inplace-v875-workreduction/20260515-013059-1051407/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v874 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v875 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame107-inplace-v876` | `ps1: shrink walkstuf1 low frame107 payload in place` | Continue the fixed-sector W1-low lane after v875 stayed cadence-neutral. Entry `107` / source frame `214` was the next largest open fixed-sector candidate, shrinking `1959 -> 1900` bytes with sector coverage `2 -> 2`. | Rewrote only entry `107` / source frame `214` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790381 -> 790322`. Transform summary: `scratch/walkstuf1-low-frame107-inplace-v876/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame107-inplace-v876-focused/20260515-013820-1093815/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame107-inplace-v876-workreduction/20260515-013926-1100512/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v875 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v876 as the new WALKSTUF1 low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame172-inplace-v877` | `ps1: shrink building2 high frame172 payload in place` | Reopen B2-high tail trimming through no-shift/fixed-sector form after v829 showed whole-pack tail trimming moved cadence negative. Entry `172` / source frame `231` was the largest fixed-sector preserve-offset tail candidate, shrinking `1831 -> 851` bytes with sector coverage `2 -> 2`. | Rewrote only entry `172` / source frame `231` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `674798 -> 673818`. Transform summary: `scratch/building2-high-frame172-inplace-v877/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame172-inplace-v877-focused-labelmatch/20260515-015703-1199148/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame172-inplace-v877-workreduction/20260515-015812-1206115/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v703 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v877 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame39-inplace-v878` | Log-only; pack restored and PS1 rebuilt. | Test the largest non-empty W1-low preserve-offset sector-count trim after v876. Entry `39` / source frame `49` shrank `7835 -> 4724` bytes and sector coverage `4 -> 3`, aiming to convert the payload lane into a real under-99 timing win. | Generated `scratch/walkstuf1-low-frame39-inplace-v878/WALK1LOW.candidate.FG2` from the v876 pack with `--preserve-offsets --frames 39`, preserving the `1535263` byte footprint while cutting active payload `790322 -> 787211`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame39-inplace-v878-focused/20260515-021255-1290393/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame39-inplace-v878/WALK1LOW.before.FG2`, leaving v876 as the runtime baseline. | Failed/no promotion. The one-sector reduction was cadence-negative: scene/loop/target moved `1769/1477/1432 -> 1783/1491/1427`, overrun `45 -> 64`, blocking `65 -> 79`, hidden refill `20 -> 30`, loop reads/read VBlanks `58/259 -> 64/297`, while due happened to improve `11 -> 10`. Pack LBA and PS-EXE bucket stayed fixed. | Close W1-low entry39/source49 sector-count trimming on the current baseline. Remaining W1-low sector-changing trims should be treated as high-risk phase changes; prefer fixed-sector no-shift trims for safe payload reduction or generated scheduler/read ownership for speed. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-frame171-inplace-v879` | `ps1: shrink building2 high frame171 payload in place` | Continue the B2-high no-shift/fixed-sector tail-trim lane after v877 stayed cadence-neutral. Entry `171` / source frame `228` was the next largest fixed-sector preserve-offset tail candidate, shrinking `1980 -> 1025` bytes with sector coverage `2 -> 2`. | Rewrote only entry `171` / source frame `228` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `673818 -> 672863`. Transform summary: `scratch/building2-high-frame171-inplace-v879/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame171-inplace-v879-focused/20260515-021642-1312174/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame171-inplace-v879-workreduction/20260515-021752-1319242/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v877 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v879 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame96-inplace-v880` | `ps1: shrink building2 high frame96 payload in place` | Continue the B2-high no-shift/fixed-sector trim lane after v879 stayed cadence-neutral. Entry `96` / source frame `119` was the next largest fixed-sector preserve-offset candidate, shrinking `8781 -> 7944` bytes with sector coverage `5 -> 5`. | Rewrote only entry `96` / source frame `119` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `672863 -> 672026`. Transform summary: `scratch/building2-high-frame96-inplace-v880/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame96-inplace-v880-focused/20260515-022451-1359605/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame96-inplace-v880-workreduction/20260515-022557-1366267/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v879 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v880 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-early17-33-v881` | Log-only; source restored and PS1 rebuilt. | Retest the early `17..33` B2-high read group on the current v880 payload baseline because the current read plan still advertised it as a 16-sector candidate that could save three reads. | Temporarily inserted `{17, 33, 0}` ahead of the existing BUILDING2 high retained rows in `kBuilding2HighReadGroups12[]`, then ran the focused strict gate against `building2-high-frame96-inplace-v880`. Failed artifact: `scratch/ps1-perf-iterate/building2-high-early17-33-v881-focused/20260515-023218-1402775/summary.json`. The source row was removed after the failed gate. | Failed/no promotion. The row fired and cut loop reads `58 -> 55` plus blocking `54 -> 53`, but it regressed scene/loop `1602/1351 -> 1607/1356` and overrun `40 -> 45`; hidden refill stayed `18` and due stayed `7`. `foregroundPilotPlay` grew by `4` bytes and shifted hot symbols by `+4`. | Close B2-high `17..33` as a hand-authored read row on the current baseline. Future B2-high scheduler work needs generated ownership metadata or a data-shape transform that proves fewer reads without raising loop/overrun. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-high-frame138-inplace-v882` | `ps1: shrink walkstuf1 high frame138 payload in place` | Continue the W1-high fixed-sector preserve-offset payload lane after v844. Entry `138` / source frame `246` was the largest remaining fixed-sector high candidate, shrinking `4716 -> 3804` bytes with sector coverage `3 -> 3`. | Rewrote only entry `138` / source frame `246` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `861486 -> 860574`. Transform summary: `scratch/walkstuf1-high-frame138-inplace-v882/summary.json`; strict speed gate artifact after label correction: `scratch/ps1-perf-iterate/walkstuf1-high-frame138-inplace-v882-focused-labelmatch/20260515-023710-1432241/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame138-inplace-v882-workreduction/20260515-023819-1439184/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v844 profile at scene `1764`, loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`; the strict speed gate failed only because no key metric improved. | Keep v882 as the new WALKSTUF1 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame173-inplace-v883` | Log-only; pack restored and PS1 rebuilt. | Test the next largest B2-high fixed-sector preserve-offset payload candidate after v880. Entry `173` / source frame `235` shrank `1765 -> 1134` bytes with sector coverage `2 -> 2`, which looked like the same low-risk no-shift class as v877/v879/v880. | Generated `scratch/building2-high-frame173-inplace-v883/BUILDING2.candidate.FG2` from the v880 pack with `--preserve-offsets --frames 173`, preserving the `1303332` byte footprint while cutting active payload `672026 -> 671395`. Transform summary: `scratch/building2-high-frame173-inplace-v883/summary.json`; failed strict artifacts: `scratch/ps1-perf-iterate/building2-high-frame173-inplace-v883-focused/20260515-024539-1480839/building2-high-canary/headless-regtest.log` and `scratch/ps1-perf-iterate/building2-high-frame173-inplace-v883-focused-retry/20260515-024610-1484819/building2-high-canary/headless-regtest.log`. The pack was restored from `scratch/building2-high-frame173-inplace-v883/BUILDING2.before.FG2`, leaving v880 as the runtime baseline. | Failed/no promotion. Both attempts exited `137` before `JCPERF2` correctness and did not produce a summary; the retry was killed during B2-high playback around frame `132/334`, so the candidate does not prove correctness or speed. | Close B2-high entry173/source235 preserve-offset trimming on the current baseline. The remaining B2-high fixed-sector payload trims are not automatically safe; require a complete correctness summary before promotion. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-high-frame135-inplace-v884` | `ps1: shrink walkstuf1 high frame135 payload in place` | Continue the W1-high fixed-sector preserve-offset payload lane after v882. Entry `135` / source frame `243` was the next largest already-clipped high candidate, shrinking `4415 -> 3850` bytes with sector coverage `3 -> 3`. | Rewrote only entry `135` / source frame `243` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `860574 -> 860009`. Transform summary: `scratch/walkstuf1-high-frame135-inplace-v884/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame135-inplace-v884-focused/20260515-024925-1504527/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame135-inplace-v884-workreduction/20260515-025030-1511183/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v882 profile at scene `1764`, loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`; the strict speed gate failed only because no key metric improved. | Keep v884 as the new WALKSTUF1 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-high-frame139-inplace-v885` | Log-only; pack restored and PS1 rebuilt. | Test the last meaningful W1-high fixed-sector preserve-offset payload candidate after v884. Entry `139` / source frame `247` shrank `4011 -> 3668` bytes with sector coverage `3 -> 3`, aiming to finish the already-clipped high payload lane. | Generated `scratch/walkstuf1-high-frame139-inplace-v885/WALKSTUF1.candidate.FG2` from the v884 pack with `--preserve-offsets --frames 139`, preserving the `1535263` byte footprint while cutting active payload `860009 -> 859666`. Transform summary: `scratch/walkstuf1-high-frame139-inplace-v885/summary.json`; failed strict artifacts: `scratch/ps1-perf-iterate/walkstuf1-high-frame139-inplace-v885-focused/20260515-025800-1553449/walkstuf1-high/headless-regtest.log` and `scratch/ps1-perf-iterate/walkstuf1-high-frame139-inplace-v885-focused-retry/20260515-025856-1559995/walkstuf1-high/headless-regtest.log`. The pack was restored from `scratch/walkstuf1-high-frame139-inplace-v885/WALKSTUF1.before.FG2`, leaving v884 as the runtime baseline. | Failed/no promotion. Both attempts exited `137` before `JCPERF2` correctness and did not produce a summary; the retry was killed before the scene reached correctness, so the candidate does not prove correctness or speed. | Close W1-high entry139/source247 preserve-offset trimming on the current baseline. Fixed-sector W1-high trims are no longer presumed safe; require a complete correctness summary before any further payload shrink promotion. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `scene-kind-cache-v886` | Log-only; source restored and PS1 rebuilt. | Test runtime-shape idea #21 by caching a scene-family byte at `foregroundPilotRuntimeStart()` and replacing hot-path `fgSceneEquals(gFgRuntime.sceneName, ...)` checks in previous-frame delta, min-slack, direct-stage, fallthrough, and W1-low prepare-first logic. The goal was to reduce repeated string-compare policy checks without changing CD ownership or pack data. | Temporarily added `sceneKind` to `TFgPilotRuntime`, emitted a local scene-kind enum/classifier, and rewired the hot scheduler/prefetch checks to byte comparisons. The PS1 build stayed in the `217088` byte bucket, then the focused W1-low strict gate ran against `walkstuf1-low-frame107-inplace-v876`. Failed artifact: `scratch/ps1-perf-iterate/scene-kind-cache-v886-walkstuf1-low-focused/20260515-030903-1619128/summary.json`. The source change was reverted and `./scripts/build-ps1.sh` passed, restoring the current baseline. | Failed/no promotion. The code-shape shift was cadence-negative despite fixed pack LBA and EXE bucket: W1-low moved scene/loop/target `1769/1477/1432 -> 1776/1484/1429`, overrun `45 -> 55`, blocking/refill `65/20 -> 77/25`, reads `58 -> 59`, due `11 -> 12`, and `foregroundPilotPlay` grew by `252` bytes. | Close broad cached scene-kind/bitflag hot-path rewrites on the current W1-low baseline. Future generated policy work must avoid growing `foregroundPilotPlay`; prefer no-code pack metadata, cold setup-only tables, or a proven hot-symbol budget before another scheduler-source probe. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-frame170-inplace-v887` | `ps1: shrink building2 high frame170 payload in place` | Continue the B2-high fixed-sector preserve-offset trim lane after v883 proved frame173 unsafe before correctness. Entry `170` / source frame `226` was the next largest fixed-sector candidate with complete-summary potential, shrinking `1683 -> 1186` bytes while keeping sector coverage `1 -> 1`. | Rewrote only entry `170` / source frame `226` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `672026 -> 671529`. Transform summary: `scratch/building2-high-frame170-inplace-v887/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame170-inplace-v887-focused/20260515-031545-1658313/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame170-inplace-v887-workreduction/20260515-031652-1665073/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v880 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v887 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame97-inplace-v888` | `ps1: shrink building2 high frame97 payload in place` | Continue the B2-high fixed-sector preserve-offset trim lane after v887 stayed cadence-neutral. Entry `97` / source frame `121` was the next largest untouched fixed-sector candidate, shrinking `8718 -> 8258` bytes while keeping sector coverage `5 -> 5`. | Rewrote only entry `97` / source frame `121` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `671529 -> 671069`. Transform summary: `scratch/building2-high-frame97-inplace-v888/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame97-inplace-v888-focused/20260515-032404-1706988/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame97-inplace-v888-workreduction/20260515-032512-1713849/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v887 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v888 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame98-inplace-v889` | `ps1: shrink building2 high frame98 payload in place` | Continue the B2-high fixed-sector preserve-offset trim lane after v888 stayed cadence-neutral. Entry `98` / source frame `123` was the next largest untouched fixed-sector candidate, shrinking `8876 -> 8637` bytes while keeping sector coverage `6 -> 6`. | Rewrote only entry `98` / source frame `123` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `671069 -> 670830`. Transform summary: `scratch/building2-high-frame98-inplace-v889/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame98-inplace-v889-focused/20260515-033031-1744035/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame98-inplace-v889-workreduction/20260515-033138-1751090/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v888 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v889 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame174-inplace-v890` | `ps1: shrink building2 high frame174 payload in place` | Test the next largest B2-high fixed-sector preserve-offset trim after v889. Entry `174` / source frame `239` sits adjacent to the failed frame173 tail candidate, so the first gate had to prove complete JCPERF2 correctness before treating it as another work-reduction lane. | Rewrote only entry `174` / source frame `239` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `670830 -> 670665`. Transform summary: `scratch/building2-high-frame174-inplace-v890/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame174-inplace-v890-focused/20260515-033652-1781294/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame174-inplace-v890-workreduction/20260515-033759-1788379/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v889 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v890 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame99-inplace-v891` | `ps1: shrink building2 high frame99 payload in place` | Continue the B2-high fixed-sector preserve-offset trim lane after v890 stayed cadence-neutral. Entry `99` / source frame `126` was the next largest remaining fixed-sector candidate, shrinking `8843 -> 8728` bytes while keeping sector coverage `5 -> 5`. | Rewrote only entry `99` / source frame `126` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `670665 -> 670550`. Transform summary: `scratch/building2-high-frame99-inplace-v891/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame99-inplace-v891-focused/20260515-034300-1816593/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame99-inplace-v891-workreduction/20260515-034406-1823592/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v890 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v891 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame168-inplace-v892` | `ps1: shrink building2 high frame168 payload in place` | Continue the B2-high fixed-sector preserve-offset trim lane after v891 stayed cadence-neutral. Entry `168` / source frame `219` was the next largest safe fixed-sector candidate after the closed frame173 structural failure, shrinking `1372 -> 1266` bytes while keeping sector coverage `2 -> 2`. | Rewrote only entry `168` / source frame `219` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `670550 -> 670444`. Transform summary: `scratch/building2-high-frame168-inplace-v892/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame168-inplace-v892-focused-rerun/20260515-035919-1910053/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame168-inplace-v892-workreduction/20260515-040026-1916756/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v891 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v892 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame90-inplace-v894` | Log-only; pack restored and PS1 rebuilt. | Test the next largest unclosed B2-high preserve-offset sector-shrink after the closed frame89 miss. Entry `90` / source frame `109` shrank `8849 -> 4769` bytes with modeled sector coverage `5 -> 3`, aiming to turn the post-hot boundary payload into a timing-bearing read reduction without moving offsets. | Generated `scratch/building2-high-frame90-inplace-v894/BUILDING2.candidate.FG2` from the v892 pack with `--preserve-offsets --frames 90`, preserving the `1303332` byte footprint while cutting active payload `670444 -> 666364`. Transform summary: `scratch/building2-high-frame90-inplace-v894/summary.json`; failed strict artifact: `scratch/ps1-perf-iterate/building2-high-frame90-inplace-v894-focused/20260515-041955-2028544/summary.json`. The pack was restored from `scratch/building2-high-frame90-inplace-v894/BUILDING2.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate emitted complete `JCPERF2` and saved one loop read (`58 -> 57`), but visible cadence regressed: scene/loop/target `1602/1351/1311 -> 1605/1354/1310`, overrun `40 -> 44`, and hidden refill `18 -> 20`; blocking stayed `54` and due stayed `7`. The pack LBA and PS-EXE bucket stayed fixed. | Close B2-high entry90/source109 preserve-offset sector shrinking on the current baseline. Like frame89, the post-hot sector cut steals cadence despite reducing reads; future B2-high speed work needs generated deadline ownership around the accepted `206..230` shape or a data representation that proves no overrun/refill increase before timing. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-frame169-inplace-v896` | `ps1: shrink building2 high frame169 payload in place` | Continue the B2-high preserve-offset trim lane after the v894 post-hot sector-cut miss by testing a late-tail candidate adjacent to accepted tail trims. Entry `169` / source frame `223` shrank `1820 -> 1495` bytes with sector coverage `2 -> 1`, requiring a complete rerun after the first attempt exited before `JCPERF2`. | Rewrote only entry `169` / source frame `223` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `670444 -> 670119`. Transform summary: `scratch/building2-high-frame169-inplace-v896/summary.json`; first structural artifact: `scratch/ps1-perf-iterate/building2-high-frame169-inplace-v896-focused/20260515-043203-2099880/building2-high-canary/headless-regtest.log`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame169-inplace-v896-focused-rerun/20260515-043231-2103791/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame169-inplace-v896-workreduction/20260515-043338-2110662/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v892 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v896 as the new BUILDING2 high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-window-scan-v897` / `walkstuf1-low-window-scan-v897b` | Log-only; no source changes. | Test whether the now-smaller W1-low payload can trade more setup-prime/window residency for fewer active-loop CD reads without changing pack data. This was a reversible boot-arg probe before attempting generated scheduler ownership. | Ran v876 W1-low with `prefetch-window` sizes `20 KB`, `24 KB`, `28 KB`, `32 KB`, `48 KB`, and `64 KB` using `--skip-build --case-local-cd`. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-window-scan-v897/20260515-044427-2172585/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-window-scan-v897b/20260515-044735-2190728/summary.json`. | Failed/no promotion. Baseline stays `1769/1477/1432`, overrun `45`, blocking/refill `65/20`, reads/read VBlanks `58/259`, due `11`. The closest misses were `20 KB` and `24 KB` at `1783/1489/1431`, overrun `58`, with blocking/refill `78/29` and `64/28`; `28 KB` regressed to `1799/1505/1435`, `32 KB` to `1796/1502/1435`, `48 KB` to `1819/1525/1438`, and `64 KB` to `1836/1542/1443`. | Close scalar W1-low window growth on the current baseline. Lower read count alone is not enough; the larger windows increase hidden refill and/or visible blocking. Future W1-low green work should use generated read/deadline ownership, targeted read groups, or upload/static-work reduction rather than changing the global resident window size. |
| 2026-05-15 | `building2-high-rg210-226-v898` / `building2-high-rg83-95-v898b` | Log-only; source restored and PS1 rebuilt. | Retest two B2-high direct read-table candidates against the current v896 payload baseline. The refreshed plan still showed middle-cluster `210..226` as a three-read candidate, and the failed-run visible-cost planner resurfaced the lower-visible-risk `83..95` row, so both were checked before moving off hand-authored B2-high table rows. | Temporarily inserted `{210,226,0}` into `kBuilding2HighReadGroups12[]`, then restored it and tested `{83,95,0}` in the same table. Failed artifacts: `scratch/ps1-perf-iterate/building2-high-rg210-226-v898-focused/20260515-045736-2247730/summary.json` and `scratch/ps1-perf-iterate/building2-high-rg83-95-v898b-focused/20260515-045854-2255513/summary.json`. The source table was restored to v896 shape and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `210..226` saved three loop reads (`58 -> 55`) and loop-read time (`257 -> 250`) but regressed scene/loop `1602/1351 -> 1609/1358`, overrun `40 -> 47`, blocking `54 -> 62`, and refill `18 -> 26`. `83..95` saved one read (`58 -> 57`) but regressed to scene/loop/target `1609/1358/1310`, overrun `48`, blocking `63`, and refill `26`. Both kept pack LBA and PS-EXE bucket fixed, but each added a table row and shifted tracked hot symbols by `+4`. | Close these current-baseline B2-high direct rows. The remaining B2-high gap is no longer a good hand-authored table problem: even read-saving rows steal visible/refill cadence. Future B2-high green work should use generated deadline ownership, pack-side byte/work reduction with scheduler coupling, or a code-neutral metadata path instead of more local read-table rows. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building4-low-all-inplace-v899` | Log-only; pack restored and PS1 rebuilt. | Test whether the remaining BUILDING4 low preserve-offset draw-tail trims could be applied as one fixed-layout payload shrink after v895. The scan found 18 entries still mechanically trim-capable, including already-closed sector-changing frames and smaller fixed-sector tails, for a large `15131` byte active-payload reduction without moving file offsets. | Backed up `generated/ps1/foreground/BUIL4LOW.FG2`, ran `compact-fgp3-trim-draw-tails.py --preserve-offsets --copy-unparseable --in-place`, then gated B4-low against the v895 focused baseline. The transform preserved the `1714154` byte pack footprint, LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `809364 -> 794233`. Transform summary: `scratch/building4-low-all-inplace-v899/summary.json`; failed strict artifact: `scratch/ps1-perf-iterate/building4-low-all-inplace-v899-focused/20260515-050721-2303901/summary.json`. The pack was restored from `scratch/building4-low-all-inplace-v899/BUIL4LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate emitted valid `JCPERF2` and improved refill overrun `34 -> 31`, but public cadence regressed: scene/loop/target `3128/2853/2816 -> 3133/2858/2817`, overrun `37 -> 41`, blocking `40 -> 46`, loop-read VBlanks `215 -> 219`, and due misses `1 -> 2`; loop reads stayed `30`. | Close bulk catch-up trimming for B4 low. The known-bad sector-collapse entries still dominate phase even when mixed with smaller tails, so do not batch-apply mechanically trim-capable entries. Future B4-low green work should use generated deadline/static-upload ownership or isolated fixed-sector checks only when they prove exact-flat. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-low-rg218-230-after-v739-v900` / `building2-low-rg96-112-after-v739-v900b` | Log-only; source restored and PS1 rebuilt. | Retest two BUILDING2 low read-table questions on the current v739 pack-speed baseline. First, replace the accepted `{218,229,8}` row with the older `{218,230,8}` near-miss to see if the v739 pack shape absorbed the prior one-refill failure without adding code rows. Then test the refreshed planner's narrower `96..112` current-window cluster, which was smaller than the already-closed `96..120` probe. | Temporarily changed `kBuilding2LowReadGroups12[]` from `{218,229,8}` to `{218,230,8}`, restored it, then temporarily inserted `{96,112,0}` ahead of the accepted low rows. Failed artifacts: `scratch/ps1-perf-iterate/building2-low-rg218-230-after-v739-v900-focused/20260515-051243-2334318/summary.json` and `scratch/ps1-perf-iterate/building2-low-rg96-112-after-v739-v900b-focused/20260515-051406-2342557/summary.json`. The accepted source table was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `{218,230,8}` stayed timing-flat at `1603`, `1339/1317`, overrun `22`, and refill `0`, but regressed blocking `53 -> 54` and produced no key improvement. `{96,112,0}` saved reads (`37 -> 34`) but regressed scene/loop/target `1603/1339/1317 -> 1608/1344/1313`, overrun `22 -> 31`, blocking `53 -> 57`, refill `0 -> 2`, and due `12 -> 11`. Both kept pack LBA and PS-EXE bucket fixed; the added `96..112` row grew/shuffled hot symbols by `+208` bytes. | Close these current-baseline B2-low scalar retries. The accepted `{218,229,8}` row remains the only safe hand-authored salvage; wider/tighter scalar forms either become inert-plus-worse-blocking or trade read count for visible/refill debt. Future B2-low green work should use generated deadline/refill ownership, upload/restore reduction, or a pack-side change that preserves `prefetch_overrun_vb=0`. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg279-311-v900` | Log-only; source restored and PS1 rebuilt. | Test the largest current W1-low modeled retained-append cluster after scalar window growth closed. The refreshed read plan ranked sectors `279..311` as a seven-saved-read candidate spanning entries `143..155`, but it was a tight visible-cadence cluster, so the binary question was whether the existing W1-low large setup/window capacity could absorb the append without converting it into refill debt. | Temporarily inserted `{279,311,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the W1-low focused gate against the v876 payload baseline. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg279-311-v900-focused/20260515-052223-2390590/summary.json`. The source table was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate stayed in the fixed `217088` byte PS-EXE bucket and reduced loop reads `58 -> 51` plus due misses `11 -> 10`, but visible cadence regressed: scene/loop/target `1769/1477/1432 -> 1784/1492/1431`, overrun `45 -> 61`, blocking `65 -> 77`, and hidden refill `20 -> 31`; loop-read VBlanks were effectively flat at `259 -> 255`. `group_hits=0`, so the table row mostly changed hot code/layout and scheduling phase instead of activating a useful grouped append. | Close W1-low `279..311` as a hand-authored read-table row on the current baseline. The read-count signal is real but not promotable without generated deadline/refill ownership; do not retry this region as a simple local table insertion. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-high-rg156-164-v901` | Log-only; source restored and PS1 rebuilt. | Test the refreshed W1-high low-visible-risk retained-read candidate after W1-low scalar grouping closed. Sectors `156..164` were an 8-sector, zero-overread, one-saved-read candidate with `append_start_fireable=true`, making it a cheap binary check against the current v884 high baseline. | Temporarily inserted `{156,164,0}` after the accepted `{78,91,0}` row in `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran the W1-high focused gate against `walkstuf1-high-frame135-inplace-v884`. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg156-164-v901-focused/20260515-053031-2438816/summary.json`. The source row was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. The row kept pack LBA `24883`, pack sectors `750`, and the fixed `217088` byte PS-EXE bucket, but visible cadence regressed: scene/loop/target `1764/1476/1434 -> 1774/1486/1427`, overrun `42 -> 59`, blocking `81 -> 102`, and due `16 -> 17`; loop reads stayed `63` and hidden refill stayed `23`. Hot symbol addresses shifted by `-4` from the table insertion. | Close W1-high `156..164` as a hand-authored read-table row on the current baseline. Even the lower-visible-risk mid-pack row turns into visible blocking without generated scheduler ownership; future W1-high green work should use generated deadline/refill metadata, pack-side byte/work reduction with scheduler coupling, or upload/restore reduction rather than another local table insertion. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-high-rg238-246-v902` | Log-only; source restored and PS1 rebuilt. | Test the other refreshed low-visible-risk W1-high one-read candidate before fully closing the current hand-authored mid-pack table lane. Sectors `238..246` were zero-overread and `append_start_fireable=true`, but sat after the accepted `213..229` row, so it needed a strict binary gate. | Temporarily inserted `{238,246,0}` after the accepted `{213,229,0}` row in `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran the W1-high focused gate against the v884 baseline. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg238-246-v902-focused/20260515-053411-2460300/summary.json`. The source row was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. The row kept pack LBA `24883`, pack sectors `750`, and the fixed `217088` byte PS-EXE bucket, but regressed scene/loop/target `1764/1476/1434 -> 1778/1490/1420`, overrun `42 -> 70`, blocking `81 -> 111`, hidden refill `23 -> 32`, and due `16 -> 19`; loop reads worsened `63 -> 66`. | Close W1-high `238..246` as a hand-authored table row on the current baseline. Together with v901, the refreshed low-visible-risk W1-high scalar rows are proven phase-negative; future W1-high green work needs generated scheduler ownership or non-table pack/render data changes. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building4-low-frame41-inplace-v903` | `ps1: shrink building4 low frame41 payload in place` | Continue the BUILDING4-low fixed-sector preserve-offset lane after v895 and the rejected v899 bulk trim. Entry `41` / source frame `72` was the largest remaining same-sector row, shrinking `8852 -> 8518` bytes while keeping sector coverage `5 -> 5`; isolating it avoided the known-bad sector-collapse rows from the bulk miss. | Rewrote only entry `41` / source frame `72` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `809364 -> 809030`. Transform summary: `scratch/building4-low-frame41-inplace-v903/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building4-low-frame41-inplace-v903-focused/20260515-053728-2479508/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame41-inplace-v903-workreduction/20260515-053902-2488765/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v895 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v903 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-high-rg304-310-v904` | Log-only; source restored and PS1 rebuilt. | Close the remaining untested one-read W1-high suffix table row after `292..298` and `306..312` were already exact-flat. The current read-plan still listed `304..310` as a fireable one-read candidate, but prior suffix rows made promotion require a strict improvement gate. | Temporarily inserted `{304,310,0}` after the accepted `{213,229,0}` row in `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran the W1-high focused gate against the v884 baseline. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg304-310-v904-focused/20260515-055241-2566890/summary.json`. The source row was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. The row kept pack LBA `24883`, pack sectors `750`, and the fixed `217088` byte PS-EXE bucket, but it was exact-flat: scene `1764`, loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, reads/read VBlanks `63/276`, and due `16`. The strict gate failed only because no key metric improved. | Close W1-high `304..310` as another inert scalar suffix row. The remaining suffix table variants do not expose a work win through hand-authored grouped append; future W1-high green work needs generated scheduler ownership or pack/render data changes. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-rg210-226-slack-v905-v907` | Log-only; source restored and PS1 rebuilt. | Sweep guarded variants of the strongest current B2-high read-saving miss. The unguarded `210..226` row saved three loop reads but regressed visible/refill cadence, so the binary question was whether a min-slack threshold could keep only safe ownership slots. | Temporarily inserted `{210,226,minSlack}` after `{206,230,0}` in `kBuilding2HighReadGroups12[]` and tested minSlack `8`, `4`, and `6` against the v896 baseline. Failed artifacts: slack8 `scratch/ps1-perf-iterate/building2-high-rg210-226-slack8-v905-focused/20260515-055627-2588823/summary.json`, slack4 `scratch/ps1-perf-iterate/building2-high-rg210-226-slack4-v906-focused/20260515-055751-2597403/summary.json`, and slack6 `scratch/ps1-perf-iterate/building2-high-rg210-226-slack6-v907-focused/20260515-055914-2605860/summary.json`. The source row was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. Slack8 and slack6 were exact-flat at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, reads/read VBlanks `58/257`, and due `7`, so they failed only for no key improvement. Slack4 reproduced the unsafe ownership: reads improved `58 -> 55`, but scene/loop regressed `1602/1351 -> 1609/1358`, overrun `40 -> 47`, blocking/refill `54/18 -> 62/26`, and no key speed metric improved. | Close guarded B2-high `210..226` as a local read-table path. Threshold `4` fires and is phase-negative; thresholds `6` and `8` are safe but inert. Future B2-high green work needs generated deadline ownership that can prove a non-visible slot before adding hot code, or pack/render data reduction with scheduler coupling. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-frame110-inplace-v908` | `ps1: shrink walkstuf1 low frame110 payload in place` | Continue the W1-low fixed-sector preserve-offset lane after v876 and the scalar read/window misses. Entry `110` / source frame `218` was the largest remaining unclosed fixed-sector trim, shrinking `4671 -> 4616` bytes while keeping sector coverage `3 -> 3`. | Rewrote only entry `110` / source frame `218` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790322 -> 790267`. Transform summary: `scratch/walkstuf1-low-frame110-inplace-v908/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame110-inplace-v908-focused/20260515-060609-2646402/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame110-inplace-v908-workreduction/20260515-060716-2653210/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v876 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v908 as the new W1-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame111-inplace-v909` | `ps1: shrink walkstuf1 low frame111 payload in place` | Continue the W1-low fixed-sector preserve-offset lane after v908 stayed cadence-neutral. Entry `111` / source frame `219` was the next largest same-sector trim, shrinking `4974 -> 4940` bytes while keeping sector coverage `4 -> 4`. | Rewrote only entry `111` / source frame `219` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790267 -> 790233`. Transform summary: `scratch/walkstuf1-low-frame111-inplace-v909/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame111-inplace-v909-focused/20260515-061642-2707106/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame111-inplace-v909-workreduction/20260515-061750-2714032/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v908 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v909 as the new W1-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-frame106-inplace-v910` | `ps1: shrink walkstuf1 low frame106 payload in place` | Continue the W1-low fixed-sector preserve-offset lane after v909 stayed cadence-neutral. Entry `106` / source frame `209` was the next same-sector trim, shrinking `944 -> 919` bytes while keeping sector coverage `2 -> 2`. | Rewrote only entry `106` / source frame `209` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `25633/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `790233 -> 790208`. Transform summary: `scratch/walkstuf1-low-frame106-inplace-v910/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame106-inplace-v910-focused/20260515-062433-2752205/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-low-frame106-inplace-v910-workreduction/20260515-062538-2758861/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v909 profile at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads/read VBlanks `58/259`, and due `11`; the strict speed gate failed only because no key metric improved. | Keep v910 as the new W1-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-frame293-inplace-v911` | `ps1: shrink building4 low frame293 payload in place` | Continue the BUILDING4-low fixed-sector preserve-offset lane after v903 and the W1-low payload cleanup. Entry `293` / source frame `449` was the largest remaining fixed-sector B4-low candidate, shrinking `604 -> 329` bytes while keeping sector coverage `1 -> 1`. | Rewrote only entry `293` / source frame `449` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `809030 -> 808755`. Transform summary: `scratch/building4-low-frame293-inplace-v911/summary.json`; strict speed gate artifact with the corrected baseline label: `scratch/ps1-perf-iterate/building4-low-frame293-inplace-v911-focused-match/20260515-063442-2811353/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame293-inplace-v911-workreduction/20260515-063619-2820890/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v903 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v911 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-frame284-inplace-v912` | `ps1: shrink building4 low frame284 payload in place` | Continue the BUILDING4-low fixed-sector preserve-offset lane after v911 stayed cadence-neutral. Entry `284` / source frame `434` was the next largest fixed-sector B4-low candidate, shrinking `8484 -> 8267` bytes while keeping sector coverage `5 -> 5`. | Rewrote only entry `284` / source frame `434` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `808755 -> 808538`. Transform summary: `scratch/building4-low-frame284-inplace-v912/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building4-low-frame284-inplace-v912-focused/20260515-064442-2868786/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame284-inplace-v912-workreduction/20260515-064619-2878246/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v911 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v912 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-frame283-inplace-v913` | `ps1: shrink building4 low frame283 payload in place` | Continue the BUILDING4-low fixed-sector preserve-offset lane after v912 stayed cadence-neutral. Entry `283` / source frame `432` was the next fixed-sector B4-low candidate, shrinking `8571 -> 8445` bytes while keeping sector coverage `5 -> 5`. | Rewrote only entry `283` / source frame `432` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `808538 -> 808412`. Transform summary: `scratch/building4-low-frame283-inplace-v913/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building4-low-frame283-inplace-v913-focused/20260515-065321-2918708/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame283-inplace-v913-workreduction/20260515-065458-2928182/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v912 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v913 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame173-inplace-v914` | `ps1: shrink building2 high frame173 payload in place` | Retry the largest remaining fixed-sector B2-high payload candidate after v883 had only pre-JCPERF2 structural artifacts. Entry `173` / source frame `235` still shrank `1765 -> 1134` bytes while keeping sector coverage `2 -> 2`, so the binary question was whether the current harness could prove correctness and no regression. | Rewrote only entry `173` / source frame `235` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `670119 -> 669488`. Transform summary: `scratch/building2-high-frame173-inplace-v914/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building2-high-frame173-inplace-v914-focused/20260515-070623-2993675/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame173-inplace-v914-workreduction/20260515-070731-3000534/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v896 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed gate failed only because no key metric improved. | Keep v914 as the new BUILDING2-high payload baseline. Public rollup remains about `+0.2696%` over target / `99.7347%` target speed, raw signed remains about `-0.4974%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `johnny1-black-clear-v915` | Log-only; source restored and PS1 rebuilt. | Retry the JOHNNY1 black-backdrop cleanup/upload signal as a code-neutral primitive path after v393 improved high tide only by growing the PS-EXE bucket and shifting pack LBAs. Fresh v915 paired baseline confirmed both tides at scene `2069`, loop/target `1973/1945`, overrun `28`, blocking/refill `25/25`, reads `7`, due `0`, restore bytes `408872`, upload bytes `5631360`, and dirty rows `8799`. | Tested two in-place variants in `grRestoreCleanBgSpanFromRects()` while keeping the `217088` byte PS-EXE bucket and `JOHNNY1.FG2` LBA `13983`: first a `DrawPrim` TILE clear path, then a lower-overhead direct `GPU_GP0` TILE packet path. Baseline artifact: `scratch/ps1-perf-iterate/johnny1-current-v915-baseline/20260515-071848-3064606/summary.json`; DrawPrim high artifact: `scratch/ps1-perf-iterate/johnny1-black-tile-clear-v915-focused/20260515-072255-3089006/johnny1-high-v915-baseline/perf-summary.json`; DrawPrim low structural artifact: `scratch/ps1-perf-iterate/johnny1-black-tile-clear-v915-focused/20260515-072255-3089006/johnny1-low-v915-baseline/perf-summary.json`; GP0 strict artifact: `scratch/ps1-perf-iterate/johnny1-black-gp0-clear-v915-high/20260515-072611-3109013/summary.json`. | Failed/no promotion. Both primitive forms reduced high-tide upload work (`5631360 -> 5225600` bytes, dirty rows `8799 -> 8165`, upload rects `207 -> 205`) with fixed pack LBA and fixed PS-EXE bucket, but high stayed exact-flat on key timing at scene `2069`, loop/target `1973/1945`, overrun `28`, blocking/refill `25/25`, reads `7`, and due `0`; the GP0 strict gate failed only for no key improvement. The DrawPrim low rerun died before `JCPERF2`, and high had already failed the speed requirement. | Close code-neutral per-span black TILE clears as a JOHNNY1 speed lane on the current baseline. The remaining signal is real work reduction but not target-speed movement; the v393 high win was not reproducible without its unsafe layout/bucket drift. Future JOHNNY1 green work should use generated/coalesced black cleanup metadata, a pack opcode that reduces CPU restore work, or a scheduler-owned data-shape change rather than per-span GPU clears. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-rg17-33-v916` | Log-only; source restored and PS1 rebuilt. | Retest the strongest unclosed early BUILDING2 high read-plan row after the v914 payload baseline. Candidate `17..33` promised three saved reads with a long first visible gap, but the current plan still marked tight internal cadence risk, so the binary question was whether a local row or min-slack guard could make it safe. | Temporarily inserted `{17,33,minSlack}` before the accepted high-tide read groups in `kBuilding2HighReadGroups12[]`, testing unguarded, slack-4, and slack-2 variants against the v914 focused baseline. Failed artifacts: unguarded `scratch/ps1-perf-iterate/building2-high-rg17-33-v916-focused/20260515-073110-3137510/summary.json`; slack-4 `scratch/ps1-perf-iterate/building2-high-rg17-33-slack4-v916-focused/20260515-073230-3145169/summary.json`; slack-2 structural log `scratch/ps1-perf-iterate/building2-high-rg17-33-slack2-v916-focused/20260515-073458-3159189/building2-high-canary/headless-regtest.log`. The source row was removed afterward and `./scripts/build-ps1.sh` passed. | Failed/no promotion. The unguarded row fired and reduced loop reads `58 -> 55` plus blocking `54 -> 53`, but regressed scene/loop `1602/1351 -> 1607/1356` and overrun `40 -> 45` while refill stayed `18`. Slack-4 was exact-flat/inert at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, reads `58`, and due `7`. Slack-2 did not emit complete `JCPERF2`; the run reached only frame `131/334` before exit `137`, so it is a hard structural failure for promotion. | Close B2-high `17..33` as a hand-authored read-table lane on the current baseline. The saved reads are real when unguarded, but the row either steals visible cadence, becomes inert, or fails to complete. Future B2-high green work should use generated deadline ownership, code-neutral metadata, or pack/render data reduction with scheduler coupling. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-frame132-inplace-v916` | Log-only; pack restored and PS1 rebuilt. | Probe the largest under-99 row with an unclosed one-sector preserve-offset trim outside the already bad W1-low frame55/frame60 family. Entry `132` / source frame `240` shrank `4402 -> 2967` bytes with modeled sector coverage `4 -> 3`, so the test asked whether a late-tail sector-boundary shrink could move timing without the earlier phase debt. | Backed up `generated/ps1/foreground/WALK1LOW.FG2`, rewrote only entry `132` in place with `compact-fgp3-trim-draw-tails.py --preserve-offsets --frames 132`, and rebuilt inside the fixed `217088` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-frame132-inplace-v916/summary.json`; failed focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame132-inplace-v916-focused/20260515-074043-3192999/summary.json`. The pack was restored from `scratch/walkstuf1-low-frame132-inplace-v916/WALK1LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Scene/loop/target stayed `1769/1477/1432` and overrun stayed `45`, but the sector-boundary shrink regressed blocking `65 -> 66` and hidden refill `20 -> 21`; loop reads/read VBlanks stayed `58/259` and due stayed `11`. The strict gate failed for refill regression and no key metric improvement. | Close W1-low entry132/source240 sector-boundary shrinking on the v910 baseline. Late-tail placement is still cadence-sensitive even when the visible timing stays flat, so future W1-low trim probes should prefer fixed-sector work reductions or generated deadline/read ownership before more sector-collapse attempts. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `read-groups-cold-data-refactor-v917` | Log-only; source restored and PS1 rebuilt. | First big-swing attempt at code-neutral scheduler/read metadata: move the accepted read-group tables from `foreground_pilot.c` into a separate cold data translation unit so future candidate rows could be tested as data changes instead of hot playback edits. The acceptance condition was a no-regression canary over the current under-99 leaders before trying any new row. | Added `src/foreground_read_groups.{c,h}`, exported fixed counts, changed `foreground_pilot.c` to use the external arrays, and rebuilt successfully. Focused canary artifact: `scratch/ps1-perf-iterate/read-groups-cold-data-refactor-v917-canary/20260515-075636-3288739/`. W1-low completed and stayed at the current baseline (`1769/1477/1432`, overrun `45`, blocking/refill `65/20`, reads `58`), but B2-high exited `137` before `JCPERF2` at frame `184/334`; log: `scratch/ps1-perf-iterate/read-groups-cold-data-refactor-v917-canary/20260515-075636-3288739/building2-high/headless-regtest.log`. | Failed/no promotion. The refactor kept the `217088` byte PS-EXE bucket and early foreground helper addresses fixed, but grew `foregroundPilotPlay` by `8` bytes and shifted following hot symbols by `+8`; B2-high never proved correctness or speed. | Close separate-object read-group decoupling as unsafe on the current layout. The next scheduler/read big swing should avoid hot-code growth entirely by replacing an existing same-length table row or by generating metadata into an already-sized slot before attempting new groups. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg410-422-replace-tail-v918` | Log-only; source restored and PS1 rebuilt. | Test a same-length, no-hot-growth scheduler/read candidate after separate-object read groups failed. The planner ranked `410..422` as W1-low's cleanest current `scheduler-owned-candidate`, and the table already had nearly duplicate tail rows `{443,455}` and `{444,456}`, so the binary was whether replacing `{444,456}` with `{410,422}` could save a read without changing table size or hot code. | Temporarily changed the final W1-low read group from `{444,456,0}` to `{410,422,0}`, rebuilt, and ran the strict W1-low gate against the v910 baseline with exact hot-symbol identity. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg410-422-replace-tail-v918-focused/20260515-080214-3321810/summary.json`. The source row was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The replacement kept pack LBA `25633` and PS-EXE bucket `217088`, and it saved one loop read (`58 -> 57`), but visible timing regressed: scene/loop/target `1769/1477/1432 -> 1770/1478/1432`, overrun `45 -> 46`, while blocking/refill stayed `65/20` and due stayed `11`. The strict gate also reported no key metric improvement. | Close W1-low `410..422` as a same-length replacement lane. The saved read is real, but without explicit deadline/refill ownership it still steals one visible VBlank. Next same-length retries should prefer different low-risk slots (`190..202`, `273..285`) or non-read work reduction. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg190-202-replace-tail-v919` | Log-only; source restored and PS1 rebuilt. | Test the other low-risk W1-low scheduler-owned row as a same-length replacement after `410..422` saved a read but regressed visible cadence. Candidate `190..202` had one modeled saved read and a long internal gap, and used the same `{444,456}` replacement slot to avoid table-size and hot-code changes. | Temporarily changed the final W1-low read group from `{444,456,0}` to `{190,202,0}`, rebuilt, and ran the strict W1-low gate against the v910 baseline with exact hot-symbol identity. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg190-202-replace-tail-v919-focused/20260515-080424-3334791/summary.json`. The source row was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The replacement was exact-flat at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads `58`, and due `11`; the strict gate failed only because no key metric improved. | Close W1-low `190..202` as inert on the current baseline. The planner still lists it because the observed read pair remains, but the runtime slot does not convert it into a grouped append. Future W1-low read attempts should either change ownership timing explicitly or move to non-read work reduction. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg273-285-replace-tail-v920` | Log-only; source restored and PS1 rebuilt. | Close the remaining same-length W1-low low-risk replacement idea before pivoting away from scalar read groups. Candidate `273..285` appeared as a later low-risk row after the `410..422` probe, so it was tested in the same `{444,456}` replacement slot with exact hot-symbol identity. | Temporarily changed the final W1-low read group from `{444,456,0}` to `{273,285,0}`, rebuilt, and ran the strict W1-low gate against the v910 baseline. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg273-285-replace-tail-v920-focused/20260515-080653-3349234/summary.json`. The source row was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The replacement was exact-flat at scene `1769`, loop/target `1477/1432`, overrun `45`, blocking/refill `65/20`, loop reads `58`, and due `11`; the strict gate failed only because no key metric improved. | Close W1-low same-length scalar replacement for the current baseline. `410..422` fires but steals a visible VBlank, while `190..202` and `273..285` are inert. Future W1-low green work needs explicit deadline/refill ownership or a non-read work reduction. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-rg17-33-replace249-v921` | Log-only; source restored and PS1 rebuilt. | Move the no-hot-growth replacement idea to B2-high after W1-low scalar replacements closed. Earlier insertion of `17..33` saved reads and one blocking VBlank but regressed visible cadence; this test replaced the small tail group `{249,257}` with `{17,33}` to keep the table size fixed and see if less total grouping debt would preserve the early read/blocking signal. | Temporarily changed the final B2-high read group from `{249,257,0}` to `{17,33,0}`, rebuilt, and ran the strict B2-high gate against the v914 baseline with exact hot-symbol identity. Failed artifact: `scratch/ps1-perf-iterate/building2-high-rg17-33-replace249-v921-focused/20260515-080900-3361849/summary.json`; log: `scratch/ps1-perf-iterate/building2-high-rg17-33-replace249-v921-focused/20260515-080900-3361849/building2-high-canary/headless-regtest.log`. The source row was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The run exited `137` before complete `JCPERF2`, last heartbeat `frame=290/334`, so correctness and speed were not proven. The build stayed in the `217088` byte PS-EXE bucket with hot symbol sizes unchanged, but the missing metrics make it a hard failure. | Close B2-high `17..33` same-length replacement. The early read cluster remains too cadence-sensitive even without table growth; future B2-high work should pivot to pack/render work reduction or a scheduler gate that proves completion before claiming ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-frame90-inplace-v922` | Log-only; pack restored and PS1 rebuilt. | Take the next large B2-high pack/render swing after scalar read-table replacement failed. Entry `90` / source frame `109` was the next unclosed sector-collapsing candidate after the known-bad frame89 row, shrinking `8849 -> 4769` bytes with modeled sector coverage `5 -> 3`, so the test asked whether a nearby but distinct large payload collapse could reduce read pressure without stealing visible cadence. | Backed up `generated/ps1/foreground/BUILDING2.FG2`, rewrote only entry `90` in place with `compact-fgp3-trim-draw-tails.py --preserve-offsets --frames 90`, and rebuilt inside the fixed `217088` byte PS-EXE bucket with pack LBA/sectors unchanged at `6181/637`. Transform summary: `scratch/building2-high-frame90-inplace-v922/summary.json`; failed strict artifact: `scratch/ps1-perf-iterate/building2-high-frame90-inplace-v922-focused/20260515-081409-3391930/summary.json`. The pack was restored from `scratch/building2-high-frame90-inplace-v922/BUILDING2.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The trim reduced active payload `669488 -> 665408` and loop reads `58 -> 57`, but regressed scene/loop `1602/1351 -> 1605/1354`, overrun `40 -> 44`, and refill overrun `18 -> 20`; blocking stayed `54` and due stayed `7`. The strict gate also reported no key metric improvement. | Close B2-high entry90/source109 sector-collapse shrinking on the v914 baseline. The saved read is real, but this payload shift moves work into the wrong cadence window, matching the broader B2-high lesson: sector-boundary trims need scheduler/deadline ownership before they can be promotable. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building4-low-frame289-inplace-v923` | Log-only; pack restored and PS1 rebuilt. | Isolate the largest unclosed B4-low late-tail sector-collapse candidate after the v899 bulk trim proved the batch was unsafe. Entry `289` / source frame `441` shrank `8565 -> 4827` bytes with modeled sector coverage `5 -> 3`, so this tested whether the late-tail component alone could keep the useful refill reduction from the bulk run without importing its visible cadence debt. | Backed up `generated/ps1/foreground/BUIL4LOW.FG2`, rewrote only entry `289` in place with `compact-fgp3-trim-draw-tails.py --preserve-offsets --frames 289`, and rebuilt inside the fixed `217088` byte PS-EXE bucket with pack LBA/sectors unchanged at `9118/837`. Transform summary: `scratch/building4-low-frame289-inplace-v923/summary.json`; failed strict artifact: `scratch/ps1-perf-iterate/building4-low-frame289-inplace-v923-focused/20260515-081720-3410485/summary.json`. The pack was restored from `scratch/building4-low-frame289-inplace-v923/BUIL4LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The trim reduced active payload `808412 -> 804674` and improved refill overrun `34 -> 31`, but regressed scene/loop `3128/2853 -> 3133/2858`, overrun `37 -> 41`, blocking `40 -> 46`, and due misses `1 -> 2`; loop reads stayed `30`. | Close B4-low entry289/source441 as the dominant unsafe piece of the v899 bulk trim. The refill win is real, but it comes with larger visible blocking debt; future B4-low green work should avoid sector-collapse trims unless paired with explicit deadline/refill ownership. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building4-low-frame40-inplace-v924` | `ps1: shrink building4 low frame40 payload` | Continue the B4-low fixed-sector preserve-offset lane after v923 isolated the unsafe sector-collapse row. Entry `40` / source frame `70` was the largest remaining same-sector row in the refreshed scan, shrinking `8788 -> 7899` bytes while keeping sector coverage `5 -> 5`. | Rewrote only entry `40` / source frame `70` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `808412 -> 807523`. Transform summary: `scratch/building4-low-frame40-inplace-v924/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/building4-low-frame40-inplace-v924-focused/20260515-081951-3425123/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame40-inplace-v924-workreduction/20260515-082122-3434512/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v913 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due `1`; the strict speed gate failed only because no key metric improved. | Keep v924 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-frame91-inplace-v925` | Log-only; pack restored and PS1 rebuilt. | Test the no-decode preserve-offset variant of the B2-high frame91 boundary after earlier frame91 D4/gap1 probes either added runtime cost or stayed inert. Entry `91` / source frame `111` shrank `8867 -> 5427` bytes with modeled sector coverage `6 -> 4`, making it the next large post-hot payload-collapse row after the now-closed frame89/frame90 misses. | Backed up `generated/ps1/foreground/BUILDING2.FG2`, rewrote only entry `91` in place with `compact-fgp3-trim-draw-tails.py --preserve-offsets --frames 91`, and rebuilt inside the fixed `217088` byte PS-EXE bucket with pack LBA/sectors unchanged at `6181/637`. Transform summary: `scratch/building2-high-frame91-inplace-v925/summary.json`; failed strict artifact: `scratch/ps1-perf-iterate/building2-high-frame91-inplace-v925-focused/20260515-082907-3478196/summary.json`. The pack was restored from `scratch/building2-high-frame91-inplace-v925/BUILDING2.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The trim reduced active payload `669488 -> 666048` and loop reads `58 -> 57`, but regressed scene/loop `1602/1351 -> 1605/1354`, overrun `40 -> 44`, and refill overrun `18 -> 20`; blocking stayed `54` and due stayed `7`. | Close B2-high entry91/source111 preserve-offset sector-collapse shrinking on the v914 baseline. Frame90 and frame91 now share the same negative cadence signature: saved reads are real, but unowned boundary shifts move work into a visible/refill window. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-high-frame100-inplace-v926` | `ps1: shrink building2 high frame100 payload` | After the post-hot sector-collapse misses, return B2-high to the safe fixed-sector payload lane. Entry `100` / source frame `129` was the largest remaining same-sector B2-high trim, shrinking `8701 -> 8621` bytes while keeping sector coverage `5 -> 5`. | Rewrote only entry `100` / source frame `129` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the `1303332` byte file size, all entry offsets, pack LBA/sectors `6181/637`, and the `217088` byte PS-EXE bucket while shrinking active payload `669488 -> 669408`. Transform summary: `scratch/building2-high-frame100-inplace-v926/summary.json`; first strict run exited `137` before `JCPERF2` at `scratch/ps1-perf-iterate/building2-high-frame100-inplace-v926-focused/20260515-083200-3495192/building2-high-canary/headless-regtest.log`; strict rerun artifact: `scratch/ps1-perf-iterate/building2-high-frame100-inplace-v926-rerun/20260515-083251-3501099/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building2-high-frame100-inplace-v926-workreduction/20260515-083359-3507826/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v914 profile at scene `1602`, loop/target `1351/1311`, overrun `40`, blocking/refill `54/18`, loop reads/read VBlanks `58/257`, and due `7`; the strict speed rerun failed only because no key metric improved. | Keep v926 as the new BUILDING2-high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-high-frame139-inplace-v927` | `ps1: shrink walkstuf1 high frame139 payload` | Continue the W1-high fixed-sector preserve-offset lane after the v884 frame135 baseline. Entry `139` / source frame `247` was the largest remaining same-sector high-tide trim, shrinking `4011 -> 3668` bytes while keeping sector coverage `3 -> 3`. | Rewrote only entry `139` / source frame `247` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the `1535263` byte file size, all entry offsets, pack LBA/sectors `24883/750`, and the `217088` byte PS-EXE bucket while shrinking active payload `860009 -> 859666`. Transform summary: `scratch/walkstuf1-high-frame139-inplace-v927/summary.json`; strict speed gate artifact: `scratch/ps1-perf-iterate/walkstuf1-high-frame139-inplace-v927-focused/20260515-084534-3573454/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame139-inplace-v927-workreduction/20260515-084642-3580260/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v884 profile at scene `1764`, loop/target `1476/1434`, overrun `42`, blocking/refill `81/23`, loop reads/read VBlanks `63/276`, and due `16`; the strict speed gate failed only because no key metric improved. | Keep v927 as the new WALKSTUF1-high payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-frame425-inplace-v928` | `ps1: shrink building4 low frame425 payload` | Continue the B4-low fixed-sector preserve-offset lane after v924. Entry `425` / source frame `619` was the largest remaining same-sector B4-low trim, shrinking `661 -> 401` bytes while keeping sector coverage `2 -> 2`. | Rewrote only entry `425` / source frame `619` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the `1714154` byte file size, all entry offsets, pack LBA/sectors `9118/837`, and the `217088` byte PS-EXE bucket while shrinking active payload `807523 -> 807263`. Transform summary: `scratch/building4-low-frame425-inplace-v928/summary.json`; first strict run exited `137` before `JCPERF2` at `scratch/ps1-perf-iterate/building4-low-frame425-inplace-v928-focused/20260515-085419-3623505/building4-low-canary/headless-regtest.log`; strict rerun artifact: `scratch/ps1-perf-iterate/building4-low-frame425-inplace-v928-rerun/20260515-085557-3633897/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-frame425-inplace-v928-workreduction/20260515-085732-3643230/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat on the v924 profile at scene `3128`, loop/target `2853/2816`, overrun `37`, blocking/refill `40/34`, loop reads/read VBlanks `30/215`, and due `1`; the strict speed rerun failed only because no key metric improved. | Keep v928 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2697%` over target / `99.7347%` target speed, raw signed remains `-0.4975%` / `100.5171%`, and bands remain `117` green / `9` yellow. Methodology total remains about `17.13` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-runtime-narrow-upload-v929` | Log-only; source restored and PS1 rebuilt. | Test the next big-swing upload-byte idea on B4-low only: reuse the existing dirty-row X bounds to upload partial-width framebuffer bands, repacking the band into the existing SCR stream scratch area instead of allocating new heap. | Temporarily added an opt-in `grDrawBackground()` narrow-upload path and enabled it only for `building4` low tide. The first align probe missed `JCPERF2`; align-8 emitted GPU transfer rounding and unknown GP0 commands; align-16 and align-16-plus-product-guard still stalled before the first `JCHB`/`JCPERF2`. Failed artifacts: `scratch/ps1-perf-narrow-b4-v929/20260515-092257-3786203/summary.json`, `scratch/ps1-perf-narrow-b4-v929-align8/20260515-092757-3814201/building4-low-narrow-v929/headless-regtest.log`, `scratch/ps1-perf-narrow-b4-v929-align16/20260515-093109-3834052/summary.json`, and `scratch/ps1-perf-narrow-b4-v929-align16p16/20260515-093433-3852887/summary.json`. The disabled-control run with the code compiled but opt-in off completed at `scene/loop/target 3129/2853/2815`, confirming the stall was the runtime narrow-upload path, not the B4-low harness: `scratch/ps1-perf-narrow-b4-v929-disabled/20260515-093701-3866969/summary.json`. | Failed/no promotion. Runtime partial-width upload is not safe in this form: it added first-frame GPU/`LoadImage` fragility, stalled scene playback before complete metrics, and grew the PS-EXE bucket from `217088` to `219136` while compiled. | Close runtime x-band upload repacking on the current baseline. Future upload-byte work should be pack-time/direct-layout or a code-neutral metadata path that avoids strided scratch repacking, preserves the `217088` byte bucket, and proves first-frame B4-low completion before broader gates. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `fgp3-adjacent-span-canonicalization-v930` | Log-only; no pack/source change promoted. | Test a zero-runtime pack-side row-span canonicalizer after the runtime x-band miss. The hypothesis was that current under-99 FGP3/v4 packs still had exactly adjacent PAL4 draw spans on the same row, allowing metadata removal without filling gaps, changing pixels, or adding runtime code. | Built a throwaway analyzer/transform and dry-ran it on the remaining yellow packs only. The real adjacent-span merge count was zero for the timing-bearing candidates. Apparent byte savings came from re-encoding away already-known zero-tail/sector-collapse payloads: W1 low would drop `30319` bytes mostly through previously closed frames `55`, `60`, `59`, `63`, `39`, `56`, `65`, `66`, and `132`; B2 high would drop `17902` bytes mostly through already closed frames `89..95`; B4 low would drop `13030` bytes through known-bad frames `30`, `33`, and `289`; W1 high would drop `12070` bytes mostly through the already closed frame55/56/57 family. Dry-run summaries: `scratch/fgp3-merge-adjacent-dryrun-v930/WALK1LOW.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/WALKSTUF1.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/BUILDING2.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/BUIL2LOW.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/BUIL4LOW.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/JOHNNY1.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/JOHN1LOW.summary.json`, `scratch/fgp3-merge-adjacent-dryrun-v930/VISITOR3.summary.json`, and `scratch/fgp3-merge-adjacent-dryrun-v930/VIST3LOW.summary.json`. | Failed/no promotion. The transform did not find a new row-span metadata win. Applying the apparent savings would batch-retry sector-collapse trims already proven phase-negative, and JOHNNY1/VISITOR3/BUILDING2-low had no useful savings. | Close adjacent draw-span canonicalization for the current under-99 packs. Do not batch-apply zero-tail canonicalization as a shortcut; it reopens known-bad sector-boundary trims. Future pack-side green work should either isolate fixed-sector exact-flat entries or move to generated scheduler/deadline ownership, static/precomposed upload data, or a new representation with explicit phase control. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `fgp3-cleanup-adjacent-merge-v931` | Log-only; host-only no pack/source change. | Check the complementary zero-runtime cleanup-prefix canonicalization path after v930 found no draw-span merge. The hypothesis was that adjacent or overlapping cleanup restore spans on the same row could be merged without drawing new pixels, reducing restore-call metadata and possibly black/clean-background span work. | Ran a host-only dry-run over the remaining under-99 PAL4 compact residual packs with exact-adjacent/overlap merge semantics for cleanup spans only. Artifacts: `scratch/fgp3-cleanup-adjacent-merge-dryrun-v931/summary.json` plus one summary per scanned pack. Results were zero across the timing-bearing candidates: W1 low `44764` cleanup spans / `0` mergeable / `0` bytes saved, W1 high `45543/0/0`, B2 high `53563/0/0`, B2 low `52390/0/0` with two D4 frames skipped, B4 low `61263/0/0`, and JOHNNY1 high/low `484/0/0` each. | Failed/no promotion. Current cleanup spans are already non-adjacent at the exact pixel-interval level for the remaining yellow packs, so there is no safe metadata-only cleanup merge to gate on PS1. | Close exact cleanup-span adjacency canonicalization for this baseline. Future cleanup/render wins need generated coalesced metadata, cleanup deferral with visual proof, black-background row masks, or static/precomposed ownership; simple interval merging has no payload to remove. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `johnny1-local-lz-v932` | `ps1: compress johnny1 full-frame payloads` | Take a larger data-shape swing on the remaining JOHNNY1 high/low yellow rows after black-clear and cleanup-merge probes proved cadence-flat. A dry scan showed the two full-frame entries were highly local-copy compressible: entry `1` modeled `114294 -> 7201` bytes and entry `50` modeled `114165 -> 16743` bytes, enough to remove full sectors without moving the fixed `448370` byte pack footprint. | Added a scene-local sentinel payload decoder in `src/foreground_pilot.c` plus `scripts/compact-fgp3-local-lz.py`, then transformed entries `1` and `50` in both `generated/ps1/foreground/JOHNNY1.FG2` and `generated/ps1/foreground/JOHN1LOW.FG2`. The packs preserve file size, offsets, high/low LBA/sectors `13983/219` and `14202/219`, and the `217088` byte PS-EXE bucket while cutting active payload `316608 -> 112093`. Transform summaries: `scratch/johnny1-local-lz-v932/JOHNNY1.summary.json` and `scratch/johnny1-local-lz-v932/JOHN1LOW.summary.json`; high proof: `scratch/ps1-perf-iterate/johnny1-local-lz-v932-high-rerun/20260515-101641-4098077/summary.json`; low proof: `scratch/ps1-perf-iterate/johnny1-local-lz-v932-low-3600/20260515-102102-4124867/summary.json`. | Promoted. Both tides pass at scene `2024`, loop/target `1948/1945`, overrun `3`, blocking/refill `5/5`, loop reads/read VBlanks `7/37`, due `0`, and target speed `99.846%`, moving JOHNNY1 high/low into green from the prior `1973/1945`, overrun `28`, blocking/refill `25/25`, and read time `58` profile. | Keep v932 as the JOHNNY1 baseline. Public rollup improves to `+0.2492%` over target / `99.7548%` target speed; raw signed rollup is `-0.5179%` / `100.5371%`; bands move to `119` green / `7` yellow. Methodology total is about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-local-lz-v933` | Log-only; pack/source restored and PS1 rebuilt. | Try to generalize the v932 local-LZ data-shape swing to the worst remaining yellow row, W1-low. A dry scan found multiple large compressible payloads, including entry/source `55/65` `4716 -> 551`, `60/81` `4432 -> 943`, `59/78` `4200 -> 902`, `63/91` `4643 -> 1665`, `65/96` `4630 -> 2025`, and `39/49` `7835 -> 5034`, with several modeled sector collapses. | Temporarily rewrote W1-low entries `55`, `60`, `59`, `63`, `65`, `39`, `56`, `66`, `0`, `132`, `83`, `140`, `134`, `143`, `144`, `192`, and `33` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size and offsets but cutting active payload via local-LZ sentinels. The first gate exposed that W1-low expanded payloads exceed the encoded-size frame-buffer allocation, causing a correctness trip at frame `38`: `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-v933-focused/20260515-104410-62802/walkstuf1-low/headless-regtest.log`. A temporary static expanded-size floor proved the candidate could complete, then the strict artifact was recorded at `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-v933-focused/20260515-104819-86747/summary.json`. Transform summary: `scratch/walkstuf1-low-local-lz-v933/summary.json`. | Failed/no promotion. Completion with the temporary runtime floor was cadence-negative and code-size-negative: scene/loop `1769/1477 -> 1810/1518`, overrun `45 -> 96`, blocking `65 -> 89`, refill overrun `20 -> 25`, loop reads `58 -> 59`, and due `11 -> 14`. The helper also grew the PS-EXE sector bucket `217088 -> 219136` and shifted hot symbols, while the pack LBA moved `25633 -> 25634` in the failed layout. | Close broad W1-low local-LZ batching on the current baseline. The compression ratio is real, but the expanded-buffer floor and sector-boundary/cadence changes make it unsuitable as a general under-99 promotion. Future local-LZ retries need a code-neutral expanded-size metadata path or one/two isolated fixed-layout entries with proven flat symbols and flat pack LBA. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-local-lz-v934` | Log-only; pack restored and PS1 rebuilt. | Retest W1-low local-LZ as a pack-only batch that leaves entry `39` uncompressed. This keeps the existing `7835` byte frame-buffer maximum and avoids the v933 temporary runtime expanded-size floor, so the candidate isolates whether local-LZ compression can help W1-low without new source code. | Rewrote entries `55`, `60`, `59`, `63`, `65`, `56`, `66`, `0`, `132`, `83`, `140`, `134`, `143`, `144`, `192`, and `33` in `generated/ps1/foreground/WALK1LOW.FG2`, preserving file size and offsets while cutting active payload by `24046` bytes. Transform summary: `scratch/walkstuf1-low-local-lz-v934/summary.json`; failed candidate artifact: `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-v934-focused/20260515-105232-111341/summary.json`; restored current-control artifact for future W1-low gates: `scratch/ps1-perf-iterate/walkstuf1-low-current-control-v934/20260515-105412-121463/summary.json`. | Failed/no promotion. Against the fresh current control, v934 traded in the wrong direction: scene/loop/target `1773/1481/1428 -> 1811/1519/1420`, overrun `53 -> 99`, blocking `78 -> 115`, and due `11 -> 19`, while only loop reads `61 -> 57` and refill overrun `27 -> 17` improved. | Close multi-entry W1-low local-LZ without deadline ownership. The batch saves sectors, but the decode/read phase shift moves too much work into visible cadence. Future W1-low local-LZ work should be one isolated entry at a time, preferably after refreshing the control baseline and requiring visible timing to stay flat or improve. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg209-225-v935` | `ps1: add walkstuf1 low rg209-225` | Use the fresh post-v932 W1-low control from v934 to retest retained-read ownership. The read planner showed the old retained rows did not line up with the current low-tide read starts, while `209..225` had the best visible-cost signal with a long first gap and three modeled saved reads. | Added `{209, 225, 0}` to `kWalkstuf1LowReadGroups12[]`, rebuilt inside the fixed current `219136` byte PS-EXE bucket, and gated against `scratch/ps1-perf-iterate/walkstuf1-low-current-control-v934/20260515-105412-121463/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg209-225-v935-focused/20260515-105715-139226/summary.json`; W1-high canary: `scratch/ps1-perf-iterate/walkstuf1-low-rg209-225-v935-high-canary/20260515-105918-151269/summary.json`. | Promoted. W1-low keeps scene/loop flat at `1773/1481`, improves target `1428 -> 1431`, overrun `53 -> 50`, blocking `78 -> 72`, loop reads/read time `61/279 -> 58/267`, and due `11 -> 10`; pack LBA/sectors stay `25634/750` and tracked hot symbols stay fixed. | Keep v935 as the W1-low current baseline. The public rollup is `+0.2520%` over target / `99.7522%` target speed, raw signed is about `-0.5152%` / `100.5345%`, and bands remain `119` green / `7` yellow. Methodology total remains about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-rg225-241-v936-v938` | Log-only; source restored and PS1 rebuilt. | Continue the refreshed W1-low retained-read lane after v935. The new read plan ranked `225..241` as a lower-risk 16-sector candidate with two modeled saved reads, so this tested whether the next adjacent ownership row could stack on top of `209..225`. | Temporarily added `{225, 241, minSlack}` to `kWalkstuf1LowReadGroups12[]` and tested unguarded, slack4, and slack8 forms against the v935 summary. Artifacts: unguarded `scratch/ps1-perf-iterate/walkstuf1-low-rg225-241-v936-focused/20260515-110927-208930/summary.json`; slack4 `scratch/ps1-perf-iterate/walkstuf1-low-rg225-241-slack4-v937-focused/20260515-111056-218242/summary.json`; slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg225-241-slack8-v938-focused/20260515-111215-226305/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Unguarded and slack4 saved loop reads `58 -> 56` and blocking `72 -> 71`, but regressed hidden refill `27 -> 28`. Slack8 suppressed the refill debt but also suppressed the useful work, staying exact-flat with no key metric improvement. | Close W1-low `225..241` under scalar retained-row forms. The useful read save exists, but this row needs generated deadline ownership or a different phase shape to avoid trading one blocking VBlank for one hidden refill VBlank. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-setupseg282-306-v939` | Log-only; source restored and PS1 rebuilt. | Take a larger W1-low swing after scalar retained rows became cadence-bound: move the planner's 24-sector mid-scene cluster `282..306` into a tide-gated retained setup segment so multiple active-loop reads could be served from resident memory. | Temporarily added a low-tide-only `walkstuf1` setup segment at sectors `282..306` using the reusable setup-segment buffer, rebuilt, and gated against the v935 accepted summary. Failed artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg282-306-v939-focused/20260515-111624-250590/summary.json`. The runtime code grew `foregroundPilotPlay` by `196` bytes and shifted tracked hot symbols by `+196`, but the PS-EXE sector bucket stayed fixed at `219136`. | Failed/no promotion. The segment did remove active-loop reads (`58 -> 53`), but the extra setup-resident CD read landed in the wrong phase and regressed scene/loop/target `1773/1481/1431 -> 1791/1486/1429`, overrun `50 -> 57`, blocking `72 -> 95`, refill overrun `27 -> 30`, and due misses `10 -> 14`. | Close runtime W1-low setup-segment injection for the `282..306` cluster. The read save is real, but a raw resident segment increases visible blocking more than it saves; future W1-low work should require planner-visible deadline ownership or a code-neutral pack/data transform before moving more cluster data into setup. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg283-307-v940-v942` | Log-only; source restored and PS1 rebuilt. | Test the largest current W1-low read-plan cluster as a raw retained-read row after the setup segment proved phase-negative. The current yellow-row matrix ranked `289..313` and `283..307` as the top W1-low clusters, with five to six modeled saved reads, but marked them scheduler-owned-only because of tight internal gaps. | First added `{289,313,0}` as v940, which completed exact-flat but recorded `group_hits=0`, proving that row start did not own the preserved window start. Then tested the aligned `{283,307,0}` v941 and a strict `{283,307,8}` v942 guard against the v935 accepted summary. Artifacts: v940 diagnostic `scratch/ps1-perf-iterate/walkstuf1-low-rg289-313-v940-focused/20260515-112136-280767/summary.json`; v941 unguarded `scratch/ps1-perf-iterate/walkstuf1-low-rg283-307-v941-focused/20260515-112351-294083/summary.json`; v942 slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg283-307-slack8-v942-focused/20260515-112504-301338/summary.json`. | Failed/no promotion. v941 proved the read save is real (`loop_reads 58 -> 54`, `due 10 -> 9`) but regressed visible timing and refill: scene/loop/target `1773/1481/1431 -> 1779/1487/1433`, overrun `50 -> 54`, blocking `72 -> 75`, and refill overrun `27 -> 33`. v942 suppressed the bad phase but also suppressed the useful group, staying exact-flat with no key metric improvement. | Close raw W1-low `283..307`/`289..313` read-table ownership. The remaining W1-low clusters need explicit scheduler/deadline metadata or a code-neutral pack phase change; minSlack alone only chooses between regression and inert behavior. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `walkstuf1-low-rg283-307-slack6-v943` | Log-only; source restored and PS1 rebuilt. | Split the binary between the v941 unguarded regression and the v942 slack8 inert result for the same W1-low `283..307` cluster. | Temporarily changed the aligned row to `{283,307,6}`, rebuilt, and ran the strict focused gate against the v935 accepted summary. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg283-307-slack6-v943-focused/20260515-112825-320965/summary.json`. | Failed/no promotion. Slack6 was exact-flat to v935 (`1773/1481/1431`, overrun `50`, blocking/refill `72/27`, loop reads/read VBlanks `58/267`, due `10`) and failed because no key metric improved. | Close the minSlack search for this row. The useful `283..307` grouping fires only below the safe slack threshold; at `6+` VBlanks it is inert. Public rollup and methodology totals remain unchanged. |
| 2026-05-15 | `building2-low-rg104-128-v944-v947` | Log-only; source restored and PS1 rebuilt. | Move the read-cluster swing to B2-low after W1-low raw ownership closed. The current-yellow matrix ranked B2-low `104..128` as a five-read candidate and `112..128` as a smaller medium-risk candidate, both potentially large enough to move the row toward green. | First tested `{104,128,0}` as a table insertion against the stale v739 matrix row, then reran a fresh current-control on the current branch, and tested same-size replacements of the existing `{365,381,0}` row with `{104,128,0}` and `{112,128,0}`. Artifacts: insertion v944 `scratch/ps1-perf-iterate/building2-low-rg104-128-v944-focused/20260515-113035-333924/summary.json`; same-size v945 `scratch/ps1-perf-iterate/building2-low-rg104-128-replace365-v945-focused/20260515-113204-342853/summary.json`; current control v946 `scratch/ps1-perf-iterate/building2-low-current-control-v946/20260515-113322-350875/summary.json`; smaller replacement v947 `scratch/ps1-perf-iterate/building2-low-rg112-128-replace365-v947-focused/20260515-113447-359472/summary.json`. | Failed/no promotion. The fresh current control measured B2-low at `1606/1341/1316`, overrun `25`, blocking/refill `56/2`, loop reads/read VBlanks `37/252`, due `12`. The `{104,128}` replacement saved reads (`37 -> 32`) and due (`12 -> 11`) but still landed at `1341/1314` with worse blocking/refill `57/4`; `{112,128}` regressed to `1609/1344/1312`, overrun `32`, blocking/refill `60/4`, and reads `34`. | Close raw B2-low early-cluster read-table replacement. As with W1-low, the read saves are real but phase-negative; future B2-low work needs scheduler/deadline ownership or data-shape/render work rather than another unowned grouped row. |
| 2026-05-15 | `walkstuf1-high-prepare-before-window-v948-v949` | Log-only; source restored and PS1 rebuilt. | Test whether the W1-low prepare-before-window scheduler exception can generalize to W1-high, which also shows CD-reserved and prepare-blocked pressure. | Temporarily removed the low-tide guard from the W1 prepare-before-window branch, built, and ran W1-high. Because the v927 matrix row predates the current `219136` byte executable bucket, also ran a restored current-control before judging the candidate. Artifacts: prepare-first v948 `scratch/ps1-perf-iterate/walkstuf1-high-prepare-before-window-v948-focused/20260515-113716-374169/summary.json`; restored current-control v949 `scratch/ps1-perf-iterate/walkstuf1-high-current-control-v949/20260515-113835-382305/summary.json`. | Failed/no promotion. Current-control W1-high is `1770/1481/1428`, overrun `53`, blocking/refill `88/24`, reads/read VBlanks `67/287`, due `15`. The prepare-first high-tide variant saved two reads (`67 -> 65`) but regressed scene/loop `1770/1481 -> 1772/1483`, blocking `88 -> 101`, refill `24 -> 25`, and due `15 -> 18`. | Keep the prepare-before-window exception low-tide-only. W1-high needs a different scheduler/data-shape approach; copying the low-tide rule moves too much CD pressure into visible blocking. |
| 2026-05-15 | `building2-high-rg17-33-replace249-v951` | Log-only; source restored and PS1 rebuilt. | Retry the earlier B2-high `17..33` replacement against a fresh current-control artifact, because the prior v921 run failed before metrics and the row is still one of the largest non-W1 yellow read clusters. | Temporarily replaced the same-size tail row `{249,257,0}` with `{17,33,0}`, rebuilt, and gated with a longer timeout against the current B2-high control from `scratch/ps1-perf-iterate/current-yellow-controls-v950/20260515-114033-393998/summary.json`. Candidate artifact: `scratch/ps1-perf-iterate/building2-high-rg17-33-replace249-v951-focused/20260515-114633-428029/summary.json`. | Failed/no promotion. Current-control B2-high is `1607/1356/1312`, overrun `44`, blocking/refill `58/21`, loop reads `58`, due `7`. The replacement saved reads (`58 -> 56`) but regressed scene/loop `1607/1356 -> 1611/1360`, overrun `44 -> 48`, blocking `58 -> 61`, and refill `21 -> 25`. | Close B2-high `17..33` replacement on the current baseline. It is no longer a structural unknown; it has the same phase-negative signature as the B2-low and W1 clusters. |
| 2026-05-15 | `building4-low-rg178-202-rg262-286-v952` | Log-only; source restored and PS1 rebuilt. | Try a larger B4-low retained-read swing after the scalar B2/W1 clusters showed real read saves but bad phase. The candidate paired the two biggest current B4-low clusters, `178..202` and `262..286`, to see whether read ownership could move the row with only one due miss remaining. | Temporarily added low-tide `building4` read groups `{178,202,0}` and `{262,286,0}`, rebuilt, and gated against the B4-low current control from `scratch/ps1-perf-iterate/current-yellow-controls-v950/20260515-114033-393998/summary.json`. Candidate artifact: `scratch/ps1-perf-iterate/building4-low-rg178-202-rg262-286-v952-focused/20260515-114906-443159/summary.json`. | Failed/no promotion. The run was exact-flat to current control at scene/loop/target `3129/2853/2815`, overrun `38`, blocking/refill `42/36`, loop reads `30`, and due `1`, so the strict improvement gate correctly rejected it. | Close B4-low scalar retained-read pairing for these clusters. The row is not read-count bound in this shape; the next B4-low swing should target pack/data shape, static upload ownership, or explicit scheduler metadata rather than another raw read-group table entry. |
| 2026-05-15 | `building4-low-local-lz-entry30-v953` | `ps1: compress building4 low frame30 payload` | Take the next B4-low data-shape swing after raw retained-read ownership stayed inert. The local-LZ scan showed entry `30` / source frame `54` could shrink `4650 -> 679` bytes and collapse modeled entry sectors `3 -> 1` without moving the fixed pack footprint. | Rewrote only entry `30` in `generated/ps1/foreground/BUIL4LOW.FG2` with `compact-fgp3-local-lz.py`, preserving the `1714154` byte file size, pack LBA/sectors `9119/837`, and the `219136` byte PS-EXE bucket. Transform summary: `scratch/building4-low-local-lz-entry30-v953/summary.json`; strict speed artifact: `scratch/ps1-perf-iterate/building4-low-local-lz-entry30-v953-focused/20260515-115446-475797/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-local-lz-entry30-v953-workreduction/20260515-115639-486778/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat to the current B4-low control at scene/loop/target `3129/2853/2815`, overrun `38`, blocking/refill `42/36`, loop reads/read VBlanks `30/223`, and due `1`; active payload drops `807263 -> 803292`. | Keep v953 as the new BUILDING4-low payload baseline. The refreshed public rollup is `+0.2523%` over target / `99.7519%` target speed, raw signed is about `-0.5148%` / `100.5342%`, and bands remain `119` green / `7` yellow. Methodology speed total remains about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-local-lz-entry33-v954` | `ps1: compress building4 low frame33 payload` | Stack the next B4-low local-LZ payload shrink on top of v953. The scan showed entry `33` / source frame `59` could shrink `6656 -> 2761` bytes and collapse modeled entry sectors `4 -> 2` without moving the fixed pack footprint. | Rewrote only entry `33` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving file size, pack LBA/sectors `9119/837`, and the `219136` byte PS-EXE bucket. Transform summary: `scratch/building4-low-local-lz-entry33-v954/summary.json`; strict speed artifact: `scratch/ps1-perf-iterate/building4-low-local-lz-entry33-v954-focused/20260515-120904-556416/summary.json`; accepted no-regression work gate: `scratch/ps1-perf-iterate/building4-low-local-lz-entry33-v954-workreduction/20260515-121036-565640/summary.json`. | Promoted as same-speed payload work reduction, not a new VBlank win. Timing/CD stay exact-flat to v953 at scene/loop/target `3129/2853/2815`, overrun `38`, blocking/refill `42/36`, loop reads/read VBlanks `30/223`, and due `1`; active payload drops `803292 -> 799397`. | Keep v954 as the new BUILDING4-low payload baseline. Public rollup remains `+0.2523%` over target / `99.7519%` target speed, raw signed remains about `-0.5148%` / `100.5342%`, and bands remain `119` green / `7` yellow. Methodology speed total remains about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building4-low-local-lz-entry289-v955` | Log-only; pack restored and PS1 rebuilt. | Close the risky B4-low late-sector local-LZ candidate on top of v954. The prior preserve-offset frame289 test improved refill but regressed visible cadence; local-LZ tested whether the same large payload collapse could be safer with a different encoded shape. | Temporarily rewrote entry `289` / source frame `441` in `generated/ps1/foreground/BUIL4LOW.FG2`, shrinking `8565 -> 5187` bytes with modeled sectors `5 -> 3` while preserving the fixed pack footprint. Transform summary: `scratch/building4-low-local-lz-entry289-v955/summary.json`; failed gate artifact: `scratch/ps1-perf-iterate/building4-low-local-lz-entry289-v955-focused/20260515-121428-587843/summary.json`. The pack was restored from `scratch/building4-low-local-lz-entry289-v955/BUIL4LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. It repeated the known phase-negative signature: refill improved `36 -> 33`, but scene/loop regressed `3129/2853 -> 3134/2858`, overrun `38 -> 43`, blocking `42 -> 46`, and due misses `1 -> 2`; loop reads stayed `30`. | Close B4-low entry289/source441 under both preserve-offset and local-LZ forms. The late payload collapse saves hidden refill but costs visible blocking; future B4-low speed work should target static upload/restore ownership or generated deadline scheduling, not more late sector-collapse payloads. |
| 2026-05-15 | `building2-high-local-lz-entry89-v956` | Log-only; pack restored and PS1 rebuilt. | Test whether local-LZ changes the known-bad B2-high frame89 sector-collapse signature. The preserve-offset trim saved a read but worsened cadence; local-LZ used a different encoded shape for the same large entry to see whether it could keep the CD win without target-budget debt. | Temporarily rewrote entry `89` / source frame `107` in `generated/ps1/foreground/BUILDING2.FG2`, shrinking `8814 -> 4843` bytes with modeled sectors `5 -> 3` while preserving the fixed pack footprint. Transform summary: `scratch/building2-high-local-lz-entry89-v956/summary.json`; failed gate artifact: `scratch/ps1-perf-iterate/building2-high-local-lz-entry89-v956-focused/20260515-121716-604453/summary.json`. The pack was restored from `scratch/building2-high-local-lz-entry89-v956/BUILDING2.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The candidate improved several CD metrics versus the fresh current control (`scene 1607 -> 1606`, `loop 1356 -> 1355`, blocking `58 -> 54`, refill `21 -> 20`, reads `58 -> 57`) but lowered the target budget `1312 -> 1308`, worsening overrun `44 -> 47`; due stayed `7`. | Close B2-high entry89/source107 under local-LZ as well as preserve-offset trim. Local-LZ can reduce CD pressure here, but the target-budget/advance cadence moves the row farther from green; future B2-high work needs generated deadline ownership or a representation that preserves the target cadence while taking the read win. |
| 2026-05-15 | `walkstuf1-high-local-lz-entry55-v957` | Log-only; pack restored and PS1 rebuilt. | Test the largest W1-high local-LZ payload after B2/B4 local-LZ showed that some same-speed payload reductions are safe but sector-collapse rows can steal cadence. Entry `55` / source frame `65` is highly compressible (`4716 -> 551`, modeled sectors `3 -> 1`) and W1-high remains one of the worst yellow rows. | Temporarily rewrote entry `55` in `generated/ps1/foreground/WALKSTUF1.FG2`, preserving the fixed pack footprint. Transform summary: `scratch/walkstuf1-high-local-lz-entry55-v957/summary.json`; failed artifact with a mismatched baseline label but complete metrics: `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-entry55-v957-focused/20260515-122017-622095/summary.json`. The pack was restored from `scratch/walkstuf1-high-local-lz-entry55-v957/WALKSTUF1.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Against the v949 current control it regressed scene/loop/target `1770/1481/1428 -> 1792/1503/1422`, overrun `53 -> 81`, blocking `88 -> 110`, refill `24 -> 30`, and due `15 -> 16`, despite reducing reads `67 -> 65`. | Close W1-high entry55/source65 local-LZ. This is the same broad W1 lesson as low-tide local-LZ: the byte win is real, but the decode/read phase shift is too expensive without generated deadline ownership or a code-neutral expanded-size/data-shape plan. |
| 2026-05-15 | `building2-high-local-lz-entry90-v958` | Log-only; pack restored and PS1 rebuilt. | Test the next B2-high local-LZ sector-collapse candidate after entry89 showed a CD-pressure win but target-budget regression. Entry `90` / source frame `109` was the next large compressible payload, shrinking `8849 -> 5275` bytes with modeled sectors `5 -> 3`. | Temporarily rewrote entry `90` in `generated/ps1/foreground/BUILDING2.FG2`, preserving the fixed `1303332` byte pack footprint, pack LBA/sectors `6182/637`, and the `219136` byte PS-EXE bucket. Transform summary: `scratch/building2-high-local-lz-entry90-v958/summary.json`; failed artifact: `scratch/ps1-perf-iterate/building2-high-local-lz-entry90-v958-focused/20260515-122347-642395/summary.json`. The pack was restored from `scratch/building2-high-local-lz-entry90-v958/BUILDING2.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Against the fresh B2-high current control it regressed scene/loop/target `1607/1356/1312 -> 1609/1358/1310`, overrun `44 -> 48`, blocking `58 -> 71`, refill `21 -> 24`, and due `7 -> 9`; loop reads improved only `58 -> 57`. | Close B2-high entry90/source109 under both preserve-offset trim and local-LZ forms. This is another phase-negative post-hot sector collapse: read count drops, but visible blocking and target cadence get worse. Future B2-high green work needs generated deadline ownership or a representation that takes the read win without lowering the target budget. |
| 2026-05-15 | `walkstuf1-high-rg352-376-v959-v960` | Log-only; source restored and PS1 rebuilt. | Test a bigger W1-high retained-read swing after local-LZ proved too phase-negative. The current read planner ranked `352..376` as a medium-risk 24-sector row with four modeled saved reads, enough theoretical size to matter for one of the largest remaining yellow gaps. | Temporarily added `{352,376,0}` to `kWalkstuf1HighReadGroups12[]`, then retested the same row as `{352,376,6}` after the raw form saved reads but regressed loop cadence. Artifacts: unguarded `scratch/ps1-perf-iterate/walkstuf1-high-rg352-376-v959-focused/20260515-122803-667169/summary.json`; slack6 `scratch/ps1-perf-iterate/walkstuf1-high-rg352-376-slack6-v960-focused/20260515-122921-674901/summary.json`. The source table was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Unguarded saved four loop reads (`67 -> 63`) and nudged blocking `88 -> 87`, but regressed scene/loop `1770/1481 -> 1776/1487`, only raised target `1428 -> 1431`, and worsened overrun `53 -> 56`. Slack6 proved the row only fires in the unsafe slack-5 phase: it stayed exact-flat with no key metric improvement. | Close W1-high `352..376` under scalar retained-row forms. This is a useful read cluster, but not one the current scheduler can consume safely; future W1-high work needs explicit generated deadline/slack ownership or a different pack/data phase shape before retrying this cluster. |
| 2026-05-15 | `walkstuf1-high-rg178-194-rg280-296-v961-v963` | Log-only; source restored and PS1 rebuilt. | Split-test two lower-visible-risk W1-high retained-read rows after the larger `352..376` row proved unsafe. The planner ranked `178..194` and `280..296` as scheduler-owned candidates with two modeled saved reads each, so the first test applied them together as a bigger four-read swing and then isolated each half. | Temporarily added `{178,194,0}` and `{280,296,0}` to `kWalkstuf1HighReadGroups12[]`, then tested `{178,194,0}` alone and `{280,296,0}` alone. Artifacts: combined `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-rg280-296-v961-focused/20260515-123129-687759/summary.json`; `178..194` alone `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-v962-focused/20260515-123240-695066/summary.json`; `280..296` alone `scratch/ps1-perf-iterate/walkstuf1-high-rg280-296-v963-focused/20260515-123352-702521/summary.json`. The source table was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The combined and `178..194`-only runs both saved two reads (`67 -> 65`) but regressed scene/loop/target `1770/1481/1428 -> 1772/1483/1425`, overrun `53 -> 58`, blocking `88 -> 94`, and due `15 -> 17`. The `280..296` row alone saved more reads (`67 -> 63`) but still regressed scene/loop `1770/1481 -> 1772/1483`, overrun `53 -> 55`, and refill `24 -> 27`. | Close scalar W1-high `178..194` and `280..296`. Their read saves are real but phase-negative; do not retry more W1-high hand-authored read rows until generated deadline/slack ownership can decide when those rows may fire. |
| 2026-05-15 | `walkstuf1-high-prepare-slack5-v964` | Log-only; source restored and PS1 rebuilt. | Test a W1-high scheduler swing based on the retained-row misses: the unsafe rows tended to fire at the slack-5 CD-window slot, so this variant moved W1-high staged-frame prepare from slack 4 to slack 5 and allowed the existing W1 prepare-before-window priority to apply on high tide. | Temporarily changed `fgRuntimeCanPrepareStagedFrame()` to use slack 5 for W1-high and removed the low-tide guard from the prepare-before-window branch. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-prepare-slack5-v964-focused/20260515-123651-720118/summary.json`. The source change was reverted and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The scheduler did remove active reads (`67 -> 61`) but moved much more work into visible delay: scene/loop/target `1770/1481/1428 -> 1780/1491/1426`, overrun `53 -> 65`, blocking `88 -> 109`, refill `24 -> 26`, and due `15 -> 19`. It also grew `foregroundPilotPlay` by `160` bytes and shifted downstream hot symbols by `+160`. | Close W1-high prepare-slot promotion as a runtime-code swing. Earlier prepare is worse than the unsafe CD refill it replaces; W1-high needs a code-neutral generated schedule or data/layout change, not a broader hot-path scheduler rule. |
| 2026-05-15 | `building2-high-entry89-catchup6-v965` / `building2-high-entry89-catchup4-v966` | Log-only; pack/source restored and PS1 rebuilt. | Reopen B2-high local-LZ entry89 only with explicit catch-up ownership, because the v956 local-LZ miss improved CD pressure but lowered target budget. The binary question was whether relaxing or tightening the scene-pack catch-up threshold for the BUILDING2 high retained-read table could keep the read win without the target/overrun regression. | Temporarily rewrote entry `89` / source frame `107` in `generated/ps1/foreground/BUILDING2.FG2` with local-LZ (`8814 -> 4843` bytes, modeled sectors `5 -> 3`) and tested a BUILDING2-high-only catch-up threshold of `6`, then the opposite threshold of `4`. Artifacts: relaxed v965 `scratch/ps1-perf-iterate/building2-high-entry89-catchup6-v965-focused/20260515-124259-755435/summary.json`; strict v966 `scratch/ps1-perf-iterate/building2-high-entry89-catchup4-v966-focused/20260515-124420-763512/summary.json`; transform summary `scratch/building2-high-entry89-catchup6-v965/local-lz-entry89-summary.json`. The pack was restored from `scratch/building2-high-entry89-catchup6-v965/BUILDING2.before.FG2`, the source change was reverted, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Against the fresh B2-high current control `1607/1356/1312`, overrun `44`, blocking/refill `58/21`, the relaxed threshold regressed to `1637/1369/1321`, overrun `48`, blocking/refill `63/22`, reads `60`, due `8`. The strict threshold regressed to `1630/1362/1304`, overrun `58`, blocking/refill `70/28`, reads `64`, due `8`. Both kept pack LBA `6182` and the `219136` byte PS-EXE bucket fixed, but neither produced a key improvement. | Close B2-high entry89 plus scene-local catch-up thresholding. The local-LZ read win is not recoverable by a scalar catch-up knob: relaxed catch-up lets visible debt accumulate, while stricter catch-up steals target budget and refill. Future B2-high work needs generated deadline/slack metadata or a representation that removes payload/read cost without changing scene-pack advance cadence. |
| 2026-05-15 | `walkstuf1-high-deadline-140-156-plus1-v967` / `walkstuf1-high-deadline-140-156-minus1-v968` | Log-only; pack restored and PS1 rebuilt. | Test a code-neutral generated-deadline swing instead of another hand-authored read row. Entries `140..156` cover the dense W1-high `274..313` sector cluster where prior retained-read candidates saved reads but fired in a bad phase, so the probe redistributed one cumulative host tick across that interval while preserving the final scene deadline. | Patched only the host-deadline field in `generated/ps1/foreground/WALKSTUF1.FG2`, first adding `+1` tick to cumulative deadlines for entries `140..156`, then restoring and testing `-1` tick across the same interval. The final deadline stayed `1211` ticks and pack LBA/PS-EXE bucket stayed fixed. Artifacts: plus v967 `scratch/ps1-perf-iterate/walkstuf1-high-deadline-140-156-plus1-v967-focused/20260515-124846-788936/summary.json`; minus v968 `scratch/ps1-perf-iterate/walkstuf1-high-deadline-140-156-minus1-v968-focused/20260515-125013-797495/summary.json`; transform summaries under `scratch/walkstuf1-high-deadline-140-156-plus1-v967/summary.json` and `scratch/walkstuf1-high-deadline-140-156-minus1-v968/summary.json`. The pack was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Against current W1-high `1770/1481/1428`, overrun `53`, blocking/refill `88/24`, the `+1` redistribution regressed to `1811/1504/1422`, overrun `82`, blocking/refill `125/27`, reads `63`, due `21`. The `-1` redistribution improved refill `24 -> 20` but still regressed to `1803/1496/1424`, overrun `72`, blocking `114`, reads `66`, due `21`. | Close scalar W1-high deadline redistribution for this interval. Moving a single host tick around the dense cluster changes read cadence too violently and turns hidden/refill movement into visible blocking. Future W1-high generated scheduling must be per-read/per-frame aware rather than a simple cumulative-deadline interval shift. |
| 2026-05-15 | `walkstuf1-high-local-lz-entry56-v969` / `walkstuf1-high-local-lz-entry57-v970` | Log-only; pack restored and PS1 rebuilt. | Test the adjacent W1-high early local-LZ sector-collapse candidates after entry55 was closed. Entries `56` and `57` each shrink from two sectors to one, making them the next biggest data-shape swings in the same early cluster. | Temporarily rewrote entry `56` / source frame `67` (`3425 -> 700`, sectors `2 -> 1`), restored it, then rewrote entry `57` / source frame `70` (`3409 -> 696`, sectors `2 -> 1`) in `generated/ps1/foreground/WALKSTUF1.FG2`. Artifacts: entry56 transform `scratch/walkstuf1-high-local-lz-entry56-v969/summary.json` and gate `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-entry56-v969-focused/20260515-125303-814013/summary.json`; entry57 transform `scratch/walkstuf1-high-local-lz-entry57-v970/summary.json` and gate `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-entry57-v970-focused/20260515-125417-821351/summary.json`. The pack was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Entry56 regressed current W1-high from `1770/1481/1428`, overrun `53`, blocking/refill `88/24`, to `1795/1488/1427`, overrun `61`, blocking/refill `107/25`, reads `64`, due `20`. Entry57 regressed to `1801/1494/1424`, overrun `70`, blocking/refill `112/27`, reads `67`, due `20`. | Close W1-high early local-LZ sector-collapse entries `55..57`. The byte reductions are large, but the decode/sector phase is consistently visible-negative; future W1-high data-shape work needs fixed-sector/no-collapse work reduction or generated schedule ownership, not early cluster sector collapse. |
| 2026-05-15 | `building4-low-local-lz-entry270-v971` | `ps1: compress building4 low frame270 payload` | Continue the B4-low local-LZ data-shape lane after entries `30` and `33` stayed cadence-neutral. Entry `270` / source frame `410` was the next modeled sector-collapse candidate, small in bytes but enough to test whether a mid-late payload cut could move the row without the entry289 visible-blocking failure. | Rewrote only entry `270` in `generated/ps1/foreground/BUIL4LOW.FG2`, preserving the fixed `1714154` byte pack footprint, pack LBA/sectors `9119/837`, and the `219136` byte PS-EXE bucket. Transform summary: `scratch/building4-low-local-lz-entry270-v971/summary.json`; strict zero-regression artifact: `scratch/ps1-perf-iterate/building4-low-local-lz-entry270-v971-focused/20260515-125623-833814/summary.json`; accepted material gate: `scratch/ps1-perf-iterate/building4-low-local-lz-entry270-v971-material/20260515-125826-845625/summary.json`. | Promoted as a small speed win with an explicit scene-length tradeoff. Against v954, loop/target improves `2853/2815 -> 2851/2815`, overrun `38 -> 36`, and refill `36 -> 35`; blocking, read time, and due stay flat at `42/223/1`. Total scene VBlanks rise `3129 -> 3143`, but that is within the `0.5%` material-regression gate and does not change the active-loop target-speed improvement. Active payload drops `799397 -> 799277`. | Keep v971 as the new BUILDING4-low baseline. Public rollup improves to `+0.2517%` over target / `99.7525%` target speed, raw signed rollup moves to about `-0.5154%` / `100.5348%`, and bands remain `119` green / `7` yellow. Methodology speed total remains about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `building2-high-setupseg86-242-v979` | `ps1: prime building2 high midpack sectors` | Take a larger scheduler/ownership swing for the remaining BUILDING2 high yellow gap after scalar read groups, local-LZ sector collapses, catch-up thresholds, and payload-only trims either failed or stayed same-speed. The idea was to pay a tight mid-pack CD cluster during setup rather than trying to merge it through active-loop retained-read rows. | Added a high-tide-only BUILDING2 setup segment for relative sectors `86..242` in `src/foreground_pilot.c`, reusing the existing setup-segment copy path. Boundary probes showed `90..242` was safe but smaller, `88..242` was good, `86..242` was best, `84..242` regressed, and `82..242`, `72..242`, `54..242`, and `3..242` crossed either clean-pressure or allocation failure cliffs. Artifacts: safe `90..242` `scratch/ps1-perf-iterate/building2-high-setupseg90-242-v973-focused/20260515-131308-929115/summary.json`, clean-pressure failures `82..242`/`72..242`/`54..242`, allocation failure `3..242`, accepted focused `scratch/ps1-perf-iterate/building2-high-setupseg86-242-v979-focused/20260515-132142-981166/summary.json`, and accepted material gate `scratch/ps1-perf-iterate/building2-high-setupseg86-242-v979-material/20260515-132437-998877/summary.json`. | Promoted as an active-loop CD win with an explicit setup-cost trade. Against the fresh current control, active loop/target improves `1356/1312 -> 1349/1315`, overrun `44 -> 34`, blocking/refill `58/21 -> 48/12`, reads/read time `58/259 -> 25/117`, and due stays `7`. Setup cost rises scene `1607 -> 1659` and setup bytes `282260 -> 601748`, accepted under the `4%` material setup-regression gate. Pack footprint, pack LBA/sectors `6182/637`, and PS-EXE bucket `219136` stay fixed. | Keep v979 as the new BUILDING2-high baseline. Public rollup improves to `+0.2481%` over target / `99.7560%` target speed, raw signed rollup moves to about `-0.5190%` / `100.5383%`, and bands remain `119` green / `7` yellow. Methodology speed total is about `17.15` public over-target points removed and `+12.66` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-15 | `walkstuf1-low-setupseg238-388-v989` | `ps1: prime walkstuf1 low late sectors` | Take the same setup-residency swing that worked for BUILDING2 high and aim it at the remaining WALKSTUF1 low CD-bound gap. Prior W1-low scalar retained rows, local-LZ sector collapses, and small setup segments either stayed cadence-bound or moved the work into visible delay; the larger hypothesis was that the dense late low-tide sector range needed to be fully paid during setup. | Added a low-tide-only WALKSTUF1 setup segment for relative sectors `238..388` in `src/foreground_pilot.c`, reusing the existing setup-segment buffer and leaving the high-tide branch untouched. Boundary probes showed `238..313` and `238..336` were safe but smaller, two-segment variants were safe but slower, `238..388` was best, `238..396` and `232..388` were safe but slower, and `238..404`/`238..422` crossed the clean-pressure cliff and disabled prefetch. Accepted material artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-388-v989-material/20260515-134954-1150788/summary.json`; best focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-388-v984-focused/20260515-134014-1092726/summary.json`. | Promoted as a large active-loop CD win with an explicit setup-cost trade. Against v935, active loop/target improves `1481/1431 -> 1469/1447`, overrun `50 -> 22`, blocking/refill `72/27 -> 34/12`, reads/read time `58/267 -> 28/145`, and due `10 -> 4`. Setup cost rises scene `1773 -> 1820` and setup bytes `421428 -> 728628`, accepted under the `4%` material setup-regression gate. Pack LBA/sectors `25634/750` and the `219136` byte PS-EXE bucket stay fixed. | Keep v989 as the new WALKSTUF1-low baseline. Public rollup improves to `+0.2325%` over target / `99.7709%` target speed, raw signed rollup moves to about `-0.5347%` / `100.5532%`, and bands remain `119` green / `7` yellow. Methodology total is about `17.17` public over-target points removed and `+12.67` target-speed points gained since the compact full-matrix baseline. Do not extend this segment past `388` without heap/prefetch pressure relief. |
| 2026-05-15 | `walkstuf1-high-setupseg242-388-v998` | `ps1: prime walkstuf1 high late sectors` | Apply the same setup-residency big swing to WALKSTUF1 high after scalar retained rows, local-LZ, and deadline probes either regressed or stayed cadence-bound. The aim was to pay the dense late high-tide CD range during setup instead of fighting the active-loop scheduler. | Added a high-tide-only WALKSTUF1 setup segment for relative sectors `242..388`, sharing the existing setup-segment buffer with the low-tide `238..388` path. Boundary probes showed `287..388` was safe but smaller, `252..388` was safe, `244..388` improved further, `242..388` was best, `240..388` and `241..388` gave back loop speed, and `228..388` plus split `252..388 + 228..252` crossed the structural cliff with missing `JCPERF2`. Accepted material artifact: `scratch/ps1-perf-iterate/walkstuf1-high-setupseg242-388-v998-material/20260515-141421-1292648/summary.json`. | Promoted as a large active-loop CD win with an explicit setup-cost trade. Against the v949 current control, active loop/target improves `1481/1428 -> 1476/1441`, overrun `53 -> 35`, blocking/refill `88/24 -> 49/17`, reads/read time `67/287 -> 37/182`, and due `15 -> 7`. Setup cost rises scene `1770 -> 1822` and setup bytes `413236 -> 712244`, accepted under the `4%` material setup-regression gate. Pack LBA/sectors `24884/750` and the `219136` byte PS-EXE bucket stay fixed. | Keep v998 as the new WALKSTUF1-high baseline. Public rollup improves to `+0.2285%` over target / `99.7746%` target speed, raw signed rollup moves to about `-0.5387%` / `100.5570%`, and bands remain `119` green / `7` yellow. Stop here for the point release; next work should move to a separate allocator branch or resume with BUILDING2 high after release. |
| 2026-05-17 | `allocator-targeted-setup-segments` | branch `perf/allocator-era-under-green-20260517` after `339df94f5` | After the memory-region allocator promotion, the old full-scene setup-segment buffers no longer fit reliably: TRANSIENT exhausted, full CACHE buffers ran into clean-rect pressure, and suppressing the stream window saved memory but regressed timing. The binary question was whether smaller dual setup slices could keep the useful W1/B2 residency while staying inside MEM_REGION_CACHE. | Added MEM_REGION_CACHE-backed targeted setup slices in `src/foreground_pilot.c`: B2 high caches `3..35` and `202..242`; W1 low caches `197..243` and `410..434`; W1 high caches `198..244` and `411..435`. Failed variants were logged under `scratch/ps1-perf-iterate/transient-setup-segment-fix/20260517-203044-710399`, `cache-setup-segment-fix/20260517-203842-754946`, `cache-setup-no-window/20260517-204654-800739`, and wider B2 `176..242` / `176..216` probes. Accepted command: `./scripts/ps1-perf-iterate.sh --skip-build --case-local-cd --frames 7200 --timeout 240 --output scratch/ps1-perf-iterate/targeted-setup-segments-guarded-final --case "walkstuf1-high::fgpilot walkstuf1 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --case "walkstuf1-low::fgpilot walkstuf1 lowtide 1 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1" --case "building2-high::fgpilot building2 lowtide 0 night 1 holiday 0 raft-stage 4 island-pos -154 54 perf-log noloop seed 1"`. Artifact: `scratch/ps1-perf-iterate/targeted-setup-segments-guarded-final/20260517-212742-1029262/summary.json`. | Promoted. W1 high improves `1509/1425 -> 1489/1430`, overrun `84 -> 59`, blocking/refill `137/34 -> 92/15`, reads/read time `81/369 -> 56/256`, and due `23 -> 15`. W1 low improves `1507/1426 -> 1477/1434`, overrun `81 -> 43`, blocking/refill `142/26 -> 58/16`, reads/read time `75/353 -> 51/236`, and due `25 -> 9`. B2 high changes `1347/1311 -> 1350/1310`, but read pressure improves `57/246 -> 47/208` and due `8 -> 7` while the allocator-focused run remains measured instead of failing clean-rect allocation. | Keep as the allocator-era W1/B2 setup baseline. Public rollup improves to `+0.5371%` over target / `99.5143%` target speed; raw signed rollup is about `-0.1798%` / `100.2447%`; bands are `118` green, `6` yellow, `0` orange, and `2` red. Methodology total is about `16.86` public over-target points removed and `+12.41` public target-speed points gained since the compact full-matrix baseline. Next W1 work should try generated ownership around high `296..312` and low `285..297` / `306..322`; next B2 work should only retry wider setup coverage after lowering clean-rect/CACHE pressure. |
| 2026-05-17 | `visitor3-stage1-cleanrelief` | `ps1: keep visitor3 stage1 prefetch under clean relief` | After the allocator merge, VISITOR3 high/low regressed into red because force-relief dropped every optional prefetch buffer to survive split clean-rect allocation. The binary question was whether VISITOR3 could keep the small stage1 frame buffer while still suppressing the large setup-prime/window allocations that caused clean-rect pressure. | Full prefetch/setup-prime restore was rejected first: `scratch/ps1-perf-iterate/visitor3-allocator-prefetch-restore/20260517-214134-1106888/summary.json` failed with missing `JCPERF2` because both tides hit `JCBSOD-FATAL CACHE exhausted` while allocating a `97280` byte clean snapshot. The promoted source keeps `cleanMemoryRelief=1` for VISITOR3, preserves `gFgPrefetchFrameBuffer`, frees the stream window, and exposes the prefetch frame buffer to runtime even under relief. Slack `4` (`scratch/ps1-perf-iterate/visitor3-stage1-slack4/20260517-215204-1165926/summary.json`) and slack `5` (`scratch/ps1-perf-iterate/visitor3-stage1-slack5/20260517-215520-1184627/summary.json`) were rejected because low improved slightly but high regressed versus the default slack `6`. Accepted canary: `scratch/ps1-perf-iterate/visitor3-stage1-under-green-canaries/20260517-215910-1206316/summary.json`, covering VISITOR3 high/low plus BUILDING2 high/low, BUILDING4 high/low, and WALKSTUF1 high/low. | Promoted. VISITOR3 high improves `1232/1033 -> 1135/1035`, overrun `199 -> 100`, blocking `478 -> 387`, loop reads/read time `137/478 -> 135/462`, due `137 -> 109`, and target speed `83.847% -> 91.189%`. VISITOR3 low improves `1231/1040 -> 1110/1040`, overrun `191 -> 70`, blocking `438 -> 343`, loop reads/read time `126/438 -> 124/422`, due `126 -> 98`, and target speed `84.484% -> 93.694%`. B2/W1 allocator rows stayed on the accepted targeted setup profiles and B4 remained measured. | Keep this as the allocator-era VISITOR3 baseline. Public rollup improves to `+0.3685%` over target / `99.6457%` target speed; raw signed rollup is about `-0.3484%` / `100.3760%`; bands are `118` green, `6` yellow, `2` orange, and `0` red. Methodology total is about `17.03` public over-target points removed and `+12.55` public target-speed points gained since the compact full-matrix baseline. Next VISITOR3 work needs generated scheduler/data-shape ownership to cross 99%; do not retry full setup-prime/window restore until clean-rect or CACHE pressure is lowered. |
| 2026-05-17 | `visitor3-high-window64-cleanrelief` | `ps1: keep visitor3 high stream window under clean relief` | Continue the allocator-era VISITOR3 path after stage1-only relief left both tides orange. The binary question was whether a small stream window could recover the useful window path without reviving the full setup-prime/window allocator cliff. | Full setup-prime/window restore remained invalid: `scratch/ps1-perf-iterate/visitor3-window-clean76-focused/20260517-222321-1342045/summary.json` still BSODed during clean-rect allocation even after a temporary `76 KiB` clean-rect split. A `64 KiB` window for both tides passed but regressed low (`scratch/ps1-perf-iterate/visitor3-window64-clean76-focused/20260517-223024-1381293/summary.json`: low `1110/1040 -> 1137/1041`). The accepted shape keeps the normal `96 KiB` clean-rect split, disables setup-prime under clean relief, preserves the stream window only for high tide, and leaves low tide on the stage1-only path. Accepted focused proof: `scratch/ps1-perf-iterate/visitor3-highonly-window64-clean96-final-focused/20260517-225353-1513633/summary.json`; accepted under-green canary: `scratch/ps1-perf-iterate/visitor3-highonly-window64-clean96-final-canaries/20260517-225651-1530793/summary.json`. | Promoted. VISITOR3 high improves from the allocator refresh `1232/1033` to `1118/1041`, overrun `199 -> 77`, blocking `478 -> 98`, loop reads/read time `137/478 -> 14/128`, due `137 -> 7`, and target speed `83.847% -> 93.113%`. VISITOR3 low improves from `1231/1040` to `1107/1042`, overrun `191 -> 65`, blocking `438 -> 347`, loop reads/read time `126/438 -> 124/426`, due `126 -> 98`, and target speed `84.484% -> 94.128%`. The same under-green canary refreshes W1 high/low to `1475/1433` and `1479/1435`, B2 high/low to `1351/1311` and `1339/1315`, and B4 high/low to `2847/2816` and `2853/2816`. | Keep this as the current allocator-era under-green baseline. Public rollup improves to `+0.3414%` over target / `99.6691%` target speed; raw signed rollup is about `-0.3755%` / `100.3994%`; bands remain `118` green, `6` yellow, `2` orange, and `0` red. Methodology total is about `17.06` public over-target points removed and `+12.57` public target-speed points gained since the compact full-matrix baseline. Next focus remains VISITOR3 data-shape/scheduler ownership because the high-only window is still orange and low cannot keep the window without visible regression. |
| 2026-05-17 | `visitor3-high-window68-cleanrelief` | `ps1: tune visitor3 high clean-relief window` | Tighten the accepted VISITOR3 high clean-relief stream-window size after the `64 KiB` promotion proved allocator-safe but still left `98` blocking VBlanks. The binary question was whether a slightly larger high-only window could remove more CD stalls without crossing the clean-rect/CACHE pressure cliff or stealing visible cadence. | Swept nearby sizes on the same high-only relief path. `72 KiB` passed but regressed active speed to `1121/1042`; `70 KiB` passed but was weaker than the knee at `1115/1041`; `69 KiB` and `67 KiB` were phase-negative at `1124/1041` and `1123/1041`. The accepted shape is `68 KiB`, with focused proof `scratch/ps1-perf-iterate/visitor3-high-window68-focused/20260517-232145-1670419/summary.json` and under-green canary `scratch/ps1-perf-iterate/visitor3-high-window68-canaries/20260517-232912-1713414/summary.json`. | Promoted. VISITOR3 high improves from the `64 KiB` baseline `1118/1041` to `1113/1042`, overrun `77 -> 71`, blocking `98 -> 83`, loop reads/read time `14/128 -> 8/95`, due `7 -> 5`, and target speed `93.113% -> 93.621%`. VISITOR3 low, WALKSTUF1 high/low, BUILDING2 high/low, and BUILDING4 high/low stayed exact to the current matrix in the canary. | Keep `68 KiB` as the new VISITOR3 high window knee. Public rollup improves to `+0.3367%` over target / `99.6731%` target speed; raw signed rollup is about `-0.3802%` / `100.4035%`; bands remain `118` green, `6` yellow, `2` orange, and `0` red. Next VISITOR3 work should be data-shape/scheduler ownership because scalar window size is now swept and nonmonotonic around the knee. |
| 2026-05-17 | `visitor3-low-window16-cleanrelief` | `ps1: keep visitor3 low stream window under clean relief` | Reopen low-tide VISITOR3 stream-window residency after the 64 KiB both-tide attempt regressed low. The binary question was whether a much smaller low-only clean-relief window could recover CD pressure without the old allocator cliff or hidden refill debt. | Enabled the clean-relief stream window for both VISITOR3 tides, kept high at the accepted `68 KiB` knee, and capped low at `16 KiB` behind a slack-5 guard. Slack `4` improved low locally to `1088/1039` but failed with `prefetch_overrun_vb=3`; slack `5` passed cleanly; slack `6` was exact-flat to slack `5`. Low window `20 KiB` regressed versus the accepted knee to `1092/1039`, and `24 KiB` passed but was slower at `1090/1039`. Accepted under-green canary: `scratch/ps1-perf-iterate/visitor3-low-window16-slack5-canaries/20260517-235835-1880144/summary.json`. | Promoted. VISITOR3 low improves from the prior stage1/high-window baseline `1107/1042` to `1088/1038`, overrun `65 -> 50`, blocking `347 -> 104`, loop reads/read time `124/426 -> 21/123`, due `98 -> 17`, and target speed `94.128% -> 95.404%`. VISITOR3 high stays exact at `1113/1042`, and WALKSTUF1 high/low, BUILDING2 high/low, and BUILDING4 high/low stayed exact to the current matrix in the canary. | Keep `16 KiB` plus slack `5` as the VISITOR3 low clean-relief window knee. Public rollup improves to `+0.3255%` over target / `99.6833%` target speed; raw signed rollup is about `-0.3914%` / `100.4136%`; bands move to `118` green, `7` yellow, `1` orange, and `0` red. Methodology total is about `17.07` public over-target points removed and `+12.58` public target-speed points gained since the compact full-matrix baseline. Next VISITOR3 work should target high-tide data-shape/scheduler ownership; scalar low window size is now swept around the knee. |
| 2026-05-18 | `visitor3-high-payloadtrim-cleanrelief` | `ps1: trim visitor3 high terminal window overread` | Continue the VISITOR3 high cleanup path after the `68 KiB` window knee still left `83` blocking VBlanks. The active terminal refill was reading past the last required non-resident payload and into relative sectors already covered by the high-tide setup segment, so the binary question was whether trimming the read to the payload boundary could recover timing without changing residency or hidden refill. | Added a VISITOR3 high-only read-end trim in `fgRuntimeFillWindowForEntry()` that detects resident setup-segment starts inside the current read, scans the entry table for the last needed payload before that segment, sector-aligns that boundary, and shortens the refill. The larger `203..245` setup-segment swing was rejected despite improving high to `1088/1043` because it raised hidden refill `5 -> 6`; slack `5`/`6` variants still failed the same refill gate. Accepted focused artifact: `scratch/ps1-perf-iterate/visitor3-high-payloadtrim-focused/20260518-003133-2068582/summary.json`; accepted canary: `scratch/ps1-perf-iterate/visitor3-high-payloadtrim-canaries-pass/20260518-004641-2155905/summary.json`. | Promoted. VISITOR3 high improves from the accepted `68 KiB` row `1113/1042` to `1106/1042`, overrun `71 -> 64`, blocking `83 -> 76`, loop read time `95 -> 88`, and target speed `93.621% -> 94.213%`; loop reads and due misses stay flat at `8` and `5`, and hidden refill stays flat at `5`. VISITOR3 low, WALKSTUF1 high/low, BUILDING2 high/low, and BUILDING4 high/low stayed exact to the current matrix in the canary. | Keep this as the VISITOR3 high terminal-window baseline. Public rollup improves to `+0.3202%` over target / `99.6880%` target speed; raw signed rollup is about `-0.3967%` / `100.4183%`; bands remain `118` green, `7` yellow, `1` orange, and `0` red. Methodology total is about `17.08` public over-target points removed and `+12.59` public target-speed points gained since the compact full-matrix baseline. Next VISITOR3 high swings need data-shape or scheduler ownership; the rejected wider setup-segment path is only worth reopening if hidden-refill debt can be reduced. |
| 2026-05-18 | `visitor3-high-seg203-229-cleanrelief` | `ps1: extend visitor3 high setup segment` | Continue after the terminal-window trim lowered high-tide blocking but still left VISITOR3 high orange. The binary question was whether a narrower version of the rejected broad `203..245` setup-segment swing could keep the useful terminal residency while avoiding the hidden-refill `6` debt. | Extended `FG_VISITOR3_HIGH_SETUP_SEGMENT2_BYTES` from `1` to `26` sectors, covering relative sectors `203..229`, while keeping the terminal read trim. Size sweep: `18` sectors passed but smaller (`1102/1042`, blocking `74`); `26` sectors passed best (`1096/1040`, blocking `71`); `27`, `30`, and `34` sectors failed with hidden refill `6`; `28` sectors passed but was weaker (`1097/1041`, blocking `73`). Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-seg203-229-payloadtrim-focused/20260518-010949-2287886/summary.json`; canary artifact: `scratch/ps1-perf-iterate/visitor3-high-seg203-229-payloadtrim-canaries-pass/20260518-011814-2337084/summary.json`. | Promoted. VISITOR3 high improves from `1106/1042` to `1096/1040`, overrun `64 -> 56`, blocking `76 -> 71`, loop read time `88 -> 83`, and target speed `94.213% -> 94.891%`; loop reads, hidden refill, and due misses stay flat at `8/5/5`. VISITOR3 low, WALKSTUF1 high/low, BUILDING2 high/low, and BUILDING4 high/low stayed exact to the current matrix in the canary. | Keep `203..229` as the clean high setup-segment2 knee. Public rollup improves to `+0.3141%` over target / `99.6933%` target speed; raw signed rollup is about `-0.4028%` / `100.4237%`; bands remain `118` green, `7` yellow, `1` orange, and `0` red. Methodology total is about `17.09` public over-target points removed and `+12.59` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `visitor3-no-force-relief-current` | Log-only; source restored and PS1 rebuilt. | After the setup-segment2 `203..229` promotion, retest the largest allocator-era binary swing: remove VISITOR3's forced clean-memory relief and allow the normal full setup-prime/window path. The hypothesis was that the new allocator plus targeted setup segment might now fit the old full-residency path and recover a large VISITOR3 high gap. | Temporarily changed `fgSceneForcesCleanMemoryRelief()` to return `0` for VISITOR3 and ran the focused high gate against `scratch/ps1-perf-iterate/visitor3-high-seg203-229-payloadtrim-canaries-pass/20260518-011814-2337084/summary.json`. Artifact: `scratch/ps1-perf-iterate/visitor3-no-force-relief-focused/20260518-014212-2473713/summary.json`; log: `scratch/ps1-perf-iterate/visitor3-no-force-relief-focused/20260518-014212-2473713/visitor3-high/headless-regtest.log`. | Failed/no promotion. The scene did not reach `JCPERF2`; it hit `JCBSOD-FATAL CACHE exhausted (region+libc both): req=97280 have=82576` while allocating the second `grCleanRectPixels` chunk after allocating a `327680` byte `fg-stream-window`. At halt, CACHE used/peak was `572784` and TRANSIENT used/peak was `703492`. | Restore forced clean relief for VISITOR3. Full setup-prime/window residency still exceeds the allocator budget; next big swing should be a smaller relief-preserving resident window/segment or moving clean-rect pixels/another large resident out of CACHE before retrying full relief removal. Public rollup remains `+0.3141%` over target / `99.6933%` target speed; bands remain `118` green, `7` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `visitor3-relief-setupprime-sweep` | Log-only; source restored and PS1 rebuilt. | Follow the no-relief allocator miss with a smaller version of the same idea: keep VISITOR3 clean-memory relief, keep the `68 KiB` high-tide stream window, but allow a capped setup-prime window to recover some early residency without allocating the failing full `320 KiB` window. | Added a temporary VISITOR3 high-only clean-relief setup-prime cap and swept `96 KiB`, `80 KiB`, and `64 KiB` against the accepted `visitor3-high-seg203-229` baseline. Artifacts: `scratch/ps1-perf-iterate/visitor3-relief-setupprime96-focused/20260518-014805-2507129/summary.json`, `scratch/ps1-perf-iterate/visitor3-relief-setupprime80-focused/20260518-014953-2517812/summary.json`, and `scratch/ps1-perf-iterate/visitor3-relief-setupprime64-focused/20260518-015157-2529943/summary.json`. | Failed/no promotion. `96 KiB` regressed VISITOR3 high to `1107/1039` with blocking `86` and overrun `68`; `80 KiB` regressed to `1101/1035` with blocking `83` and overrun `66`; `64 KiB` still regressed to `1098/1038` with blocking `77`, overrun `60`, and prefetch overrun `9`. | Keep clean-relief setup-prime disabled for VISITOR3. Priming the setup region changes active window timing enough to increase visible blocking/overrun even when it does not grow CACHE. Next VISITOR3 high work should avoid setup-prime and target payload shape, clean-rect memory ownership, or late-read scheduling. Public rollup remains `+0.3141%` over target / `99.6933%` target speed; bands remain `118` green, `7` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `walkstuf1-midseg2-relocation` | Log-only; source restored and PS1 rebuilt. | Test whether W1's second setup segment should move from the old late tail (`410..434` low, `411..435` high) to the read planner's current hot mid cluster (`297..321` low, `298..322` high). This reused the allocator-era targeted setup-segment technique without increasing setup bytes. | Temporarily changed `FG_WALKSTUF1_LOW_SETUP_SEGMENT2_START` to `297` sectors and `FG_WALKSTUF1_HIGH_SETUP_SEGMENT2_START` to `298` sectors, keeping both segment2 lengths at `24` sectors. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-midseg2-297-321-298-322-focused/20260518-015741-2562573/summary.json`. | Failed/no promotion. W1 high regressed `1475/1433 -> 1482/1431`, overrun `42 -> 51`, and blocking `76 -> 80`; W1 low regressed `1479/1435 -> 1481/1433`, overrun `44 -> 48`, and blocking `65 -> 76`. Scene correctness completed, but the baseline gate rejected both tides. | Keep the W1 second setup segment on the late tail. The hot mid cluster only looks attractive as additional coverage; replacing the tail loses more than it saves. A future W1 big swing needs a third segment/larger grouped window or payload reshaping, not a one-for-one tail swap. Public rollup remains `+0.3141%` over target / `99.6933%` target speed; bands remain `118` green, `7` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `building2-low-setupseg-sweep` | Log-only; source restored and PS1 rebuilt. | Try to push the near-green BUILDING2 low row over 99% using a small targeted setup segment around the read planner's hottest current clusters. The row only needs a small loop reduction, so the hypothesis was that a 12-24 sector CACHE setup segment could remove active-loop CD stalls without meaningful setup/cadence debt. | Temporarily added a low-tide BUILDING2 setup-segment branch in `fgRuntimePrimeSetupSegment()` and swept `96..120`, `112..124`, and `96..108`. Artifacts: `scratch/ps1-perf-iterate/building2-low-setupseg96-120-focused/20260518-020248-2592161/summary.json`, `scratch/ps1-perf-iterate/building2-low-setupseg112-124-focused/20260518-020446-2603844/summary.json`, and `scratch/ps1-perf-iterate/building2-low-setupseg96-108-focused/20260518-020644-2615410/summary.json`. | Failed/no promotion. `96..120` improved blocking only `54 -> 53` and due `12 -> 11`, but regressed loop `1339 -> 1341` and scene setup `1605 -> 1619`; `112..124` regressed loop to `1343` and blocking to `55`; `96..108` regressed loop to `1345`, blocking to `68`, and due to `14`. | Do not add B2 low setup segments on this axis. The active row is phase-sensitive; paying these clusters during setup shifts/refills poorly enough to lose loop speed. Next B2 low attempts should be read-group/scheduler-owned, payload-shape, or dirty-work reductions rather than setup residency. Public rollup remains `+0.3141%` over target / `99.6933%` target speed; bands remain `118` green, `7` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `building4-high-setupseg264-288` | `ps1: prime building4 high mid sectors` | Push the nearest under-99 row across the 99% line using the allocator-era targeted setup-segment technique. BUILDING4 high needed only a few loop VBlanks, and the read planner showed a mid-pack cluster that could remove active CD pressure without touching pack layout. | Added a high-tide BUILDING4 setup segment for relative sectors `264..288` in `fgRuntimePrimeSetupSegment()`, allocated as a small CACHE setup segment with a `MEM_REGION_RATIONALE`. Rejected the paired high/low `23..47` / `178..202` probe because low regressed, and rejected high-only `31..47` because it left blocking debt and setup cost without enough loop gain. Accepted focused artifact: `scratch/ps1-perf-iterate/building4-high-setupseg264-288-focused/20260518-021744-2678785/summary.json`; accepted canary: `scratch/ps1-perf-iterate/building4-high-setupseg264-288-canaries-pass/20260518-023050-2754222/summary.json`. | Promoted as an active-loop speed win with a bounded setup-cost trade. BUILDING4 high improves `2847/2816 -> 2843/2816`, overrun `31 -> 27`, blocking `36 -> 34`, prefetch overrun `32 -> 30`, loop reads/read time `49/256 -> 47/251`, and target speed `98.911% -> 99.050%`; due stays `1`. Scene/setup rises `3115 -> 3121` (+0.19%), accepted under the 0.25% canary allowance. BUILDING4 low, WALKSTUF1 high/low, and BUILDING2 high/low stayed exact-flat. | Keep `264..288` as the BUILDING4 high setup-segment knee. Public rollup improves to `+0.3130%` over target / `99.6944%` target speed; raw signed rollup is about `-0.4039%` / `100.4248%`; bands move to `119` green, `6` yellow, `1` orange, and `0` red. Methodology total is about `17.09` public over-target points removed and `+12.59` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `visitor3-budgetshift-no-force-relief` | Log-only; source restored. | Test whether the no-force-relief VISITOR3 allocator cliff could be avoided by moving static region budget from CACHE to TRANSIENT instead of changing the foreground scheduler. The binary question was whether `CACHE 640 KiB / TRANSIENT 768 KiB -> CACHE 576 KiB / TRANSIENT 832 KiB` would let enough clean-rect pixels remain scene-transient while keeping the full setup-prime/window path alive. | Temporarily shifted the region budgets, changed `fgSceneForcesCleanMemoryRelief()` to return `0`, and ran the focused VISITOR3 high gate against `scratch/ps1-perf-iterate/visitor3-high-seg203-229-payloadtrim-canaries-pass/20260518-011814-2337084/summary.json`. Artifact: `scratch/ps1-perf-iterate/visitor3-budgetshift-no-force-relief-vnext/20260518-030235-2934063/summary.json`; log: `scratch/ps1-perf-iterate/visitor3-budgetshift-no-force-relief-vnext/20260518-030235-2934063/visitor3-high/headless-regtest.log`. | Failed/no promotion. The scene still did not reach `JCPERF2`; after allocating a `327680` byte `fg-stream-window`, the second `grCleanRectPixels` allocation failed with `JCBSOD-FATAL CACHE exhausted (region+libc both): req=97280 have=114324`. At halt, CACHE used/peak was `475500` and TRANSIENT used/peak was `800772`, leaving only about `51 KiB` transient headroom and about `114 KiB` cache headroom for roughly `366 KiB` of VISITOR3 clean-rect snapshots. | Restore the R33 budgets and forced VISITOR3 clean relief. Budget shifting alone cannot make full setup-prime/window residency fit; a future retry would need to remove or compress a large live resident, not just repartition the same 1440 KiB static pool. Public rollup remains `+0.3130%` over target / `99.6944%` target speed; bands remain `119` green, `6` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `building4-low-readgroup-sweep` | Log-only; source restored. | After the low-tide setup-segment and local-LZ attempts failed, test whether BUILDING4 low could get a no-setup-cost scheduler win from the read planner's two best `current-read-group-compatible` rows. | Temporarily added a low-tide BUILDING4 read-group table and selected it in the stream-window policy. Swept `{178,202,0}` and `{262,286,0}` against the accepted BUILDING4 canary baseline. Artifacts: `scratch/ps1-perf-iterate/building4-low-rg178-202-vnext/20260518-031112-2982237/summary.json` and `scratch/ps1-perf-iterate/building4-low-rg262-286-vnext/20260518-031324-2994968/summary.json`. | Failed/no promotion. Both rows were exact-flat at BUILDING4 low `3125/2853/2816`, overrun `37`, blocking/refill `42/35`, loop reads `30`, due `1`, and fixed pack LBA/PS-EXE bucket, while growing `foregroundPilotPlay` by `72` bytes and shifting following hot symbols by `+72`. | Close BUILDING4-low scalar read groups on this baseline. The planner-visible rows do not fire in a way the runtime can convert into measured work; remaining B4-low paths should be payload/layout changes, dirty-work reduction, or a different scheduler primitive rather than a one-row read table. Public rollup remains `+0.3130%` over target / `99.6944%` target speed; bands remain `119` green, `6` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `walkstuf1-readgroup-replace-tail-sweep` | Log-only; source restored. | Retest W1 scheduler-owned read groups after the setup-segment tail relocation failed. The binary question was whether replacing the redundant tail read-group row, rather than moving setup residency, could capture the planner's mid-cluster or lower-visible-risk candidates without changing executable layout. | Temporarily replaced the final W1 high/low read-group row with `{298,322,0}` / `{297,321,0}`, then retried the same rows with slack `4`, then replaced them with the lower-visible-risk rows `{178,194,0}` / `{91,107,0}`. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-midcluster-rg-replace-tail-vnext/20260518-032033-3034825/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-midcluster-rg-replace-tail-slack4-vnext/20260518-032357-3054597/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-lowrisk-rg-replace-tail-vnext/20260518-032720-3074180/summary.json`. | Failed/no promotion. The no-slack mid-cluster rows regressed W1 high `1475/1433 -> 1485/1431` and W1 low `1479/1435 -> 1484/1435`. Slack `4` was exact-flat to baseline for both tides, with no key metric improved. The lower-visible-risk rows regressed high to `1485/1430`, overrun `55`, blocking `89`, and low to `1481/1435`; low blocking improved `65 -> 59`, but loop and overrun still regressed. | Close one-for-one W1 read-group replacement on this baseline. The row either fires too early and steals visible cadence, or slack-gates itself inert. Remaining W1 gains likely need additive residency, payload/dirty-work reduction, or a scheduler primitive that preserves the existing tail coverage while grouping the mid clusters. Public rollup remains `+0.3130%` over target / `99.6944%` target speed; bands remain `119` green, `6` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `building2-high-rg83-95-replace-tail` | `ps1: retime building2 high read group` | Take a low-risk scheduler swing at BUILDING2 high after W1 and B4 scalar read groups either regressed or stayed inert. The planner marked relative sectors `83..95` as a low-visible-risk candidate, and the current BUILDING2 high table still had a short tail row that could be replaced without increasing table size or shifting executable layout. | Replaced the final BUILDING2 high read-group row `{249,257,0}` with `{83,95,0}` in `src/foreground_pilot.c`. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg83-95-replace-tail-vnext/20260518-033207-3101577/summary.json`; accepted no-regression canary: `scratch/ps1-perf-iterate/building2-high-rg83-95-replace-tail-canaries-pass/20260518-034414-3171499/summary.json`, covering BUILDING2 high/low, BUILDING4 high/low, and WALKSTUF1 high/low. | Promoted. BUILDING2 high keeps loop flat at `1351` while target improves `1311 -> 1313`, overrun improves `40 -> 38`, prefetch overrun improves `18 -> 14`, and scene improves `1625 -> 1624`; blocking/read time/due stay flat at `50/207/7`. BUILDING2 low, BUILDING4 high/low, and WALKSTUF1 high/low stayed exact-flat in the canary. | Keep `83..95` as the new BUILDING2 high scheduler row. Public rollup improves to `+0.3117%` over target / `99.6956%` target speed; raw signed rollup is about `-0.4052%` / `100.4260%`; bands remain `119` green, `6` yellow, `1` orange, and `0` red. Methodology total is about `17.09` public over-target points removed and `+12.60` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `visitor3-high-tail-segment-sweep` | Log-only; source restored. | Reopen VISITOR3 high after the BUILDING2 high scheduler win, targeting the remaining terminal active read around relative sectors `262..274`. The binary question was whether extending the accepted terminal setup segment backward from `277..293` could trim the active refill enough to move the single orange row without disturbing hidden refill. | Swept terminal setup-segment starts `262`, `270`, `274`, `275`, and `276` while keeping the accepted `203..229` segment and terminal read trim. Also retried the best visible-timing row, `274..293`, with `FG_VISITOR3_HIGH_WINDOW_MIN_SLACK_VBLANKS=5`. Artifacts: `scratch/ps1-perf-iterate/visitor3-high-seg262-293-vnext/20260518-040230-3275342/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-seg270-293-vnext/20260518-040426-3286494/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-seg274-293-vnext/20260518-040631-3298643/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-seg274-293-slack5-vnext/20260518-040813-3308549/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-seg276-293-vnext/20260518-040951-3318066/summary.json`, and `scratch/ps1-perf-iterate/visitor3-high-seg275-293-vnext/20260518-041132-3327951/summary.json`. | Failed/no promotion. Broad `262..293` regressed to `1104/1041`, blocking `80`; `270..293` regressed to `1099/1041`, blocking `73`; `276..293` regressed to `1097/1041`, blocking `72`, hidden refill `6`; `275..293` regressed to `1097/1041`, blocking `73`. The only visible-timing win was `274..293`, which improved loop `1096 -> 1094` and overrun `56 -> 53`, but it raised hidden/prefetch overrun `5 -> 6`; slack `5` made it worse at `1099/1041`, blocking `76`, hidden refill `9`. | Close the terminal setup-segment extension lane on this baseline. The remaining VISITOR3 high gap is not a simple residency boundary problem: one extra sector can trade visible loop time against hidden refill debt, and added slack destroys the win. Next VISITOR3 high work should target payload shape, clean-rect memory ownership, or a scheduler primitive that can model hidden refill debt rather than broadening setup residency. Public rollup remains `+0.3117%` over target / `99.6956%`; bands remain `119` green, `6` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `visitor3-high-d4-paid-gap-sweep` | Log-only; pack/source restored. | Test a pack-authored version of the remaining VISITOR3 high residency problem without growing setup segments. The accepted `203..229` setup segment contains a `16,229` byte paid gap; frames `135` and `139` can be D4-compressed small enough to move into that gap while keeping pack size, LBA/sectors, and residency bytes fixed. | Rewrote `generated/ps1/foreground/VISITOR3.FG2` out-of-place, first making frame `135` a `14,246` byte previous-frame D4 payload at offset `417308`, then restoring and making frame `139` a `13,418` byte D4 payload at the same paid-gap offset. Each probe temporarily added the high-tide frame to `fgRuntimeUsesPreviousFrameDelta()`. Transform summaries: `scratch/visitor3-high-frame135-d4-gap-vnext/summary.json` and `scratch/visitor3-high-frame139-d4-gap-vnext/summary.json`; focused artifacts: `scratch/ps1-perf-iterate/visitor3-high-frame135-d4-gap-vnext/20260518-041741-3362532/summary.json` and `scratch/ps1-perf-iterate/visitor3-high-frame139-d4-gap-vnext/20260518-041941-3374075/summary.json`. | Failed/no promotion. Frame `135` reduced CD pressure (`blocking 71 -> 68`, loop reads `8 -> 7`, due `5 -> 4`) but D4 decode cost regressed loop `1096 -> 1123` and overrun `56 -> 82`. Frame `139` showed the same tradeoff: blocking `71 -> 67`, loop reads `8 -> 7`, due `5 -> 4`, but loop regressed `1096 -> 1117` and overrun `56 -> 77`. | Close large-frame previous-D4 relocation for VISITOR3 high. Paid-gap movement is real, but the current D4 decoder costs more visible time than the removed CD read. Future high pack work needs no-decode relocation, a cheaper custom/dictionary shape, or generated scheduler ownership; do not spend more time moving 13-14 KiB D4 payloads into the setup gap. Public rollup remains `+0.3117%` over target / `99.6956%`; bands remain `119` green, `6` yellow, `1` orange, and `0` red. |
| 2026-05-18 | `visitor3-low-thirdseg206-230` | `ps1: add visitor3 low third setup segment` | Reopen VISITOR3 low after the high-side terminal and D4 paid-gap lanes closed. The read plan still showed a low-tide setup cluster at relative sectors `206..230`, and the allocator-era third-segment question was whether adding a small retained segment, rather than replacing the accepted `150..177` or `281..305` segments, could remove active CD debt without disturbing VISITOR3 high or the other under-99 canaries. | First tested inert low read groups `{206,230,0}` and `{248,264,0}`; both were exact-flat with `group_hits=0`. Replacing segment2 `150..177` with `206..233` failed (`1088 -> 1097`, overrun `50 -> 62`, blocking `104 -> 123`, due `17 -> 21`). The accepted source change adds a third VISITOR3 low setup segment for `206..230`, teaches runtime setup lookup about the third retained buffer, and updates `ps1-foreground-read-plan.py` so setup coverage reports all three ranges. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-thirdseg206-230-vnext/20260518-043124-3440753/summary.json`; visitor high/low canary: `scratch/ps1-perf-iterate/visitor3-low-thirdseg206-230-visitor-canaries-pass/20260518-043607-3468206/summary.json`; non-visitor canary: `scratch/ps1-perf-iterate/visitor3-low-thirdseg206-230-nonvisitor-canaries-pass/20260518-043907-3485772/summary.json`. | Promoted as an active-loop speed win. VISITOR3 low improves scene `1378 -> 1377`, active loop/target `1088/1038 -> 1074/1039`, overrun `50 -> 35`, blocking `104 -> 85`, loop reads `21 -> 19`, loop-read VBlanks `123 -> 103`, and due misses `17 -> 15`; hidden refill stays `0`, pack LBA/sectors stay `23379/760`, and the row moves to `96.741%` target speed. VISITOR3 high stayed exact-flat at `1096/1040`, blocking `71`, reads/due `8/5`; BUILDING2 high/low, BUILDING4 high/low, and WALKSTUF1 high/low stayed exact-flat. | Keep `206..230` as the third VISITOR3 low retained setup segment. Public rollup improves to `+0.3002%` over target / `99.7062%` target speed; raw signed rollup is about `-0.4167%` / `100.4366%`; bands remain `119` green, `6` yellow, `1` orange, and `0` red. Methodology total is about `17.10` public over-target points removed and `+12.61` public target-speed points gained since the compact full-matrix baseline. Next targets remain VISITOR3 high first, then WALKSTUF1 low/high, BUILDING2 high/low, and BUILDING4 low. |
| 2026-05-18 | `visitor3-high-frame139-rawgap` | `ps1: move visitor3 high frame139 into retained gap` | Follow up on the rejected VISITOR3 high D4 paid-gap sweep. The D4 probes proved the paid-gap relocation reduced CD reads but lost more loop time to decode. The binary question was whether frame `139`'s existing raw payload could fit in the already retained `203..229` setup-segment gap and remove the same read with zero new runtime decode. | Rewrote only `generated/ps1/foreground/VISITOR3.FG2`, moving frame `139`'s raw `15679` byte payload to offset `417308` inside the retained setup-segment gap. The transform preserved file bytes `1555450`, pack LBA/sectors `22619/760`, and the PS-EXE bucket; no D4 predicate or source change was required. Transform summary: `scratch/visitor3-high-frame139-rawgap-vnext/summary.json`. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame139-rawgap-vnext/20260518-050255-3620479/summary.json`; canary artifact: `scratch/ps1-perf-iterate/visitor3-high-frame139-rawgap-canaries-pass/20260518-050438-3630173/summary.json`. | Promoted as a no-decode pack-layout speed win. VISITOR3 high keeps active loop flat at `1096` while target improves `1040 -> 1041`, overrun `56 -> 55`, blocking/read time `71/83 -> 67/79`, loop reads `8 -> 7`, and due misses `5 -> 4`; hidden refill stays `5`. VISITOR3 low stays exact at `1074/1039`, and BUILDING2 high/low, BUILDING4 high/low, and WALKSTUF1 high/low stayed exact to the current matrix in the canary. | Keep frame `139` raw in the retained gap as the new VISITOR3 high baseline. Public rollup improves to `+0.2994%` over target / `99.7069%` target speed; raw signed rollup is about `-0.4175%` / `100.4373%`; bands remain `119` green, `6` yellow, `1` orange, and `0` red. Methodology total is about `17.10` public over-target points removed and `+12.61` public target-speed points gained since the compact full-matrix baseline. Next VISITOR3 high work should target the remaining uncovered `228..262` read with no-decode payload shape, scheduler ownership, or memory relief rather than D4 decode. |
| 2026-05-18 | `visitor3-high-frame135-d4-min16-gap` | Log-only; pack/source restored and PS1 rebuilt. | Try one narrower D4 follow-up after frame139's no-decode relocation. Frame `138` could not be trimmed or local-LZ-compressed enough to fit the `244..254` in-pack hole, so the next binary question was whether frame `135` could be encoded with fewer D4 commands (`min_match=16`) and moved into that same hole to shorten the remaining `228..262` active read without the heavier min-8 D4 decode cost. | Temporarily encoded frame `135` against frame `134` as a `15487` byte D4 payload with `263` commands and moved it to offset `501526` in `generated/ps1/foreground/VISITOR3.FG2`, then temporarily added high-tide frame `135` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/visitor3-high-frame135-d4-min16-gap-vnext/summary.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame135-d4-min16-gap-vnext/20260518-053139-3780797/summary.json`. | Failed/no promotion. The split read shape became worse: VISITOR3 high regressed scene/loop `1391/1096 -> 1401/1106`, target stayed `1041`, overrun `55 -> 65`, blocking/read time `67/79 -> 77`, loop reads `7 -> 8`, and due stayed `5`; hidden refill stayed `5`. The PS-EXE bucket and pack LBA stayed fixed. | Restore frame `135` raw at its accepted offset and close large-frame D4 movement even with a higher min-match. For VISITOR3 high, D4 decode plus altered read splitting is still worse than the current raw frame139 relocation. Next attempts should be no-decode raw fit, generated deadline ownership, or memory/clean-rect changes, not another large-frame previous-D4 payload. |
| 2026-05-18 | `visitor3-high-thirdseg228-262` | `ps1: add visitor3 high third setup segment` | Take the remaining big VISITOR3 high residency swing after no-decode frame139 relocation and the D4 min16 miss. The read planner showed exactly one uncovered high-tide active read, relative sectors `228..262`, covering frames `135`, `136`, and `138`. The binary question was whether a third retained setup segment could pay that whole range during setup without reintroducing allocator pressure or regressing other under-green rows. | Added high-tide `FG_VISITOR3_HIGH_SETUP_SEGMENT3_START=228` and `FG_VISITOR3_HIGH_SETUP_SEGMENT3_BYTES=34` in `src/foreground_pilot.c`, reusing the existing third setup-segment allocation path already proven by VISITOR3 low. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-thirdseg228-262-vnext/20260518-053501-3800130/summary.json`; canary artifact: `scratch/ps1-perf-iterate/visitor3-high-thirdseg228-262-canaries-pass/20260518-053640-3809465/summary.json`. | Promoted as an active-loop CD win. VISITOR3 high improves scene `1391 -> 1395`, active loop/target `1096/1041 -> 1082/1042`, overrun `55 -> 40`, blocking/read time `67/79 -> 50/58`, loop reads `7 -> 6`, and due misses `4 -> 3`; hidden refill stays `5`, pack LBA/sectors stay `22619/760`, and the read planner reports `uncovered=0`. VISITOR3 low, BUILDING2 high/low, BUILDING4 high/low, and WALKSTUF1 high/low stayed exact to the current matrix in the canary. | Keep `228..262` as the third VISITOR3 high retained setup segment. Public rollup improves to `+0.2880%` over target / `99.7174%` target speed; raw signed rollup is about `-0.4289%` / `100.4478%`; bands move to `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.11` public over-target points removed and `+12.62` public target-speed points gained since the compact full-matrix baseline. Next targets remain VISITOR3 low, WALKSTUF1 low/high, BUILDING2 high/low, and BUILDING4 low. |
| 2026-05-18 | `visitor3-low-residency-rebalance-231-265` | `docs: log visitor3 low residency rebalance misses` | Try to turn the allocator-era VISITOR3 low remaining `96.741%` row into a larger residency win. The read planner ranked a fourth retained segment around `231..265` as the biggest visible-cost candidate after the accepted `206..230` third segment, with up to six saved active reads. | Tested three variants. A fourth TRANSIENT segment `231..265` failed before JCPERF2 with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=223232 have=168956`; artifact: `scratch/ps1-perf-iterate/visitor3-low-fourthseg231-265-vnext/20260518-060014-3940999/summary.json`. Moving that fourth-segment bundle to CACHE also failed before JCPERF2 by starving clean-rect CACHE allocation (`req=90880 have=51720`, `memCacheUsed=603640`); artifact: `scratch/ps1-perf-iterate/visitor3-low-fourthseg231-265-cache-vnext/20260518-060418-3964014/summary.json`. A transient-cap replacement of low segment3 from `206..230` to `231..262` passed focused low gates; artifact: `scratch/ps1-perf-iterate/visitor3-low-seg3-231-262-vnext/20260518-060818-3986738/summary.json`. | Rejected all variants. The focused replacement improved low-side CD shape but not enough: `1074/1039 -> 1076/1041`, overrun flat `35`, blocking `85 -> 79`, reads `19 -> 18`, due `15 -> 14`. The canary immediately showed an unacceptable phase regression in VISITOR3 high: `1082/1042 -> 1093/1043`, overrun `40 -> 50`, despite no source logic touching the high segment constants. Partial canary artifact: `scratch/ps1-perf-iterate/visitor3-low-seg3-231-262-canaries/20260518-061005-3997016/visitor3-high/perf-summary.json`. | Reverted source to the accepted `206..230` low third segment. Do not retry large VISITOR3 low residency by simply adding CACHE/TRANSIENT sectors; the hard cap is around `82` retained low sectors and high-tide phase is sensitive to even source-size-neutral macro/layout changes. Next VISITOR3 low attempts should be data-shape/no-decode relocation, generated read scheduling for the `214..226` cluster, or allocator/clean-rect memory relief that preserves high-tide phase. |
| 2026-05-18 | `walkstuf1-low-rg297-321` | `docs: log walkstuf1 low read-group misses` | Try a larger WALKSTUF1 low read-group swing after VISITOR3 low residency closed. The planner ranked `297..321` as a high-value current-read-group-compatible candidate, estimated to save five active reads without adding retained setup memory. | First added `{297,321,0}` to `kWalkstuf1LowReadGroups12` with the existing two retained setup segments. That failed before JCPERF2 by exhausting CACHE during clean-rect allocation after `fg-stream-window=155648` and `fgSetupSegmentBuffer=143360`; artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-vnext/20260518-061708-4037341/summary.json`. Then kept the read group but disabled the low-only `410..434` retained setup segment to free 48 KiB of CACHE; artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg297-321-dropseg2-vnext/20260518-062006-4054374/summary.json`. | Rejected. The budgeted variant produced JCPERF2 but regressed timing: WALKSTUF1 low moved `1479/1435 -> 1486/1433`, overrun `44 -> 53`, blocking `65 -> 92`, loop reads `50 -> 49`, and prefetch overrun `18 -> 15`. Dropping the `410..434` retained segment cost more than the `297..321` grouped append saved. | Reverted source to the accepted WALKSTUF1 low segment/read-group baseline. Do not retry `297..321` by itself under the current CACHE budget. Future W1 low work should either free clean-rect/CACHE pressure first, use a smaller low-risk read group that does not change allocation phase, or move payload/data shape so the `285..321` cluster is cheaper without removing the `410..434` retained segment. |
| 2026-05-18 | `building2-high-additive-rg-sweep` | Log-only; source restored. | Test whether BUILDING2 high has another allocator-era scheduler-only win after the accepted `83..95` tail-row retime. The planner exposed several current-read-group-compatible clusters while the row remained at `97.187%`, so the binary question was whether additive rows could reduce active CD reads enough to move visible timing without new setup residency. | Temporarily added three shapes to `kBuilding2HighReadGroups12`: `{122,146,0}`, `{185,197,0}`, `{241,257,0}`, then a combined `{185,197,0}` plus `{241,257,0}` row set. Artifacts: `scratch/ps1-perf-iterate/building2-high-rg122-146-add-vnext/20260518-062646-4092062/summary.json`, `scratch/ps1-perf-iterate/building2-high-rg185-197-add-vnext/20260518-062839-4102980/summary.json`, `scratch/ps1-perf-iterate/building2-high-rg241-257-add-vnext/20260518-063027-4113464/summary.json`, and `scratch/ps1-perf-iterate/building2-high-rg185-197-rg241-257-add-vnext/20260518-063214-4123881/summary.json`. | Failed/no promotion. Every variant stayed exact-flat on the speed-bearing row: BUILDING2 high remained `1624/1351/1313`, overrun `38`, blocking/refill `50/14`, and due `7`. The `185..197` and combined variants reduced reported loop reads `47 -> 45`, but that read-count drop did not reduce loop, overrun, blocking, or refill VBlanks, so it is not a speed promotion. Each additive row also shifted hot symbols by `+4` bytes. | Restored the accepted four-row BUILDING2 high table. Do not retry additive B2-high read groups unless the scheduler can convert lower read count into visible timing, or a pack/payload change changes the read cadence. Remaining B2-high paths should target dirty-work reduction, payload shape, or setup/prefetch policy changes with direct VBlank movement rather than another inert read-count-only group. |
| 2026-05-18 | `walkstuf1-low-setupseg238-342` | `ps1: retarget walkstuf1 low setup segment` | Take the W1 low big-swing lesson from the failed `297..321` read group: replacing the late tail segment alone was bad, but the read planner showed a large active cluster around `238..342`. The binary question was whether one larger retained setup segment could replace both current split slices without crossing the allocator clean-rect CACHE cliff. | Replaced `FG_WALKSTUF1_LOW_SETUP_SEGMENT_START/BYTES` with `238` / `104` sectors and disabled the second low segment by setting `FG_WALKSTUF1_LOW_SETUP_SEGMENT2_BYTES=0`. Larger replacements failed before JCPERF2 from CACHE exhaustion: `238..388` (`scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-388-replace-vnext/20260518-063845-4161159/summary.json`), `238..376` (`scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-376-replace-vnext/20260518-064130-4176764/summary.json`), `238..366` (`scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-366-replace-vnext/20260518-064359-4190888/summary.json`), and `238..350` (`scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-350-replace-vnext/20260518-064628-11398/summary.json`). The 96-sector `238..334` shape passed but was weaker; focused artifact `scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-334-replace-vnext/20260518-064906-26425/summary.json`. Accepted focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-342-replace-vnext/20260518-065116-38864/summary.json`; canary artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-342-replace-canaries/20260518-065731-74208/summary.json`. | Promoted as an active-loop CD/setup-residency win. WALKSTUF1 low improves scene `1808 -> 1813`, active loop/target `1479/1435 -> 1480/1442`, overrun `44 -> 38`, blocking/read time `65/230 -> 55/211`, refill overrun `18 -> 15`, loop reads `50 -> 38`, and due misses `10 -> 6`; pack LBA/sectors stay `25641/750`, and the PS-EXE bucket stays `233472`. WALKSTUF1 high, VISITOR3 high/low, BUILDING2 high/low, and BUILDING4 high/low stayed stable in the canary. | Keep `238..342` as the current WALKSTUF1 low retained setup segment and leave low segment2 disabled. Public rollup improves to `+0.2846%` over target / `99.7207%` target speed; raw signed rollup is about `-0.4323%` / `100.4510%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.12` public over-target points removed and `+12.62` public target-speed points gained since the compact full-matrix baseline. Next targets remain VISITOR3 high/low, WALKSTUF1 high, BUILDING2 high/low, and BUILDING4 low. |
| 2026-05-18 | `walkstuf1-high-setupseg286-342` | `ps1: retarget walkstuf1 high setup segment` | Follow the W1 low setup-residency win with the matching high-tide mid-cluster swing. The failed question was whether replacing the first high slice with `286..342` while keeping the tail would help; that lost critical early coverage. The promoted question was whether keeping the accepted `198..244` first slice and replacing the old `411..435` tail with `286..342` would move visible timing without crossing the allocator clean-rect cliff. | First moved the first high segment to `286..342` and kept tail `411..435`; artifact `scratch/ps1-perf-iterate/walkstuf1-high-setupseg286-342-keeptail-vnext/20260518-072822-247997/summary.json` regressed high `1475/1433 -> 1490/1432`, overrun `42 -> 58`, blocking `76 -> 77`, and prefetch overrun `15 -> 23`. The promoted source keeps `FG_WALKSTUF1_HIGH_SETUP_SEGMENT_START/BYTES=198/46` and changes segment2 to `286/56`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-keepfirst-setupseg286-342-vnext/20260518-073013-258774/summary.json`; accepted canary: `scratch/ps1-perf-iterate/walkstuf1-high-keepfirst-setupseg286-342-canaries/20260518-073441-284390/summary.json`. A wider `286..350` variant failed before `JCPERF2` from CACHE exhaustion during clean-rect allocation; artifact `scratch/ps1-perf-iterate/walkstuf1-high-keepfirst-setupseg286-350-vnext/20260518-073209-269951/summary.json`. | Promoted as an active-loop CD/setup-residency win. WALKSTUF1 high improves scene `1801 -> 1807`, active loop/target `1475/1433 -> 1472/1438`, overrun `42 -> 34`, blocking/read time `76/229 -> 60/228`, loop reads `55 -> 48`, and due misses `15 -> 10`; prefetch overrun stays flat at `15`, pack LBA/sectors stay `24891/750`, and the PS-EXE bucket stays `233472`. WALKSTUF1 low, VISITOR3 high/low, BUILDING2 high/low, and BUILDING4 high/low stayed exact to the current matrix in the canary. | Keep `198..244` plus `286..342` as the current WALKSTUF1 high retained setup shape. Public rollup improves to `+0.2801%` over target / `99.7249%` target speed; raw signed rollup is about `-0.4368%` / `100.4553%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.12` public over-target points removed and `+12.62` public target-speed points gained since the compact full-matrix baseline. Next targets remain VISITOR3 high/low, BUILDING2 high, WALKSTUF1 low/high, BUILDING2 low, and BUILDING4 low. |
| 2026-05-18 | `visitor3-low-readgroup-post-thirdseg-sweep` | Log-only; source restored. | Retest VISITOR3 low scalar grouped-appends after the accepted third setup segment changed the uncovered read shape. The planner now ranked several fireable `current-read-group-compatible` rows, so the binary question was whether any one-row table could convert the remaining `96.741%` low-tide CD debt into visible VBlank movement without more setup residency. | Temporarily added a VISITOR3-low read-group table and selected it only for low tide, then swept `{248,264,0}`, `{256,268,0}`, `{239,251,0}`, and `{231,243,0}` against the W1-high-promotion canary baseline. Artifacts: `scratch/ps1-perf-iterate/visitor3-low-rg248-264-vnext/20260518-080058-433807/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-rg256-268-vnext/20260518-080313-446742/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-rg239-251-vnext/20260518-080451-456331/summary.json`, and `scratch/ps1-perf-iterate/visitor3-low-rg231-243-vnext/20260518-080626-465719/summary.json`. | Failed/no promotion. Every row completed cleanly but stayed exact-flat at VISITOR3 low `1377/1074/1039`, overrun `35`, blocking/refill `85/0`, loop reads/read time `19/103`, due `15`, pack LBA/sectors `23379/760`, and PS-EXE bucket `233472`. The temporary table grew `foregroundPilotPlay` by `36` bytes and shifted following hot symbols by `+36` without moving a speed metric. | Close hand-authored VISITOR3-low read groups on the current post-third-segment baseline. The remaining low-tide debt needs generated deadline ownership, data-shape/no-decode relocation, or memory/clean-rect relief; scalar one-row grouped appends are inert despite the planner-visible candidates. Public rollup and methodology totals remain at the W1-high baseline. |
| 2026-05-18 | `visitor3-low-frame134-d4-gap` | Log-only; pack/source restored. | Test the no-extra-residency data-shape lane for VISITOR3 low after scalar read groups were inert. The layout has a `206..230` retained segment with a paid gap ending near frame `133`, and D4 can shrink terminal frame `134` enough to move it onto a sector boundary inside that gap while preserving pack size and LBA. | Temporarily encoded frame `134` against frame `133` as a `15435` byte D4 payload (`267` commands) and moved it to offset `458752` in `generated/ps1/foreground/VIST3LOW.FG2`; temporarily added low-tide frame `134` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/visitor3-low-frame134-d4-gap-vnext/summary.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-low-frame134-d4-gap-vnext/20260518-081252-502061/summary.json`. | Failed/no promotion. The moved D4 payload regressed VISITOR3 low from `1377/1074/1039` to `1385/1082/1039`, overrun `35 -> 43`, blocking `85 -> 91`, loop reads `19 -> 20`, due `15 -> 16`, while prefetch overrun stayed `0`. The codec scan also showed local-LZ is effectively inert for the terminal frames and only D4 saves meaningful bytes, so the read/decode phase trade is the blocker. | Close single-frame VISITOR3-low paid-gap D4 relocation on this baseline. Future data-shape work needs a cheaper/no-decode representation or generated deadline ownership; moving a 15 KiB D4 terminal frame into the gap costs more visible time than it saves. |
| 2026-05-18 | `building2-low-setupseg112-128` | `ps1: prime building2 low setup segment` | Take the next BUILDING2 low allocator-era setup-residency swing after the 24-sector `96..120` setup segment saved reads but regressed visible loop timing. The read plan showed a smaller `112..128` cluster with three modeled saved active reads, so the binary question was whether a 16-sector setup segment could cut active-loop CD debt without crossing the setup-cost/phase line. | Added a low-tide-only BUILDING2 setup segment at relative sectors `112..128` using the reusable CACHE setup-segment buffer. The rejected `96..120` probe artifact is `scratch/ps1-perf-iterate/building2-low-setupseg96-120-vnext/20260518-081613-521090/summary.json`. The accepted focused artifact is `scratch/ps1-perf-iterate/building2-low-setupseg112-128-vnext/20260518-081759-531324/summary.json`, and the under-green canary is `scratch/ps1-perf-iterate/building2-low-setupseg112-128-canaries/20260518-081945-541653/summary.json`. The source helper for read-plan generation now parses BUILDING2, BUILDING4, WALKSTUF1, FISHING3, and VISITOR3 setup-segment macros so the regenerated read-candidate matrix does not rank already resident ranges. | Promoted as a small active-loop CD/setup-residency win. BUILDING2 low pays setup cost `265 -> 275` VBlanks and setup reads `13 -> 14`, but keeps active loop flat at `1339` while target improves `1315 -> 1316`, overrun `24 -> 23`, blocking `54 -> 53`, loop reads/read time `37/152 -> 34/141`, and due `12 -> 11`; prefetch overrun stays `2`, pack LBA/sectors stay `6826/637`, and the PS-EXE bucket stays `233472`. BUILDING2 high, VISITOR3 high/low, WALKSTUF1 high/low, and BUILDING4 high/low stayed stable in the canary. | Keep `112..128` as the current BUILDING2 low retained setup segment. Public rollup improves to `+0.2794%` over target / `99.7255%` target speed; raw signed rollup is about `-0.4375%` / `100.4559%`; bands remain `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.12` public over-target points removed and `+12.63` public target-speed points gained since the compact full-matrix baseline. Next targets remain VISITOR3 high/low first, then BUILDING2 high, WALKSTUF1 low/high, BUILDING2 low, and BUILDING4 low. |
| 2026-05-18 | `walkstuf1-low-rg195-211` | Log-only; source restored. | Test the top regenerated post-BUILDING2-readplan W1-low candidate after the `238..342` setup-segment promotion. The row overlapped the existing early low read groups but started earlier at `195`, so the binary question was whether an additive `{195,211,0}` grouped append could pick up one more active read without disturbing the accepted retained setup segment. | Temporarily added `{195,211,0}` to `kWalkstuf1LowReadGroups12[]` and ran focused W1 low. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg195-211-vnext/20260518-085210-724584/summary.json`. The perf runner reported a baseline-label mismatch because the probe used a per-case baseline summary for `--baseline`, but the parsed JCPERF2 metrics were complete. | Failed/no promotion. Against the committed W1-low baseline `1813/1480/1442`, overrun `38`, blocking/refill `55/15`, loop reads `38`, and due `6`, the probe measured `1812/1479/1440`, overrun `39`, blocking/refill `62/15`, loop reads `39`, and due `7`; `group_hits=0`, so the additive row did not fire usefully and the row lost visible cadence. | Restore the accepted W1-low table. Do not retry `{195,211}` as a scalar additive row on this baseline; the new read-plan output instead points at smaller scheduler-owned candidates such as `91..107`, `155..167`, or `211..227`, but they need proper multi-case baseline gating and probably generated deadline ownership rather than another blind table append. |
| 2026-05-18 | `walkstuf1-low-rg91-107` | Log-only; source restored. | Follow the failed `{195,211}` additive row with a smaller earlier `91..107` candidate from the focused read plan. This range was modeled as a low-visible-risk 16-sector grouped append with two saved reads and no pack/layout movement, so the binary question was whether it would convert read-count movement into target-relative speed on the current `238..342` retained setup baseline. | Temporarily added `{91,107,0}` to `kWalkstuf1LowReadGroups12[]` and reran focused W1 low against the multi-case BUILDING2-low promotion canary baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg91-107-vnext/20260518-085533-743859/summary.json`. | Failed/no promotion. The gate correctly compared against W1-low `1813/1480/1442`, overrun `38`, blocking/refill `55/15`, reads `38`, due `6`. The probe measured `1814/1481/1439`, overrun `42`, blocking/refill `60/20`, reads `36`, due `5`; the read-count and due improvements were real, but visible loop/overrun/blocking/refill regressed and no key speed metric improved. Pack LBA and PS-EXE bucket stayed fixed. | Restore the accepted W1-low table. Close scalar additive early W1-low rows for now: even low-visible-risk candidates need generated deadline ownership or data-shape work because raw grouped appends are trading fewer reads for worse visible cadence. |
| 2026-05-18 | `building2-high-setupseg264-280` | Log-only; source restored. | Test the top regenerated BUILDING2-high post-setup-segment candidate as a retained setup slice rather than another additive read group. The accepted high row already owns `3..35` and `202..242`; the read plan ranked `264..280` as the next balanced 16-sector candidate, so the binary question was whether a third CACHE setup segment could pay that read before the active loop without crossing the B2-high clean-rect/refill phase line. | Temporarily added `FG_BUILDING2_HIGH_SETUP_SEGMENT3_START/BYTES=264/16` and extended the B2-high setup-segment allocation to read a third buffer. Focused artifact: `scratch/ps1-perf-iterate/building2-high-setupseg264-280-vnext/20260518-085920-765410/summary.json`. | Failed/no promotion. Against BUILDING2 high `1624/1351/1313`, overrun `38`, blocking/refill `50/14`, reads `47`, due `7`, the probe measured `1640/1357/1312`, overrun `45`, blocking/refill `51/20`, reads `47`, due `7`. Pack LBA and PS-EXE bucket stayed fixed, but setup residency shifted the active cadence negative and did not reduce active reads. | Restore the accepted two-segment B2-high setup shape. Do not add `264..280` as a third setup segment on this baseline; remaining B2-high work needs payload/dirty-work reduction or generated scheduler ownership, not more blind retained residency. |
| 2026-05-18 | `building2-high-rg271-287` | `ps1: add building2 high late read group` | Take the next BUILDING2-high scheduler-owned read-group candidate after setup-segment residency and earlier additive rows failed to move visible timing. The read plan showed a balanced late cluster that could fire under the existing allocator budget, so the binary question was whether a guarded row could lower B2-high loop pressure without disturbing the other under-green rows. | Added `{271,287,5}` to `kBuilding2HighReadGroups12[]`. Focused proof with the accepted refill tradeoff: `scratch/ps1-perf-iterate/building2-high-rg271-287-slack5-pass/20260518-094305-1011430/summary.json`; stricter focused proof before the explicit tradeoff gate: `scratch/ps1-perf-iterate/building2-high-rg271-287-slack5-vnext/20260518-092745-922611/summary.json`; six-case under-green canary excluding B2-high: `scratch/ps1-perf-iterate/building2-high-rg271-287-canaries/20260518-093317-954726/summary.json`. | Promoted as a small active-loop scheduler/read-group win. BUILDING2 high improves scene `1624 -> 1621`, active loop `1351 -> 1347`, overrun `38 -> 34`, blocking `50 -> 41`, read time `207 -> 203`, and due `7 -> 6`; target stays `1313`, loop reads stay `47`, and the accepted hidden refill tradeoff is `14 -> 16`. VISITOR3 high/low, BUILDING2 low, WALKSTUF1 high/low, and BUILDING4 low stayed exact-flat in canary. | Keep `271..287` with `minSlack=5` as the new B2-high baseline. Public rollup improves to `+0.2771%` over target / `99.7278%` target speed; raw signed rollup is about `-0.4398%` / `100.4582%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. This promotion adds about `0.0023` public over-target points removed and `+0.0023` public target-speed points, keeping the methodology total at about `17.12` and `+12.63` since the compact full-matrix baseline. |
| 2026-05-18 | `building2-high-rg249-261-stack` | Log-only; source restored to the accepted B2-high row set. | Test whether the next balanced BUILDING2-high read-cluster could stack on top of the accepted late scheduler row. The candidate `{249,261}` was still ranked near the top of the focused read-candidate matrix, so this closed the binary question before letting it drive the next pass. | Temporarily added `{249,261,0}` to `kBuilding2HighReadGroups12[]` alongside the accepted late row and ran a focused B2-high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-rg249-261-rg271-287-vnext/20260518-092931-932909/summary.json`. | Failed/no promotion. The stack regressed BUILDING2 high from `1624/1351/1313` to `1631/1357/1314`, overrun `38 -> 43`, blocking `50 -> 55`, and due `7 -> 8`; no key timing metric improved. | Restore the accepted late-row-only B2-high table. Do not retry `{249,261}` as a scalar stacked append on this baseline; if this cluster returns, it needs generated deadline ownership or a payload/data-shape change first. |
| 2026-05-18 | `building2-high-rg315-327` | `ps1: add building2 high late cluster read group` | Take the next remaining B2-high scheduler-owned cluster after `271..287` became the baseline. The matrix ranked `315..327` as the only remaining balanced/guarded B2-high candidate above the tight high-risk rows, so the binary question was whether it could lower active CD pressure without moving visible timing or any other under-green row. | Added `{315,327,0}` to `kBuilding2HighReadGroups12[]`. Focused proof: `scratch/ps1-perf-iterate/building2-high-rg315-327-vnext/20260518-100045-1109718/summary.json`; strict six-case under-green canary: `scratch/ps1-perf-iterate/building2-high-rg315-327-canaries/20260518-100237-1120561/summary.json`. | Promoted as a same-loop CD-pressure win. BUILDING2 high stays scene/loop/target `1621/1347/1313`, overrun `34`, and refill `16`, while blocking improves `41 -> 39`, loop reads `47 -> 45`, loop-read time `203 -> 199`, and due misses `6 -> 5`. VISITOR3 high/low, BUILDING2 low, WALKSTUF1 high/low, and BUILDING4 low stayed exact-flat in the canary. | Keep `315..327` as an additive B2-high row after `271..287`. Public rollup remains `+0.2771%` over target / `99.7278%` target speed because loop/target did not move; raw signed remains about `-0.4398%` / `100.4582%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology totals remain about `17.12` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `upload-gap8-bands` | `ps1: retune dirty upload band merge` | Retune the global dirty-upload vertical band clean-gap merge after scalar read groups saturated. The binary question was whether a wider row merge would reduce BUILDING4 low upload/rect pressure without hurting the other under-green rows. | Changed `GR_UPLOAD_BAND_MERGE_GAP` from `0` to `8`. Gap-8 canary artifact: `scratch/ps1-perf-iterate/upload-gap8-canaries/20260518-102731-1261476/summary.json`; BUILDING2 high focused refresh: `scratch/ps1-perf-iterate/upload-gap8-b2high/20260518-103925-1329185/summary.json`; rejected gap-11 probe: `scratch/ps1-perf-iterate/upload-gap11-b4low/20260518-103711-1316435/summary.json`. | Promoted as a BUILDING4 low active-loop/present-pressure win. BUILDING4 low improves `2853/2816 -> 2849/2816`, overrun `37 -> 33`, blocking/refill `42/35 -> 38/31`, and read time `223 -> 222`; loop reads/due stay `30/1`. VISITOR3 high/low, BUILDING2 high/low, and WALKSTUF1 high/low stayed key-flat. Gap `11` regressed BUILDING4 low by one VBlank versus gap `8`. | Keep gap `8` as the current dirty-upload merge. Public rollup improves to `+0.2760%` / `99.7289%`; raw signed rollup is `-0.4410%` / `100.4593%`; methodology adds about `0.0011` public over-target points removed and `+0.0011` speed points. Next targets: VISITOR3 high/low, WALKSTUF1 low/high, BUILDING2 high/low, then the final BUILDING4 low gap. |
| 2026-05-18 | `visitor3-high-window-retune-64-66-72` | Log-only; source restored to the accepted `68 KiB` high-tide VISITOR3 clean-relief window. | Close the cheap binary retune around the accepted VISITOR3 high clean-relief prefetch window after the gap-8 upload-band promotion. The question was whether the third-segment allocator-era shape moved the local optimum away from `68 KiB` enough to reduce the remaining `96.303%` row without data-shape work. | Temporarily changed `fgSceneCleanReliefWindowBytes("visitor3" high)` from `68 KiB` to `72 KiB`, then restored and tested `64 KiB` and `66 KiB` against the committed gap-8 baseline. Artifacts: `scratch/ps1-perf-iterate/visitor3-high-window72-current/20260518-105524-1418767/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-window64-current/20260518-105813-1434790/summary.json`, and `scratch/ps1-perf-iterate/visitor3-high-window66-current/20260518-105948-1444142/summary.json`. | Failed/no promotion. `72 KiB` regressed VISITOR3 high from `1082/1042`, overrun `40`, blocking/refill `50/5`, reads/due `6/3` to `1088/1042`, overrun `46`, blocking/refill `59/0`, reads/due `7/4`. `64 KiB` also measured `1088/1042`, overrun `46`, blocking/refill `58/0`, reads/due `6/4`. `66 KiB` regressed further to `1089/1042`, overrun `47`, blocking/refill `62/6`, reads/due `9/4`. | Keep the accepted `68 KiB` VISITOR3 high window. The remaining high-tide VISITOR3 debt is not a simple window-size knee; next work should target no-decode retained-gap relocation, generated scheduler/deadline ownership, or foreground/background ownership for upload-ready preprocessing. Public rollup remains `+0.2760%` / `99.7289%`; bands remain `119` green, `7` yellow, `0` orange, and `0` red. |
| 2026-05-18 | `upload-gap10-b4low` | Log-only; source restored to gap `8`. | Check the midpoint between the accepted B4-low gap `8` upload-band merge and the rejected gap `11` probe. Gap `11` lowered upload rectangle pressure but regressed visible timing, so the binary question was whether gap `10` could keep the timing win while reducing command count further. | Temporarily changed `GR_UPLOAD_BAND_MERGE_GAP` from `8` to `10` and ran focused BUILDING4 low against the accepted gap-8 canary. Artifact: `scratch/ps1-perf-iterate/upload-gap10-b4low-current/20260518-110351-1467307/summary.json`. | Failed/no promotion; no speed promotion. BUILDING4 low stayed exact-flat on key timing at `2849/2816`, overrun `33`, blocking/refill `38/31`, loop reads/read time `30/222`, and due `1`. The only work movement was mixed: upload rects `895 -> 890`, upload bytes `22791040 -> 22821760`, and dirty rows `35611 -> 35659`. | Keep gap `8`; gap `10` is a command-count/byte-volume trade, not a green-conversion speed win. Future BUILDING4 low work should move to static-mask/precomposed upload or restore/upload work elimination rather than widening the global band merge further. |
| 2026-05-18 | `building2-low-restore-minus-current-current` | Log-only; host-only byte-identical retest. | Reopen the large historical B2-low restore-minus-current idea against the allocator-era baseline before spending emulator time. Older runs showed a big visible improvement with hidden-refill debt; the current B2-low pack already carries later trim/setup work, so the first question was whether any same-frame cleanup-minus-current spans still exist. | Backed up `generated/ps1/foreground/BUIL2LOW.FG2` to `scratch/building2-low-restore-minus-current-current/BUIL2LOW.before.FG2`, then ran `scripts/compact-fgp3-restore-minus-current.py --in-place --pad-to-input-size`. The tool reported active payload `538534 -> 538534`, cleanup spans `52390 -> 52390`, cleanup pixels `231667 -> 231667`, and restore bytes `463334 -> 463334`; `cmp` confirmed the generated pack is byte-identical to the backup. | Failed/no runtime promotion path. There is no remaining generic same-frame cleanup-minus-current work in the current B2-low pack, so the historical visible/refill trade cannot be retested through the existing transformer. | Close generic B2-low restore-minus-current on this baseline. Future B2-low green conversion needs generated scheduler/refill ownership, upload/precompose work, or a different pack-side byte-shape change; do not rerun the current cleanup-minus-current transformer without a motion-aware or next-frame-aware extension. |
| 2026-05-18 | `walkstuf1-high-setupseg286-344` | `ps1: extend walkstuf1 high setup slice` | Try the smallest safe extension after the accepted W1-high `198..244 + 286..342` setup-slice retarget and the gap-8 renderer promotion. The binary question was whether paying two more mid-cluster sectors during setup could reduce active-loop CD/refill pressure without crossing the allocator clean-rect cliff or losing visible cadence. | Changed `FG_WALKSTUF1_HIGH_SETUP_SEGMENT2_BYTES` from `56` to `58` sectors, making segment2 `286..344`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-setupseg286-344-current/20260518-111109-1509132/summary.json`; under-green canary artifact: `scratch/ps1-perf-iterate/walkstuf1-high-setupseg286-344-canaries/20260518-111308-1520614/summary.json`. Rejected wider `286..346` probe at `scratch/ps1-perf-iterate/walkstuf1-high-setupseg286-346-current/20260518-110921-1498439/summary.json` reduced some CD pressure but regressed overrun to `35`, so it was not promoted. | Promoted as a same-overrun CD/refill pressure win. WALKSTUF1 high moves from `1472/1438` to `1475/1441`, keeping overrun flat at `34` while improving blocking `60 -> 59`, prefetch overrun `15 -> 13`, loop reads `48 -> 46`, and loop-read time `228 -> 212`; due stays `10`, pack LBA/sectors stay `24891/750`, and the PS-EXE bucket stays fixed. VISITOR3 high/low, WALKSTUF1 low, BUILDING2 high/low, and BUILDING4 low stayed stable in the canary. | Keep `286..344` as the current W1-high segment2 boundary. Public rollup remains `+0.2760%` over target / `99.7290%` target speed because the VBlank overrun did not move; raw signed remains about `-0.4410%` / `100.4593%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology totals remain about `17.12` public over-target points removed and `+12.63` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `walkstuf1-low-setupseg238-344-current` | Log-only; source restored to accepted `238..342`. | Test the smallest possible W1-low retained-setup edge beyond the accepted `238..342` boundary, mirroring the successful W1-high two-sector extension but avoiding the already known larger `238..346+` clean-pressure cliff. | Temporarily changed `FG_WALKSTUF1_LOW_SETUP_SEGMENT_BYTES` from `104` to `106` sectors, making the low retained setup segment `238..344`, then ran a focused W1-low gate against the latest under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg238-344-current/20260518-113619-1651972/summary.json`; headless log shows `JCBSOD-FATAL CACHE exhausted` immediately after allocating a `217088` byte `fgSetupSegmentBuffer` and then requesting a `97280` byte `grCleanRectPixels` allocation. | Failed/no promotion. The probe produced no `JCPERF2` metrics because CACHE exhausted before playback entered the measured loop: `memCacheUsed=491848`, `memTransientUsed=716240`, and the clean-rect allocation failed. PS-EXE bucket stayed `233472`, but the structural failure makes the candidate non-promotable. | Keep `238..342` as the W1-low CACHE-backed boundary. Do not extend W1-low retained setup bytes past `104` sectors without first freeing CACHE, moving this residency to a proven-safe allocator region, or reducing clean-rect/setup pressure; future W1-low work should use scheduler/data-shape/upload reductions rather than larger CACHE segments. |
| 2026-05-18 | `cache648-w1low-setupseg238-343/344` | Log-only; source restored to accepted allocator and W1-low boundary. | Follow the structural W1-low edge miss with an allocator-partition big swing: move `8 KiB` from TRANSIENT to CACHE (`640/768 -> 648/760`) and retest the one-sector and two-sector low setup edges. The binary question was whether the extra CACHE could make `238..343` or `238..344` measurable and safe without destabilizing the hidden refill budget. | Temporarily changed `MEM_CACHE_BUDGET` to `648 KiB`, `MEM_TRANSIENT_BUDGET` to `760 KiB`, then tested `FG_WALKSTUF1_LOW_SETUP_SEGMENT_BYTES=106` and `105`. Artifacts: two-sector `scratch/ps1-perf-iterate/cache648-w1low-setupseg238-344-focused/20260518-114116-1679988/summary.json`; one-sector `scratch/ps1-perf-iterate/cache648-w1low-setupseg238-343-focused/20260518-114558-1706973/summary.json`. | Failed/no promotion. The `238..344` rebalance made the edge measurable and improved visible loop/overrun/blocking from `1480/38/55` to `1479/36/42`, but hidden refill regressed `15 -> 20`, failing the zero-regression gate. The `238..343` rebalance was worse: scene/loop/target `1813/1480/1442 -> 1816/1483/1440`, overrun `38 -> 43`, blocking `55 -> 54`, refill `15 -> 19`, and due `6 -> 5`. | Restore `640 KiB CACHE / 768 KiB TRANSIENT` and the accepted `238..342` W1-low segment. Do not promote partition rebalance for W1-low edges; the visible CD win turns into hidden refill debt or timing regression. Future W1-low work should first reduce clean-rect/setup pressure or add generated deadline/refill ownership. |
| 2026-05-18 | `visitor3-high-cleanrelief-setupprime-sweep` | Log-only; source restored to accepted high clean-relief setup-prime suppression. | Test a big allocator-era VISITOR3 high swing: let high tide keep some setup-prime residency even when clean-memory relief shrinks its active window to `68 KiB`. The binary question was whether the new allocator budget could pay a retained early pack window and remove the remaining high-side active CD debt. | Temporarily computed the VISITOR3 high setup-prime size before applying clean-relief window shrink and exempted high tide from the clean-relief setup-prime suppression. Swept high prime sizes `320`, `304`, `240`, `208`, `128`, and `96 KiB`. Artifacts: no-op control `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime-current/20260518-115407-1752906/summary.json`; full active prime `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime-active/20260518-115605-1764520/summary.json`; `304 KiB` `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime304-current/20260518-115844-1779760/summary.json`; `240 KiB` `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime240-current/20260518-120148-1797290/summary.json`; `208 KiB` `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime208-current/20260518-120425-1812440/summary.json`; `128 KiB` `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime128-current/20260518-120716-1828710/summary.json`; `96 KiB` `scratch/ps1-perf-iterate/visitor3-high-cleanrelief-setupprime96-current/20260518-121008-1845157/summary.json`. | Failed/no promotion. The active `320/304/240/208/128 KiB` variants all failed before JCPERF2 by exhausting CACHE during clean-rect allocation after the enlarged `fg-stream-window`; the 128 KiB version made it through the baseline four clean chunks but failed on the next `59584` byte chunk. The only measurable candidate, `96 KiB`, completed but regressed VISITOR3 high from `1395/1082/1042`, overrun `40`, blocking/refill `50/5`, reads/due `6/3` to `1410/1091/1041`, overrun `50`, blocking/refill `63/0`, reads/due `4/4`. | Keep clean-relief setup-prime suppression for VISITOR3 high. The allocator-era budget still cannot coexist with useful high setup-prime residency and the one small measurable prime is phase-negative. Future VISITOR3 high work should target no-decode pack layout, clean-rect memory reduction, or generated CD/deadline ownership, not scalar high setup-prime restoration. |
| 2026-05-18 | `visitor3-high-cleanrect64-prime128` | Log-only; source restored to accepted clean-rect striping and high clean-relief setup-prime suppression. | Follow the setup-prime sweep with a clean-rect memory-shape probe. The `128 KiB` high-prime variant reached the baseline clean chunks before failing on the next clean-rect allocation, so this tested whether splitting clean rectangles to `64 KiB` chunks could make a smaller high setup-prime coexist with VISITOR3 high clean-memory relief. | Temporarily changed the VISITOR3 high setup-prime cap to `128 KiB`, computed setup-prime before applying the `68 KiB` clean-relief stream window, exempted high tide from clean-relief setup-prime suppression, and reduced `fgBackdropSaveCleanBgRectsForPack()` clean-rect chunking from `96 KiB` to `64 KiB`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-cleanrect64-prime128-current/20260518-121508-1873773/summary.json`. | Failed/no promotion. The probe still failed before JCPERF2 with `JCBSOD-FATAL CACHE exhausted`: after allocating a `131072` byte `fg-stream-window`, clean-rect chunks `64448` x4, `60800`, and `48640` were allocated, then another `48640` byte `grCleanRectPixels` request failed with `memCacheUsed=593408` and `memTransientUsed=773124`. | Close scalar clean-rect striping plus high setup-prime restoration for VISITOR3 high. Smaller clean-rect chunks avoid the earlier single large-chunk cliff but increase allocation count/fragmentation and still cannot coexist with useful high setup-prime residency. Future VISITOR3 high work should reduce clean-rect bytes, move ownership out of CACHE, or change pack/scheduler ownership rather than retrying chunk-size plus prime combinations. |
| 2026-05-18 | `visitor3-high-early-direct-rawgap-sweep` | Log-only; source and `VISITOR3.FG2` restored, PS1 rebuilt. | Probe the remaining VISITOR3 high early direct-stage cluster after terminal retained setup was saturated. The current CD log showed fragmented reads around relative sectors `34..37`; the binary question was whether direct-stage grouping or no-decode relocation of tiny frames `56..58` into the already-read `228..262` retained gap could remove reads without new setup-prime/CACHE pressure. | Tried source-only no-direct for frame `56`, no-direct for frame `57`, a 3-sector direct-stage group `34..37`, pack-only raw moves of frame `57`, frame `56`, frames `56+57`, and frames `56+57+58`, plus a frame56 raw move with high window slack raised from `4` to `5`. Artifacts: `scratch/ps1-perf-iterate/visitor3-high-nodirect-frame56-current/20260518-122411-1924624/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-nodirect-frame57-current/20260518-122601-1935539/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-directgroup34-37-current/20260518-122824-1949375/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-frame57-rawgap-current/20260518-123054-1963881/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-current/20260518-123240-1974446/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-frame56-58-rawgap-current/20260518-123441-1986270/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-frame56-rawgap-current/20260518-123627-1996807/summary.json`; `scratch/ps1-perf-iterate/visitor3-high-frame56-rawgap-slack5-current/20260518-123824-2008273/summary.json`. Transform summaries are under `scratch/visitor3-high-frame57-rawgap-current/`. | Failed/no promotion. The best visible shapes were near misses: no-direct frame56/frame57 improved loop `1082 -> 1077`, overrun `40 -> 34`, blocking `50 -> 43`, and due `3 -> 2`, but regressed refill overrun `5 -> 16`; directgroup and single-frame raw moves improved to `1080/1043`, overrun `37`, reads `5`, but still regressed refill `5 -> 6`; moving frames `56+57` improved reads to `4`, loop `1079`, blocking `47`, but still refill `6`; moving frame58 as well removed refill debt but regressed loop/blocking to `1084/56`; the slack5 guard regressed timing outright. | Close the early `34..37` cluster under scalar direct-stage and raw retained-gap relocation. The read-save signal is real, but every strict-safe form either adds hidden refill or moves visible blocking the wrong way. Reopen only with generated deadline ownership that can schedule this cluster before the unsafe slack slot, or with a no-decode data shape that preserves the original safe refill boundary. |
| 2026-05-18 | `visitor3-high-retained-gap-scheduler-followup` | Log-only; source and `VISITOR3.FG2` restored. | Follow the early `34..37` near miss with larger existing-gap data-shape and scheduler-side variants. The retained high-tide segment `228..262` has a paid gap at offsets `501526..518354`; frames `56..63` fit exactly, and the previous `56+57` move was only one hidden-refill VBlank away from strict promotion. | Temporarily moved frames `56..63` into the retained gap, then restored and retested the narrower `56+57` raw-gap move with a frame-58 direct-stage hint, VISITOR3 high window slack `5`, and VISITOR3 high window slack `9`. Artifacts: full-gap transform `scratch/visitor3-high-frames56-63-rawgap-vnext/summary.json`; full-gap focused gate `scratch/ps1-perf-iterate/visitor3-high-frames56-63-rawgap-vnext/20260518-142048-2587545/summary.json`; direct58 focused gate `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-direct58-vnext/20260518-142258-2600024/summary.json`; slack5 focused gate `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-slack5-vnext/20260518-142458-2611788/summary.json`; slack9 focused gate `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-slack9-vnext/20260518-142645-2622237/summary.json`. | Failed/no promotion. Moving frames `56..63` removed hidden-refill pressure (`5 -> 1`) but regressed visible timing to `1085/1041`, overrun `44`, blocking `57`, and due `4`. The direct58 and slack5 variants stayed identical to the earlier `56+57` near miss (`1079/1043`, overrun `36`, blocking `47`, refill `6`). Slack9 removed refill debt (`5 -> 0`) but moved it into visible blocking (`50 -> 56`) and loop `1084/1045`. | Close existing-gap VISITOR3 high frames `56..63` and scalar slack/direct-stage followups. The remaining high-tide gap needs generated deadline ownership that can schedule the early cluster in an earlier safe slot, or a pack format/work reduction that does not move the downstream window boundary. |
| 2026-05-18 | `visitor3-low-frame138-rawgap-seg206-232` | `ps1: move visitor3 low frame138 into retained gap` | Take the VISITOR3 low no-decode data-shape lane after scalar read groups, large residency, and D4 paid-gap movement were closed. The low pack had a paid gap at offsets `457772..474769`, and frame `138`'s existing raw `16888` byte payload fit if the third low setup segment was extended by only two sectors from `206..230` to `206..232`. | First tried a smaller no-decode relocation of frames `111..117`, `132`, and `137` into the retained gap. That reduced loop reads `19 -> 18` but was exact-flat on scene/loop/target `1377/1074/1039`, overrun `35`, blocking `85`, and refill `0`, so it was rejected; artifacts: transform summary `scratch/visitor3-low-early98-105-rawgap-vnext/summary.json` and focused gate `scratch/ps1-perf-iterate/visitor3-low-early98-105-rawgap-vnext/20260518-124632-2053994/summary.json`. The promoted variant moves frame `138` raw to offset `457772` in `generated/ps1/foreground/VIST3LOW.FG2` and changes `FG_VISITOR3_LOW_SETUP_SEGMENT3_BYTES` from `24` to `26` sectors. Transform summary: `scratch/visitor3-low-frame138-rawgap-seg206-232-vnext/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-low-frame138-rawgap-seg206-232-vnext/20260518-124907-2068964/summary.json`; five-case canary: `scratch/ps1-perf-iterate/visitor3-low-frame138-rawgap-seg206-232-canaries/20260518-125107-2080541/summary.json`; W1-high canary: `scratch/ps1-perf-iterate/visitor3-low-frame138-rawgap-seg206-232-walkstuf1-high-canary/20260518-125910-2126182/summary.json`. | Promoted as a no-decode active-loop speed win. VISITOR3 low improves scene `1377 -> 1369`, active loop/target `1074/1039 -> 1065/1039`, overrun `35 -> 26`, blocking/read time `85/103 -> 75/97`, loop reads `19 -> 18`, and due misses `15 -> 14`; hidden refill stays `0`, pack LBA/sectors stay `23379/760`, and the PS-EXE bucket stays `233472`. VISITOR3 high, WALKSTUF1 high/low, BUILDING2 high/low, and BUILDING4 low stayed exact-flat in canaries. | Keep frame `138` raw in the paid gap and keep the low third setup segment at `206..232`. Public rollup improves to `+0.2690%` over target / `99.7354%` target speed; raw signed rollup is about `-0.4479%` / `100.4658%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.13` public over-target points removed and `+12.64` target-speed points gained since the compact full-matrix baseline. Next targets remain VISITOR3 high first, then WALKSTUF1 low, BUILDING2 high, VISITOR3 low, WALKSTUF1 high, BUILDING2 low, and BUILDING4 low. |
| 2026-05-18 | `w1low-clean48-runtimecap-setup238-344` | `ps1: split walkstuf1 low clean rects for setup edge` | Reopen the W1-low `238..344` setup edge after the prior direct probe failed on clean-rect allocation and the allocator rebalance probe traded visible wins for hidden refill debt. The binary question was whether reducing clean-rect chunk size only for W1 low would free enough CACHE headroom to keep the two extra retained sectors without destabilizing W1 high or the other under-green rows. | First tested a global 48 KiB clean-rect cap plus `FG_WALKSTUF1_LOW_SETUP_SEGMENT_BYTES=106`: W1-low improved, but wide PAL4 canaries hit `JCBSOD-FATAL clean-rect alloc failed (TRANSIENT budget shortfall)`. A scene-wide WALKSTUF1 cap then regressed W1 high (`1475 -> 1481`, overrun `34 -> 43`, blocking `59 -> 63`). The promoted implementation stores the clean-rect cap in a runtime variable reset to `96 KiB` and sets it to `48 KiB` only while priming W1 low. Focused artifact: `scratch/ps1-perf-iterate/w1low-clean48-runtimecap-setup238-344-focused/20260518-134826-2404755/summary.json`; W1-high canary: `scratch/ps1-perf-iterate/w1low-clean48-runtimecap-setup238-344-w1high-canary/20260518-135014-2415120/summary.json`; five-case under-green canary: `scratch/ps1-perf-iterate/w1low-clean48-runtimecap-setup238-344-canaries/20260518-135202-2425616/summary.json`; VISITOR3-low canary: `scratch/ps1-perf-iterate/w1low-clean48-runtimecap-setup238-344-visitor3low-canary/20260518-140437-2497177/summary.json`. | Promoted as an active-loop speed win. WALKSTUF1 low improves scene `1813 -> 1809`, active loop/target `1480/1442 -> 1475/1443`, overrun `38 -> 32`, blocking/read time `55/211 -> 48/200`, prefetch overrun `15 -> 12`, and due stays `6`; loop reads move `38 -> 39`. W1 high, VISITOR3 high/low, BUILDING2 high/low, and BUILDING4 low stayed stable, pack LBAs/sectors stayed fixed, and the PS-EXE bucket stayed `233472`. | Keep W1 low at retained setup `238..344` and keep clean-rect chunking at `48 KiB` only after W1-low setup priming; all other rows reset to the previous `96 KiB` cap. Public rollup improves to `+0.2657%` over target / `99.7386%` target speed; raw signed rollup is about `-0.4512%` / `100.4689%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total remains about `17.13` public over-target points removed and `+12.64` target-speed points gained since the compact full-matrix baseline. Next target remains VISITOR3 high first, then BUILDING2 high, VISITOR3 low, WALKSTUF1 high/low, BUILDING2 low, and BUILDING4 low. |
| 2026-05-18 | `building2-high-broad-setupseg-sweep` | Log-only; source restored. | Reopen BUILDING2 high setup ownership as a big allocator-era swing. The current baseline owns setup sectors `3..35` and `202..242`; the read plan still shows hot active-loop pressure before that second segment. The binary question was whether moving the second segment earlier could recover a larger setup-owned win without clean-rect/CACHE failure. | Swept broad and narrowed second-segment boundaries while preserving the first `3..35` segment. `86..242` in TRANSIENT failed before `JCPERF2` with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=385024 have=165016`; artifact `scratch/ps1-perf-iterate/building2-high-setupseg3-35-86-242-transient-vnext/20260518-143609-2675011/summary.json`. CACHE `176..242` and `179..242` also failed before timing during clean-rect allocation; artifacts `scratch/ps1-perf-iterate/building2-high-setupseg3-35-176-242-vnext/20260518-143856-2690774/summary.json` and `scratch/ps1-perf-iterate/building2-high-setupseg3-35-179-242-vnext/20260518-144125-2704964/summary.json`. Feasible `184..242` and `194..242` completed and were gated at `scratch/ps1-perf-iterate/building2-high-setupseg3-35-184-242-vnext/20260518-144358-2719547/summary.json` and `scratch/ps1-perf-iterate/building2-high-setupseg3-35-194-242-vnext/20260518-144552-2730622/summary.json`. | Failed/no promotion. `184..242` reduced loop reads `45 -> 41` but regressed scene/loop/target `1621/1347/1313 -> 1632/1352/1311`, overrun `34 -> 41`, blocking/refill `39/16 -> 44/19`, and due stayed `5`. `194..242` was similar: reads `45 -> 43`, but scene/loop/target `1621/1347/1313 -> 1628/1352/1310`, overrun `34 -> 42`, blocking/refill `39/16 -> 44/20`. Pack LBA and PS-EXE bucket stayed fixed when timing completed. | Restore the accepted `202..242` second segment. Do not retry B2-high broad setup-segment ownership by only moving the boundary earlier; allocator-feasible variants trade read-count wins for worse visible cadence/refill, while broad variants exceed CACHE/TRANSIENT headroom. Future B2-high work should use generated deadline ownership or pack/upload work reduction, not larger setup residency. |
| 2026-05-18 | `visitor3-high-scheduler-minslack-sweep` | Log-only; source restored. | Close the remaining scalar VISITOR3 high scheduler knobs after the read plan showed all active reads already covered by setup/window ownership. The binary question was whether prepare-before-window or a tighter/looser high-tide refill slack threshold could recover the visible-blocking wins seen in earlier raw-gap probes without carrying hidden refill debt. | Temporarily added a VISITOR3 high prepare-before-window branch mirroring the accepted W1-low scheduler rule, then restored and swept `FG_VISITOR3_HIGH_WINDOW_MIN_SLACK_VBLANKS` from the accepted `4` to `3` and `5`, plus the combined `3 + prepare-first` variant. Artifacts: prepare-first `scratch/ps1-perf-iterate/visitor3-high-prepare-first-vnext/20260518-145344-2774728/summary.json`; min-slack-3 `scratch/ps1-perf-iterate/visitor3-high-minslack3-vnext/20260518-145555-2787283/summary.json`; min-slack-5 `scratch/ps1-perf-iterate/visitor3-high-minslack5-vnext/20260518-145733-2796846/summary.json`; combined `scratch/ps1-perf-iterate/visitor3-high-minslack3-prepare-first-vnext/20260518-145919-2807135/summary.json`. | Failed/no promotion. Prepare-first at the accepted slack was exact-flat at `1395/1082/1042`, overrun `40`, blocking/refill `50/5`, reads/due `6/3`, while growing hot text by `228` bytes. Min-slack `3` saved reads `6 -> 4`, improved blocking `50 -> 45`, and due `3 -> 1`, but regressed scene/loop `1395/1082 -> 1396/1083` and hidden refill `5 -> 31`; adding prepare-first produced the same metrics plus the hot-code growth. Min-slack `5` also failed at `1396/1083/1044`, blocking/refill `53/9`, reads/due `4/3`. | Keep VISITOR3 high min slack at `4` and do not add a scalar prepare-first branch. The read-count signal is real, but scalar scheduler knobs only trade visible blocking for hidden refill or exact-flat code drift. Reopen VISITOR3 high scheduling only with generated per-read/deadline ownership that proves nonzero owner hits and no refill increase before PS1 timing. |
| 2026-05-18 | `walkstuf1-low-rg91-107-post-clean48` | `ps1: add walkstuf1 low first boundary read group` | Retest the earlier rejected `{91,107}` W1-low additive read group after the low-only 48 KiB clean-rect cap and `238..344` setup-edge promotion changed allocator/cache pressure. The prior attempt failed on the older `238..342` baseline, so the binary question was whether the same boundary read could now convert the read-count win into visible timing without hidden refill debt. | Added `{91,107,0}` after the existing `{78,91,0}` row in `kWalkstuf1LowReadGroups12[]`. Focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-rg91-107-post-clean48-vnext/20260518-150229-2825275/summary.json`; five-case no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-rg91-107-post-clean48-canaries-noregress/20260518-151229-2883104/summary.json`; W1-high canary: `scratch/ps1-perf-iterate/walkstuf1-low-rg91-107-w1high-canary/20260518-152051-2931418/summary.json`; VISITOR3-low canary: `scratch/ps1-perf-iterate/walkstuf1-low-rg91-107-visitor3low-canary/20260518-152240-2942030/summary.json`. | Promoted as an active-loop speed/CD-pressure win. WALKSTUF1 low improves scene `1809 -> 1807`, active loop/target `1475/1443 -> 1473/1444`, overrun `32 -> 29`, blocking/read time `48/200 -> 43/195`, prefetch overrun `12 -> 11`, loop reads `39 -> 36`, and due misses `6 -> 5`. W1 high, VISITOR3 high/low, BUILDING2 high/low, and BUILDING4 low stayed stable in canaries, with pack LBA/sectors fixed at `25641/750`. | Keep `{91,107,0}` as the first W1-low post-boundary read after `{78,91,0}`. Public rollup improves to `+0.2640%` over target / `99.7402%` target speed; raw signed rollup is about `-0.4529%` / `100.4705%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.14` public over-target points removed and `+12.64` target-speed points gained since the compact full-matrix baseline. Next target remains VISITOR3 high first, then BUILDING2 high, VISITOR3 low, WALKSTUF1 high/low, BUILDING2 low, and BUILDING4 low. |
| 2026-05-18 | `walkstuf1-high-readgroup-sweep-post-w1low91` | Log-only; source restored. | Test whether the regenerated W1-high scheduler-owned read-group queue can convert the row after the W1-low `{91,107}` promotion. VISITOR3 high has no standalone read candidates left, so this was the next largest modeled scalar read swing: `84..108` advertised four saved reads, while `164..188` advertised three saved reads with cleaner visible-gap scoring. | Temporarily added W1-high rows `{84,108,0}`, `{84,108,4}`, `{80,92,0}`, and `{164,188,0}` in separate focused gates against the current W1-high baseline. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-high-rg84-108-vnext/20260518-160749-3189948/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-high-rg84-108-slack4-vnext/20260518-160943-3200969/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-high-rg80-92-vnext/20260518-161134-3211699/summary.json`; `scratch/ps1-perf-iterate/walkstuf1-high-rg164-188-vnext/20260518-161329-3222854/summary.json`. | Failed/no promotion. `{84,108,0}` had a useful CD signal, improving blocking `59 -> 56`, refill `13 -> 12`, reads `46 -> 42`, and due `10 -> 8`, but regressed loop/overrun `1475/34 -> 1477/36`. Slack `4` suppressed the regression but was exact-flat with no key metric win. `{80,92,0}` regressed target/overrun/blocking/refill to `1438/37/66/18` and reads only `46 -> 45`. `{164,188,0}` reduced reads `46 -> 44`, but regressed target/overrun/blocking to `1440/35/66`. | Restore the accepted W1-high table. Close scalar W1-high `84..108`, `80..92`, and `164..188` after the W1-low `{91,107}` promotion; the read-save signal is real but still phase-negative without generated deadline/refill ownership. Next W1-high work should require scheduler-owned firing proof or target render/upload work, not another raw grouped append. |
| 2026-05-18 | `walkstuf1-high-rg156-180-post-w1low91` | Log-only; source restored. | Retest the regenerated top W1-high candidate after the first post-W1-low scalar sweep. The matrix ranked `156..180` as a 24-sector scheduler-owned-only row with a modeled three-read saving, so this was the next binary check before leaving W1-high scalar grouped appends. | Temporarily added `{156,180,0}` after `{78,91,0}` in `kWalkstuf1HighReadGroups12[]` and ran focused W1 high against the current W1-high canary baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg156-180-vnext/20260518-161741-3246714/summary.json`. | Failed/no promotion. The row reduced loop reads `46 -> 44` and due misses `10 -> 9`, but lost visible timing and refill budget: target/overrun/blocking/refill moved `1441/34/59/13 -> 1438/37/62/22`; loop stayed `1475`, so the target regression increased overrun by three VBlanks. | Restore the accepted W1-high table. Close `156..180` on the current post-W1-low baseline; W1-high scalar grouped appends are now exhausted as blind source-table probes until generated deadline/refill ownership can prove the saved reads land in safe slots. |
| 2026-05-18 | `building2-high-rg122-146-post-w1low91` | Log-only; source restored. | Shift to BUILDING2 high after closing the current W1-high blind scalar lane. The refreshed candidate matrix ranked `122..146` as a four-read high-risk row, making it the biggest remaining simple B2-high binary candidate after the accepted `{271,287,5}` and `{315,327,0}` rows. | Temporarily added `{122,146,0}` to `kBuilding2HighReadGroups12[]` and ran focused B2 high against the current under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg122-146-vnext/20260518-162313-3278027/summary.json`. | Failed/no promotion. The row did not fire usefully: `group_hits=0`, and timing/CD metrics were exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, loop reads `45`, read time `199`, and due `5`. | Restore the accepted B2-high table. Close `122..146` as an inert scalar append on this baseline; if this early cluster returns, it needs generated append-start/deadline ownership rather than another hand-authored row. |
| 2026-05-18 | `building2-high-rg95-119-post-w1low91` | Log-only; source restored. | Test whether the next B2-high four-read early candidate can fire by chaining directly after the accepted `{83,95}` row. The read matrix ranked `95..119` immediately after the inert `122..146`, and the binary question was whether matching the accepted row end would create runtime group hits. | Temporarily added `{95,119,0}` after `{83,95,0}` in `kBuilding2HighReadGroups12[]` and ran focused B2 high against the current under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg95-119-vnext/20260518-162616-3295536/summary.json`. | Failed/no promotion. The row also produced `group_hits=0` and exact-flat metrics: scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, loop reads `45`, read time `199`, and due `5`. | Restore the accepted B2-high table. Close `95..119` as an inert scalar append. Future B2-high early-cluster work should first prove runtime append-start hits from generated metadata/logging rather than assuming planner `fire=yes` rows will fire. |
| 2026-05-18 | `building2-high-rg255-271-post-w1low91` | Log-only; source restored. | Test the late B2-high candidate adjacent to the accepted `{271,287,5}` row after two early candidates proved inert. The matrix ranked `255..271` as a two-read current-window-compatible row, so the binary question was whether the late cluster had real runtime append hits. | Temporarily added `{255,271,0}` before `{271,287,5}` in `kBuilding2HighReadGroups12[]` and ran focused B2 high against the current under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg255-271-vnext/20260518-162927-3313663/summary.json`. | Failed/no promotion. The row also produced `group_hits=0` and exact-flat metrics: scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, loop reads `45`, read time `199`, and due `5`. | Restore the accepted B2-high table. Close `255..271` as another inert scalar append; current B2-high read-plan rows need append-start instrumentation/generated ownership before more hand table probes. |
| 2026-05-18 | `building4-low-rg274-298-route` | Log-only; source restored. | Try a non-B2 scalar route by adding BUILDING4-low read-group support and the best remaining low-visible-risk B4-low row. BUILDING4 low has no existing read-group table, so this tested whether enabling group capacity for its 32 KiB stream window plus `{274,298,0}` could collapse the two-read late cluster. | Temporarily added `kBuilding4LowReadGroups12[] = { {274,298,0} }` and routed low-tide `building4` through it, then ran focused B4 low. Artifact: `scratch/ps1-perf-iterate/building4-low-rg274-298-vnext/20260518-163348-3338542/summary.json`. | Failed/no promotion. The probe was exact-flat at scene/loop/target `3121/2849/2816`, overrun `33`, blocking/refill `38/31`, loop reads/read time `30/222`, and due `1`, with `group_hits=0`; it also grew `foregroundPilotPlay` by `20` bytes and shifted later hot symbols by `+20`. | Restore the accepted B4-low route. Close `{274,298}` as a hand-authored B4-low group-route row; B4-low needs generated append-start ownership, static upload/restore elimination, or a data-shape change instead. |
| 2026-05-18 | `clean-no-full-first-upload-visitor3-high` | Log-only; source restored. | VISITOR3 high still has a frame with max upload `614400` bytes and a remaining `40` VBlank overrun, so this binary probe tested whether the first-frame clean-baseline dirty policy was forcing an avoidable full upload. | First removed the full-dirty mark from `grSaveCleanBgRects()`, then also removed it from `grSaveCleanBgTiles()`, and ran focused VISITOR3 high after each change. Artifacts: `scratch/ps1-perf-iterate/cleanrect-no-full-first-upload-visitor3-high-vnext/20260518-163900-3367927/summary.json` and `scratch/ps1-perf-iterate/clean-no-full-first-upload-visitor3-high-vnext/20260518-164046-3378263/summary.json`. | Failed/no promotion. Both probes were exact-flat at `1395/1082/1042`, overrun `40`, blocking/refill `50/5`, loop reads `6`, due `3`, and max upload `614400`; removing both marks only shifted code addresses. | Restore the full first-frame dirty marks in both clean-save paths. Do not weaken the clean-baseline invariant for speed; future VISITOR3 high work should inspect the actual upload producer/static upload mask or continue with data-shape/window scheduling rather than toggling clean-save dirty marks. |
| 2026-05-18 | `visitor3-low-cleanrelief-readplan-and-rg-sweep` | Log-only; source route restored; tooling retained. | The read-candidate matrix incorrectly counted VISITOR3 setup-prime coverage even though runtime clean-memory relief forces VISITOR3 setup-prime bytes to zero. After correcting that analysis, the matrix exposed low-tide VISITOR3 grouped-append candidates `9..33`, `9..25`, and `92..108`; the binary question was whether any of those low-risk rows could convert actual active reads into speed. | Updated `scripts/ps1-foreground-read-plan.py` so VISITOR3 high/low clean-relief reports zero setup-prime bytes while keeping targeted setup segments. Regenerated `docs/ps1/performance-read-candidate-matrix.{md,csv}` from `scratch/current-under-green-candidates-20260518-w1low91-107`. Temporarily routed VISITOR3 low read groups and tested `{9,33,0}`, `{9,25,0}`, and `{92,108,0}`. Artifacts: `scratch/ps1-perf-iterate/visitor3-low-rg9-33-vnext/20260518-164901-3424623/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-rg9-25-vnext/20260518-165046-3434768/summary.json`, and `scratch/ps1-perf-iterate/visitor3-low-rg92-108-vnext/20260518-165221-3444117/summary.json`. | Failed/no promotion; no speed promotion. `{9,33}` changed cadence but regressed scene/loop/blocking `1369/1065/75 -> 1371/1067/76` with overrun flat at `26`. `{9,25}` saved one loop read but regressed to `1374/1070/1039`, overrun `31`, blocking `79`. `{92,108}` also saved one loop read but regressed to `1371/1067/1041`, blocking `76`, overrun flat at `26`. | Keep the read-plan clean-relief correction and the regenerated matrix; restore the VISITOR3 low read-group route. Low-tide grouped appends are phase-negative even when the corrected matrix labels them balanced, so the next VISITOR3 low work should be pack/data-shape or setup residency that changes read placement without creating visible cadence drift. |
| 2026-05-18 | `visitor3-high-early34-40-residency-sweep` | Log-only; source restored and PS1 rebuilt. | After the corrected read-plan showed VISITOR3 high's earliest uncovered cluster at relative sectors `34..40`, test both sides of the binary: pay it during setup, or let a live grouped append own the exact fireable start. | First swapped the existing terminal setup segment `277..293` to `34..40`; then restored the terminal segment and added a fourth high-only setup segment `34..40` read last during setup; finally restored setup code and tried a live `{34,40,0}` VISITOR3-high read group. Artifacts: `scratch/ps1-perf-iterate/visitor3-high-seg34-40-swap-vnext/20260518-170009-3488336/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-seg34-40-fourth-vnext/20260518-170326-3507260/summary.json`, and `scratch/ps1-perf-iterate/visitor3-high-rg34-40-vnext/20260518-170620-3523800/summary.json`. | Failed/no promotion. The terminal-swap setup form lost too much late residency and regressed to `1422/1116/1042`, overrun `74`, blocking `91`, and loop reads `9`. The additive fourth setup segment was structurally safe but still regressed cadence to `1410/1092/1040`, overrun `52`, blocking `64`, and loop reads `11`. The live `{34,40}` read group was exact-flat at the accepted `1395/1082/1042`, overrun `40`, blocking/refill `50/5`, loop reads `6`, and due misses `3`. | Restore the accepted three VISITOR3 high setup segments and no VISITOR3 high read-group route. The early `34..40` cluster is now closed as terminal-swap setup, additive setup residency, and hand-authored grouped append; reopening it needs generated deadline ownership or pack/data-shape work that does not perturb the downstream window boundary. |
| 2026-05-18 | `visitor3-high-frame56-57-rawgap-tight64` | `ps1: tighten visitor3 high raw-gap refill` | Convert the previous VISITOR3 high frame56/57 raw-gap near miss into a strict promotion. The prior layout saved visible reads but raised hidden refill `5 -> 6`; the binary question was whether keeping the raw-gap data shape and capping only tight-slack speculative refills could keep the visible gain without extra hidden debt. | Restored the frame56/57 raw relocation into the retained high-tide `228..262` gap, swept VISITOR3 high min-slack `6`, `7`, and `8`, then added a high-only tight-window cap. The `32 KiB` cap was too small; the accepted cap is `64 KiB`. Artifacts: slack failures `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-slack6-vnext/20260518-171602-3578793/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-slack7-vnext/20260518-171741-3588558/summary.json`, and `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-slack8-vnext/20260518-171919-3598099/summary.json`; tight32 failure `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-tight32-vnext/20260518-172359-3624616/summary.json`; focused proof `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-tight64-vnext/20260518-172551-3635538/summary.json`; stale-baseline eight-case guard `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-tight64-canaries/20260518-172833-3650847/summary.json`; current-baseline no-regress gate `scratch/ps1-perf-iterate/visitor3-high-frame56-57-rawgap-tight64-currentbaseline/20260518-174143-3725529/summary.json`; transform summary `scratch/visitor3-high-frame57-rawgap-current/summary-frame56-57.json`. | Promoted as an active-loop CD-pressure win. VISITOR3 high improves scene `1395 -> 1392`, active loop/target `1082/1042 -> 1079/1043`, overrun `40 -> 36`, blocking/read time `50/62 -> 49/57`, loop reads `6 -> 4`, and due stays `3`; hidden refill stays `5`, pack LBA/sectors stay `22619/760`, and pack bytes stay `1555450`. BUILDING2 high/low, BUILDING4 low, and WALKSTUF1 low stayed exact-flat in the current-baseline no-regress gate; the eight-case guard only failed an older stale BUILDING2-high baseline that expected prefetch `14` instead of the accepted `16`. | Keep frames `56` and `57` raw in the retained high gap and keep the VISITOR3 high tight-slack speculative refill cap at `64 KiB`. Public rollup improves to `+0.2610%` over target / `99.7431%` target speed; raw signed rollup is about `-0.4560%` / `100.4734%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total remains about `17.14` public over-target points removed and `+12.64` target-speed points gained since the compact full-matrix baseline. Next targets remain BUILDING2 high, VISITOR3 low, WALKSTUF1 high/low, BUILDING2 low, BUILDING4 low, and any further VISITOR3 high generated/deadline swing. |
| 2026-05-18 | `walkstuf1-high-rg142-166-post-v3tight64` | Log-only; source restored and PS1 rebuilt. | Test the regenerated top W1-high candidate after the VISITOR3 high tight-window promotion. The matrix ranked `142..166` as the largest active under-99 scalar read-group row, with a modeled three-read saving, and it had not been closed by the earlier W1-high scalar sweep. | Temporarily added `{142,166,0}` after `{78,91,0}` in `kWalkstuf1HighReadGroups12[]`, then retried the same range as `{142,166,4}` to check the guarded side of the binary. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-high-rg142-166-vnext/20260518-180641-3865353/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-high-rg142-166-slack4-vnext/20260518-180836-3876507/summary.json`. | Failed/no promotion. Both variants produced the same regression: active loop stayed `1475`, target dropped `1441 -> 1433`, overrun rose `34 -> 42`, blocking rose `59 -> 66`, refill rose `13 -> 24`, and loop reads rose `46 -> 49`. The source table was restored. | Close W1-high `142..166` as a blind scalar append on the current baseline. The failure pattern matches the earlier W1-high grouped-append lesson: modeled read savings are not sufficient without generated deadline/refill ownership, and this row actively perturbs cadence/refill rather than saving visible time. Public rollup remains `+0.2610%` over target / `99.7431%` target speed; bands remain `119` green, `7` yellow, `0` orange, and `0` red. |
| 2026-05-18 | `building2-high-rg140-164-post-v3tight64` | Log-only; source restored and PS1 rebuilt. | Test the largest active BUILDING2 high matrix row after the VISITOR3 tight-window promotion. `140..164` advertised four saved reads, so it was worth one focused probe even though the matrix classified it as scheduler-owned only. | Temporarily added `{140,164,0}` to `kBuilding2HighReadGroups12[]` after the accepted `{83,95,0}` row and ran focused B2 high against the current under-green baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg140-164-vnext/20260518-181318-3903345/summary.json`. | Failed/no promotion. The probe was exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, loop reads/read time `45/199`, and due `5`, with `group_hits=0`. | Restore the accepted BUILDING2 high table. Close `140..164` as an inert blind scalar append on this baseline; the planner row needs generated append-start ownership before it can be retried as a real scheduler candidate. Public rollup remains `+0.2610%` over target / `99.7431%` target speed; bands remain `119` green, `7` yellow, `0` orange, and `0` red. |
| 2026-05-18 | `building2-high-rg158-182-post-v3tight64` | Log-only; source restored and PS1 rebuilt. | Follow the inert `140..164` B2-high probe with the next largest unclosed B2-high scheduler-owned row. `158..182` also advertised four saved reads and a fireable start in the host matrix. | Temporarily added `{158,182,0}` to `kBuilding2HighReadGroups12[]` after the accepted `{83,95,0}` row and ran focused B2 high against the current under-green baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg158-182-vnext/20260518-181632-3922005/summary.json`. | Failed/no promotion. The probe was exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, loop reads/read time `45/199`, and due `5`; it produced no key metric improvement. | Restore the accepted BUILDING2 high table. Close `158..182` as another inert blind scalar append. The B2-high direct table lane is now giving host-matrix false positives, so the next B2 work should be generated scheduler ownership, pack/data shape, or render/upload work rather than more hand-authored append rows. |
| 2026-05-18 | `building4-low-setupseg262-286-post-v3tight64` | Log-only; source restored and PS1 rebuilt. | Try a low-tide-only BUILDING4 retained setup segment at the late `262..286` cluster after hand-authored B4-low read groups proved inert. B4 low is the closest under-99 row and still has one due miss, so this was a scene-local binary setup-residency swing rather than another blind append row. | Temporarily added `FG_BUILDING4_LOW_SETUP_SEGMENT_START/BYTES=262/24` and routed BUILDING4 low through the same CACHE-backed setup-segment path as BUILDING4 high, then ran focused B4 low against the current under-green baseline. Artifact: `scratch/ps1-perf-iterate/building4-low-setupseg262-286-vnext/20260518-182725-3982667/summary.json`. | Failed/no promotion. The setup slice reduced active reads `30 -> 29` but regressed timing and hidden refill: scene/loop/target `3121/2849/2816 -> 3135/2853/2818`, overrun `33 -> 35`, blocking/refill `38/31 -> 42/35`, and due stayed `1`. Pack LBA and PS-EXE bucket stayed fixed. | Restore BUILDING4 low to no setup segment. Close `262..286` as phase-negative setup residency on this baseline; B4-low work should move to static upload/restore elimination, generated scheduler ownership, or data-shape changes rather than paying this late cluster during setup. Public rollup remains `+0.2610%` over target / `99.7431%` target speed; bands remain `119` green, `7` yellow, `0` orange, and `0` red. |
| 2026-05-18 | `walkstuf1-high-rg156-172-post-v3tight64` | Log-only; source restored and PS1 rebuilt. | Test the top unclosed W1-high scheduler-owned matrix row after the VISITOR3 tight-window promotion. The direct `156..172` range advertised two saved reads, so the binary check tested both unguarded and `4`-VBlank guarded append forms before closing the lane. | Temporarily added `{156,172,0}` after `{78,91,0}` in `kWalkstuf1HighReadGroups12[]`, then retried the same range as `{156,172,4}`. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-high-rg156-172-vnext/20260518-183233-4012105/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-high-rg156-172-slack4-vnext/20260518-183445-4024844/summary.json`. | Failed/no promotion. Both variants reduced reads `46 -> 44` and due misses `10 -> 9`, but active loop stayed `1475`, target dropped `1441 -> 1439`, overrun rose `34 -> 36`, and hidden refill rose `13 -> 18`; blocking stayed `59`. | Restore the accepted W1-high table. Close `156..172` as another phase-negative scalar append on the current baseline. W1-high still has read-save signal, but local table rows are converting it into target/refill debt; future W1-high work needs generated deadline/refill ownership, pack-side data shape, or render/upload reduction. Public rollup remains `+0.2610%` over target / `99.7431%` target speed; bands remain `119` green, `7` yellow, `0` orange, and `0` red. |
| 2026-05-18 | `walkstuf1-low-rg384-408-post-v3tight64` | Log-only; source restored and PS1 rebuilt. | Test the largest open W1-low scheduler-owned row after the `{91,107}` promotion. The `384..408` candidate overlapped the existing late `{394,410}` table and advertised three saved reads, making it the biggest remaining W1-low binary table check. | Temporarily added `{384,408,0}` before `{394,410,0}` in `kWalkstuf1LowReadGroups12[]`, then retried the same row as `{384,408,4}` after the direct form regressed visible cadence. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-rg384-408-vnext/20260518-183754-4042989/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-rg384-408-slack4-vnext/20260518-183950-4054259/summary.json`. | Failed/no promotion. Unguarded reduced reads `36 -> 33` but regressed scene/loop `1807/1473 -> 1812/1478`, overrun `29 -> 35`, and blocking `43 -> 50`; refill stayed `11`. The `4`-VBlank guard restored exact baseline timing and reads, but had no key metric improvement. | Restore the accepted W1-low table. Close `384..408` as phase-negative when direct and inert when guarded. Remaining W1-low work should require generated scheduler/refill ownership or pack/render work reduction rather than another late scalar table row. Public rollup remains `+0.2610%` over target / `99.7431%` target speed; bands remain `119` green, `7` yellow, `0` orange, and `0` red. |
| 2026-05-18 | `building2-low-setupseg226-238` | `ps1: prime building2 low late setup segment` | Convert the B2-low setup-addition lane from tight early clusters into a later slack-bearing cluster. The `153..177` 24-sector setup probe could not fit clean-rect/CACHE pressure, and the smaller `137..149` / `158..170` probes only reduced read pressure without reducing overrun. The binary question was whether the later `226..238` cluster had enough visible slack to buy real active-loop speed inside the same 28-sector setup footprint as the accepted `112..128` slice. | Added a second low-tide-only BUILDING2 retained setup segment at relative sectors `226..238` beside the existing `112..128` slice. Failed/near-miss artifacts: `scratch/ps1-perf-iterate/building2-low-setupseg153-177-vnext/20260518-184842-4104062/summary.json`, `scratch/ps1-perf-iterate/building2-low-setupseg153-177-clean48-vnext/20260518-185123-4119345/summary.json`, `scratch/ps1-perf-iterate/building2-low-setupseg158-170-vnext/20260518-185408-4134958/summary.json`, and `scratch/ps1-perf-iterate/building2-low-setupseg137-149-vnext/20260518-185618-4147419/summary.json`. Focused proof: `scratch/ps1-perf-iterate/building2-low-setupseg226-238-vnext/20260518-190157-4178936/summary.json`; seven-case under-green canary: `scratch/ps1-perf-iterate/building2-low-setupseg226-238-canaries/20260518-190345-4189268/summary.json`. | Promoted as an active-loop CD/setup-residency win. BUILDING2 low improves scene `1615 -> 1619` with setup cost paid before the loop, active loop `1339 -> 1336`, target stays `1316`, overrun `23 -> 20`, blocking/read time `53/141 -> 48/148`, prefetch overrun `2 -> 0`, due `11 -> 10`, and pack LBA/sectors plus the `233472` byte PS-EXE bucket stay fixed. BUILDING2 high, VISITOR3 high/low, WALKSTUF1 high/low, and BUILDING4 low stayed exact-flat in the canary. | Keep B2-low setup slices `112..128` and `226..238` as the new baseline. Public rollup improves to `+0.2591%` over target / `99.7448%` target speed; raw signed rollup is about `-0.4578%` / `100.4752%`; bands remain `119` green, `7` yellow, `0` orange, and `0` red. Methodology total remains about `17.14` public over-target points removed and `+12.64` target-speed points gained since the compact full-matrix baseline. Next focus remains VISITOR3 high/low, BUILDING2 high, WALKSTUF1 high/low, BUILDING2 low residual late clusters, and BUILDING4 low. |
| 2026-05-18 | `building2-high-setupseg86-242-cache-followup` | Log-only; source restored and PS1 rebuilt. | Recheck the broad B2-high setup-residency idea using the current CACHE-backed path after the refreshed baseline still ranked pre-`202..242` pressure highly. The binary question was whether the historic `86..242` / `90..242` midpack residency could now fit and convert the remaining `34` VBlank overrun. | Temporarily widened the accepted B2-high second setup segment from `202..242` to `86..242`, then backed off to `90..242` after the first probe hit the clean-rect allocator cliff. Artifacts: `scratch/ps1-perf-iterate/building2-high-setupseg86-242-vnext/20260518-194246-215265/summary.json` and `scratch/ps1-perf-iterate/building2-high-setupseg90-242-vnext/20260518-194538-231618/summary.json`. | Failed/no promotion. Both variants failed before `JCPERF2` during clean-rect allocation. `86..242` allocated a `385024` byte `fgSetupSegmentBuffer` and then failed a `97280` byte `grCleanRectPixels` request with only `93152` bytes available; `90..242` reduced the setup buffer to `376832` bytes but still failed the same clean chunk despite `101344` total free bytes, confirming a CACHE contiguous-block cliff. | Keep the accepted `3..35` plus `202..242` B2-high setup shape. The earlier feasible broad-boundary sweep already showed `184..242` and `194..242` regress cadence, so do not retry wider B2-high setup residency without first reducing clean-rect/CACHE pressure or adding generated scheduler ownership. |
| 2026-05-18 | `upload-gap7-9-b4low` | Log-only; source restored to accepted gap `8` and PS1 rebuilt. | Fill the two untested neighbors around the accepted BUILDING4-low dirty-upload merge knee. Gap `8` produced the current B4-low speed win, gap `10` was exact-flat, and gap `11` regressed, so the binary question was whether `7` or `9` could uncover one more VBlank toward green. | Temporarily changed `GR_UPLOAD_BAND_MERGE_GAP` to `9`, then to `7`, with focused B4-low gates against the current under-green baseline. Artifacts: `scratch/ps1-perf-iterate/upload-gap9-b4low-current/20260518-195428-281353/summary.json` and `scratch/ps1-perf-iterate/upload-gap7-b4low-current/20260518-195647-294724/summary.json`. | Failed/no promotion. Both neighbor gaps were exact-flat at scene/loop/target `3121/2849/2816`, overrun `33`, blocking/refill `38/31`, loop reads/read time `30/222`, and due `1`; neither had a key metric improvement. | Keep `GR_UPLOAD_BAND_MERGE_GAP=8`. The global upload-band knob is saturated for B4-low green conversion; future B4-low work should use static upload/restore elimination, generated scheduler ownership, or pack/data-shape changes rather than more global gap sweeps. |
| 2026-05-18 | `building4-low-xband-upload-big-swing` | Log-only; renderer restored and PS1 rebuilt. | Test the biggest remaining BUILDING4-low render-work swing: use the existing per-row dirty X ranges to upload narrower framebuffer bands instead of full 320-pixel tile-width bands. The hypothesis was that B4-low's static x-band opportunity could remove enough upload DMA to close the last `33` VBlank overrun without pack changes. | Temporarily added three `grDrawBackground()` variants: v1 row-wise 4-pixel-aligned partial uploads, v2 scratch-chunked 8-pixel-aligned x-band uploads, and v3 scratch-chunked 32-pixel-aligned x-band uploads with stack pressure removed. Artifacts: `scratch/ps1-perf-iterate/building4-low-xband-upload-v1/20260518-200534-344227/summary.json`, `scratch/ps1-perf-iterate/building4-low-xband-upload-v2/20260518-200951-368277/summary.json`, and `scratch/ps1-perf-iterate/building4-low-xband-upload-v3/20260518-201318-387629/summary.json`. | Failed/no promotion. v1 and v2 missed `JCPERF2` by the 120s cap; v1 also shifted the PS-EXE bucket and emitted illegal transfer-alignment warnings. v3 completed with legal alignment but regressed B4-low scene/loop/target `3121/2849/2816 -> 3154/2882/2814`, overrun `33 -> 68`, blocking `38 -> 62`, refill `31 -> 55`, and upload work `30` calls / `1330` rects / `9735232` bytes; the code also grew `grDrawBackground` by `884` bytes and shifted the PS-EXE sector bucket. | Close runtime x-band framebuffer uploads for now. The DMA byte reduction is outweighed by copy/LoadImage overhead and executable layout debt; future B4-low work should stay pack-authored or scheduler-owned unless a new GPU primitive can upload strided tile memory without CPU repacking. Public rollup remains `+0.2591%` over target / `99.7448%` target speed, with `119` green and `7` yellow rows. |
| 2026-05-18 | `walkstuf1-high-rg351-367-overlap` | Log-only; source restored and PS1 rebuilt. | Test the regenerated top W1-high matrix row after the B2-low promotion and B4-low x-band closure. The `351..367` row overlapped the accepted `{344,360}` group and advertised two saved reads, so the binary check tested it first as a size-neutral replacement and then as an additive overlap row. | Temporarily replaced `{344,360,0}` with `{351,367,0}`, then restored `{344,360,0}` and added `{351,367,0}` after it for the additive form. Artifacts: replacement `scratch/ps1-perf-iterate/walkstuf1-high-rg351-367-replace344-360/20260518-202036-428686/summary.json`; additive `scratch/ps1-perf-iterate/walkstuf1-high-rg351-367-additive/20260518-202243-440994/summary.json`. | Failed/no promotion. Both forms produced the same phase-negative result: reads improved `46 -> 44`, but scene/loop regressed `1811/1475 -> 1813/1477`, target dropped `1441 -> 1439`, overrun worsened `34 -> 38`, blocking worsened `59 -> 64`, and hidden refill worsened `13 -> 16`. | Close W1-high `{351,367}` as both replacement and additive scalar grouped append on this baseline. The read-save signal is real but lands in the same unsafe phase as the earlier `352..376` cluster; future W1-high green work needs generated deadline/refill ownership, setup/data-shape changes, or render/upload work reduction rather than another blind overlap row. |
| 2026-05-18 | `walkstuf1-high-rg149-165` | `ps1: add walkstuf1 high rg149-165` | Take the next regenerated W1-high matrix row after closing the overlapping `351..367` lane. The `149..165` row was the top open scheduler-owned candidate and did not overlap accepted high groups, so it was the last bounded scalar-table check before pivoting to generated ownership/data-shape work. | Added `{149,165,0}` after `{78,91,0}` in `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran a focused W1-high gate plus a seven-case under-green canary. Focused proof: `scratch/ps1-perf-iterate/walkstuf1-high-rg149-165-vnext/20260518-202745-469634/summary.json`; accepted canary: `scratch/ps1-perf-iterate/walkstuf1-high-rg149-165-canaries/20260518-202932-479774/summary.json`. | Promoted as a same-overrun CD-pressure win. WALKSTUF1 high stays exact-flat on public timing at scene/loop/target `1811/1475/1441`, overrun `34`, refill `13`, and due `10`, while improving blocking `59 -> 57`, loop reads `46 -> 44`, and loop-read time `212 -> 199`; pack LBA/sectors and the `233472` byte PS-EXE bucket stay fixed. WALKSTUF1 low, VISITOR3 high/low, BUILDING2 high/low, and BUILDING4 low stayed exact-flat in the canary. | Keep `{149,165,0}` as the current W1-high early read-pressure row. Public rollup remains `+0.2591%` over target / `99.7448%` target speed because the VBlank overrun did not move; raw signed remains about `-0.4578%` / `100.4752%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total remains about `17.14` public over-target points removed and `+12.64` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `walkstuf1-high-rg165-189-slack-sweep` | Log-only; source restored and PS1 rebuilt. | Probe the next regenerated W1-high scheduler-owned-only row after the accepted `149..165` pressure win. The matrix ranked `165..189` as a 24-sector, three-saved-read candidate, so the binary check tested whether the adjacent early cluster could turn saved reads into visible speed without adding blocking debt. | Temporarily inserted `{165,189,0}` after `{149,165,0}`, rebuilt, and ran the focused W1-high gate against the accepted `149..165` canary baseline. After the raw form improved visible speed but failed the blocking gate, retried guarded forms `{165,189,4}` and `{165,189,2}`. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-high-rg165-189-vnext/20260518-204954-594465/summary.json`; slack4 `scratch/ps1-perf-iterate/walkstuf1-high-rg165-189-slack4-vnext/20260518-205149-605533/summary.json`; slack2 `scratch/ps1-perf-iterate/walkstuf1-high-rg165-189-slack2-vnext/20260518-205343-616636/summary.json`. | Failed/no promotion. The raw row improved scene/loop/target `1811/1475/1441 -> 1807/1471/1440` and overrun `34 -> 31`, with loop reads `44 -> 42`, but it regressed visible blocking `57 -> 64`, failing the promotion gate. Slack4 and slack2 both no-opped exactly against the accepted baseline: `1811/1475/1441`, overrun `34`, blocking `57`, refill `13`, reads `44`, due `10`. | Close `165..189` for direct W1-high table ownership on the current baseline. The speed signal is real but the unguarded form trades it for blocking debt, while guarded forms do not fire; future work for this cluster needs generated deadline/refill ownership or pack/data-shape changes rather than another scalar row. |
| 2026-05-18 | `walkstuf1-high-rg171-195-slack1` | Log-only; source restored and PS1 rebuilt. | Test the adjacent later W1-high `171..195` row before pivoting away from blind W1 scalar groups. The matrix still ranked it as a three-read scheduler-owned-only opportunity, and the later start avoided part of the blocking spike seen in `165..189`, so it deserved one raw/guarded binary check. | Temporarily inserted `{171,195,0}` after `{149,165,0}`, rebuilt, and ran the focused W1-high gate against the accepted `149..165` canary baseline. Because the raw form only missed on hidden refill, retried `{171,195,1}` as the tightest guard that might still fire. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-high-rg171-195-vnext/20260518-205704-635897/summary.json`; slack1 `scratch/ps1-perf-iterate/walkstuf1-high-rg171-195-slack1-vnext/20260518-205912-648154/summary.json`. | Failed/no promotion. Both forms improved scene/loop/target `1811/1475/1441 -> 1807/1471/1440`, overrun `34 -> 31`, and loop reads `44 -> 42`, while keeping due misses at `10`; however both also moved blocking `57 -> 58` and hidden refill overrun `13 -> 14`, and the strict gate rejected the refill regression. | Close direct W1-high `171..195` for now. It is a tempting speed tradeoff, but the current promotion rule does not accept hidden-refill debt to buy public speed unless the visible/blocking profile also improves. Future retries should be generated-owner or pack-shape based, not another hand-authored scalar row. |
| 2026-05-18 | `building2-high-rg249-265-current` | Log-only; source restored and PS1 rebuilt. | Pivot from W1-high scalar rows to the top Building2-high matrix gap. The `249..265` row sat between accepted high groups `226..242` and `271..287`, advertised two modeled saved reads, and was the largest open Building2-high direct-table candidate. | Temporarily inserted `{249,265,0}` in `kBuilding2HighReadGroups12[]`, rebuilt, and ran the strict focused Building2-high gate against the accepted under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg249-265-vnext/20260518-210333-673086/summary.json`. | Failed/no promotion. The row was phase-negative: scene `1621 -> 1627`, active loop/target `1347/1313 -> 1353/1314`, overrun `34 -> 39`, blocking `39 -> 53`, hidden refill `16 -> 17`, loop reads stayed `45`, and due misses moved `5 -> 7`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close Building2-high `249..265` as a direct scalar read group. This gap does not expose useful read savings on the current allocator-era layout; next Building2-high work should test a distinct phase cluster such as `158..174` or move to generated scheduling/pack-shape work. |
| 2026-05-18 | `building2-high-rg95-107-current` | Log-only; source restored and PS1 rebuilt. | Test the next Building2-high current matrix row after `249..265` failed. The `95..107` range is adjacent to the accepted `{83,95}` group and modeled as a one-read saver, so it was a plausible tighter-phase follow-up before leaving B2 high scalar rows. | Temporarily inserted `{95,107,0}` after `{83,95,0}` in `kBuilding2HighReadGroups12[]`, rebuilt, and ran the strict focused Building2-high gate against the accepted under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg95-107-vnext/20260518-210638-690880/summary.json`. | Failed/no promotion. The row reduced loop reads `45 -> 43`, but regressed scene `1621 -> 1626`, active loop/target `1347/1313 -> 1353/1312`, overrun `34 -> 41`, blocking `39 -> 42`, and hidden refill `16 -> 18`; due misses stayed `5`, pack LBA and PS-EXE bucket stayed fixed. | Close Building2-high `95..107` as phase-negative. Like the late gap, it saves reads but spends the saved CD in visible/refill cadence; future work should avoid blind scalar grouping unless generated ownership can choose a safe deadline slot. |
| 2026-05-18 | `building2-high-rg122-134-current` | Log-only; source restored and PS1 rebuilt. | Test the last current Building2-high direct scalar row from the regenerated matrix after `249..265` and `95..107` failed. The `122..134` row modeled one saved read and occupied a distinct early phase gap, so it was the final cheap B2-high table probe before moving to another optimization family. | Temporarily inserted `{122,134,0}` after `{83,95,0}` in `kBuilding2HighReadGroups12[]`, rebuilt, and ran the strict focused Building2-high gate against the accepted under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg122-134-vnext/20260518-210932-707820/summary.json`. | Failed/no promotion. The row reduced loop reads `45 -> 43`, but regressed scene `1621 -> 1622`, active loop/target `1347/1313 -> 1349/1311`, overrun `34 -> 38`, blocking `39 -> 43`, hidden refill `16 -> 17`, and kept due misses at `5`; pack LBA and PS-EXE bucket stayed fixed. | Close Building2-high `122..134` as phase-negative. The current B2-high direct read-table lane is exhausted; remaining B2-high work should be generated deadline ownership, pack-side byte/work reduction, or upload/restore reduction rather than more scalar grouped rows. |
| 2026-05-18 | `walkstuf1-low-rg148-164-current` | Log-only; source restored and PS1 rebuilt. | Try the high-side early pressure win as a low-tide carryover. The regenerated matrix ranked W1-low `148..164` as the largest open low-tide row, and the accepted W1-high `149..165` row proved the nearby sectors can reduce CD pressure when the phase is right. | Temporarily inserted `{148,164,0}` after the accepted low `{91,107,0}` row in `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the accepted under-green canary baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg148-164-vnext/20260518-211244-726345/summary.json`. | Failed/no promotion. The row reduced loop reads `36 -> 35`, but regressed scene `1807 -> 1813`, active loop `1473 -> 1479`, overrun `29 -> 35`, blocking `43 -> 48`, hidden refill `11 -> 13`, and kept due misses at `5`; pack LBA and PS-EXE bucket stayed fixed. | Close W1-low `148..164` as a direct scalar carryover. The high-tide early-row win does not transfer to low tide; remaining W1-low gains need phase-owned scheduling or pack/upload work reduction, not another blind early grouped append. |
| 2026-05-18 | `visitor3-high-tight56-cap` | `ps1: tune visitor3 high tight refill cap` | Convert the accepted VISITOR3 high frame56/57 raw-gap layout from a safe `64 KiB` tight-window cap into the best current cap. The binary sweep tested whether the new raw-gap layout could cap speculative refills harder without splitting visible reads. | Changed `FG_VISITOR3_HIGH_TIGHT_WINDOW_BYTES` from `64 KiB` to `48`, `56`, `60`, and `52 KiB`, rebuilding and running focused Visitor3-high gates each time. Artifacts: 48 `scratch/ps1-perf-iterate/visitor3-high-tight48-vnext/20260518-213003-822373/summary.json`; 56 focused proof `scratch/ps1-perf-iterate/visitor3-high-tight56-vnext/20260518-213151-833022/summary.json`; 60 `scratch/ps1-perf-iterate/visitor3-high-tight60-vnext/20260518-213336-843296/summary.json`; 52 `scratch/ps1-perf-iterate/visitor3-high-tight52-vnext/20260518-213525-853836/summary.json`; seven-case canary `scratch/ps1-perf-iterate/visitor3-high-tight56-canaries/20260518-213723-865271/summary.json`. | Promoted as an active-loop CD/refill speed win. The `48` and `52 KiB` caps were too tight, regressing to `1083/1042` and `1085/1042` with blocking `58`; `60 KiB` was safe but only improved hidden refill `5 -> 4`. The accepted `56 KiB` cap improves VISITOR3 high scene `1392 -> 1388`, active loop/target `1079/1043 -> 1075/1043`, overrun `36 -> 32`, blocking/read time `49/57 -> 46/54`, prefetch overrun `5 -> 3`, while loop reads and due stay `4`/`3`. W1 high/low, VISITOR3 low, B2 high/low, and B4 low stayed exact-flat in canaries. | Keep the frame56/57 raw-gap layout but lower the VISITOR3 high tight-window cap to `56 KiB`. Public rollup improves to `+0.2561%` over target / `99.7477%` target speed; raw signed rollup is about `-0.4608%` / `100.4780%`; bands stay `119` green, `7` yellow, `0` orange, and `0` red. Methodology total is about `17.14` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-18 | `building2-high-local-lz-89-family` | Log-only; pack/code restored and PS1 rebuilt. | Test a pack-side local-LZ big swing for BUILDING2 high after scalar read groups exhausted. Frames `89`, `90`, and `91` each shrink from `5` sectors to `3`, and frames `92`, `94`, and `95` add more sector reductions, so the binary question was whether lower CD sectors could beat local decode cost on the second-worst yellow row. | Temporarily local-LZ encoded frames `89,90,91,92,94,95`, then fixed the discovered frame-buffer sizing trip with a temporary `building2` expanded-size floor, then tested narrower `89,90,91` and `89`-only variants. Artifacts: full trip `scratch/ps1-perf-iterate/building2-high-local-lz-89-95-vnext/20260518-220732-1033523/summary.json`; full with floor `scratch/ps1-perf-iterate/building2-high-local-lz-89-95-floor-vnext/20260518-221047-1052377/summary.json`; narrow `scratch/ps1-perf-iterate/building2-high-local-lz-89-91-vnext/20260518-221316-1066893/summary.json`; single `scratch/ps1-perf-iterate/building2-high-local-lz-89-vnext/20260518-221523-1079347/summary.json`. | Failed/no promotion. The unbounded full set initially tripped at frame `95` because the compressed entry lowered max frame allocation below the `10247` byte expanded payload. After the temporary floor fixed correctness, the full set regressed B2 high `1347/1313 -> 1373/1311`, overrun `34 -> 62`, blocking `39 -> 41`; `89..91` regressed `1347/1313 -> 1357/1312`, overrun `34 -> 45`, blocking `39 -> 41`; and `89` alone regressed `1347/1313 -> 1353/1310`, overrun `34 -> 43`, blocking `39 -> 52`, refill `16 -> 20`. | Close BUILDING2-high local-LZ on the current runtime. Even real sector cuts lose to decode/cadence here, and any local-LZ compression of the largest frame needs an expanded-size allocation floor. Move the pack-side LZ swing to WALKSTUF1, where the candidate frames have larger sector drops and do not shrink the scene below their expanded payloads. |
| 2026-05-18 | `walkstuf1-local-lz-family` | Log-only; packs restored and PS1 rebuilt. | Continue the pack-side local-LZ big swing on WALKSTUF1 after B2 showed decode/cadence risk. The codec scan found large sector drops in both tide packs, including high frame `55` (`3 -> 1` sectors), high frames `55..57`, low frames `55,59,60,63,65` (`3 -> 1` each on most), and the low frame `83` candidate that mapped into the current `159..165` read-plan row. | Temporarily encoded high frames `55..57`, high frame `55` alone, low frames `55,59,60,63,65`, low frames `59,60`, and low frame `83`. Artifacts: high set `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-55-57-vnext/20260518-221954-1105299/summary.json`; high single `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-55-vnext/20260518-222213-1118812/summary.json`; low set `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-55-59-60-63-65-vnext/20260518-222440-1133043/summary.json`; low `59,60` `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-59-60-vnext/20260518-222656-1146346/summary.json`; low `83` `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-83-vnext/20260518-222917-1160049/summary.json`. | Failed/no promotion. High `55..57` and high `55` alone both regressed W1 high from `1475/1441` to `1475/1433`, overrun `34 -> 42`, blocking `57 -> 71`, refill `13 -> 24`. Low `55,59,60,63,65` and low `59,60` both regressed W1 low `1473/1444 -> 1475/1443`, overrun `29 -> 32`, blocking `43 -> 46`, while only improving refill `11 -> 10`. Low frame `83` regressed further to `1481/1443`, overrun `38`, blocking `52`. | Close local-LZ for WALKSTUF1 on this runtime. The byte/sector saves are real but the changed entry sizes perturb refill cadence and target timing more than they save; future pack-shape work needs no-decode relocation, generated deadline ownership, or an LZ-aware scheduler model rather than blind local-LZ entry shrinking. |
| 2026-05-18 | `walkstuf1-high-scheduler-row99-115` | Log-only; source restored and PS1 rebuilt. | Test the next open W1-high scheduler family after local-LZ closed. The accepted W1-low prepare-before-window rule suggested a possible high-tide scheduler ownership win, and the regenerated read matrix ranked W1-high `99..115` as the top open current candidate. | Temporarily extended the W1 prepare-before-window branch to high tide, then tested the same branch with a `64 KiB` high window and with a stricter high window slack skip. Separately added `{99,115,0}` and then guarded `{99,115,4}` to `kWalkstuf1HighReadGroups12[]`. Artifacts: prepare-first `scratch/ps1-perf-iterate/walkstuf1-high-prepare-first-vnext/20260518-223257-1181560/summary.json`; prepare-first window64 `scratch/ps1-perf-iterate/walkstuf1-high-prepare-first-window64-vnext/20260518-223525-1195905/summary.json`; prepare-first min5 `scratch/ps1-perf-iterate/walkstuf1-high-prepare-first-window-min5-vnext/20260518-223742-1209176/summary.json`; raw row `scratch/ps1-perf-iterate/walkstuf1-high-rg99-115-vnext/20260518-223957-1222339/summary.json`; guarded row `scratch/ps1-perf-iterate/walkstuf1-high-rg99-115-slack4-vnext/20260518-224156-1234138/summary.json`. | Failed/no promotion. Prepare-first had a useful visible-blocking signal (`1475 -> 1474`, blocking `57 -> 48`, overrun flat `34`) but regressed hidden refill `13 -> 17`; larger window and stricter slack skip did not change the metrics. Raw `{99,115}` reduced reads `44 -> 42` and refill `13 -> 12`, but regressed loop `1475 -> 1477`, overrun `34 -> 37`, and blocking `57 -> 63`. Guarded `{99,115,4}` no-opped exactly against baseline. | Keep W1-high scheduler and read groups at the accepted baseline. Prepare-first is a candidate for a future generated owner that can cap hidden refill debt, but the scalar branch is not strict-promotable. Close `{99,115}` direct row ownership; raw is phase-negative and guarded is inert. |
| 2026-05-18 | `building2-high-rg140-152` | Log-only; source restored and PS1 rebuilt. | Test the top open BUILDING2-high direct scheduler row after the B2 local-LZ path failed. The regenerated matrix ranked `140..152` as the next non-closed high-tide candidate, with one modeled saved read, so the binary check covered raw and guarded ownership. | Temporarily inserted `{140,152,0}` into `kBuilding2HighReadGroups12[]`, then tested guarded `{140,152,4}` and strict `{140,152,8}`. Artifacts: raw `scratch/ps1-perf-iterate/building2-high-rg140-152-vnext/20260518-224500-1252105/summary.json`; slack4 `scratch/ps1-perf-iterate/building2-high-rg140-152-slack4-vnext/20260518-224651-1263004/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-high-rg140-152-slack8-vnext/20260518-224844-1274151/summary.json`. | Failed/no promotion. Raw and slack4 both fired and regressed B2 high from `1347/1313` to `1347/1312`, overrun `34 -> 35`, blocking `39 -> 41`, refill `16 -> 18`, and reads `45 -> 43`. Slack8 no-opped exactly at `1347/1313`, overrun `34`, blocking `39`, refill `16`, reads `45`. | Close BUILDING2-high `140..152` as direct row ownership. Like other B2-high scalar rows, the read-count win lands in an unsafe phase; a strict guard turns it inert. Continue with other scenes or generated scheduling, not more variants of this range. |
| 2026-05-18 | `walkstuf1-low-rg172-184` | Log-only; source restored and PS1 rebuilt. | Test the top open W1-low current row after B2-high `140..152` closed. The regenerated matrix ranked `172..184` as a low-visible-risk scheduler-owned candidate with one modeled saved read, so the binary check covered raw and strict guarded ownership. | Temporarily inserted `{172,184,0}` into `kWalkstuf1LowReadGroups12[]`, then tested strict `{172,184,8}`. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg172-184-vnext/20260518-225149-1292153/summary.json`; slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg172-184-slack8-vnext/20260518-225347-1303769/summary.json`. | Failed/no promotion. Raw reduced refill `11 -> 10` and reads `36 -> 35`, but regressed W1 low from `1473/1444` to `1477/1444`, overrun `29 -> 33`, and blocking `43 -> 45`. Slack8 no-opped exactly at `1473/1444`, overrun `29`, blocking `43`, refill `11`, reads `36`. | Close W1-low `172..184` as direct row ownership. The raw row trades hidden refill for visible loop/blocking debt, and the strict guard is inert. Continue to the next distinct W1-low/B2-low candidate or generated scheduler work. |
| 2026-05-18 | `walkstuf1-high-rg357-369` | Log-only; source restored and PS1 rebuilt. | Test the next open W1-high scheduler-owned row after the matrix refresh closed the recent misses. The row promised two modeled saved reads, but overlapped an accepted `344..360` window, so the first binary check was whether raw ownership could fire at all. | Temporarily inserted `{357,369,0}` into `kWalkstuf1HighReadGroups12[]`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg357-369-vnext/20260518-225825-1330379/summary.json`. | Failed/no promotion. Raw was an exact no-op on key metrics: W1 high stayed `1475/1441`, overrun `34`, blocking `57`, refill `13`, and loop reads `44`; runtime `group_hits=0`, so a strict guard would also be inert. | Close W1-high `357..369` as a direct scalar row. The matrix read-save model is blocked by overlap/current append starts here; continue with a distinct W1-high/W1-low candidate or a generated scheduler owner that can reason across overlapping windows. |
| 2026-05-18 | `walkstuf1-high-rg171-183` | Log-only; source restored and PS1 rebuilt. | Test the new top open W1-high row after `357..369` proved inert. This range was lower visible-risk than the unsafe W1-high rows and did not overlap the accepted `344..360` group. | Temporarily inserted `{171,183,0}` into `kWalkstuf1HighReadGroups12[]`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg171-183-vnext/20260518-230211-1351837/summary.json`. | Failed/no promotion. Raw reduced loop reads `44 -> 43`, but all speed-bearing metrics stayed flat: `1475/1441`, overrun `34`, blocking `57`, refill `13`, and due misses `10`. | Close W1-high `171..183` as a direct scalar row for speed purposes. It saves one read without timing gain, so it is not worth carrying while the objective is getting under-99 scenes into the green. |
| 2026-05-18 | `walkstuf1-low-rg225-237` | Log-only; source restored and PS1 rebuilt. | Test the last top-ranked W1 direct row before pivoting to larger Visitor3/B2-low scheduler-owned swings. The range had one modeled saved read but overlaps the existing `209..225`/`213..229` boundary, so raw and strict guarded variants were checked. | Temporarily inserted `{225,237,0}` into `kWalkstuf1LowReadGroups12[]`, then tested strict `{225,237,8}`. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg225-237-vnext/20260518-230504-1368696/summary.json`; slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg225-237-slack8-vnext/20260518-230652-1379374/summary.json`. | Failed/no promotion. Raw regressed W1 low from `1473/1444` to `1479/1444`, overrun `29 -> 35`, blocking `43 -> 45`, refill `11 -> 15`. Slack8 no-opped exactly at `1473/1444`, overrun `29`, blocking `43`, refill `11`, reads `36`. | Close W1-low `225..237` as direct scalar ownership. The raw row lands in a bad phase and the safe guard is inert; stop spending time on W1 one-read rows unless the generated scheduler can own the full overlap. |
| 2026-05-18 | `visitor3-low-rg248-family` | Log-only; source restored and PS1 rebuilt. | Take a bigger swing after W1 one-read rows closed: add a temporary VISITOR3-low read-group selector/table for the top modeled low-tide ranges with three to four saved reads. | Temporarily added a low-tide VISITOR3 selector plus exact range `248..272` (`{248,272,0}`), then replaced it with exact range `248..264` (`{248,264,0}`). Artifacts: broad `scratch/ps1-perf-iterate/visitor3-low-rg248-272-vnext/20260518-231018-1399382/summary.json`; narrow `scratch/ps1-perf-iterate/visitor3-low-rg248-264-vnext/20260518-231209-1410350/summary.json`. | Failed/no promotion. Both variants added `foregroundPilotPlay` size/layout cost (`+20` bytes, hot symbols `+20`) but produced `group_hits=0`, kept loop reads flat at `18`, and regressed VISITOR3 low from `1065/1039` to `1067/1041` with blocking `75 -> 76`. | Close VISITOR3-low `248..272` and `248..264` direct-table ownership. The modeled range needs first-class generated scheduler ownership, not the current append-group hook; direct selector/table overhead is harmful without a firing group. |
| 2026-05-18 | `visitor3-low-rg240-264` | Log-only; source restored and PS1 rebuilt. | Check whether the next four-read-save VISITOR3-low range could fire after the `248` family failed. | Temporarily added the same low-tide VISITOR3 selector/table shape with exact range `240..264` (`{240,264,0}`). Artifact: `scratch/ps1-perf-iterate/visitor3-low-rg240-264-vnext/20260518-231633-1435489/summary.json`. | Failed/no promotion. It repeated the direct-table failure shape: `group_hits=0`, loop reads stayed `18`, `foregroundPilotPlay` grew `+20`, and VISITOR3 low regressed from `1065/1039` to `1067/1041` with blocking `75 -> 76`. | Close `240..264` as direct VISITOR3-low table ownership. More direct VISITOR3 low rows should be skipped until generated scheduler ownership can fire without selector/layout debt. |
| 2026-05-18 | `building2-low-late-readgroups` | Log-only; source restored and PS1 rebuilt. | Test the B2-low late read cluster after Visitor3 direct-table ownership failed. Unlike Visitor3, B2-low already has an active read-group selector, so the binary check focused on whether large existing-table rows can convert modeled read saves into speed. | Temporarily inserted exact range `245..269` raw (`{245,269,0}`), strict `245..269` slack8 (`{245,269,8}`), exact range `252..268` raw (`{252,268,0}`), and strict `252..268` slack8 (`{252,268,8}`) into `kBuilding2LowReadGroups12[]`. Artifacts: raw 24-sector `scratch/ps1-perf-iterate/building2-low-rg245-269-vnext/20260518-231927-1452436/summary.json`; slack8 24-sector `scratch/ps1-perf-iterate/building2-low-rg245-269-slack8-vnext/20260518-232151-1466347/summary.json`; raw 16-sector `scratch/ps1-perf-iterate/building2-low-rg252-268-vnext/20260518-232341-1477156/summary.json`; slack8 16-sector `scratch/ps1-perf-iterate/building2-low-rg252-268-slack8-vnext/20260518-232540-1488872/summary.json`. | Failed/no promotion. Raw `245..269` reduced loop reads `35 -> 31` and blocking `48 -> 45`, but regressed `1336/1316 -> 1339/1317` and overrun `20 -> 22`. Raw `252..268` reduced reads `35 -> 32` and blocking `48 -> 46`, but also regressed to `1339/1317`, overrun `22`. Both slack8 variants no-opped exactly at `1336/1316`, overrun `20`, blocking `48`, reads `35`. | Close direct B2-low `245..269` and `252..268` table ownership. The modeled CD savings are real but land in a bad phase; strict guards are safe but inert. Continue with a different B2-low cluster or generated scheduler ownership that can shift the read earlier without consuming active-loop cadence. |
| 2026-05-18 | `building2-low-rg153-177` | Log-only; source restored and PS1 rebuilt. | Test the next large B2-low early cluster after late-cluster rows showed read savings can become loop overrun. | Temporarily inserted exact range `153..177` raw (`{153,177,0}`), then strict `153..177` slack8 (`{153,177,8}`) into `kBuilding2LowReadGroups12[]`. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg153-177-vnext/20260518-232829-1505388/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg153-177-slack8-vnext/20260518-233012-1515628/summary.json`. | Failed/no promotion. Raw was strongly phase-negative: B2 low regressed `1336/1316 -> 1345/1317`, overrun `20 -> 28`, blocking `48 -> 55`, and refill `0 -> 7` despite loop reads dropping `35 -> 31`. Slack8 no-opped exactly at `1336/1316`, overrun `20`, blocking `48`, reads `35`. | Close direct B2-low `153..177`. The early cluster is worse than the late cluster under raw ownership; a safe guard is inert. |
| 2026-05-18 | `building2-low-rg145-169` | Log-only; source restored and PS1 rebuilt. | Test the adjacent B2-low early four-read-save range to confirm whether the `153..177` failure was range-specific or cluster-wide. | Temporarily inserted exact range `145..169` raw (`{145,169,0}`), then strict `145..169` slack8 (`{145,169,8}`) into `kBuilding2LowReadGroups12[]`. Artifacts: raw `scratch/ps1-perf-iterate/building2-low-rg145-169-vnext/20260518-233256-1531874/summary.json`; slack8 `scratch/ps1-perf-iterate/building2-low-rg145-169-slack8-vnext/20260518-233440-1542213/summary.json`. | Failed/no promotion. Raw was worse than `153..177`: B2 low regressed `1336/1316 -> 1357/1320`, overrun `20 -> 37`, blocking `48 -> 53`, refill `0 -> 7`; slack8 no-opped exactly at `1336/1316`, overrun `20`, blocking `48`, reads `35`. | Close direct B2-low `145..169`. The early cluster is phase-hostile under current table ownership; continue with smaller row probes only if they target a distinct timing gap, otherwise pivot to scheduler/data-shape work. |
| 2026-05-18 | `building2-low-setup-seg2-late` | Log-only; source restored and PS1 rebuilt. | Bigger allocator-era swing after direct read groups converted CD savings into active-loop overrun: preload the B2-low late cluster through reusable setup-segment residency instead of held-frame append reads. | Temporarily raised `FG_BUILDING2_LOW_SETUP_SEGMENT2_BYTES` from `12` sectors to `43`, then `42`, then `41`, then `36` sectors. Artifacts: `226..269`/43 sectors `scratch/ps1-perf-iterate/building2-low-seg2-226-269-vnext/20260518-233755-1561058/summary.json`; `226..268`/42 sectors `scratch/ps1-perf-iterate/building2-low-seg2-226-268-vnext/20260518-234045-1577317/summary.json`; `226..267`/41 sectors `scratch/ps1-perf-iterate/building2-low-seg2-226-267-vnext/20260518-234320-1592192/summary.json`; `226..262`/36 sectors `scratch/ps1-perf-iterate/building2-low-seg2-226-262-vnext/20260518-234601-1607578/summary.json`. | Failed/no promotion. All variants hit allocator halt before `JCPERF2`. The log shows `JCBSOD-FATAL CACHE exhausted` during the third `grCleanRectPixels` allocation after `fgSetupSegmentBuffer` grew (`126976`, `118784`, `116736`, and `106496` bytes respectively). Even with `have=100080` for a request of `87724`, CACHE shape/fragmentation could not provide the clean-rect allocation. | Close the simple B2-low setup-segment expansion. The idea is promising but needs a companion clean-rect memory-shape change, different region placement, or generated residency that does not sit in CACHE before clean rect allocation. |
| 2026-05-19 | `building2-low-seg226-262-clean80-rg141` | `ps1: move building2 low into green` | Convert the promising B2-low `226..262` setup-segment expansion into an allocator-safe promotion by pairing it with low-only clean-rect memory shaping, one extra window slack VBlank, and an earlier small read group to recover visible cadence. | Raised the second low-tide BUILDING2 setup segment from `226..238` to `226..262`, capped low-tide BUILDING2 clean-rect strips at `80 KiB`, raised the B2-low window slack from `4` to `5`, and inserted `{141,153,0}` ahead of the existing low read groups. Focused proof: `scratch/ps1-perf-iterate/building2-low-seg2-226-262-clean80-slack5-rg141-vnext/20260519-002451-1832337/summary.json`; seven-case no-regression canary: `scratch/ps1-perf-iterate/building2-low-seg2-clean80-slack5-rg141-canaries-norequire/20260519-003746-1907273/summary.json`. | Promoted as a green-band allocator/CD residency win. BUILDING2 low improves `1336/1316 -> 1327/1318`, target speed `98.503% -> 99.322%`, overrun `20 -> 9`, blocking `48 -> 47`, hidden refill stays `0`, loop reads `35 -> 27`, and due `10 -> 9`. BUILDING2 high, VISITOR3 high/low, WALKSTUF1 high/low, and BUILDING4 low stayed exact-flat in the canary. | Keep the widened B2-low setup slice, low-only `80 KiB` clean strip cap, slack-5 guard, and `{141,153}` row. Public rollup improves to `+0.2494%` over target / `99.7542%` target speed; raw signed rollup is about `-0.4675%` / `100.4845%`; bands move to `120` green, `6` yellow, `0` orange, and `0` red. Methodology total is about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `visitor3-high-clean64` | `ps1: cap visitor3 high clean strips` | Retest the clean-rect strip-size lever on VISITOR3 high after BUILDING2 low proved allocator-era clean-shape changes can move timing without changing pack data. The binary sweep checked whether a high-only cap could improve the raw-gap tight56 baseline, and whether adjacent caps were safe. | Added a VISITOR3 high-only `gFgCleanRectMaxBytes = 64 KiB` while keeping all retained setup segments and pack bytes unchanged. Focused proof: `scratch/ps1-perf-iterate/visitor3-high-clean64-vnext/20260519-010801-2078106/summary.json`; seven-case canary: `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Rejected neighbors: `80 KiB` at `scratch/ps1-perf-iterate/visitor3-high-clean80-vnext/20260519-010619-2068175/summary.json` failed to improve and regressed overrun `32 -> 33`; `48 KiB` at `scratch/ps1-perf-iterate/visitor3-high-clean48-vs-clean64/20260519-012105-2153740/summary.json` missed `JCPERF2`. | Promoted as a small allocator-shape speed win. VISITOR3 high improves active loop/target `1075/1043 -> 1075/1044`, overrun `32 -> 31`, blocking/read time `46/54 -> 45/53`, and target speed `97.023% -> 97.116%`; hidden refill stays `3`, reads/due stay `4/3`, pack LBA/sectors stay fixed, and BUILDING2 high/low, VISITOR3 low, WALKSTUF1 high/low, and BUILDING4 low stayed stable in the canary. | Keep the high-only `64 KiB` clean-strip cap. Public rollup improves to `+0.2487%` over target / `99.7549%` target speed; raw signed rollup is about `-0.4683%` / `100.4852%`; bands remain `120` green, `6` yellow, `0` orange, and `0` red. Methodology total remains about `17.15` public over-target points removed and `+12.65` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `building2-high-broad-setup-clean-cap` | Log-only; source restored. | Reopen the broad BUILDING2-high setup-residency swing after B2-low and VISITOR3-high proved clean-strip shaping can avoid allocator cliffs. The binary question was whether a high-only clean cap could let wider B2-high setup coverage run, and whether clean-shaping alone moved the current `1347/1313` yellow row. | Temporarily widened the second B2-high setup segment from `202..242` to `90..242` with an `80 KiB` clean cap, then narrowed to `128..242` with the same cap. After both failed before metrics, restored `202..242` and tested clean caps alone at `80 KiB` and `96 KiB`. Artifacts: broad `90..242` `scratch/ps1-perf-iterate/building2-high-seg90-242-clean80-vnext/20260519-013759-2248092/summary.json`; narrowed `128..242` `scratch/ps1-perf-iterate/building2-high-seg128-242-clean80-vnext/20260519-014040-2263389/summary.json`; clean80 alone `scratch/ps1-perf-iterate/building2-high-clean80-vnext/20260519-014307-2277269/summary.json`; clean96 alone `scratch/ps1-perf-iterate/building2-high-clean96-vnext/20260519-014452-2287513/summary.json`. | Failed/no promotion. `90..242` and `128..242` got past the old allocation cliff but missed `JCPERF2`, so broad setup ownership is still structurally unsafe in the fixed frame window. Clean80 alone reduced blocking/refill `39/16 -> 38/15` but regressed active loop/overrun `1347/34 -> 1348/35`. Clean96 was exact-flat at `1347/1313`, overrun `34`, blocking/refill `39/16`, reads `45`, and due `5`. | Keep the accepted B2-high `3..35` plus `202..242` setup shape with no high-only clean cap. Close broad setup plus clean-cap shaping for B2 high; future work should use generated deadline ownership or pack/upload work reduction rather than pulling more setup coverage forward. |
| 2026-05-19 | `visitor3-low-clean-cap` | Log-only; source restored. | Test whether the clean-strip lever that promoted VISITOR3 high also helps VISITOR3 low, which still carries the largest visible blocking count among yellow rows but has no hidden refill debt. | Temporarily added low-tide VISITOR3 `gFgCleanRectMaxBytes` caps at `64 KiB` and `80 KiB` while keeping setup segments and pack data unchanged. Artifacts: clean64 `scratch/ps1-perf-iterate/visitor3-low-clean64-vnext/20260519-014742-2303679/summary.json`; clean80 `scratch/ps1-perf-iterate/visitor3-low-clean80-vnext/20260519-014918-2313125/summary.json`. | Failed/no promotion. Clean64 was phase-negative, regressing scene/loop/overrun/blocking `1369/1065/26/75 -> 1374/1069/30/79`. Clean80 kept scene/loop/blocking flat but dropped target `1039 -> 1038`, worsening overrun `26 -> 27` with no key metric improvement. | Keep VISITOR3 low without a clean-strip cap. Unlike high tide, low tide is already at the clean-shape knee; remaining low work needs generated scheduler ownership, data-shape/preprocess work, or a different render/upload reduction. |
| 2026-05-19 | `building4-low-clean-cap` | Log-only; source restored. | Test whether BUILDING4 low, the closest remaining yellow row, benefits from low-tide clean-strip shaping before moving into heavier pack/render work. | Temporarily added a BUILDING4 low-only clean cap at `80 KiB`, then tightened it to `64 KiB`. Artifacts: clean80 `scratch/ps1-perf-iterate/building4-low-clean80-vnext/20260519-015337-2337329/summary.json`; clean64 `scratch/ps1-perf-iterate/building4-low-clean64-vnext/20260519-015549-2350035/summary.json`. | Failed/no promotion. Clean80 was exact-flat at `2849/2816`, overrun `33`, blocking/refill `38/31`, reads `30`, and due `1`. Clean64 regressed to `2852/2815`, overrun `37`, blocking/refill `40/32`. | Keep BUILDING4 low on the default `96 KiB` clean cap. The row needs static upload/restore elimination, generated scheduler ownership, or pack-side data-shape work rather than more clean-strip retunes. |
| 2026-05-19 | `walkstuf1-high-clean-cap` | Log-only; source restored. | Test the remaining clean-strip cap candidate: WALKSTUF1 high has default clean shaping while WALKSTUF1 low already carries a strict `48 KiB` cap. | Temporarily added high-only caps at `80 KiB` and `64 KiB`. Artifacts: clean80 `scratch/ps1-perf-iterate/walkstuf1-high-clean80-vnext/20260519-015852-2367384/summary.json`; clean64 `scratch/ps1-perf-iterate/walkstuf1-high-clean64-vnext/20260519-020039-2377785/summary.json`. | Failed/no promotion. Clean80 was exact-flat at `1475/1441`, overrun `34`, blocking/refill `57/13`, reads `44`, and due `10`. Clean64 regressed to `1477/1439`, overrun `38`, blocking/refill `62/15`. | Keep WALKSTUF1 high on default clean shaping. Clean caps are now exhausted for the remaining yellow rows; move to generated scheduler ownership or pack/render data-shape work. |
| 2026-05-19 | `building4-low-local-lz-entry284` | Log-only; pack restored and PS1 rebuilt. | Close the only untested BUILDING4-low local-LZ sector-collapse candidate after entries `30`, `33`, and `270` were already accepted and entry `289` was closed as phase-negative. Entry `284` / source frame `434` was small but isolated, shrinking `8267 -> 8187` bytes and modeling a `5 -> 4` sector entry collapse without changing the fixed pack footprint. | Temporarily rewrote only entry `284` in `generated/ps1/foreground/BUIL4LOW.FG2` with `compact-fgp3-local-lz.py`, preserving file size `1714154`, pack LBA `9126`, and the `233472` byte PS-EXE bucket. Transform summary: `scratch/building4-low-local-lz-entry284-vnext/summary.json`; failed focused gate: `scratch/ps1-perf-iterate/building4-low-local-lz-entry284-vnext/20260519-020711-2414909/summary.json`. The pack was restored from `scratch/building4-low-local-lz-entry284-vnext/BUIL4LOW.before.FG2`, byte-compared clean, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The small encoded-size shift was strongly phase-negative: BUILDING4 low regressed from `2849/2816`, overrun `33`, blocking/refill `38/31`, due `1`, to `2892/2813`, overrun `79`, blocking/refill `49/36`, due `2`; loop reads stayed `30`. | Keep BUILDING4-low local-LZ at the accepted entries `30`, `33`, and `270` only. The remaining local-LZ candidates are now either closed or too phase-negative; B4-low green work needs static upload/restore elimination, generated scheduler ownership, or a different no-decode pack/data-shape change. |
| 2026-05-19 | `walkstuf1-high-window53-55-current` | Log-only; source restored and PS1 rebuilt. | Retest the old WALKSTUF1 high adjacent window-size hazard under the allocator-era layout. The older `53 KiB` source-default probe had failed structurally even though the boot override looked promising, so the current binary check tested both sides of the accepted `54 KiB` default. | Temporarily changed `FG_WALKSTUF1_HIGH_RESIDUAL_WINDOW_BYTES` from `54 KiB` to `53 KiB`, rebuilt, and ran focused W1-high; then changed it to `55 KiB`, rebuilt, and reran the same gate. Artifacts: `53 KiB` `scratch/ps1-perf-iterate/walkstuf1-high-window53-current/20260519-021628-2467453/summary.json`; `55 KiB` `scratch/ps1-perf-iterate/walkstuf1-high-window55-current/20260519-021830-2479291/summary.json`. Source was restored to `54 KiB` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both adjacent defaults are now structurally safe, but both are exact-flat against the current accepted baseline: W1 high stays `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, loop reads `44`, and due `10`; pack LBA and the `233472` byte PS-EXE bucket stay fixed. | Keep the accepted `54 KiB` high residual window. The adjacent scalar window lane is closed on the current allocator-era baseline; W1-high green work still needs generated deadline/refill ownership, pack-side data-shape changes, or render/upload reduction rather than another raw window-size retune. |
| 2026-05-19 | `building4-low-setupseg178-202-current` | Log-only; source restored and PS1 rebuilt. | Test the only distinct BUILDING4-low setup-residency swing left from the current read plan. Direct append ownership for `178..202` was exact-flat, and the previous low setup segment at `262..286` reduced reads but regressed cadence, so this checked whether setup-owned `178..202` coverage could remove held-loop reads without active-loop debt. | Temporarily added a BUILDING4 low-only reusable setup segment covering sectors `178..202` while keeping the existing BUILDING4 high `264..288` setup segment unchanged. Artifact: `scratch/ps1-perf-iterate/building4-low-setupseg178-202-current/20260519-022157-2499377/summary.json`. | Failed/no promotion. The segment reduced loop reads `30 -> 29`, but was phase-negative: BUILDING4 low regressed from `2849/2816` to `2857/2818`, overrun `33 -> 39`, blocking/refill `38/31 -> 45/39`, and scene VB `3121 -> 3139`; no key metric improved. | Close BUILDING4-low setup residency for `178..202`. Both modeled low-tide setup windows are now negative, so the remaining B4-low path should prioritize static upload/restore elimination, no-decode data-shape changes, or generated scheduler ownership rather than more raw setup coverage. |
| 2026-05-19 | `fg-setup-prime-bg-upload` | Log-only; source restored and PS1 rebuilt. | Big-swing test for all remaining yellow rows: move the forced full first-frame clean-background upload out of active playback and into setup, then clear dirty state before `loop_start`. The hypothesis was that the residue fix could stay intact while removing a full-frame DMA charge from the first active FG2 frame. | Temporarily added `grPrimeCleanBackgroundForScene()` and replaced the pre-loop `grForceFullRedrawNextFrame()` call with setup-time full upload plus dirty-state clear. Tested the conservative VSync-before-upload form, then the binary opposite no-extra-VSync form after the first one moved CD/display phase badly. Artifacts: VSync form `scratch/ps1-perf-iterate/setup-prime-bg-upload-vnext-focused/20260519-024213-2613456/summary.json`; no-extra-VSync form `scratch/ps1-perf-iterate/setup-prime-bg-upload-novsync-vnext-focused/20260519-024402-2624219/summary.json`. | Failed/no promotion. The VSync form regressed VISITOR3 high from `1389/1075/1044`, overrun `31`, blocking `45`, to `1392/1077/1042`, overrun `35`, blocking `48`. The no-extra-VSync form was exact-flat on speed-bearing metrics (`1389/1075/1044`, overrun `31`, blocking `45`, refill `3`, reads/due `4/3`) and therefore failed `--require-improvement`. | Close setup-time full-background priming as a speed lever. The active-loop full upload is either phase-bearing or already amortized by the current accounting; remaining yellow rows still need generated deadline/refill ownership, no-decode data-shape changes, or targeted render/upload work that moves per-frame cost rather than scene-transition cost. |
| 2026-05-19 | `walkstuf1-high-prepare-row-cluster` | Log-only; source restored and PS1 rebuilt. | Close the W1-high scalar prepare/read-row cluster after the accepted W1-low prepare-before-window rule suggested a possible high-tide mirror. The binary question was whether the high tide could take the same prepare-first blocking win if paired with row ownership around the adjacent hot reads. | Temporarily enabled prepare-before-window for WALKSTUF1 high and swept combined/direct row variants around `99..115`, `178..194`, and `180..196`. Artifacts: prepare plus `{99,115,0}` `scratch/ps1-perf-iterate/walkstuf1-high-prepare-rg99-combo-vnext/20260519-024839-2650843/summary.json`; raw `{180,196,0}` `scratch/ps1-perf-iterate/walkstuf1-high-rg180-196-vnext/20260519-025055-2664149/summary.json`; raw `{178,194,0}` `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-vnext/20260519-025253-2675755/summary.json`; prepare plus `{178,194,0}` `scratch/ps1-perf-iterate/walkstuf1-high-prepare-rg178-194-vnext/20260519-025453-2687584/summary.json`; prepare plus `{180,196,0}` `scratch/ps1-perf-iterate/walkstuf1-high-prepare-rg180-196-vnext/20260519-025655-2699583/summary.json`. | Failed/no promotion. The prepare path consistently improved visible blocking `57 -> 48` and due `10 -> 7`, with loop `1475 -> 1474`, but it moved hidden refill `13 -> 17` and often lowered target `1441 -> 1440`; combined row variants did not offset the refill debt. Raw `{180,196,0}` was exact-flat, and raw `{178,194,0}` only reduced work volume (`44 -> 42` reads) with no speed-bearing metric movement, so neither met the current speed objective. | Restore the accepted W1-high table and keep prepare-before-window low-tide-only. This scalar lane is closed for W1 high: it has real blocking signal, but high tide converts it into refill debt. Reopen only with generated deadline/refill ownership that can prove the prepare work lands outside the hidden-refill boundary, or with pack/render work reduction instead of more appended rows. |
| 2026-05-19 | `building4-low-narrow-upload-runtime` | Log-only; source restored and PS1 rebuilt. | Test the B4-low static/upload big swing directly in the renderer. The current B4-low row still uploads about `22.8 MiB` through full tile-width dirty bands; the host x-band analyzer showed large theoretical byte savings, so this checked whether runtime packing into the existing SCR stream scratch buffer could beat the DMA/phase cost without pack-format work. | Temporarily added a BUILDING4 low-only `grSetNarrowDirtyUpload()` mode. The first variant packed every narrow dirty band into `gScrStreamRows` before `LoadImage`; the second binary variant limited this to bands `<=96 px` wide. Artifacts: full narrow path `scratch/ps1-perf-iterate/building4-low-narrow-upload-vnext/20260519-030506-2745826/summary.json`; `<=96 px` path `scratch/ps1-perf-iterate/building4-low-narrow-upload96-vnext/20260519-030754-2761906/summary.json`; host analyzer CSV `scratch/building4-low-preprocess-current-20260519-030245.csv`. | Failed/no promotion. Both variants timed out before `JCPERF2` and crossed the PS-EXE bucket `233472 -> 235520`; `grDrawBackground` grew by roughly `+776..+804` bytes. The emulator stayed alive at about `60 VPS`, so this is a structural performance miss rather than a crash: per-frame CPU packing/code-size cost overwhelms any possible upload-byte reduction before the scene completes. | Close runtime scratch-packed x-band upload as a green-conversion path. The host byte signal is real, but B4-low needs a pack-emitted/precomputed upload representation or a smaller code-neutral renderer primitive; copying tile RAM into narrow upload scratch during active playback is too expensive and too large. |
| 2026-05-19 | `visitor3-high-frames58-63-paidgap` | Log-only; pack restored. | Test a no-code VISITOR3 high pack-shape swing after the preprocessing/pack scan found a `13 KiB` already-paid gap inside the accepted high setup segment `228..262`. The binary question was whether moving early active frames `58..63` out of sectors `36..43` and into that resident gap could reduce early CD pressure without moving pack LBA, changing source, or growing the executable. | Temporarily rewrote `generated/ps1/foreground/VISITOR3.FG2` so entries `58..63` moved byte-for-byte from offsets `75129..87997` into the paid gap `505315..518183`, leaving `171` spare bytes before the next payload. Transform summary: `scratch/visitor3-high-frames58-63-paidgap-vnext/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-frames58-63-paidgap-vnext/20260519-032427-2855361/summary.json`. The pack was restored from `scratch/visitor3-high-frames58-63-paidgap-vnext/VISITOR3.before.FG2`. | Failed/no promotion. The relocation was phase-negative: VISITOR3 high regressed scene/loop `1389/1075 -> 1405/1091`, overrun `31 -> 50`, and blocking `45 -> 61`; target moved `1044 -> 1041`, loop reads moved `4 -> 8`, and due misses moved `3 -> 4`, while pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close raw VISITOR3 high early-frame relocation into the setup paid gap. The gap is physically real, but these payloads need their original active-sector phase; future VISITOR3 high pack work should be generated deadline/refill ownership, a different no-decode data-shape change, or render/upload work reduction rather than relocating frames `58..63` into the current setup gap. |
| 2026-05-19 | `walkstuf1-high-prepare-owner-lockout` | Log-only; source restored and PS1 rebuilt. | Try a generated scheduler-owner version of the previously promising W1-high prepare-before-window idea. The older blind high-tide prepare path improved loop/blocking/due but failed on hidden refill, so this tested whether stricter ownership could keep the win without letting speculative CD refill steal the prepared-frame slack. | Temporarily let W1-high prepare before a window refill only at the staged-frame prepare slot, first with a conservative `5`-VBlank threshold, then with the real `4`-VBlank prepare threshold plus a high-only lockout that suppresses window refill while a prepared frame is waiting to present. A companion `{180,196,0}` read group tested the best new scheduler-owned candidate after the owner-lockout pass. Artifacts: threshold `5` `scratch/ps1-perf-iterate/walkstuf1-high-prep-before-window-slack5-vnext/20260519-032807-2876599/summary.json`; owner-lockout `scratch/ps1-perf-iterate/walkstuf1-high-prep-owner-lockout-vnext/20260519-033023-2889894/summary.json`; owner plus `{180,196}` `scratch/ps1-perf-iterate/walkstuf1-high-prep-owner-rg180-196-vnext/20260519-033222-2901801/summary.json`. | Failed/no promotion. The `5`-VBlank threshold no-opped exactly at baseline `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, reads `44`, due `10`. The `4`-VBlank owner-lockout kept the real signal, improving scene/loop `1811/1475 -> 1810/1474`, blocking `57 -> 48`, and due `10 -> 7`, but still regressed hidden refill `13 -> 17`; adding `{180,196}` reduced reads `45 -> 43` versus the owner pass but left the same refill failure. | Keep prepare-before-window low-tide-only. W1-high generated ownership needs a deeper refill-deadline model than this local lockout; the simple staged-prepare owner still converts visible CD wins into hidden-refill debt. Future W1-high work should target pack/data-shape or a scheduler change that can prove refill-safe ownership before presentation. |
| 2026-05-19 | `building2-high-frame90-d4-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the strongest current B2-high previous-frame delta candidate after the targeted hotspot scan found frame `90` could shrink from `8849` to `4749` bytes with only `50` D4 commands and a modeled `5 -> 3` sector drop. This was distinct from the old trim-tail frame90 miss because it used the runtime D4 decoder and the current allocator-era pack layout. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` frame `90` as a previous-frame D4 payload against frame `89` and added high-tide frame `90` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/building2-high-frame90-d4-current-vnext/summary.json`; focused gate: `scratch/ps1-perf-iterate/building2-high-frame90-d4-current-vnext/20260519-034408-2968713/summary.json`. The pack was restored from `scratch/building2-high-frame90-d4-current-vnext/BUILDING2.before.FG2`. | Failed/no promotion. Decode/phase cost dominated the sector drop: B2 high regressed scene/loop `1621/1347 -> 1627/1353`, target `1313 -> 1309`, overrun `34 -> 44`, blocking `39 -> 51`, refill `16 -> 21`, due `5 -> 7`, and reads only moved `45 -> 44`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close current-baseline B2-high frame90 D4. Adjacent B2-high D4 remains a bad trade without generated deadline ownership; future B2-high work should avoid runtime decode additions unless a validator proves the decode lands outside the visible/refill boundary. |
| 2026-05-19 | `building2-high-rg158-174-current` | Log-only; source restored and PS1 rebuilt. | Close the distinct B2-high scalar cluster left open by the previous `249..265` rejection note. The current read plan still ranked `158..174` as a multi-read candidate, so this bounded check tested whether that separate phase pocket could convert read-count savings on top of the accepted `83..95`, guarded `271..287`, and `315..327` rows. | Temporarily inserted `{158,174,0}` into `kBuilding2HighReadGroups12[]`, rebuilt, and ran focused B2 high against the current allocator-era under-green baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg158-174-vnext/20260519-035106-3008348/summary.json`. | Failed/no promotion. The row did fire and reduced loop reads `45 -> 42`, but it regressed all speed-bearing metrics: scene `1621 -> 1624`, loop `1347 -> 1351`, overrun `34 -> 38`, and blocking `39 -> 41`, with hidden refill flat at `16` and due misses flat at `5`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close B2-high `158..174` as another phase-negative hand-authored read group. The pattern is now strong enough that remaining B2-high scalar rows should not be retried unless generated deadline/refill ownership can prove the grouped append lands outside the visible cadence boundary. |
| 2026-05-19 | `fg-second-stage-prefetch-current` | Log-only; source restored and PS1 rebuilt. | Big-swing scheduler test for the remaining under-99 rows: add a third frame buffer and let held-frame slack stage `N+2` while `N+1` is already staged, so long holds can advance one frame farther ahead than the accepted stage1/window scheduler. | Temporarily added `prefetchFrameBuffer2`, second staged-entry bookkeeping, three-buffer rotation on staged consume, and a scene-gated lookahead staging pass. First tested VISITOR3 high with the gate enabled for all yellow families, then removed VISITOR3 from the gate and tested BUILDING2 high/low, WALKSTUF1 high/low, and BUILDING4 low. Artifacts: VISITOR3 high `scratch/ps1-perf-iterate/visitor3-high-second-stage-prefetch/20260519-041102-3119288/summary.json`; no-VISITOR3 family gate `scratch/ps1-perf-iterate/yellow-second-stage-no-visitor3/20260519-041309-3131815/summary.json`. | Failed/no promotion. VISITOR3 high improved blocking `45 -> 40` but regressed scene/loop `1389/1075 -> 1403/1089` and hidden refill `3 -> 26`. The no-VISITOR3 gate had the same tradeoff: B2 high blocking/refill improved `39/16 -> 37/13` but loop regressed `1347 -> 1349`; W1 high improved blocking `57 -> 52` but regressed loop/refill `1475/13 -> 1476/25`; W1 low improved blocking `43 -> 41` but regressed loop/refill `1473/11 -> 1476/26`; B4 low regressed across timing and CD. B2 low improved, but it is already green and not enough to justify under-99 regressions. | Close generic second-stage frame prefetch on this baseline. The extra lookahead can move visible blocking, but it creates hidden refill and code/layout debt in the yellow rows. Reopen only as generated per-scene/per-frame ownership with proven slack windows, not as a broad third-buffer scheduler rule. |
| 2026-05-19 | `building2-high-entries92-94-95-trim-current` | Log-only; pack restored and PS1 rebuilt. | Test the next no-code BUILDING2 high pack-shape lane after the frame90/91 sector-collapse misses: entries `92`, `94`, and `95` can trim draw tails in place while preserving entry offsets, file size, pack LBA, and executable layout. The batch saved enough active payload to answer whether a larger adjacent no-decode cut could become visible where single entries did not. | Rewrote only `generated/ps1/foreground/BUILDING2.FG2` entries `92`, `94`, and `95` with `compact-fgp3-trim-draw-tails.py --preserve-offsets --copy-unparseable`, shrinking active payload `669408 -> 663590` and modeled entry sector counts `5 -> 4`, `6 -> 5`, and `6 -> 5`. Transform summary: `scratch/b2-preserve-trim-test-summary.json`; focused gate: `scratch/ps1-perf-iterate/building2-high-entries92-94-95-trim-current/20260519-042851-3220211/summary.json`. The pack was restored from `scratch/b2-high-entries92-94-95-trim-current/BUILDING2.before.FG2` and the restored PS1 build passed. | Failed/no promotion. The batch was safe but exact-flat: BUILDING2 high stayed scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, loop reads `45`, and due `5`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. The strict gate failed only because no key speed metric improved. | Close B2-high entries `92`, `94`, and `95` as a current-baseline no-decode trim batch. These adjacent sector-boundary savings are now proven inert unless generated deadline ownership can make the saved bytes alter an actual visible read boundary. |
| 2026-05-19 | `walkstuf1-high-entry136-trim-current` | Log-only; pack restored and PS1 rebuilt. | Test the remaining WALKSTUF1 high no-code sector-collapse candidate from the current draw-tail scan. Entry `136` / source frame `244` could shrink in place without changing offsets, file size, LBA, source, or executable layout, and its modeled entry coverage dropped from three sectors to two. | Rewrote only `generated/ps1/foreground/WALKSTUF1.FG2` entry `136` with `compact-fgp3-trim-draw-tails.py --preserve-offsets --copy-unparseable --frames 136`, shrinking active payload `859666 -> 858500` and payload size `3762 -> 2596`. Transform summary: `scratch/w1h-preserve-trim-136-summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-entry136-trim-current/20260519-043124-3235485/summary.json`. The pack was restored from `scratch/w1h-entry136-trim-current/WALKSTUF1.before.FG2` and the restored PS1 build passed. | Failed/no promotion. The trim was safe but exact-flat: WALKSTUF1 high stayed scene/loop/target `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, loop reads `44`, and due `10`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. The strict gate failed only because no key speed metric improved. | Close W1-high entry `136` / source `244` as an isolated draw-tail physical trim. Current isolated no-code sector cuts are not changing the W1-high visible schedule; remaining W1-high speed work should target generated deadline/refill ownership or larger render/upload data-shape changes. |
| 2026-05-19 | `building4-low-dup-alias-within32` | Log-only; pack restored and PS1 rebuilt. | Try a no-code duplicate-payload aliasing swing on BUILDING4 low after isolated sector trims and scalar read groups stalled. The current pack has repeated payloads close enough to fit inside the 32 KiB stream window, so this tested whether aliasing later identical entries to earlier physical payloads could reduce due reads without source/runtime changes. | Rewrote only the `BUIL4LOW.FG2` frame table for `31` duplicate entries whose identical source payload was within `32 KiB`, preserving file size, pack LBA, payload bytes, and executable layout. Transform summary: `scratch/b4low-dup-alias-within32-summary.json`; focused gate: `scratch/ps1-perf-iterate/building4-low-dup-alias-within32/20260519-043633-3264997/summary.json`. The pack was restored from `scratch/b4low-dup-alias-within32/BUIL4LOW.before.FG2` and the restored PS1 build passed. | Failed/no promotion. The alias batch was strongly phase-negative: BUILDING4 low regressed scene/loop `3121/2849 -> 3151/2879`, target `2816 -> 2811`, overrun `33 -> 68`, blocking `38 -> 74`, hidden refill `31 -> 59`, reads `30 -> 36`, and due `1 -> 2`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close broad same-window duplicate aliasing for B4-low. The duplicate bytes are real, but moving entry offsets backward changes the stream direction/cadence enough to add reads and visible delay. Future alias attempts need a planner that proves per-entry stream-window residency and forward progress before timing. |
| 2026-05-19 | `indexed8-wordpack-vnext` | Log-only; source restored and PS1 rebuilt. | Test a renderer-side big swing for the two remaining indexed8 yellow rows after scalar read groups, local-LZ, clean caps, and isolated no-code trims stalled. The binary question was whether writing two indexed8 palette-expanded pixels per 32-bit store inside `grCompositeIndexed8OpaqueRun()` could reduce WALKSTUF1 compose cost enough to offset any code-size/layout movement. | Temporarily changed the indexed8 opaque-run hot loop to align the destination and emit two 16-bit pixels per 32-bit store, then ran a strict paired WALKSTUF1 high/low gate. Artifact: `scratch/ps1-perf-iterate/indexed8-wordpack-vnext/20260519-044552-3317394/summary.json`. Source was restored to the scalar loop and `./scripts/build-ps1.sh` passed afterward, returning `build-ps1/jcreborn.exe` to `228K`. | Failed/no promotion. The code-size/layout cost dominated. W1 high moved scene/loop `1811/1475 -> 1810/1474`, but target dropped `1441 -> 1439`, overrun regressed `34 -> 35`, blocking/refill regressed `57/13 -> 62/15`, and the PS-EXE bucket crossed `233472 -> 235520` with pack LBA `24891 -> 24892`. W1 low regressed `1807/1473/1444 -> 1812/1478/1445`, overrun `29 -> 33`, blocking `43 -> 45`, reads `36 -> 37`, and pack LBA `25641 -> 25642`. | Close generic indexed8 word-packed compositing as a current-baseline green path. The render hot loop is not free enough to absorb the executable bucket and timing/layout shift; future indexed8 work should be pack-emitted/no-code data shape, generated scheduler ownership, or a smaller code-neutral renderer primitive rather than broad 32-bit stores in the shared compositer. |
| 2026-05-19 | `no-heartbeat-vnext` | Log-only; source restored and PS1 rebuilt. | Test a broad diagnostic-overhead swing across every remaining yellow row. The playback loop emits `JCHB` heartbeat lines every ~120 VBlanks; the binary question was whether compiling that diagnostic cadence out by default would reduce measured active-loop cost or code layout enough to move under-99 rows. | Temporarily wrapped the `JCHB` loop variables, `VSync(-1)` heartbeat check, and `printf` behind a disabled `FG_HEARTBEAT_LOGS` macro, shrinking `foregroundPilotPlay` by `200` bytes while keeping the PS-EXE sector bucket fixed at `233472`. Six-row gate: `scratch/ps1-perf-iterate/no-heartbeat-vnext/20260519-045415-3365343/summary.json`. Source was restored to the accepted heartbeat path and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Four rows were exact-flat: B2 high `1621/1347/1313`, W1 high `1811/1475/1441`, W1 low `1807/1473/1444`, and B4 low `3121/2849/2816`. VISITOR3 high regressed `1389/1075/1044 -> 1396/1082/1042`, overrun `31 -> 40`, blocking `45 -> 51`, refill `3 -> 6`; VISITOR3 low regressed `1369/1065/1039 -> 1370/1066/1039`, overrun `26 -> 27`. | Keep the heartbeat cadence in the speed binary. The diagnostic serial text is not a free removal because its `VSync(-1)` timing check participates in the current scheduler/display phase; code-size reduction alone is not enough. Future diagnostic overhead work needs a paired replacement cadence check or a release-speed methodology change, not a blind heartbeat compile-out. |
| 2026-05-19 | `heartbeat-poll-no-print-vnext` | Log-only; source restored and PS1 rebuilt. | Split the failed heartbeat removal into the binary opposite: preserve the `VSync(-1)` heartbeat cadence and only compile out the serial `printf` payload/string. This tested whether the cadence was required but log text overhead or call-site size was still removable. | Temporarily added disabled `FG_HEARTBEAT_PRINTF`, kept the heartbeat `VSync(-1)` poll/update in the loop, and removed only the `printf` path from the speed binary. The focused VISITOR3 gate artifact is `scratch/ps1-perf-iterate/heartbeat-poll-no-print-vnext/20260519-050542-3431342/summary.json`. Source was restored to the accepted heartbeat path and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The smaller layout change still reproduced the VISITOR3 regressions: high regressed `1389/1075/1044 -> 1396/1082/1042`, overrun `31 -> 40`, blocking `45 -> 51`, refill `3 -> 6`; low regressed `1369/1065/1039 -> 1370/1066/1039`, overrun `26 -> 27`. The PS-EXE bucket stayed fixed but `foregroundPilotPlay` shrank by `164` bytes and shifted downstream hot symbols by `-164` bytes. | Close heartbeat print-only removal. On this layout, VISITOR3 is sensitive to the heartbeat code footprint itself, not just the cadence or serial text. Do not spend more time shrinking this loop until hot-symbol padding or release-speed methodology can isolate code-size effects from scheduler phase. |
| 2026-05-19 | `visitor3-low-rg86-98-vnext` | Log-only; source restored and PS1 rebuilt. | Test the remaining distinct low-risk VISITOR3-low grouped-append candidate from the current clean-relief read plan. Earlier sweeps closed `9..33`, `9..25`, `92..108`, and the late `248` family, but `{86,98}` had a different early-gap shape and still looked fireable with one saved read. | Temporarily added a one-row VISITOR3 low-only read-group table `{86,98,0}` and routed it only when the low-tide VISITOR3 stream window used the default size. Focused artifact: `scratch/ps1-perf-iterate/visitor3-low-rg86-98-vnext/20260519-051050-3460997/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row fired and reduced loop reads `18 -> 17`, but it was visibly phase-negative: VISITOR3 low regressed scene/loop/target `1369/1065/1039 -> 1374/1070/1041`, overrun `26 -> 29`, blocking `75 -> 79`, with hidden refill still `0`; the source branch grew `foregroundPilotPlay` by `20` bytes and shifted following hot symbols by `+20`. | Close V3-low `{86,98}` as another hand-authored grouped-append miss. Even low-risk one-read savings are not enough when the visible cadence shifts; remaining VISITOR3-low work needs generated deadline ownership or pack/data-shape changes rather than more scalar read-group rows. |
| 2026-05-19 | `heartbeat-noprint-padded-vnext` | Log-only; source restored and PS1 rebuilt. | Try a layout-neutralized heartbeat variant after the no-print shrink proved VISITOR3 phase-sensitive. The hypothesis was that preserving the call-site text footprint with a skipped NOP sled might keep hot-code phase while removing serial output cost. | Temporarily compiled out the heartbeat `printf` and inserted a branch-over `39`-NOP sled at the same site. Focused VISITOR3 artifact: `scratch/ps1-perf-iterate/heartbeat-noprint-padded-vnext/20260519-051440-3483097/summary.json`. Source was restored to the accepted heartbeat path and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The padding still changed `foregroundPilotPlay` by `+20` bytes and repeated the same VISITOR3 failure: high regressed `1389/1075/1044 -> 1396/1082/1042`, overrun `31 -> 40`, blocking `45 -> 51`, refill `3 -> 6`; low regressed `1369/1065/1039 -> 1370/1066/1039`, overrun `26 -> 27`. | Close heartbeat/layout surgery. These rows are too code-phase-sensitive for heartbeat changes to be a safe optimization path; leave the accepted diagnostic heartbeat intact and target pack/data-shape, scheduler ownership, or measured render/upload work instead. |
| 2026-05-19 | `walkstuf1-high-prep-owner-refill-guards` | Log-only; source restored and PS1 rebuilt. | Follow up the W1-high prepare-before-window near miss with both sides of the refill-safety binary. The owner-lockout path had real visible signal (`loop 1475 -> 1474`, blocking `57 -> 48`, due `10 -> 7`) but failed because hidden refill rose `13 -> 17`; this pass tested whether tighter high-tide refill sizing, higher slack gating, or extra read-group assists could keep the visible win without hidden debt. | Temporarily allowed WALKSTUF1 high to prepare before window refill, added a high-only `48 KiB` low-slack window cap, then tested a `6`-VBlank high-tide window minimum and a companion `{180,196,0}` plus `{372,388,0}` read-group assist. Artifacts: tight cap `scratch/ps1-perf-iterate/walkstuf1-high-prep-owner-tight48-vnext/20260519-052520-3543711/summary.json`; min-slack `6` `scratch/ps1-perf-iterate/walkstuf1-high-prep-owner-min6-tight48-vnext/20260519-052908-3565569/summary.json`; read-group assist `scratch/ps1-perf-iterate/walkstuf1-high-prep-owner-rg180-372-tight48-vnext/20260519-053115-3578068/summary.json`. | Failed/no promotion. The `48 KiB` cap repeated the original tradeoff: scene/loop `1811/1475 -> 1810/1474`, blocking `57 -> 48`, due `10 -> 7`, but refill still `13 -> 17`. Raising the high-tide slack minimum to `6` eliminated refill overrun but regressed visible timing badly: loop `1475 -> 1483`, overrun `34 -> 39`, blocking `57 -> 102`, due `10 -> 21`. Adding `{180,196}` and `{372,388}` reduced loop reads `44 -> 41`, but regressed scene/loop `1811/1475 -> 1813/1477`, overrun `34 -> 36`, and refill stayed `17`. | Close W1-high prepare-before-window plus local refill guards for now. The visible-prep ownership signal is real, but local thresholds and scalar read groups cannot prove refill-safe ownership; reopening this lane needs a generated per-frame/deadline model that decides when to prepare and when to suppress/refill windows without stealing hidden slack. Public rollup remains `+0.2487%` over target / `99.7549%` target speed; bands remain `120` green, `6` yellow, `0` orange, and `0` red. |
| 2026-05-19 | `visitor3-high-setupprime-cleanrelief-sweep` | Log-only; source restored and PS1 rebuilt. | Test a big-swing VISITOR3 high clean-memory-relief variant: allow setup-prime residency even when clean relief keeps the stream window, then find the largest setup-prime window that still leaves enough allocator headroom for clean-rect snapshots. | Temporarily allowed `visitor3` through the non-default-window setup-prime guard and skipped the clean-relief setup-prime zeroing only for `visitor3`. Swept high setup-prime windows at `320`, `304`, `272`, `192`, `184`, `128`, and `96` KiB. Artifacts: `scratch/ps1-perf-iterate/visitor3-high-setupprime-cleanrelief-policy-vnext/20260519-054328-3647202/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupprime304-cleanrelief-vnext/20260519-054610-3662806/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupprime272-cleanrelief-vnext/20260519-054847-3677898/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupprime192-cleanrelief-vnext/20260519-055158-3696543/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupprime184-cleanrelief-vnext/20260519-055438-3711974/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupprime128-cleanrelief-vnext/20260519-055810-3732099/summary.json`, and `scratch/ps1-perf-iterate/visitor3-high-setupprime96-cleanrelief-vnext/20260519-060105-3748925/summary.json`. | Failed/no promotion. The `320`, `304`, `272`, `192`, `184`, and `128` KiB variants structurally halted before `JCPERF2` during clean-rect CACHE allocation; the failures moved from raw budget exhaustion to contiguous-block pressure as the prime shrank. The `96` KiB variant completed but regressed VISITOR3 high scene/loop/target `1389/1075/1044 -> 1410/1090/1042`, overrun `31 -> 48`, blocking `45 -> 62`, loop reads stayed `4`, and due misses rose `3 -> 4`. | Restore the accepted clean-relief behavior: non-default stream windows skip setup-prime and clean-relief windows zero `setupPrimeWindowBytes`. Close V3-high setup-prime under clean relief on this baseline; larger primes starve clean-rect residency and the smallest viable prime shifts visible phase negative instead of removing loop reads. Public rollup remains `+0.2487%` over target / `99.7549%` target speed; bands remain `120` green, `6` yellow, `0` orange, and `0` red. |
| 2026-05-19 | `visitor3-high-cache-budget-prime184` | Log-only; source restored and PS1 rebuilt. | Follow the setup-prime clean-relief allocator cliff with the direct allocator-budget binary: either enlarge CACHE enough to fit the V3-high `184 KiB` prime plus clean-rect spill, or rebalance a small amount of fixed total region space from TRANSIENT to CACHE. | Tested total-region CACHE bumps to `768 KiB` and `704 KiB`, then fixed-total rebalances `704/704 KiB` and `648/760 KiB` for CACHE/TRANSIENT while keeping the V3-high prime184 clean-relief route enabled. Artifacts: total `768` `scratch/ps1-perf-iterate/visitor3-high-cache768-setupprime184-vnext/20260519-060550-3776090/summary.json`; total `704` `scratch/ps1-perf-iterate/visitor3-high-cache704-setupprime184-vnext/20260519-060846-3792994/summary.json`; rebalance `704/704` `scratch/ps1-perf-iterate/visitor3-high-cache704-trans704-setupprime184-vnext/20260519-061119-3807771/summary.json`; rebalance `648/760` `scratch/ps1-perf-iterate/visitor3-high-cache648-trans760-setupprime184-vnext/20260519-061412-3824374/summary.json`. | Failed/no promotion. Enlarging total region failed at boot: libc could not allocate `1605632` or `1540096` bytes for the region buffer. The `704/704` fixed-total rebalance booted but starved TRANSIENT before the V3 setup segment (`req=155648 have=103420`). The minimal `648/760` rebalance preserved TRANSIENT but still failed clean-rect CACHE allocation (`req=60800 have=73604`) due contiguous-block pressure. | Keep allocator budgets at CACHE `640 KiB` / TRANSIENT `768 KiB` and keep V3 setup-prime suppressed under clean relief. The remaining V3-high residency win cannot be unlocked by simple budget knobs; future allocator work needs targeted lifetime changes, chunk routing, or compaction/defrag semantics rather than resizing this static split. |

| 2026-05-19 | `visitor3-low-thirdseg206-234-current` | Log-only; source restored and PS1 rebuilt. | Test the smallest unclosed VISITOR3-low third-segment extension after the accepted frame138 raw-gap promotion. The retained low segment ended at `206..232`; this checked whether adding two more sectors to `206..234` could remove due-read or blocking pressure without the allocator cliff seen in broader late-residency attempts. | Temporarily changed `FG_VISITOR3_LOW_SETUP_SEGMENT3_BYTES` from `26` to `28` sectors and ran a focused VISITOR3-low strict gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/visitor3-low-thirdseg206-234-vnext/20260519-062404-3880852/summary.json`. | Failed/no promotion. VISITOR3 low regressed scene/loop `1369/1065 -> 1371/1067`, target shifted `1039 -> 1041`, blocking `75 -> 76`, hidden read time `22 -> 19`, loop reads stayed `18`, due stayed `14`, and no key metric improved. | Restore and keep the accepted `206..232` third segment. The extra two sectors add setup residency but shift visible cadence negative; remaining VISITOR3-low work needs generated deadline ownership or pack/data-shape changes, not more simple retained setup bytes. |

| 2026-05-19 | `walkstuf1-low-trim-preserve-current` | Log-only; pack restored and PS1 rebuilt. | Test a code-neutral WALKSTUF1-low pack-shape swing after local-LZ decode variants failed. The current transform scan still showed `13` offset-preserving draw-tail trims with no trimmed pixels, so this checked whether no-decode sector cuts could convert the remaining W1-low yellow gap without source/layout changes. | Rewrote `generated/ps1/foreground/WALK1LOW.FG2` in place with `compact-fgp3-trim-draw-tails.py --preserve-offsets --copy-unparseable`, preserving file size `1535263`, pack LBA, payload offsets, and the `233472` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-low-trim-preserve-current/summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-trim-preserve-current/20260519-063123-3922866/summary.json`. | Failed/no promotion. Active payload dropped `790208 -> 759889`, but timing moved the wrong way: W1 low stayed at loop `1473` while target dropped `1444 -> 1441`, overrun worsened `29 -> 32`, blocking `43 -> 45`, refill `11 -> 16`, reads rose `36 -> 40`, and due moved `5 -> 7`. | Restore the accepted W1-low pack. Offset-preserving no-decode draw-tail trimming is not safe for W1-low green conversion on this baseline; the byte savings perturb target/read cadence instead of reducing loop time. Remaining W1-low work needs generated deadline ownership or a new pack representation that preserves cadence while reducing runtime work. |

| 2026-05-19 | `walkstuf1-high-trim-55-57-current` | Log-only; pack restored and PS1 rebuilt. | Test the remaining WALKSTUF1-high no-code draw-tail trim family after local-LZ for entries `55..57` failed and entry `136` was already exact-flat. This variant preserved payload offsets, file size, pack LBA, and executable layout while shrinking only the selected physical payload tails. | Rewrote `generated/ps1/foreground/WALKSTUF1.FG2` entries `55`, `56`, and `57` with `compact-fgp3-trim-draw-tails.py --preserve-offsets --copy-unparseable --frames 55,56,57`, preserving file size `1535263` and the `233472` byte PS-EXE bucket. Transform summary: `scratch/walkstuf1-high-trim-55-57-current/summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-trim-55-57-current/20260519-063544-3947850/summary.json`. | Failed/no promotion. Active payload dropped `859666 -> 848762`, but timing repeated the local-LZ cadence loss: W1 high stayed at loop `1475` while target dropped `1441 -> 1433`, overrun worsened `34 -> 42`, blocking `57 -> 71`, refill `13 -> 24`, reads `44 -> 49`, and due stayed `10`. | Restore the accepted W1-high pack. Close the `55..57` no-decode trim lane on this baseline; the physical byte savings are real but still perturb stream/target cadence instead of reducing active-loop time. Remaining W1-high work needs generated deadline/refill ownership or a pack representation that preserves original cadence while reducing runtime work. |

| 2026-05-19 | `walkstuf1-low-rg155-179-current` | Log-only; source restored and PS1 rebuilt. | Test the strongest still-open WALKSTUF1-low generated-read candidate from the current read plan after the early `148..164` carryover and no-decode trim lanes failed. The `155..179` row was a 24-sector grouped append with modeled four-read savings, no partial touch, and low-visible-risk planner classification. | Temporarily inserted `{155,179,0}` after the accepted low-tide `{91,107,0}` row in `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran a strict focused W1-low gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg155-179-vnext/20260519-064412-3995675/summary.json`. | Failed/no promotion. The row reduced loop reads `36 -> 33`, but it was strongly phase-negative: scene/loop `1807/1473 -> 1815/1481`, overrun `29 -> 37`, blocking `43 -> 51`, hidden refill `11 -> 15`, due stayed `5`, and pack LBA plus the `233472` byte PS-EXE bucket stayed fixed. | Close W1-low `155..179` as a direct grouped-append row. The read-count savings are real but land in unsafe visible/refill cadence; remaining W1-low work should avoid blind scalar groups and move to generated deadline ownership or pack/render data-shape changes. |
| 2026-05-19 | `walkstuf1-low-rg155-167-current` | Log-only; source restored and PS1 rebuilt. | Test the narrower current-baseline W1-low scheduler candidate after `{155..179}` failed. The planner marked `{155..167}` as append-start fireable at the observed sector `155`, with two modeled saved reads and less overread than the broad row, so this checked whether the smaller span could keep cadence while converting the same cluster. | Temporarily inserted `{155,167,0}` after the accepted low-tide `{91,107,0}` row in `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran a strict focused W1-low gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg155-167-vnext/20260519-065446-4055466/summary.json`. | Failed/no promotion. The row reduced loop reads `36 -> 33` and due misses `5 -> 4`, and blocking stayed flat at `43`, but active timing regressed: scene/loop `1807/1473 -> 1814/1480`, target `1444 -> 1443`, overrun `29 -> 37`, and hidden refill `11 -> 15`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-low `155..167` as a direct grouped-append row. Even the narrowed cluster steals hidden/refill cadence; future W1-low work in this sector family needs generated deadline/refill ownership or a pack representation that removes the read without changing the append phase. |
| 2026-05-19 | `walkstuf1-low-rg195-211-current` | Log-only; source restored and PS1 rebuilt. | Test the current-baseline balanced W1-low candidate after `{155..167}` and `{155..179}` proved the early cluster steals refill. The planner ranked `{195..211}` as append-start fireable with long internal slack and one modeled saved read. | Temporarily inserted `{195,211,0}` after `{91,107,0}` in `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran a strict focused W1-low gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg195-211-current/20260519-065826-4076699/summary.json`. | Failed/no promotion. The row regressed scene/loop `1807/1473 -> 1808/1474`, target `1444 -> 1441`, overrun `29 -> 33`, and blocking `43 -> 49`; refill stayed `11`, reads rose `36 -> 37`, and due stayed `5`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-low `195..211` as a direct grouped-append row. The balanced planner score did not preserve visible cadence; remaining W1-low sector work needs generated deadline/refill ownership or pack-side work reduction, not more blind hand rows. |
| 2026-05-19 | `building4-low-upload-gap9-10-current` | Log-only; renderer restored and PS1 rebuilt. | Sweep the missing BUILDING4-low dirty-upload merge knees around the accepted `GR_UPLOAD_BAND_MERGE_GAP=8` before leaving runtime upload tuning. Gap `11` was already negative, but `9` and `10` could have reduced `LoadImage` rect pressure without the larger byte penalty. | Temporarily changed `GR_UPLOAD_BAND_MERGE_GAP` to `9`, rebuilt, and ran the strict focused B4-low gate; then repeated with gap `10`. Artifacts: gap9 `scratch/ps1-perf-iterate/building4-low-upload-gap9-current/20260519-070534-4117199/summary.json`; gap10 `scratch/ps1-perf-iterate/building4-low-upload-gap10-current/20260519-070804-4131757/summary.json`. | Failed/no promotion. Both variants were exact-flat on timing and layout: B4 low stayed scene/loop/target `3121/2849/2816`, overrun `33`, blocking/refill `38/31`, reads/due `30/1`, pack LBA fixed, and PS-EXE bucket fixed. Work-volume moved the wrong direction: gap9 changed upload `22791040/895 -> 22802560/893`, and gap10 changed it to `22821760/890`. | Keep the accepted gap `8`. The runtime full-width band merge knee is exhausted through `11`: higher gaps only buy tiny rect reductions by uploading more bytes, with no speed movement. Remaining B4-low work should be pack-emitted/precomputed upload data, static-restore elimination, or generated scheduler ownership rather than more merge-threshold tuning. |
| 2026-05-19 | `early-prepare-ge4-canaries` | Log-only; source restored and PS1 rebuilt. | Test a broad scheduler-policy swing before continuing scalar/read-row work. The runtime only allowed staged-frame visual prepare at exactly `4` held VBlanks; this checked whether allowing prepare at `>=4` would move restore/compose/upload off the due path in rows with spare held slack while preserving the existing CD-first guard. | Temporarily changed `fgRuntimeCanPrepareStagedFrame()` from `heldSlack == 4` to `heldSlack >= 4`, rebuilt, and ran the six-yellow canary against the accepted VISITOR3-high-clean64 baseline. Artifact: `scratch/ps1-perf-iterate/early-prepare-ge4-canaries/20260519-071602-4176872/summary.json`. | Failed/no promotion. The policy was exact-flat across all six yellow rows: VISITOR3 high `1075/1044`, VISITOR3 low `1065/1039`, BUILDING2 high `1347/1313`, WALKSTUF1 high `1475/1441`, WALKSTUF1 low `1473/1444`, and BUILDING4 low `2849/2816`; all retained the same overrun, blocking, refill, reads/due, pack LBA, and `233472` byte PS-EXE bucket. | Restore the exact-`4` prepare gate. The current scheduler loop already prioritizes CD-window work when it can matter, so simply preparing earlier does not create a new hidden-work slot. Reopen only with generated per-frame/deadline ownership that can prove a specific prepare/refill trade rather than a global threshold. |
| 2026-05-19 | `walkstuf1-high-widegroup-current` | Log-only; source restored and PS1 rebuilt. | Test the generated-read planner's larger-window W1-high candidates instead of another 12/16-sector scalar row. The large scan ranked `74..122` as an eight-saved-read low-visible-risk candidate and `351..383` as a five-saved-read late cluster, both requiring a wider retained group window than the current 24-sector cap. | Temporarily added a W1-high scene-local wider group-capacity path and tested `{74,122,0}` with a 48-sector capacity, then `{351,383,0}` and `{351,383,4}` with a 32-sector capacity. Artifacts: early wide `scratch/ps1-perf-iterate/walkstuf1-high-widegroup74-122-vnext/20260519-072943-61604/summary.json`; late raw `scratch/ps1-perf-iterate/walkstuf1-high-widegroup351-383-vnext/20260519-073146-73932/summary.json`; late slack4 `scratch/ps1-perf-iterate/walkstuf1-high-widegroup351-383-slack4-vnext/20260519-073340-84847/summary.json`. | Failed/no promotion. `{74..122}` reduced loop reads `44 -> 36`, due `10 -> 6`, blocking `57 -> 53`, and refill `13 -> 12`, but regressed scene/loop/target `1811/1475/1441 -> 1821/1485/1443`, overrun `34 -> 42`. `{351..383}` reduced reads `44 -> 39`, due `10 -> 9`, and blocking `57 -> 56`, but regressed scene/loop/target `1811/1475/1441 -> 1813/1477/1442`, overrun `34 -> 35`, and refill `13 -> 16`; slack4 was identical to raw. | Close W1-high larger grouped-window ownership on the current baseline. The generated planner correctly found read-count wins, but wider append windows still shift target/held cadence and hidden refill. Future W1-high work needs generated per-frame deadline ownership or pack/render work elimination, not just a larger group capacity. |
| 2026-05-19 | `building2-high-widegroup-current` | Log-only; source restored and PS1 rebuilt. | Test the generated-read planner's larger-window B2-high candidates after narrow scalar rows failed. The scan ranked `95..135` and `140..180` as eight-saved-read 40-sector candidates, with `95..127` as the smaller fireable 32-sector form. | Temporarily added a B2-high scene-local wider group-capacity path and tested `{95,135,0}` with 40 sectors, `{95,127,0}` with 32 sectors, and `{140,180,0}` with 40 sectors. Artifacts: `scratch/ps1-perf-iterate/building2-high-widegroup95-135-vnext/20260519-073652-103774/summary.json`; `scratch/ps1-perf-iterate/building2-high-widegroup95-127-vnext/20260519-073927-118617/summary.json`; `scratch/ps1-perf-iterate/building2-high-widegroup140-180-vnext/20260519-074126-130388/summary.json`. | Failed/no promotion. `95..135` and `140..180` never emitted `JCPERF2` before timeout, leaving only heartbeat output; `95..127` completed but exact-flat at `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads/due `45/5`. All kept pack LBA and the `233472` byte PS-EXE bucket fixed. | Close B2-high larger grouped-window ownership. The broad rows are structurally unsafe and the smaller row does not fire/usefully change reads; remaining B2-high work needs generated per-frame deadline ownership, pack/render work elimination, or non-window data-shape changes. |
| 2026-05-19 | `walkstuf1-low-widegroup155-195-current` | Log-only; source restored and PS1 rebuilt. | Test the last generated large-window W1-low candidate after W1-high and B2-high larger grouped windows proved that modeled read-count wins can still lose target/refill cadence. The `155..195` row was the remaining eight-read-family swing from the current scan and required a 40-sector retained group window. | Temporarily added `{155,195,0}` to `kWalkstuf1LowReadGroups12[]` and raised only W1-low's group window capacity to 40 sectors. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-widegroup155-195-vnext/20260519-074708-162671/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row produced the expected read-count win, reducing loop reads `36 -> 29`, but it made the scene much slower: scene/loop/target `1807/1473/1444 -> 1820/1486/1442`, overrun `29 -> 44`, blocking `43 -> 58`, due `5 -> 6`, and hidden refill overrun `11 -> 22`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-low large grouped-window ownership. This confirms the allocator-era remaining W1 rows are not blocked by raw read count alone; broad grouped appends shift visible/refill phase too much. Future W1-low work should use generated per-frame deadline ownership, pack-side data-shape elimination, or render/restore work reduction rather than larger read windows. |
| 2026-05-19 | `building4-low-grdrawbackground-o2-current` | Log-only; renderer restored and PS1 rebuilt. | Test a primitive-level dirty-upload swing after B4-low runtime x-band, upload-gap, read-group, setup-segment, clean-cap, and local-LZ paths saturated. `grDrawBackground()` was compiled for size with `optimize("Os")`; this checked whether letting the dirty-band scanner compile at the normal optimization level would reduce the closest remaining yellow row without changing pack data. | Temporarily removed the `optimize("Os")` attribute from `grDrawBackground()`, rebuilt in the fixed `233472` byte PS-EXE bucket, and ran the strict focused B4-low gate. Artifact: `scratch/ps1-perf-iterate/building4-low-grdrawbackground-o2-vnext/20260519-075332-199218/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. BUILDING4 low was exact-flat at scene/loop/target `3121/2849/2816`, overrun `33`, blocking/refill `38/31`, reads/due `30/1`, pack LBA fixed, and PS-EXE bucket fixed. The only material change was worse code shape: `grDrawBackground` grew by `476` bytes and shifted hot symbols. | Keep the size-optimized dirty-upload path. The remaining B4-low gap is not solved by compiler optimization of the current band scanner; future work needs pack-emitted/background-owned upload data, static restore elimination, or generated scheduler ownership rather than a larger renderer function. |
| 2026-05-19 | `building4-low-cleanrect-dirtyrows-current` | Log-only; renderer restored and PS1 rebuilt. | Test a clean-rect restore CPU swing after B4-low upload/read/window lanes saturated. The existing `grCleanRectCopyIn()` scans every row in every clean-rect backup and only copies rows present in `prevDirty`; this checked whether iterating directly over dirty rows per tile could reduce restore overhead without changing copied bytes, dirty rows, or upload rectangles. | Temporarily rewrote `grCleanRectCopyIn()` to call a tile-local dirty-row helper for bg tiles `0/1/3/4`, clamping the clean-rect intersection to each tile and `prevDirtyMinY/MaxY` before copying. Rebuilt in the fixed `233472` byte PS-EXE bucket and ran the strict focused B4-low gate. Artifact: `scratch/ps1-perf-iterate/building4-low-cleanrect-dirtyrows-vnext/20260519-080202-247169/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. BUILDING4 low was exact-flat at scene/loop/target `3121/2849/2816`, overrun `33`, blocking/refill `38/31`, reads/due `30/1`, upload VBlanks `6`, pack LBA fixed, and PS-EXE bucket fixed. The summary also showed no measured render-phase movement, so the row-scan work is not currently a timing limiter. | Keep the simpler clean-rect restore loop. Close clean-rect dirty-row iteration as a standalone B4-low speed path; remaining B4-low work needs to reduce upload/restore bytes or change scheduler ownership rather than only reducing row-loop bookkeeping. |
| 2026-05-19 | `walkstuf1-high-rg178-194-current` | Log-only; source restored and PS1 rebuilt. | Validate the only distinct W1-high current read-plan row not covered by the earlier wide-group failures. The planner ranked `178..194` as a 16-sector append-start candidate with two modeled saved reads and lower visible-risk classification than the closed early/late wide clusters. | Temporarily inserted `{178,194,0}` after the accepted W1-high `{149,165,0}` group in `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran the strict focused W1-high gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg178-194-vnext/20260519-081212-304646/summary.json`. | Failed/no promotion. The row did fire and reduced loop reads `44 -> 42`, but all speed-bearing metrics stayed exact-flat: scene/loop/target `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, and due misses `10`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-high `178..194` as a direct grouped-append row. It proves read count alone is no longer enough for this scene; the remaining W1-high gap needs generated per-frame deadline ownership or pack/render work reduction rather than another scalar read group. |
| 2026-05-19 | `visitor3-low-rg9-33-current` | Log-only; source restored and PS1 rebuilt. | Test the cleanest low-side VISITOR3 candidate from `performance-under-green-attribution.csv`. The row was distinct from the closed late `206..234` setup/read families and was ranked as a scheduler-owned low-risk `9..33` group with two modeled saved reads and long internal slack. | Temporarily added a VISITOR3-low-only read group table containing `{9,33,0}`, selected it only when `visitor3` low tide used the default stream window, rebuilt, and ran the strict focused VISITOR3-low gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/visitor3-low-rg9-33-vnext/20260519-081933-346264/summary.json`. | Failed/no promotion. The group did not convert into a useful runtime read change: loop reads stayed `18` and due stayed `14`, while scene/loop regressed `1369/1065 -> 1371/1067`, blocking regressed `75 -> 76`, and no key metric improved. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed, but the extra selector path shifted hot symbols by `+16`. | Close VISITOR3-low `9..33` as a direct C table row. The planner label still points at scheduler ownership, but the ownership cannot be expressed as another hot read table; the next VISITOR3-low attempt should use generated per-frame deadlines or pack/data-shape work. |
| 2026-05-19 | `visitor3-high-twohop-pack-current` | Log-only; pack restored and PS1 rebuilt. | Test a no-code VISITOR3-high paid-gap relocation swing after the pack scanner found `13,039` unused bytes inside the accepted `228..262` setup segment. The idea was to move retained frame `140` into that paid gap, then use its freed retained slot in sectors `277..293` for hot runtime frames `115` and `117` from the `103..127` read cluster. | Temporarily rewrote `generated/ps1/foreground/VISITOR3.FG2` in place: frame `140` moved to offset `505315`, frame `115` moved to offset `576267`, and frame `117` moved to offset `582796`, preserving file size `1555450`, pack LBA, and source layout. Transform summary: `scratch/ps1-perf-iterate/visitor3-high-twohop-pack-summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-twohop-pack/20260519-083225-418488/summary.json`. | Failed/no promotion. The relocation was exact-flat against the accepted baseline: scene/loop/target stayed `1389/1075/1044`, overrun `31`, blocking `45`, refill `3`, loop reads `4`, and due misses `3`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close this specific V3-high two-hop paid-gap relocation. Moving `115` and `117` out of the cluster does not change the runtime grouping enough to reduce blocking; future V3-high pack work needs to remove or relocate the boundary/large frame `116`/`128` class, or switch to generated deadline ownership rather than partial same-cluster hole filling. |
| 2026-05-19 | `visitor3-high-114-115-d4-pack-current` | Log-only; pack/source restored and PS1 rebuilt. | Follow the exact-flat two-hop relocation by covering the read-boundary frames instead of only mid-cluster frames. Frame `115` compressed from `6529` to a cheap `5244`-byte / `3`-command D4 delta against frame `114`, making raw frame `114` plus D4 frame `115` fit into the retained slot freed by moving frame `140` into the paid `228..262` gap. | Temporarily rewrote `generated/ps1/foreground/VISITOR3.FG2`: frame `140` moved to offset `505315`, frame `114` moved to `576267`, frame `115` moved to `583438` as D4, and high-tide VISITOR3 frame `115` was added to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/ps1-perf-iterate/visitor3-high-114-115-d4-pack-summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-114-115-d4-pack/20260519-083729-447299/summary.json`. | Failed/no promotion. The boundary relocation moved debt into visible blocking: scene/loop/target regressed `1389/1075/1044 -> 1399/1085/1043`, overrun `31 -> 42`, blocking `45 -> 59`, and due misses `3 -> 4`; hidden refill improved `3 -> 0`, but not in a promotable way. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed; the source branch shifted hot symbols by `+12`. | Close V3-high frame `114/115` paid-gap relocation plus frame115 D4. The D4 decoder itself is cheap, but this changes the visible refill boundary in the wrong direction. Future V3-high pack work must avoid partial boundary relocation unless it also removes the large frame `116`/`128` class or has generated deadline ownership proving the read lands outside visible cadence. |
| 2026-05-19 | `walkstuf1-low-lz56-pack-current` | Log-only; pack restored and PS1 rebuilt. | Test the one unclosed W1-low local-LZ boundary candidate after the previous local-LZ sweep closed frames `59`, `60`, `63`, `65`, and `83`. Frame `56` sits at the end of the accepted `91..107` read group and shrinks strongly, so it was a plausible no-code way to reduce the read boundary. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` frame `56` as local-LZ: payload `3425 -> 700` bytes, `52` commands, file size `1535263`, pack LBA, and source layout unchanged. Transform summary: `scratch/ps1-perf-iterate/walkstuf1-low-lz56-pack-summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-low-lz56-pack/20260519-084108-468459/summary.json`. | Failed/no promotion. The payload reduction was timing-flat: scene/loop/target stayed `1807/1473/1444`, overrun `29`, blocking `43`, refill `11`, loop reads `36`, and due misses `5`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-low frame56 local-LZ as a standalone compression lane. The byte saving does not change the grouped read boundary; remaining W1-low work needs generated deadline ownership, boundary-frame relocation that removes a whole read, or render/restore reduction rather than isolated local-LZ. |
| 2026-05-19 | `building4-low-no-prepare-current` | Log-only; source restored and PS1 rebuilt. | Test a targeted B4-low scheduler swing after the current row showed only one due miss but high hidden refill overrun plus `prepared_wasted=5`. The hypothesis was that disabling speculative visual prepare for BUILDING4 would redirect held slack toward CD/window work and avoid wasted prepared frames. | Temporarily skipped `fgRuntimePrepareStagedFrameForPresent()` for `building4` while leaving the rest of the stage/window scheduler intact, rebuilt, and ran the strict focused B4-low gate. Artifact: `scratch/ps1-perf-iterate/building4-low-no-prepare/20260519-084439-488765/summary.json`. | Failed/no promotion. Speculative prepare is still beneficial for this row: scene/loop/target regressed `3121/2849/2816 -> 3134/2862/2816`, overrun `33 -> 46`, blocking `38 -> 46`, and refill `31 -> 39`; reads/due stayed `30/1`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed, but the source branch shifted hot symbols by `+48`. | Keep BUILDING4 prepared-frame scheduling enabled. The prepared-wasted count is not the limiter; remaining B4-low work needs to reduce CD/upload/restore work or add generated deadline ownership, not remove visual prepare. |
| 2026-05-19 | `building4-low-rg262-286-current` | Log-only; source restored and PS1 rebuilt. | Test the next distinct B4-low generated-read candidate after `178..202` exact-flat and setup variants failed. The attribution report ranked `262..286` as a scheduler-owned candidate with one modeled saved read and a different late-gap phase. | Temporarily added a BUILDING4 low-only read group `{262,286,0}` under the default stream window, rebuilt, and ran the strict focused B4-low gate. Artifact: `scratch/ps1-perf-iterate/building4-low-rg262-286/20260519-084812-509304/summary.json`. | Failed/no promotion. The row was exact-flat: scene/loop/target stayed `3121/2849/2816`, overrun `33`, blocking `38`, refill `31`, reads/due `30/1`, pack LBA fixed, and PS-EXE bucket fixed; the source branch shifted hot symbols by `+20`. | Close B4-low `262..286` as a direct C table row. Like `178..202`, the modeled saved-read opportunity needs generated deadline ownership or pack/render work reduction; a hot read-table row alone does not move timing. |
| 2026-05-19 | `building4-low-widegroup262-298-current` | Log-only; source restored and PS1 rebuilt. | Escalate the exact-flat `262..286` row into a broader late-cluster test that could cover both `262..286` and `274..298` candidates in one append. This raised only BUILDING4-low group capacity to `36` sectors to allow `{262,298,0}` to fit. | Temporarily added a BUILDING4 low-only read group `{262,298,0}` and a scene-local `36 * 2048` byte stream-window capacity when that group was active, rebuilt, and ran the strict focused B4-low gate. Artifact: `scratch/ps1-perf-iterate/building4-low-widegroup262-298/20260519-085209-531972/summary.json`. | Failed/no promotion. The broader window was also exact-flat: scene/loop/target stayed `3121/2849/2816`, overrun `33`, blocking `38`, refill `31`, reads/due `30/1`, pack LBA fixed, and PS-EXE bucket fixed; the source branch shifted hot symbols by `+108`. | Close B4-low broad late grouped-window ownership for `262..298`. The remaining B4 gap is not reachable through direct read-table capacity changes; future work should target generated deadline ownership with a different scheduler mechanism, pack-emitted/precomputed upload data, or static restore/upload reduction. |
| 2026-05-19 | `building2-high-lz89-pack-current` | Log-only; pack restored and PS1 rebuilt. | Test a no-source B2-high pack-compression swing after the hotspot scan found frame `89` local-LZ shrinks `8814 -> 4843` bytes, a larger sector reduction than the rejected frame90 D4 path and at a different boundary. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` frame `89` as local-LZ: `132` commands, `3971` saved bytes, file size `1303332`, pack LBA, and source layout unchanged. Transform summary: `scratch/ps1-perf-iterate/building2-high-lz89-pack-summary.json`; focused gate: `scratch/ps1-perf-iterate/building2-high-lz89-pack/20260519-085625-556379/summary.json`. | Failed/no promotion. The sector reduction moved decode/cadence debt into the visible path: loop/target regressed `1347/1313 -> 1353/1310`, overrun `34 -> 43`, blocking `39 -> 52`, refill `16 -> 20`, reads `45 -> 44`, and due misses `5 -> 7`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close B2-high frame89 local-LZ on this runtime. Like frame90 D4, decode-side compression in the active boundary is too expensive; future B2-high work should prefer no-decode relocation, generated deadline ownership, or render/upload work reduction. |
| 2026-05-19 | `visitor3-low-zero-noop-current` | Log-only; pack restored and PS1 rebuilt. | Test a pack-side render-elimination swing after direct VISITOR3-low read rows and setup extensions saturated. The `compact-fgp3-zero-noop-entries.py` scanner was updated with `--copy-unparseable` so already-compressed D4/local-LZ sentinel payloads can be copied while parseable residual entries are checked for true visual no-ops. | Temporarily rewrote `generated/ps1/foreground/VIST3LOW.FG2` with `--pad-to-input-size --copy-unparseable`. The transform found five true no-op entries (`sourceFrame 159..163`), converting only `10` active payload bytes to empty timed holds while preserving file size `1555450`, pack LBA, and source layout. Focused gate: `scratch/ps1-perf-iterate/visitor3-low-zero-noop-vist3low/20260519-090647-614825/summary.json`. The pack was restored from `scratch/ps1-perf-iterate/VIST3LOW.FG2.before-zero-noop` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Emptying those holds disrupted VISITOR3-low prefetch cadence badly: scene/loop/target regressed `1369/1065/1039 -> 1410/1108/1036`, overrun `26 -> 72`, blocking `75 -> 143`, loop reads `18 -> 28`, and due misses `14 -> 24`; hidden refill stayed `0`. | Close V3-low zero-noop empty-hold conversion. Even visually empty residual entries still participate in the scheduler's frame/hold cadence; removing their render/load work steals the slack the current low-tide prefetch path depends on. Future V3-low work should keep cadence-bearing entries intact unless generated deadlines are rebuilt around the new empty holds. |
| 2026-05-19 | `building4-low-window24-current` | `ps1: narrow building4 low stream window` | Reopen the B4-low scene-local stream-window knee after upload-gap, read-group, setup-segment, clean-cap, local-LZ, renderer, and prepare-scheduler lanes saturated. The binary sweep tested whether smaller or larger B4-low windows would shift the final one-percent gap without changing pack layout or executable bucket. | Temporarily swept `FG_BUILDING4_LOW_WINDOW_BYTES` from the accepted `32 KiB` baseline to `48 KiB`, then to `24 KiB`, rebuilding and running strict focused B4-low gates against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifacts: rejected 48 KiB `scratch/ps1-perf-iterate/building4-low-window48/20260519-091453-660737/summary.json`; accepted 24 KiB `scratch/ps1-perf-iterate/building4-low-window24/20260519-091725-675136/summary.json`. | Promoted. The `48 KiB` probe regressed active loop/target `2849/2816 -> 2895/2813`, overrun `33 -> 82`, blocking/refill `38/31 -> 61/62`, and reads/due `30/1 -> 20/0`. The `24 KiB` knee improves scene/loop/target `3121/2849/2816 -> 3117/2847/2820`, overrun `33 -> 27`, blocking/refill `38/31 -> 32/27`, and target speed `98.842% -> 99.052%`, while reads rise `30 -> 43`, read time `222 -> 252`, due stays `1`, pack LBA stays `9126`, and the PS-EXE bucket stays `233472`. | Keep B4 low at `24 KiB`. BUILDING4 low moves green, leaving five yellow rows: VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high/low. Public rollup moves to `+0.2470%` over target / `99.7566%` target speed; raw signed rollup is about `-0.4699%` / `100.4869%`; bands are `121` green, `5` yellow, `0` orange, and `0` red. |
| 2026-05-19 | `walkstuf1-window48-32-current` | Log-only; source restored and PS1 rebuilt. | Apply the successful B4-low stream-window-knee lesson to the remaining WALKSTUF1 yellow pair without changing setup-prime caps. High `54 KiB -> 48 KiB` and low `40 KiB -> 32 KiB` should have changed active-loop stream cadence while keeping the accepted setup coverage and read groups. | Temporarily changed `FG_WALKSTUF1_HIGH_RESIDUAL_WINDOW_BYTES` to `48 KiB` and `FG_WALKSTUF1_LOW_RESIDUAL_WINDOW_BYTES` to `32 KiB`, rebuilt, and ran a strict paired W1 high/low gate against `scratch/ps1-perf-iterate/visitor3-high-clean64-canaries/20260519-010953-2088970/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-window48-32/20260519-093046-750241/summary.json`. | Failed/no promotion. Both rows were exact-flat: W1 high stayed scene/loop/target `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, reads/due `44/10`; W1 low stayed `1807/1473/1444`, overrun `29`, blocking/refill `43/11`, reads/due `36/5`. Pack LBAs and the `233472` byte PS-EXE bucket stayed fixed. | Keep W1 high/low at `54 KiB` / `40 KiB`. The B4-low window-knee win does not transfer to WALKSTUF1 at these paired sizes; future W1 work needs either generated deadline ownership, pack/data-shape boundary changes, or a wider sweep that changes setup-prime caps deliberately. |
| 2026-05-19 | `building2-high-window12-20-24-current` | Log-only; source restored and PS1 rebuilt. | Apply the B4-low stream-window-knee lesson to BUILDING2 high while preserving the accepted B2 read groups. The binary check allowed the scene-local high-tide window to move away from the default `16 KiB` but removed the default-window guard from B2 read-group selection so prior grouped ownership stayed active. | Temporarily added a BUILDING2 runtime-window policy with low tide kept at default and high tide swept through `12 KiB`, `24 KiB`, then `20 KiB`; B2 read groups were selected regardless of the window override during the test. Artifacts: `12 KiB` `scratch/ps1-perf-iterate/building2-high-window12-groups-current/20260519-093717-787607/summary.json`; `24 KiB` `scratch/ps1-perf-iterate/building2-high-window24-groups-current/20260519-093900-797842/summary.json`; `20 KiB` `scratch/ps1-perf-iterate/building2-high-window20-groups-current/20260519-094045-808194/summary.json`. | Failed/no promotion. `12 KiB` over-sliced the stream: reads rose `45 -> 57`, loop/target `1347/1313 -> 1355/1311`, overrun `34 -> 44`, and blocking `39 -> 52` despite hidden refill falling `16 -> 13`. `24 KiB` over-held refill: reads dropped `45 -> 28`, but loop/target regressed to `1357/1312`, overrun `45`, blocking `55`, and refill `28`. `20 KiB` was worst on visible blocking, regressing to `1362/1313`, overrun `49`, blocking `62`, and refill `22`. | Keep BUILDING2 high on the default `16 KiB` stream window and default-window read-group gate. The B2-high remaining gap is not a window-knee problem; future work should target generated deadline ownership, no-decode relocation, or render/upload reduction. |
| 2026-05-19 | `walkstuf1-window12-8-cap-current` | Log-only; source restored and PS1 rebuilt. | Deliberately push WALKSTUF1 windows below the setup-prime cap knee after the `48/32 KiB` sweep proved adjacent window sizes were inert. The binary check asked whether reducing both stream read size and derived setup-prime residency could change W1 playback timing while preserving pack layout. | Temporarily changed `FG_WALKSTUF1_HIGH_RESIDUAL_WINDOW_BYTES` and `FG_WALKSTUF1_LOW_RESIDUAL_WINDOW_BYTES` to `12 KiB`, ran a paired high/low strict gate, then pushed both to `8 KiB` and repeated. Artifacts: `12/12 KiB` `scratch/ps1-perf-iterate/walkstuf1-window12-12-cap-current/20260519-094543-836899/summary.json`; `8/8 KiB` `scratch/ps1-perf-iterate/walkstuf1-window8-8-cap-current/20260519-094908-856913/summary.json`. | Failed/no promotion. Both variants were exact-flat on both tides despite changing the generated read-plan setup coverage to `prime=[2,74]` high and `prime=[2,78]` low. W1 high stayed `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, reads/due `44/10`; W1 low stayed `1807/1473/1444`, overrun `29`, blocking/refill `43/11`, reads/due `36/5`. Pack LBAs and the `233472` byte PS-EXE bucket stayed fixed. | Keep W1 high/low at `54 KiB` / `40 KiB`. The deliberate setup-prime-cap/window phase lane is now closed; W1 timing is pinned by prepared/render/CD deadline ownership or payload/dirty-work shape, not the derived setup-prime cap. |
| 2026-05-19 | `building2-high-restore-minus-layout-current` | Log-only; pack/tooling restored. | Test a no-code BUILDING2-high restore-minus-current/layout compaction swing after scalar windows, setup, read groups, local-LZ, D4, and trim-tail lanes saturated. The transform needed a temporary copy-unparseable helper because several current B2 entries carry trailing-byte/local compressed payload shapes; the measured candidate preserved file size and executable layout while repacking payload offsets. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` through `compact-fgp3-restore-minus-current.py` with copy-unparseable behavior, padded back to `1303332` bytes, rebuilt, and ran focused B2 high. Transform output was `scratch/building2-high-restore-minus-current-vnext/BUILDING2.restore-minus.FG2`; focused artifact: `scratch/ps1-perf-iterate/building2-high-restore-minus-layout-vnext/20260519-095942-916985/summary.json`. | Failed/no promotion. The transform found no removable cleanup work (`active_payload 669408 -> 669408`, restore bytes `424092 -> 424092`) and the pure layout shift was phase-negative: B2 high regressed from `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads/due `45/5`, to `1631/1357/1314`, overrun `43`, blocking/refill `52/12`, reads/due `49/8`. | Restore the original B2-high pack and helper script. Close restore-minus-current/layout compaction for BUILDING2 high on this baseline; because cleanup work is already irreducible, future B2-high pack work needs targeted no-decode boundary relocation or generated deadline ownership rather than whole-pack repacking. |
| 2026-05-19 | `walkstuf1-early-prepare5-current` | Log-only; source restored and PS1 rebuilt. | Test a targeted WALKSTUF1 scheduler swing after raw read groups and pack compression lanes stalled. The hypothesis was that allowing WALKSTUF1 to prepare staged visual work at `5` held VBlanks, one VBlank earlier than the global `4`-VBlank bridge, could preserve the accepted low-tide prepare-first benefit while leaving enough slack for CD/window work. | Temporarily widened `fgRuntimeCanPrepareStagedFrame()` for `walkstuf1` only to allow held slack `4` or `5`, rebuilt in the same `233472` byte PS-EXE bucket, and ran a strict paired W1 high/low gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-early-prepare5/20260519-100830-967084/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both rows were exact-flat on speed-bearing metrics and failed `--require-improvement`: W1 high stayed `1811/1475/1441`, overrun `34`, blocking/refill `57/13`, loop reads/due `44/10`; W1 low stayed `1807/1473/1444`, overrun `29`, blocking/refill `43/11`, reads/due `36/5`. The probe only grew `foregroundPilotPlay` by `292` bytes and shifted hot symbols. | Keep the global `4`-VBlank prepare bridge and the existing low-tide prepare-first special case. The W1 prepare-boundary lane is closed on this baseline; remaining W1 work needs generated deadline/refill ownership, no-code data-shape changes, or render/restore reduction that changes measured work without code-layout-only churn. |
| 2026-05-19 | `visitor3-low-early-residency-rebalance-current` | Log-only; source restored and PS1 rebuilt. | Test the biggest remaining VISITOR3-low visible due cluster with setup residency instead of another direct read group. The corrected read plan showed early uncovered reads across `1..30`, but prior low grouped appends were phase-negative, so this tested both additive residency and budget-neutral slot rebalances. | Three variants were tested against the current under-green canary baseline. An additive fourth retained segment `1..35` failed before JCPERF2 with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=227328 have=168956`; artifact: `scratch/ps1-perf-iterate/visitor3-low-seg1-35-fourth/20260519-101621-1011796/summary.json`. Replacing segment2 `150..177` with `1..30` completed but regressed; artifact: `scratch/ps1-perf-iterate/visitor3-low-seg2-1-30-rebalance/20260519-102024-1034851/summary.json`. Replacing the terminal segment `281..305` with `1..30` also completed but regressed; artifact: `scratch/ps1-perf-iterate/visitor3-low-seg1-1-30-rebalance/20260519-102211-1045479/summary.json`. | Failed/no promotion. Segment2 rebalance regressed scene/loop/target `1369/1065/1039 -> 1392/1094/1034`, overrun `26 -> 60`, blocking `75 -> 117`, reads `18 -> 21`, and due `14 -> 19`. Terminal rebalance was less bad but still failed: `1369/1065/1039 -> 1377/1081/1038`, overrun `26 -> 43`, blocking `75 -> 99`, reads `18 -> 19`, and due `14 -> 17`. | Restore the accepted V3-low `281..305`, `150..177`, and `206..232` retained segments. The early `1..30` cluster is real but cannot be bought by simple retained residency: additive residency exceeds the allocator cap, and stealing either accepted slot creates larger visible debt. Future V3-low work needs generated deadline ownership or pack/data-shape changes that keep all three accepted retained clusters intact. |
| 2026-05-19 | `building2-high-d4-71-77-current` | Log-only; pack/source restored and PS1 rebuilt. | Retest the high-tide BUILDING2 D4 generalization on the current allocator-era baseline after frame77 appeared as a large scan candidate (`8828 -> 909`) and frame71 remained the cheapest possible D4 candidate (`6656 -> 13`). The older combined `71/77` test failed on a prior baseline, so this isolated each side against the current under-green canary baseline. | Temporarily rewrote high-tide `BUILDING2.FG2` frame `77` as previous-frame D4 and enabled BUILDING2 high frame77 in `fgRuntimeUsesPreviousFrameDelta()`; transform summary `scratch/ps1-perf-iterate/building2-high-d4-77-pack/summary-frame77.json`, focused gate `scratch/ps1-perf-iterate/building2-high-d4-77-pack/20260519-102629-1070619/summary.json`. Then restored the pack and tested frame `71` alone with the high-tide predicate changed to frame71; transform summary `scratch/ps1-perf-iterate/building2-high-d4-71-pack/summary-frame71.json`, focused gate `scratch/ps1-perf-iterate/building2-high-d4-71-pack/20260519-102841-1083472/summary.json`. | Failed/no promotion. Frame77 saved one loop read but regressed scene/loop/target `1621/1347/1313 -> 1626/1353/1314`, overrun `34 -> 39`, blocking `39 -> 41`, and refill `16 -> 17`. Frame71 was worse despite the tiny encoded payload: `1621/1347/1313 -> 1626/1353/1310`, overrun `34 -> 43`, blocking `39 -> 43`, refill `16 -> 20`, and reads `45 -> 46`. | Keep BUILDING2 previous-frame D4 low-tide-only for frames `71` and `77`. Close high-tide BUILDING2 D4 on the current baseline: decode/predicate phase and shifted payload timing dominate even the cheapest candidate. Future B2-high work should stay no-decode, generated-deadline-owned, or render/upload-work focused rather than adding high-tide D4 decode. |
| 2026-05-19 | `visitor3-high-d4-129-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the largest unlogged VISITOR3-high previous-frame D4 scan candidate after frame `129` showed a `17025 -> 609` byte encoding with only `57` commands. The binary question was whether enabling the same frame already used by VISITOR3 low would shave a high-tide read or blocking without changing pack LBA or executable bucket. | Temporarily rewrote `generated/ps1/foreground/VISITOR3.FG2` frame `129` as previous-frame D4 against frame `128` and enabled high-tide frame `129` in `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/ps1-perf-iterate/visitor3-high-d4-129-pack/summary-frame129.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-high-d4-129-pack/20260519-103836-1139503/summary.json`. The original pack was restored from `scratch/ps1-perf-iterate/visitor3-high-d4-129-pack/VISITOR3.before.FG2`, the source predicate was restored, and PS1 was rebuilt afterward. | Failed/no promotion. The focused gate was exact-flat and failed `--require-improvement`: VISITOR3 high stayed scene/loop/target `1389/1075/1044`, overrun `31`, blocking/refill `45/3`, reads/due `4/3`, pack LBA `22619`, and PS-EXE bucket `233472`. The D4 payload saved bytes inside the current file but did not change any measured high-tide read boundary. | Close same-offset VISITOR3-high frame129 D4. The scan saving is real but inert without relocating or changing scheduler ownership; future VISITOR3-high work should avoid same-offset D4-only transforms and focus on no-decode boundary ownership, generated deadline metadata, or render/upload work reduction. |
| 2026-05-19 | `walkstuf1-high-frame92-d4-current` | `ps1: add walkstuf1 high frame92 delta` | Reopen WALKSTUF1 high previous-frame D4 after current allocator-era read/setup changes, but avoid the stale-baseline trap from the old frame100 test. Frame `92` was the largest unclosed high-tide D4 candidate with low command count, shrinking `6035 -> 2056` bytes with only `26` commands. | Rewrote high-tide `generated/ps1/foreground/WALKSTUF1.FG2` frame `92` as previous-frame D4 against frame `91` and enabled only high-tide WALKSTUF1 frame `92` in `fgRuntimeUsesPreviousFrameDelta()`. Focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame92-d4-current/20260519-104205-1159825/summary.json`; transform summary: `scratch/ps1-perf-iterate/walkstuf1-high-frame92-d4-current/summary-frame92.json`; under-green canaries: `scratch/ps1-perf-iterate/walkstuf1-high-frame92-d4-current-canaries/20260519-104405-1171311/summary.json`. | Promoted. WALKSTUF1 high improves scene/loop/target `1811/1475/1441 -> 1807/1471/1440`, overrun `34 -> 31`, and target speed `97.695% -> 97.893%`; blocking/refill stay `57/13`, due stays `10`, pack LBA stays `24891`, and the PS-EXE bucket stays `233472`. The expected tradeoff is loop reads/read time `44/199 -> 45/209`; the D4 payload is still net-positive because visible loop and overrun fall. VISITOR3 high/low, BUILDING2 high/low, WALKSTUF1 low, and BUILDING4 low canaries stayed flat on accepted profiles. | Keep frame `92` as the current W1-high D4 baseline. Public rollup moves to `+0.2453%` over target / `99.7581%` target speed; raw signed rollup is about `-0.4716%` / `100.4885%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology total remains about `17.15` public over-target points removed and `+12.66` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `visitor3-low-frame119-d4-current` | Log-only; pack/source restored and PS1 rebuilt. | Test VISITOR3-low's cheapest unclosed previous-frame D4 candidate after the frame119 scan showed a one-command copy-only payload (`2628 -> 13` bytes). The hypothesis was that a near-free payload at frame119 might reduce one visible low-tide read or boundary stall without disturbing the high-tide path. | Temporarily rewrote `generated/ps1/foreground/VIST3LOW.FG2` frame `119` as previous-frame D4 against frame `118` and enabled only low-tide VISITOR3 frame119 in `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/ps1-perf-iterate/visitor3-low-frame119-d4-current/summary-frame119.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-low-frame119-d4-current/20260519-110615-1296714/summary.json`. The original pack was restored from `scratch/ps1-perf-iterate/visitor3-low-frame119-d4-current/VIST3LOW.before.FG2`, the source predicate was restored, and PS1 was rebuilt afterward. | Failed/no promotion. The focused gate was exact-flat and failed `--require-improvement`: VISITOR3 low stayed scene/loop/target `1369/1065/1039`, overrun `26`, blocking/refill `75/0`, reads/due `18/14`, pack LBA `23379`, and PS-EXE bucket `233472`. The only source-side effect was a `+52` byte `fgRuntimeLoadSceneFrame` code-shape shift. | Close standalone VISITOR3-low frame119 D4. A one-command copy-only payload is still inert when it does not change read ownership or the active visible boundary; future V3-low work should stay focused on generated deadline ownership, retained-segment tradeoffs that keep accepted clusters intact, or no-decode pack relocation. |
| 2026-05-19 | `walkstuf1-high-frame94-d4-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the next W1-high D4 in the same early hot read family as promoted frame92. Frame `94` shrinks `5544 -> 2214` bytes, saves two modeled sectors, and might have moved the still-open `84..108`/`92..108` boundary after frame92 became the accepted baseline. | Temporarily rewrote `generated/ps1/foreground/WALKSTUF1.FG2` frame `94` as previous-frame D4 against frame `93` and widened the high-tide WALKSTUF1 D4 predicate to frames `92` and `94`. Transform summary: `scratch/ps1-perf-iterate/walkstuf1-high-frame94-d4-current/summary-frame94.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-frame94-d4-current/20260519-111040-1322051/summary.json`. The original pack was restored from `scratch/ps1-perf-iterate/walkstuf1-high-frame94-d4-current/WALKSTUF1.before.FG2`, the source predicate was restored to frame92-only, and PS1 was rebuilt afterward. | Failed/no promotion. The focused gate was exact-flat against the post-frame92 baseline: WALKSTUF1 high stayed scene/loop/target `1807/1471/1440`, overrun `31`, blocking/refill `57/13`, reads/due `45/10`, pack LBA `24891`, and PS-EXE bucket `233472`. The source change only shifted hot foreground symbols by `+16`. | Close standalone W1-high frame94 D4. The promoted frame92 changed timing because it crossed an exposed boundary; frame94's byte saving remains hidden inside the current ownership window. Future W1-high D4 retries should require a read-plan boundary proof or combine with generated deadline ownership rather than adding same-cluster standalone decode. |
| 2026-05-19 | `walkstuf1-high-gap1-repack-current` | Log-only; pack restored and PS1 rebuilt. | Retest the historical WALKSTUF1 high cleanup-gap/repack lane on the allocator-era frame92 baseline as a no-source data-shape swing. The older prefix-gap experiment had visible blocking signal on a pre-allocator baseline, so this checked whether compacting current FG2 payload offsets without changing pixel payloads could improve the remaining W1-high boundary. | Temporarily repacked `generated/ps1/foreground/WALKSTUF1.FG2` with `scratch/walkstuf1-cleanup-gap-merge-v263/merge_cleanup_gaps.py` over frames `0..37`, `--max-gap 1`, and non-preserved offsets. The transform changed no selected frame payloads but moved the sound/event tail earlier (`886399 -> 860079`) by removing internal pack gaps. Transform summary: `scratch/ps1-perf-iterate/walkstuf1-high-gap1-repack-current/summary.json`; focused gate: `scratch/ps1-perf-iterate/walkstuf1-high-gap1-repack-current/20260519-111618-1354441/summary.json`. The original pack was restored from `scratch/ps1-perf-iterate/walkstuf1-high-gap1-repack-current/WALKSTUF1.before.FG2`, and PS1 was rebuilt afterward. | Failed/no promotion. The pure layout compaction was phase-negative: WALKSTUF1 high regressed scene/loop/target `1807/1471/1440 -> 1813/1476/1434`, overrun `31 -> 42`, blocking/refill `57/13 -> 67/17`, and reads/due stayed `45/10`; pack LBA and PS-EXE bucket stayed fixed. | Close current-baseline W1-high prefix gap/repack compaction. Removing internal FG2 gaps shifts physical payload timing enough to hurt the accepted frame92 boundary; future W1-high pack work should be targeted no-decode boundary ownership or generated deadline/refill metadata rather than whole-pack repacking. |
| 2026-05-19 | `building2-high-rg249-261-current` | Log-only; source restored and PS1 rebuilt. | Test the one low-visible-risk BUILDING2-high read-group candidate that remained distinct from the closed wide-window and scalar rows. The current read plan ranked `249..261` as a 12-sector scheduler-owned candidate with one modeled saved read and much looser first/internal gaps than the already-closed high-tight B2 clusters. | Temporarily inserted `{249,261,0}` into `kBuilding2HighReadGroups12[]`, rebuilt, and ran the strict focused B2-high gate against the post-WALKSTUF1-frame92 baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg249-261-current/20260519-112335-1395725/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The new row shifted the late cluster into a worse cadence: BUILDING2 high regressed scene/loop/target `1621/1347/1313 -> 1631/1357/1314`, overrun `34 -> 43`, blocking `39 -> 55`, loop reads `45 -> 47`, and due misses `5 -> 8`; hidden refill improved only `16 -> 14`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close B2-high `249..261` as a direct read-table row. Even the planner's low-risk late cluster is phase-negative without generated deadline ownership; remaining B2-high work should shift to pack/render work reduction or a scheduler that can own per-frame read deadlines instead of more static grouped appends. |
| 2026-05-19 | `building2-high-setup-first-render-current` | Log-only; source restored and PS1 rebuilt. | Test a bigger scheduler/work-placement swing for BUILDING2 high after static read rows and pack transforms saturated: force the first full-screen redraw/render before `loop_start`, then start active playback with frame `0` already rendered so the first held loop can spend time on CD/prepare work instead of the residue-protection full upload. | Temporarily kept `grForceFullRedrawNextFrame()` but immediately ran `grBeginResidualCleanBgFirstFrame()`, `grUpdateDisplay(NULL,NULL,NULL)`, marked the frame rendered, and reset `sceneClockTick` before `ps1PerfMarkLoopStart()` for high-tide BUILDING2 only. Focused artifact: `scratch/ps1-perf-iterate/building2-high-setup-first-render-current/20260519-112912-1427921/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Moving the full redraw out of the active loop destabilized cadence instead of freeing it: BUILDING2 high regressed scene/loop/target `1621/1347/1313 -> 1636/1362/1309`, overrun `34 -> 53`, blocking `39 -> 49`, refill `16 -> 24`, and no key metric improved; reads/due stayed `45/5`. The source probe also grew `foregroundPilotPlay` by `132` bytes while keeping pack LBA and PS-EXE bucket fixed. | Close setup-first-render for B2 high. The full redraw is acting as part of the accepted first-frame cadence; moving it before `loop_start` lowers target and worsens CD/refill phase. Future first-frame work needs a generated timing model or a truly cheaper first upload, not just relocating the same upload earlier. |
| 2026-05-19 | `walkstuf1-high-prepare-rg372-frame92-current` | Log-only; source restored and PS1 rebuilt. | Retest the promising WALKSTUF1-high prepare-before-window scheduler lane on the post-frame92 D4 baseline, then try to pay back its refill debt with the new read-plan's low-risk `{372,388}` candidate. Frame92 changed the exposed high-tide boundary, so this was a current-baseline recheck of a lane that previously had visible-blocking signal but failed strict refill gates. | First removed the low-tide qualifier from the WALKSTUF1 prepare-before-window branch and ran focused W1-high: artifact `scratch/ps1-perf-iterate/walkstuf1-high-prepare-first-frame92-current/20260519-113150-1443517/summary.json`. Then added `{372,388,0}` to `kWalkstuf1HighReadGroups12[]` and reran: artifact `scratch/ps1-perf-iterate/walkstuf1-high-prepare-rg372-frame92-current/20260519-113352-1455511/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Prepare-first alone improved visible blocking `57 -> 45` and due misses `10 -> 7`, but regressed scene/loop `1807/1471 -> 1808/1472`, overrun `31 -> 32`, and refill `13 -> 15`. The `{372,388}` assist reduced loop reads `45 -> 43` and kept due at `7`, but worsened the trade: scene/loop `1807/1471 -> 1811/1475`, overrun `31 -> 34`, blocking `57 -> 47`, and refill `13 -> 17`. Pack LBA and PS-EXE bucket stayed fixed. | Keep WALKSTUF1 prepare-before-window low-tide-only and keep `{372,388}` out of the high table. The current frame92 baseline still turns high-tide prepare ownership into hidden-refill and cadence debt; local read assists do not fix it. Reopen only with generated per-frame deadline ownership or a true render/upload reduction, not more scalar prepare/read-table variants. |
| 2026-05-19 | `walkstuf1-low-transient-setupseg238-350-current` | Log-only; source restored and PS1 rebuilt. | Test an allocator-era big swing for WALKSTUF1 low: move the retained setup segment out of CACHE and extend low setup residency from `238..344` to `238..350`, aiming to buy the remaining visible W1-low due cluster without more static read rows. | Temporarily changed `FG_WALKSTUF1_LOW_SETUP_SEGMENT_BYTES` from `106` to `112` sectors and changed the WALKSTUF1 setup segment allocation from `MEM_REGION_CACHE` to `MEM_REGION_TRANSIENT`. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-transient-setupseg238-350-current/20260519-113725-1476170/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The run produced no `JCPERF2` and halted structurally with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=229376 have=167472`; the captured allocator state was `memCacheUsed=274756`, `memTransientUsed=618960`, and `sceneAllocBalance=10`. | Close monolithic W1-low transient setup extension. Moving the larger retained buffer to TRANSIENT trades CACHE pressure for worse TRANSIENT/libc pressure. Future W1-low allocator work needs split residency, a smaller edge allocation, or reduced background/clean/transient pressure before a larger retained setup segment can be promoted. |
| 2026-05-19 | `walkstuf1-low-split-seg344-350-current` | `ps1: split walkstuf1 low setup edge` | Promote the allocator-era split-residency version of the W1-low setup edge. The previous monolithic `238..350` attempts showed the active-loop cluster was useful but too large for a single TRANSIENT allocation, and the binary CACHE-only alternative was tested before promotion. | Kept the accepted W1-low `238..344` setup segment in CACHE and added a separate low-only `344..350` setup segment allocated from TRANSIENT. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-split-seg344-350-current/20260519-114347-1512113/summary.json`; under-green canaries: `scratch/ps1-perf-iterate/walkstuf1-low-split-seg344-350-canaries/20260519-114542-1523237/summary.json`. The binary monolithic CACHE check `scratch/ps1-perf-iterate/walkstuf1-low-cache-setupseg238-350-current/20260519-115337-1569294/summary.json` produced no `JCPERF2` and stayed rejected. | Promoted as a material setup-cost trade. WALKSTUF1 low keeps loop `1473` flat while target improves `1444 -> 1447`, overrun `29 -> 26`, blocking `43 -> 41`, hidden refill/prefetch overrun `11 -> 7`, loop-read VBlanks `195 -> 186`, and target speed `98.031% -> 98.235%`; due stays `5`, loop reads stay `36`, pack LBA stays `25641`, and the PS-EXE bucket stays `233472`. Setup/scene cost rises `1807 -> 1815` because the small edge is a separate setup read, accepted here because active speed and CD pressure improve and all VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay flat. | Keep the split edge as the new W1-low baseline and do not retry monolithic `238..350` in CACHE or TRANSIENT. Public rollup moves to `+0.2437%` over target / `99.7598%` target speed; raw signed rollup is about `-0.4733%` / `100.4901%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology total is about `17.15` public over-target points removed and `+12.66` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `visitor3-high-setup-residency-current` | Log-only; source restored and PS1 rebuilt. | Try a larger allocator-era VISITOR3-high residency swing after the clean64 baseline left only four loop reads but still `31` VBlanks over target. The binary question was whether moving early high-tide setup coverage into retained CACHE, and dropping high-tide stream-window residency under clean relief, could trade setup memory for active-loop CD savings. | Temporarily reshaped VISITOR3 high setup segments from accepted `277..293`, `203..229`, and `228..262` into larger early/CACHE-resident clusters and tested progressively smaller high-tide variants: `1..159`, `1..127`, `1..127` without high stream-window residency, `1..102` without the window, and `1..71` without the window. Artifacts: `scratch/ps1-perf-iterate/visitor3-high-setupseg1-159-current/20260519-121042-1664773/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupseg1-127-current/20260519-121323-1680058/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupseg1-127-nowindow-current/20260519-121636-1698393/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-setupseg1-102-nowindow-current/20260519-121924-1714251/summary.json`, and `scratch/ps1-perf-iterate/visitor3-high-setupseg1-71-nowindow-current/20260519-122202-1729270/summary.json`. | Failed/no promotion. The `1..159` and `1..127` CACHE-resident attempts halted before `JCPERF2` with CACHE exhaustion on the enlarged setup buffer. Removing high-tide stream-window residency got past setup but the `1..127` and `1..102` variants exhausted CACHE during clean-rect allocation. The smallest `1..71` no-window variant still produced no `JCPERF2` and no comparable timing metrics. All variants kept the PS-EXE bucket at `233472`, but the source probe grew `foregroundPilotPlay` by `36` bytes in the final smallest shape. | Close broad VISITOR3-high early setup residency on this baseline. The allocator now exposes that VISITOR3 high is constrained by contiguous CACHE/clean-rect pressure before active-loop read savings can be measured. Future VISITOR3-high work should avoid larger retained setup coverage until clean-rect/backdrop memory is reduced or should target generated deadline ownership, no-decode render/upload reduction, or a smaller split edge with a proved memory budget. |
| 2026-05-19 | `walkstuf1-high-split-edge-current` | Log-only; source restored and PS1 rebuilt. | Try the same allocator-era split-residency pattern that promoted WALKSTUF1 low, but on the remaining WALKSTUF1 high late cluster. The read plan ranked `344..376` as a high-upside late cluster and `344..360` was already a known active read group, so this tested whether paying that edge during setup from a separate TRANSIENT buffer could reduce active-loop CD pressure without growing the main CACHE setup allocation. | Temporarily added a high-tide third setup segment at `344..376` in TRANSIENT while keeping accepted high segments `198..244` and `286..344` in CACHE. After the 32-sector variant failed, shrank the edge to `344..360` as the allocator-fit binary check. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-high-split-seg344-376-current/20260519-122928-1771689/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-high-split-seg344-360-current/20260519-123210-1787095/summary.json`. | Failed/no promotion. Both variants produced no `JCPERF2` and halted at the same allocator boundary after `walkPilotInit clean-buf alloc failed`: `JCBSOD-FATAL CACHE exhausted (region+libc both): req=40960 have=20232`, with `memCacheUsed=635128` and `sceneAllocBalance=12`. The 32-sector edge used `memTransientUsed=781776`; the 16-sector edge lowered TRANSIENT to `749008` but did not change the CACHE failure, so the split edge itself does not make W1-high structurally viable on this baseline. | Close W1-high setup edge `344..376`/`344..360` until CACHE headroom is reduced elsewhere. The late cluster remains interesting, but the current scene already sits on the walk-clean CACHE cliff; future W1-high work should first shrink CACHE pressure or use no-code/render-data reductions rather than adding retained setup residency. |
| 2026-05-19 | `walkstuf1-high-clean64-current` | Log-only; source restored and PS1 rebuilt. | Test whether reducing WALKSTUF1 high clean-rect strip size could free enough cache/clean headroom to support the remaining late-cluster work. This was the direct follow-up to the split-edge structural failure, using a smaller high-tide clean cap without changing retained setup coverage or pack layout. | Temporarily set WALKSTUF1 high `gFgCleanRectMaxBytes` to `64 KiB` while leaving accepted setup segments `198..244` and `286..344` intact. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-clean64-current/20260519-123556-1808779/summary.json`. Source was restored and the PS1 build passed afterward. | Failed/no promotion. The cap was structurally clean but phase-negative: W1 high regressed scene/loop/target `1807/1471/1440 -> 1811/1475/1438`, overrun `31 -> 37`, blocking/refill `57/13 -> 67/15`, due `10 -> 11`, and reads stayed `45`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Keep W1-high at the default clean cap. Smaller high-tide clean strips do free a different memory shape, but they perturb the accepted CD/refill cadence in the wrong direction; W1-high still needs data-shape or ownership work rather than clean-cap narrowing. |
| 2026-05-19 | `building2-high-setupseg249-265-current` | Log-only; source restored and PS1 rebuilt. | Test setup-paid ownership for the BUILDING2 high `249..265` cluster after the direct active read row `249..261` was phase-negative. The hypothesis was that a separate TRANSIENT retained setup segment could remove the active read pressure without changing append cadence in the playback loop. | Temporarily added a high-tide third setup segment `249..265` in TRANSIENT, leaving accepted BUILDING2 high CACHE segments `3..35` and `202..242` unchanged. Focused artifact: `scratch/ps1-perf-iterate/building2-high-setupseg249-265-current/20260519-124148-1842862/summary.json`. Source was restored and the PS1 build passed afterward. | Failed/no promotion. The setup-paid version was worse than the direct row: BUILDING2 high regressed scene/loop/target `1621/1347/1313 -> 1641/1358/1312`, overrun `34 -> 46`, blocking/refill `39/16 -> 59/22`, due `5 -> 7`, and loop reads stayed `45`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close B2-high `249..265` as both direct read-group and setup-paid residency. This cluster is cadence-sensitive; remaining B2-high work should use render/upload/data-shape reductions or generated deadline ownership, not hand-paid setup residency for the same sector family. |
| 2026-05-19 | `building2-high-nonpreserve-trim-current` | Log-only; pack restored and PS1 rebuilt. | Test a larger BUILDING2 high data-shape swing after preserve-offset draw-tail trims were exact-flat. The non-preserving transform trims all parseable draw tails and lets payload offsets compact, asking whether a `17.9 KiB` active-payload cut can shift the remaining high-tide boundary in a useful direction. | Temporarily replaced `generated/ps1/foreground/BUILDING2.FG2` with `scratch/current-transform-scans-20260519/BUILDING2.trim.FG2`, generated by `compact-fgp3-trim-draw-tails.py` without preserving offsets. Transform summary: `scratch/current-transform-scans-20260519/BUILDING2.trim.summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-nonpreserve-trim-current/20260519-124445-1860035/summary.json`. The pack was restored from `scratch/current-run-backups/BUILDING2.before-nonpreserve-trim.FG2` and the PS1 build passed afterward. | Failed/no promotion. The payload cut changed physical phase in the wrong direction: BUILDING2 high regressed scene/loop/target `1621/1347/1313 -> 1626/1353/1312`, overrun `34 -> 41`, blocking/refill `39/16 -> 53/17`, due `5 -> 8`, and loop reads `45 -> 47`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close whole-pack non-preserve BUILDING2 high draw-tail compaction on this baseline. The byte savings are real, but current B2 high is phase-bound; future data-shape work needs targeted boundary moves or generated ownership, not broad offset compaction. |
| 2026-05-19 | `walkstuf1-low-nonpreserve-trim-current` | Log-only; pack restored and PS1 rebuilt. | Test the remaining WALKSTUF1 low broad data-shape opportunity after the split setup-edge promotion. The transform trims about `30.3 KiB` from active payload across 13 entries and compacts offsets, so it could have shifted W1-low's final `26` VBlank gap if the new physical phase landed well. | Temporarily replaced `generated/ps1/foreground/WALK1LOW.FG2` with `scratch/current-transform-scans-20260519/WALK1LOW.trim.FG2`. Transform summary: `scratch/current-transform-scans-20260519/WALK1LOW.trim.summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-nonpreserve-trim-current/20260519-124715-1874812/summary.json`. The pack was restored from `scratch/current-run-backups/WALK1LOW.before-nonpreserve-trim.FG2` and the PS1 build passed afterward. | Failed/no promotion. The compacted physical phase regressed W1 low: scene/loop/target `1815/1473/1447 -> 1821/1479/1441`, overrun `26 -> 38`, blocking/refill `41/7 -> 43/16`, reads `36 -> 34`, and due stayed `5`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close broad W1-low non-preserve draw-tail compaction. Like B2 high, read-count and payload savings are real but phase-negative; remaining W1-low data-shape work needs targeted boundary proofs instead of whole-pack compaction. |
| 2026-05-19 | `visitor3-high-frame122-d4-current` | Log-only; pack/source restored and PS1 rebuilt. | Test a tiny VISITOR3 high D4 candidate after larger D4 and residency attempts either regressed or exhausted memory. Frame `122` shrinks `2616 -> 1233` bytes with only four D4 commands and a modeled one-sector drop, so it was the lowest-decode-risk high-tide D4 boundary still open. | Temporarily rewrote `generated/ps1/foreground/VISITOR3.FG2` frame `122` as a previous-frame D4 against frame `121` and enabled only high-tide frame `122` in `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/visitor3-high-frame122-d4-current/summary-frame122.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-high-frame122-d4-current/20260519-125144-1900810/summary.json`. The pack was restored from `scratch/visitor3-high-frame122-d4-current/VISITOR3.before.FG2`, the predicate was restored, and the PS1 build passed afterward. | Failed/no promotion. The candidate was exact-flat and failed only `--require-improvement`: VISITOR3 high stayed scene/loop/target `1389/1075/1044`, overrun `31`, blocking/refill `45/3`, reads/due `4/3`, pack LBA `22619`, and PS-EXE bucket `233472`. The source probe grew `fgRuntimeLoadSceneFrame` by `20` bytes while adding no speed-bearing movement. | Close standalone VISITOR3-high frame122 D4. One tiny sector drop is not enough to change the current high-tide read boundary; future VISITOR3 work should batch only with a boundary proof or use placement/ownership changes rather than isolated D4 predicates. |
| 2026-05-19 | `visitor3-low-setup-rebalance-current` | Log-only; source restored and PS1 rebuilt. | Test whether VISITOR3 low could move or extend retained setup coverage onto the read-plan's hot `248..272` cluster without changing pack layout. The first binary variant kept memory flat by moving the existing `281..305` segment to `248..272`; the second kept `281..305` and tried to extend the existing `206..232` segment forward to cover the cluster. | Temporarily changed `FG_VISITOR3_LOW_SETUP_SEGMENT_START` from `281` to `248`, then restored that and changed `FG_VISITOR3_LOW_SETUP_SEGMENT3_BYTES` from `26` to `54` sectors. Artifacts: rebalance `scratch/ps1-perf-iterate/visitor3-low-seg248-272-current/20260519-125629-1928081/summary.json`; extension `scratch/ps1-perf-iterate/visitor3-low-seg206-260-current/20260519-125828-1939505/summary.json`. Source was restored and the PS1 build passed afterward. | Failed/no promotion. The same-size rebalance completed but regressed VISITOR3 low `1369/1065/1039 -> 1370/1066/1038`, overrun `26 -> 28`, blocking `75 -> 83`, reads `18 -> 20`, and due `14 -> 16`. The extension produced no `JCPERF2` and halted with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=215040 have=168956`, with `memTransientUsed=617476`. | Keep the accepted VISITOR3 low retained segments `281..305`, `150..177`, and `206..232`. The terminal segment is load-bearing, and the allocator cannot afford enough extension to reach the `248` cluster; future V3-low work should target generated ownership or no-decode placement instead of bigger TRANSIENT setup residency. |
| 2026-05-19 | `drawgap-b2-w1low-current` | Log-only; packs restored and PS1 rebuilt. | Test a broad no-code FGP3 draw-span compaction swing for the remaining BUILDING2 high and WALKSTUF1 low rows. The transform preserves entry offsets and file size while merging draw spans across pixels already known to hold the same palette value, so it offered large payload savings without changing pack LBA or source layout. | Regenerated the current-pack compaction with `merge_known_draw_gaps.py` and tested aggressive `BUILDING2` gap-2 plus `WALK1LOW` gap-4, then conservative gap-1 for both packs. Transform summaries: `scratch/big-swing-drawgap-current/BUILDING2.gap2.summary.json`, `scratch/big-swing-drawgap-current/WALK1LOW.gap4.summary.json`, `scratch/big-swing-drawgap-current/BUILDING2.gap1.current.summary.json`, and `scratch/big-swing-drawgap-current/WALK1LOW.gap1.current.summary.json`. Perf artifacts: aggressive `scratch/ps1-perf-iterate/drawgap-b2-w1low-big-swing/20260519-130950-2003574/summary.json`; conservative `scratch/ps1-perf-iterate/drawgap-b2-w1low-gap1/20260519-131747-2048451/summary.json`. | Failed/no promotion. Aggressive compaction saved about `79 KiB` in BUILDING2 and `96 KiB` in WALK1LOW but regressed BUILDING2 high `1621/1347/1313 -> 1622/1349/1312`, overrun `34 -> 37`, blocking/refill `39/16 -> 41/17`; W1 low regressed `1815/1473/1447 -> 1818/1476/1438`, overrun `26 -> 38`, blocking/refill `41/7 -> 43/22`. Conservative gap-1 still regressed BUILDING2 high `1347 -> 1349` loop and W1 low `1473 -> 1476` loop, with W1-low refill still at `22`. | Close broad preserve-offset draw-gap compaction for BUILDING2 and WALK1LOW on this baseline. Payload and span counts drop substantially, but the added drawn pixels and changed decode/render cadence are phase-negative. Future pack data-shape work should target whole-read boundary removal or generated deadline ownership, not broad same-value span merging. |
| 2026-05-19 | `upload-gap0-4-five-yellow` | Log-only; renderer restored and PS1 rebuilt. | Sweep the dirty-upload band merge knee against all five remaining yellow rows after the accepted BUILDING4-low gap-8 promotion. The hypothesis was that a smaller merge gap could cut full-width upload bytes or avoid row overmerge for VISITOR3, BUILDING2 high, and WALKSTUF1 high/low without touching pack data or CD scheduling. | Temporarily changed `GR_UPLOAD_BAND_MERGE_GAP` from the accepted `8` to `0`, ran the five-yellow strict gate, then changed it to `4` and repeated. Artifacts: gap0 `scratch/ps1-perf-iterate/upload-gap0-five-yellow/20260519-132920-2113299/summary.json`; gap4 `scratch/ps1-perf-iterate/upload-gap4-five-yellow/20260519-133716-2158313/summary.json`. | Failed/no promotion. Both variants were exact-flat on every speed-bearing metric for all five yellow rows: VISITOR3 high `1075/1044`, BUILDING2 high `1347/1313`, VISITOR3 low `1065/1039`, WALKSTUF1 high `1471/1440`, and WALKSTUF1 low `1473/1447`, with unchanged overrun, blocking, refill, reads/due, pack LBAs, and PS-EXE bucket. Gap0 changed upload bytes/rects but did not improve timing; gap4 also stayed flat while shifting work volume, e.g. B2 high upload `21879040/851 -> 21603200/912` and W1 low `17875840/624 -> 17782400/647`. | Keep the accepted upload merge gap `8`. The renderer merge-threshold lane is exhausted for the five current yellow rows: upload byte/rect tradeoffs are too small to move loop timing. Remaining under-green work should shift to generated read-deadline ownership, targeted no-decode payload relocation, or pack-emitted/precomposed render data rather than global upload-band tuning. |
| 2026-05-19 | `walkstuf1-low-trim-retarget-current` | Log-only; pack/source restored before promotion. | Test the broad W1-low physical compaction again, but this time retarget setup and read ownership to the compacted sector layout instead of judging only the shifted full pack. The binary question was whether the prior broad compaction was bad because ownership still pointed at old sectors. | Temporarily replaced `generated/ps1/foreground/WALK1LOW.FG2` with `scratch/current-transform-scans-20260519/WALK1LOW.trim.FG2`, retargeted the low setup slices to `185..285` plus `286..292`, then swept wide/narrow read groups, slack5, wider setup overlap, setup-only, read-only, prepare-first, and a 36 KiB stream window. Main artifact: `scratch/ps1-perf-iterate/walkstuf1-low-trim-retarget-current/20260519-140059-2291561/summary.json`; representative variants: `walkstuf1-low-trim-retarget-full-current`, `walkstuf1-low-trim-retarget-slack5-current`, `walkstuf1-low-trim-retarget-setupoverlap-current`, `walkstuf1-low-trim-setup-retarget-current`, `walkstuf1-low-trim-read-retarget-current`, and `walkstuf1-low-trim-retarget-window36-current`. | Failed/no promotion. The best retargeted broad compaction was visibly useful but failed the strict no-regression gate: W1-low improved loop `1473 -> 1462`, target `1447 -> 1444`, overrun `26 -> 18`, blocking `41 -> 29`, and reads `36 -> 34`, but hidden refill regressed `7 -> 10`. Other variants either kept the refill debt or gave back the loop gain. | Close full W1-low non-preserve compaction even with sector retargeting on this baseline. The useful signal is real, but hidden refill debt is not acceptable; continue with targeted boundary trims or generated deadline ownership rather than whole-pack offset compaction. |
| 2026-05-19 | `walkstuf1-low-frame132-nonpreserve-current` | `ps1: trim walkstuf1 low frame132 payload` | Promote the targeted version of the W1-low data-shape swing after broad compaction failed refill. Frame132/source240 was small enough to trim as a single-entry non-preserve candidate, asking whether one boundary move could keep the useful read/loop signal without the full-pack phase debt. | Rebuilt `WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py` for frame `132` only, keeping file size `1535263`, pack LBA/sectors, and PS-EXE bucket fixed while reducing active payload `790208 -> 788773` (`1435` bytes). Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-frame132-nonpreserve-current/20260519-141917-2396414/summary.json`; no-regression five-yellow canary: `scratch/ps1-perf-iterate/walkstuf1-low-frame132-nonpreserve-canaries-noregress/20260519-142843-2449554/summary.json`; transform summary: `scratch/current-transform-scans-20260519/WALK1LOW.frame132.trim.summary.json`. | Promoted. W1-low improves scene/loop/target `1815/1473/1447 -> 1812/1470/1445`, overrun `26 -> 25`, blocking `41 -> 35`, loop reads/read time `36/186 -> 31/163`, due `5 -> 4`, and target speed `98.235% -> 98.299%`; hidden refill stays `7`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stayed flat. | Keep the frame132 trim as the new W1-low baseline. Public rollup moves to `+0.2431%` over target / `99.7603%` target speed; raw signed rollup is about `-0.4738%` / `100.4906%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology total is about `17.22` public over-target points removed and `+12.72` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `walkstuf1-low-rg378-390-current` | `ps1: add walkstuf1 low read group 378` | Split the current W1-low generated read-plan batch after the four-group probe showed read/blocking signal but hidden refill debt. The binary question was whether the lowest-risk balanced `378..390` group could keep the CD-pressure win without the broader batch's refill regression. | First tested the batch `113..129`, `136..160`, `160..184`, and `378..390` at `scratch/ps1-perf-iterate/walkstuf1-low-planbatch113-390-current/20260519-152924-2793909/summary.json`; then kept only `378..390` with focused proof `scratch/ps1-perf-iterate/walkstuf1-low-rg378-390-current/20260519-153117-2804581/summary.json` and five-yellow no-regression canary `scratch/ps1-perf-iterate/walkstuf1-low-rg378-390-canaries-flat-ok/20260519-154041-2858310/summary.json`. | Promoted. Failed/no promotion for the broader batch: it saved reads `31 -> 26`, blocking `35 -> 33`, and due `4 -> 3` while keeping loop flat, but refill regressed `7 -> 15`; close `113..129`, `136..160`, and `160..184` together until generated scheduler/deadline ownership can pay their refill cost. The isolated `378..390` group improves W1-low scene/loop/target `1812/1470/1445 -> 1812/1470/1446`, overrun `25 -> 24`, blocking/refill `35/7 -> 34/6`, loop reads/read time `31/163 -> 30/159`, and target speed `98.299% -> 98.367%`; due stays `4`, pack LBA/sectors stay `25641/750`, and the PS-EXE bucket stays `233472`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stayed exact-flat. | Keep `378..390` as the new W1-low baseline. Public rollup moves to `+0.2425%` over target / `99.7608%` target speed; raw signed rollup is about `-0.4744%` / `100.4911%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology total remains about `17.22` public over-target points removed and `+12.72` public target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `walkstuf1-high-rg345-369-current` | Log-only; source restored and PS1 rebuilt. | Test the top refreshed W1-high current read-candidate row after the W1-low `{378..390}` promotion closed three low-side batch ranges. The `345..369` row promised four saved reads but was marked scheduler-owned because it touches a tight visible gap; this was the direct-table binary check before moving back to data-shape or generated ownership work. | Temporarily inserted `{345,369,0}` after the accepted `{344,360,0}` row in `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran the focused W1-high gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg345-369-current/20260519-160124-2974302/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row reduced loop reads `45 -> 41`, but it was phase-negative: W1-high regressed scene/loop/target `1807/1471/1440 -> 1813/1477/1441`, overrun `31 -> 36`, blocking/refill `57/13 -> 63/14`, and due stayed `10`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-high direct read group `345..369`. The remaining W1-high late cluster still needs generated deadline/refill ownership or a render/data-shape reduction; raw grouped appends keep converting nominal saved reads into visible cadence debt. |
| 2026-05-19 | `visitor3-low-d4-122-124-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the next VISITOR3-low data-shape swing after the current D4 scan showed two very cheap one-sector drops: frame `122` shrinks `2616 -> 1233` bytes with `4` commands, and frame `124` shrinks `3293 -> 1949` bytes with `5` commands. The binary question was whether combining the two tiny-decode deltas could move the accepted low-tide read boundary without perturbing high tide. | Temporarily rewrote `generated/ps1/foreground/VIST3LOW.FG2` frames `122` and `124` as previous-frame D4 deltas against frames `121` and `123`, and enabled only low-tide VISITOR3 frames `122`/`124` in `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/visitor3-low-d4-122-124-current/summary.json`; focused gate: `scratch/ps1-perf-iterate/visitor3-low-d4-122-124-current/20260519-145002-2568518/summary.json`. The original pack was restored from `scratch/visitor3-low-d4-122-124-current/VIST3LOW.before.FG2`, the source predicate was restored, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The combined delta saved `2727` active payload bytes but moved cadence negative: VISITOR3 low regressed scene/loop/target `1369/1065/1039 -> 1396/1075/1040`, overrun `26 -> 35`, blocking `75 -> 77`, and no key metric improved; hidden refill stayed `0`, reads stayed `18`, due stayed `14`, pack LBA stayed `23379`, and the PS-EXE bucket stayed `233472`. | Close standalone VISITOR3-low frames `122`/`124` D4 on this baseline. Tiny one-sector deltas are still not enough without read-deadline ownership; future V3-low D4 should require a boundary proof or pair with placement/generated scheduling instead of only enabling more decode predicates. |
| 2026-05-19 | `visitor3-low-d4-122-124-split-current` | Log-only; packs/source restored and PS1 rebuilt. | Split the failed VISITOR3-low frame `122`/`124` D4 pair to check whether only one of the two tiny deltas caused the cadence loss. This keeps the binary path honest before closing the low-tide one-sector D4 mini-cluster. | First enabled only frame `122`, using transform summary `scratch/visitor3-low-d4-122-current/summary.json` and focused artifact `scratch/ps1-perf-iterate/visitor3-low-d4-122-current/20260519-145317-2587334/summary.json`. Then restored and enabled only frame `124`, using transform summary `scratch/visitor3-low-d4-124-current/summary.json` and focused artifact `scratch/ps1-perf-iterate/visitor3-low-d4-124-current/20260519-145503-2597912/summary.json`. Each run kept file size, pack LBA, and the `233472` byte PS-EXE bucket fixed; after the probes the original pack/source were restored and `./scripts/build-ps1.sh` passed. | Failed/no promotion. Both single-frame variants exactly reproduced the pair's negative timing profile: VISITOR3 low regressed `1369/1065/1039 -> 1396/1075/1040`, overrun `26 -> 35`, blocking `75 -> 77`, hidden refill stayed `0`, reads stayed `18`, due stayed `14`, and no key metric improved. | Close VISITOR3-low frames `122` and `124` as standalone or paired D4 predicates. Their sector drops perturb the low-tide stage/window cadence before saving an observed read, so the remaining V3-low D4 work needs generated deadline placement or a larger placement-aware change rather than cheap same-offset deltas. |
| 2026-05-19 | `visitor3-low-readgroup-current` | Log-only; source restored and PS1 rebuilt. | Test VISITOR3-low direct read ownership after the same-offset D4 lane failed. The large `248..264` candidate promised three saved reads but carried tight-cadence risk; the smaller `86..98` candidate was the planner's low-visible-risk one-read row. | Temporarily added a low-tide VISITOR3 stream-read-group table and routed it through the existing grouped append path. First tested `{248,264,0}` with artifact `scratch/ps1-perf-iterate/visitor3-low-rg248-264-current/20260519-145833-2617997/summary.json`, then changed the table to `{86,98,0}` with artifact `scratch/ps1-perf-iterate/visitor3-low-rg86-98-current/20260519-150014-2627925/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `{248,264}` was phase-negative: VISITOR3 low regressed `1369/1065/1039 -> 1396/1074/1040`, overrun `26 -> 34`, and blocking `75 -> 77` with reads still `18`. `{86,98}` did fire and saved one read (`18 -> 17`), but still regressed `1369/1065/1039 -> 1396/1074/1039`, overrun `26 -> 35`, and blocking `75 -> 79`. Hidden refill stayed `0` and the pack LBA/PS-EXE bucket stayed fixed in both runs. | Close VISITOR3-low direct grouped appends for these current candidates. The scheduler can collapse reads, but static append ownership lowers visible cadence before the saved read helps; future V3-low read work needs generated per-frame deadlines or pack placement, not another hand table row. |
| 2026-05-19 | `walkstuf1-low-trim-retarget-tight24-current` | Log-only; pack/source restored and PS1 rebuilt. | Reopen the useful W1-low broad-compaction signal with a hidden-refill fix attempt. The previous retargeted compaction had strong visible wins but failed because refill rose `7 -> 10`; this tested whether a low-tide `24 KiB` tight-window cap could preserve the gains while avoiding hidden refill debt. | Rebuilt the current `WALK1LOW.FG2` through `compact-fgp3-trim-draw-tails.py --copy-unparseable --pad-to-input-size`, trimming active payload `788773 -> 759889`, retargeted setup coverage to `185..285` plus `286..292`, then tested a `24 KiB` low-tide tight-window cap first at held slack `<=8` and then across all held slack (`<=80`). Transform summary: `scratch/walkstuf1-low-trim-retarget-tight24-current/trim.summary.json`; focused artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-trim-retarget-tight24-current/20260519-150403-2649718/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-trim-retarget-tight24-allslack-current/20260519-150601-2661080/summary.json`. The accepted pack/source were restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both caps produced the same profile: W1-low improved scene/loop/target `1812/1470/1445 -> 1808/1468/1445`, overrun `25 -> 23`, and blocking `35 -> 31`, but hidden refill still regressed `7 -> 10`; reads stayed `31`, due stayed `5`, pack LBA stayed `25641`, and the PS-EXE bucket stayed `233472`. | Close simple W1-low tight-window capping as the refill fix for broad compaction. The visible signal remains real, but refill debt is coming from ownership/timing rather than just oversized speculative windows; future retry needs generated refill/deadline ownership or a different targeted boundary move. |
| 2026-05-19 | `yellow-prepare-first-current` | Log-only; scheduler source restored and PS1 rebuilt. | Test a scheduler-level big swing after byte-only and static read-group lanes stopped moving the remaining yellow rows. The experiment broadened the existing W1-low prepare-before-window rule so VISITOR3 high/low, WALKSTUF1 high/low, and BUILDING2 high would let visual prepare claim the slack-4 slot before speculative stream-window refill. | Temporarily changed the held-frame scheduler branch in `foreground_pilot.c`, built the PS1 image, and ran the strict five-yellow gate against the accepted frame132 baseline. Artifact: `scratch/ps1-perf-iterate/yellow-prepare-first-current/20260519-151327-2703423/summary.json`. After the failed gate, the scheduler source was restored to the accepted W1-low-only rule and `./scripts/build-ps1.sh` passed. | Failed/no promotion. VISITOR3 high, VISITOR3 low, and W1-low were exact-flat. W1-high proved the intended signal but still failed: blocking improved `57 -> 45`, yet loop/overrun/refill regressed `1471/31/13 -> 1472/32/15`. BUILDING2 high regressed visibly: scene/loop/target `1621/1347/1313 -> 1625/1351/1311`, overrun `34 -> 40`, blocking `39 -> 45`, and refill `16 -> 19`. | Close broad prepare-before-window scheduling for the yellow set. The W1-high blocking gain is real but needs a scene-specific refill/deadline guard before it can promote; BUILDING2 high should not use this rule. Future scheduler work should be generated/scene-local rather than globally broadening the slack-4 prepare owner. |
| 2026-05-19 | `visitor3-low-rg239-263-current` | Log-only; source restored and PS1 rebuilt. | Test the top refreshed VISITOR3-low generated read-candidate row after the W1-low `{378,390}` promotion. The `239..263` row promised four saved reads but was marked scheduler-owned because it spans a tight visible gap; this was the direct-table binary check before moving to generated ownership or data-shape work. | Temporarily added a VISITOR3 low-only stream read group `{239,263,0}`, rebuilt, and ran the strict focused VISITOR3-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-rg239-263-current/20260519-160540-2998663/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was phase-negative with no key metric improvement: VISITOR3 low regressed scene/loop `1369/1065 -> 1371/1067`, target moved `1039 -> 1041`, blocking rose `75 -> 76`, hidden refill stayed `0`, reads stayed `18`, due stayed `14`, pack LBA stayed `23379`, and the PS-EXE bucket stayed `233472`. | Close VISITOR3-low direct read group `239..263`. The cluster is real, but static grouped append ownership still converts modeled read savings into visible cadence debt; future V3-low read work needs generated per-frame deadline/refill ownership or pack placement instead of another hand table row. |
| 2026-05-19 | `visitor3-low-rg256-268-current` | Log-only; source restored and PS1 rebuilt. | Test the next refreshed VISITOR3-low current read-candidate row after `239..263` failed. The `256..268` row was a smaller 12-sector subset with two modeled saved reads, still marked scheduler-owned because it lives inside the same tight visible cadence cluster. | Temporarily added a VISITOR3 low-only stream read group `{256,268,0}`, rebuilt, and ran the strict focused VISITOR3-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-rg256-268-current/20260519-160924-3020341/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The smaller row reproduced the same negative timing profile: VISITOR3 low regressed scene/loop `1369/1065 -> 1371/1067`, target moved `1039 -> 1041`, blocking rose `75 -> 76`, hidden refill stayed `0`, reads stayed `18`, due stayed `14`, pack LBA stayed `23379`, and the PS-EXE bucket stayed `233472`. | Close VISITOR3-low direct read group `256..268`. This cluster is consistently not promotable through static grouped appends; continue with generated deadline/refill ownership or different pack/data-shape work rather than another hand table in this same cadence pocket. |
| 2026-05-19 | `visitor3-low-rg256-272-current` | Log-only; source restored and PS1 rebuilt. | Finish the current VISITOR3-low direct read-group lane with the last wider row in the same cadence pocket after `239..263` and `256..268` both produced the same phase-negative result. | Temporarily added a VISITOR3 low-only stream read group `{256,272,0}`, rebuilt, and ran the strict focused VISITOR3-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/visitor3-low-rg256-272-current/20260519-161237-3039027/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row again reproduced the same profile: VISITOR3 low regressed scene/loop `1369/1065 -> 1371/1067`, target moved `1039 -> 1041`, blocking rose `75 -> 76`, hidden refill stayed `0`, reads stayed `18`, due stayed `14`, pack LBA stayed `23379`, and the PS-EXE bucket stayed `233472`. | Close VISITOR3-low direct read groups `256..272`, `239..251`, and `248..260` as dominated subsets of the now-proven failed tight-cadence pocket. The direct hand-table lane is closed for current VISITOR3 low; future attempts need generated deadline/refill ownership, retained-residency redesign, or pack placement changes that preserve the accepted three setup clusters. |
| 2026-05-19 | `walkstuf1-low-rg174-198-current` | Log-only; source restored and PS1 rebuilt. | Test the top refreshed WALKSTUF1-low current read-candidate row after VISITOR3-low direct appends were closed. The `174..198` row promised four saved reads but overlapped the earlier broad-batch refill-debt zone, so it needed isolated proof. | Temporarily inserted `{174,198,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg174-198-current/20260519-161542-3056878/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The group was refill-negative: W1-low regressed scene/loop/target `1812/1470/1446 -> 1816/1474/1445`, overrun `24 -> 29`, blocking/refill `34/6 -> 36/16`, due improved `4 -> 3`, and reads stayed `30`; pack LBA stayed `25641` and the PS-EXE bucket stayed `233472`. | Close W1-low direct read group `174..198`. The useful due signal is outweighed by hidden refill and visible loop debt; remaining W1-low grouped reads need generated deadline/refill ownership rather than another static append in this zone. |
| 2026-05-19 | `walkstuf1-low-rg179-203-current` | Log-only; source restored and PS1 rebuilt. | Test the second four-read W1-low candidate in the same mid-pack append pocket after `174..198` failed with refill debt. This isolated whether shifting the group right could keep the current loop/refill baseline while still saving work. | Temporarily inserted `{179,203,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg179-203-current/20260519-161832-3073515/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat and failed `--require-improvement`: W1-low stayed scene/loop/target `1812/1470/1446`, overrun `24`, blocking/refill `34/6`, reads `30`, due `4`, pack LBA `25641`, and PS-EXE bucket `233472`. | Close W1-low direct read group `179..203`. Unlike `174..198`, this placement is not harmful, but it does not move speed; keep searching for changes that actually reduce loop/block/refill or target overrun. |
| 2026-05-19 | `walkstuf1-low-rg167-183-current` | Log-only; source restored and PS1 rebuilt. | Test the top remaining smaller W1-low direct read row after both 24-sector rows failed or went flat. The `167..183` row was interesting because it overlapped the earlier refill-debt batch but had a lower visible-cost score. | Temporarily inserted `{167,183,0}` into `kWalkstuf1LowReadGroups12[]`, then tried `{167,183,5}` and `{167,183,8}` min-slack guards, and finally paired `{167,183,8}` with a `40 KiB -> 32 KiB` W1-low stream window. Artifacts: unguarded `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-current/20260519-162126-3090318/summary.json`; slack5 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-slack5-current/20260519-162334-3102707/summary.json`; slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-slack8-current/20260519-162521-3113099/summary.json`; window32 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-window32-current/20260519-162719-3124537/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. All variants produced the same useful but gated profile: W1-low improved scene/loop `1812/1470 -> 1810/1468`, overrun `24 -> 23`, and blocking `34 -> 32`, but target dipped `1446 -> 1445`, reads rose `30 -> 33`, and hidden refill regressed `6 -> 9`; due stayed `4`, pack LBA stayed `25641`, and the PS-EXE bucket stayed `233472`. | Close W1-low direct read group `167..183` for raw table, min-slack, and simple window-knee variants. This is a good generated-ownership lead, but not promotable under the no-regression gate until refill debt is explicitly owned or eliminated. |
| 2026-05-19 | `walkstuf1-low-rg120-136-current` | Log-only; source restored and PS1 rebuilt. | Test the next distinct W1-low direct-read pocket after the `167..183` near-miss. The `120..136` row promised two saved reads but belonged to the earlier broad-batch refill-debt region, so it needed isolated proof. | Temporarily inserted `{120,136,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg120-136-current/20260519-163019-3141975/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was strongly phase-negative: W1-low regressed scene/loop/target `1812/1470/1446 -> 1816/1474/1442`, overrun `24 -> 32`, blocking/refill `34/6 -> 39/11`, reads `30 -> 33`, and due `4 -> 5`; pack LBA stayed `25641` and the PS-EXE bucket stayed `233472`. | Close W1-low direct read group `120..136`. This early-mid cluster worsens every speed-bearing metric as a raw table append; future work here needs generated deadline ownership or a different pack/data-shape boundary. |
| 2026-05-19 | `walkstuf1-low-rg174-186-current` | Log-only; source restored and PS1 rebuilt. | Test a smaller left-edge subset of the failed `174..198` W1-low row to see whether dropping the tail avoided the hidden-refill spike. | Temporarily inserted `{174,186,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg174-186-current/20260519-163323-3159707/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The subset was worse than the wider row: W1-low regressed scene/loop/target `1812/1470/1446 -> 1818/1476/1441`, overrun `24 -> 35`, blocking/refill `34/6 -> 38/17`, reads `30 -> 33`, and due stayed `4`; pack LBA stayed `25641` and the PS-EXE bucket stayed `233472`. | Close W1-low direct read group `174..186`. The left edge of this mid-pack pocket is unsafe as a raw append; continue with different offsets or generated ownership rather than more left-edge subsets. |
| 2026-05-19 | `walkstuf1-low-rg182-194-current` | Log-only; source restored and PS1 rebuilt. | Test the right-edge subset of the W1-low mid-pack pocket after the left-edge `174..186` row regressed badly. | Temporarily inserted `{182,194,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg182-194-current/20260519-163612-3176137/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat and failed `--require-improvement`: W1-low stayed scene/loop/target `1812/1470/1446`, overrun `24`, blocking/refill `34/6`, reads `30`, due `4`, pack LBA `25641`, and PS-EXE bucket `233472`. | Close W1-low direct read group `182..194`. The right edge is safe but inert; the only W1-low direct-table signal remains the unpromotable `167..183` refill-debt near-miss. |
| 2026-05-19 | `walkstuf1-low-rg120-132-current` | Log-only; source restored and PS1 rebuilt. | Test the smaller early W1-low row after the wider `120..136` append regressed every speed-bearing metric. This checked whether dropping the right tail avoided the early-cluster refill/target penalty. | Temporarily inserted `{120,132,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg120-132-current/20260519-163901-3192559/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The smaller row still regressed hard: W1-low moved scene/loop/target `1812/1470/1446 -> 1818/1476/1440`, overrun `24 -> 36`, blocking/refill `34/6 -> 40/16`, reads `30 -> 33`, and due `4 -> 5`; pack LBA stayed `25641` and the PS-EXE bucket stayed `233472`. | Close W1-low direct read group `120..132`. Both early-cluster rows are now proven phase-negative as raw table appends; generated ownership or data-shape changes are required for any retry. |
| 2026-05-19 | `walkstuf1-low-rg366-378-current` | Log-only; source restored and PS1 rebuilt. | Test the late W1-low candidate adjacent to the accepted `{378,390}` group. The question was whether the previous-sector side of that cluster could add another one-read gain without refill debt. | Temporarily inserted `{366,378,0}` before the accepted `{378,390,0}` row in `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg366-378-current/20260519-164151-3209011/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row saved one loop read (`30 -> 29`) and kept blocking/refill flat (`34/6`), but still regressed scene/loop/overrun `1812/1470/24 -> 1813/1471/25` with no key metric improvement; target stayed `1446`, pack LBA stayed `25641`, and the PS-EXE bucket stayed `233472`. | Close W1-low direct read group `366..378`. The accepted `378..390` remains the only promotable late W1-low append; the previous-sector neighbor shifts cadence negative despite the read-count win. |
| 2026-05-19 | `walkstuf1-low-rg179-185-current` | Log-only; source restored and PS1 rebuilt. | Test the last exposed W1-low direct read-candidate row after the larger mid-pack and early/late variants were closed. | Temporarily inserted `{179,185,0}` into `kWalkstuf1LowReadGroups12[]`, rebuilt, and ran the strict focused W1-low gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-rg179-185-current/20260519-164443-3225757/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat and failed `--require-improvement`: W1-low stayed scene/loop/target `1812/1470/1446`, overrun `24`, blocking/refill `34/6`, reads `30`, due `4`, pack LBA `25641`, and PS-EXE bucket `233472`. | Close W1-low direct read group `179..185`. The refreshed direct-table candidate matrix is now exhausted for W1-low; remaining work should use generated deadline/refill ownership, pack/data-shape changes, or render/dirty-work reduction. |
| 2026-05-19 | `visitor3-high-setupseg103-cache-current` | Log-only; source restored and PS1 rebuilt. | Test a split-residency VISITOR3 high swing after the direct-read lane was exhausted. The idea was to merge the accepted overlapping `203..229` and `228..262` setup segments into one `203..262` segment, keep `277..293`, and spend extra CACHE on part of the active `103..127` loop window without increasing TRANSIENT clean-rect pressure. | First tried a full `103..127` CACHE-backed third segment. Artifact: `scratch/ps1-perf-iterate/visitor3-high-setupseg103-127-cache-current/20260519-170110-3317558/summary.json`. After it exhausted CACHE before `JCPERF2`, tested a smaller `113..127` segment. Artifact: `scratch/ps1-perf-iterate/visitor3-high-setupseg113-127-cache-current/20260519-170422-3335960/summary.json`. Both probes kept pack LBA and the `233472` byte PS-EXE bucket fixed; the source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Full `103..127` halted with `JCBSOD-FATAL CACHE exhausted` during clean-rect allocation (`req=48640`, `memCacheUsed=581124`, `memTransientUsed=771076`). The smaller `113..127` variant completed but was phase-negative: VISITOR3 high regressed scene/loop/target `1389/1075/1044 -> 1404/1086/1043`, overrun `31 -> 43`, blocking `45 -> 57`, and due `3 -> 4`; hidden refill improved `3 -> 0` but not enough to offset visible CD debt. | Close split CACHE-backed VISITOR3-high setup residency for the `103..127` loop window. The full slice exceeds the current clean/cache budget, and the smaller affordable tail shifts the terminal read cadence negative. Future V3-high work should reduce clean/upload memory first, use pack-side data-shape reductions, or add generated deadline ownership rather than another retained setup segment. |
| 2026-05-19 | `building2-high-trim-preserve-current` | Log-only; pack restored and PS1 rebuilt. | Test the no-decode BUILDING2 high trim-tail data-shape lane after scalar read groups had closed. The preserve-offset transform kept pack size/LBA fixed while shrinking hot frames around the max-payload cluster, especially entries `89`, `90`, and `91`, each dropping by two sectors. | Temporarily replaced `generated/ps1/foreground/BUILDING2.FG2` with the full preserve-offset trim from `scratch/current-transform-scans-20260519/BUILDING2.trim-preserve.FG2`, then split the three largest hot entries as isolated probes. Artifacts: full trim `scratch/ps1-perf-iterate/building2-high-trim-preserve-current/20260519-170819-3358905/summary.json`; entry89 `scratch/ps1-perf-iterate/building2-high-trim-entry89-current/20260519-171015-3370269/summary.json`; entry90 `scratch/ps1-perf-iterate/building2-high-trim-entry90-current/20260519-171157-3380518/summary.json`; entry91 `scratch/ps1-perf-iterate/building2-high-trim-entry91-current/20260519-171348-3391458/summary.json`. Transform summaries live under `scratch/building2-high-trim-preserve-current/`. | Failed/no promotion. The full trim reduced loop reads `45 -> 43` and hidden refill `16 -> 15`, but regressed scene/loop/overrun/blocking `1621/1347/34/39 -> 1622/1349/36/41`. The single-entry probes for `89`, `90`, and `91` all reproduced the same negative `1349/1313`, overrun `36`, blocking `41` profile while only saving one read (`45 -> 44`). Pack LBA and the PS-EXE bucket stayed fixed throughout. | Close BUILDING2 high same-offset trim-tail for the current hot cluster. The byte/sector reductions are real, but even isolated boundary moves shift the visible CD cadence negative before the saved sectors help; future B2-high data-shape work needs generated deadline ownership, a different placement strategy, or render/upload reduction instead of same-offset tail shrinking. |
| 2026-05-19 | `walkstuf1-high-trim-entry136-current` | `ps1: trim walkstuf1 high entry136 payload` | Test the WALKSTUF1 high preserve-offset trim scan after direct read groups and prepare-owner variants had closed. The full four-entry scan included three large early-prefix reductions plus one late active entry; the split check asked whether the late entry could be banked as a no-regression work-volume baseline even if the early prefix was phase-negative. | First tested the full preserve-offset trim from `scratch/current-transform-scans-20260519/WALKSTUF1.trim-preserve.FG2` with artifact `scratch/ps1-perf-iterate/walkstuf1-high-trim-preserve-current/20260519-171641-3408276/summary.json`. Then restored and trimmed only entry `136` / source frame `244`, with transform summary `scratch/walkstuf1-high-trim-preserve-current/entry136.summary.json`, focused artifact `scratch/ps1-perf-iterate/walkstuf1-high-trim-entry136-current/20260519-171838-3419779/summary.json`, and five-yellow no-regression canary `scratch/ps1-perf-iterate/walkstuf1-high-trim-entry136-canaries-flat-ok/20260519-172035-3431058/summary.json`. | Promoted as work-volume, not a VBlank speed win. The full batch regressed W1 high badly (`1471/1440 -> 1477/1433`, overrun `31 -> 44`, blocking/refill `57/13 -> 71/24`, reads `45 -> 50`). The isolated entry136 trim was exact-flat on W1 high and the five-yellow canary: W1 high stays `1471/1440`, overrun `31`, blocking/refill `57/13`, reads/due `45/10`, while the payload shrinks `3762 -> 2596` bytes and `3 -> 2` sectors. Pack footprint/LBA/sectors and PS-EXE bucket stay fixed. | Keep entry136 trimmed as the new W1-high pack baseline. Public rollup remains `+0.2425%` over target / `99.7608%` target speed; raw signed rollup remains about `-0.4744%` / `100.4911%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology speed total remains about `17.22` public over-target points removed and `+12.72` public target-speed points gained since the compact full-matrix baseline, with additional same-speed W1-high payload work removed. |
| 2026-05-19 | `walkstuf1-low-rg167-183-maxslack-current` | Log-only; source restored and PS1 rebuilt. | Convert the W1-low `167..183` near-miss into a narrower generated-owner shape by adding max-slack gating to grouped appends. The ungated row had a useful visible signal (`1470 -> 1468`, blocking `34 -> 32`) but failed because hidden refill rose `6 -> 9`; the question was whether only the low-slack subset could keep the speed win without refill debt. | Temporarily extended `TFgPilotReadGroup` with `maxSlackVBlanks`, added `{167,183}` to the W1-low table, and swept max slack `4`, `8`, `12`, `16`, `32`, plus the ungated `0` control. Artifacts: max4 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-max4-current/20260519-173821-3531495/summary.json`; max8 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-max8-current/20260519-174017-3542882/summary.json`; max12 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-max12-current/20260519-174402-3565027/summary.json`; max16 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-max16-current/20260519-174555-3576136/summary.json`; max32 `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-max32-current/20260519-174743-3586807/summary.json`; ungated control `scratch/ps1-perf-iterate/walkstuf1-low-rg167-183-max0-current/20260519-174208-3553803/summary.json`. | Failed/no promotion. Every bounded max-slack gate skipped the useful owner and stayed exact-flat at `1812/1470/1446`, overrun `24`, blocking/refill `34/6`, reads/due `30/4`. The ungated control reproduced the visible win but also reproduced the refill failure: `1810/1468/1445`, overrun `23`, blocking `32`, reads `33`, and hidden refill `9`. | Close scalar max-slack ownership for W1-low `167..183`. The only firing slot is the high-slack slot that creates the refill debt; making the gate narrower turns it inert. Future W1-low generated ownership needs an actual refill-budget owner or pack/data-shape change, not just min/max slack around the same grouped append. |
| 2026-05-19 | `walkstuf1-high-trim-entry57-current` | `ps1: trim walkstuf1 high entry57 payload` | Test the remaining W1-high preserve-offset trim from the failed full batch after entry136 promoted; entry55 was known risky and entry57/source70 had a one-sector no-decode tail trim. | Temporarily rewrote `generated/ps1/foreground/WALKSTUF1.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 57`. Transform summary: `scratch/walkstuf1-high-trim-entry57-current/entry57.summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-trim-entry57-current/20260519-175201-3611472/summary.json`; five-yellow canary: `scratch/ps1-perf-iterate/walkstuf1-high-trim-entry57-canaries-flat-ok/20260519-175348-3622153/summary.json`. | Promoted as work-volume, not a VBlank speed win. W1 high and the five-yellow canary are exact-flat at `1471/1440`, overrun `31`, blocking/refill `57/13`, reads/due `45/10`; entry57 shrinks `3409 -> 320` bytes and `3 -> 2` sectors; active payload drops `854521 -> 851432`; pack footprint/LBA/sectors and PS-EXE bucket stay fixed. | Keep entry57 trimmed on top of entry136. Public/raw rollups and bands remain unchanged: `+0.2425%` / `99.7608%` public, `-0.4744%` / `100.4911%` raw, `121` green / `5` yellow / `0` orange / `0` red. |
| 2026-05-19 | `visitor3-high-seg103-125-swap-current` | Log-only; source restored and PS1 rebuilt. | Try the larger VISITOR3-high active-window residency swing after direct read candidates were exhausted: trade the terminal `277..293` setup segment for the active `103..127` read window so the loop can avoid the late `103..127` transaction. | First tested full `103..127` as segment1 while keeping `203..229` and `228..262`; artifact `scratch/ps1-perf-iterate/visitor3-high-seg103-127-swap-current/20260519-181827-3760869/summary.json`. That requested `84` setup sectors and failed before `JCPERF2` with `TRANSIENT region+libc both exhausted: req=172032 have=168956`. Then tested the largest fitting front slice, `103..125` (`82` total setup sectors); artifact `scratch/ps1-perf-iterate/visitor3-high-seg103-125-swap-current/20260519-182124-3777607/summary.json`. | Failed/no promotion. The fitting slice completed but moved debt into visible CD cadence: VISITOR3 high regressed scene/loop/target `1389/1075/1044 -> 1423/1108/1041`, overrun `31 -> 67`, blocking `45 -> 84`, loop reads `4 -> 6`, and due misses `3 -> 5`; hidden refill stayed `3`. Pack LBA and the PS-EXE bucket stayed fixed. | Close terminal-to-active setup-segment swapping for V3 high. Full active-window coverage exceeds the current TRANSIENT ceiling, and the largest fitting slice is phase-negative. Reopen only after reducing clean/setup memory, adding a fourth allocator-safe buffer class, or introducing generated deadline ownership that can cover `103..127` without dropping the terminal slice. |
| 2026-05-19 | `walkstuf1-low-seg244-edge179-rg113-129-current` | `ps1: retarget walkstuf1 low setup edge` | Convert the W1-low `167..183`/early-mid near-miss family into an allocator-era setup/owner shape after raw read groups and max-slack gating failed on refill debt. The working hypothesis was that moving the main setup segment to `244..350`, preserving a small `179..185` setup edge, and adding only the `{113,129}` owner could keep the read-count win without the earlier broader batch's hidden-refill spike. | First tried a third retained `167..183` setup segment and clean-rect reductions, but the full variant exhausted CACHE/TRANSIENT. Then tested zero-new-buffer segment trades: `167..173`, `179..185`, `244..350 + 179..185`, and low-tide slack guards. Artifacts include `scratch/ps1-perf-iterate/walkstuf1-low-seg167-183-current/20260519-182738-3812923/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-edge179-185-current/20260519-184033-3886360/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-seg244-350-edge179-185-current/20260519-184426-3908887/summary.json`, and inert slack5 `scratch/ps1-perf-iterate/walkstuf1-low-seg244-350-edge179-185-slack5-current/20260519-184956-3940214/summary.json`. The promoted focused proof is `scratch/ps1-perf-iterate/walkstuf1-low-seg244-350-edge179-185-rg113-129-current/20260519-185205-3952617/summary.json`; the five-yellow canary is `scratch/ps1-perf-iterate/walkstuf1-low-seg244-edge179-rg113-129-canaries/20260519-185402-3963960/summary.json`. | Promoted as a same-target-speed CD-pressure win. The setup-only `244..350 + 179..185` shape improved loop/blocking/due but failed hidden refill (`1470/1446 -> 1468/1445`, blocking `34 -> 28`, due `4 -> 3`, refill `6 -> 10`). Adding `{113,129}` resolves the refill gate while keeping W1-low at `1470/1446`, overrun `24`, and due `4`; scene improves `1812 -> 1809`, blocking/refill improve `34/6 -> 33/5`, loop reads/read time improve `30/159 -> 26/150`, pack LBA/sectors stay `25641/750`, and the PS-EXE bucket stays `233472`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stayed flat. | Keep `244..350`, `179..185`, and `{113,129}` as the new W1-low CD-pressure baseline. Public/raw rollups and bands remain unchanged because `loop_vb/target_vb` stays `1470/1446`: `+0.2425%` / `99.7608%` public, `-0.4744%` / `100.4911%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.22` public over-target points removed and `+12.72` public target-speed points gained since the compact full-matrix baseline, with lower W1-low CD debt for the next scheduler/guarded probe (`160..176`). |
| 2026-05-19 | `walkstuf1-low-seg244-rg113-rg160-176-current` | Log-only; source restored and PS1 rebuilt. | Test the top refreshed W1-low scheduler/guarded candidate after the `244..350`/`179..185` plus `{113,129}` promotion. The read plan ranked `160..176` as the only balanced candidate left, with a one-read upside but a partial-overread/refill risk. | Temporarily added `{160,176}` to `kWalkstuf1LowReadGroups12[]` on top of the accepted W1-low setup/owner baseline, then tested raw, min-slack `8`, and min-slack `32`. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-seg244-rg113-rg160-176-current/20260519-191032-4057692/summary.json`; slack8 `scratch/ps1-perf-iterate/walkstuf1-low-seg244-rg113-rg160-176-slack8-current/20260519-191237-4069708/summary.json`; slack32 `scratch/ps1-perf-iterate/walkstuf1-low-seg244-rg113-rg160-176-slack32-current/20260519-191425-4080243/summary.json`. | Failed/no promotion. Raw and slack8 both produced the same visible near-miss but failed hidden refill: W1-low moved `1470/1446 -> 1468/1444`, blocking `33 -> 32`, but refill regressed `5 -> 8`; overrun stayed `24`, reads rose `26 -> 32`, and due stayed `4`. Slack32 skipped the useful owner and stayed exact-flat at `1470/1446`, blocking/refill `33/5`, reads `26`, and due `4`, so it is safe but inert. | Close W1-low `160..176` as a raw/min-slack retained read group on this baseline. It needs a true scheduler/CD budget owner, not a table row, because the only firing slot that moves loop time also steals hidden refill slack. |
| 2026-05-19 | `walkstuf1-low-directstage-owner-current` | Log-only; source restored and PS1 rebuilt. | Test direct-stage ownership for the remaining W1-low scheduler-owned ranges after raw grouped appends kept converting modeled read savings into hidden-refill or blocking debt. Unlike grouped appends, this owner reads only the staged entry payload for selected physical ranges, asking whether the scheduler can buy the same ranges without overreading the refill window. | Temporarily added a low-tide WALKSTUF1 sector-gated direct-stage rule in `fgRuntimeShouldDirectStageEntry()` and tested four variants against the current five-yellow baseline. Artifacts: `129..153` ungated `scratch/ps1-perf-iterate/walkstuf1-low-directstage129-153-current/20260519-192226-4125536/summary.json`; `160..176` ungated `scratch/ps1-perf-iterate/walkstuf1-low-directstage160-176-current/20260519-192421-4136686/summary.json`; `160..176` slack4-only `scratch/ps1-perf-iterate/walkstuf1-low-directstage160-176-slack4-current/20260519-192624-4148478/summary.json`; late `355..379` ungated `scratch/ps1-perf-iterate/walkstuf1-low-directstage355-379-current/20260519-192816-4159354/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `129..153` and slack4-only `160..176` were exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, and due `4`. Ungated `160..176` improved scene/loop `1809/1470 -> 1807/1468`, but target dropped `1446 -> 1443`, overrun regressed `24 -> 25`, refill regressed `5 -> 12`, and reads rose `26 -> 34`. Ungated `355..379` kept loop/overrun flat but regressed blocking `33 -> 38`, reads `26 -> 27`, and due `4 -> 5`. | Close W1-low direct-stage ownership for `129..153`, `160..176`, and `355..379` on this baseline. The only firing forms either no-op or trade speed for hidden-refill/visible-blocking debt; remaining W1-low work needs a real deadline/refill-budget scheduler or a pack/render data-shape change, not another hand-coded owner path. |
| 2026-05-19 | `walkstuf1-low-rg153-177-current` | Log-only; source restored and PS1 rebuilt. | Test the top remaining W1-low scheduler-owned read range after the direct-stage owner lane closed. The range `153..177` promised two saved reads with a longer first gap than the tight late pockets, so the question was whether it could keep the useful `160..176` loop/blocking signal with less hidden-refill debt. | Temporarily inserted `{153,177}` into `kWalkstuf1LowReadGroups12[]` on top of the accepted `244..350`/`179..185` plus `{113,129}` W1-low setup/owner baseline. Tested raw, min-slack `8`, and min-slack `32`; artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg153-177-current/20260519-193733-18247/summary.json`, slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg153-177-slack8-current/20260519-193925-29000/summary.json`, and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg153-177-slack32-current/20260519-194114-39755/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Raw and slack8 both improved scene/loop `1809/1470 -> 1807/1468` and blocking `33 -> 31`, but target tightened `1446 -> 1444`, hidden refill regressed `5 -> 6`, reads rose `26 -> 31`, and overrun stayed `24`, so the strict no-regression gate failed. Slack32 skipped the useful owner and stayed exact-flat at `1809/1470/1446`, blocking/refill `33/5`, reads/due `26/4`. | Close W1-low `153..177` as a raw/min-slack retained read group on this baseline. Like `160..176`, the only firing slot converts read savings into hidden-refill/target debt; the remaining W1-low rows need generated refill-budget ownership or pack/render data-shape work rather than another static table row. |
| 2026-05-19 | `walkstuf1-low-rg355-371-current` | `ps1: group walkstuf1 low 355 read range` | Test the next open W1-low scheduler-owned row after closing `153..177`. The range `355..371` was a late 16-sector pocket with two modeled saved reads adjacent to the accepted `{378..390}` row; the promotion question was whether it could lower CD work without repeating the direct-stage `355..379` visible-blocking regression. | Added `{355,371,0}` to `kWalkstuf1LowReadGroups12[]`, rebuilt in the unchanged `233472` byte PS-EXE bucket, ran the strict focused W1-low gate, then ran the five-yellow no-regression canary. Artifacts: focused `scratch/ps1-perf-iterate/walkstuf1-low-rg355-371-current/20260519-194519-63468/summary.json`; canary `scratch/ps1-perf-iterate/walkstuf1-low-rg355-371-canaries-flat-ok/20260519-194709-74452/summary.json`. | Promoted as same-speed CD work-volume, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, and due `4`, while loop reads/read time drop `26/150 -> 24/147`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high stayed exact-flat in the canary. | Keep `{355,371}` as the new W1-low read-pressure baseline. Public rollup remains `+0.2425%` over target / `99.7608%` target speed; raw signed rollup is about `-0.4744%` / `100.4911%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology speed total remains about `17.22` public over-target points removed and `+12.72` public target-speed points gained since the compact full-matrix baseline, with two fewer W1-low loop reads banked for the next scheduler/data-shape pass. |
| 2026-05-19 | `walkstuf1-low-rg147-171-current` | Log-only; source restored and PS1 rebuilt. | Test the refreshed top open W1-low scheduler-owned row after the `{355..371}` same-speed read-work promotion. The range `147..171` promised two modeled saved reads in the early-mid pocket, but overlapped the already phase-sensitive `153..177` and `160..176` family. | Temporarily inserted `{147,171}` into `kWalkstuf1LowReadGroups12[]` on top of the accepted W1-low `244..350`/`179..185`, `{113,129}`, and `{355,371}` baseline. Tested raw, min-slack `8`, and min-slack `32`; artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg147-171-current/20260519-200946-201439/summary.json`, slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg147-171-slack8-current/20260519-201142-212770/summary.json`, and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg147-171-slack32-current/20260519-201339-224328/summary.json`. | Failed/no promotion. Raw and slack8 were identical and badly phase-negative: W1-low regressed `1809/1470/1446 -> 1817/1478/1441`, overrun `24 -> 37`, blocking/refill `33/5 -> 44/16`, reads `24 -> 25`, and due `4 -> 5`. Slack32 skipped the harmful owner and stayed exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. | Close W1-low `147..171` as a static retained read group on this baseline. It confirms the early-mid pocket needs generated refill-budget ownership or data-shape changes; a raw table row burns visible and hidden slack, while high slack simply prevents the row from firing. |
| 2026-05-19 | `walkstuf1-low-rg153-169-current` | Log-only; source restored and PS1 rebuilt. | Test the narrower top W1-low scheduler-owned row after closing the broader `147..171` and prior `153..177` ranges. The question was whether the smaller `153..169` pocket could keep the one-VBlank visible blocking signal without the broader range's hidden-refill and target debt. | Temporarily inserted `{153,169}` into `kWalkstuf1LowReadGroups12[]` on top of the accepted W1-low `244..350`/`179..185`, `{113,129}`, and `{355,371}` baseline. Tested raw, min-slack `8`, and min-slack `32`; artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg153-169-current/20260519-201708-244560/summary.json`, slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg153-169-slack8-current/20260519-201902-255830/summary.json`, and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg153-169-slack32-current/20260519-202053-266680/summary.json`. | Failed/no promotion. Raw and slack8 were identical: scene/loop stayed `1809/1470`, blocking improved `33 -> 32`, and due stayed `4`, but target regressed `1446 -> 1445`, overrun regressed `24 -> 25`, refill regressed `5 -> 8`, and reads rose `24 -> 27`. Slack32 skipped the useful owner and stayed exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. | Close W1-low `153..169` as a static retained read group on this baseline. It has the same failure mode as the wider early-mid ranges: visible blocking can move, but only by tightening the target/refill budget. |
| 2026-05-19 | `walkstuf1-low-rg142-154-current` | Log-only; source restored and PS1 rebuilt. | Test the next narrow W1-low early-mid row after closing `147..171` and `153..169`. The range `142..154` had one modeled saved read and less overread than the wider rows, so it was a candidate for a same-speed work-volume promotion if the shorter range avoided phase debt. | Temporarily inserted `{142,154}` into `kWalkstuf1LowReadGroups12[]` on top of the accepted W1-low `244..350`/`179..185`, `{113,129}`, and `{355,371}` baseline. Tested raw, min-slack `8`, and min-slack `32`; artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg142-154-current/20260519-202453-289650/summary.json`, slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg142-154-slack8-current/20260519-202652-301357/summary.json`, and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg142-154-slack32-current/20260519-202853-313141/summary.json`. | Failed/no promotion. Raw and slack8 were identical work-volume-positive but timing-negative: loop reads improved `24 -> 23` and due improved `4 -> 3`, but scene/loop regressed `1809/1470 -> 1811/1472`, overrun regressed `24 -> 25`, and refill regressed `5 -> 7`; blocking stayed `33`. Slack32 skipped the useful owner and stayed exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. | Close W1-low `142..154` as a static retained read group on this baseline. The row proves the read/due work can move, but the static append point still pushes active timing and refill the wrong direction. |
| 2026-05-19 | `walkstuf1-low-rg136-148-current` | Log-only; source restored and PS1 rebuilt. | Test the remaining smaller early-mid W1-low row after closing `142..154`. The range `136..148` had the same one-read modeled upside but an earlier first gap, so the goal was to confirm whether it could avoid the timing debt seen in later early-mid rows. | Temporarily inserted `{136,148}` into `kWalkstuf1LowReadGroups12[]` on top of the accepted W1-low `244..350`/`179..185`, `{113,129}`, and `{355,371}` baseline. Tested raw and min-slack `32`; artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg136-148-current/20260519-203250-335864/summary.json` and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg136-148-slack32-current/20260519-203454-347990/summary.json`. | Failed/no promotion. Raw kept scene/loop flat at `1809/1470` and refill flat at `5`, but target regressed `1446 -> 1445`, overrun regressed `24 -> 25`, blocking regressed `33 -> 36`, reads stayed `24`, and due regressed `4 -> 5`. Slack32 skipped the harmful owner and stayed exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. | Close W1-low `136..148` as a static retained read group on this baseline. Unlike `142..154`, this variant has no useful read/due signal and worsens visible blocking, so it does not need further min-slack probes. |
| 2026-05-19 | `walkstuf1-low-rg147-163-current` | Log-only; source restored and PS1 rebuilt. | Close the final open static W1-low early-mid retained row from the refreshed read-candidate matrix after `147..171`, `153..169`, `142..154`, and `136..148` all failed. The exact `147..163` subset was the remaining binary check before leaving scalar table rows. | Temporarily inserted `{147,163}` into `kWalkstuf1LowReadGroups12[]` on top of the accepted W1-low `244..350`/`179..185`, `{113,129}`, and `{355,371}` baseline. Tested raw and min-slack `32`; artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg147-163-current/20260519-203839-369556/summary.json` and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg147-163-slack32-current/20260519-204028-380313/summary.json`. | Failed/no promotion. Raw kept loop/blocking flat but regressed target `1446 -> 1444`, overrun `24 -> 26`, refill `5 -> 9`, and loop reads `24 -> 27`. Slack32 skipped the harmful owner and stayed exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. | Close W1-low `147..163` and the remaining static W1-low read-table lane. The early-mid read/due signal can only be promoted with generated refill-budget ownership or pack/render data-shape changes; scalar rows either burn target/refill slack or become inert under high slack guards. |
| 2026-05-19 | `visitor3-high-active103-cache-current` | Log-only; source restored and PS1 rebuilt. | Take a larger VISITOR3-high setup-ownership swing on the worst yellow row after the static read-table lane closed. The prior `103..127` active-window setup swap missed the allocator by only a few KiB, so this tried to keep the accepted terminal and middle residency while adding active-window coverage using the allocator-era CACHE region. | First replaced V3-high setup coverage with `103..127`, merged `203..262`, and `277..293`, and moved high-tide VISITOR3 setup allocation from TRANSIENT to CACHE. Artifact: `scratch/ps1-perf-iterate/visitor3-high-active103-cache-combined-current/20260519-204854-427979/summary.json`. That exhausted CACHE during six split clean-rect allocations after `memCacheUsed=605820`, failing before `JCPERF2`. Then kept the active-window CACHE setup shape but disabled the high-tide clean-relief stream window to free CACHE. Artifact: `scratch/ps1-perf-iterate/visitor3-high-active103-cache-no-window-current/20260519-205206-446288/summary.json`. | Failed/no promotion. The combined CACHE form was structurally over budget: `grCleanRectPixels` needed another `48640` bytes with only fragmented/insufficient CACHE left. The no-window allocator trade completed, but it regressed VISITOR3 high hard by removing the stage1-window behavior: scene/loop/target `1389/1075/1044 -> 1400/1090/1040`, overrun `31 -> 50`, blocking `45 -> 315`, loop reads `4 -> 114`, and due misses `3 -> 88`; refill improved only `3 -> 0`. | Restore the accepted V3-high `277..293`, `203..229`, and `228..262` setup segments, TRANSIENT setup allocation, and high-tide clean-relief stream window. This big swing proves the active `103..127` window cannot be bought by simple CACHE setup residency unless clean-rect/CACHE pressure is reduced first; future V3-high work should reduce clean/upload/restore memory or generate deadline ownership without dropping the stream window. |
| 2026-05-19 | `visitor3-high-clean-chunk-sweep-current` | Log-only; source restored and PS1 rebuilt. | Follow the failed active-window setup swing by testing whether V3-high clean-rect split geometry could reduce clean/CACHE pressure or improve cadence without changing scene pixels. The accepted high-tide cap is `64 KiB`; the sweep tested larger and smaller caps around that baseline. | Temporarily changed the high-tide VISITOR3 `gFgCleanRectMaxBytes` cap and ran focused V3-high gates against the accepted five-yellow baseline. Artifacts: `96 KiB` `scratch/ps1-perf-iterate/visitor3-high-clean96-current/20260519-205603-469028/summary.json`, `80 KiB` `scratch/ps1-perf-iterate/visitor3-high-clean80-current/20260519-205756-480079/summary.json`, and `56 KiB` `scratch/ps1-perf-iterate/visitor3-high-clean56-current/20260519-205939-490437/summary.json`. | Failed/no promotion. `96 KiB` improved scene `1389 -> 1388` but regressed target/blocking/overrun `1044/45/31 -> 1043/46/32`. `80 KiB` was worse: target `1044 -> 1042`, blocking `45 -> 46`, overrun `31 -> 33`. `56 KiB` was safe but inert, staying exact-flat at `1389/1075/1044`, overrun `31`, blocking/refill `45/3`, reads/due `4/3`. | Keep the accepted `64 KiB` V3-high clean-rect cap. Split geometry alone does not free enough useful pressure for the `103..127` active window and can tighten target cadence; future V3-high clean work must reduce clean area/bytes or move ownership, not just retune chunk size. |
| 2026-05-19 | `visitor3-high-setupprime-window-current` | Log-only; source restored and PS1 rebuilt. | Test the next VISITOR3-high big swing after active setup residency and clean chunking closed: re-enable a setup-time stream-window prime under clean-memory relief. The goal was to buy early active-sector coverage using the existing stream-window path, first by growing capacity and then by trying the no-extra-memory accepted `68 KiB` window size. | Temporarily allowed high-tide VISITOR3 to keep a setup-prime window despite clean relief. Artifacts: `128 KiB` `scratch/ps1-perf-iterate/visitor3-high-setupprime128-current/20260519-210710-533251/summary.json`, `96 KiB` `scratch/ps1-perf-iterate/visitor3-high-setupprime96-current/20260519-210950-548824/summary.json`, `80 KiB` `scratch/ps1-perf-iterate/visitor3-high-setupprime80-current/20260519-211220-563241/summary.json`, and no-extra-memory `68 KiB` `scratch/ps1-perf-iterate/visitor3-high-setupprime68-current/20260519-211409-574007/summary.json`. | Failed/no promotion. `128 KiB` and `96 KiB` failed before `JCPERF2` at the clean-rect allocator cliff; `128 KiB` exhausted CACHE with `req=57600 have=28416`, while `96 KiB` still could not satisfy the final contiguous `57600` byte clean chunk despite nominal `have=61184`. `80 KiB` completed but regressed scene/loop/target `1389/1075/1044 -> 1442/1108/1038`, overrun `31 -> 70`, blocking/refill `45/3 -> 65/6`, reads `4 -> 7`, and due `3 -> 4`. The pure `68 KiB` setup-prime completed but was still timing-negative: `1426/1095/1040`, overrun `55`, blocking `52`, refill/read/due `3/4/3`. | Keep V3-high clean-relief setup-prime disabled. Even without extra memory, setup-window priming changes the active CD cadence in the wrong direction; growing it reopens the CACHE fragmentation cliff. The remaining V3-high path needs generated deadline ownership, clean-area byte reduction, or pack/render work reduction rather than reusing the setup-prime stream window. |
| 2026-05-19 | `building2-high-setup-window-sweep-current` | Log-only; source restored and PS1 rebuilt. | Test a BUILDING2-high setup-segment big swing after static B2 rows and same-offset trim-tail work failed: move or add retained setup coverage for the exposed `95..119`, `122..146`, and `140..164` hot clusters while preserving the current allocator-era baseline as the comparison target. | First retargeted the existing second setup segment away from accepted `202..242` to `122..162`, `95..135`, and `140..180`; artifacts: `scratch/ps1-perf-iterate/building2-high-seg2-122-162-current/20260519-212007-608213/summary.json`, `scratch/ps1-perf-iterate/building2-high-seg2-95-135-current/20260519-212154-618785/summary.json`, and `scratch/ps1-perf-iterate/building2-high-seg2-140-180-current/20260519-212341-629361/summary.json`. Then preserved accepted `202..242` and added a third segment over `122..146`, `122..145`, `122..144`, `122..138`, and `95..111`, with a clean-cap pairing check at `80 KiB`/`88 KiB`; artifacts: `scratch/ps1-perf-iterate/building2-high-add-seg3-122-146-current/20260519-212708-649164/summary.json`, `scratch/ps1-perf-iterate/building2-high-add-seg3-122-clean80-current/20260519-213001-665802/summary.json`, `scratch/ps1-perf-iterate/building2-high-add-seg3-122-145-current/20260519-213154-677016/summary.json`, `scratch/ps1-perf-iterate/building2-high-add-seg3-122-clean88-current/20260519-213446-693510/summary.json`, `scratch/ps1-perf-iterate/building2-high-add-seg3-122-144-current/20260519-213637-704506/summary.json`, `scratch/ps1-perf-iterate/building2-high-add-seg3-122-138-current/20260519-213909-719169/summary.json`, and `scratch/ps1-perf-iterate/building2-high-add-seg3-95-111-current/20260519-214100-730123/summary.json`. | Failed/no promotion. Trading away `202..242` proved that late coverage is essential: `122..162` regressed `1347/1313/34` to `1355/1311/44`, blocking `39 -> 48`, refill `16 -> 23`; `95..135` regressed overrun `34 -> 39` with only refill `16 -> 15`; `140..180` regressed overrun/blocking/refill to `42/45/21`. Additive `122..146` and `122..145/144` hit the CACHE clean-rect cliff before `JCPERF2` (`fgSetupSegmentBuffer` `196608/194560/192512`, then final `grCleanRectPixels req=87724` failed despite nominal `have=87000/89048/91096`). Clean-cap pairings ran but regressed timing: `80 KiB` gave `1348/1312/36`, blocking `41`, and `88 KiB` gave `1350/1312/38`, blocking `43`. Smaller additive windows fit but were phase-negative: `122..138` regressed to `1355/1311/44`, blocking/refill `48/22`, and `95..111` regressed to `1353/1310/43`, blocking/refill `48/21`. | Keep BUILDING2-high setup coverage at accepted `3..35` plus `202..242` and keep the default `96 KiB` clean cap. B2-high cannot buy the exposed early clusters through simple setup-retarget/additive residency: trading the late cluster breaks cadence, while additive coverage either exhausts CACHE or moves read/refill phase the wrong way. Future B2-high work should move to generated deadline/refill ownership, selective pack/render/upload reduction, or allocator/clean-area byte reduction before retrying larger residency. |
| 2026-05-19 | `walkstuf1-high-frame56-rg178-194-current` | `ps1: trim walkstuf1 high frame56 read pair` | Promote a combined W1-high data-shape plus read-owner row after the standalone frame56 trim showed the right CD/blocking signal but failed strict target accounting. The binary question was whether shrinking the frame56/source67 payload before adding the planner's `178..194` range would save the hot read pair without repeating the earlier high-tide static-row phase debt. | Trimmed `generated/ps1/foreground/WALKSTUF1.FG2` entry `56` / source frame `67` in place with preserved offsets and input-size padding (`3425 -> 324` bytes, `2 -> 1` sectors, active payload `851432 -> 848331`) and added `{178,194,0}` to `kWalkstuf1HighReadGroups12[]`. The standalone trim probe failed strict promotion at `scratch/ps1-perf-iterate/walkstuf1-high-frame56-inplace-current-focused/20260519-214753-769358/summary.json`: scene/loop stayed flat but target tightened `1440 -> 1439` and overrun regressed `31 -> 32`. The combined focused gate passed at `scratch/ps1-perf-iterate/walkstuf1-high-frame56-rg178-194-current-strict/20260519-220811-884805/summary.json` after a same-commit baseline refresh at `scratch/ps1-perf-iterate/walkstuf1-high-frame56-rg178-194-current-baseline/20260519-220027-841107/summary.json`. The five-row no-regression gate passed at `scratch/ps1-perf-iterate/walkstuf1-high-frame56-rg178-194-current-canary-noregress/20260519-220955-894839/summary.json`; an earlier comparison to a stale dirty baseline at `scratch/ps1-perf-iterate/walkstuf1-high-frame56-rg178-194-canaries/20260519-215155-792855/summary.json` was rejected as a baseline artifact, not as candidate evidence. | Promoted as same-speed CD-pressure reduction. WALKSTUF1 high stays scene/loop/target `1807/1471/1440`, overrun `31`, hidden refill `13`, and due `10`, while blocking improves `57 -> 56` and loop reads/read time drop `45/209 -> 43/207`. Pack LBA stays `24891`, the `WALKSTUF1.FG2` footprint stays `1535263` bytes, and the PS-EXE bucket stays `233472`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 low stayed flat in the same-commit no-regression canary. | Keep frame56 trim plus `{178,194}` as the new W1-high CD-pressure baseline. Public rollup remains `+0.2425%` over target / `99.7608%` target speed because active loop and target are unchanged; raw signed rollup remains about `-0.4744%` / `100.4911%`; bands remain `121` green, `5` yellow, `0` orange, and `0` red. Methodology speed total remains about `17.22` public over-target points removed and `+12.72` target-speed points gained since the compact full-matrix baseline, with two fewer W1-high loop reads banked for the next generated-owner/data-shape swing. |
| 2026-05-19 | `visitor3-high-window80-tight56` | `ps1: widen visitor3 high relief window` | Promote the VISITOR3-high clean-relief stream-window retune after the active-window/CACHE setup swing and setup-prime window lane both proved phase-negative. The binary question was whether the accepted `68 KiB` relief window had a larger in-loop knee when kept separate from setup-prime behavior and paired with the accepted `56 KiB` tight-refill cap. | Captured a same-commit focused baseline at `scratch/ps1-perf-iterate/visitor3-high-window-baseline/20260519-222848-1000252/summary.json`, then tested `96 KiB` at `scratch/ps1-perf-iterate/visitor3-high-window96/20260519-223030-1010109/summary.json` and `96 KiB` with the tight cap restored at `scratch/ps1-perf-iterate/visitor3-high-window96-tight56/20260519-223211-1019798/summary.json`; both were rejected because they moved `1075/1044` to `1077/1042`, overrun `31 -> 35`, despite reads `4 -> 3`. The promoted `80 KiB` focused gate is `scratch/ps1-perf-iterate/visitor3-high-window80-tight56/20260519-223400-1030327/summary.json`; the five-yellow no-regression canary is `scratch/ps1-perf-iterate/visitor3-high-window80-tight56-canary/20260519-223543-1040318/summary.json`. | Promoted. VISITOR3 high improves scene/loop/target `1389/1075/1044 -> 1388/1071/1045`, overrun `31 -> 26`, blocking `45 -> 35`, hidden refill `3 -> 1`, and due `3 -> 2`. The tradeoff is more/smaller loop reads and read time, `4/53 -> 7/69`, which still buys better visible cadence and target speed. BUILDING2 high, VISITOR3 low, and WALKSTUF1 high/low stayed flat in the canary. | Keep the `80 KiB` high-tide clean-relief stream window with the existing `56 KiB` tight-refill cap. Public rollup improves to `+0.2387%` over target / `99.7644%` target speed; raw signed rollup is about `-0.4782%` / `100.4948%`; bands remain `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total is now about `17.16` public over-target points removed and `+12.66` target-speed points gained since the compact full-matrix baseline. |
| 2026-05-19 | `building2-high-prepare-before-window-current` | Log-only; source restored and PS1 rebuilt. | Test whether BUILDING2-high could use the W1-low prepare-before-window ordering to move work before the active stream window without adding new pack data. | Temporarily allowed high-tide BUILDING2 to enter the prepare-before-window scheduler branch and ran the focused B2-high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-prepare-before-window-current/20260519-230301-1192478/summary.json`. | Failed/no promotion. B2 high regressed scene/loop/target `1621/1347/1313 -> 1625/1351/1311`, overrun `34 -> 40`, blocking `39 -> 45`, refill `16 -> 19`, while loop reads stayed `45`. `foregroundPilotPlay` grew by `60` bytes. | Keep B2-high on the current stream-window ordering. The prepare-before-window shape moves cadence the wrong way for this scene; future B2 work needs generated ownership or pack/render byte reduction, not reuse of the W1-low ordering branch. |
| 2026-05-19 | `building2-high-directstage-current` | Log-only; source restored and PS1 rebuilt. | Test whether exact-payload direct-stage ownership could buy the remaining B2-high clusters without overreading the refill window. | Temporarily added high-tide BUILDING2 direct-stage branches for `249..261` with normal slack, then for `185..197` with a `12 KiB` cap. Artifacts: `scratch/ps1-perf-iterate/building2-high-directstage249-261-current/20260519-230513-1205103/summary.json` and `scratch/ps1-perf-iterate/building2-high-directstage185-197-current/20260519-230731-1218229/summary.json`. | Failed/no promotion. Both variants were exact-flat/inert at `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads `45`, and due `5`, while the source branches grew `foregroundPilotPlay` by `104` and then `196` bytes. | Close B2-high hand-coded direct-stage ownership for `249..261` and `185..197`. A useful direct-stage path needs generated metadata/code-neutral ownership before it is worth another PS1 gate. |
| 2026-05-19 | `building2-high-xband-upload-staged-current` | Log-only; source restored and PS1 rebuilt. | Test a runtime uploader big swing: stage narrow dirty x-bands for small row bands so B2-high uploads fewer full 320-pixel rows. | Temporarily modified `grDrawBackground()` to copy bands with `<=8` rows into static per-band staging buffers before `LoadImage`. Artifact: `scratch/ps1-perf-iterate/building2-high-xband-upload-staged-current/20260519-231233-1246571/summary.json`. | Failed structurally/no promotion. The source grew the PS-EXE bucket `233472 -> 235520`, grew `grDrawBackground` by `536` bytes, and did not produce a valid `JCPERF2` summary before timeout/termination despite the emulator continuing at about 60 FPS. | Close runtime scratch-copy x-band staging in the hot renderer. Upload-byte work should be pack-emitted/precomposed or code-size-neutral, not staged through a larger runtime copy path. |
| 2026-05-19 | `building2-high-entries92-94-95-trim` | `ps1: trim building2 high entries 92 94 95` | Promote the safe subset from the B2-high preserve-offset trim lane after scheduler and runtime-owner probes failed. The goal was to reduce active payload/max-entry pressure without moving pack offsets or changing the already fragile B2-high CD cadence. | Rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-trim-draw-tails.py --frames 92,94,95 --preserve-offsets`. Transform summary: `scratch/building2-high-entries92-94-95-trim-refresh/summary.json`. Focused proof: `scratch/ps1-perf-iterate/building2-high-entries92-94-95-trim-refresh/20260519-231722-1273404/summary.json`; five-yellow canary: `scratch/ps1-perf-iterate/building2-high-entries92-94-95-trim-refresh-canary/20260519-231905-1283101/summary.json`. | Promoted as work-volume, not a VBlank speed win. Entries `92`, `94`, and `95` shrink `8834 -> 6370`, `8873 -> 6939`, and `10247 -> 8827` bytes, cutting active payload `669408 -> 663590` with fixed pack footprint/LBA/sectors and fixed `233472` byte PS-EXE bucket. B2-high stays exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads/read time `45/199`, and due `5`; VISITOR3 high/low and WALKSTUF1 high/low stayed flat. | Keep this as the new B2-high payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. The next B2-high swing should target generated ownership, upload/restore split, or sector-phase-changing data shape; byte-only trim is now down to max payload entry `78` at `9834` bytes. |
| 2026-05-19 | `walkstuf1-high-rg423-439-current` | `ps1: group walkstuf1 high tail reads` | Promote the only remaining scheduler-or-guarded W1-high candidate after the frame56/source67 trim plus `{178,194}` baseline. The binary question was whether the tail `423..439` row could reduce active CD work without repeating earlier high-tide phase regressions. | Added `{423,439,0}` to `kWalkstuf1HighReadGroups12[]`. Focused proof: `scratch/ps1-perf-iterate/walkstuf1-high-rg423-439-current/20260519-234116-1407198/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-high-rg423-439-current-canary/20260519-234312-1418051/summary.json`. | Promoted as same-speed CD work, not a VBlank speed win. WALKSTUF1 high stays exact-flat at scene/loop/target `1807/1471/1440`, overrun `31`, blocking/refill `56/13`, and due `10`, while loop reads/read time improve `43/207 -> 42/205`. BUILDING2 high, VISITOR3 high/low, and WALKSTUF1 low stayed exact-flat with fixed pack LBAs and fixed `233472` byte PS-EXE bucket. | Keep `{423,439}` as the new W1-high CD-pressure baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained since the compact full-matrix baseline; the refreshed read-candidate matrix now has `0` standalone, `0` scheduler-or-guarded, `7` scheduler-owned-only, and `45` closed exact ranges. |
| 2026-05-20 | `building2-high-rg185-197-current` | `ps1: group building2 high mid reads` | Promote the only B2-high current direct table row that reduced active CD work without visible cadence debt after the latest trim baseline. Companion probes tested whether nearby planner ranges could become speed wins first. | Added `{185,197,0}` to `kBuilding2HighReadGroups12[]`. Focused proof: `scratch/ps1-perf-iterate/building2-high-rg185-197-current/20260520-001246-1584536/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/building2-high-rg185-197-current-canary/20260520-001437-1595009/summary.json`. Rejected companions: `{287,311,0}` was exact-flat with no key improvement at `scratch/ps1-perf-iterate/building2-high-rg287-311-current/20260520-000821-1559112/summary.json`, and `{249,261,0}` regressed B2-high to `1631/1357/1314`, blocking `39 -> 55`, loop reads `45 -> 47`, and due `5 -> 8` at `scratch/ps1-perf-iterate/building2-high-rg249-261-current/20260520-001044-1572875/summary.json`. | Promoted as same-speed CD work, not a VBlank speed win. BUILDING2 high stays exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, and due `5`, while loop reads/read time improve `45/199 -> 43/197`. VISITOR3 high/low and WALKSTUF1 high/low stayed exact-flat with fixed pack LBAs and fixed `233472` byte PS-EXE bucket. | Keep `{185,197}` as the new B2-high CD-pressure baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained since the compact full-matrix baseline; the refreshed read-candidate matrix remains `0` standalone, `0` scheduler-or-guarded, `7` scheduler-owned-only, and `45` closed exact ranges. |
| 2026-05-20 | `visitor3-high-rg40-52-current` | Log-only; source restored and PS1 rebuilt. | Test the top unclosed VISITOR3-high scheduler-owned read-candidate family after the B2-high `{185..197}` promotion. The planner modeled a three-read upside for `40..52`, but marked it unsafe because the first visible gap is tight; this was the binary direct-table check before treating that family as generated-owner-only. | Temporarily added high-tide VISITOR3 `{40,52,0}` to the grouped read table and routed only non-low-tide VISITOR3 through it. Ran a strict focused gate against `scratch/ps1-perf-iterate/building2-high-rg185-197-current-canary/20260520-001437-1595009/summary.json`, then restored source and rebuilt. Artifact: `scratch/ps1-perf-iterate/visitor3-high-rg40-52-current/20260520-003309-1698730/summary.json`. | Failed/no promotion. The row was exact-flat/inert and failed `--require-improvement`: VISITOR3 high stayed scene/loop/target `1388/1071/1045`, overrun `26`, blocking/refill `35/1`, loop reads `7`, and due `2`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close VISITOR3-high raw `{40..52}` as a direct grouped row on this baseline. Remaining `40..64` family work needs generated deadline/refill ownership or a render/data-shape reduction; a static table route does not convert the modeled read upside into measured VBlank movement. |
| 2026-05-20 | `walkstuf1-high-rg352-368-current` | Log-only; source restored and PS1 rebuilt. | Test the top remaining unclosed W1-high scheduler-owned read-candidate row after the B2-high and VISITOR3-high direct checks. The `352..368` row modeled two saved reads in the late cluster, but was marked unsafe because the visible gap is tight. | Temporarily inserted `{352,368,0}` into `kWalkstuf1HighReadGroups12[]`, rebuilt, and ran a strict focused W1-high gate against `scratch/ps1-perf-iterate/building2-high-rg185-197-current-canary/20260520-001437-1595009/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg352-368-current/20260520-003650-1719863/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was phase-negative: W1-high regressed scene/loop/target `1807/1471/1440 -> 1813/1477/1439`, overrun `31 -> 38`, blocking/refill `56/13 -> 63/18`, and due stayed `10`, despite reducing loop reads to `40`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-high raw `{352..368}`. The late cluster still needs generated deadline/refill ownership or a data-shape/render reduction; plain grouped append saves reads but steals visible cadence and hidden refill slack. |
| 2026-05-20 | `walkstuf1-high-rg404-416-current` | `ps1: group walkstuf1 high late reads` | Promote the smaller W1-high late-cluster row after `{352..368}` proved phase-negative. The binary question was whether a one-read grouped append at `404..416` could reduce hidden CD work without stealing loop cadence. | Added `{404,416,0}` to `kWalkstuf1HighReadGroups12[]`. Focused proof stayed exact-flat but failed `--require-improvement` because only CD work moved: `scratch/ps1-perf-iterate/walkstuf1-high-rg404-416-current/20260520-004047-1742269/summary.json`. Five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-high-rg404-416-current-canary/20260520-004246-1753495/summary.json`. | Promoted as same-speed CD work, not a VBlank speed win. WALKSTUF1 high stays exact-flat at scene/loop/target `1807/1471/1440`, overrun `31`, blocking/refill `56/13`, and due `10`, while loop reads/read time improve `42/205 -> 41/200` and hidden read time drops `149 -> 144`. BUILDING2 high, VISITOR3 high/low, and WALKSTUF1 low stayed exact-flat with fixed pack LBAs and fixed `233472` byte PS-EXE bucket. | Keep `{404,416}` as the new W1-high CD-pressure baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained since the compact full-matrix baseline; the next W1-high work should move to generated-owner/render/data-shape swings unless a same-speed work-volume row is explicitly worth banking. |
| 2026-05-20 | `building2-high-trimdrawtails-current` / `building2-high-trimdrawtails-entry91-current` | Log-only; pack restored and PS1 rebuilt. | Take a B2-high pack-side render-data swing after scalar read/setup rows exhausted. The preserve-offset draw-tail transform could remove `12084` active payload bytes across 11 entries, mostly sector-collapsing entries `89..91`; after the broad probe failed, entry `91` was isolated because entries `89/90` were already known phase traps. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets`, then restored the pack and tested entry `91` alone with `--frames 91`. Transform summary: `scratch/building2-high-trimdrawtails-current/summary.json`; broad artifact: `scratch/ps1-perf-iterate/building2-high-trimdrawtails-current/20260520-010146-1859565/summary.json`; entry91 artifact: `scratch/ps1-perf-iterate/building2-high-trimdrawtails-entry91-current/20260520-010330-1869410/summary.json`. | Failed/no promotion. Broad trim improved prefetch overrun `16 -> 15` and loop reads `43 -> 41`, but regressed scene/loop `1621/1347 -> 1622/1349`, overrun `34 -> 36`, and blocking `39 -> 41`. Entry91 alone still regressed the same strict counters while only reducing reads `43 -> 42`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close B2-high draw-tail sector-collapse for entries `89..91` on this baseline. The byte/read savings are real, but removing this payload tail changes visible cadence in the wrong direction; future B2-high work needs generated deadline ownership, upload/restore elimination, or a no-decode data shape that preserves target cadence. |
| 2026-05-20 | `visitor3-high-rg40-56-current` | Log-only; source restored and PS1 rebuilt. | Close the remaining top VISITOR3-high `40..64` direct-table family member after `{40..52}` was exact-flat. The planner still modeled `40..56` as fireable with three saved reads, but marked it scheduler-owned because the first internal visible gap is only about `0.0835s`. | Temporarily added a high-tide VISITOR3 read group `{40,56,0}` and routed only non-low-tide VISITOR3 through it, then ran the strict focused gate against `scratch/ps1-perf-iterate/walkstuf1-high-rg404-416-current-canary/20260520-004246-1753495/summary.json`. Artifact: `scratch/ps1-perf-iterate/visitor3-high-rg40-56-current/20260520-011048-1910629/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat/inert and failed `--require-improvement`: VISITOR3 high stayed scene/loop/target `1388/1071/1045`, overrun `26`, blocking/refill `35/1`, loop reads `7`, and due `2`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close VISITOR3-high raw `{40..56}` as a hand-authored grouped row. The remaining early `40..64` read family needs generated deadline/refill ownership or pack/render data-shape reduction; the current append table cannot convert the modeled saved reads into measured VBlank movement. |
| 2026-05-20 | `walkstuf1-high-rg74-86-current` | Log-only; source restored and PS1 rebuilt. | Test the remaining top W1-high fireable direct-table candidate after the late-cluster CD-pressure promotions. The `74..86` row starts just after setup-prime coverage and promised one saved read, so it was the next binary check before treating early W1-high work as generated-owner/data-shape only. | Temporarily inserted `{74,86,0}` ahead of the accepted W1-high read groups and ran the strict focused gate against `scratch/ps1-perf-iterate/walkstuf1-high-rg404-416-current-canary/20260520-004246-1753495/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg74-86-current/20260520-011458-1934570/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row fired and improved visible blocking `56 -> 52` plus due misses `10 -> 7`, but stole loop cadence: scene/loop/target regressed `1807/1471/1440 -> 1813/1477/1439`, overrun `31 -> 38`, and hidden refill `13 -> 15`; pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-high raw `{74..86}`. This confirms the early setup-edge cluster needs generated deadline/refill ownership or a paired data-shape reduction; a static grouped append can reduce reads while still missing the 99% speed objective. |
| 2026-05-20 | `visitor3-high-rg40-46-covered-current` | Log-only; no source change. | Close the smaller VISITOR3-high `{40..46}` direct-table row after the `{40..56}` superset was exact-flat. The runtime tight-window cap already reads beyond sector `56` for this high-tide refill, so ending the grouped append at `46` cannot create a different read boundary than the tested superset. | No new emulator run; this closure is derived from the accepted source path and the `{40..56}` artifact `scratch/ps1-perf-iterate/visitor3-high-rg40-56-current/20260520-011048-1910629/summary.json`, which kept VISITOR3 high exact-flat at `1388/1071/1045` with reads/due `7/2`. | Failed/no promotion by coverage. The smaller row is dominated by the already-tested exact-flat superset and only risks another hot-source table branch. | Close VISITOR3-high raw `{40..46}` as covered by `{40..56}`. Remaining VISITOR3-high early read work should skip hand tables and move to generated deadline/refill ownership or pack/render data-shape reductions. |
| 2026-05-20 | `building2-high-frame71-primegap-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the B2-high placement version of the frame71 D4 idea after same-offset D4 and local-LZ failed. Frame `71` shrinks to a 13-byte previous-frame D4 payload, small enough to fit in the already paid setup-sector gap at file offset `6144`, so this asked whether moving ownership into the retained setup segment could avoid the active read-boundary debt. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-delta-previous.py --delta-frame 71 --move-frame 71:6144`, then enabled only high-tide BUILDING2 frame `71` in `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/building2-high-frame71-primegap-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-frame71-primegap-current/20260520-013252-2034671/summary.json`. The source and pack were restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The setup-owned payload still repeated the high-tide D4 cadence loss: B2-high regressed scene/loop/target `1621/1347/1313 -> 1626/1353/1310`, overrun `34 -> 43`, blocking/refill `39/16 -> 43/20`, and reads only moved `43 -> 44`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close B2-high frame71 D4 even when moved into the setup gap. The missing variable is not only payload ownership; the high-tide D4 decode/predicate phase itself is unsafe. Remaining B2-high work should skip runtime decode variants and focus on generated deadline ownership, upload/restore elimination, or no-decode data that preserves cadence. |
| 2026-05-20 | `building2-high-setup-swap90-95-current` / `building2-high-setup-swap90-95-rg149-165-current` | Log-only; pack/source restored and PS1 rebuilt. | Test a B2-high pack-only no-decode same-footprint setup residency swap after D4/read rows failed. Entries `90..95` sit in the active hot `141..164` physical span, while the accepted high-tide setup segment already pays for `202..242`; the binary question was whether moving active bytes into setup-owned sectors could reduce active CD pressure without runtime decode cost. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` by moving entries `90..95` from sectors `141..164` into the setup segment at `202..223` and evicting setup-resident entries `105..109` into the old hole at `141..162`, preserving file size `1303332`, pack LBA, and source layout. Transform summary: `scratch/building2-high-setup-swap90-95-current/summary.json`; raw focused artifact: `scratch/ps1-perf-iterate/building2-high-setup-swap90-95-current/20260520-014411-2098182/summary.json`. Then temporarily added a compensating B2-high read group `{149,165,0}` over the evicted cluster; paired artifact: `scratch/ps1-perf-iterate/building2-high-setup-swap90-95-rg149-165-current/20260520-014616-2110053/summary.json`. | Failed/no promotion. The raw swap regressed B2-high target `1313 -> 1311` and overrun `34 -> 36`, blocking/refill `39/16 -> 40/18`, while scene/loop stayed `1621/1347` and reads stayed `43`. The paired `{149..165}` row reduced reads `43 -> 40` but regressed scene/loop/target to `1625/1351/1311`, overrun `40`, blocking/refill `45/20`, and due stayed `5`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close this same-footprint B2-high setup swap shape. Relocation only helps if the evicted setup bytes do not become a new tight active cluster; future B2-high work should use generated deadline ownership, upload/restore reduction, or no-decode relocation with harmless eviction. |
| 2026-05-20 | `visitor3-high-prepare-first-current` | Log-only; source restored and PS1 rebuilt. | Test whether VISITOR3 high could use the same visual-prepare-before-window scheduler priority that is accepted for WALKSTUF1 low. The current high-tide row has few reads left but still reports `35` blocking VBlanks, so this checked whether staged visual work rather than speculative stream-window refill owned the remaining slack bottleneck. | Temporarily broadened the held-frame scheduler branch so high-tide `visitor3` prepared a staged frame before a speculative window refill when `fgRuntimeCanPrepareStagedFrame()` was true. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-prepare-first-current/20260520-015421-2155850/summary.json`. The source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The probe was exact-flat: VISITOR3 high stayed scene/loop/target `1388/1071/1045`, overrun `26`, blocking/refill `35/1`, reads/due `7/2`, and the scheduler counters matched baseline (`visual_prepare=30`, `cd_reserved=8`, `prep_blocked_cd=0`). The only source movement was hot-code growth: `foregroundPilotPlay +68` bytes in the same `233472` byte PS-EXE bucket. | Close VISITOR3-high local prepare-first ownership. The branch does not fire differently on the current baseline; remaining VISITOR3-high work needs generated append/deadline ownership or pack/render data-shape reduction rather than another prepare-first priority tweak. |
| 2026-05-20 | `walkstuf1-low-trim-entry65-current` | `ps1: trim walkstuf1 low entry65 payload` | Promote a no-decode W1-low work-volume trim after the current static read-table lane was exhausted. Entry `65` / source frame `96` was a large preserve-offset draw-tail candidate (`4630 -> 1666` bytes, `3 -> 1` sectors) that could reduce active payload without moving pack LBA, pack size, source code, or accepted read ownership. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 65`, keeping file size `1535263` and pack LBA fixed. Transform summary: `scratch/walkstuf1-low-trim-entry65-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry65-current/20260520-020311-2205496/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry65-canaries-flat-ok/20260520-020513-2216980/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and the canary keeps VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `788773 -> 785809` (`2964` bytes). | Keep entry65/source frame96 as a banked payload-only baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `building2-high-window-size-current` | Log-only; source restored and PS1 rebuilt. | Test a B2-high stream-window-size owner swing after scalar read groups and setup-residency swaps stalled. The idea was to keep the accepted B2 read groups but change only the high-tide normal stream window from `16 KiB` to nearby `24 KiB`, `20 KiB`, or `12 KiB`, asking whether overread/read-boundary phase rather than table ownership was blocking the last `34` overrun VBlanks. | Temporarily added a high-tide BUILDING2 window policy and routed B2 read groups independent of the default window-size check. Tested `24 KiB`, then `20 KiB`, then `12 KiB`; artifacts: `scratch/ps1-perf-iterate/building2-high-window24-current/20260520-021817-2290475/summary.json`, `scratch/ps1-perf-iterate/building2-high-window20-current/20260520-022006-2301046/summary.json`, and `scratch/ps1-perf-iterate/building2-high-window12-current/20260520-022150-2310843/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `24 KiB` reduced loop reads `43 -> 28` but regressed scene/loop/target `1621/1347/1313 -> 1631/1357/1312`, overrun `34 -> 45`, blocking/refill `39/16 -> 55/28`. `20 KiB` was worse at `1637/1363/1311`, overrun `52`, blocking/refill `66/26`, due `7`. `12 KiB` cut hidden refill `16 -> 13` but still regressed `1628/1355/1311`, overrun `44`, blocking `52`, reads `57`, and due `10`. | Close simple B2-high stream-window-size retuning on this baseline. Larger windows reduce reads but steal visible/refill cadence, while smaller windows improve refill only by exploding active reads and blocking. The next B2-high path should be generated deadline/refill-budget ownership, upload/restore reduction, or no-decode relocation with harmless eviction, not scalar window size. |
| 2026-05-20 | `clean-rect-row-owner-b2-high-current` | Log-only; source restored and PS1 rebuilt. | Test a graphics-side big swing shared by all five remaining yellow rows: cache the single clean-rect owner for each screen row so `grRestoreCleanBgSpanFromRects()` can skip the per-span scan over up to eight split clean rectangles, while falling back to the old full scan for overlapping rows. | Temporarily added a `gGrCleanRectRowOwner[480]` cache in `graphics_ps1.c`, routed unique-owner rows through a helper that preserved the old copy/dirty-mark behavior, rebuilt, and ran a strict focused B2-high gate against the latest five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/clean-rect-row-owner-b2-high-current/20260520-022946-2355589/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The code-size/layout cost dominated: B2-high regressed scene/loop/target `1621/1347/1313 -> 1625/1351/1311`, overrun `34 -> 40`, blocking/refill `39/16 -> 45/20`, while loop reads stayed `43` and due stayed `5`. The PS-EXE bucket grew `233472 -> 235520`, shifting `BUILDING2.FG2` LBA `6189 -> 6190`; no key metric improved. | Close runtime clean-rect row-owner caching in source form. The restore-scan idea still belongs in generated/pack-owned metadata or a code-size-neutral graphics split, but adding a hot graphics helper and row cache crosses the executable bucket and worsens CD/layout phase before any CPU savings are visible. |
| 2026-05-20 | `visitor3-high-rg44-50-current` | Log-only; source restored and PS1 rebuilt. | Test the only refreshed read-candidate matrix row not already closed by the experiment log: high-tide VISITOR3 `44..50` was a fireable 6-sector current-window group with one modeled saved read, but marked scheduler-owned because its first visible gap is only about `0.084s`. | Temporarily added `kVisitor3HighReadGroups12[] = {{44,50,0}}` and routed it only for non-low-tide VISITOR3 under the clean-relief `80 KiB` stream window. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-rg44-50-current/20260520-023650-2395484/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row was exact-flat/inert and failed `--require-improvement`: VISITOR3 high stayed scene/loop/target `1388/1071/1045`, overrun `26`, blocking/refill `35/1`, reads/due `7/2`, pack LBA `22619`, and PS-EXE bucket `233472`; foreground hot code grew/shuffled by `24` bytes while no key metric improved. | Close VISITOR3-high raw `{44..50}` as a hand-authored read group. The current hand-table candidate matrix is exhausted; further read work for the yellow rows needs generated deadline/refill ownership rather than another static source table. |
| 2026-05-20 | `walkstuf1-high-trim-entry55-current` | Log-only; pack restored and PS1 rebuilt. | Test the only remaining W1-high preserve-offset draw-tail candidate from the current yellow trim scan. Entry `55` / source frame `65` could shrink `4716 -> 2` bytes (`4 -> 1` sectors) without source changes, pack-size changes, pack LBA movement, or table retiming. | Temporarily rewrote `generated/ps1/foreground/WALKSTUF1.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 55`. Transform summary: `scratch/walkstuf1-high-trim-entry55-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-trim-entry55-current/20260520-024222-2426984/summary.json`. The original pack was restored from `scratch/walkstuf1-high-trim-entry55-current/WALKSTUF1.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The fixed-layout trim was phase-negative: W1-high regressed scene/loop/target `1807/1471/1440 -> 1811/1475/1438`, overrun `31 -> 37`, blocking/refill `56/13 -> 64/18`, while reads/due became `39/10` and no key metric improved. Pack LBA stayed `24891` and the PS-EXE bucket stayed `233472`. | Close W1-high entry55/source frame65 draw-tail trimming. This frame's tail bytes are phase-bearing despite being visually empty, so future W1-high pack work should avoid sector-collapsing early entries unless generated deadline ownership can absorb the shifted cadence. |
| 2026-05-20 | `walkstuf1-low-trim-entry39-current` | `ps1: trim walkstuf1 low entry39 payload` | Reopen W1-low entry `39` / source frame `49` after the current setup/read baseline and entry65 payload trim. The old v878 baseline made this sector-count trim phase-negative, but the current row has different setup ownership and fewer active reads, so the binary question was whether the same fixed-layout payload reduction is now safe as work-volume. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 39`, keeping file size `1535263`, pack LBA, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry39-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry39-current/20260520-024603-2448101/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry39-canaries-flat-ok/20260520-024749-2458159/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `785809 -> 782698` (`3111` bytes). | Keep entry39/source frame49 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `walkstuf1-high-rg345-361-current` / `walkstuf1-high-rg345-361-slack8-current` / `walkstuf1-high-directstage345-361-current` | Log-only; source restored and PS1 rebuilt. | Close the only open scheduler-owned row from the refreshed read-candidate matrix after the W1-low entry39 promotion. The `345..361` W1-high cluster covered entries `171..176` with two modeled saved reads, but its first visible gap was tight enough that a plain table row was marked scheduler-only. | Tested three forms against the W1-low entry39 five-yellow baseline: raw grouped append `{345,361,0}` at `scratch/ps1-perf-iterate/walkstuf1-high-rg345-361-current/20260520-030445-2552866/summary.json`, guarded append `{345,361,8}` at `scratch/ps1-perf-iterate/walkstuf1-high-rg345-361-slack8-current/20260520-030645-2564373/summary.json`, and an exact-payload direct-stage owner for entries starting in sectors `345..360` at `scratch/ps1-perf-iterate/walkstuf1-high-directstage345-361-current/20260520-030906-2577952/summary.json`. | Failed/no promotion. Raw grouping reduced loop reads `41 -> 39` and hidden refill `13 -> 12`, but regressed scene/loop/target `1807/1471/1440 -> 1809/1473/1439`, overrun `31 -> 34`, and blocking `56 -> 58`. The slack8 guard was exact-flat/inert at `1807/1471/1440`, reads `41`, and blocking/refill `56/13`. Direct-stage ownership kept loop flat and improved overrun/refill `31/13 -> 30/7`, but regressed blocking `56 -> 67`, due `10 -> 12`, and grew `foregroundPilotPlay` by `96` bytes. | Close W1-high `345..361` for raw grouped append, min-slack grouped append, and hand-coded direct-stage ownership. The row needs a code-size-neutral generated scheduler with an explicit visible/refill budget before retrying; the current hand-table and exact-payload forms either steal visible cadence or become inert. |
| 2026-05-20 | `building2-high-upload-gap4-current` / `building2-high-upload-gap0-current` / `upload-gap0-five-yellow-canary` | Log-only; source restored and PS1 rebuilt. | Test a renderer-side dirty-upload banding swing after read-table candidates were exhausted. The default `GR_UPLOAD_BAND_MERGE_GAP=8` can over-upload clean rows between dirty bands; this tried gap `4` and then gap `0` to see whether lower upload bytes could move B2-high or the five remaining yellow rows. | Temporarily changed `GR_UPLOAD_BAND_MERGE_GAP` in `grDrawBackground()` from `8` to `4`, built, and ran focused B2-high at `scratch/ps1-perf-iterate/building2-high-upload-gap4-current/20260520-031639-2620742/summary.json`. Then changed the gap to `0`, built, ran focused B2-high at `scratch/ps1-perf-iterate/building2-high-upload-gap0-current/20260520-031829-2630931/summary.json`, and ran the five-yellow canary at `scratch/ps1-perf-iterate/upload-gap0-five-yellow-canary/20260520-032111-2646392/summary.json`. Source was restored to gap `8` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Gap `4` saved B2-high upload bytes `21836800 -> 21566080` with exact-flat timing. Gap `0` saved more B2-high upload bytes `21836800 -> 21205760` and kept all five yellow rows exact-flat on scene/loop/target, blocking, refill, layout, and the `233472` byte PS-EXE bucket. However it increased VISITOR3 upload bytes (`13393280 -> 13531520` high, `13257600 -> 13367040` low), only reduced W1 upload bytes modestly, and did not move any VBlank speed metric. | Close global dirty-upload gap retuning as a speed path. It is safe but mixed and not enough to move the remaining CD/refill-bound yellow rows; revisit upload policy only as scene-local/generated metadata or paired with a measured CD/deadline owner, not as a global constant change. |
| 2026-05-20 | `walkstuf1-high-directstage-prepare-combo-current` | Log-only; source restored and PS1 rebuilt. | Test whether two individually useful W1-high scheduler signals could cancel each other's prior regressions. The exact-payload `345..361` direct-stage owner had improved overrun/refill but hurt blocking, while the broad prepare-first probe had improved blocking but hurt refill/loop cadence. | Temporarily combined a high-tide WALKSTUF1 direct-stage owner for entries starting in sectors `345..360` with a walkstuf1-only prepare-before-window scheduler branch, then ran the strict focused W1-high gate against the W1-low entry39 five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-directstage-prepare-combo-current/20260520-033352-2717817/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The combo preserved the same pack LBA and `233472` byte PS-EXE bucket and improved hidden refill `13 -> 8`, but regressed scene/loop/target `1807/1471/1440 -> 1810/1474/1441`, overrun `31 -> 33`, blocking `56 -> 63`, and due `10 -> 11`; foreground hot code grew by `76` bytes. | Close the hand-coded W1-high direct-stage plus prepare-first combo. The refill win is real, but the visible cadence cost still dominates; future W1-high ownership needs generated, code-size-neutral per-frame budget metadata instead of combining hot-source scheduler branches. |
| 2026-05-20 | `building2-high-setup-alias141-142-current` | `ps1: alias building2 high setup duplicates` | Promote a no-decode B2-high work-volume baseline after the static read-table and window-size lanes stalled. Entries `141` and `142` duplicate setup-resident entries `116` and `118`; aliasing them asks whether removing two active payload owners from the late hot span is safe before larger generated-owner work. | Rebuilt `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-delta-previous.py --move-frame`, pointing entry `141` / source frame `189` at setup-resident offset `493055` and entry `142` / source frame `190` at setup-resident offset `494942`. Transform summary: `scratch/building2-high-setup-alias141-142-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/building2-high-setup-alias141-142-current/20260520-034134-2761004/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/building2-high-setup-alias141-142-canary/20260520-034712-2793517/summary.json`. Companion rows `{249..261}` and `{255..271}` were tested and rejected/restored. | Promoted as same-speed payload ownership work, not a VBlank speed win. B2-high stays exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads/due `43/5`, pack LBA `6189`, and the `233472` byte PS-EXE bucket. VISITOR3 high/low and WALKSTUF1 high/low also stay exact-flat. Active B2-high uncovered entries/payload drop `286/519400 -> 284/518994`. | Keep the alias as the new B2-high ownership baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; next B2-high work should use generated deadline ownership or harmless no-decode relocation rather than scalar source rows. |
| 2026-05-20 | `building2-high-setup-alias33-current` | Log-only; pack restored and PS1 rebuilt. | Test the last obvious B2-high setup-resident duplicate after the `141`/`142` alias promotion. Entry `33` / source frame `40` duplicates entry `30` / source frame `37`, and entry `30` is fully covered by the accepted high-tide setup segment, so this asked whether moving entry `33` to that resident payload could remove an early active owner safely. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-delta-previous.py --move-frame 33:64064`, preserving file size, pack LBA, and source code. Transform summary: `scratch/building2-high-setup-alias33-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-setup-alias33-current/20260520-040736-2907903/summary.json`. The pack was restored from `scratch/building2-high-setup-alias33-current/BUILDING2.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The no-decode alias kept pack LBA `6189` and the `233472` byte PS-EXE bucket fixed, but B2-high regressed scene/loop/target `1621/1347/1313 -> 1622/1349/1309`, overrun `34 -> 40`, blocking/refill `39/16 -> 46/19`, while loop reads stayed `43` and due stayed `5`. | Close B2-high entry33/source frame40 setup aliasing. This duplicate is phase-bearing because it moves an early payload across the setup-segment edge; further B2-high relocation needs generated deadline/refill ownership or a harmless eviction proof, not another duplicate alias. |
| 2026-05-20 | `building2-high-alias5960-current` | Log-only; pack restored and PS1 rebuilt. | Test the early B2-high duplicate-payload alias pair after the setup-resident duplicate lane produced one same-speed ownership win and one phase-negative failure. Entries `59` and `60` duplicate entries `62` and `63`; the latter pair sits inside the accepted `60..72` grouped-read zone, so this asked whether moving early payload ownership forward into that paid zone could reduce uncovered active bytes without changing source code or pack layout. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-delta-previous.py --move-frame 59:125262 --move-frame 60:127099`, preserving file size, pack LBA, and the `233472` byte PS-EXE bucket. Transform summary: `scratch/building2-high-alias5960-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-alias5960-current/20260520-042200-2988927/summary.json`. The pack was restored from `scratch/building2-high-alias5960-current/BUILDING2.before.FG2` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The alias was phase-negative: B2-high regressed scene/loop/target `1621/1347/1313 -> 1625/1351/1311`, overrun `34 -> 40`, blocking/refill `39/16 -> 44/19`, while loop reads regressed `43 -> 45` and due stayed `5`. | Close B2-high entries `59`/`60` forward aliasing into entries `62`/`63`. Even identical payloads are cadence-bearing when moved across the early grouped-read boundary; future B2-high no-decode relocation must either move late payload backward into already-resident ownership or prove a harmless eviction path with generated deadline/refill metadata. |
| 2026-05-20 | `restore-span-hoist-w1-high-current` / `restore-span-hoist-v3-high-current` | Log-only; source restored and PS1 rebuilt. | Test a code-size-neutral restore-path swing after the row-owner clean-rect cache failed by growing hot code and shifting the executable bucket. The small variant hoisted fixed tile selection out of `grRestoreCleanBgSpanFromRects()`' clean-rect loop, targeting the remaining rows' high restore byte counts without adding a row cache or changing any pack. | Temporarily hoisted `tileLocalY`, `tileLeft`, and `tileRight` once per cleanup span in `src/graphics_ps1.c`, rebuilt in the same `233472` byte PS-EXE bucket, then ran strict focused gates for W1-high and VISITOR3-high. Artifacts: `scratch/ps1-perf-iterate/restore-span-hoist-w1-high-current/20260520-042541-3009967/summary.json` and `scratch/ps1-perf-iterate/restore-span-hoist-v3-high-current/20260520-042729-3020128/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both rows were exact-flat and failed `--require-improvement`: W1-high stayed `1807/1471/1440`, overrun `31`, blocking/refill `56/13`, reads/due `41/10`; VISITOR3-high stayed `1388/1071/1045`, overrun `26`, blocking/refill `35/1`, reads/due `7/2`. The only measurable effect was hot-symbol address movement of about `-8` bytes in the same executable bucket. | Close this source-form restore-span hoist as a speed path. The remaining restore work needs generated/pack-owned metadata or a larger renderer design that changes measured restore/upload work, not local loop hoisting that compiles to exact-flat runtime metrics. |
| 2026-05-20 | `visitor3-low-window20-current` / `visitor3-low-window12-current` | Log-only; source restored and PS1 rebuilt. | Retest the VISITOR3-low clean-relief stream-window knee on the current allocator-era baseline. The accepted low-tide relief window is `16 KiB`; this sweep checked both sides after the later retained setup segments and B2/W1 baseline changes, asking whether the current `75` blocking / `14` due pattern could trade read count for speed. | Temporarily changed low-tide VISITOR3's `fgSceneCleanReliefWindowBytes()` return from `16 KiB` to `20 KiB`, rebuilt, and ran focused V3-low at `scratch/ps1-perf-iterate/visitor3-low-window20-current/20260520-043140-3043891/summary.json`. Then changed it to `12 KiB`, rebuilt, and ran `scratch/ps1-perf-iterate/visitor3-low-window12-current/20260520-043325-3054151/summary.json`. Source was restored to `FG_PREFETCH_DEFAULT_WINDOW_BYTES` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `20 KiB` reduced reads/due `18/14 -> 16/13` but regressed scene/loop/target `1369/1065/1039 -> 1380/1076/1042`, overrun `26 -> 34`, and blocking `75 -> 76`. `12 KiB` preserved overrun `26` but regressed scene/loop `1369/1065 -> 1370/1067`, blocking `75 -> 88`, reads `18 -> 24`, and due `14 -> 19`. Both stayed in the same pack LBA and `233472` byte PS-EXE bucket. | Keep VISITOR3 low at the `16 KiB` clean-relief window. The current baseline still sits at the same window-size knee: larger windows steal visible cadence despite fewer reads, and smaller windows starve due-frame coverage. Remaining V3-low work needs generated deadline/refill ownership or pack/data-shape changes, not scalar window size. |
| 2026-05-20 | `walkstuf1-low-trim-entry55-current` | `ps1: trim walkstuf1 low entry55 payload` | Continue the current W1-low preserve-offset payload lane after entry65 and entry39 proved safe. Entry `55` / source frame `65` had a no-decode draw-tail trim from `4716 -> 2` bytes (`3 -> 1` sectors), so the binary question was whether removing those payload bytes stayed harmless under the current setup/read baseline. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 55`, keeping file size `1535263`, pack LBA, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry55-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry55-current/20260520-043650-3073641/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry55-canary/20260520-043841-3084073/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `782698 -> 777984` (`4714` bytes); the three current W1-low payload trims now total `10789` bytes from the `788773` pre-entry65 baseline. | Keep entry55/source frame65 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `linkorder-cd-before-fg-canary` | Log-only; `CMakeLists.txt` restored and PS1 rebuilt. | Test the first code-phase/link-order big swing after the direct read-candidate matrix closed. The binary variant moved `src/cdrom_ps1.c` before `src/foreground_pilot.c` in the executable source list so CD helpers and foreground hot code shifted together without changing scene logic, generated packs, or file LBAs. | Temporarily reordered the PS1 source list, rebuilt, and ran the current five-yellow canary against `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry55-canary/20260520-043841-3084073/summary.json`. Artifact: `scratch/ps1-perf-iterate/linkorder-cd-before-fg-canary/20260520-045809-3193268/summary.json`. The source order was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. All five rows were exact-flat: VISITOR3 high `1071/1045`, VISITOR3 low `1065/1039`, B2-high `1347/1313`, W1-high `1471/1440`, and W1-low `1470/1446`; blocking/refill/read/due metrics and pack LBAs also stayed flat. The only measured effect was foreground hot-symbol address movement of about `+10984` bytes while the PS-EXE bucket stayed `233472`. | Close the `cdrom_ps1.c` before `foreground_pilot.c` link-order bucket as a speed path. Pure link-order movement at this granularity is inert for the current five-yellow baseline; future code-phase work should use narrower function alignment/object padding controls or move to generated scheduler/pack-side work. |
| 2026-05-20 | `walkstuf1-low-trim-entry56-current` | `ps1: trim walkstuf1 low entry56 payload` | Continue the W1-low preserve-offset payload lane after entry65, entry39, and entry55 proved safe. Entry `56` / source frame `67` had a no-decode draw-tail trim from `3425 -> 324` bytes (`3 -> 1` sectors), so the binary question was whether removing those payload bytes stayed harmless under the current setup/read baseline. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 56`, keeping file size `1535263`, pack LBA, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry56-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry56-current/20260520-050742-3247385/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry56-canary/20260520-050926-3257358/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `777984 -> 774883` (`3101` bytes); the four current W1-low payload trims now total `13890` bytes from the `788773` pre-entry65 baseline. | Keep entry56/source frame67 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `walkstuf1-low-trim-entry60-current` / `walkstuf1-low-trim-entry60-rg199-211-current` / `walkstuf1-low-trim-entry60-rg204-220-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the largest remaining W1-low preserve-offset payload candidate after entry65/39/55/56 were banked. Entry `60` / source frame `81` trims `4432 -> 476` bytes on the current pack (`4 -> 2` sectors), so the binary question was whether the larger byte drop could convert to visible speed without hidden refill debt; two paired scalar owner rows tested the new shifted read starts. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 60`, reducing active payload `774883 -> 770927`. Focused artifacts: bare trim `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry60-current/20260520-052607-3350307/summary.json`, paired `{199..211}` owner `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry60-rg199-211-current/20260520-052835-3364489/summary.json`, and paired `{204..220}` owner `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry60-rg204-220-current/20260520-053035-3376024/summary.json`; transform summary: `scratch/walkstuf1-low-trim-entry60-current/summary.json`. The pack was restored from `scratch/walkstuf1-low-trim-entry60-current/WALK1LOW.before.FG2`, the scalar owner was removed, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The bare trim had a real visible-speed signal, improving scene/loop/target `1809/1470/1446 -> 1807/1468/1445`, overrun `24 -> 23`, blocking `33 -> 28`, and due `4 -> 3`, but it regressed hidden refill `5 -> 10` and loop reads `24 -> 27`, so the strict baseline gate failed. The `{199..211}` owner worsened target/overrun/refill to `1470/1444`, overrun `26`, refill `16`; `{204..220}` behaved like the bare trim and still failed refill `5 -> 10`. | Close W1-low entry60/source frame81 as a standalone preserve-offset trim and with scalar `{199..211}` / `{204..220}` owner pairings on this baseline. The candidate is a useful signal for generated refill-budget ownership, but not a safe hand-authored payload/read-table promotion. |
| 2026-05-20 | `walkstuf1-low-trim-entry59-current` | `ps1: trim walkstuf1 low entry59 payload` | Continue the W1-low preserve-offset payload lane after entry65, entry39, entry55, and entry56 proved safe and entry60 proved refill-negative. Entry `59` / source frame `78` had a no-decode draw-tail trim from `4200 -> 456` bytes (`3 -> 2` sectors), so the binary question was whether this smaller sector-collapse candidate stayed harmless under the current setup/read baseline. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 59`, keeping file size `1535263`, pack LBA, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry59-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry59-current/20260520-053427-3398149/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry59-canary/20260520-053628-3409612/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `774883 -> 771139` (`3744` bytes); the five current W1-low payload trims now total `17634` bytes from the `788773` pre-entry65 baseline. | Keep entry59/source frame78 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `walkstuf1-low-trim-entry63-current` | `ps1: trim walkstuf1 low entry63 payload` | Continue the W1-low preserve-offset payload lane after entry59 banked cleanly. Entry `63` / source frame `91` had a no-decode draw-tail trim from `4643 -> 1244` bytes (`3 -> 2` sectors), so the binary question was whether this next fixed-layout sector-collapse candidate stayed exact-flat under the current setup/read baseline. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 63`, keeping file size `1535263`, pack LBA, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry63-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry63-current/20260520-055126-3493665/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry63-canary/20260520-055318-3504596/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `771139 -> 767740` (`3399` bytes); the six current W1-low payload trims now total `21033` bytes from the `788773` pre-entry65 baseline. | Keep entry63/source frame91 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `walkstuf1-low-trim-entry66-current` | `ps1: trim walkstuf1 low entry66 payload` | Continue the W1-low preserve-offset payload lane after entry63 banked cleanly. Entry `66` / source frame `100` had a no-decode draw-tail trim from `4661 -> 2082` bytes (`3 -> 2` sectors), so the binary question was whether this next fixed-layout sector-collapse candidate stayed exact-flat under the current setup/read baseline. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 66`, keeping file size `1535263`, pack LBA, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry66-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry66-current/20260520-060517-3572808/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry66-canary/20260520-060704-3583196/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `767740 -> 765161` (`2579` bytes); the seven current W1-low payload trims now total `23612` bytes from the `788773` pre-entry65 baseline. | Keep entry66/source frame100 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `walkstuf1-low-entry60-refillguard-current` / `walkstuf1-low-entry60-rg229-241-current` / `walkstuf1-low-entry60-guard-rg229-241-current` / `walkstuf1-low-entry60-guard-rg160-176-current` | Log-only; pack/source restored and PS1 rebuilt. | Follow up the visible-positive but refill-negative entry60 trim after entry66 became the accepted W1-low payload baseline. The hypothesis was that the `4432 -> 476` byte trim could be recovered by a narrow W1-low refill guard or by the newly exposed `229..241` / `160..176` owner rows instead of leaving hidden refill at `10`. | Reapplied the entry60 preserve-offset trim on top of the entry66 pack (`765161 -> 761205` active payload), then tested four focused combinations: low-only 4-VBlank scheduler guard at `scratch/ps1-perf-iterate/walkstuf1-low-entry60-refillguard-current/20260520-062425-3681697/summary.json`; entry60 plus `{229..241}` at `scratch/ps1-perf-iterate/walkstuf1-low-entry60-rg229-241-current/20260520-062636-3694432/summary.json`; guard plus `{229..241}` at `scratch/ps1-perf-iterate/walkstuf1-low-entry60-guard-rg229-241-current/20260520-062840-3706570/summary.json`; and guard plus `{160..176}` at `scratch/ps1-perf-iterate/walkstuf1-low-entry60-guard-rg160-176-current/20260520-063036-3718023/summary.json`. | Failed/no promotion. The guard alone reduced hidden refill `5 -> 2` but regressed scene/loop/blocking to `1811/1472`, blocking `40`, due `6`. Entry60 plus `{229..241}` preserved the bare visible win (`1807/1468/1445`, blocking `28`) but still failed hidden refill `5 -> 10`. Guard plus `{229..241}` held scene/loop/target flat at `1809/1470/1446` and refill `2`, but regressed blocking `33 -> 39`; guard plus `{160..176}` regressed badly to `1817/1478/1447`, overrun `31`, blocking `46`. | Close scalar recovery for W1-low entry60. The candidate remains an important generated-ownership target because the byte trim can reduce visible loop/blocking, but static read rows and coarse slack guarding either keep the hidden-refill failure or trade it for visible blocking. Future entry60 work needs per-frame generated deadline/refill-budget ownership before another PS1 gate. |
| 2026-05-20 | `xband-upload-b2-high-current` / `xband-upload8-b2-high-current` / `xband-upload16-b2-high-current` / `xband-upload16safe-b2-high-current` | Log-only; renderer restored and PS1 rebuilt. | Retry runtime x-band framebuffer upload as a bigger cross-scene render swing after the previous per-scene scratch-copy lanes failed. Host modeling showed large theoretical upload reductions on the remaining yellow rows, with BUILDING2 high estimated around `67%` fewer upload bytes if dirty row bands could be copied to contiguous narrow rectangles. | Temporarily changed `grDrawBackground()` to compute per-band X ranges, pack the selected bands into `gScrStreamRows`, and call `LoadImage()` with narrow rectangles. Tested `4`, `8`, and `16` pixel alignment, then a safety gate that fell back unless packed transfer pixels were 16-halfword aligned. Artifacts: `scratch/ps1-perf-iterate/xband-upload-b2-high-current/20260520-064158-3781813/summary.json`, `scratch/ps1-perf-iterate/xband-upload8-b2-high-current/20260520-064450-3798323/summary.json`, `scratch/ps1-perf-iterate/xband-upload16-b2-high-current/20260520-064741-3814662/summary.json`, and `scratch/ps1-perf-iterate/xband-upload16safe-b2-high-current/20260520-065031-3830939/summary.json`. | Failed/no promotion. Every variant failed before `JCPERF2`. The `16` and `16safe` forms reached BUILDING2 frame `132/334` and then stalled; PSn00b transfer rounding warnings remained, and the source grew the PS-EXE bucket `233472 -> 235520` with `grDrawBackground` about `+1088` bytes. | Close generic runtime scratch-packed x-band upload. The byte signal is real, but copying narrow tile rows during active playback adds too much hot code/cadence risk and still does not produce a valid scene gate. Future upload work must be pack-emitted/upload-ready, background-owned/precomposed, or implemented with a code-size-neutral GPU primitive rather than runtime scratch repacking. |
| 2026-05-20 | `building2-high-rg249-257-current` | Log-only; source restored and PS1 rebuilt. | Test the next B2-high CD-side candidate after the x-band lane closed. The refreshed read plan ranked `249..257` as a medium-gap, append-start-fireable range with one modeled saved read and less visible risk than the already-closed tight early clusters. | Temporarily added `{249,257,0}` to `kBuilding2HighReadGroups12[]`, rebuilt in the same `233472` byte PS-EXE bucket, and ran the strict focused B2-high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-rg249-257-current/20260520-065801-3873374/summary.json`; refreshed read plan: `scratch/b2high-current-after-xband-close-readplan.json`. | Failed/no promotion. B2-high regressed scene/loop/target `1621/1347/1313 -> 1622/1349/1313`, overrun `34 -> 36`, and blocking `39 -> 40`; hidden refill improved `16 -> 14` and loop reads moved `43 -> 42`, but `group_hits=0`, so the row itself did not fire and the movement is code/data phase rather than a controlled grouped-append win. | Close B2-high `249..257` as a hand-authored table row. The useful signal is not promotable without generated scheduler/refill ownership; slack-gating the same inert row would only preserve the phase shift or skip the candidate, not create a safe append. |
| 2026-05-20 | `walkstuf1-low-trim-entry85-current` | `ps1: trim walkstuf1 low entry85 payload` | Continue the W1-low preserve-offset payload lane after entry66 banked cleanly. Entry `85` / source frame `148` had a no-decode draw-tail trim from `4854 -> 4351` bytes (`4 -> 3` sectors), so the binary question was whether this smaller fixed-layout sector-collapse candidate stayed exact-flat under the current setup/read baseline. | Rebuilt `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --frames 85`, keeping file size `1535263`, pack LBA `25641`, and the `233472` byte PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry85-current/summary.json`; focused proof: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry85-current/20260520-070250-3901099/summary.json`; five-yellow no-regression canary: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry85-canary/20260520-070435-3911282/summary.json`. | Promoted as same-speed payload work, not a VBlank speed win. W1-low stays exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`, and VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 high canaries stay exact-flat with fixed pack LBAs and the same `233472` byte PS-EXE bucket. Active W1-low payload drops `765161 -> 764658` (`503` bytes); the eight current W1-low payload trims now total `24115` bytes from the `788773` pre-entry65 baseline. | Keep entry85/source frame148 as the new W1-low payload baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; this only reduces future decode/CD work surface. |
| 2026-05-20 | `prepare-before-window-yellow-canary` | Log-only; source restored. | Test a broad scheduler-owner big swing after the scalar read-row lane exhausted: allow BUILDING2, VISITOR3, and WALKSTUF1 yellow rows to prepare the staged frame before a speculative stream-window read, extending the W1-low-only ordering rule to all remaining yellow scene families. | Temporarily widened the prepare-before-window branch in `foregroundPilotPlay()` to `walkstuf1`, `building2`, and `visitor3`, then ran the five-yellow canary against the entry85 baseline. Artifact: `scratch/ps1-perf-iterate/prepare-before-window-yellow-canary/20260520-072420-4023427/summary.json`. The source branch was restored. | Failed/no promotion. VISITOR3 high regressed `1071/1045 -> 1088/1045`, overrun `26 -> 43`, and blocking `35 -> 36`; VISITOR3 low regressed `1065/1039 -> 1075/1040`, overrun `26 -> 35`, and blocking `75 -> 77`; BUILDING2 high regressed `1347/1313 -> 1353/1308`, overrun `34 -> 45`, blocking/refill `39/16 -> 47/23`. Both WALKSTUF1 cases missed `JCPERF2` before the 120s cap. | Close broad prepare-before-window ownership. The ordering is scene/phase-specific: it remains useful only for the accepted W1-low path and is too blunt for B2/V3/W1-high. Future scheduler work needs per-cluster ownership rather than family-wide prepare-first toggles. |
| 2026-05-20 | `building2-high-rg249-261-slack8-current` | Log-only; source restored. | Convert the previously phase-negative B2-high `249..261` row into a generated-owner-style guarded append by requiring `8` VBlanks of slack before it can fire. This tested whether a deadline guard could keep the modeled one-read saving while avoiding the unguarded cadence debt. | Temporarily inserted `{249,261,8}` into `kBuilding2HighReadGroups12[]` and ran the focused B2-high gate against the entry85 canary baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-rg249-261-slack8-current/20260520-073650-4094750/summary.json`. The source row was removed after the failed gate. | Failed/no promotion. The guarded row still regressed B2-high: scene/loop/target `1621/1347/1313 -> 1641/1352/1311`, overrun `34 -> 41`, blocking `39 -> 42`, and hidden refill `16 -> 17`, while loop reads stayed at `43`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close `249..261` with an `8`-VBlank deadline guard. The cluster is phase-negative even when slack-gated at this level; do not retry it as a static table row. If revisited, it needs true per-frame generated ownership or a pack/data-shape change that removes the read without extending this active-window boundary. |
| 2026-05-20 | `building2-high-hot-dup-alias-current` | Log-only; pack restored. | Test whether exact duplicate payloads in B2-high's hot active cluster could be no-decode aliased to earlier duplicate entries without changing source code, pack size, pack LBA, or the executable bucket. This was a follow-up to the accepted setup-resident `141/142` alias, but aimed at active duplicate ownership instead of setup-owned duplicates. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-delta-previous.py --move-frame 137:496935 --move-frame 156:504514 --move-frame 162:509982 --move-frame 164:510125`, pointing entries `137`, `156`, `162`, and `164` back to duplicate payloads in the active cluster. Transform summary: `scratch/building2-high-hot-dup-alias-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-hot-dup-alias-current/20260520-074050-4117347/summary.json`. The pack was restored from `scratch/building2-high-hot-dup-alias-current/BUILDING2.before.FG2`. | Failed/no promotion. The exact aliasing was strongly phase-negative: B2-high regressed scene/loop/target `1621/1347/1313 -> 1659/1370/1311`, overrun `34 -> 59`, blocking/refill `39/16 -> 76/17`, loop reads `43 -> 50`, and due misses `5 -> 11`, with backward seeks rising and no source/layout change to explain it away. | Close backward active-cluster duplicate aliasing for B2-high. Identical payload ownership is not sufficient when it introduces reverse/mixed seek churn inside the `248..261` hot cluster; future no-decode relocation must be setup-resident or forward-order with a harmless-eviction proof, otherwise use generated deadline/refill ownership. |
| 2026-05-20 | `building2-high-setup3-43-current` | Log-only; source restored and PS1 rebuilt. | Test the unclosed B2-high setup-edge variant after active duplicate aliasing failed: extend the first retained setup segment from sectors `3..35` to `3..43` so entries at the `34..42` overlap boundary could be setup-resident without changing pack bytes, pack LBA, or scene pixels. | Temporarily changed `FG_BUILDING2_HIGH_SETUP_SEGMENT_BYTES` from `32` to `40` sectors, rebuilt in the same `233472` byte PS-EXE bucket, and ran the focused B2-high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-setup3-43-current/20260520-074813-4159145/summary.json`. The constant was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The larger setup segment reduced uncovered active entries `284 -> 276` but regressed timing: B2-high moved scene/loop/target `1621/1347/1313 -> 1641/1348/1310`, overrun `34 -> 38`, blocking `39 -> 44`, and hidden refill `16 -> 15`; loop reads stayed `43`. | Close B2-high first setup-segment extension to `3..43`. The boundary coverage is real, but extra setup debt and target/cadence movement outweigh it; future B2-high setup residency still needs prior clean/allocator byte reduction or generated ownership that proves `blocking_vb <= 39`, `refill <= 16`, and no target shrink. |
| 2026-05-20 | `walkstuf1-low-restore-minus-current` | Log-only; pack/script restored. | Test a pack-side cleanup subtraction swing on W1-low after the B2 setup-edge lane closed. A temporary `restore-minus-current --copy-unparseable` variant reduced modeled W1-low restore bytes `722524 -> 720598` while growing active payload `764658 -> 765786`, so the binary question was whether lower cleanup pixels beat the extra payload and allocator shape. | Temporarily generated `WALK1LOW.FG2` from `compact-fgp3-restore-minus-current.py --copy-unparseable --pad-to-input-size`, preserving pack footprint and PS-EXE bucket, then ran focused W1-low. Transform log: `scratch/restore-minus-current-probe/WALK1LOW.log`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-restore-minus-current/20260520-075228-4183405/summary.json`. The pack was restored from `scratch/walkstuf1-low-restore-minus-current/WALK1LOW.before.FG2`, and the temporary script option was removed. | Failed/no promotion. The scene did not reach `JCPERF2`: it halted with `JCBSOD-FATAL CACHE exhausted` during split clean-rect allocation (`req=48640`, `have=90076`, `memCacheUsed=565284`, `memTransientUsed=777168`). | Close restore-minus-current for W1-low in this source form. Even a small cleanup-byte reduction can alter clean-rect allocation shape enough to hit the allocator cliff; future cleanup subtraction needs a clean-rect allocation cap/budget proof or a per-frame generated selector that avoids increasing split-rect pressure. |
| 2026-05-20 | `walkstuf1-high-trim-entry55-current` | Log-only; pack restored. | Test the largest current W1-high no-decode draw-tail candidate. Entry `55` / source frame `65` trims from `4716` bytes to `2` bytes (`4 -> 1` sectors) with zero draw pixels removed, so it looked like a high-upside pack-side sector collapse. | Temporarily generated `WALKSTUF1.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets`, then ran focused W1-high. Transform summary: `scratch/trim-candidate-scan/w1high-summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-trim-entry55-current/20260520-075635-12863/summary.json`. The pack was restored from `scratch/walkstuf1-high-trim-entry55-current/WALKSTUF1.before.FG2`. | Failed/no promotion. The scene did not reach `JCPERF2`: it halted with `JCBSOD-FATAL CACHE exhausted` during clean-rect allocation (`req=97280`, `have=49548`, `memCacheUsed=605812`, `memTransientUsed=716240`). | Close W1-high entry55/source frame65 draw-tail collapse on this baseline. The byte/sector signal is real, but the transformed startup/clean-rect shape trips the allocator before the speed gate; retry only after clean-rect allocation pressure is reduced or the transform is made frame-selective with an allocator proof. |
| 2026-05-20 | `walkstuf1-high-entry55-empty-current-canonical` / `walkstuf1-high-entry55-trim-canonical` | Log-only; pack restored and PS1 rebuilt. | Recheck the W1-high entry55 family using the exact canonical matrix boot after the first quick probe used the simplified W1-high boot and hit a noncanonical clean-rect shape. The entry55 payload parses as zero cleanup rows and zero draw rows with zero trailing bytes, so the two valid binary questions were whether converting it to a runtime empty hold, or keeping the preserve-offset `2` byte no-op payload, could bank the `4.7 KiB` byte cut without changing pixels or cadence. | First rewrote entry `55` / source frame `65` to `width=0`, `height=0`, `dataSize=0` while preserving source/hold timing, then ran W1-high with `night 1 holiday 0 raft-stage 4 island-pos -154 54`. Then restored the pack and tested the preserve-offset trim form (`4716 -> 2` bytes, `4 -> 1` sectors) with the same canonical boot. Summaries/artifacts: `scratch/walkstuf1-high-entry55-empty-current/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-high-entry55-empty-current-canonical/20260520-081111-95473/summary.json`, `scratch/walkstuf1-high-entry55-trim-canonical/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-high-entry55-trim-canonical/20260520-081302-106533/summary.json`. | Failed/no promotion. Empty-entry regressed W1-high from `1807/1471/1440` to `1811/1475/1438`, overrun `31 -> 37`, blocking `56 -> 63`, refill `13 -> 18`, and reads `41 -> 39`. Preserve-offset trim was nearly identical and slightly worse on blocking: `1811/1475/1438`, blocking `64`, refill `18`, reads `39`. Pack LBA and the `233472` byte PS-EXE bucket stayed fixed. | Close W1-high entry55/source frame65 in both empty-entry and preserve-offset no-op forms. The byte/read reduction is real but it moves cadence into visible blocking and hidden refill; future W1-high no-op payload work needs generated per-frame deadline/refill ownership or a paired scheduler/data-shape change, not a standalone entry55 rewrite. |
| 2026-05-20 | `walkstuf1-high-prepare-first-current` | `ps1: prepare walkstuf1 high before window refill` | After the broad yellow-row prepare-first swing failed, isolate the one remaining plausible case: W1-high had the same staged-prepare pressure as W1-low plus `56` blocking VBlanks and `10` due misses, while B2/V3 had already proved phase-negative under the broad toggle. | Removed the low-tide guard from the existing WALKSTUF1 prepare-before-window branch in `foregroundPilotPlay()`, rebuilt in the same `233472` byte PS-EXE bucket, ran a focused W1-high proof at `scratch/ps1-perf-iterate/walkstuf1-high-prepare-first-current/20260520-082352-168216/summary.json`, then ran the five-yellow canary at `scratch/ps1-perf-iterate/walkstuf1-high-prepare-first-canary/20260520-082537-178447/summary.json`. | Promoted as scheduler/CD-pressure improvement. W1-high scene/loop/target shifts `1807/1471/1440 -> 1808/1472/1441`, keeping overrun flat at `31` and hidden refill flat at `13`, while blocking improves `56 -> 43` and due misses improve `10 -> 7`. The other five-yellow rows stay exact-flat; pack LBAs and the PS-EXE bucket stay fixed. | Keep prepare-first as the new W1-high scheduler baseline. Public rollup is `+0.2387%` over target / `99.7644%` target speed, raw signed rollup is about `-0.4782%` / `100.4948%`, and bands remain `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; next W1-high work needs generated ownership or pack/render reduction because the row is still yellow. |
| 2026-05-20 | `walkstuf1-high-rg365-389-current` / `walkstuf1-high-rg365-389-slack4-current` / `walkstuf1-high-rg365-389-slack8-current` | Log-only; source restored and PS1 rebuilt. | Test the top refreshed W1-high direct-read candidate after the prepare-first promotion. The read planner ranked sector range `365..389` as a 24-sector scheduler-owned row with three modeled saved reads, so the binary question was whether a static row or coarse slack guard could safely remove the late-cluster reads without stealing visible/refill cadence. | Temporarily inserted `{365,389,0}`, then `{365,389,4}`, then `{365,389,8}` in `kWalkstuf1HighReadGroups12[]`, rebuilding in the same `233472` byte PS-EXE bucket for each focused W1-high proof. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-high-rg365-389-current/20260520-084807-305396/summary.json`, slack4 `scratch/ps1-perf-iterate/walkstuf1-high-rg365-389-slack4-current/20260520-085003-316875/summary.json`, and slack8 `scratch/ps1-perf-iterate/walkstuf1-high-rg365-389-slack8-current/20260520-085313-335015/summary.json`. | Failed/no promotion. Raw and slack4 were identical and regressed W1-high scene/loop/target `1808/1472/1441 -> 1812/1476/1443`, overrun `31 -> 33`, blocking/refill `43/13 -> 46/16`, while reads improved `44 -> 41` and due stayed `7`; the baseline gate rejected overrun, blocking, and refill. Slack8 was safe but exact-flat: W1-high remained `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, reads/due `44/7`, so it had no promotable improvement. | Close W1-high `365..389` as a hand-authored table row and as a coarse slack4/slack8 guard. The range has real read-count signal but only when it fires in a phase that adds visible and hidden refill debt; future late-cluster work needs generated per-frame deadline/refill ownership or a smaller isolated row that proves cadence ownership. |
| 2026-05-20 | `walkstuf1-high-rg379-403-current` / `walkstuf1-high-rg379-403-slack8-current` | Log-only; source restored and PS1 rebuilt. | Continue the W1-high late-cluster direct-read closure after `365..389` proved phase-negative. The refreshed matrix ranked `379..403` as the next 24-sector row with three modeled saved reads, but flagged it `unsafe:tight-visible-gap`, so the proof covered both raw fire and a coarse 8-VBlank slack guard. | Temporarily inserted `{379,403,0}` and then `{379,403,8}` in `kWalkstuf1HighReadGroups12[]`, rebuilding in the same `233472` byte PS-EXE bucket for each focused W1-high proof. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-high-rg379-403-current/20260520-085738-360546/summary.json` and slack8 `scratch/ps1-perf-iterate/walkstuf1-high-rg379-403-slack8-current/20260520-085939-372913/summary.json`. | Failed/no promotion. Raw reduced loop reads `44 -> 40` but regressed W1-high scene/loop/target `1808/1472/1441 -> 1814/1478/1441`, overrun `31 -> 37`, blocking `43 -> 50`, and hidden refill `13 -> 20`. Slack8 avoided the bad fire and stayed exact-flat at `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, reads/due `44/7`, which is safe but not an improvement. | Close W1-high `379..403` as a static table row and coarse slack8 guard. The saved-read model is outweighed by visible/refill debt when it fires; the remaining late-cluster queue should move to smaller isolated rows or generated deadline/refill ownership rather than more broad 24-sector appends. |
| 2026-05-20 | `walkstuf1-high-rg379-395-current` / `walkstuf1-high-rg379-395-slack4-current` / `walkstuf1-high-rg379-395-slack8-current` | Log-only; source restored and PS1 rebuilt. | Test the smaller W1-high late-cluster slice after the broad `365..389` and `379..403` rows failed. The `379..395` row modeled two saved reads with less overread surface, so the proof checked raw fire plus 4- and 8-VBlank slack guards. | Temporarily inserted `{379,395,0}`, `{379,395,4}`, and `{379,395,8}` in `kWalkstuf1HighReadGroups12[]`, rebuilding in the same `233472` byte PS-EXE bucket for each focused W1-high proof. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-high-rg379-395-current/20260520-090326-394820/summary.json`, slack4 `scratch/ps1-perf-iterate/walkstuf1-high-rg379-395-slack4-current/20260520-090525-406594/summary.json`, and slack8 `scratch/ps1-perf-iterate/walkstuf1-high-rg379-395-slack8-current/20260520-090716-417532/summary.json`. | Failed/no promotion. Raw and slack4 were identical: loop reads improved `44 -> 42` and overrun stayed `31`, but scene/loop/target moved `1808/1472/1441 -> 1809/1473/1442` and blocking/refill regressed `43/13 -> 44/14`, crossing the strict gate. Slack8 avoided the bad fire and stayed exact-flat at `1808/1472/1441`, blocking/refill `43/13`, reads/due `44/7`. | Close W1-high `379..395` as a static row and coarse slack4/slack8 guard. This confirms the first half of the late cluster is only one VBlank from viable, but still needs generated refill/deadline ownership or a paired render/work reduction before it can be promoted. |
| 2026-05-20 | `walkstuf1-high-rg395-411-current` | `ps1: add walkstuf1 high 395 read group` | Continue the smaller-row W1-high late-cluster path after the `379..395` half proved one VBlank too expensive. The `395..411` row modeled two saved reads but starts after the worst tight cluster, so the binary question was whether it could bank CD work without visible/refill debt. | Added `{395,411,0}` to `kWalkstuf1HighReadGroups12[]`, rebuilt in the same `233472` byte PS-EXE bucket, ran focused W1-high at `scratch/ps1-perf-iterate/walkstuf1-high-rg395-411-current/20260520-091049-438248/summary.json`, then ran the five-yellow no-regression canary at `scratch/ps1-perf-iterate/walkstuf1-high-rg395-411-canary/20260520-091302-451039/summary.json`. | Promoted as same-speed CD-pressure work, not a VBlank speed win. W1-high stays exact-flat at scene/loop/target `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, and due `7`, while loop reads/read time improve `44/204 -> 42/201`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 low canaries stay exact-flat; pack LBAs and the PS-EXE bucket stay fixed. | Keep `{395..411}` as the new W1-high CD-pressure baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; the material gain is lower CD work on the same timing row. |
| 2026-05-20 | `walkstuf1-high-rg92-116-current` / `walkstuf1-high-rg92-116-slack1-current` / `walkstuf1-high-rg92-116-slack2-current` / `walkstuf1-high-rg92-116-slack4-current` | Log-only; source restored and PS1 rebuilt. | Test the top refreshed W1-high early-cluster row after the `{395..411}` CD-pressure promotion. The candidate matrix ranked `92..116` as the largest remaining scheduler-owned W1-high row with three modeled saved reads, but marked it unsafe because the first visible gap is tight. | Temporarily inserted `{92,116,0}`, `{92,116,4}`, `{92,116,2}`, and `{92,116,1}` in `kWalkstuf1HighReadGroups12[]`, rebuilding in the same `233472` byte PS-EXE bucket for each focused W1-high proof. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-high-rg92-116-current/20260520-093031-548942/summary.json`, slack4 `scratch/ps1-perf-iterate/walkstuf1-high-rg92-116-slack4-current/20260520-093222-559711/summary.json`, slack2 `scratch/ps1-perf-iterate/walkstuf1-high-rg92-116-slack2-current/20260520-093408-569965/summary.json`, and slack1 `scratch/ps1-perf-iterate/walkstuf1-high-rg92-116-slack1-current/20260520-093554-580268/summary.json`. | Failed/no promotion. Raw reduced loop reads/read time `42/201 -> 39/199` and due misses `7 -> 6`, but regressed W1-high scene/loop/target `1808/1472/1441 -> 1810/1474/1438`, overrun `31 -> 36`, blocking `43 -> 47`, and hidden refill `13 -> 15`. Slack1, slack2, and slack4 all avoided the bad fire and stayed exact-flat at `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, reads/read time `42/201`, and due `7`. | Close W1-high `92..116` as a hand-authored table row and coarse slack1/slack2/slack4 guard. The early cluster has real read-count signal only when it steals target cadence and refill slack; remaining W1-high read-table work should use the refreshed candidate CSV rather than literal ranges in this ledger, otherwise it needs generated deadline/refill ownership or pack/render data-shape work. |
| 2026-05-20 | `walkstuf1-high-rg411-423-replace422-exact-current` | `ps1: retarget walkstuf1 high tail read group` | Re-test the last open W1-high scheduler-owned tail row with canonical boot variants after an initial simplified-boot canary produced false VISITOR3/B2 drift. The focused form replaced the older high-tide `{422..434}` row with `{411..423}` instead of adding another table entry. | Replaced `{422,434,0}` with `{411,423,0}` in `kWalkstuf1HighReadGroups12[]`, rebuilt in the same `233472` byte PS-EXE bucket, ran focused W1-high at `scratch/ps1-perf-iterate/walkstuf1-high-rg411-423-replace422-exact-current/20260520-101248-790740/summary.json`, then ran the canonical five-yellow canary at `scratch/ps1-perf-iterate/walkstuf1-high-rg411-423-replace422-exact-canary/20260520-101432-800575/summary.json`. | Promoted as same-speed CD-pressure work, not a VBlank speed win. W1-high stays exact-flat at `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, and due `7`, while loop reads/read time improve `42/201 -> 41/198`. VISITOR3 high/low, BUILDING2 high, and WALKSTUF1 low stay exact-flat under canonical boot variants; pack LBAs and the PS-EXE bucket stay fixed. | Keep the `{411..423}` replacement as the new W1-high CD-pressure baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; the refreshed read-candidate matrix now has top open W1-high `379..391` as scheduler-owned-only. |
| 2026-05-20 | `building2-high-generated-owner-122-146-current` | Log-only; source restored and PS1 rebuilt. | Test the next BUILDING2-high big swing after same-speed W1-high CD-pressure work: generated/deadline ownership for the hot early clusters around `122..146`, trying to avoid another inert static table row while preserving exact pixels and the allocator-era baseline. | Captured a same-commit B2-high baseline at `scratch/ps1-perf-iterate/building2-high-baseline-ee72a56/20260520-103632-924198/summary.json`, then tested static row probes, setup-resident probes, and direct-window ownership. Artifacts: slack table row `scratch/ps1-perf-iterate/building2-high-rg122-146-slack10/20260520-103850-937356/summary.json`, raw table row `scratch/ps1-perf-iterate/building2-high-rg122-146-raw/20260520-104047-948615/summary.json`, additive CACHE setup3 `scratch/ps1-perf-iterate/building2-high-setup3-122-146/20260520-104458-972220/summary.json`, additive TRANSIENT setup3 `scratch/ps1-perf-iterate/building2-high-setup3-122-146-transient/20260520-104825-991605/summary.json`, direct-stage bypass `scratch/ps1-perf-iterate/building2-high-window-owner-78-99/20260520-105044-1004795/summary.json`, prefetch-only direct window `scratch/ps1-perf-iterate/building2-high-direct-window-122-146/20260520-105303-1018089/summary.json`, blocking direct window overread `scratch/ps1-perf-iterate/building2-high-direct-window-122-146-blocking/20260520-105458-1029138/summary.json`, and real-entry direct window `scratch/ps1-perf-iterate/building2-high-direct-window-123-146/20260520-105916-1053545/summary.json`. | Failed/no promotion. The static `122..146` row was inert in both slack10 and raw forms (`group_hits=0`, exact-flat at `1621/1347/1313`, blocking/refill `39/16`, reads `43`) because the runtime entries begin at sector `123`, not the planner's overread boundary. CACHE setup3 failed before `JCPERF2` with `JCBSOD-FATAL CACHE exhausted` during clean-rect allocation (`req=87724`, `have=89872`). TRANSIENT setup3 completed but regressed to `1634/1348/1312`, overrun `36`, blocking `43`, reads `38`. Direct-stage bypass and `122` direct-window probes were exact-flat. The real-entry `123..146` direct-window owner reduced reads `43 -> 39` but regressed scene/loop/target `1621/1347/1313 -> 1628/1355/1311`, overrun `34 -> 44`, blocking `39 -> 46`, and hidden refill `16 -> 22`. | Close B2-high `122/123..146` hand-authored ownership on this baseline. The read-count signal is real only when it steals too much active/refill cadence, and additive residency either exhausts CACHE or shifts the phase negative. Future B2-high work should target code-neutral generated metadata with frame/deadline awareness, smaller `146..158`/`146..162` follow-on ranges, clean/upload/restore byte reduction, or a pack data-shape change that proves `blocking_vb <= 39`, `prefetch_overrun_vb <= 16`, and no target shrink. |
| 2026-05-20 | `building2-high-rg158-174-current` | `ps1: group building2 high 158 reads` | Retry B2-high `158..174` after the current allocator-era baseline changed underneath the earlier phase-negative attempt. The accepted `{185..197}` row, W1-high tail retarget, and rejected `122/123..146` ownership probes left this as the next bounded CD-pressure check with real read-count upside. | Added `{158,174,0}` to `kBuilding2HighReadGroups12[]`, rebuilt in the same `233472` byte PS-EXE bucket, ran focused B2 high at `scratch/ps1-perf-iterate/building2-high-rg158-174-current/20260520-110416-1081729/summary.json`, then ran the five-yellow no-regression canary at `scratch/ps1-perf-iterate/building2-high-rg158-174-current-canary/20260520-110609-1092385/summary.json`. | Promoted as same-speed CD-pressure work, not a VBlank speed win. B2-high stays exact-flat at scene/loop/target `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, and due `5`, while loop reads/read time improve `43/197 -> 40/189` and hidden read VBlanks drop `158 -> 150`. VISITOR3 high/low and WALKSTUF1 high/low stay exact-flat in the canary. | Keep `{158..174}` as the new B2-high CD-pressure baseline. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained; the next B2-high work should avoid the closed `122/123..146` cluster and target generated deadline ownership, `174..186`, or pack/upload/restore reduction. |
| 2026-05-20 | `building2-high-post158-open-scalar-closures` | Log-only; source restored and PS1 rebuilt. | Close the refreshed B2-high open scalar rows after the `{158..174}` CD-pressure promotion made them the top candidate-matrix entries. The goal was to see whether any remaining simple bridge/early rows could bank speed or CD work without writing generated scheduler ownership. | Temporarily tested `{174,186,0}`, `{117,141,0}`, and `{140,156,0}` in `kBuilding2HighReadGroups12[]` against the committed `{158..174}` baseline. Artifacts: `scratch/ps1-perf-iterate/building2-high-rg174-186-current/20260520-112650-1207796/summary.json`, `scratch/ps1-perf-iterate/building2-high-rg117-141-current/20260520-112843-1218725/summary.json`, and `scratch/ps1-perf-iterate/building2-high-rg140-156-current/20260520-113038-1229826/summary.json`. | Failed/no promotion. `{174..186}` regressed B2-high to `1625/1351/1312`, overrun `39`, blocking `44`, and refill `18`. `{117..141}` passed the strict no-regression gate but was inert: timing, loop reads/read time, hidden reads, attempts, and `group_hits=0` all stayed exact-flat at the current `1621/1347/1313`, `40/189` baseline. `{140..156}` regressed harder to `1626/1353/1312`, overrun `41`, blocking `45`, and refill `20`. | Close B2-high `{117..141}`, `{140..156}`, and `{174..186}` as hand-authored scalar table rows. The remaining B2 path should not be more raw read-group rows; it needs generated deadline/refill ownership, pack data-shape relocation, clean/upload/restore byte reduction, or a row-specific render-work reduction that creates enough slack before retrying the same sector spans. |
| 2026-05-20 | `walkstuf1-high-rg379-391-current` | Log-only; source restored and PS1 rebuilt. | Test the only refreshed open W1-high scheduler-owned read-table row after the `{411..423}` retarget and B2 `{158..174}` promotion. The range had one modeled saved read and was the last non-closed W1-high scalar candidate in the generated matrix. | Temporarily inserted `{379,391,0}` into `kWalkstuf1HighReadGroups12[]` and ran focused W1 high against the committed five-yellow canary baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-rg379-391-current/20260520-113243-1241832/summary.json`. | Failed/no promotion. The row moved W1-high target favorably `1441 -> 1442` and overrun `31 -> 30`, but violated the blocking gate: blocking `43 -> 48` and due misses `7 -> 8`, while loop stays `1472`. This is not promotable under the no-dropped-frame/pixel-perfect acceptance rule. | Close W1-high `{379..391}` as a hand-authored scalar row. The next W1-high path should be deadline-owned scheduling or render/pack work around the late cluster, not another raw table append. |
| 2026-05-20 | `walkstuf1-prepare-slack5-current` | Log-only; source restored and PS1 rebuilt. | Test a generated-owner-style scheduler swing for the WALKSTUF1 yellow pair after scalar read rows closed: allow staged visual preparation one held VBlank earlier for `walkstuf1` (`slack=5` as well as the accepted `slack=4`) so the next held loop could spend the former prepare slot on CD window work. | Temporarily widened `fgRuntimeCanPrepareStagedFrame()` only for `walkstuf1`, rebuilt in the same `233472` byte PS-EXE bucket, and ran a focused high/low gate against the accepted five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/walkstuf1-prepare-slack5-current/20260520-114109-1289612/summary.json`. | Failed/no promotion. Both WALKSTUF1 rows stayed exact-flat: high remained `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, reads/due `41/7`; low remained `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. The source shape grew `foregroundPilotPlay` by `276` bytes and shifted downstream hot symbols without any work-volume or speed improvement. | Close early prepared-frame ownership for WALKSTUF1 as a standalone scheduler fix. The accepted `slack=4` prepare point remains the right local shape; future W1 work needs per-cluster deadline/refill metadata, pack/data-shape changes, or render-work reduction rather than preparing the same staged frame earlier. |
| 2026-05-20 | `walkstuf1-low-no-direct-stage-current` | Log-only; source restored and PS1 rebuilt. | Re-test the older W1-low no-direct-stage ownership signal on the current allocator/payload baseline. The prior version reduced visible blocking but failed hidden refill and layout gates; the current banked W1-low payload trims and B2/W1 CD-pressure changes made it worth checking whether the refill cost was now absorbable. | Temporarily disabled the small-payload direct-stage shortcut for `walkstuf1` low tide only, rebuilt in the same `233472` byte PS-EXE bucket, and ran a focused W1-low strict gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-no-direct-stage-current/20260520-114656-1322913/summary.json`. | Failed/no promotion. Due misses improved `4 -> 2` and hits increased `212 -> 214`, but the refill/target tradeoff is still not acceptable: scene/loop/target regressed `1809/1470/1446 -> 1811/1472/1444`, overrun `24 -> 28`, hidden refill `5 -> 14`, and loop reads `24 -> 25`; blocking stayed `33`. The source shape grew `foregroundPilotPlay` by `60` bytes and shifted downstream hot symbols. | Keep the direct-stage shortcut enabled. The W1-low ownership signal is real but still needs generated per-frame/refill-budget control or pack/data-shape work; a broad low-tide direct-stage disable trades fewer due misses for worse target cadence and hidden refill. |
| 2026-05-20 | `upload-gap0-gap4-five-yellow-current` | Log-only; renderer restored and PS1 rebuilt. | Re-test global dirty-upload merge-gap reduction after the B2-high `{158..174}` promotion changed the five-yellow baseline. Gap `0` and `4` had previously been exact-flat, but the newer CD-pressure baseline could have made upload-byte reduction visible. | Temporarily changed `GR_UPLOAD_BAND_MERGE_GAP` from `8` to `0`, built, and ran the canonical five-yellow gate; then changed it to `4`, rebuilt, and repeated. Artifacts: gap0 `scratch/ps1-perf-iterate/upload-gap0-five-yellow-current/20260520-115957-1396578/summary.json`; gap4 `scratch/ps1-perf-iterate/upload-gap4-five-yellow-current/20260520-120905-1449371/summary.json`. Source was restored to gap `8` and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both variants were exact-flat on all speed-bearing metrics: B2 high stayed `1621/1347/1313`, VISITOR3 high `1388/1071/1045`, VISITOR3 low `1369/1065/1039`, W1 high `1808/1472/1441`, and W1 low `1809/1470/1446`, with unchanged overrun, blocking, refill, reads/due, pack LBAs, and PS-EXE bucket. Gap0 reduced B2/W1 upload bytes but increased VISITOR3 upload bytes; gap4 mildly reduced B2, VISITOR3-low, and W1 upload bytes, but VISITOR3-high still increased and no timing moved. | Keep `GR_UPLOAD_BAND_MERGE_GAP=8`. Global full-width upload-band tuning is exhausted as a 99% speed path on this baseline; further upload work must be scene-local/generated, pack-emitted/precomposed, or paired with explicit CD/deadline ownership. |
| 2026-05-20 | `walkstuf1-low-entry60-window-swing-current` | Log-only; pack/source restored and PS1 rebuilt. | Follow up the visible-positive but refill-negative W1-low entry60/source frame81 trim with a scalar resident-window recovery ladder. The binary question was whether giving the low-tide residual window more room could absorb the extra refill while preserving the two-VBlank loop gain. | Reapplied the entry60 preserve-offset trim (`4432 -> 476` bytes, `4 -> 2` sectors, active payload `764658 -> 760702`) and tested `FG_WALKSTUF1_LOW_RESIDUAL_WINDOW_BYTES` at `48 KiB`, `56 KiB`, and `64 KiB`; also tested the `48 KiB` variant with a `{204..210}` scalar row and with the low-only 4-VBlank scheduler guard. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-entry60-window48-current/20260520-122353-1534008/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-entry60-window48-rg204-210-current/20260520-122604-1546824/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-entry60-window48-guard4-current/20260520-122811-1558916/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-low-entry60-window56-current/20260520-123002-1569881/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-low-entry60-window64-current/20260520-123232-1584423/summary.json`; transform summary: `scratch/walkstuf1-low-entry60-window-swing-current/entry60.summary.json`. | Failed/no promotion. All window sizes kept the bare visible win but failed the same hidden refill gate: W1-low moved `1809/1470/1446 -> 1807/1468/1445`, overrun `24 -> 23`, blocking `33 -> 28`, and due `4 -> 3`, while hidden refill regressed `5 -> 10` and loop reads `24 -> 27`. The `{204..210}` row and 4-VBlank guard were inert under the `48 KiB` window. | Close scalar resident-window growth for W1-low entry60. The signal is still valuable, but promotion needs generated deadline/refill ownership or a different pack/render work reduction; larger resident windows alone do not recover the hidden refill budget. |
| 2026-05-20 | `visitor3-low-rg9-33-current` / `visitor3-low-rg86-98-current` / `visitor3-low-rg92-108-current` | Log-only; source restored and PS1 rebuilt. | Test VISITOR3-low direct grouped-append rows after the W1-low window ladder closed. The refreshed read plan ranked `{9..33}`, `{86..98}`, and `{92..108}` as lower-risk low-tide candidates with one or two modeled saved reads and enough visible gap to justify a focused binary proof. | Temporarily added a low-tide VISITOR3 read-group table and routed VISITOR3 low through it under the default clean-relief stream window, testing one row per build. Artifacts: `{9..33}` `scratch/ps1-perf-iterate/visitor3-low-rg9-33-current/20260520-123653-1609574/summary.json`, `{86..98}` `scratch/ps1-perf-iterate/visitor3-low-rg86-98-current/20260520-123832-1619384/summary.json`, and `{92..108}` `scratch/ps1-perf-iterate/visitor3-low-rg92-108-current/20260520-124011-1629293/summary.json`. | Failed/no promotion. `{9..33}` was source-shape negative with no read-count gain, regressing loop `1065 -> 1067` and blocking `75 -> 76`. `{86..98}` fired and reduced loop reads `18 -> 17`, but regressed scene/loop/target to `1374/1070/1041`, overrun `29`, and blocking `79`. `{92..108}` also reduced reads `18 -> 17` but still regressed to `1371/1067/1041`, blocking `76`. | Close VISITOR3-low hand-authored grouped appends on this baseline. The remaining VISITOR3-low gap needs generated per-frame deadline/refill ownership or pack/render byte reduction that creates slack before the append, not a static low-tide table branch. |
| 2026-05-20 | `building2-high-setupseg86-242-current` / `building2-high-setupseg90-242-current` / `building2-high-setupseg104-242-current` / `building2-high-setupseg104-242-transient-current` / `building2-high-setupseg135-242-current` / `building2-high-setupseg151-242-current` / `building2-high-setupseg185-242-current` | Log-only; source restored and PS1 rebuilt. | Test the large B2-high setup-residency swing after scalar rows and hot-cluster ownership closed. The goal was to pay more of the dense active read clusters during setup and reduce active-loop CD pressure enough to move the remaining `1347/1313` yellow row toward green. | Temporarily widened `FG_BUILDING2_HIGH_SETUP_SEGMENT2_*` through a descending ladder, then restored the accepted `202..242` segment. Artifacts: `86..242` `scratch/ps1-perf-iterate/building2-high-setupseg86-242-current/20260520-124445-1655614/summary.json`, `90..242` `scratch/ps1-perf-iterate/building2-high-setupseg90-242-current/20260520-124736-1671900/summary.json`, `104..242` `scratch/ps1-perf-iterate/building2-high-setupseg104-242-current/20260520-125217-1698274/summary.json`, `104..242` TRANSIENT `scratch/ps1-perf-iterate/building2-high-setupseg104-242-transient-current/20260520-125621-1721436/summary.json`, `135..242` `scratch/ps1-perf-iterate/building2-high-setupseg135-242-current/20260520-125938-1740361/summary.json`, `151..242` `scratch/ps1-perf-iterate/building2-high-setupseg151-242-current/20260520-130218-1755785/summary.json`, and `185..242` `scratch/ps1-perf-iterate/building2-high-setupseg185-242-current/20260520-130519-1773144/summary.json`. | Failed/no promotion. The large CACHE-backed variants structural-failed before `JCPERF2`: `86..242`, `90..242`, `104..242`, `135..242`, and `151..242` all exhausted CACHE during clean-rect allocation after setup buffers from `251904` to `385024` bytes; the TRANSIENT-backed `104..242` variant exhausted TRANSIENT/libc on the `348160` byte setup buffer. The memory-safe `185..242` variant reached counters and reduced loop reads `40 -> 38`, but regressed B2-high scene/loop/target `1621/1347/1313 -> 1630/1350/1311`, overrun `34 -> 39`, blocking `39 -> 45`, and prefetch overrun `16 -> 19`. | Close large B2-high retained setup expansion on this allocator-era baseline. Extra setup residency either collides with the clean-rect/CACHE budget or steals active cadence even when memory-safe; future B2-high work should focus on generated deadline/refill ownership, scene-local clean/upload/restore byte reduction, or no-decode pack shapes that do not add setup-resident live bytes. |
| 2026-05-20 | `building2-high-rg249-261-slack12-current` / `building2-high-rg249-261-slack10-current` / `building2-high-rg249-261-slack9-current` | Log-only; source restored and PS1 rebuilt. | Close the guarded form of the B2-high `249..261` read candidate after raw and slack8 variants had already shown phase-negative behavior. This tested whether a stricter refill-budget guard could retain the modeled saved-read upside without stealing target/refill cadence. | Temporarily added `{249,261,12}`, then `{249,261,10}`, then `{249,261,9}` to `kBuilding2HighReadGroups12[]`, rebuilding for each focused B2-high gate. Artifacts: slack12 `scratch/ps1-perf-iterate/building2-high-rg249-261-slack12-current/20260520-131227-1813576/summary.json`, slack10 `scratch/ps1-perf-iterate/building2-high-rg249-261-slack10-current/20260520-131413-1824081/summary.json`, and slack9 `scratch/ps1-perf-iterate/building2-high-rg249-261-slack9-current/20260520-131555-1834261/summary.json`. | Failed/no promotion. Slack9, slack10, and slack12 were all safe but inert: B2-high stayed exact-flat at `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads `40`, and due `5`; no loop read or work-volume metric improved. Lower guards are already closed by prior slack8/raw regressions. | Close B2-high `249..261` as a hand-authored guarded row on this baseline. The safe guard threshold prevents the bad fire; the firing threshold regresses cadence. This range needs generated per-frame ownership with target/refill prediction or a preceding data-shape reduction before any retry. |
| 2026-05-20 | `building2-high-rg249-261-prefetch-only-current` | Log-only; source restored and PS1 rebuilt. | Test a generated-owner shape for the same B2-high `249..261` range: permit the grouped append only for hidden prefetch work and reject it on visible due/window fills. The hypothesis was that raw `249..261` was bad because it fired on the visible path, while a prefetch-only owner might keep the read-count signal off the critical cadence. | Temporarily added `{249,261,0}` and passed the prefetch ownership flag into `fgRuntimeGroupedAppendTargetEnd()`, returning the original target for non-prefetch B2-high `249` appends. Focused artifact: `scratch/ps1-perf-iterate/building2-high-rg249-261-prefetch-only-current/20260520-131943-1856077/summary.json`. | Failed/no promotion. Prefetch-only ownership was worse than the guarded inert forms: B2-high regressed scene/loop/target `1621/1347/1313 -> 1631/1357/1314`, overrun `34 -> 43`, blocking `39 -> 55`, due `5 -> 8`, and loop reads `40 -> 42`, despite prefetch overrun improving `16 -> 14`. The helper growth also added `132` bytes to `fgRuntimeFillWindowForEntry` while staying inside the same PS-EXE sector bucket. | Close B2-high `249..261` prefetch-only generated ownership. Moving the append off visible due reads is insufficient; the hidden fire still perturbs later due cadence. Retry only with frame-index-specific deadline/refill metadata or a preceding pack/render reduction that creates slack before this cluster. |
| 2026-05-20 | `walkstuf1-low-entry60-prepare5-current` / `walkstuf1-low-entry60-minslack4-current` | Log-only; pack/source restored and PS1 rebuilt. | Close the remaining scalar recovery forms for the visible-positive W1-low entry60/source frame81 trim. The binary question was whether preparing the staged visual frame one VBlank earlier, or raising the low-tide refill slack floor, could preserve the two-VBlank loop gain without increasing hidden refill. | Reapplied the entry60 preserve-offset trim (`4432 -> 476` bytes, `4 -> 2` sectors, active payload `764658 -> 760702`). Variant A widened `fgRuntimeCanPrepareStagedFrame()` for `walkstuf1` to allow slack `5` as well as the accepted slack `4`; variant B restored normal prepared-frame timing and raised W1-low window refill minimum slack to `4`. Artifacts: prepare5 `scratch/ps1-perf-iterate/walkstuf1-low-entry60-prepare5-current/20260520-134814-2013355/summary.json` and minSlack4 `scratch/ps1-perf-iterate/walkstuf1-low-entry60-minslack4-current/20260520-135033-2026917/summary.json`. | Failed/no promotion. Prepare5 preserved the visible win, moving `1809/1470/1446 -> 1807/1468/1445`, overrun `24 -> 23`, blocking `33 -> 28`, and due `4 -> 3`, but hidden refill regressed `5 -> 10` and reads rose `24 -> 27`. MinSlack4 fixed hidden refill `5 -> 2` but traded it into visible cadence: `1809/1470/1446 -> 1811/1472/1446`, overrun `24 -> 26`, blocking `33 -> 40`, reads `24 -> 26`, and due `4 -> 6`. | Close W1-low entry60 recovery through scalar prepared-frame and refill-slack controls. The trim still has real visible signal, but promotion now requires generated frame/deadline/refill ownership or a different pack/render reduction; scalar slack knobs only move the cost between hidden refill and visible blocking. |
| 2026-05-20 | `restore-minus-current-preserve-scan-current` | Log-only; tooling retained, packs unchanged. | Reopen the cleanup-minus-current pack-side lane as a safe scan after several current yellow rows still reported high restore bytes. The binary question was whether any remaining B2/V3/W1 FGP3/v4 entries could be shortened in place without moving offsets, and without aborting on known D4/no-op/trailing-byte payload forms. | Extended `scripts/compact-fgp3-restore-minus-current.py` with `--copy-unparseable`, `--preserve-offsets`, `--frames`, and `--summary`, then scanned `BUILDING2.FG2`, `VISITOR3.FG2`, `VIST3LOW.FG2`, `WALKSTUF1.FG2`, and `WALK1LOW.FG2` in preserve-offset mode. Transform summaries: `scratch/big-swing-scans-current/BUILDING2.restoreminus-preserve.summary.json`, `scratch/big-swing-scans-current/VISITOR3.restoreminus-preserve.summary.json`, `scratch/big-swing-scans-current/VIST3LOW.restoreminus-preserve.summary.json`, `scratch/big-swing-scans-current/WALKSTUF1.restoreminus-preserve.summary.json`, and `scratch/big-swing-scans-current/WALK1LOW.restoreminus-preserve.summary.json`. | Failed/no pack promotion. All five current yellow packs had `changed_entries=0` and `saved_bytes=0`. BUILDING2 only exposed already-known unparseable trailing-byte entries; VISITOR3 high/low had no shrinkable normal entries and frame `143` would grow `2857 -> 2861`; WALKSTUF1 high/low similarly had only unparseable or growth cases. | Keep the hardened scanner because it makes future preserve-offset cleanup scans deterministic, but close restore-minus-current as a current five-yellow speed lane. Remaining cleanup/restore work needs a new transform family, generated clean ownership, or clean-rect allocation relief before another PS1 gate. |
| 2026-05-20 | `walkstuf1-high-local-lz55-current` / `building2-high-local-lz89-91-current` / `walkstuf1-low-local-lz60-current` | Log-only; packs restored. | Test the largest current local-LZ sector-collapse candidates using the runtime's existing `L4` decoder, avoiding new PS1 code. The scan found no VISITOR3 high/low whole-sector local-LZ candidates; the best remaining candidates were W1-high entry `55`, B2-high entries `89..91`, and W1-low entry `60`. | Encoded W1-high entry `55` (`4716 -> 551`, `3 -> 1` sectors), B2-high entries `89`, `90`, and `91` (`8814 -> 4843`, `8849 -> 5275`, `8867 -> 5834`, each `5 -> 3` sectors), and W1-low entry `60` (`4432 -> 943`, `3 -> 1` sectors), one pack family at a time. Artifacts: transform summaries under `scratch/walkstuf1-high-local-lz55-current/summary.json`, `scratch/building2-high-local-lz89-91-current/summary.json`, and `scratch/walkstuf1-low-local-lz60-current/summary.json`; focused gates `scratch/ps1-perf-iterate/walkstuf1-high-local-lz55-current/20260520-140812-2126379/summary.json`, `scratch/ps1-perf-iterate/building2-high-local-lz89-91-current/20260520-141005-2137253/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-low-local-lz60-current/20260520-141150-2147503/summary.json`. | Failed/no promotion. W1-high saved reads but regressed `1808/1472/1441 -> 1810/1474/1439`, overrun `31 -> 35`, blocking `43 -> 47`, and refill `13 -> 16`. B2-high saved modeled sectors but regressed badly: `1621/1347/1313 -> 1632/1359/1311`, overrun `34 -> 48`, blocking `39 -> 42`, and refill `16 -> 18`. W1-low reproduced the known entry60 visible-positive/refill-negative profile: `1809/1470/1446 -> 1807/1468/1445`, overrun `24 -> 23`, blocking `33 -> 28`, due `4 -> 3`, but hidden refill `5 -> 10`. | Close local-LZ sector-collapse as a standalone current-yellow speed lane. It can save sectors, but the decode and shifted read cadence consume the win unless a generated deadline/refill owner or a preceding render/restore reduction creates slack first. |
| 2026-05-20 | `walkstuf1-low-rg129-153-current` / `walkstuf1-low-rg129-153-slack8-current` / `walkstuf1-low-rg129-153-slack32-current` | Log-only; source restored. | Test the distinct retained-read form of the W1-low `129..153` candidate after the direct-stage owner form had closed as inert. The read planner modeled three saved reads with zero group overread, making this the last plausible early-mid scalar table proof before treating the pocket as generated-owner-only. | Temporarily inserted `{129,153,0}`, then `{129,153,8}`, then `{129,153,32}` into `kWalkstuf1LowReadGroups12[]`, rebuilding through the focused W1-low gate each time. Artifacts: raw `scratch/ps1-perf-iterate/walkstuf1-low-rg129-153-current/20260520-142144-2203022/summary.json`, slack8 `scratch/ps1-perf-iterate/walkstuf1-low-rg129-153-slack8-current/20260520-142333-2213530/summary.json`, and slack32 `scratch/ps1-perf-iterate/walkstuf1-low-rg129-153-slack32-current/20260520-142525-2224355/summary.json`. | Failed/no promotion. Raw and slack8 fired and reduced loop reads `24 -> 21`, but regressed W1-low `1809/1470/1446 -> 1811/1472/1445`, overrun `24 -> 27`, blocking `33 -> 35`, and hidden refill `5 -> 8`. Slack32 avoided the bad fire and stayed exact-flat/inert at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. | Close W1-low `{129..153}` as a static retained-read row and coarse guard. The firing threshold converts modeled read savings into target/refill/visible debt; the safe threshold skips the work. The early-mid W1-low pocket now needs generated per-frame deadline/refill ownership or a preceding render/pack reduction before another retry. |
| 2026-05-20 | `building2-high-setup-alias38-current` | `ps1: alias building2 high duplicate entry 38` | Continue the B2-high duplicate-payload relocation lane after backward hot aliases and the entry33 setup-edge probe proved phase-bearing. Entry `38` duplicates setup-edge entry `35`, while entry `41` also duplicates the same payload, so the proof first checked the tempting pair and then the smaller single-entry move. | Moved entry `38` to payload offset `73149` (`2743` bytes) with fixed file size/LBA and no runtime source change; transform summary: `scratch/building2-high-setup-alias38-current/summary.json`. Focused proof: `scratch/ps1-perf-iterate/building2-high-setup-alias38-current/20260520-144112-2313137/summary.json`. Five-yellow canary: `scratch/ps1-perf-iterate/building2-high-setup-alias38-canary/20260520-144415-2330463/summary.json`. The rejected pair probe moved entries `38` and `41`; artifact: `scratch/ps1-perf-iterate/building2-high-setup-alias38-41-current/20260520-143929-2303111/summary.json`. | Promoted the single entry38 alias as layout-neutral data ownership cleanup, not a VBlank speed win. Focused B2-high stays exact-flat at `1621/1347/1313`, overrun `34`, blocking/refill `39/16`, reads/due `40/5`, pack LBA `6189`, and PS-EXE bucket `233472`; VISITOR3 high/low and WALKSTUF1 high/low stay exact-flat in the canary. The pair alias is closed: it regressed B2-high to `1630/1357/1311`, overrun `46`, blocking/refill `52/19`, reads `42`, and due `6`. | Keep entry38 aliased and do not retry the `38`/`41` pair without generated deadline/refill ownership. Public/raw rollups and bands remain unchanged: `+0.2387%` / `99.7644%` public, about `-0.4782%` / `100.4948%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.16` public over-target points removed and `+12.66` public target-speed points gained. |
| 2026-05-20 | `visitor3-high-freshgroup40-52-current` | `docs: close visitor3 high fresh group probe` | Test the missing VISITOR3 high mechanism after hand-authored append groups for `40..52`/`40..56` were exact-flat: make the early direct-stage tiny-read cluster owned by a fresh grouped window so sector `40` can cover the tight `40..48` payload run. | Added a temporary high-tide VISITOR3 `{40,52}` group, bypassed direct-stage when the group covered multiple payloads, and capped grouped fresh fills to the group end. Focused artifact: `scratch/ps1-perf-iterate/visitor3-high-freshgroup40-52-current/20260520-150555-2453011/summary.json`. | Rejected and reverted. The probe saved one loop read (`7 -> 6`) but regressed high from `1388/1071/1045` to `1398/1082/1043`, overrun `26 -> 39`, blocking `35 -> 47`, and refill `1 -> 14`; it also shifted VISITOR3 LBA `22619 -> 22620` and crossed the PS-EXE bucket `233472 -> 235520`. | Close the VISITOR3 high early `40..52` fresh/direct-group path as scalar runtime ownership. Reopen only with generated deadline/refill-budget scheduling, code-size-neutral cold metadata, or pack/render data-shape reduction that avoids moving the cost into visible blocking/refill debt. |

| 2026-05-20 | `visitor3-high-setupseg42-49-current` / `visitor3-high-setupseg42-49-canary` | `ps1: slide visitor3 high setup edge` | Follow up the promoted `40..47` VISITOR3-high retained setup edge with a same-footprint slide. The goal was not a new VBlank speed win; it was to reduce the remaining active-loop CD pressure without touching timing or pack layout. | Changed `FG_VISITOR3_HIGH_SETUP_SEGMENT3_START` from relative sector `40` to `42` while keeping the `7`-sector retained span and the merged `203..262` terminal setup coverage. Focused proof: `scratch/ps1-perf-iterate/visitor3-high-setupseg42-49-current/20260520-170807-3150282/summary.json`. Five-yellow canary: `scratch/ps1-perf-iterate/visitor3-high-setupseg42-49-canary/20260520-171420-3186250/summary.json`. Rejected neighbors: `41..48` stayed timing-flat but did not reduce reads, and `43..50` regressed to `1398/1077/1042`, overrun `35`, blocking `44`, refill `10`, and reads `9`. | Promoted as same-speed CD-pressure work. VISITOR3 high stays `1391/1070/1046`, overrun `24`, blocking `35`, hidden refill `0`, and due `2`, while loop reads/read time improve `5/61 -> 4/59`, hidden CD VBlanks improve `26 -> 24`, and total reads drop `21 -> 20`. VISITOR3 low, BUILDING2 high, and WALKSTUF1 high/low stay exact-flat in the canary. | Keep retained high setup coverage `42..49`, `203..262`, and `277..293` as the new VISITOR3-high CD-pressure baseline. Public/raw rollups and bands stay unchanged at `+0.2371%` / `99.7659%` public, about `-0.4798%` / `100.4962%` raw, and `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.16` public over-target points removed and `+12.67` public target-speed points gained. |

| 2026-05-20 | `visitor3-high-setupseg203-262-edge40-47-current` | `ps1: retain visitor3 high setup edge 40` | Merge the overlapping VISITOR3 high terminal setup coverage and spend the spare allocator-safe setup budget on the early no-append-start `40..47` edge after the fresh/direct-group path regressed cadence. | Widened `FG_VISITOR3_HIGH_SETUP_SEGMENT2` from `203..229` to retained `203..262`, retargeted the third retained setup segment from `228..262` to `40..47`, rebuilt in the same `233472` byte PS-EXE bucket, ran focused proof at `scratch/ps1-perf-iterate/visitor3-high-setupseg203-262-edge40-47-current/20260520-152408-2558154/summary.json`, and ran the five-yellow canary at `scratch/ps1-perf-iterate/visitor3-high-setupseg203-262-edge40-47-canary/20260520-152544-2567537/summary.json`. Rejected probes: `36..48` structural allocator failure at `req=178176 have=168956`; `40..46` passed but was inferior at `1071/1044`, overrun `27`, reads `4`. | Promoted as a VBlank speed win. VISITOR3 high improves `1388/1071/1045 -> 1391/1070/1046`, overrun `26 -> 24`, hidden refill `1 -> 0`, and loop reads `7 -> 5`; blocking stays `35`, due stays `2`, VISITOR3 LBA stays `22619`, and the PS-EXE bucket stays `233472`. VISITOR3 low, BUILDING2 high, and WALKSTUF1 high/low stay exact-flat in the canary. | Keep retained setup coverage `40..47`, `203..262`, and `277..293` as the new VISITOR3-high baseline. Public/raw rollups improve to `+0.2371%` / `99.7659%` public and about `-0.4798%` / `100.4962%` raw; bands remain `121` green / `5` yellow / `0` orange / `0` red. Methodology total is about `17.16` public over-target points removed and `+12.67` public target-speed points gained. |

| 2026-05-20 | `building2-high-frameorder-pack-current` / `building2-high-frameorder-rg257-273-current` / `building2-high-frameorder-preserve-setup-current` / `building2-high-tailorder-preserve119-current` | Log-only; pack/source restored and PS1 rebuilt. | Test a no-decode B2-high pack-shape swing after static read rows and setup residency closed. The goal was to reorder physical payload bodies into playback order while preserving payload bytes, sound tail, file size, pack LBA, and pixel output, then see whether the late-cluster refill savings could move the worst yellow row. | Generated three temporary `BUILDING2.FG2` layouts: full playback-order repack (`242` offsets moved, sound tail stable), full repack plus a temporary `{257..273}` owner row exposed by the new read plan, setup-resident-offset-preserving repack (`49` setup entries preserved), and tail-only repack preserving the original prefix through entry `119`. Artifacts: full repack `scratch/ps1-perf-iterate/building2-high-frameorder-pack-current/20260520-155407-2728008/summary.json`, row assist `scratch/ps1-perf-iterate/building2-high-frameorder-rg257-273-current/20260520-155550-2737798/summary.json`, setup-preserving `scratch/ps1-perf-iterate/building2-high-frameorder-preserve-setup-current/20260520-155811-2751558/summary.json`, and tail-only `scratch/ps1-perf-iterate/building2-high-tailorder-preserve119-current/20260520-160008-2762754/summary.json`. | Failed/no promotion. Full repack improved target/refill but regressed blocking/due: `1621/1347/1313 -> 1621/1347/1314`, overrun `34 -> 33`, refill `16 -> 12`, blocking `39 -> 41`, due `5 -> 7`, reads `40 -> 43`. The `{257..273}` assist reduced reads but regressed loop/blocking to `1627/1353/1315`, overrun `38`, blocking `50`. Setup-preserving still regressed `1623/1349/1314`, overrun `35`, blocking `44`. Tail-only was worse at `1626/1353/1312`, overrun `41`, blocking/refill `49/18`. | Close B2-high playback-order/no-decode repacking as a current-baseline big swing. The late-cluster refill signal is real, but the physical relocation moves cost into visible blocking, due misses, or target shrink. Future B2-high no-decode relocation needs generated per-frame deadline/refill ownership or a setup-resident/forward-order proof narrower than whole-tail repacking. |
| 2026-05-20 | `visitor3-low-frameorder-preserve-setup-current` | Log-only; pack restored and PS1 rebuilt. | Test the same no-decode physical-order swing on VISITOR3 low while preserving the accepted retained setup windows `150..177`, `206..232`, and `281..305`. The low pack has enough slack to copy all active payload bytes, so the binary question was whether moving only non-setup-resident payloads into frame order could reduce visible due stalls without changing pixels, file size, LBA, or the sound table. | Generated `VIST3LOW.frameorder-preserve-setup.FG2` with all setup-overlapping entries pinned (`22` entries) and non-pinned payloads copied in frame order outside the retained windows. The transform moved `9` active entries, kept the `1555450` byte footprint and sound table tail unchanged, then ran focused VISITOR3 low at `scratch/ps1-perf-iterate/visitor3-low-frameorder-preserve-setup-current/20260520-160607-2796735/summary.json`. | Failed/no promotion. The pack shape regressed VISITOR3 low from `1369/1065/1039` to `1374/1070/1039`, overrun `26 -> 31`, blocking `75 -> 81`, loop reads `18 -> 19`, and due `14 -> 15`. | Close setup-preserving whole-pack frame-order relocation for VISITOR3 low. The accepted low pack is already mostly ordered outside the retained windows; the few remaining moves perturb the deadline boundary in the wrong direction. Reopen low no-decode work only for a targeted boundary trim or generated deadline owner, not another whole-pack reorder. |
| 2026-05-20 | `visitor3-low-relief-window18-current` / `visitor3-low-relief-window20-current` / `visitor3-low-relief-window24-current` / `visitor3-low-relief-window32-current` | Log-only; source restored and PS1 rebuilt. | Sweep VISITOR3 low clean-relief stream-window size after static append rows closed. Low tide was still using the default `16 KiB` relief window while high tide has a larger scene-specific window, so the test checked whether `18`, `20`, `24`, or `32 KiB` could cut active CD stalls enough to move the remaining yellow row. | Temporarily changed the low-tide `fgSceneCleanReliefWindowBytes("visitor3")` return from `16 KiB` to `32`, `24`, `20`, and `18 KiB`, rebuilding each in the same `233472` PS-EXE bucket. Artifacts: `32 KiB` `scratch/ps1-perf-iterate/visitor3-low-relief-window32-current/20260520-160838-2811190/summary.json`, `24 KiB` `scratch/ps1-perf-iterate/visitor3-low-relief-window24-current/20260520-161020-2821120/summary.json`, `20 KiB` `scratch/ps1-perf-iterate/visitor3-low-relief-window20-current/20260520-161208-2831632/summary.json`, and `18 KiB` `scratch/ps1-perf-iterate/visitor3-low-relief-window18-current/20260520-161349-2841406/summary.json`. | Failed/no promotion. `32 KiB` reduced loop reads `18 -> 11` and blocking `75 -> 73`, but regressed loop/overrun to `1083/42`. `24 KiB` was closest, cutting blocking to `64` and reads to `14`, but still regressed `1065/1039` to `1069/1041`, overrun `26 -> 28`. `20 KiB` regressed blocking and loop (`1076`, blocking `76`), while `18 KiB` still regressed loop/overrun (`1072`, overrun `30`) for only a one-VBlank blocking reduction. | Keep VISITOR3 low at the default `16 KiB` clean-relief window. Larger relief windows can remove reads, but they shift cost into target cadence/hidden work before they cross the 99% threshold. Future low-tide work needs generated per-frame ownership or pack/render byte reduction that creates slack before trying larger windows again. |
| 2026-05-20 | `walkstuf1-low-setupseg141-153-current` / `walkstuf1-low-setupseg141-147-current` / `walkstuf1-low-setupseg147-153-current` | Log-only; source restored and PS1 rebuilt. | Test the unclosed W1-low no-append-start pocket at `141..153` as retained setup instead of another hot-path append. The accepted low secondary setup slice is `179..185`; this swept a full 12-sector retarget and two 6-sector same-footprint halves to separate memory feasibility from phase value. | Temporarily retargeted `FG_WALKSTUF1_LOW_SETUP_SEGMENT2` from `179..185` to `141..153`, then `141..147`, then `147..153`. Artifacts: full `141..153` `scratch/ps1-perf-iterate/walkstuf1-low-setupseg141-153-current/20260520-161742-2863716/summary.json`, `141..147` `scratch/ps1-perf-iterate/walkstuf1-low-setupseg141-147-current/20260520-162019-2878619/summary.json`, and `147..153` `scratch/ps1-perf-iterate/walkstuf1-low-setupseg147-153-current/20260520-162211-2889536/summary.json`. | Failed/no promotion. The 12-sector form structurally failed before `JCPERF2` with `JCBSOD-FATAL CACHE exhausted` during clean-rect allocation. Both 6-sector halves completed and improved scene/loop `1809/1470 -> 1807/1468`, but target dropped `1446 -> 1441` and strict pressure regressed: overrun `24 -> 27`, blocking `33 -> 38`, refill `5 -> 9`, reads `24 -> 31`, and due `4 -> 5`. | Keep the accepted `179..185` low secondary retained slice. The `141..153` pocket has a real visible-loop signal, but paying it by stealing the existing `179..185` residency loses target/refill cadence; paying all 12 sectors exceeds the current clean/CACHE shape. Reopen only with generated refill-budget ownership, additive residency after clean-byte reduction, or a pack/data-shape change that preserves `179..185`. |
| 2026-05-20 | `walkstuf1-low-setupseg179-185-plus141-147-current` | Log-only; source restored and PS1 rebuilt. | Test the additive version of the W1-low `141..147` signal after the same-footprint swaps proved that displacing `179..185` is too expensive. This temporarily kept the accepted `179..185` split segment and added `141..147` as a third retained setup segment using the existing setup-segment runtime plumbing. | Added a temporary `FG_WALKSTUF1_LOW_SETUP_SEGMENT3` for `141..147` and split the low-tide owned TRANSIENT setup buffer between segment2 and segment3. Focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-setupseg179-185-plus141-147-current/20260520-162634-2914403/summary.json`. | Failed/no promotion as structural. The run halted before `JCPERF2` with `JCBSOD-FATAL CACHE exhausted (req=48640 have=67240)` during the third clean-rect chunk after `fgSetupSegmentBuffer=217088`; `memCacheUsed=588120`, `memTransientUsed=740816`, and the PS-EXE bucket stayed `233472`. | Close additive W1-low `141..147` residency until clean-rect/CACHE pressure is reduced first. The setup bytes themselves are TRANSIENT-backed, but the added live scene shape still reaches the existing CACHE clean-rect cliff; generated deadline ownership or pack-byte reduction remains the next viable route. |
| 2026-05-20 | `walkstuf1-low-clean40-current` / `walkstuf1-low-clean40-plus141-147-current` | Log-only; source restored and PS1 rebuilt. | Check whether reducing W1-low clean-rect chunks from `48 KiB` to `40 KiB` creates enough allocator headroom to promote the additive `141..147` retained setup signal without displacing the accepted `179..185` slice. | First changed only low-tide `gFgCleanRectMaxBytes` to `40 KiB`; artifact `scratch/ps1-perf-iterate/walkstuf1-low-clean40-current/20260520-163247-2949792/summary.json`. Then kept clean40 and added temporary `141..147` as a third setup slice; artifact `scratch/ps1-perf-iterate/walkstuf1-low-clean40-plus141-147-current/20260520-163456-2962177/summary.json`. | Failed/no promotion. Clean40 alone was exact-flat at `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`. The additive form still failed structurally before `JCPERF2`: after three `40 KiB` CACHE clean chunks, the fourth `30 KiB` clean chunk hit `JCBSOD-FATAL CACHE exhausted (req=30720 have=41636)`, with `memCacheUsed=613724` and `memTransientUsed=766416`. | Close scalar clean-rect chunk-size reduction as the additive W1-low setup unlock. Smaller chunks preserve baseline timing but do not create usable CACHE/TRANSIENT headroom once the extra retained slice is live; W1-low `141..153` now needs generated refill ownership or a real pack/render-byte reduction before residency is retried. |
| 2026-05-20 | `visitor3-low-align135-136-current` / `visitor3-low-align134-136-current` | Log-only; pack restored and PS1 rebuilt. | Test the targeted no-decode boundary-alignment form for the tight VISITOR3-low `248..272` terminal cluster. Frames `135` and `136` start mid-sector and create repeated one-sector plus aligned reads; the probe moved the same payload bytes forward to sector-aligned offsets inside the existing post-frame136 gap while preserving file size, LBA, payload bytes, and runtime code. | Variant A moved frames `135` and `136` to sector starts `249` and `258`; transform summary `scratch/visitor3-low-align135-136-current/summary.json`, focused artifact `scratch/ps1-perf-iterate/visitor3-low-align135-136-current/20260520-164323-3009468/summary.json`. Variant B also moved frame `134` to sector start `241` and placed `135`/`136` at `250`/`259`; transform summary `scratch/visitor3-low-align134-136-current/summary.json`, focused artifact `scratch/ps1-perf-iterate/visitor3-low-align134-136-current/20260520-164526-3021364/summary.json`. | Failed/no promotion. The `135`/`136` alignment reduced modeled uncovered sectors `142 -> 141` but stayed exact-flat at VISITOR3 low `1369/1065/1039`, overrun `26`, blocking `75`, reads/due `18/14`. The wider `134..136` alignment regressed to `1370/1066/1039`, overrun `27`, blocking `76`, with reads/due still `18/14`. | Close simple no-decode terminal sector alignment for VISITOR3 low. Aligning the raw payload starts changes host read shape but not the due-read cadence enough to move speed, and widening the alignment shifts phase negative. The next V3-low lane should be custom row-reference/generated deadline ownership, not another byte-for-byte terminal boundary move. |
| 2026-05-20 | `building2-high-group-slack6-current` | Log-only; source restored and PS1 rebuilt. | Test a broad B2-high refill-budget owner gate by raising every existing B2-high read-group minimum slack to `6`. The question was whether the accepted grouped rows were stealing hidden refill cadence and could be made safer without losing their banked CD-pressure wins. | Temporarily changed `kBuilding2HighReadGroups12[]` so `{60..72}`, `{206..230}`, `{226..242}`, `{83..95}`, `{158..174}`, `{185..197}`, `{271..287}`, and `{315..327}` all required at least six slack VBlanks, then ran focused B2-high against the five-yellow baseline. Artifact: `scratch/ps1-perf-iterate/building2-high-group-slack6-current/20260520-165317-3065819/summary.json`. | Failed/no promotion. B2-high stayed at `1621/1347/1313`, overrun `34`, but the gate made the row worse: blocking regressed `39 -> 41`, loop reads `40 -> 42`, and due misses `5 -> 6`, while hidden refill stayed `16`. No key speed or work-volume metric improved. | Close scalar all-current-group slack gating for B2-high. The accepted rows are not the remaining refill owner by themselves; the coarse guard starves useful groups without reducing the hidden overrun. Future B2-high refill work needs frame/deadline-specific generated ownership or a preceding data-shape/render-byte reduction. |
| 2026-05-20 | `building2-high-rg145-161-current` | Log-only; source restored and PS1 rebuilt. | Test the remaining low-risk B2-high current read-plan candidate around the dense `145..161` active cluster. The planner modeled two saved reads with a large visible safety score, making this the next scalar proof after broad slack gating failed. | Temporarily inserted `{145,161,0}` into `kBuilding2HighReadGroups12[]`, rebuilt in the same PS-EXE bucket, and ran a focused B2-high gate. Artifact: `scratch/ps1-perf-iterate/building2-high-rg145-161-current/20260520-170101-3109673/summary.json`. | Failed/no promotion. The row regressed B2-high from `1621/1347/1313` to `1626/1353/1311`: overrun `34 -> 42`, blocking `39 -> 45`, hidden refill `16 -> 21`, while loop reads stayed `40` and due misses stayed `5`. | Close B2-high `{145..161}` as a static grouped-read row. The modeled saved-read opportunity is phase-negative in the live scheduler and shifts cost into both visible blocking and hidden refill. Future work in this cluster needs generated per-frame ownership or a data-shape/render reduction before retrying nearby spans. |
| 2026-05-20 | `building2-high-owned-append-current` | Log-only; source restored and PS1 rebuilt. | Test a bigger B2-high generated-owner shape instead of another static row: schedule append groups only from prepared-frame idle slack, with no immediate payload read pending, so CD work cannot fire through the visible due/window-fill path. | Temporarily added B2-high owner rows `{95..119}`, `{122..146}`, `{249..265}`, `{255..271}`, and `{287..311}` and a prepared-frame-idle scheduler hook that called `fgRuntimeTryExtendWindow()` only when `gFgRuntime.preparedFrameValid` was already true. Focused artifact: `scratch/ps1-perf-iterate/building2-high-owned-append-current/20260520-173726-3315756/summary.json`. | Failed/no promotion. The probe regressed B2-high from `1621/1347/1313` to `1625/1352/1309`, overrun `34 -> 43`, blocking `39 -> 45`, hidden refill `16 -> 20`, with reads still `40`. It also shifted `BUILDING2.FG2` LBA `6189 -> 6190` and crossed the PS-EXE sector bucket `233472 -> 235520`. | Close prepared-frame-idle runtime ownership for B2-high. Even hidden-only owner rows perturb target cadence unless the owner is frame-index/deadline/refill-budget generated outside the hot scheduler or a pack/render reduction creates slack first. |
| 2026-05-20 | `building2-high-trim-entry89-current` | Log-only; pack restored and PS1 rebuilt. | Test the isolated B2-high preserve-offset draw-tail trim for entry `89` / source frame `107` after broader/local-LZ and entry91 forms failed. The transform removes `4510` active payload bytes and collapses entry89 from `5 -> 3` sectors while preserving pack size, pack LBA, runtime source, and the `233472` byte PS-EXE bucket. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --pad-to-input-size --frames 89`. Transform summary: `scratch/b2-entry89-trim-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-trim-entry89-current/20260520-174210-3342501/summary.json`. The pack was restored from `scratch/b2-entry89-trim-current/BUILDING2.FG2.before`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. B2-high regressed from `1621/1347/1313` to `1622/1349/1312`, overrun `34 -> 37`, blocking `39 -> 42`, hidden refill stayed `16`, loop reads improved only `40 -> 39`, and due stayed `5`. | Close B2-high entry89/source frame107 as a standalone preserve-offset trim on this baseline. The saved read is not enough to pay for the cadence shift; entry90 remains the only untested isolated member of the current `89..91` sector-collapse split. |
| 2026-05-20 | `building2-high-trim-entry90-current` | Log-only; pack restored and PS1 rebuilt. | Test the remaining isolated B2-high preserve-offset draw-tail trim in the `89..91` sector-collapse split. Entry `90` / source frame `109` removes `4080` active payload bytes and collapses from `5 -> 3` sectors while preserving file size, pack LBA, runtime source, and the `233472` byte PS-EXE bucket. | Temporarily rewrote `generated/ps1/foreground/BUILDING2.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --pad-to-input-size --frames 90`. Transform summary: `scratch/b2-entry90-trim-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-trim-entry90-current/20260520-174621-3366150/summary.json`. The pack was restored from `scratch/b2-entry90-trim-current/BUILDING2.FG2.before`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. B2-high matched the entry89 failure profile: `1621/1347/1313 -> 1622/1349/1312`, overrun `34 -> 37`, blocking `39 -> 42`, hidden refill stayed `16`, loop reads improved only `40 -> 39`, and due stayed `5`. | Close entry90/source frame109 as a standalone preserve-offset trim. The current `89..91` draw-tail sector-collapse split is now fully closed under strict B2-high gates; future B2-high data-shape work needs generated deadline/refill ownership or non-CD render/upload reduction first. |
| 2026-05-20 | `visitor3-low-rg86-98-current` | Log-only; source restored and PS1 rebuilt. | Test the only low-risk VISITOR3-low current read-plan candidate after the B2 trim lane closed. The host read plan ranked `86..98` as a current-window-compatible `scheduler-owned-candidate` with one expected saved read, long internal slack, and fixed pack layout. | Temporarily added low-tide VISITOR3 read group `{86,98,0}` to `src/foreground_pilot.c`, rebuilt in the same `233472` byte PS-EXE bucket, and ran focused VISITOR3 low. Read-plan snapshot: `scratch/ps1-perf-iterate/v3-low-current-read-plan-after-trims.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-low-rg86-98-current/20260520-175126-3395181/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The row saved one loop read (`18 -> 17`) but regressed VISITOR3 low from `1369/1065/1039` to `1374/1070/1041`, overrun `26 -> 29`, blocking `75 -> 79`, and due stayed `14`. The source-only probe also shifted hot foreground symbols by `+60` bytes while keeping pack LBA fixed. | Close VISITOR3-low `{86..98}` as a hand-authored read group. The saved read is real but not cadence-safe; low-tide VISITOR3 still needs frame/deadline/refill ownership or a pack/render reduction before grouped reads can be promoted. |
| 2026-05-20 | `walkstuf1-high-d4-181-187-current` | Log-only; pack/source restored and PS1 rebuilt. | Test a previous-frame D4 data-shape swing for WALKSTUF1 high after static read groups and payload trims exhausted. Frames `181`, `183`, `185`, and `187` each delta-compress against the prior frame without changing pixels, pack size, pack LBA, or the PS-EXE bucket. | Temporarily encoded the four frames with `compact-fgp3-delta-previous.py` and added those frame indices to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-high-d4-181-187-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-d4-181-187-current/20260520-180155-3453957/summary.json`. The pack was restored from `scratch/walkstuf1-high-d4-181-187-current/WALKSTUF1.before.FG2`, source was restored, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Active payload dropped by `11465` bytes (`181: 4708 -> 1484`, `183: 4602 -> 2251`, `185: 4483 -> 2626`, `187: 4764 -> 731`), but W1-high regressed `1808/1472/1441 -> 1809/1473/1441`, overrun `31 -> 32`, blocking `43 -> 56`, loop reads `41 -> 42`, while hidden refill improved only `13 -> 10`. | Close W1-high frames `181/183/185/187` as standalone D4 deltas. The payload savings are real, but the delta decode/read shape steals visible CD cadence; future W1-high D4 work needs a deadline-owned read plan or a single-frame canary before combining multiple frames. |
| 2026-05-20 | `walkstuf1-high-d4-frame187-current` | Log-only; pack/source restored and PS1 rebuilt. | Split the rejected W1-high four-frame D4 cluster and test the largest byte-signal frame by itself. Frame `187` delta-compresses against frame `186`, preserving pixels, pack size, pack LBA, and the PS-EXE bucket. | Temporarily encoded only frame `187` with `compact-fgp3-delta-previous.py` and added frame `187` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-high-d4-frame187-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-d4-frame187-current/20260520-180605-3477710/summary.json`. The pack was restored from `scratch/walkstuf1-high-d4-frame187-current/WALKSTUF1.before.FG2`, source was restored, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Frame `187` shrank `4764 -> 731` bytes, saving `4033` active bytes, but W1-high stayed exact-flat at `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, loop reads `41`, and due `7`; `--require-improvement` correctly rejected the run because no key speed or work-volume metric improved. | Close single frame187 as an isolated W1-high D4 speed path. It avoids the combined-cluster cadence regression but is inert, so the remaining binary split should try frames `181`, `183`, and `185` independently before any wider D4 combination is reconsidered. |
| 2026-05-20 | `walkstuf1-high-d4-frame181-current` | Log-only; pack/source restored and PS1 rebuilt. | Continue the W1-high D4 binary split with the next-largest byte-signal frame. Frame `181` delta-compresses against frame `180`, preserving pixels, pack size, pack LBA, and the PS-EXE bucket. | Temporarily encoded only frame `181` with `compact-fgp3-delta-previous.py` and added frame `181` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-high-d4-frame181-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-d4-frame181-current/20260520-181012-3501246/summary.json`. The pack was restored from `scratch/walkstuf1-high-d4-frame181-current/WALKSTUF1.before.FG2`, source was restored, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Frame `181` shrank `4708 -> 1484` bytes, saving `3224` active bytes, and improved scene/loop/overrun/refill `1808/1472/31/13 -> 1807/1471/29/9`, but it regressed blocking `43 -> 54`, loop reads `41 -> 43`, and due misses `7 -> 10`. | Close single frame181 as cadence-negative. The speed-side signal is not usable because the D4/read shape moves too much work onto visible blocking; continue the split with frames `183` and `185` independently. |
| 2026-05-20 | `walkstuf1-high-d4-frame183-current` | Log-only; pack/source restored and PS1 rebuilt. | Continue the W1-high D4 binary split with frame `183`, the next remaining payload shrink after frames `187` and `181` closed. | Temporarily encoded only frame `183` with `compact-fgp3-delta-previous.py` and added frame `183` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-high-d4-frame183-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-d4-frame183-current/20260520-181306-3518065/summary.json`. The pack was restored from `scratch/walkstuf1-high-d4-frame183-current/WALKSTUF1.before.FG2`, source was restored, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Frame `183` shrank `4602 -> 2251` bytes, saving `2351` active bytes, but regressed W1-high `1808/1472/1441 -> 1813/1477/1441`, overrun `31 -> 36`, blocking `43 -> 47`, loop reads `41 -> 42`, and due `7 -> 8`, while hidden refill improved only `13 -> 11`. | Close single frame183 as cadence-negative. It confirms the D4 byte win is not enough without deadline-owned reads; only frame `185` remains in this split. |
| 2026-05-20 | `walkstuf1-high-d4-frame185-current` | Log-only; pack/source restored and PS1 rebuilt. | Finish the W1-high D4 binary split with frame `185`, the last single-frame member of the rejected `181/183/185/187` cluster. | Temporarily encoded only frame `185` with `compact-fgp3-delta-previous.py` and added frame `185` to `fgRuntimeUsesPreviousFrameDelta()`. Transform summary: `scratch/walkstuf1-high-d4-frame185-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-d4-frame185-current/20260520-181557-3534550/summary.json`. The pack was restored from `scratch/walkstuf1-high-d4-frame185-current/WALKSTUF1.before.FG2`, source was restored, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Frame `185` shrank `4483 -> 2626` bytes, saving `1857` active bytes, but regressed W1-high `1808/1472/1441 -> 1811/1475/1441`, overrun `31 -> 34`, blocking `43 -> 45`, loop reads `41 -> 42`, and due `7 -> 8`, while hidden refill improved only `13 -> 11`. | Close the full W1-high single-frame D4 split for frames `181/183/185/187`. The byte savings either stay inert or move cost into visible blocking; the next W1-high big swing should be no-decode payload shrink, clean/upload/restore reduction, or generated deadline ownership rather than more hand-added D4 deltas. |
| 2026-05-20 | `walkstuf1-low-trim-entry83-current` | Log-only; pack restored and PS1 rebuilt. | Test the remaining unclosed W1-low preserve-offset trim from the current shape scan. Entry `83` / source frame `145` trims from `4615 -> 3981` bytes without changing sector count (`3 -> 3`), so the question was whether a no-offset, no-sector-collapse work-volume reduction could still move W1-low speed on the accepted allocator-era baseline. | Temporarily rewrote `generated/ps1/foreground/WALK1LOW.FG2` with `compact-fgp3-trim-draw-tails.py --copy-unparseable --preserve-offsets --pad-to-input-size --frames 83`, keeping file size, pack LBA, runtime source, and the PS-EXE bucket fixed. Transform summary: `scratch/walkstuf1-low-trim-entry83-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-low-trim-entry83-current/20260520-182507-3586242/summary.json`. The pack was restored from `scratch/walkstuf1-low-trim-entry83-current/WALK1LOW.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The transform saved `634` active payload bytes, but W1-low stayed exact-flat at scene/loop/target `1809/1470/1446`, overrun `24`, blocking/refill `33/5`, reads/due `24/4`; `--require-improvement` rejected it because no key speed or work-volume metric visible to the gate improved. | Close entry83/source frame145 as a speed path on this baseline. It is harmless but inert; remaining W1-low payload-only work should be batched only if we need byte-surface cleanup, not counted as a route to the last under-99 rows. Next speed work should target frame/deadline ownership or render/cleanup reductions that affect blocking/refill directly. |
| 2026-05-20 | `cd-sequential-setloc-skip-b2-high-current` | Log-only; source restored and PS1 rebuilt. | Try a CD-level big swing after static/generated read rows exhausted: skip redundant `CdlSetloc` calls for aligned stream-window reads when the next requested LBA exactly matches the end of the previous streaming read. The theory was that BUILDING2 high's many sequential foreground reads could save blocking time without changing pack bytes or pixels. | Temporarily added a sequential-read LBA tracker in `cdrom_ps1.c`, invalidated it around non-stream CD operations, and allowed the skip only in `ps1_streamReadAlignedFromCdFileInto()`. Rebuilt in the same visible PS-EXE size class and ran focused B2-high. Artifact: `scratch/ps1-perf-iterate/cd-sequential-setloc-skip-b2-high-current/20260520-183448-3640581/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. B2-high regressed catastrophically from `1621/1347/1313` to `2222/1952/1286`, overrun `34 -> 666`, blocking `39 -> 156`, due misses `5 -> 33`, and work identity collapsed (`restore_calls 335 -> 80`, `compose_calls 335 -> 76`). | Close sequential `CdlSetloc` skipping for foreground streaming. The PS1 CD state is not safely resumable across these foreground windows even when host-sector ranges look contiguous; retry only with explicit hardware-proven async/state ownership plus frame hash validation, not by eliding Setloc from synchronous reads. |
| 2026-05-20 | `building2-high-alias-entry33-current` / `building2-high-alias-entry33-rg64-76-current` | Log-only; pack/source restored and PS1 rebuilt. | Test a B2-high no-decode setup-edge alias found by the current duplicate scan. Entry `33` / source frame `40` is byte-identical to setup-resident entry `30` / source frame `37`; aliasing it into the retained `3..35` setup edge should remove one active payload owner without changing pixels, file size, pack LBA, or the PS-EXE bucket. The follow-up paired the alias with the newly exposed `{64..76}` read group to see whether the saved-read recovery could absorb the phase shift. | Temporarily moved entry `33`'s data offset to entry `30`'s existing payload at `64064`, preserving the `2187` byte payload and pack footprint. Focused artifact: `scratch/ps1-perf-iterate/building2-high-alias-entry33-current/20260520-184713-3710475/summary.json`; transform summary: `scratch/building2-high-alias-entry33-current/summary.json`. Then added temporary B2-high `{64,76,0}` and reran the focused gate. Paired artifact: `scratch/ps1-perf-iterate/building2-high-alias-entry33-rg64-76-current/20260520-184905-3721336/summary.json`. The pack and source row were restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The alias alone reduced uncovered sectors/entries `262/284 -> 261/283` but regressed B2-high from `1621/1347/1313` to `1622/1349/1309`, overrun `34 -> 40`, blocking `39 -> 46`, and hidden refill `16 -> 19`. The paired `{64..76}` row saved one loop read (`40 -> 39`) and partly recovered target/refill, but still failed: `1622/1349/1312`, overrun `37`, blocking `44`, refill `17`. | Close B2-high entry33 setup-edge aliasing and the alias-plus-`64..76` recovery row on this baseline. Even a byte-identical alias can move the early setup edge into worse active-loop phase; retry only if a future pack/layout change preserves the accepted early cadence or if generated deadline ownership can prove the `64..76` recovery fires without refill debt. |
| 2026-05-20 | `building2-high-alias-all-dups-current` | Log-only; pack restored and PS1 rebuilt. | Take the larger version of the B2 duplicate-payload alias idea after the isolated setup-edge alias failed. The pack contains many exact duplicate early payloads; this pass pointed every later byte-identical same-geometry duplicate at the first matching payload, preserving file size, pack LBA, runtime source, and the PS-EXE bucket. | Temporarily moved `58` entry-table offsets, covering about `57.9 KiB` of duplicate payload surface. Transform summary: `scratch/building2-high-alias-all-dups-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/building2-high-alias-all-dups-current/20260520-185356-3748960/summary.json`. The pack was restored from `scratch/building2-high-alias-all-dups-current/BUILDING2.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The broader alias pass changed read topology in the wrong direction: B2-high regressed from `1621/1347/1313` to `1717/1443/1309`, overrun `34 -> 134`, blocking `39 -> 175`, hidden refill `16 -> 24`, loop reads `40 -> 67`, and due misses `5 -> 27`. Correctness remained clean, so the failure is cadence/read ownership, not visual mismatch. | Close blanket B2 duplicate aliasing. The duplicate payloads are not free storage aliases under the current stream-window scheduler; their physical positions are acting as cadence anchors. Future B2 no-decode relocation must be much more selective and deadline-owned, or preceded by render/restore reduction that creates slack before the read topology changes. |
| 2026-05-20 | `prefetch-only-grouped-appends-b2-current` | Log-only; source restored and PS1 rebuilt. | Test a scheduler-owner variant for all existing B2 grouped reads: allow grouped append extension only during hidden prefetch reads, not on visible due-path window fills. This was a broader version of the earlier per-row prefetch-only idea, aimed at preventing accepted CD-pressure groups from overreading on the active path. | Temporarily gated `fgRuntimeGroupedAppendTargetEnd()` inside `fgRuntimeTryExtendWindow()` behind `countAsPrefetch`, rebuilt in the same `233472` byte PS-EXE bucket, and ran focused B2-high. Artifact: `scratch/ps1-perf-iterate/prefetch-only-grouped-appends-b2-current/20260520-185715-3767958/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Visible loop and target stayed flat at `1621/1347/1313`, overrun `34`, and hidden refill `16`, but blocking regressed `39 -> 41`, loop reads/read time regressed `40/189 -> 42/193`, and due misses regressed `5 -> 6`. The helper also grew `fgRuntimeFillWindowForEntry` by `76` bytes and shifted downstream hot symbols. | Close broad prefetch-only grouped append ownership. The current accepted B2 groups are still useful on some due-path windows; removing them there increases blocking/read count before it helps refill. Future owner work needs per-frame/deadline metadata rather than a single prefetch-only policy. |
| 2026-05-20 | `walkstuf1-high-rg372-388-current` / `walkstuf1-high-rg365-389-current` / `walkstuf1-high-rg268-280-current` | Log-only; source restored and PS1 rebuilt. | Test the remaining W1-high current-plan grouped-read swings after the D4 split and B2 data-shape lanes closed. The planner ranked `{372..388}` as a fireable low-visible-risk 16-sector row with two expected saved reads and no overread; `{365..389}` was the adjacent 24-sector three-read variant; `{268..280}` was the smaller mid-pack low-risk control. | Temporarily added each candidate as a W1-high read-group row in `src/foreground_pilot.c`, rebuilt in the same `233472` byte PS-EXE bucket, and ran focused W1-high gates. Artifacts: `scratch/ps1-perf-iterate/walkstuf1-high-rg372-388-current/20260520-190908-3835720/summary.json`, `scratch/ps1-perf-iterate/walkstuf1-high-rg365-389-current/20260520-191107-3847255/summary.json`, and `scratch/ps1-perf-iterate/walkstuf1-high-rg268-280-current/20260520-191305-3858590/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `{372..388}` saved two loop reads (`41 -> 39`) but regressed W1-high from `1808/1472/1441` to `1813/1477/1442`, overrun `31 -> 35`, blocking `43 -> 45`, and refill `13 -> 15`. `{365..389}` saved three loop reads (`41 -> 38`) but still regressed to `1813/1477/1443`, overrun `34`, blocking `46`, refill `16`. `{268..280}` saved one loop read (`41 -> 40`) but regressed harder to `1815/1479/1440`, overrun `39`, blocking `52`, refill `16`, and due `7 -> 9`. | Close W1-high static grouped-read additions in the `268..280` and late `365..389`/`372..388` pockets on this baseline. The saved reads are real but phase-negative; remaining W1-high CD work needs generated deadline/refill ownership, no-hot-C metadata, or a pack/render data-shape win before more hand-authored table rows. |
| 2026-05-20 | `packed4-dirty-singlecolumn-w1-high-current` | Log-only; source restored and PS1 rebuilt. | Test a render-side source-shape swing after W1-high static grouped reads closed: replace the packed PAL4 span compositor's per-row generic `grMarkRectDirty()` calls with the existing single-column dirty helper, aiming to reduce compose-side clipping/tile-boundary work without changing pixels or upload semantics. | Temporarily changed `grCompositePacked4SpansToBackground()` in `src/graphics_ps1.c` to call `grMarkSingleColumnDirty()` for the left/right row dirty ranges, rebuilt, and ran focused W1-high. Artifact: `scratch/ps1-perf-iterate/packed4-dirty-singlecolumn-w1-high-current/20260520-191655-3880570/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The helper swap crossed the PS-EXE bucket (`233472 -> 235520`) and moved the W1-high pack LBA (`24891 -> 24892`). Timing also regressed from `1808/1472/1441` to `1809/1473/1440`, overrun `31 -> 33`, blocking `43 -> 45`, and refill `13 -> 17`; due improved only `7 -> 6`, which is not useful with the layout and timing regressions. | Close direct single-column dirty marking in the packed PAL4 compositor. The inlined helper shape costs more code/layout than it saves; future render-side work needs code-size-neutral direct row marking, pack-side work reduction, or a source-headroom pass before touching this hot path. |
| 2026-05-20 | `upload-band-gap0-walkstuf1-current` / `upload-band-gap0-five-yellow-current` | Promoted as code-headroom; source kept and PS1 rebuilt. | Test a render-side source-headroom retune after the packed PAL4 helper failed: lower `GR_UPLOAD_BAND_MERGE_GAP` from `8` to `0` so dirty upload bands no longer merge across clean rows, simplifying the band scanner while preserving pixel output and upload semantics. | Changed `src/graphics_ps1.c` in `grDrawBackground()` and ran a focused W1 high/low proof followed by the five-yellow canary. Artifacts: `scratch/ps1-perf-iterate/upload-band-gap0-walkstuf1-current/20260520-192341-3919459/summary.json` and `scratch/ps1-perf-iterate/upload-band-gap0-five-yellow-current/20260520-192708-3939015/summary.json`. | Promoted as work/code-headroom, not speed. The five-yellow canary stayed exact-flat on key timing, correctness, scene identity, and pack layout. `grDrawBackground` shrank by `36` bytes, downstream hot symbols shifted by `-36`, the PS-EXE bucket remained `233472`, and foreground pack LBAs stayed fixed. | Keep `GR_UPLOAD_BAND_MERGE_GAP=0` as the new source-headroom baseline. Do not count it as a VBlank speed win; use the smaller hot path to reduce PS-EXE bucket risk for upcoming generated-owner and data-shape code. |
| 2026-05-20 | `walkstuf1-low-owner129-153-*` / `walkstuf1-low-owner160-176-fresh-current` | `ps1: add walkstuf1 low fresh owner window` | Start the reordered generated-owner lane with W1-low fresh window ownership. The broad `129..153` read cluster modeled three saved reads, while the smaller `160..176` cluster modeled one saved read with a long validation gap and less refill risk. | First tried append/due owner hooks for `129..153`; both were exact-flat because they did not own the fresh read. A generic fresh-owner sidecar crossed the PS-EXE bucket and was not run as promotable. The targeted `129..153` fresh owner fired but failed (`blocking 33 -> 31`, `due 4 -> 3`, but target `1446 -> 1444`, overrun `24 -> 26`, refill `5 -> 14`, reads `24 -> 25`). Slack8/slack12 variants were worse (`1472/1445`, blocking `36`, refill `11`). The smaller `160..176` fresh owner then ran focused and five-yellow artifacts: `scratch/ps1-perf-iterate/walkstuf1-low-owner160-176-fresh-current/20260520-200456-4153242/summary.json` and `scratch/ps1-perf-iterate/walkstuf1-low-owner160-176-fresh-five-yellow-current/20260520-200639-4163023/summary.json`. | Promoted as same-speed CD/refill headroom, not a VBlank speed win. W1-low stays `1809/1470/1446`, overrun `24`, loop reads/read time `24/146`, and due `4`, while blocking improves `33 -> 32` and hidden refill improves `5 -> 4`. VISITOR3 high/low, BUILDING2 high, and W1-high stay exact-flat in the five-yellow canary; pack LBAs and the `233472` byte PS-EXE bucket stay fixed. The five-row `--require-improvement` wrapper exits nonzero because unchanged rows have no individual improvement, but the consolidated summaries show no regressions outside W1-low. | Keep the `160..176` fresh owner as the W1-low CD/refill baseline and close broad `129..153` fresh ownership on this baseline. Remaining W1-low direct rows still need generated deadline/refill ownership or a data-shape/render reduction before retrying. Public/raw rollups and bands remain unchanged: `+0.2371%` / `99.7659%` public, about `-0.4798%` / `100.4962%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.67` public target-speed points gained. |
| 2026-05-20 | `building2-high-fresh249-261-current` / `building2-high-owner249-261-current` | Log-only; source restored and PS1 rebuilt. | Retry B2-high with narrower generated ownership after the broad prepared-frame owner failed. The first probe made `249..261` a high-tide, frame-owned fresh-window sidecar; the second moved that same owner into the append-extension path where the read-plan showed the candidate actually fires. | Fresh-window artifact: `scratch/ps1-perf-iterate/building2-high-fresh249-261-current/20260520-202900-94475/summary.json`. It stayed exact-flat because the `249..261` opportunity was not a fresh read. Append-owner artifact: `scratch/ps1-perf-iterate/building2-high-owner249-261-current/20260520-203213-113320/summary.json`. The append version was pointer-gated to `kBuilding2HighReadGroups12`, frames `138..145`, and sectors `249..261`; a source build stayed at `233472`, but the perf-embedded BOOTMODE build crossed `233472 -> 235520`. | Failed/no promotion. Fresh ownership was inert at B2-high `1621/1347/1313`, overrun/blocking/refill `34/39/16`, reads/due `40/5`. Append ownership fired but regressed B2-high to `1625/1352/1309`, overrun `43`, blocking `45`, hidden refill `20`, with loop reads still `40`; it also shifted `BUILDING2.FG2` LBA `6189 -> 6190` under the perf build. | Close narrow B2-high `249..261` generated ownership on this baseline. The model correctly found the append seam, but owning it without deeper deadline/refill metadata still moves cost into visible blocking and hidden refill, and the code shape has no sector-bucket headroom under embedded BOOTMODE. B2-high should move to pack/render work reduction or no-hot-C generated metadata rather than another hot scheduler owner. |
| 2026-05-20 | `walkstuf1-low-local-lz-frame60-current` / `walkstuf1-low-local-lz60-rg204-220-current` | Log-only; pack/source restored and PS1 rebuilt. | Retest the visible-positive W1-low entry60/source frame81 sector-collapse swing under the newer `160..176` fresh-owner baseline, then try the strongest modeled refill recovery row. The local-LZ transform keeps the pack footprint and LBA stable while changing only frame `60` to an existing `L4` payload. | Transform summary: `scratch/walkstuf1-low-local-lz-frame60-current/summary.json` (`4432 -> 943` bytes, `3 -> 1` sectors). Focused local-LZ artifact: `scratch/ps1-perf-iterate/walkstuf1-low-local-lz-frame60-current/20260520-203739-144353/summary.json`. Recovery artifact after adding temporary `{204,220,0}`: `scratch/ps1-perf-iterate/walkstuf1-low-local-lz60-rg204-220-current/20260520-204057-163402/summary.json`. | Failed/no promotion. Both forms produced the same W1-low profile: `1809/1470/1446 -> 1807/1468/1443`, overrun `24 -> 25`, blocking `32 -> 27`, hidden refill `4 -> 13`, loop reads `24 -> 28`, and due misses `4 -> 2`, with fixed pack LBA `25641` and PS-EXE bucket `233472`. | Close W1-low frame60 local-LZ plus static `{204..220}` recovery on this baseline. The visible/blocking signal is real, but the target shrink and refill debt still dominate; the next W1-low retry must be generated deadline/refill ownership or a different render/data-shape reduction, not another static read row around `204..220`. |
| 2026-05-20 | `visitor3-low-seg206-241-current` / `visitor3-low-seg215-241-current` | Log-only; source restored and PS1 rebuilt. | Test whether VISITOR3-low frame133's terminal payload can be paid by retained setup rather than active-loop reads. The first probe extended the accepted third setup segment from `206..232` to `206..241`; the binary follow-up kept the same 26-sector footprint but retargeted it to `215..241`. | Full extension artifact: `scratch/ps1-perf-iterate/visitor3-low-seg206-241-current/20260520-205925-266650/summary.json`; it halted before complete `JCPERF2` with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=176128 have=168956`, after `clean-relief scene=visitor3 clean=524876 maxFrame=17069 no-prefetch`. Memory-neutral retarget artifact: `scratch/ps1-perf-iterate/visitor3-low-seg215-241-current/20260520-210337-290153/summary.json`. | Failed/no promotion. The full `206..241` form is allocator-infeasible on the current clean-relief shape. The `215..241` retarget completed but regressed VISITOR3 low from `1369/1065/1039` to `1371/1067/1038`, overrun `26 -> 29`, blocking `75 -> 79`, loop reads stayed `18`, and due misses stayed `14`; the lost early `206..216` coverage costs more than the terminal read coverage helps. | Keep VISITOR3-low retained setup at accepted `150..177`, `206..232`, and `281..305`. Do not retry retained tail slides for frame133 without a separate allocator/clean-byte reduction and a planner proof. The next V3-low path should be generated deadline/refill ownership or a custom terminal row-reference data shape that uses the existing paid `206..232` dictionary. |
| 2026-05-20 | `building2-high-transient-seg3-122-146-current` / `building2-high-cache-seg3-122-146-transient-current` | Log-only; source restored and PS1 rebuilt. | Retest the closed B2-high `122..146` setup-residency idea using the allocator-era split model. The binary question was whether moving all retained setup to TRANSIENT, or keeping accepted CACHE setup slices while adding only the new early slice in TRANSIENT, could avoid the prior CACHE clean-rect cliff and bank the early read-count signal. | First added `122..146` as a third setup slice with all B2-high setup residency allocated from TRANSIENT. Artifact: `scratch/ps1-perf-iterate/building2-high-transient-seg3-122-146-current/20260520-211034-329516/summary.json`; it failed before complete `JCPERF2` with `JCBSOD-FATAL TRANSIENT region+libc both exhausted: req=196608 have=165016` after `walkPilotInit clean-buf alloc failed`. Then kept the accepted `3..35` and `202..242` slices in CACHE and allocated only the new `122..146` slice from TRANSIENT. Artifact: `scratch/ps1-perf-iterate/building2-high-cache-seg3-122-146-transient-current/20260520-211343-347209/summary.json`. | Failed/no promotion. The split form completed and saved loop reads `40 -> 35`, but regressed B2-high from `1621/1347/1313` to `1636/1350/1312`, overrun `34 -> 38`, blocking `39 -> 44`, prefetch overrun `16 -> 18`, and setup/code cost grew `foregroundPilotPlay` by `72` bytes. | Close B2-high additive `122..146` setup residency in both all-TRANSIENT and split CACHE/TRANSIENT forms. The read-count signal is real, but setup/cadence/code debt still dominates; future B2-high work should use generated deadline/refill ownership or render/pack work reduction before revisiting this early cluster. |
| 2026-05-20 | `visitor3-low-d4-frame133-*` | Log-only; pack/source restored and PS1 rebuilt. | Test the custom-terminal data-shape lane for VISITOR3 low by encoding frame `133`, the current max terminal payload, as a previous-frame D4 against decoded frame `132`. The transform shrank the entry from `17069 -> 14188` bytes and `9 -> 7` sectors, directly targeting the terminal `240..264` read pressure. | Transform summary: `scratch/visitor3-low-d4-frame133-fast-current/summary.json`. First focused artifacts `scratch/ps1-perf-iterate/visitor3-low-d4-frame133-fast-current/20260520-212154-393255/summary.json` and `scratch/ps1-perf-iterate/visitor3-low-d4-frame133-fast-framebuf-current/20260520-212436-408699/summary.json` halted with `trip=1`, `last_frame=132` because staged frame132 remained encoded (`baseSize=768`) and frame133 could not decode against the true previous frame. A temporary staged-decode debug build proved correctness at `scratch/ps1-perf-iterate/visitor3-low-d4-frame133-stageddecode-debug/20260520-213212-452104/summary.json`, then the code-size-neutral binary alternative forced frames `132/133` through the normal on-demand decode path at `scratch/ps1-perf-iterate/visitor3-low-d4-frame133-ondemand132-current/20260520-213436-465790/summary.json`. | Failed/no promotion. The staged-decode debug form passed correctness but crossed the PS-EXE bucket and regressed to `1373/1070/1040`, overrun `30`, blocking `79`, reads `19`. The on-demand form stayed in the `233472` byte bucket and passed correctness, but still regressed VISITOR3 low from `1369/1065/1039` to `1372/1068/1039`, overrun `26 -> 29`, blocking `75 -> 77`, reads `18 -> 19`, and grew `foregroundPilotPlay` by `76` bytes. | Close VISITOR3-low frame133 previous-frame D4 on this baseline. The sector shrink is real, but decode/stage scheduling costs more than the CD read savings; retry only as a no-hot-C/predecoded staged-delta path or a custom terminal row-reference codec that avoids the D4 command-count and scheduler debt. |
| 2026-05-20 | `visitor3-low-d4-frame134-current` | Log-only; pack/source restored and PS1 rebuilt. | Test the next VISITOR3-low terminal D4 split after frame133 closed. Frame `134` encodes against raw frame `133`, so it avoids the frame132 encoded-base staging problem and still saves nearly two sectors. | Temporarily encoded frame `134` with `compact-fgp3-delta-previous.py`, shrinking `17001 -> 14202` bytes with `2799` saved bytes, and forced only frame `134` through on-demand decode to avoid consuming the staged D4 payload encoded. Transform summary: `scratch/visitor3-low-d4-frame134-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/visitor3-low-d4-frame134-current/20260520-213842-489468/summary.json`. | Failed/no promotion. Correctness passed and the pack LBA plus `233472` byte PS-EXE bucket stayed fixed, but VISITOR3 low regressed from `1369/1065/1039` to `1372/1068/1039`, overrun `26 -> 29`, blocking `75 -> 78`, loop reads `18 -> 19`, and `foregroundPilotPlay` grew by `36` bytes. | Close VISITOR3-low frame134 previous-frame D4 as an isolated terminal data-shape. The byte saving is real but decode/scheduler debt still dominates; future VISITOR3 terminal work needs a predecoded/no-hot-C path or a custom row-reference representation before more single-frame D4 retries. |
| 2026-05-20 | `hot-scene-id-five-yellow-current` | `ps1: cache foreground scene id for scheduler` | Take a source-headroom swing after multiple generated-owner and D4/data-shape probes either regressed cadence or grew hot code. The hot scheduler repeatedly compared `gFgRuntime.sceneName` strings inside per-frame window, staging, and held-loop decisions; caching a compact scene ID at scene start should be pixel/timing-neutral while shrinking hot foreground paths. | Added `gFgRuntimeSceneId`, set it in `foregroundPilotRuntimeStart()`, reset it in `fgRuntimeReset()`, and replaced hot `fgSceneEquals(gFgRuntime.sceneName, ...)` checks in scheduler/window helpers with ID comparisons. Built with `./scripts/build-ps1.sh`, then ran the strict five-yellow no-regression gate against `scratch/ps1-perf-iterate/walkstuf1-low-owner160-176-fresh-five-yellow-current/20260520-200639-4163023/summary.json`. Artifact: `scratch/ps1-perf-iterate/hot-scene-id-five-yellow-current/20260520-215337-573156/summary.json`. | Promoted as code-headroom, not a VBlank speed win. B2-high, VISITOR3 high/low, and WALKSTUF1 high/low stayed exact-flat on scene/loop/target, overrun, blocking, hidden refill, reads, due misses, pack LBAs, and the `233472` byte PS-EXE bucket. Hot tracked foreground symbols shrink: `foregroundPilotPlay -84`, `fgRuntimeLoadSceneFrame -52`, `fgRuntimeFillWindowForEntry -24`, and `fgRuntimeTryPrefetchWindow -12`; ELF shrinks by `100` bytes. | Keep the scene-ID cache as the current code-headroom baseline. Public/raw rollups and bands remain unchanged: `+0.2371%` / `99.7659%` public, about `-0.4798%` / `100.4962%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.16` public over-target points removed and `+12.67` public target-speed points gained; use the code-size headroom for generated-owner, pack/render, and custom terminal data-shape swings. |
| 2026-05-20 | `walkstuf1-low-fresh-owner153-177-current` | Log-only; source restored and PS1 rebuilt. | Spend the scene-ID code headroom on the next generated-owner shape: convert the W1-low fresh-owner hook into compact metadata and add `153..177` behind the promoted `160..176` row. The hypothesis was that a fresh-window owner, unlike the closed retained-group form, might preserve the visible loop gain while keeping refill debt bounded. | Temporarily added a `TFgPilotFreshOwner` metadata table with rows for promoted `160..176` and candidate `153..177`, prioritized `160..176` first, built in the unchanged `233472` byte PS-EXE bucket, then ran the focused W1-low no-regression gate against `scratch/ps1-perf-iterate/hot-scene-id-five-yellow-current/20260520-215337-573156/summary.json`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-low-fresh-owner153-177-current/20260520-221445-691111/summary.json`. | Failed/no promotion. The owner fired and improved scene/loop `1809/1470 -> 1807/1468`, but target tightened `1446 -> 1444`, blocking regressed `32 -> 33`, hidden refill regressed `4 -> 8`, and loop reads rose `24 -> 30`; overrun stayed `24` only because loop and target both moved. The PS-EXE bucket and pack LBA stayed fixed, while `fgRuntimeFillWindowForEntry` grew by `40` bytes. | Close W1-low `153..177` fresh metadata ownership on this baseline. The same phase pattern remains: the row can buy loop time only by spending target/refill budget. Rebuild restored the promoted `160..176` hardcoded owner and the accepted `1028316` byte ELF. Next W1-low work should use pack/render data-shape reduction or a true no-hot-C deadline/refill budget, not another fresh-window owner around `153..177`. |
| 2026-05-20 | `walkstuf1-high-local-lz-late-current` / `walkstuf1-high-local-lz-entry140-current` | Log-only; pack restored and PS1 rebuilt. | Test the unclosed late W1-high local-LZ sector-collapse family after early entries `55..57`, D4 splits, and static grouped reads had closed. The batch encoded entries `133`, `140..144`, and `192`; the split then tested the largest single member, entry `140` / source frame `248`. | Batch transform summary: `scratch/walkstuf1-high-local-lz-late-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-late-current/20260520-222736-762991/summary.json`. Single-entry transform summary: `scratch/walkstuf1-high-local-lz-entry140-current/summary.json`; focused artifact: `scratch/ps1-perf-iterate/walkstuf1-high-local-lz-entry140-current/20260520-222931-774068/summary.json`. The pack was restored from `scratch/walkstuf1-high-local-lz-late-current/WALKSTUF1.before.FG2`, and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The seven-entry batch saved seven modeled sectors but regressed W1-high from `1808/1472/1441` to `1855/1519/1432`, overrun `31 -> 87`, blocking `43 -> 64`, hidden refill `13 -> 16`, reads `41 -> 44`, and due `7 -> 11`. The split entry140 probe still failed: `1817/1481/1439`, overrun `42`, blocking/refill `48/15`, reads/due `42/8`. | Close late W1-high local-LZ sector-collapse on this baseline. Even a single late one-sector collapse shifts target/refill/visible cadence the wrong way; future W1-high data-shape work needs no-decode fixed-sector work reduction, generated deadline/refill ownership, or render/upload reduction rather than L4 sector shrinkage. |
| 2026-05-20 | `walkstuf1-low-prev-visible-cleanup-late-subset-fresh-canary` | `ps1: compact w1 low previous-visible cleanup` | Take a bigger data-shape swing after scalar generated-owner and local-LZ attempts hit refill/target phase traps: teach `compact-fgp3-restore-minus-current.py` a `--previous-visible` mode that clips cleanup spans to pixels drawn by the previous parseable frame, then apply only the W1-low late subset that proved timing-safe. | Broad previous-visible transforms were tested and rejected first: B2-high (`scratch/ps1-perf-iterate/building2-high-prev-visible-cleanup-current/20260520-223918-828674/summary.json`) improved scene VBlanks but regressed blocking/refill; VISITOR3 high/low (`scratch/ps1-perf-iterate/visitor3-high-prev-visible-cleanup-current/20260520-224504-861889/summary.json`, `scratch/ps1-perf-iterate/visitor3-low-prev-visible-cleanup-current/20260520-224640-871156/summary.json`) regressed loop/overrun; W1-high (`scratch/ps1-perf-iterate/walkstuf1-high-prev-visible-cleanup-current/20260520-224836-882274/summary.json`) regressed loop/target/refill; W1-low broad plus recovery rows reduced visible work but regressed refill. The promoted W1-low late subset used frames `194,196,198,200,202,206,208,210`, transform summary `scratch/walkstuf1-low-prev-visible-cleanup-late-subset-current/summary-rerun.json`, focused proof `scratch/ps1-perf-iterate/walkstuf1-low-prev-visible-cleanup-late-subset-current/20260520-225656-929899/summary.json`, refreshed baseline `scratch/ps1-perf-iterate/current-clean-five-yellow-refresh/20260520-230738-990373/summary.json`, and final canary `scratch/ps1-perf-iterate/walkstuf1-low-prev-visible-cleanup-late-subset-fresh-canary/20260520-231518-1033705/summary.json`. | Promoted as payload/restore work-headroom, not a VBlank speed win. The late subset changes eight W1-low entries and preserves file size/LBA/sectors while active low-pack payload drops `764658 -> 755808` (`8850` bytes), cleanup spans drop `2611 -> 173`, cleanup pixels drop `24946 -> 287`, and cleanup restore bytes drop `49892 -> 574`. The refreshed five-yellow canary stays exact-flat: B2-high `1357/1307`, VISITOR3 low `1071/1039`, VISITOR3 high `1082/1045`, W1-high `1472/1441`, and W1-low `1470/1446`; W1-low remains at overrun `24`, blocking/refill `32/4`, reads/due `24/4`. | Keep `--previous-visible` for targeted frame-subset cleanup compaction. Do not retry broad previous-visible pack sweeps on the current five-yellow baseline; they reduce bytes but alter cadence. Public/raw rollups after the refreshed CSV are `+0.2615%` / `99.7430%` public and about `-0.4554%` / `100.4734%` raw with `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total is about `17.14` public over-target points removed and `+12.64` public target-speed points gained. |
| 2026-05-21 | `building2-high-prev-visible-cleanup-promote-current` / `building2-high-prev-visible-cleanup-promote-five-yellow` | `ps1: compact building2 high previous-visible cleanup` | Reopen the broad B2-high previous-visible cleanup family after W1-low proved the tool path and the current baseline exposed large restore/upload work. The binary question was whether the loop/target/render win outweighed small CD-pressure growth under a speed-prioritized five-yellow gate. | Rewrote `generated/ps1/foreground/BUILDING2.FG2` in place with `compact-fgp3-restore-minus-current.py --previous-visible --copy-unparseable --preserve-offsets`; transform summary `scratch/building2-high-prev-visible-cleanup-promote-current/summary.json`. Focused proof artifact: `scratch/ps1-perf-iterate/building2-high-prev-visible-cleanup-promote-current/20260520-234653-1209845/summary.json`. A first five-yellow canary used an invalid WALKSTUF1 boot and was discarded; the canonical five-yellow canary is `scratch/ps1-perf-iterate/building2-high-prev-visible-cleanup-promote-five-yellow/20260521-000235-1299970/summary.json`. CSVs were regenerated from that canonical artifact. | Promoted as a B2-high speed/work-volume win. Active high-pack payload drops `663590 -> 574094`, cleanup restore bytes `439186 -> 80522`, cleanup spans `49420 -> 15664`, and cleanup pixels `219593 -> 40261` with fixed file size/LBA. B2-high improves scene/loop/target `1599/1357/1307 -> 1585/1343/1311`, overrun `50 -> 32`, upload bytes `26753280 -> 24341120`, and runtime restore bytes `438988 -> 116648`; small pressure tradeoffs are accepted (`blocking/refill 49/26 -> 51/18`, loop reads `40 -> 44`, due `5 -> 7`). VISITOR3 high/low and WALKSTUF1 high/low stayed exact-flat in the canonical canary. | Keep the B2-high previous-visible cleanup as the new baseline and treat broad previous-visible as scene-specific rather than globally closed. Public/raw rollups after the refreshed CSV are `+0.2505%` / `99.7533%` public and about `-0.4664%` / `100.4837%` raw with `121` green / `5` yellow / `0` orange / `0` red. Methodology total is about `17.15` public over-target points removed and `+12.65` public target-speed points gained. Next B2-high work should target frame-deadline data shape or render reduction around the remaining scheduler-owned-only `310..322`, `135..159`, `318..334`, and `310..326` candidates. |
| 2026-05-21 | `visitor3-high-prev-visible-minus62-promote-current` / `visitor3-prev-visible-minus62-promote-five-yellow` | `ps1: compact visitor3 high previous-visible cleanup` | Reopen VISITOR3-high previous-visible cleanup after the broad sweep had regressed cadence, using the B2/W1 lesson that same-sector cleanup compaction can be promoted while sector-changing entries need binary isolation. The binary question was whether all safe entries except the read-plan-regressing entry `62` could bank restore/upload headroom without moving the yellow timing row. | First promoted the exact-flat entry `144` proof, then the no-sector-change stack, then sector entry `114`, and finally all remaining sector-changing entries except `62`. Canonical transform summary: `scratch/visitor3-high-prev-visible-minus62-promote-current/summary.json`; focused proofs: `scratch/ps1-perf-iterate/visitor3-high-prev-visible-entry144-current/20260521-002620-1433808/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-prev-visible-nosector-all-current/20260521-003158-1466404/summary.json`, `scratch/ps1-perf-iterate/visitor3-high-prev-visible-sector-rest-minus62-current/20260521-003742-1500335/summary.json`; rejected `62` group artifact: `scratch/ps1-perf-iterate/visitor3-high-prev-visible-sector-rest-current/20260521-003602-1490348/summary.json`; five-yellow canary: `scratch/ps1-perf-iterate/visitor3-prev-visible-minus62-promote-five-yellow/20260521-004059-1519648/summary.json`. | Promoted as VISITOR3-high payload/restore/upload headroom, not a VBlank speed win. The accepted transform changes `106` entries, keeps file size/LBA fixed, and drops active high-pack payload `461631 -> 437785`, cleanup restore bytes `542088 -> 36092`, cleanup spans `12373 -> 5949`, cleanup pixels `271044 -> 18046`, runtime restore bytes `471382 -> 56312`, and upload bytes `18785280 -> 18038400`. VISITOR3 high stays `1372/1082/1045`, overrun `37`, and blocking `34`; VISITOR3 low, B2-high, and W1 high/low stay flat in the canary. The isolated entry `62` sector reduction remains rejected because it regresses high to `1373/1083/1044`, overrun `39`, blocking `35`. | Keep the VISITOR3-high minus-62 previous-visible cleanup as the new work-headroom baseline and keep entry `62` closed until a scheduler/deadline or pack-layout change removes the `36..42` read-plan phase trap. Public/raw rollups and bands remain effectively unchanged after the refreshed CSV: `+0.2505%` / `99.7533%` public, about `-0.4664%` / `100.4837%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.15` public over-target points removed and `+12.65` public target-speed points gained; next VISITOR3 work should target generated deadline ownership or low-tide terminal data shape rather than broad cleanup compaction. |
| 2026-05-21 | `visitor3-screen-clip-five-yellow-current` | `ps1: clip visitor3 high offscreen cleanup spans` | Take a screen-bounds pack-shape swing after previous-visible cleanup left a small amount of VISITOR3-high cleanup work fully outside the 640x480 output. The binary question was whether clipping offscreen FGP3/v4 cleanup/draw spans could bank work headroom without changing pack footprint, pack LBA, or the five-yellow timing baseline. | Added `scripts/compact-fgp3-clip-screen-spans.py`, a preserve-offset PAL4 span clipper for compact residual payloads. Broad probes were rejected first: B2-high `scratch/ps1-perf-iterate/b2high-screen-clip-current/20260521-011611-1720149/summary.json` reduced loop reads but regressed loop/target/blocking, W1-high `scratch/ps1-perf-iterate/w1-screen-clip-current/20260521-011756-1730365/summary.json` reduced reads but regressed overrun/blocking, and W1-low `scratch/ps1-perf-iterate/w1low-screen-clip-current/20260521-012123-1750223/summary.json` improved visible loop/blocking but failed target/refill. The accepted VISITOR3 transform summary is `scratch/screen-clip-probe-current/VISITOR3.promote-summary.json`; focused proof is `scratch/ps1-perf-iterate/visitor3-screen-clip-current/20260521-012444-1769174/summary.json`; canonical five-yellow canary is `scratch/ps1-perf-iterate/visitor3-screen-clip-five-yellow-current/20260521-012917-1795438/summary.json`. | Promoted as VISITOR3-high payload/cleanup headroom, not a VBlank speed win. The accepted transform changes entries `101` and `116`, keeps file size/LBA fixed, drops active high-pack payload `437785 -> 436469`, and removes `7393` offscreen cleanup pixels. The five-yellow canary stays exact-flat: B2-high `1343/1311`, VISITOR3 low `1071/1039`, VISITOR3 high `1082/1045`, W1-high `1472/1441`, and W1-low `1470/1446`; blocking/refill/read/due metrics are unchanged. | Keep the screen-clip tool for selective, proof-gated pack surgery only. Do not broad-sweep B2 or WALKSTUF1 with physical screen clipping on this baseline; those rows are phase-negative even when read counts improve. Public/raw rollups and bands remain unchanged: `+0.2505%` / `99.7533%` public, about `-0.4664%` / `100.4837%` raw, `121` green / `5` yellow / `0` orange / `0` red. Methodology speed total remains about `17.15` public over-target points removed and `+12.65` public target-speed points gained; the next big swing should preserve sector/data phase while reducing render work, or move to generated deadline/refill ownership for the remaining five yellow rows. |
| 2026-05-21 | `b2high-screen-clip-preserve-entry-current` / `b2high-screen-clip-preserve-entry-five-yellow-current` | `ps1: preserve building2 high clipped entry sizes` | Revisit the screen-clip swing after the physical B2/WALKSTUF1 probes proved that shrinking table sizes can be phase-negative even when bytes and reads fall. The binary question was whether preserving every changed entry's original `dataSize` could reduce compact draw/cleanup parser work without changing sector math, offsets, file size, LBA, or CD phase. | Added `--preserve-entry-sizes` to `scripts/compact-fgp3-clip-screen-spans.py`, requiring `--preserve-offsets`, so table sizes stay fixed while the clipped payload is zero-padded inside the original entry. Transform summary: `scratch/screen-clip-preserve-entry-current/BUILDING2.summary.json`. Focused proof: `scratch/ps1-perf-iterate/b2high-screen-clip-preserve-entry-current/20260521-014404-1879677/summary.json`. Canonical five-yellow canary: `scratch/ps1-perf-iterate/b2high-screen-clip-preserve-entry-five-yellow-current/20260521-014550-1889928/summary.json`. | Promoted as a B2-high speed/work-volume win with fixed physical layout. The transform changes `25` entries, keeps file size `1303332`, table `dataSize`, offsets, and LBA fixed, and reduces logical active payload `574094 -> 520974` while clipping `40166` draw pixels and `1982` cleanup pixels. B2-high improves `1343/1311 -> 1343/1312`, overrun `32 -> 31`, blocking `51 -> 50`, and hidden refill `18 -> 17`; VISITOR3 high/low and WALKSTUF1 high/low stay exact-flat in the canonical canary. | Keep preserve-entry-size clipping as the next screen-clip mode for phase-sensitive yellow rows. Physical screen clipping remains closed for B2/WALKSTUF1 on this baseline, but preserve-entry-size clipping should be tried next on W1 high/low before generated deadline/refill ownership. Public/raw rollups after the refreshed CSV are `+0.2498%` / `99.7539%` public and about `-0.4671%` / `100.4843%` raw with `121` green / `5` yellow / `0` orange / `0` red. Methodology total is about `17.15` public over-target points removed and `+12.65` public target-speed points gained. |
| 2026-05-21 | `w1high-screen-clip-preserve-entry-current` / `w1high-screen-clip-preserve-entry-rg372-current` / `w1high-screen-clip-preserve-entry-rg272-current` | Log-only; pack/source restored and PS1 rebuilt. | Try the B2 preserve-entry-size screen-clip mode on WALKSTUF1 high, where physical screen clipping had been phase-negative. The broad transform looked like a real render-work swing because it could reduce parser work without changing entry table sizes or LBA. | The broad transform summary is `scratch/screen-clip-preserve-entry-w1-current/WALKSTUF1.summary.json`; focused broad proof is `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-current/20260521-020201-1981246/summary.json`. Two paired read-group recovery probes were tested against the same clipped pack: `{372..388}` in `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-rg372-current/20260521-020440-1996687/summary.json`, then `{272..284}` in `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-rg272-current/20260521-020633-2007848/summary.json`. | Failed/no promotion. The broad clip changed `66` high-pack entries and reduced logical active payload `848331 -> 777722`, removing `76024` cleanup pixels and `71281` draw pixels with fixed physical layout. It improved W1-high scene/loop `1808/1472 -> 1806/1470`, but regressed blocking/refill `43/13 -> 44/14`. The `{372..388}` recovery saved reads but regressed to `1811/1475/1440`, overrun `35`, blocking/refill `44/14`. The `{272..284}` recovery kept the visible gain and overrun improvement (`31 -> 30`) but still failed blocking/refill `44/14`. | Close broad W1-high preserve-entry-size screen clipping on this baseline. The signal is useful but phase-negative: render-work savings are spending the hidden CD/refill budget. Future W1-high work should use narrower frame subsets, generated deadline/refill ownership, or setup-resident ownership that explicitly keeps blocking/refill at or below `43/13`. |
| 2026-05-21 | `w1low-screen-clip-preserve-entry-current` / `w1low-screen-clip-preserve-entry-five-yellow-current` | `ps1: preserve w1 low clipped entry sizes` | Apply the B2 preserve-entry-size screen-clip mode to WALKSTUF1 low after W1-high showed the broad high-tide version was phase-negative. The binary question was whether low-tide's larger refill margin could bank cleanup/draw parser work and hidden-refill headroom while keeping speed exact-flat. | Transform summary: `scratch/screen-clip-preserve-entry-w1low-current/WALK1LOW.summary.json`. Focused proof: `scratch/ps1-perf-iterate/w1low-screen-clip-preserve-entry-current/20260521-021011-2028691/summary.json`. Canonical five-yellow canary: `scratch/ps1-perf-iterate/w1low-screen-clip-preserve-entry-five-yellow-current/20260521-021156-2038863/summary.json`. CSVs were regenerated from the canonical artifact with stats version `w1low-screen-clip-preserve-entry`. | Promoted as W1-low work/refill headroom, not a VBlank speed win. The transform changes `63` entries, keeps file size `1535263`, table `dataSize`, offsets, and LBA fixed, and reduces logical active payload `755808 -> 712808` while clipping `73798` cleanup pixels and `39618` draw pixels. W1-low stays `1470/1446`, overrun `24`, blocking `32`, reads/time `24/146`, and due `4`, while hidden refill improves `4 -> 3`; B2-high, VISITOR3 high/low, and W1-high stay exact-flat. | Keep W1-low preserve-entry-size clipping as the new work-headroom baseline. Public/raw rollups and bands remain `+0.2498%` / `99.7539%` public, about `-0.4671%` / `100.4843%` raw, and `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.15` public over-target points removed and `+12.65` public target-speed points gained; next work should use the refill headroom to revisit W1-low green-speed candidates or move to narrower W1-high/VISITOR3 generated deadline ownership. |
| 2026-05-21 | `v3low-screen-clip-preserve-entry-current` / `v3low-screen-clip-preserve-entry-five-yellow-current` | `ps1: preserve visitor3 low clipped entry sizes` | Take the next phase-safe screen-clip swing on the largest remaining yellow gap, VISITOR3 low. The binary question was whether low-tide VISITOR3 had any screen-invisible compact spans left that could bank parser/restore work without changing entry sizes, offsets, file size, LBA, or CD phase. | Transform summary: `scratch/screen-clip-preserve-entry-v3low-current/VIST3LOW.summary.json`. Focused proof: `scratch/ps1-perf-iterate/v3low-screen-clip-preserve-entry-current/20260521-022531-2115981/summary.json`. Canonical five-yellow canary: `scratch/ps1-perf-iterate/v3low-screen-clip-preserve-entry-five-yellow-current/20260521-022705-2125132/summary.json`. CSVs were regenerated from the canonical artifact with stats version `v3low-screen-clip-preserve-entry`. | Promoted as VISITOR3-low work headroom, not a VBlank speed win. The transform changes `10` entries, keeps file size `1555450`, table `dataSize`, offsets, and LBA fixed, and reduces logical active payload `425729 -> 423647` while clipping `38366` cleanup pixels and `2139` draw pixels. VISITOR3-low stays `1071/1039`, overrun `32`, blocking `80`, hidden refill `0`, and due `14`; B2-high, VISITOR3 high, and W1 high/low stay exact-flat. | Keep VISITOR3-low preserve-entry-size clipping as the new work-headroom baseline. Public/raw rollups and bands remain `+0.2498%` / `99.7539%` public, about `-0.4671%` / `100.4843%` raw, and `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.15` public over-target points removed and `+12.65` public target-speed points gained; next VISITOR3-low work needs deadline/read ownership or terminal data-shape changes that move the `1071/1039` row toward green. |
| 2026-05-21 | `v3low-rg86-98-current` / `v3low-rg92-108-current` / `v3low-rg248-264-current` | Log-only; source restored and PS1 rebuilt. | Follow the VISITOR3-low preserve-entry-size screen-clip promotion with direct grouped-read probes from the refreshed planner. `{86..98}` and `{92..108}` were the low-visible-risk scheduler-owned candidates, and `{248..264}` was the distinct high-tight-cluster candidate with three modeled saved reads. | Temporarily added a VISITOR3 low-tide read-group table in `src/foreground_pilot.c` for each candidate and rebuilt before each focused gate. Artifacts: `{86..98}` at `scratch/ps1-perf-iterate/v3low-rg86-98-current/20260521-023807-2188632/summary.json`, `{92..108}` at `scratch/ps1-perf-iterate/v3low-rg92-108-current/20260521-024103-2205546/summary.json`, and `{248..264}` at `scratch/ps1-perf-iterate/v3low-rg248-264-current/20260521-024300-2217007/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. `{86..98}` saved one read but regressed VISITOR3-low to `1346/1073/1039`, overrun `34`, blocking `84`, and hidden refill `1`. `{92..108}` held loop `1071` and saved one read but regressed scene/overrun/blocking `1343/32/80 -> 1344/33/81`. `{248..264}` also regressed scene/overrun/blocking to `1344/33/81` with no loop-speed win and reads unchanged at `18`. | Close VISITOR3-low hand-authored read groups on the current preserve-entry-size baseline. The scheduler model is finding real CD-shape signals, but static C rows move the cost into visible blocking/overrun. Next VISITOR3-low work should be no-hot-C generated deadline/refill ownership or custom terminal data shape, not more scalar read-group rows. |
| 2026-05-21 | `building2-high-rg310-322-replace315-current` / `building2-high-rg249-265-current` / `building2-high-rg318-334-current` | Log-only; source restored and PS1 rebuilt. | Close the remaining direct B2-high read-table probes after the preserve-entry-size screen-clip promotion. The swap `{315..327}` -> `{310..322}` tested whether the accepted tail row should fire earlier; `{249..265}` and `{318..334}` tested the current planner's low-visible-risk additive rows without pack or executable-bucket movement. | Temporarily edited `kBuilding2HighReadGroups12[]`, rebuilt before each focused B2-high proof, then restored the accepted table. Artifacts: replacement `{310..322}` at `scratch/ps1-perf-iterate/building2-high-rg310-322-replace315-current/20260521-024703-2240496/summary.json`, additive `{249..265}` at `scratch/ps1-perf-iterate/building2-high-rg249-265-current/20260521-024913-2253177/summary.json`, and additive `{318..334}` at `scratch/ps1-perf-iterate/building2-high-rg318-334-current/20260521-025120-2265625/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. The `{310..322}` replacement kept scene/loop flat but gave back accepted tail pressure wins: overrun/blocking/refill `31/50/17 -> 32/51/18`. `{249..265}` and `{318..334}` were safe but inert: B2-high stayed `1585/1343/1312`, overrun `31`, blocking/refill `50/17`, loop reads `44`, due `7`, fixed LBA, and the same `233472` byte PS-EXE bucket. | Close direct B2-high read-table replacement/additive rows for `310..322`, `249..265`, and `318..334` on this baseline. The remaining B2-high gap needs generated deadline/refill ownership that does not grow hot C code, or pack/render work reduction that creates real slack before another read row fires. |
| 2026-05-21 | `w1high-screen-clip-preserve-entry-204-211-current` / `w1high-screen-clip-preserve-entry-204-211-five-yellow-current` | `ps1: clip w1 high late cleanup spans` | Split the broad W1-high preserve-entry-size screen-clip miss into phase-safe subsets. The broad pass had a visible-loop signal but failed blocking/refill, so the binary question was whether a cleanup-only late subset could bank render/parser work without spending the hidden CD budget. | First rejected `128..147` at `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-128-147-current/20260521-025611-2293454/summary.json` because it was exact-loop but regressed blocking `43 -> 44`. Then rejected `0..67` at `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-0-67-current/20260521-025802-2304376/summary.json` because it regressed W1-high to `1810/1474/1440`, overrun `34`, blocking/refill `46/15`. The promoted subset transform summary is `scratch/w1high-screen-clip-subsets-current/summary-204-211.json`; focused proof is `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-204-211-current/20260521-025955-2315455/summary.json`; five-yellow canary is `scratch/ps1-perf-iterate/w1high-screen-clip-preserve-entry-204-211-five-yellow-current/20260521-030142-2325843/summary.json`. | Promoted as W1-high work headroom, not a VBlank speed win. The accepted subset clips cleanup-only entries `204..211`, keeps file size `1535263`, table `dataSize`, offsets, LBA, and the `233472` byte PS-EXE bucket fixed, drops subset logical active payload `20365 -> 19645`, and removes `12368` cleanup pixels. B2-high, VISITOR3 high/low, and W1 high/low stay exact-flat in the five-yellow canary; W1-high remains `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, reads/due `41/7`. | Keep W1-high cleanup-only `204..211` clipping as the new work-headroom baseline. Do not retry broad W1-high preserve-entry-size clipping or the `0..67` / `128..147` subsets on this baseline; the next W1-high work should continue with narrow cleanup-only slices, generated deadline/refill ownership, or pack/render reductions that keep blocking/refill at or below `43/13`. Public/raw rollups and bands remain `+0.2498%` / `99.7539%` public, about `-0.4671%` / `100.4843%` raw, and `121` green / `5` yellow / `0` orange / `0` red. Methodology total remains about `17.15` public over-target points removed and `+12.65` public target-speed points gained. |
| 2026-05-21 | `walkstuf1-high-owner365-389-fresh-current` / `walkstuf1-high-owner84-108-fresh-current` | Log-only; source restored and PS1 rebuilt. | Start the post-screen-clip generated-owner lane on W1-high. Static late and early read pockets were closed because they saved reads by spending blocking/refill, so this tested whether frame/slack-gated fresh-window ownership could fire only when the owning frames had enough held slack. | First added a W1-high fresh owner for frames `180..188` / sectors `365..389` with slack `>=5`. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-owner365-389-fresh-current/20260521-031835-2421477/summary.json`. Then layered the stronger modeled pocket frames `43..56` / sectors `84..108` under the same slack gate. Artifact: `scratch/ps1-perf-iterate/walkstuf1-high-owner84-108-fresh-current/20260521-032033-2432928/summary.json`. Source was restored and `./scripts/build-ps1.sh` passed afterward. | Failed/no promotion. Both focused gates stayed exact-flat at W1-high `1808/1472/1441`, overrun `31`, blocking/refill `43/13`, loop reads `41`, due `7`, fixed pack LBA `24891`, and PS-EXE bucket `233472`; `--require-improvement` correctly rejected them. The late-only probe grew `fgRuntimeFillWindowForEntry` by `136` bytes, and the combined early+late probe grew it by `260` bytes. | Close simple W1-high fresh-window owner hooks for `365..389` and `84..108` on this baseline. The next generated-owner attempt needs a hit counter plus no-hot-C metadata or a deeper append/deadline owner, because these C-side fresh hooks either do not hit the useful seam or do not alter read shape while still spending hot code. |
| 2026-05-21 | `building2-high-trimdraw67-72-current` / `building2-high-trimdraw67-72-five-yellow-current` | `ps1: trim b2 high early draw tails` | Reopen no-decode draw-tail trimming after the preserve-entry-size and previous-visible cleanup passes created a new B2-high baseline. The binary question was whether an early subset, unlike the previously rejected `89..91` sector-collapse family, could remove parser/read work without disturbing VISITOR3 or WALKSTUF1 cadence. | Ran `compact-fgp3-trim-draw-tails.py` in preserve-offset/pad-to-input-size mode on `BUILDING2.FG2` entries `67..72`; the transform changed entries `67`, `69`, `70`, `71`, and `72`. Transform summary: `scratch/b2high-trimdraw-67-72-current/summary.json`. Focused proof: `scratch/ps1-perf-iterate/building2-high-trimdraw67-72-current/20260521-032510-2459142/summary.json`. Five-yellow canary: `scratch/ps1-perf-iterate/building2-high-trimdraw67-72-five-yellow-current/20260521-032804-2475674/summary.json`. | Promoted as a B2-high speed/work-volume win. File size stays `1303332`, pack LBA stays `6189`, and the PS-EXE bucket stays `233472`; active payload drops `574094 -> 548293`. B2-high improves scene/loop/target `1585/1343/1312 -> 1583/1341/1313`, overrun `31 -> 28`, blocking/refill `50/17 -> 47/14`, read time `195 -> 194`, and target speed `97.692% -> 97.912%`. VISITOR3 high/low and WALKSTUF1 high/low stay exact-flat in the five-yellow canary. | Keep the early B2 trim-draw-tail subset as the new baseline. Public/raw rollups after the refreshed CSV are `+0.2480%` / `99.7557%` public and about `-0.4689%` / `100.4860%` raw with `121` green / `5` yellow / `0` orange / `0` red. Methodology total is about `17.15` public over-target points removed and `+12.66` public target-speed points gained. Next B2 work should target generated deadline/refill ownership or the remaining top candidates `135..159`, `310..326`, `82..98`, and `90..96`, not the older closed `89..91` draw-tail sector-collapse path. |

## Harness Notes

- `regtest_exit=137` means the waited process was killed by `SIGKILL`, but it is not always a PS1/emulator failure. If `JCPERF2` is complete, the case gate is clean, and DuckStation logs `Exiting with success`, the perf harness treats it as a post-success wrapper failure and continues to aggregate the run.
- `regtest_exit=137` with missing `JCPERF2` remains a hard failure. Those runs are still recorded as structural failures because they do not prove correctness or speed.
- As of the harness log fix after the dirty-row `memset` miss, `scratch/ps1-perf-iterate/experiments.jsonl` is appended after baseline comparison, so `gate_pass` includes `--baseline` and `--require-improvement` failures. Older JSONL rows were regtest/case-gate only; use their run `summary.json` for final baseline-gate status.
- As of v407, `--require-improvement` also fails when the supplied baseline
  summary has no matching case label. A missing match can still be warning-only
  for exploratory baseline comparisons, but not for promotion-style gates.

## Promotion Rule

An experiment is promotable when the headless perf gate passes, correctness
tripwires remain zero, and either at least one key speed metric improves without
a material regression in `loop_vb`, `blocking_vb`, `prefetch_overrun_vb`, or
scene identity, or all key timing metrics stay flat while a material
work-volume/code-size metric improves. Work-reduction promotions must be labeled
as such and must not be counted as VBlank speed wins. Failed experiments still
get recorded here so later changes can make them eligible for retry.
