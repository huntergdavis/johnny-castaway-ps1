/*
 * src/generated/pack_header_metrics.c
 *
 * **STUB** — replaced by generated content from
 * scripts/generate-pack-metrics.py. Provides the symbols declared
 * in pack_header_metrics.h with an empty table so the link succeeds.
 *
 * DO NOT edit by hand.
 */

#include "pack_header_metrics.h"

const struct PackHeaderMetric kPackHeaderMetrics[] = {
    /* Empty stub. Generator will fill this with one entry per
     * (base, holiday-variant) pack pair. */
    { 0 }
};

/* sizeof divide-by-element minus the stub-entry; size 0 means
 * memVerify* loops don't iterate. */
const size_t kPackHeaderMetricsCount = 0;
